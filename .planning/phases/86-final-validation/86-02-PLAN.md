---
phase: 86-final-validation
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements: [VAL-01, VAL-02]
must_haves:
  truths:
    - "CI pipeline completed green across all jobs: Python tests, Angular tests, E2E (amd64 + arm64), lint (ruff + eslint)"
    - "Python coverage percentage before and after the audit is documented in ROADMAP.md milestone notes"
    - "Angular coverage baselines are documented in ROADMAP.md milestone notes"
    - "The 2 pre-existing arm64 Unicode sort failures are documented as a caveat in the milestone notes"
    - "Coverage does not drop below 84% (fail_under enforced in CI)"
    - "The v1.1.2 milestone is marked as SHIPPED in ROADMAP.md"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "v1.1.2 milestone completion with coverage baselines"
      contains: "SHIPPED"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "VAL-01 and VAL-02 satisfied by milestone documentation"
      pattern: "Coverage Baseline"
---

<objective>
Verify the CI pipeline completed green on GitHub Actions, capture the Python coverage number from CI logs, and update ROADMAP.md with coverage baselines and milestone completion.

Purpose: This satisfies VAL-01 (full CI green with arm64 caveat per D-07) and VAL-02 (Python coverage documented before/after, must not drop below 84% per D-05). The ROADMAP.md update is the deliverable per D-03.

Output: Updated ROADMAP.md with v1.1.2 milestone marked SHIPPED, coverage tables, and arm64 caveat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-final-validation/86-CONTEXT.md
@.planning/phases/86-final-validation/86-RESEARCH.md
@.planning/phases/86-final-validation/86-01-SUMMARY.md

<interfaces>
<!-- ROADMAP.md sections that must be updated -->

Lines to update in .planning/ROADMAP.md:

1. Line 32 — milestone list entry:
   BEFORE: `- v1.1.2 Test Suite Audit - Phases 83-86 (in progress)`
   AFTER:  `- v1.1.2 Test Suite Audit - Phases 83-86 (shipped YYYY-MM-DD)`

2. Line 258 — milestone section header:
   BEFORE: `### v1.1.2 Test Suite Audit (Phases 83-86) - IN PROGRESS`
   AFTER:  `### v1.1.2 Test Suite Audit (Phases 83-86) - SHIPPED YYYY-MM-DD`

3. Line 265 — Phase 86 checkbox:
   BEFORE: `- [ ] **Phase 86: Final Validation** - Full CI green and coverage baseline documented`
   AFTER:  `- [x] **Phase 86: Final Validation** - Full CI green and coverage baseline documented (completed YYYY-MM-DD)`

4. After the Phase 86 checkbox — INSERT new coverage baseline section (per D-03, D-04):
   ```markdown
   **Coverage Baseline (Python):**
   | Metric | Before Audit (Phase 83) | After Audit (Phase 86) |
   |--------|------------------------|------------------------|
   | Total coverage | 85.05% | XX.XX% |
   | fail_under threshold | 84% | 84% |
   | Safety margin | 1.05pp | X.XXpp |

   **Coverage Baseline (Angular) -- Phase 84:**
   | Metric | Value |
   |--------|-------|
   | Statements | 83.34% (1682/2018) |
   | Branches | 69.01% (461/668) |
   | Functions | 79.73% (421/528) |
   | Lines | 84.21% (1622/1926) |

   **Known Caveats:**
   - 2 arm64 E2E specs fail due to pre-existing locale-dependent Unicode sort order (glibc amd64 vs arm64 difference). Not introduced by the audit. Tracked for future resolution.
   ```

5. Lines 317-319 — Phase 86 detail plans count:
   BEFORE: `**Plans:** 0/TBD`
   AFTER:  `**Plans:** 2/2 plans complete`

6. Lines 318-319 — Phase 86 plan list:
   BEFORE: `- [ ] 86-01-PLAN.md — TBD (run '/gsd-plan-phase 86')`
   AFTER:
   ```
   - [x] 86-01-PLAN.md -- Fix autoqueue harness, create arm64 todo, push to trigger CI
   - [x] 86-02-PLAN.md -- Verify CI green, document coverage baselines, complete milestone
   ```

7. Line 354 — progress table:
   BEFORE: `| 86. Final Validation | v1.1.2 | 0/TBD | Not started | - |`
   AFTER:  `| 86. Final Validation | v1.1.2 | 2/2 | Complete    | YYYY-MM-DD |`

Replace all YYYY-MM-DD with the actual date and XX.XX% with the actual coverage number from CI logs.

Known values:
- Python before: 85.05% (Phase 83)
- Angular: 83.34%/69.01%/79.73%/84.21% (Phase 84)
- fail_under: 84% (pyproject.toml)
- Python after: READ FROM CI LOGS (unittests-python job output)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify CI pipeline green on GitHub Actions</name>
  <what-built>Plan 01 pushed the autoqueue harness fix and planning artifacts to main, triggering the full CI pipeline on GitHub Actions (Python tests, Angular tests, E2E amd64 + arm64, lint, Docker build).</what-built>
  <how-to-verify>
    1. Open the GitHub Actions page for the seedsyncarr repository
    2. Find the CI run triggered by the Plan 01 push (commit message contains "enable autoqueue in harness")
    3. Verify the following jobs completed:
       - `unittests-python`: GREEN
       - `unittests-angular`: GREEN
       - `lint-python`: GREEN
       - `lint-angular`: GREEN
       - `build-docker-image`: GREEN
       - `e2etests-docker-image` (amd64): GREEN
       - `e2etests-docker-image` (arm64): Check result -- per D-07, if exactly 2 failures in dashboard.page.spec.ts (Unicode sort), this is pre-existing and acceptable. If more than 2 failures or failures in different specs, investigate.
    4. Open the `unittests-python` job log and find the coverage output line:
       `Required test coverage of 84.0% reached. Total coverage: XX.XX%`
    5. Record the XX.XX% number -- this is the VAL-02 "after" coverage value

    Expected:
    - All jobs GREEN (arm64 may show 2 pre-existing sort failures per D-06/D-07)
    - Python coverage >= 84.00% (expected ~85.05%, same as Phase 83 since no code changed)

    If the autoqueue.page.spec.ts tests FAIL (timeout on .pattern-section):
    - The harness fix did not work. Report the error output.

    If Python coverage drops below 84%:
    - This is a VAL-02 failure. Report the actual coverage number.
  </how-to-verify>
  <resume-signal>Provide: (1) "approved" or describe issues, (2) the Python coverage percentage from CI logs (e.g., "85.05%"), (3) arm64 E2E result (e.g., "35 passed, 2 failed" or "37 passed")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md with coverage baselines and milestone completion</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state -- need exact line numbers for all edit locations)
    - .planning/phases/86-final-validation/86-CONTEXT.md (D-03, D-04, D-07 decision text for exact wording)
  </read_first>
  <action>
Update `.planning/ROADMAP.md` with the actual coverage number provided by the human in Task 1. Apply ALL of the following edits (use today's date for YYYY-MM-DD):

**Edit 1 — Line 32 (milestone list):**
Change `(in progress)` to `(shipped YYYY-MM-DD)`.

**Edit 2 — Line 258 (milestone section header):**
Change `- IN PROGRESS` to `- SHIPPED YYYY-MM-DD`.

**Edit 3 — Line 265 (Phase 86 checkbox):**
Change `- [ ] **Phase 86: Final Validation** - Full CI green and coverage baseline documented` to `- [x] **Phase 86: Final Validation** - Full CI green and coverage baseline documented (completed YYYY-MM-DD)`.

**Edit 4 — After line 265 (INSERT new block):**
Insert the coverage baseline tables after the Phase 86 checkbox line. Use the ACTUAL Python coverage % from the CI logs (provided in Task 1 checkpoint). Calculate the safety margin as (actual% - 84.00) formatted to 2 decimal places followed by "pp".

The Angular baselines are fixed values from Phase 84 (per D-04):
- Statements: 83.34% (1682/2018)
- Branches: 69.01% (461/668)
- Functions: 79.73% (421/528)
- Lines: 84.21% (1622/1926)

The Known Caveats section documents the arm64 sort failures per D-07.

**Edit 5 — Line ~317 (Phase 86 plans count):**
Change `**Plans:** 0/TBD` to `**Plans:** 2/2 plans complete`.

**Edit 6 — Lines ~318-319 (Phase 86 plan list):**
Replace the single TBD line with:
```
Plans:
- [x] 86-01-PLAN.md -- Fix autoqueue harness, create arm64 todo, push to trigger CI
- [x] 86-02-PLAN.md -- Verify CI green, document coverage baselines, complete milestone
```

**Edit 7 — Line ~354 (progress table row):**
Change `| 86. Final Validation | v1.1.2 | 0/TBD | Not started | - |` to `| 86. Final Validation | v1.1.2 | 2/2 | Complete    | YYYY-MM-DD |`.

**Edit 8 — Wrap the v1.1.2 milestone section in a details block (matching the pattern of all prior shipped milestones):**
The section starting at line 258 (`### v1.1.2 Test Suite Audit...`) through the end of the Phase 86 detail section should be wrapped in `<details><summary>...</summary>...</details>` tags, following the same format as the v1.1.1 section above it.

CRITICAL: Replace ALL `YYYY-MM-DD` placeholders with today's actual date. Replace `XX.XX%` with the actual Python coverage number from CI logs. Do NOT leave any placeholder values.
  </action>
  <verify>
    <automated>grep -q "SHIPPED" .planning/ROADMAP.md && grep -q "Coverage Baseline" .planning/ROADMAP.md && grep -q "85.05%" .planning/ROADMAP.md && grep -q "83.34%" .planning/ROADMAP.md && grep -q "arm64" .planning/ROADMAP.md && grep -c "YYYY-MM-DD\|XX\.XX%\|TBD" .planning/ROADMAP.md | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md line 32 contains "shipped" (not "in progress")
    - ROADMAP.md contains "### v1.1.2 Test Suite Audit" with "SHIPPED" and an actual date
    - ROADMAP.md contains "Coverage Baseline (Python):" table with "85.05%" in the Before column and an actual percentage in the After column
    - ROADMAP.md contains "Coverage Baseline (Angular)" table with "83.34%", "69.01%", "79.73%", "84.21%"
    - ROADMAP.md contains "Known Caveats:" section mentioning "arm64" and "pre-existing"
    - ROADMAP.md Phase 86 shows "[x]" (checked) with completion date
    - ROADMAP.md Phase 86 shows "Plans: 2/2 plans complete"
    - ROADMAP.md progress table shows "86. Final Validation | v1.1.2 | 2/2 | Complete" with a date
    - Zero occurrences of "YYYY-MM-DD", "XX.XX%", or "TBD" remain in the v1.1.2 section
    - Zero occurrences of "0/TBD" remain in the Phase 86 entries
  </acceptance_criteria>
  <done>ROADMAP.md fully updated with v1.1.2 milestone marked SHIPPED, Python and Angular coverage baselines documented, arm64 caveat noted, Phase 86 marked complete with 2/2 plans</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI logs -> ROADMAP.md | Coverage number read from CI output and transcribed to documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | T (Tampering) | ROADMAP.md | accept | Documentation-only file in planning directory. No production impact. Coverage number is self-verifiable by re-running CI. |
| T-86-04 | R (Repudiation) | Coverage number | accept | The CI run log is the authoritative source. ROADMAP.md references the run. Anyone can verify by checking the GitHub Actions history. |
</threat_model>

<verification>
After ROADMAP.md update:
1. `grep "SHIPPED" .planning/ROADMAP.md` shows the v1.1.2 milestone as shipped
2. `grep "Coverage Baseline" .planning/ROADMAP.md` shows both Python and Angular sections
3. `grep "YYYY-MM-DD\|XX\.XX%\|TBD" .planning/ROADMAP.md` returns zero matches (no placeholders)
4. `grep "arm64" .planning/ROADMAP.md` shows the caveat documentation
5. The v1.1.2 section is wrapped in a `<details>` block matching prior milestones
</verification>

<success_criteria>
- VAL-01: CI pipeline green confirmed by human checkpoint (arm64 caveat documented per D-07)
- VAL-02: Python coverage before (85.05%) and after (from CI logs) documented in ROADMAP.md, both >= 84%
- D-03: Coverage documented in ROADMAP.md milestone section (no standalone file)
- D-04: Angular baselines included alongside Python
- D-07: arm64 sort failures documented as pre-existing caveat
- v1.1.2 milestone marked SHIPPED with completion date
</success_criteria>

<output>
After completion, create `.planning/phases/86-final-validation/86-02-SUMMARY.md`
</output>
