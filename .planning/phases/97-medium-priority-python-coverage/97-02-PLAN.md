---
phase: 97-medium-priority-python-coverage
plan: 02
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/python/tests/unittests/test_common/test_multiprocessing_logger.py
  - src/python/common/multiprocessing_logger.py
autonomous: true
requirements: [COVMED-01]
must_haves:
  truths:
    - "A test proves a handler raising during handle(record) makes the listener stash exc_info, set shutdown, and exit"
    - "A test proves propagate_exception() re-raises the captured exception, and a second call is a no-op"
    - "A test proves an inner-loop queue.Empty does NOT terminate the listener (more records still received afterward)"
    - "A test proves clean shutdown via stop() joins without raising and propagate_exception() is then a no-op"
    - "D-05: tests are additive; any bug surfaced (e.g. outer except swallowing) is fixed inline only if ≤10 lines + no public-API/behavior change, else deferred to v1.4.0 with a STATE.md entry"
  artifacts:
    - path: "src/python/tests/unittests/test_common/test_multiprocessing_logger.py"
      provides: "Full-path coverage of multiprocessing_logger.py:67-86 listener lifecycle"
      contains: "propagate_exception"
  key_links:
    - from: "test_multiprocessing_logger.py"
      to: "common.MultiprocessingLogger public API (start/stop/propagate_exception)"
      via: "real listener thread driven and joined via stop()"
      pattern: "propagate_exception"
---

<objective>
Add full-path coverage for the `MultiprocessingLogger` listener-thread shutdown
semantics (COVMED-01), covering all four documented branches of the `__listener` loop
at `src/python/common/multiprocessing_logger.py:67-86`.

Purpose: the listener thread, its exception-capture/propagation, and its empty-queue
resilience are entirely untested today. These are the lines that decide whether a
logging failure in a worker process silently dies or surfaces to the main process.
Output: extended `TestMultiprocessingLogger` with four new tests, each timeout-guarded.

Trivial-fix watch (D-05): if the outer `except Exception` at line 78 is found to swallow
an error that `propagate_exception()` should surface, narrow the catch / ensure
capture-before-log — ONLY if ≤10 net non-blank/non-comment lines, no public-API change,
no observable-behavior change. Borderline → defer to v1.4.0 with a STATE.md deferred-items
entry referencing the red test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<interfaces>
From src/python/common/multiprocessing_logger.py (public API the tests drive — do NOT reach into name-mangled privates):
- `MultiprocessingLogger(base_logger: logging.Logger)` — `self.logger = base_logger.getChild("MPLogger")`
- `start()` — starts the listener thread
- `stop()` — sets `__listener_shutdown` event and `join()`s the thread
- `propagate_exception()` — if exc_info was captured, raises `exc_info[1].with_traceback(...)`; clears it to None first (so a 2nd call is a no-op)
- `get_process_safe_logger() -> logging.Logger` — returns a queue-backed logger for child processes

Listener loop (lines 67-86) branches to cover:
- line 75: `self.logger.getChild(record.name).handle(record)` — handler runs here
- lines 76-77: inner `except queue.Empty: break` — exits inner drain loop ONLY
- lines 78-83: outer `except Exception` — stashes `sys.exc_info()`, logs via `self.logger.exception(...)`, sets `__listener_shutdown`, breaks
- line 70: `while not self.__listener_shutdown.is_set()` — outer loop continues across empty-queue cycles
- `__LISTENER_SLEEP_INTERVAL_IN_SECS = 0.1` (line 20)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover handler-raise + propagate_exception re-raise and no-op</name>
  <files>src/python/tests/unittests/test_common/test_multiprocessing_logger.py</files>
  <read_first>
    - src/python/tests/unittests/test_common/test_multiprocessing_logger.py (existing TestMultiprocessingLogger class — reuse setUp/tearDown, the @pytest.mark.timeout(5) decorator form, and the start/join/sleep(0.2)/stop drive ordering)
    - src/python/common/multiprocessing_logger.py (lines 67-86, plus propagate_exception at 38-46)
  </read_first>
  <behavior>
    - test_listener_captures_handler_exception_and_shuts_down: when a record is handled by a handler whose emit/handle raises, the listener stashes the exception and sets shutdown (proven via propagate_exception raising).
    - test_propagate_exception_reraises_captured: after a handler-raise, propagate_exception() re-raises the SAME exception type and message that the failing handler raised.
    - test_propagate_exception_second_call_is_noop: a second propagate_exception() call after the first raise does nothing (exc_info cleared to None at line 45).
  </behavior>
  <action>
    Extend the existing `TestMultiprocessingLogger` class (do NOT create a new file). Add three timeout-guarded tests using `@pytest.mark.timeout(5)`. To trigger the handler-raise branch (line 75 → outer except at 78-83): attach a custom `logging.Handler` subclass whose `emit` (or `handle`) raises a sentinel exception (e.g. a uniquely-named RuntimeError with a known message) onto a logger in the `self.logger.getChild(record.name)` child chain — note `handle` is invoked on `self.logger.getChild(record.name)`, so the failing handler must live on that child logger. Construct the `MultiprocessingLogger`, `start()` it, push exactly one record whose name routes to the failing handler (use a child process via `get_process_safe_logger()` matching the existing analog, OR put a record directly onto the queue through the process-safe logger), then `time.sleep(0.2)` to let the listener drain, then `stop()`. Assert the listener shut down by asserting `with self.assertRaises(<SentinelError>)`: `mp_logger.propagate_exception()` re-raises it, and assert the message matches. In a separate test, after the first `propagate_exception()` raises (caught), assert a second `propagate_exception()` call returns without raising (no-op). Assert ONLY through the public API — do NOT read `_MultiprocessingLogger__listener_exc_info`. If, while writing these RED-first, you discover the outer `except Exception` swallows the error such that `propagate_exception` does NOT surface it, apply the D-05 trivial-fix posture: in-scope ONLY if the fix is ≤10 net lines with no public-API/behavior change, else defer to v1.4.0 with a STATE.md entry referencing the failing test name.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/unittests/test_common/test_multiprocessing_logger.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Three new tests exist in TestMultiprocessingLogger, each decorated `@pytest.mark.timeout(5)`.
    - The handler-raise test asserts `propagate_exception()` raises the sentinel exception type+message.
    - The no-op test asserts the second `propagate_exception()` call does not raise.
    - No test reads any `_MultiprocessingLogger__` name-mangled attribute (grep: `grep -c '_MultiprocessingLogger__' tests/unittests/test_common/test_multiprocessing_logger.py` returns 0).
    - All tests in the file pass (pytest exit 0).
  </acceptance_criteria>
  <done>Handler-raise capture, re-raise, and no-op-after-clear branches covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover empty-queue resilience and clean shutdown</name>
  <files>src/python/tests/unittests/test_common/test_multiprocessing_logger.py</files>
  <read_first>
    - src/python/tests/unittests/test_common/test_multiprocessing_logger.py (the tests added in Task 1 + existing analogs test_main_logger_receives_records lines 26-53)
    - src/python/common/multiprocessing_logger.py (lines 70-86; note inner break at 77 and __LISTENER_SLEEP_INTERVAL_IN_SECS = 0.1 at line 20)
  </read_first>
  <behavior>
    - test_inner_queue_empty_does_not_terminate_listener: after the queue drains (inner queue.Empty break at line 77), the listener survives the empty cycle and still receives records sent afterward.
    - test_clean_shutdown_joins_without_error: stop() after a clean run joins the thread without raising, and propagate_exception() is a no-op (no exception was captured).
  </behavior>
  <action>
    Add two more `@pytest.mark.timeout(5)` tests to `TestMultiprocessingLogger`. For the empty-queue resilience test: send one or more records, then `time.sleep` past `__LISTENER_SLEEP_INTERVAL_IN_SECS` (0.1s — sleep e.g. 0.2-0.3s) so the inner loop hits `queue.Empty` and breaks the inner loop while the outer `while not __listener_shutdown` continues; THEN send MORE records, sleep again to let them drain, and assert via `testfixtures.LogCapture` (matching the existing `LogCapture("TestMultiprocessingLogger.MPLogger.process_1")` pattern) that the later records were still received — proving the listener survived an empty-queue cycle. For the clean-shutdown test: run a normal send/drain cycle, call `stop()`, assert it returns without raising (the join completes), and assert `mp_logger.propagate_exception()` is a no-op (returns None / does not raise) because no handler exception occurred. Reuse the existing setUp/tearDown and the child-process or process-safe-logger drive pattern from the analogs. Do NOT rely on the global `timeout = 60`; use the explicit per-test `@pytest.mark.timeout(5)` to match the suite.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/unittests/test_common/test_multiprocessing_logger.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Two new tests exist, each `@pytest.mark.timeout(5)`.
    - The empty-queue test sends records, sleeps past 0.1s, sends MORE records, and asserts the later batch is received (listener survived).
    - The clean-shutdown test asserts stop() joins without raising and propagate_exception() is a no-op.
    - The full file passes (pytest exit 0).
    - All four COVMED-01 branches (handler-raise, propagate re-raise, queue.Empty non-termination, clean shutdown) now have a covering test.
  </acceptance_criteria>
  <done>Empty-queue non-termination and clean-shutdown branches covered and green; COVMED-01 fully covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker process → listener thread | log records cross a multiprocessing.Queue; a malformed/exception-raising handler is the failure mode under test |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Denial of Service | listener thread exception handling | mitigate | Tests prove a handler exception is captured and surfaces via propagate_exception rather than silently killing logging; covers the swallow risk in D-05 |
| T-97-02-SC | Tampering | pip installs | accept | No new packages; pytest/testfixtures/pytest-timeout already in dev deps |
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/unittests/test_common/test_multiprocessing_logger.py -q` exits 0.
- Four new timeout-guarded tests cover the four documented branches.
- No name-mangled private attribute access in the test file.
</verification>

<success_criteria>
COVMED-01 closed: the MultiprocessingLogger listener thread has tests covering
handler-raise shutdown, propagate_exception re-raise (and no-op-after-clear),
inner-loop queue.Empty non-termination, and clean shutdown — all green and timeout-guarded.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-02-SUMMARY.md` when done.
</output>
