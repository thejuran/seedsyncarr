---
phase: 97-medium-priority-python-coverage
plan: 03
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/python/tests/unittests/test_web/test_handler/test_config_handler.py
  - src/python/web/handler/config.py
autonomous: true
requirements: [COVMED-02]
must_haves:
  truths:
    - "_validate_url rejects IPv4 private, loopback, and link-local addresses with the private/reserved message"
    - "_validate_url rejects IPv6 link-local, loopback, and unique-local addresses with the private/reserved message"
    - "_validate_url rejects IPv6-mapped private IPv4 (::ffff:10.0.0.1)"
    - "_validate_url returns 'Cannot resolve hostname' when getaddrinfo raises gaierror"
    - "_validate_url returns None for a valid public host (8.8.8.8)"
    - "_validate_url rejects non-http(s) schemes and missing hostnames without a socket call"
  artifacts:
    - path: "src/python/tests/unittests/test_web/test_handler/test_config_handler.py"
      provides: "TestValidateUrl class covering all SSRF input classes against the staticmethod directly"
      contains: "TestValidateUrl"
  key_links:
    - from: "TestValidateUrl"
      to: "ConfigHandler._validate_url"
      via: "direct staticmethod call with patched web.handler.config.socket"
      pattern: "_validate_url"
---

<objective>
Add full-path coverage for the SSRF guard `ConfigHandler._validate_url`
(`src/python/web/handler/config.py:55-85`), covering every documented input class
(COVMED-02): IPv4 private/loopback/link-local, IPv6 link-local/loopback/unique-local,
IPv6-mapped IPv4, unresolved hostname, valid public host, and the scheme/no-hostname
early returns.

Purpose: this is the project's only SSRF defense for the Sonarr/Radarr connection-test
endpoints. IPv6 and reserved-range handling is currently untested, and the IPv6-mapped
form (`::ffff:10.0.0.1`) may slip the current check.
Output: a new `TestValidateUrl` class in the existing unit test file, calling the
`@staticmethod` directly with `socket.getaddrinfo` stubbed (no bottle stack, no real DNS).

PRE-APPROVED trivial fix (D-01/D-02): if the `::ffff:10.0.0.1` test shows the IPv6-mapped
private IPv4 is NOT blocked by the current `addr.is_private or is_loopback or is_reserved
or is_link_local` check at config.py:78, add a minimal unmap-and-recheck. This is in-scope
(<10 lines, no public-API change, strengthens an existing security guard). Do NOT add
DNS-rebind handling (out of scope per the inline comment at config.py:59-62).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<interfaces>
From src/python/web/handler/config.py (the method under test — a pure @staticmethod, directly callable):
- `ConfigHandler._validate_url(url: str) -> str | None` — returns an error string or None if valid.
  - scheme not in {http, https} → "Only http and https URLs are allowed" (no socket call)
  - no hostname → "Invalid URL: no hostname" (no socket call)
  - for each `socket.getaddrinfo(hostname, None)` 5-tuple, `ip_str = addr_info[4][0]`, `addr = ipaddress.ip_address(ip_str)`; if `addr.is_private or addr.is_loopback or addr.is_reserved or addr.is_link_local` → "URL resolves to a private/reserved IP address"
  - `except socket.gaierror` → "Cannot resolve hostname"
  - otherwise → None
- The module imports `socket` at config.py:4 and `import ipaddress` at config.py:2.

Stub pattern (load-bearing) from test_config_handler.py:131-139 (TestConfigHandlerTestSonarrConnection):
- Patch target is `web.handler.config.socket` (NOT global socket).
- `mock_socket.getaddrinfo.return_value = [(None, None, None, None, (<ip_str>, 0))]` — IP at index [4][0].
- ALWAYS re-assign `mock_socket.gaierror = socket.gaierror` after patching, else `except socket.gaierror` references a MagicMock and won't catch.
- For unresolved host: `mock_socket.getaddrinfo.side_effect = socket.gaierror("...")` plus the gaierror reassignment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestValidateUrl covering all SSRF input classes</name>
  <files>src/python/tests/unittests/test_web/test_handler/test_config_handler.py</files>
  <read_first>
    - src/python/tests/unittests/test_web/test_handler/test_config_handler.py (lines 1-160: imports `import socket`, `from unittest.mock import patch`, `from web.handler.config import ConfigHandler`; the `@patch('web.handler.config.socket')` + `mock_socket.gaierror = socket.gaierror` pattern at 131-139)
    - src/python/web/handler/config.py (lines 55-85, the _validate_url staticmethod)
  </read_first>
  <behavior>
    - test_rejects_ipv4_private (10.0.0.1) → "private/reserved" in result
    - test_rejects_ipv4_loopback (127.0.0.1) → "private/reserved"
    - test_rejects_ipv4_link_local (169.254.0.1) → "private/reserved"
    - test_rejects_ipv6_link_local (fe80::1) → "private/reserved"
    - test_rejects_ipv6_loopback (::1) → "private/reserved"
    - test_rejects_ipv6_unique_local (fc00::1) → "private/reserved"
    - test_rejects_ipv6_mapped_private_ipv4 (::ffff:10.0.0.1) → "private/reserved"  [D-01: write RED first]
    - test_unresolved_hostname (getaddrinfo raises gaierror) → "Cannot resolve hostname"
    - test_accepts_public_ip (8.8.8.8) → None
    - test_rejects_ftp_scheme (ftp://host/) → "Only http and https" (no socket call)
    - test_rejects_no_hostname (http:///path) → "Invalid URL: no hostname"
  </behavior>
  <action>
    Add a new `TestValidateUrl(unittest.TestCase)` class to the existing file. Call `ConfigHandler._validate_url(<url>)` directly (it is a staticmethod — no handler instance or bottle stack needed). For each resolving case, decorate with `@patch('web.handler.config.socket')`, set `mock_socket.getaddrinfo.return_value = [(None, None, None, None, (<ip_str>, 0))]`, and ALWAYS set `mock_socket.gaierror = socket.gaierror` (the easy-to-miss line). Pass a URL like `http://host.example/` (the hostname is irrelevant because getaddrinfo is stubbed) and assert the returned string contains `"private/reserved"` via `self.assertIn(...)`, OR assert it `is None` for the public case. For the unresolved case, set `mock_socket.getaddrinfo.side_effect = socket.gaierror("name resolution failed")`, set `mock_socket.gaierror = socket.gaierror`, and assert the result is `"Cannot resolve hostname"`. For the scheme and no-hostname cases, no socket patch is needed (the method returns before the socket call) — assert the exact early-return strings. Write the `::ffff:10.0.0.1` test FIRST and run it RED to discover whether the current check at config.py:78 blocks it. Match the existing file's `unittest.TestCase` + `self.assertIn`/`self.assertIsNone` assertion style.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/unittests/test_web/test_handler/test_config_handler.py::TestValidateUrl -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - A `TestValidateUrl` class exists with one test per row in the behavior block (11 cases).
    - Every resolving test sets `mock_socket.gaierror = socket.gaierror` (grep: each `@patch('web.handler.config.socket')` test that calls getaddrinfo also assigns gaierror).
    - The scheme and no-hostname tests do NOT patch socket and assert the exact early-return strings.
    - All non-`::ffff` tests pass; the `::ffff:10.0.0.1` test is present (it may be RED until Task 2 if the guard does not block it).
  </acceptance_criteria>
  <done>All SSRF input-class tests written and calling the staticmethod directly with stubbed socket.</done>
</task>

<task type="auto">
  <name>Task 2: Apply pre-approved IPv6-mapped unmap fix if RED, else confirm green</name>
  <files>src/python/web/handler/config.py, src/python/tests/unittests/test_web/test_handler/test_config_handler.py</files>
  <read_first>
    - src/python/web/handler/config.py (lines 72-85: the getaddrinfo loop, the four-check guard at 78, the gaierror catch at 82-83)
    - .planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md (D-01, D-02, D-05)
  </read_first>
  <action>
    Run the `test_rejects_ipv6_mapped_private_ipv4` test from Task 1. If it FAILS (current guard does not block `::ffff:10.0.0.1`), apply the pre-approved D-01/D-02 trivial fix in `config.py` inside the per-addr loop, before the private/reserved check resolves to "not blocked": detect the IPv6-mapped form via `addr.ipv4_mapped` (the `ipaddress` attribute, which is non-None for `::ffff:a.b.c.d`) and, when present, re-run the same four checks (`is_private or is_loopback or is_reserved or is_link_local`) against the unmapped IPv4 (`addr.ipv4_mapped`) and return the same "URL resolves to a private/reserved IP address" string if any match. Keep it minimal (target <10 net non-blank/non-comment lines, no signature change, no new return string — reuse the existing message). Do NOT add DNS-rebind handling. If the test instead PASSES already (the platform's `ipaddress`/`getaddrinfo` normalization blocks it), make NO source change and record in the SUMMARY that the guard already covers the mapped form. If the needed fix turns out to exceed the trivial window (>10 lines or behavior change beyond strengthening the guard), STOP, revert the source edit, leave the test asserting the current (documented) behavior, and add a one-line STATE.md deferred-items entry per D-05 referencing the test name.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/unittests/test_web/test_handler/test_config_handler.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The `::ffff:10.0.0.1` test passes (asserting it resolves to "private/reserved").
    - If config.py was modified, the diff is ≤10 net non-blank/non-comment lines, no signature change, reuses the existing error string, and adds NO DNS-rebind logic (grep config.py diff: no new public method, `_validate_url` signature unchanged).
    - The full test_config_handler.py file still passes (no regression in existing Sonarr/Radarr SSRF tests).
    - If deferred instead of fixed, a STATE.md deferred-items row references the documenting test.
  </acceptance_criteria>
  <done>IPv6-mapped private IPv4 is blocked (or the limitation is deferred with a STATE.md entry); COVMED-02 fully covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-supplied Sonarr/Radarr URL → server-side requests.get | _validate_url is the SSRF guard at this boundary; attacker-controlled hostnames can resolve to internal IPs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Information Disclosure / EoP (SSRF) | ConfigHandler._validate_url | mitigate | Full-path tests assert private/reserved IPv4+IPv6 (incl. ::ffff mapped) are rejected; pre-approved unmap fix strengthens the existing guard (D-01/D-02) |
| T-97-03-02 | Spoofing (DNS rebind / TOCTOU) | _validate_url getaddrinfo-then-requests gap | accept | Documented out-of-scope per inline comment config.py:59-62; test documents the limitation, does not fix it |
| T-97-03-SC | Tampering | pip installs | accept | No new packages; uses stdlib socket/ipaddress and existing test deps |
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/unittests/test_web/test_handler/test_config_handler.py -q` exits 0.
- TestValidateUrl covers all 11 input classes; every resolving test reassigns `mock_socket.gaierror`.
- Any config.py change is within the trivial-fix window or deferred with a STATE.md entry.
</verification>

<success_criteria>
COVMED-02 closed: _validate_url is tested across IPv4 private/loopback/link-local, IPv6
link-local/loopback/unique-local, IPv6-mapped IPv4, unresolved hostname, valid public host,
and scheme/no-hostname early returns — with socket.getaddrinfo stubbed and CI-IPv6-independent.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-03-SUMMARY.md` when done.
</output>
