---
phase: 97-medium-priority-python-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.3.0-COVERAGE-BASELINE.md
autonomous: true
requirements: [RATCHET-01]
must_haves:
  truths:
    - "A baseline file records the current Python line+branch coverage total from main HEAD, measured with a command that ACTUALLY emits coverage (host `make coverage-python`, which runs `pytest --cov`)"
    - "The Python baseline is explicitly marked PROVISIONAL (host-only, excludes the real-lftp integration suite) because the containerized `make run-tests-python` CMD is `pytest -v -p no:cacheprovider` with NO --cov and therefore cannot produce coverage"
    - "The baseline file records the current Angular coverage (statements/branches/functions/lines) from main HEAD"
    - "The baseline file records the configured pytest fail_under floor (84) verbatim AND records the FACT that no Karma global coverage thresholds are configured yet (karma.conf.js has only type/dir, no check.global)"
    - "The baseline file is committed BEFORE any phase-97 test changes land"
    - "A requirement is recorded that Phase 100 must capture a comparable container-inclusive 'before' (same command, real-lftp suite included) before setting the ratchet threshold"
    - "D-06: baseline captured via existing make coverage targets (Python `make coverage-python` --cov + Angular Karma) against main HEAD, raw totals recorded verbatim, committed before any test lands"
  artifacts:
    - path: ".planning/milestones/v1.3.0-COVERAGE-BASELINE.md"
      provides: "Coverage anchor for the Phase 100 ratchet (RATCHET-02)"
      contains: "Python"
  key_links:
    - from: ".planning/milestones/v1.3.0-COVERAGE-BASELINE.md"
      to: ".planning/ROADMAP.md Coverage Ratchet table (Before column)"
      via: "manually transcribed numbers"
      pattern: "fail_under"
---

<objective>
Capture the pre-phase coverage baseline for milestone v1.3.0 and commit it BEFORE any
new test lands in Phase 97. This is the "before" anchor the Phase 100 ratchet
(RATCHET-02) compares against.

Purpose: D-06 mandates a committed baseline so the final ratchet diffs against a fixed
reference, not against whatever number happens to be in CI config at ratchet time.
Output: `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md` with Python (line+branch) and
Angular (statements/branches/functions/lines) totals from `main` HEAD, plus the
configured `fail_under` floor.

CRITICAL COMMAND CORRECTION (Codex review, source-verified): the containerized
`make run-tests-python` runs `Dockerfile:60` CMD `["pytest", "-v", "-p", "no:cacheprovider"]`
— there is NO `--cov` flag, so that target CANNOT emit coverage. The ONLY existing target
that produces coverage is host `make coverage-python` (`Makefile:138-139`:
`poetry run pytest --cov --cov-report=term-missing --cov-report=html`). Use
`make coverage-python` as the Python coverage source. It runs on the host and therefore
OMITS the real-lftp integration suite (no lftp binary / sshd on host), so the Python number
MUST be marked PROVISIONAL and Phase 100 must re-measure a container-inclusive "before"
with the same command before setting the ratchet threshold.

This plan MUST run and commit before 97-02 / 97-03 / 97-04 (they depend on it).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<notes>
- Coverage config is in `src/python/pyproject.toml`: `[tool.coverage.run] branch = true`,
  `source = ["."]`, `omit = ["tests/*","docs/*"]`; `[tool.coverage.report] fail_under = 84`,
  `show_missing = true`. DO NOT change these in this phase — the ratchet is Plan 100-03.
- COVERAGE-EMITTING COMMAND (source-verified by Codex review):
  - `Makefile:138-139` target `coverage-python` → `cd src/python && poetry run pytest --cov --cov-report=term-missing --cov-report=html`. This is the ONLY existing target with `--cov`. USE THIS for the Python number.
  - `Makefile:98` target `run-tests-python` runs the container whose CMD (`src/docker/test/python/Dockerfile:60`) is `["pytest", "-v", "-p", "no:cacheprovider"]` — NO `--cov`. It includes the real-lftp integration suite but emits NO coverage. Do NOT use it as the coverage source; an override (`pytest --cov --cov-report=term-missing` inside the container) would be required to get container-inclusive coverage, and that is Phase 100's job.
- The host `coverage-python` target OMITS the real-lftp integration suite (no lftp binary / sshd on the host). Record the Python number as PROVISIONAL (host-only) and add the Phase-100 re-measure requirement.
- Karma: `src/angular/karma.conf.js` `coverageReporter` has ONLY `type: 'html'` and `dir: 'coverage/'` (verified) — there are NO `check.global` thresholds configured. RECORD THE FACT that no Karma global thresholds exist yet; do NOT transcribe thresholds that are not in the file. Creating thresholds is Phase 100's job.
- CONTEXT.md cites a v1.1.2 baseline of 85.05%; the live config floor is 84. Record BOTH the configured `fail_under` AND the freshly-measured total so Phase 100 has an unambiguous before.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure current coverage on main HEAD</name>
  <files>(no source files modified — measurement only)</files>
  <read_first>
    - Makefile (target `coverage-python` at :138-139 — the only `--cov` target; `run-tests-python` at :98)
    - src/docker/test/python/Dockerfile (line 60 CMD `["pytest", "-v", "-p", "no:cacheprovider"]` — confirm it has NO --cov)
    - src/python/pyproject.toml (`[tool.coverage.run]` and `[tool.coverage.report]`, lines ~79-89)
    - src/angular/karma.conf.js (confirm `coverageReporter` has only type/dir and NO check.global block)
  </read_first>
  <action>
    Confirm the working tree is on `main` HEAD with no Phase-97 test changes present (none should exist yet — this plan runs first). Capture the git short SHA (`git rev-parse --short HEAD`) for the baseline header. Run the host Python coverage invocation `make coverage-python` (`cd src/python && poetry run pytest --cov --cov-report=term-missing --cov-report=html`) — this is the ONLY existing target that emits coverage. Do NOT use `make run-tests-python` for the coverage number: its container CMD (`Dockerfile:60`) is `pytest -v -p no:cacheprovider` with NO `--cov` and produces no coverage report. Capture the coverage TOTAL line (line % and, because `branch = true`, the branch data) verbatim from the `term-missing` report. Because the host run omits the real-lftp integration suite (no lftp binary / sshd on host), note that the Python number is PROVISIONAL/host-only. Then run the Angular coverage invocation (Karma with code coverage under `src/angular`, e.g. `ng test --code-coverage` / the project's `npm` equivalent) and capture the statements/branches/functions/lines percentages from the Istanbul summary. Do NOT modify pyproject.toml or karma.conf.js. Read the configured pytest `fail_under` value (84) verbatim. Inspect `karma.conf.js` `coverageReporter` and CONFIRM it has only `type`/`dir` and NO `check.global` thresholds — you will RECORD THIS FACT in Task 2, not transcribe non-existent numbers.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr && git status --porcelain src/python/pyproject.toml src/angular/karma.conf.js | grep -q . && echo "CONFIG CHANGED — FAIL" && exit 1 || echo "coverage config untouched"</automated>
  </verify>
  <acceptance_criteria>
    - A fresh Python coverage total (line %, with branch enabled) is captured from main HEAD via `make coverage-python` (the `--cov` target), NOT via `make run-tests-python`.
    - The git short SHA and the exact command run are captured for the baseline header.
    - Angular statements/branches/functions/lines percentages are captured from main HEAD.
    - The configured pytest `fail_under` (84) is read verbatim, and the ABSENCE of any Karma `check.global` thresholds is confirmed by inspecting karma.conf.js.
    - No coverage-config file was modified (`git status --porcelain` clean for pyproject.toml and karma.conf.js).
  </acceptance_criteria>
  <done>Python (host/provisional) and Angular coverage numbers measured with a real --cov command; config files unchanged; Karma threshold-absence confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Write and commit the baseline file</name>
  <files>.planning/milestones/v1.3.0-COVERAGE-BASELINE.md</files>
  <read_first>
    - .planning/ROADMAP.md (the "Coverage Ratchet — Before / After" table under the v1.3.0 section, ~lines 302-311)
    - .planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md (D-06)
  </read_first>
  <action>
    Create `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md`. Record, with section headers: (1) the date and the git HEAD short SHA the baseline was measured against, plus the EXACT command run for each language; (2) a Python section with the freshly-measured line coverage total, the branch-coverage data, the invocation used (`make coverage-python` host / `poetry run pytest --cov`), AND an explicit "PROVISIONAL — host-only, excludes the real-lftp integration suite" note explaining that the containerized `run-tests-python` CMD has no `--cov` and so cannot be used as the coverage source; (3) the configured pytest `fail_under` floor recorded verbatim as 84; (4) an Angular section with statements/branches/functions/lines percentages, and the explicit FACT "No Karma global coverage thresholds configured yet (karma.conf.js coverageReporter has only type/dir; no check.global block)" — do NOT invent threshold numbers; (5) a "Phase 100 follow-up" requirement stating that Phase 100 MUST capture a comparable container-inclusive Python "before" (real-lftp suite included, same `--cov` flags) before setting the ratchet threshold, and that creating Karma global thresholds is also Phase 100's job; (6) a one-line pointer that this file is the anchor for the Phase 100 ratchet (RATCHET-02) and that the ratchet is monotonic (only increases). Each number field (Python total, Angular four metrics) MUST contain an actual measured number — NO literal `TBD` is permitted in those fields. Do NOT edit pyproject.toml or karma.conf.js. Do NOT fill in the ROADMAP "After" column (that is Plan 100-03); the executor MAY transcribe the measured "Before" numbers into the ROADMAP Coverage Ratchet table's Before column, replacing the `TBD (Phase 97 baseline)` placeholders, since that is the documented purpose of the before column.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr && f=.planning/milestones/v1.3.0-COVERAGE-BASELINE.md; test -f "$f" && grep -qi "python" "$f" && grep -qi "angular" "$f" && grep -q "84" "$f" && grep -qi "provisional" "$f" && grep -qiE "no (karma )?(global )?(coverage )?threshold" "$f" && grep -qiE 'phase 100' "$f" && SHA=$(git rev-parse --short HEAD) && grep -qiE "\b${SHA}\b" "$f" && python3 -c "import re,sys; t=open('$f').read(); pf=t[t.lower().find('## python'):t.lower().find('## angular')] if '## python' in t.lower() else ''; a=t[t.lower().find('## angular'):] if '## angular' in t.lower() else ''; assert 'TBD' not in pf.upper().replace('TBD (PHASE','') and re.search(r'[0-9]+(\.[0-9]+)?\s*%', pf), 'Python number missing/TBD'; assert re.search(r'[0-9]+(\.[0-9]+)?\s*%', a), 'Angular number missing'; print('numeric baseline OK')" && echo "SHA + numeric baseline OK"
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md` exists.
    - Contains the git short SHA the baseline was measured against (the verify gate asserts `git rev-parse --short HEAD` appears in the file — provenance is mandatory, not optional).
    - Contains a Python section with a real measured numeric line+branch total (matched by `[0-9]+(\.[0-9]+)?%`), the `make coverage-python` invocation, and an explicit PROVISIONAL/host-only note.
    - Contains an Angular section with numeric statements/branches/functions/lines AND the explicit fact that no Karma global thresholds are configured.
    - Records the configured pytest `fail_under` value (84) verbatim.
    - Contains a Phase-100 follow-up requirement (container-inclusive re-measure before ratchet; Karma thresholds are Phase 100's job).
    - No literal `TBD` appears in the Python/Angular number fields (the only permitted `TBD` is the ROADMAP "After" placeholder, which lives in ROADMAP.md, not this file).
    - States it is the Phase 100 ratchet anchor.
    - No new test code introduced in this plan (`git diff --name-only` shows only the baseline doc and optionally ROADMAP.md).
  </acceptance_criteria>
  <done>Baseline file written with real numeric coverage, the host/provisional caveat, the Karma-threshold-absence fact, and the Phase-100 re-measure requirement; committed before any phase-97 test lands; RATCHET-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan; no runtime code, no untrusted input crosses any boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Tampering | coverage config (pyproject.toml / karma.conf.js) | mitigate | Task verify gates assert config files are untouched; ratchet is reserved for Plan 100-03 |
| T-97-01-02 | Repudiation | baseline number provenance | mitigate | Baseline records exact command + git SHA + PROVISIONAL host-only caveat so the Phase 100 ratchet diff is honest and reproducible |
| T-97-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; uses existing make targets and committed deps only |
</threat_model>

<verification>
- `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md` exists with real numeric Python + Angular numbers, the `fail_under` floor, the PROVISIONAL host-only note, the Karma-threshold-absence fact, and the Phase-100 re-measure requirement.
- Python number was produced by `make coverage-python` (a `--cov` command), not by the coverage-less `run-tests-python` container CMD.
- No coverage-config file modified.
- Committed before 97-02/03/04 run.
</verification>

<success_criteria>
RATCHET-01 closed: a committed baseline file records main-HEAD Python (line+branch, host/provisional)
and Angular coverage plus the configured pytest floor, records that no Karma global thresholds exist
yet, and obligates Phase 100 to re-measure a container-inclusive Python "before" before ratcheting —
ready as the Phase 100 ratchet anchor.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
