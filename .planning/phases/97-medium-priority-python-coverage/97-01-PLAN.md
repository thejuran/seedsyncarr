---
phase: 97-medium-priority-python-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.3.0-COVERAGE-BASELINE.md
autonomous: true
requirements: [RATCHET-01]
must_haves:
  truths:
    - "A baseline file records the current Python line+branch coverage total from main HEAD"
    - "The baseline file records the current Angular coverage (statements/branches/functions/lines) from main HEAD"
    - "The baseline file records the configured pytest fail_under floor (84) verbatim"
    - "The baseline file is committed BEFORE any phase-97 test changes land"
    - "D-06: baseline captured via existing make coverage targets (Python --cov + Angular Karma) against main HEAD, raw totals recorded verbatim, committed before any test lands"
  artifacts:
    - path: ".planning/milestones/v1.3.0-COVERAGE-BASELINE.md"
      provides: "Coverage anchor for the Phase 100 ratchet (RATCHET-02)"
      contains: "Python"
  key_links:
    - from: ".planning/milestones/v1.3.0-COVERAGE-BASELINE.md"
      to: ".planning/ROADMAP.md Coverage Ratchet table (Before column)"
      via: "manually transcribed numbers"
      pattern: "fail_under"
---

<objective>
Capture the pre-phase coverage baseline for milestone v1.3.0 and commit it BEFORE any
new test lands in Phase 97. This is the "before" anchor the Phase 100 ratchet
(RATCHET-02) compares against.

Purpose: D-06 mandates a committed baseline so the final ratchet diffs against a fixed
reference, not against whatever number happens to be in CI config at ratchet time.
Output: `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md` with Python (line+branch) and
Angular (statements/branches/functions/lines) totals from `main` HEAD, plus the
configured `fail_under` floor.

This plan MUST run and commit before 97-02 / 97-03 / 97-04 (they depend on it).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<notes>
- Coverage config is in `src/python/pyproject.toml`: `[tool.coverage.run] branch = true`,
  `source = ["."]`, `omit = ["tests/*","docs/*"]`; `[tool.coverage.report] fail_under = 84`,
  `show_missing = true`. DO NOT change these in this phase — the ratchet is Plan 100-03.
- Existing invocation to mirror (do NOT invent a new command):
  - `Makefile:138-139` target `coverage-python` → `cd src/python && poetry run pytest --cov --cov-report=term-missing --cov-report=html`.
  - `Makefile:98` target `run-tests-python` → containerized run (`seedsyncarr_test_python`) that includes the real-lftp integration suite via `src/docker/test/python/entrypoint.sh`.
- The host `coverage-python` target OMITS the real-lftp integration suite (no lftp binary / sshd on the host). The containerized `run-tests-python` includes it. Record which run produced the Python number and note the omission if the host run is used.
- CONTEXT.md cites a v1.1.2 baseline of 85.05%; the live config floor is 84. Record BOTH the configured `fail_under` AND the freshly-measured total so Phase 100 has an unambiguous before.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure current coverage on main HEAD</name>
  <files>(no source files modified — measurement only)</files>
  <read_first>
    - Makefile (targets `coverage-python` at :138 and `run-tests-python` at :98)
    - src/python/pyproject.toml (`[tool.coverage.run]` and `[tool.coverage.report]`, lines ~79-89)
    - src/docker/test/python/entrypoint.sh (confirms the real-lftp integration suite runs in-container)
  </read_first>
  <action>
    Confirm the working tree is on `main` HEAD with no Phase-97 test changes present (none should exist yet — this plan runs first). Run the existing Python coverage invocation to get a fresh total. Prefer the containerized `make run-tests-python` so the real-lftp integration suite (which 97-04 extends) is included in the line+branch total; if the container cannot run in this environment, fall back to host `make coverage-python` and explicitly record in the baseline file that the real-lftp integration suite was OMITTED from the Python number. Capture the coverage TOTAL line (line % and, because `branch = true`, the branch data) verbatim from the `term-missing` report. Then run the Angular coverage invocation (Karma with code coverage, e.g. the project's `ng test --code-coverage` / `npm run` equivalent under `src/angular`) and capture the statements/branches/functions/lines percentages from the Istanbul summary. Do NOT modify pyproject.toml, karma.conf.js, or any coverage config. Read the configured pytest `fail_under` value (84) and the Karma `coverageReporter.check.global` thresholds verbatim for transcription in Task 2.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr && git status --porcelain src/python/pyproject.toml src/angular/karma.conf.js | grep -q . && echo "CONFIG CHANGED — FAIL" && exit 1 || echo "coverage config untouched"</automated>
  </verify>
  <acceptance_criteria>
    - A fresh Python coverage total (line %, with branch enabled) is captured from main HEAD.
    - Angular statements/branches/functions/lines percentages are captured from main HEAD.
    - The configured pytest `fail_under` (84) and Karma global thresholds are read verbatim.
    - No coverage-config file was modified (`git status --porcelain` clean for pyproject.toml and karma.conf.js).
  </acceptance_criteria>
  <done>Python and Angular coverage numbers measured; config files unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Write and commit the baseline file</name>
  <files>.planning/milestones/v1.3.0-COVERAGE-BASELINE.md</files>
  <read_first>
    - .planning/ROADMAP.md (the "Coverage Ratchet — Before / After" table under the v1.3.0 section, ~lines 302-311)
    - .planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md (D-06)
  </read_first>
  <action>
    Create `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md`. Record, with section headers: (1) the date and the git HEAD short SHA the baseline was measured against; (2) a Python section with the freshly-measured line coverage total, the branch-coverage data, the invocation used (`make run-tests-python` containerized OR `make coverage-python` host), and — if the host fallback was used — an explicit note that the real-lftp integration suite was omitted; (3) the configured pytest `fail_under` floor recorded verbatim as 84; (4) an Angular section with statements/branches/functions/lines percentages and the Karma `coverageReporter.check.global` thresholds verbatim; (5) a one-line pointer that this file is the anchor for the Phase 100 ratchet (RATCHET-02) and that the ratchet is monotonic (only increases). Do NOT edit pyproject.toml or karma.conf.js. Do NOT fill in the ROADMAP "After" column (that is Plan 100-03); the executor MAY transcribe the measured "Before" numbers into the ROADMAP Coverage Ratchet table's Before column, replacing the `TBD (Phase 97 baseline)` placeholders, since that is the documented purpose of the before column.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr && test -f .planning/milestones/v1.3.0-COVERAGE-BASELINE.md && grep -q -i "python" .planning/milestones/v1.3.0-COVERAGE-BASELINE.md && grep -q -i "angular" .planning/milestones/v1.3.0-COVERAGE-BASELINE.md && grep -q "84" .planning/milestones/v1.3.0-COVERAGE-BASELINE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md` exists.
    - Contains a Python section with measured line+branch total and the invocation used.
    - Contains an Angular section with statements/branches/functions/lines.
    - Records the configured pytest `fail_under` value (84) verbatim.
    - States it is the Phase 100 ratchet anchor.
    - No new test code introduced in this plan (`git diff --name-only` shows only the baseline doc and optionally ROADMAP.md).
  </acceptance_criteria>
  <done>Baseline file written and committed before any phase-97 test lands; RATCHET-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan; no runtime code, no untrusted input crosses any boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Tampering | coverage config (pyproject.toml / karma.conf.js) | mitigate | Task verify gates assert config files are untouched; ratchet is reserved for Plan 100-03 |
| T-97-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; uses existing make targets and committed deps only |
</threat_model>

<verification>
- `.planning/milestones/v1.3.0-COVERAGE-BASELINE.md` exists with Python + Angular numbers and the `fail_under` floor.
- No coverage-config file modified.
- Committed before 97-02/03/04 run.
</verification>

<success_criteria>
RATCHET-01 closed: a committed baseline file records main-HEAD Python (line+branch) and
Angular coverage plus the configured pytest floor, ready as the Phase 100 ratchet anchor.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-01-SUMMARY.md` when done.
</output>
