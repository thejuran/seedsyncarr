---
phase: 97-medium-priority-python-coverage
plan: 04
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/python/tests/integration/test_lftp/test_lftp_protocol.py
  - src/python/lftp/lftp.py
autonomous: true
requirements: [COVMED-03]
must_haves:
  truths:
    - "A malformed jobs -v output makes the parser raise LftpJobStatusParserError"
    - "Lftp.status() increments the consecutive-error counter and returns [] while count <= MAX_CONSECUTIVE_STATUS_ERRORS"
    - "The 3rd consecutive error (count > MAX_CONSECUTIVE_STATUS_ERRORS=2) makes Lftp.status() re-raise LftpJobStatusParserError"
    - "A subsequent successful status() resets the consecutive-error counter to 0"
  artifacts:
    - path: "src/python/tests/integration/test_lftp/test_lftp_protocol.py"
      provides: "Integration coverage of the parser-error recovery counter in Lftp.status()"
      contains: "LftpJobStatusParserError"
  key_links:
    - from: "test_lftp_protocol.py"
      to: "Lftp.status() consecutive-error counter (lftp.py:298-313)"
      via: "deterministic malformed-status injection via patch.object on the real Lftp fixture"
      pattern: "status\\(\\)"
---

<objective>
Add integration coverage for the LFTP `JobStatusParser` `ValueError` recovery path
(COVMED-03), covering all four documented points of `Lftp.status()` at
`src/python/lftp/lftp.py:298-313`: a malformed `jobs -v` raises `LftpJobStatusParserError`,
the consecutive-error counter increments (swallowing while ≤ `MAX_CONSECUTIVE_STATUS_ERRORS
= 2`), the 3rd consecutive error re-raises, and a subsequent success resets the counter.

Purpose: the real lftp binary always emits well-formed `jobs -v`, so the existing
integration suite (which drives only real `LftpError` paths) has NEVER exercised the parser
-error counter — the documented recovery logic that keeps a transient parse hiccup from
crashing the controller is untested.
Output: new tests extending `TestLftpProtocol` (per D-03, integration layer) that inject a
deterministic malformed status into the real `Lftp` fixture.

Trivial-fix watch (D-05): the reset line already exists at lftp.py:305 — verify it is
reached. If a real defect surfaces needing >10 lines / a behavior change, defer to v1.4.0
with a STATE.md deferred-items entry referencing the test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<interfaces>
From src/python/lftp/lftp.py:
- `MAX_CONSECUTIVE_STATUS_ERRORS = 2` (line 12)
- `Lftp.__init__` sets `self.__consecutive_status_errors = 0` (line 52) and `self.__job_status_parser = LftpJobStatusParser()` (line 50)
- `Lftp.status() -> List[LftpJobStatus]` (lines 298-313):
    out = self.__run_command("jobs -v")          # line 302
    try: statuses = self.__job_status_parser.parse(out); self.__consecutive_status_errors = 0   # reset on success (305)
    except LftpJobStatusParserError:
        self.__consecutive_status_errors += 1     # increment (307)
        if count <= MAX_CONSECUTIVE_STATUS_ERRORS: warn; statuses = []   # swallow (308-310)
        else: raise                               # >2 → propagate (312)
    return statuses
- name-mangled members for white-box injection: `_Lftp__run_command`, `_Lftp__job_status_parser`, `_Lftp__consecutive_status_errors`

From src/python/lftp/job_status_parser.py:
- `LftpJobStatusParserError(AppError)` (line 10); `parse()` wraps internal `ValueError` → raises `LftpJobStatusParserError("Error parsing lftp job status")` at lines 723-726.

From src/python/tests/integration/test_lftp/test_lftp_protocol.py:
- `setUp` (line 98) builds `self.lftp = Lftp(address=self.host, port=self.port, user=self.user, password=None)` (line 111).
- `tearDown` (lines ~120-126) calls `self.lftp.raise_pending_error()` then `self.lftp.exit()` against the REAL binary — so any patch MUST be removed before teardown.
- Existing tests use `@pytest.mark.timeout(5)`.
- A known-malformed `jobs -v` block fixture (ends with `bad string uh oh`) exists in tests/unittests/test_lftp/test_job_status_parser.py around line 935 — reuse that shape as the malformed input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover parser-error increment, swallow, and max-exceeded re-raise</name>
  <files>src/python/tests/integration/test_lftp/test_lftp_protocol.py</files>
  <read_first>
    - src/python/tests/integration/test_lftp/test_lftp_protocol.py (setUp at line 98 building self.lftp; tearDown at ~120-126; the @pytest.mark.timeout(5) error-driving analogs test_queue_dir_wrong_file_type at ~646)
    - src/python/lftp/lftp.py (status() lines 298-313, MAX_CONSECUTIVE_STATUS_ERRORS at 12)
    - src/python/tests/unittests/test_lftp/test_job_status_parser.py (the malformed `jobs -v` fixture near line 935, ending `bad string uh oh`)
  </read_first>
  <behavior>
    - test_status_parser_error_increments_and_swallows: with status() driven against malformed parser output, the 1st and 2nd consecutive calls return [] (counter at 1 then 2, ≤ MAX) and do not raise.
    - test_status_parser_error_exceeds_max_reraises: the 3rd consecutive call (counter would become 3 > MAX=2) re-raises LftpJobStatusParserError.
  </behavior>
  <action>
    Extend `TestLftpProtocol` (do NOT create a new file — D-03 requires integration layer using the real `Lftp` fixture from setUp). Use the cleaner injection option (b) from PATTERNS.md: scope a `with patch.object(self.lftp._Lftp__job_status_parser, 'parse', side_effect=[...])` context so the patch is REMOVED before tearDown runs `exit()` against the real binary (do NOT use a decorator that outlives the assertions — see the teardown caveat). For the increment/swallow test, give the parser a `side_effect` of three `LftpJobStatusParserError("Error parsing lftp job status")` instances; call `self.lftp.status()` twice and assert each returns `[]` (an empty list) without raising. For the max-exceeded test, with the same side_effect, call `status()` twice (consuming counts 1 and 2) then assert the 3rd call raises via `with self.assertRaises(LftpJobStatusParserError):`. Add `from lftp import LftpJobStatusParserError` (or import from the package the way `LftpError` is imported at the top of the file — verify the symbol is exported) and `from unittest.mock import patch`. Decorate each test `@pytest.mark.timeout(5)` to match the suite. Note: option (b) patches the name-mangled `_Lftp__job_status_parser` — acceptable white-box coupling for a recovery test; call it out in the SUMMARY. Because the patch makes `__run_command("jobs -v")` irrelevant (parse is replaced), no real lftp jobs need to be queued — but keep the patch tightly scoped so tearDown's real-binary `exit()` is unaffected.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/integration/test_lftp/test_lftp_protocol.py -k "parser_error" -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Two new `@pytest.mark.timeout(5)` tests exist in TestLftpProtocol.
    - The increment/swallow test asserts two consecutive `status()` calls return `[]` without raising.
    - The max-exceeded test asserts the 3rd consecutive call raises `LftpJobStatusParserError`.
    - The parser patch is scoped via `with patch.object(...)` (not a method-level decorator), so it is removed before tearDown's real-binary `exit()`.
    - `LftpJobStatusParserError` is imported at module top.
    - The two new tests pass (pytest exit 0 for the `-k parser_error` selection).
  </acceptance_criteria>
  <done>Parser-error increment/swallow (≤2) and max-exceeded (>2) re-raise branches covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover counter reset on subsequent success</name>
  <files>src/python/tests/integration/test_lftp/test_lftp_protocol.py, src/python/lftp/lftp.py</files>
  <read_first>
    - src/python/tests/integration/test_lftp/test_lftp_protocol.py (the tests added in Task 1)
    - src/python/lftp/lftp.py (the reset line `self.__consecutive_status_errors = 0` at 305, inside the success branch)
  </read_first>
  <behavior>
    - test_status_parser_error_counter_resets_on_success: after one or two parser errors (counter > 0), a subsequent SUCCESSFUL status() resets the counter to 0, proven by then driving two MORE consecutive errors that are swallowed (return []) rather than immediately re-raising — which is only possible if the counter was reset.
  </behavior>
  <action>
    Add one `@pytest.mark.timeout(5)` test to `TestLftpProtocol`. Using a scoped `with patch.object(self.lftp._Lftp__job_status_parser, 'parse', side_effect=[...])`, sequence the side_effect as: two `LftpJobStatusParserError(...)` (driving the counter to 2 via two swallowed `status()` calls returning `[]`), then a SUCCESS value (an empty list `[]`, simulating a clean parse) on the next call to exercise the reset at lftp.py:305, then two MORE `LftpJobStatusParserError(...)`. After the success call, assert the two subsequent error calls are AGAIN swallowed (each returns `[]` without raising) — proving the counter reset to 0 (had it not reset, the very next error would be count 3 and would re-raise). This asserts the reset behavior purely through the public `status()` return values, not by reading `_Lftp__consecutive_status_errors`. If the test reveals the reset does NOT happen (line 305 not reached / counter not cleared on success), apply the D-05 trivial-fix posture: the in-scope fix is the single `self.__consecutive_status_errors = 0` reset line already present at lftp.py:305 — verify it is on the success path; if a real >10-line/behavior-change defect is found instead, revert any source edit, leave the test asserting current behavior, and add a one-line STATE.md deferred-items entry referencing this test. Keep the patch scoped so tearDown's real-binary `exit()` runs unpatched.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/integration/test_lftp/test_lftp_protocol.py -k "parser_error" -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - A new `@pytest.mark.timeout(5)` test asserts that after a successful status() following errors, two further consecutive errors are swallowed (return []) rather than re-raising — proving reset.
    - The assertion is made via `status()` return values only (grep: no `_Lftp__consecutive_status_errors` read in the test).
    - The patch is scoped via `with patch.object(...)`.
    - Any source change to lftp.py is the single-line reset confirmation within the trivial-fix window, or the issue is deferred via a STATE.md entry.
    - All three COVMED-03 recovery tests pass together (pytest exit 0 for `-k parser_error`).
  </acceptance_criteria>
  <done>Counter-reset-on-success branch covered and green; COVMED-03 fully covered (all four documented points).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| lftp subprocess stdout → JobStatusParser | malformed `jobs -v` output (transient or adversarial seedbox state) is parsed; recovery counter is the resilience boundary under test |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Denial of Service | Lftp.status() consecutive-error recovery | mitigate | Tests prove transient parse errors are swallowed (≤2) so a momentary malformed status does not crash the controller; persistent errors (>2) still surface |
| T-97-04-SC | Tampering | pip installs | accept | No new packages; uses existing real-lftp integration harness + unittest.mock |
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/integration/test_lftp/test_lftp_protocol.py -k "parser_error" -q` exits 0 (in the containerized real-lftp environment per CI matrix).
- Three new timeout-guarded tests cover increment/swallow, max-exceeded re-raise, and reset-on-success.
- All parser patches are scoped so tearDown's real-binary exit() is unaffected.
</verification>

<success_criteria>
COVMED-03 closed: LFTP JobStatusParser error recovery is covered — a malformed line raises
LftpJobStatusParserError, the consecutive-error counter increments and swallows while ≤2,
the 3rd consecutive error re-raises, and a subsequent success resets the counter — all via
the integration-layer real-Lftp fixture with deterministic injection.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-04-SUMMARY.md` when done.
</output>
