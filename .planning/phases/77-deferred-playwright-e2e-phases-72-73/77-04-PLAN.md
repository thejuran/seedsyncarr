---
phase: 77-deferred-playwright-e2e-phases-72-73
plan: 04
type: execute
wave: 4
depends_on: [77-01, 77-02, 77-03]
files_modified:
  - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md
autonomous: false
requirements: [UAT-01, UAT-02]
tags: [playwright, e2e, ci, verification, smoke]

must_haves:
  truths:
    - "`make run-tests-e2e SEEDSYNCARR_ARCH=amd64` exits 0 with all 26 tests green (11 existing + 5 UAT-01 + 10 UAT-02) per Phase 77 ROADMAP SC#1, SC#2, SC#3."
    - "`git diff` on src/e2e/tests/dashboard.page.spec.ts shows only additions — existing 11 tests line-identical to pre-plan baseline (per D-10)."
    - "No changes to playwright.config.ts, package.json, or CI make target (per D-20, D-21)."
    - "arm64 CI matrix row is observed green OR the developer has explicitly chosen to defer arm64 verification to post-merge CI (checkpoint gates on this)."
  artifacts:
    - path: ".planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md"
      provides: "Final phase verification report: pass/fail per spec, retry counts, any flakes observed, final test count, diff-stat on modified files"
      contains: "26 tests"
  key_links:
    - from: "`make run-tests-e2e` Docker harness"
      to: "all 15 new specs + 11 existing"
      via: "Playwright testMatch glob (unchanged)"
      pattern: "PASS.*26 passed"
---

<objective>
Run the full E2E harness end-to-end against the integrated changes from Plans 01-03 and confirm Phase 77's success criteria (all 26 tests green, existing 11 untouched, no CI/config drift). This plan does not write production code — it executes the harness, observes results, and produces a verification summary. If any spec fails or flakes, surface the failure to the developer at a human-checkpoint rather than silently patching with retry tuning (D-21 forbids global retry changes).

Purpose: Close SC#3 from ROADMAP Phase 77 ("Specs execute in CI (`make run-tests-e2e` job) and fail the build on regression") and confirm SC#1 + SC#2 end-to-end, not just type-check-only. Also produces the final diff audit proving D-10 (existing 11 tests unchanged).

Output: `.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md` with full harness output summary, final diff statistics, and explicit sign-off on each of the 4 ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-CONTEXT.md
@.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-RESEARCH.md
@.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-VALIDATION.md
@.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-01-SUMMARY.md
@.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-02-SUMMARY.md
@.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full E2E harness and collect results</name>
  <files>.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log</files>
  <read_first>
    - Makefile (lines 113-188 — `run-tests-e2e` target: verify the exact invocation shape, STAGING_VERSION + SEEDSYNCARR_ARCH env vars, compose flags)
    - src/e2e/playwright.config.ts (confirm retries: 2 on CI, workers: 1, fullyParallel: false)
    - src/docker/test/e2e/compose.yml (confirm 3-service compose: tests + remote + configure)
    - src/docker/test/e2e/run_tests.sh (confirm `npx playwright test` entrypoint)
    - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-03-SUMMARY.md (whatever the prior plans ran locally — honor their retries count as prior-art)
  </read_first>
  <action>
    Run the full E2E harness against the integrated changes. Pre-derive the STAGING_VERSION default from the Makefile so the invocation is deterministic without guess-and-retry:

    ```bash
    STAGING_DEFAULT=$(grep -E '^STAGING_VERSION.*\?=' Makefile | head -1 | awk -F'?=' '{print $2}' | tr -d ' ')
    echo "Using STAGING_VERSION=${STAGING_VERSION:-$STAGING_DEFAULT}"
    SEEDSYNCARR_ARCH=amd64 STAGING_VERSION="${STAGING_VERSION:-$STAGING_DEFAULT}" make run-tests-e2e 2>&1 | tee .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log
    ```

    The `STAGING_DEFAULT` lookup reads the Makefile's `?=` default for `STAGING_VERSION` (typical pattern: `STAGING_VERSION ?= latest`). If the executor has `STAGING_VERSION` exported in their shell, that wins; otherwise the Makefile default is used. If the grep returns empty (Makefile uses a different assignment form), fall back to `STAGING_VERSION=latest`.

    Expected outcome: **all 26 tests pass**. Playwright's line reporter output should include something like `26 passed (Xm Ys)` and exit 0.

    If any test fails, do NOT retry silently or modify the spec to make it pass. Collect the failure output and escalate via the human-verify checkpoint (Task 2). Flakes (pass on retry within Playwright's `retries: 2`) are acceptable but must be counted in the summary — per D-21, do NOT tune the retry count.

    If the harness reports a Docker build failure (not a test failure), retry ONCE (build cache may have been stale). If it fails twice, treat it as a blocker and escalate — the harness build failing indicates an environmental issue NOT covered by this plan.

    Time budget: ~8-15 min per harness run per VALIDATION.md. Allow up to 25 min if the machine is under other load.

    Collect:
    - Full log file saved at `.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log`
    - Count of pass/fail/flaky per spec
    - Retry count observed for any spec
    - Final "X passed" line from Playwright reporter
  </action>
  <verify>
    <automated>test -f .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log &amp;&amp; grep -qE "([0-9]+) passed" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log &amp;&amp; ! grep -qE "[0-9]+ failed" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log</automated>
  </verify>
  <acceptance_criteria>
    - Log file exists: `test -f .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log`
    - Harness exit code was 0 (grep for "passed" line; absence of "failed" line): `grep -qE "([0-9]+) passed" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log` AND `! grep -qE "^[^-]*[0-9]+ failed" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log`
    - Pass count matches 26: `grep -oE "[0-9]+ passed" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log | head -1 | grep -qE "^26 passed"` (if local retries produced <26, escalate)
    - Each UAT-01 spec visible in log: `grep -cE "UAT-01: " .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log` returns at least `5`
    - Each UAT-02 spec visible in log: `grep -cE "UAT-02: " .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log` returns at least `10`
  </acceptance_criteria>
  <done>
    Harness ran end-to-end; all 26 tests logged as passed (or escalated to human-verify checkpoint with full log on failure).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify harness results + arm64 CI matrix plan</name>
  <files>.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log</files>
  <read_first>
    - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log (MANDATORY — the harness log from Task 1 is the sole evidence artifact reviewed here)
    - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-VALIDATION.md (Manual-Only Verifications section — confirms arm64 is classified as CI-matrix, not local)
  </read_first>
  <action>
    Developer-only verification gate. Claude PAUSES here; the developer performs the manual audit described in the what-built / how-to-verify block below and then issues a resume-signal. Claude does NOT proceed to Task 3 until a resume-signal is received.
  </action>
  <what-built>
    Full E2E harness run against amd64 with 26 tests passing. Log file at `.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log`.
  </what-built>
  <how-to-verify>
    1. Review the log file: `less .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log`
    2. Confirm "26 passed" appears in the final Playwright reporter summary.
    3. Check for any flake messages (lines containing "retry #1" or "retry #2") — flakes are acceptable but should be noted.
    4. Verify that arm64 CI is scheduled to run on the next push. Since local runs default to amd64 and VALIDATION.md lists arm64 as manual-only (CI-matrix), confirm the expectation: arm64 will be validated post-merge in the CI job, OR the developer runs `SEEDSYNCARR_ARCH=arm64 make run-tests-e2e` locally if an arm64 host is available.
    5. Skim the `git diff` summary: confirm only `src/e2e/tests/fixtures/seed-state.ts` (new), `src/e2e/tests/dashboard.page.ts` (modified), and `src/e2e/tests/dashboard.page.spec.ts` (modified) appear. No `playwright.config.ts`, `package.json`, or `Makefile` changes per D-20 + D-21.
    6. Confirm existing 11 tests are line-identical: `git diff src/e2e/tests/dashboard.page.spec.ts | grep -cE "^-[^-]"` should return `0` (zero deletion lines outside of metadata), proving D-10.
  </how-to-verify>
  <resume-signal>
    Type "approved" to accept the results and let Task 3 write the summary.
    Type "failing: <spec name>" to flag a failing/flaky spec for revision — planner will revisit.
    Type "arm64-deferred" if you explicitly want to defer arm64 verification to post-merge CI (this is the expected path; VALIDATION.md classifies it as manual-only).
  </resume-signal>
  <verify>
    <automated>MANUAL — developer issues resume-signal; no automated command applicable for human gate</automated>
  </verify>
  <acceptance_criteria>
    - Developer issued one of the recognized resume signals ("approved", "arm64-deferred", or "failing: &lt;spec name&gt;")
    - If "failing:" was issued, Task 3 is deferred and the planner is re-invoked with a gap-closure plan.
  </acceptance_criteria>
  <done>
    Developer has inspected the harness log + git diff and issued a resume-signal accepting the results or flagging follow-up work.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write phase verification summary</name>
  <files>.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-harness-output.log (MANDATORY — the harness log from Task 1; summary references specific numbers from here)
    - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-CONTEXT.md (re-read to map D-01..D-21 to their implementation status in the summary)
    - .planning/ROADMAP.md (Phase 77 section — the 4 Success Criteria the summary must explicitly address)
    - .planning/REQUIREMENTS.md (UAT-01 and UAT-02 — the summary ties results back to each requirement)
    - .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-01-SUMMARY.md, 77-02-SUMMARY.md, 77-03-SUMMARY.md (prior plan summaries to cross-reference)
  </read_first>
  <action>
    Create `.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md` following the scaffold below. Populate every field from actual log contents and prior summaries — do NOT paste numbers not supported by evidence. If any field cannot be populated from evidence, mark it "NOT OBSERVED" rather than guessing.

    **Required scaffold — emit this exact structure, filling in the `<...>` placeholders:**

    ```markdown
    # Phase 77 Verification Summary

    ## Test Results
    - Full harness command run: `STAGING_VERSION=<version> SEEDSYNCARR_ARCH=amd64 make run-tests-e2e`
    - Exit code: 0
    - Pass count: 26 / 26 (expected per D-18)
    - Failures: 0
    - Flakes (passed on retry): <count from log; list spec names if any>
    - Duration: <minutes from log>

    ## ROADMAP Success Criteria (Phase 77)
    - [x] SC#1 — 5 new Phase 72 UAT-01 specs green: <list 5 spec names>
    - [x] SC#2 — 10 new Phase 73 UAT-02 specs green: <list 10 spec names>
    - [x] SC#3 — Specs execute in CI via `make run-tests-e2e` and fail on regression: confirmed (exit code tied to test result)
    - [x] SC#4 — FIX-01 union covered by `UAT-01: FIX-01 union — DELETED row allows Queue` spec

    ## REQUIREMENTS Mapping
    - UAT-01 (5 specs): PASS — see list under SC#1
    - UAT-02 (10 specs): PASS — see list under SC#2

    ## Decision Coverage

    | Decision | Status | Notes |
    |----------|--------|-------|
    | D-01 | ✓ Honored | Runtime HTTP seed in beforeAll (seed-state.ts) |
    | D-02 | ✓ Honored | Seed once per describe block; no afterEach |
    | D-03 | ✓ Honored | waitForFileStatus polls td.cell-status .status-badge |
    | D-04 | ✓ Honored | Seed module at src/e2e/tests/fixtures/seed-state.ts |
    | D-05 | ✓ Honored | Bulk-action specs assert UI dispatch only (toast + clear + hide) |
    | D-06 | ✓ Honored | FIX-01 covered by pre-seeded DELETED clients.jpg |
    | D-07 | ✓ Honored | Destructive-last ordering; no route-intercept |
    | D-08 | ✓ Honored | beforeAll seed plan: DELETED + DOWNLOADED + STOPPED |
    | D-09 | ✓ Honored | All 15 new specs in existing dashboard.page.spec.ts |
    | D-10 | ✓ Honored | Existing 11 tests untouched (diff audit: 0 deletions) |
    | D-11 | ✓ Honored | 9 new methods on DashboardPage (no new harness classes) |
    | D-12 | ✓ Honored | Helper gaps closed in Wave 1 (Plan 77-01) before any spec work |
    | D-13 | ✓ Honored | UAT-02 = 8 status-filter + 2 URL round-trip |
    | D-14 | ✓ Honored | 1 parent (?segment=done) + 1 sub (?segment=errors&sub=deleted) |
    | D-15 | ✓ Honored | page.reload() used for hydration, not page.goto() |
    | D-16 | ✓ Deferred as designed | Back/forward nav explicitly out of scope |
    | D-17 | ✓ Honored | Drill-down folded into status-filter specs |
    | D-18 | ✓ Honored | Final count = 26 (11 existing + 15 new) |
    | D-19 | ✓ Honored | UAT-01 item 4 landed as consolidated spec (not 5 per-action split) |
    | D-20 | ✓ Honored | Existing make run-tests-e2e harness; no CI workflow change |
    | D-21 | ✓ Deferred as designed | No retry/reporter tuning (per-test test.slow() only if needed) |

    ## Diff Audit
    - `src/e2e/tests/fixtures/seed-state.ts`: new file, <N> lines
    - `src/e2e/tests/dashboard.page.ts`: +<N> lines (9 new methods), 0 deletions
    - `src/e2e/tests/dashboard.page.spec.ts`: +<N> lines (15 new tests + 2 new describe blocks + imports/constants), 0 deletions (D-10 preserved)
    - `src/e2e/playwright.config.ts`: unchanged (D-21)
    - `src/e2e/package.json`: unchanged (no version bumps)
    - `Makefile`: unchanged (D-20)

    ## Architecture arm64 status
    - Local verification: amd64 only (see VALIDATION.md Manual-Only Verifications)
    - CI matrix: arm64 runs post-merge; developer accepted deferred path per Task 2 resume-signal.

    ## Phase Complete
    Signed off by: <developer name / date>
    ```

    The Decision Coverage table above has 21 rows covering D-01..D-21. Replace "Notes" text with concrete artifact/plan references where the template placeholder is generic. The acceptance criterion requiring 21 D-XX references is satisfiable directly from the template.
  </action>
  <verify>
    <automated>test -f .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md &amp;&amp; grep -q "26 / 26" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md &amp;&amp; grep -q "SC#4" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md &amp;&amp; grep -q "D-10 preserved" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary file exists at the expected path.
    - All 4 ROADMAP success criteria (SC#1-SC#4) explicitly called out with PASS/FAIL: `grep -cE "SC#[1-4]" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md` returns at least `4`
    - Both UAT IDs called out: `grep -q "UAT-01" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md` AND `grep -q "UAT-02" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md`
    - All 21 decisions mentioned in the Decision Coverage table: `grep -cE "D-(0[1-9]|1[0-9]|2[01])" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md` returns at least `21`
    - FIX-01 anchor spec name documented: `grep -q "FIX-01 union" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md`
    - Diff audit confirms D-10 no deletions: `grep -q "0 deletions" .planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md`
  </acceptance_criteria>
  <done>
    Summary file documents every ROADMAP success criterion, every requirement ID, every CONTEXT decision (via the 21-row Decision Coverage table), and the full diff audit. Phase 77 is ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 77 verification run does not cross any production boundary — it executes the Docker harness locally.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-08 | Repudiation | Summary claims tests pass without evidence | Mitigate | Task 1 saves full log; Task 3 acceptance criteria require grep-verifiable numbers from log file; human-verify gate at Task 2 between run and summary prevents silent pass-through. |

**Block severity:** None.
</threat_model>

<verification>
After all 3 tasks complete:
- Log file and summary file both exist
- Human checkpoint resumed with "approved" (or alternate acceptable signal)
- Summary cross-references all 4 ROADMAP SCs, both UAT REQ IDs, and all 21 CONTEXT decisions
- Phase 77 is ready for `/gsd-verify-work` + roadmap status update
</verification>

<success_criteria>
- Full amd64 E2E harness passes 26/26 tests.
- arm64 deferred to CI matrix per VALIDATION.md manual-only classification (acknowledged by developer at Task 2 checkpoint).
- Diff audit confirms no drift in playwright.config.ts, package.json, Makefile, or existing 11 tests.
- Summary file committed alongside the test additions.
</success_criteria>

<output>
The summary file itself (`.planning/phases/77-deferred-playwright-e2e-phases-72-73/77-04-SUMMARY.md`) IS the output for this plan. No additional SUMMARY.md wrapper needed (this plan's output is the verification report, not a normal execution summary).
</output>
</content>
</invoke>