---
phase: 57-readme-community-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
  - CHANGELOG.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - doc/images/screenshot-dashboard.png
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-08
  - PRES-09

must_haves:
  truths:
    - "README opens with a one-sentence value proposition before any heading"
    - "A screenshot image renders in the README on GitHub"
    - "A Docker Compose example block is visible in the README"
    - "Four badges (CI, version, Docker, license) appear in the README and link to live targets"
    - "A Related Projects section links to Triggarr"
    - "Opening a new issue shows two YAML form templates (bug report, feature request)"
    - "CONTRIBUTING.md, SECURITY.md, and CHANGELOG.md all exist and are non-empty"
  artifacts:
    - path: "README.md"
      provides: "Value-prop README with badges, screenshot, Docker Compose, Triggarr link"
      contains: "ghcr.io/thejuran/seedsyncarr"
    - path: "CONTRIBUTING.md"
      provides: "Contributor guide"
      min_lines: 20
    - path: "CHANGELOG.md"
      provides: "Release history"
      contains: "1.0.0"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "YAML form bug report template"
      contains: "type: textarea"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "YAML form feature request template"
      contains: "type: textarea"
    - path: "doc/images/screenshot-dashboard.png"
      provides: "Dashboard screenshot for README"
  key_links:
    - from: "README.md"
      to: "doc/images/screenshot-dashboard.png"
      via: "Markdown image reference"
      pattern: "screenshot-dashboard\\.png"
    - from: "README.md"
      to: "https://github.com/thejuran/triggarr"
      via: "Markdown link in Related Projects"
      pattern: "triggarr"
---

<objective>
Write the value-prop README, community health files (CONTRIBUTING.md, CHANGELOG.md), convert issue
templates from Markdown to YAML forms, and capture a dashboard screenshot.

Purpose: A first-time visitor to the repo immediately understands the value, can install in under
5 minutes, and finds structured issue templates for reporting bugs or requesting features.

Output: README.md (full rewrite), CONTRIBUTING.md, CHANGELOG.md, two YAML issue form templates,
dashboard screenshot in doc/images/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-readme-community-health/57-RESEARCH.md

<interfaces>
<!-- Existing files the executor needs to understand -->

From SECURITY.md (exists, no changes needed):
- Supported versions table
- Reporting instructions
- Best practices section

From .github/ISSUE_TEMPLATE/bug_report.md (to be REPLACED):
- Markdown template with frontmatter (name, about, title, labels)
- Sections: Description, Steps, Expected, Actual, Environment, Logs, Screenshots

From .github/ISSUE_TEMPLATE/feature_request.md (to be REPLACED):
- Markdown template with frontmatter

From Dockerfile (EXPOSE 8800):
- Port 8800 is the app port

From LICENSE.txt:
- Apache License 2.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with value prop, badges, screenshot, Docker Compose, and Triggarr link</name>
  <files>README.md, doc/images/screenshot-dashboard.png</files>
  <read_first>README.md, SECURITY.md, src/docker/build/docker-image/Dockerfile, .planning/phases/57-readme-community-health/57-RESEARCH.md</read_first>
  <action>
1. Create `doc/images/` directory.

2. Capture a dashboard screenshot:
   - Run `docker run -d --name seedsyncarr-screenshot -p 8800:8800 ghcr.io/thejuran/seedsyncarr:latest` to start the app
   - Wait for the app to be ready (curl http://localhost:8800 returns 200)
   - Use gsd-browser to navigate to http://localhost:8800, take a snapshot, then use the screenshot command to save to `doc/images/screenshot-dashboard.png`
   - Stop and remove the container: `docker stop seedsyncarr-screenshot && docker rm seedsyncarr-screenshot`
   - If Docker is unavailable or the screenshot fails, create a placeholder note and mark as checkpoint for manual screenshot later

3. Write README.md from scratch with this exact structure (delete all existing content):

```
<p align="center">
  <img src="doc/images/screenshot-dashboard.png" alt="SeedSyncarr Dashboard" width="800" />
</p>

> Sync files from your seedbox to your local media server — fast, automated, and integrated with Sonarr and Radarr.

[![CI](https://github.com/thejuran/seedsyncarr/actions/workflows/ci.yml/badge.svg)](https://github.com/thejuran/seedsyncarr/actions/workflows/ci.yml)
[![Release](https://img.shields.io/github/v/release/thejuran/seedsyncarr)](https://github.com/thejuran/seedsyncarr/releases)
[![Docker](https://img.shields.io/badge/docker-ghcr.io-blue)](https://github.com/thejuran/seedsyncarr/pkgs/container/seedsyncarr)
[![License](https://img.shields.io/github/license/thejuran/seedsyncarr)](LICENSE.txt)

## Quick Start

{Docker Compose block — see below}

## Features

{Feature list — reuse and enhance existing feature bullets}

## How It Works

{Brief architecture: LFTP sync engine + web UI + Sonarr/Radarr webhooks}

## Installation

{Docker (primary) and Debian package paths, link to docs site for details}

## Configuration

{Key config areas: remote server, local path, Sonarr/Radarr webhooks, AutoQueue}

## Screenshots

{Reference doc/images/screenshot-dashboard.png inline}

## Related Projects

- [**Triggarr**](https://github.com/thejuran/triggarr) — lightweight search automation daemon for Radarr, Sonarr, and Lidarr. SeedSyncarr handles the download-to-sync side; Triggarr handles the search-to-trigger side.

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for guidelines.

## Security

See [SECURITY.md](SECURITY.md) for reporting vulnerabilities.

## License

Apache License 2.0 — see [LICENSE.txt](LICENSE.txt).
```

4. The Docker Compose block in Quick Start must be:
```yaml
services:
  seedsyncarr:
    image: ghcr.io/thejuran/seedsyncarr:latest
    container_name: seedsyncarr
    restart: unless-stopped
    ports:
      - "8800:8800"
    volumes:
      - ~/.seedsyncarr:/root/.seedsyncarr
      - /path/to/downloads:/downloads
```

5. The badge row MUST include all four badges: CI, Release, Docker, License — in that order, each as a clickable link.

6. Do NOT reference the old seedsync repo or ipsingh06 fork anywhere. This is a standalone project README.

7. The Features section should include: LFTP-based fast transfers, web UI, auto-extraction, AutoQueue pattern matching, Sonarr/Radarr webhook integration, local+remote file management, Docker + Debian packaging, dark mode.
  </action>
  <verify>
    <automated>grep -c "ghcr.io/thejuran/seedsyncarr" README.md && grep -c "triggarr" README.md && grep -c "ci.yml/badge.svg" README.md && grep -c "v/release" README.md && grep -c "screenshot-dashboard" README.md && test -d doc/images</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Sync files from your seedbox" README.md` returns the value prop line
    - `grep "ci.yml/badge.svg" README.md` finds the CI badge
    - `grep "v/release/thejuran/seedsyncarr" README.md` finds the version badge
    - `grep "badge/docker-ghcr.io-blue" README.md` finds the Docker badge
    - `grep "license/thejuran/seedsyncarr" README.md` finds the license badge
    - `grep -i "triggarr" README.md` finds the Related Projects cross-link
    - `grep "screenshot-dashboard.png" README.md` finds the screenshot reference
    - `grep "8800:8800" README.md` finds the Docker Compose port mapping
    - `test -d doc/images` confirms screenshot directory exists
  </acceptance_criteria>
  <done>README.md has value prop above fold, four live-linking badges, Docker Compose Quick Start, screenshot reference, Triggarr cross-link in Related Projects, and no references to old fork</done>
</task>

<task type="auto">
  <name>Task 2: Convert issue templates from Markdown to YAML forms</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md</files>
  <read_first>.github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md</read_first>
  <action>
1. Delete `.github/ISSUE_TEMPLATE/bug_report.md` and `.github/ISSUE_TEMPLATE/feature_request.md` (the old Markdown templates). Both MUST be removed to prevent GitHub showing duplicate template options.

2. Create `.github/ISSUE_TEMPLATE/bug_report.yml` with this exact content:

```yaml
name: Bug Report
description: Report a bug to help us improve SeedSyncarr
title: "[Bug] "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to report a bug.
  - type: dropdown
    id: install-type
    attributes:
      label: Installation type
      options:
        - Docker
        - Debian package
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: SeedSyncarr version
      placeholder: "e.g. 1.0.0"
    validations:
      required: true
  - type: textarea
    id: description
    attributes:
      label: Description
      description: A clear description of the bug
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
  - type: textarea
    id: logs
    attributes:
      label: Logs
      description: "Docker: docker logs seedsyncarr | Debian: journalctl -u seedsyncarr"
      render: shell
```

3. Create `.github/ISSUE_TEMPLATE/feature_request.yml` with this exact content:

```yaml
name: Feature Request
description: Suggest a new feature or improvement for SeedSyncarr
title: "[Feature] "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for suggesting an improvement.
  - type: textarea
    id: problem
    attributes:
      label: Problem or motivation
      description: What problem does this feature solve? Why do you want it?
    validations:
      required: true
  - type: textarea
    id: solution
    attributes:
      label: Proposed solution
      description: How would you like this to work?
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: Any workarounds or alternative approaches you have tried
  - type: textarea
    id: context
    attributes:
      label: Additional context
      description: Any other information, screenshots, or references
```

4. Verify the old .md files are deleted and only .yml files remain in `.github/ISSUE_TEMPLATE/`.
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && test ! -f .github/ISSUE_TEMPLATE/bug_report.md && test ! -f .github/ISSUE_TEMPLATE/feature_request.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/ISSUE_TEMPLATE/bug_report.yml` succeeds
    - `test -f .github/ISSUE_TEMPLATE/feature_request.yml` succeeds
    - `test ! -f .github/ISSUE_TEMPLATE/bug_report.md` succeeds (old template deleted)
    - `test ! -f .github/ISSUE_TEMPLATE/feature_request.md` succeeds (old template deleted)
    - `grep "type: dropdown" .github/ISSUE_TEMPLATE/bug_report.yml` finds the install-type dropdown
    - `grep "type: textarea" .github/ISSUE_TEMPLATE/bug_report.yml` finds structured text fields
    - `grep "render: shell" .github/ISSUE_TEMPLATE/bug_report.yml` finds the logs field with syntax highlighting
  </acceptance_criteria>
  <done>Two YAML form templates replace the old Markdown templates. Bug report has dropdown for install type, required version field, and shell-rendered logs field. Feature request has problem, solution, alternatives, and context fields.</done>
</task>

<task type="auto">
  <name>Task 3: Create CONTRIBUTING.md and CHANGELOG.md</name>
  <files>CONTRIBUTING.md, CHANGELOG.md</files>
  <read_first>SECURITY.md, README.md</read_first>
  <action>
1. Create `CONTRIBUTING.md` with the following content. Keep it practical and concise (no boilerplate walls of text):

```markdown
# Contributing to SeedSyncarr

We welcome contributions! Here is how to get involved.

## Reporting Bugs

Open a [bug report](https://github.com/thejuran/seedsyncarr/issues/new?template=bug_report.yml) using the issue template. Include your installation type, version, and steps to reproduce.

## Requesting Features

Open a [feature request](https://github.com/thejuran/seedsyncarr/issues/new?template=feature_request.yml) describing the problem you want to solve.

## Development Setup

### Prerequisites

- Python 3.12+
- Node.js 22+
- Docker (for running the full stack and E2E tests)

### Getting Started

1. Fork and clone the repository
2. Install Python dependencies: `pip install -r src/python/requirements.txt`
3. Install Angular dependencies: `cd src/angular && npm install`
4. Run Python tests: `make run-tests-python`
5. Run Angular tests: `make run-tests-angular`

### Code Style

- **Python**: Formatted with [ruff](https://docs.astral.sh/ruff/). Run `ruff check src/python` and `ruff format src/python`.
- **Angular/TypeScript**: Linted with ESLint. Run `npm run lint` in `src/angular`.

## Pull Requests

1. Branch off `main`
2. Keep PRs focused — one feature or fix per PR
3. Ensure all tests pass before submitting
4. Describe what changed and why in the PR description

## Code of Conduct

See [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md).

## Security

To report a vulnerability, see [SECURITY.md](SECURITY.md).
```

2. Create `CHANGELOG.md` using Keep a Changelog format. Seed the 1.0.0 entry from the GitHub Release body. Run `gh api repos/thejuran/seedsyncarr/releases --jq '.[0].body'` to get the release body, then format it into the changelog.

The CHANGELOG structure must be:

```markdown
# Changelog

All notable changes to SeedSyncarr are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [1.0.0] - 2026-04-08

### Added

{Extract bullet points from the v1.0.0 release body, organized under Added}

[1.0.0]: https://github.com/thejuran/seedsyncarr/releases/tag/v1.0.0
```

3. If the release body is empty or unavailable, use these key features for the 1.0.0 entry:
   - LFTP-based file synchronization from remote seedbox
   - Web UI for monitoring and controlling transfers
   - Sonarr and Radarr webhook integration for automated media imports
   - AutoQueue with pattern-based file selection
   - Automatic file extraction after sync
   - Docker images for amd64 and arm64
   - Debian package distribution
   - Dark mode UI
   - API token authentication
   - CSP security hardening
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && test -f CHANGELOG.md && wc -l CONTRIBUTING.md | awk '{print ($1 >= 20) ? "PASS" : "FAIL"}' && grep "1.0.0" CHANGELOG.md && echo "ALL PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CONTRIBUTING.md` succeeds
    - `test -f CHANGELOG.md` succeeds
    - `wc -l CONTRIBUTING.md` shows at least 20 lines
    - `wc -l CHANGELOG.md` shows at least 10 lines
    - `grep "Keep a Changelog" CHANGELOG.md` finds the format reference
    - `grep "1.0.0" CHANGELOG.md` finds the first release entry
    - `grep "bug_report.yml" CONTRIBUTING.md` links to the YAML issue template
    - `grep "SECURITY.md" CONTRIBUTING.md` cross-references the security policy
  </acceptance_criteria>
  <done>CONTRIBUTING.md covers bug reports, feature requests, dev setup, code style, and PR process. CHANGELOG.md has a Keep a Changelog formatted 1.0.0 entry seeded from the GitHub Release body. Combined with existing SECURITY.md, all three PRES-09 files exist and are non-empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README badge URLs | External shields.io URLs render in GitHub — no user data flows |
| Issue template forms | GitHub-hosted forms — no custom code execution |
| Docker Compose example | Users copy-paste config — must not suggest insecure defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | Docker Compose example | mitigate | Do not include credentials or real server addresses in the example; use placeholder paths like `/path/to/downloads` |
| T-57-02 | Spoofing | Badge URLs | accept | shields.io is a trusted public service; badge URLs are read-only display; no auth data flows |
| T-57-03 | Tampering | Issue templates | accept | GitHub-hosted YAML forms; no custom processing; GitHub validates schema |
</threat_model>

<verification>
- `grep "Sync files from your seedbox" README.md` returns the value prop
- All four badge patterns exist in README.md (ci.yml/badge.svg, v/release, docker-ghcr.io-blue, license)
- `grep -i triggarr README.md` returns the cross-link
- YAML issue templates exist, old Markdown templates deleted
- CONTRIBUTING.md and CHANGELOG.md exist with meaningful content
- `test -d doc/images` confirms screenshot directory
</verification>

<success_criteria>
- README.md is a complete rewrite with value prop, screenshot, Docker Compose, four badges, feature list, and Triggarr cross-link
- Two YAML form issue templates replace the old Markdown templates
- CONTRIBUTING.md, CHANGELOG.md, and existing SECURITY.md satisfy PRES-09
- No references to old SeedSync fork or ipsingh06 remain in README
</success_criteria>

<output>
After completion, create `.planning/phases/57-readme-community-health/57-01-SUMMARY.md`
</output>
