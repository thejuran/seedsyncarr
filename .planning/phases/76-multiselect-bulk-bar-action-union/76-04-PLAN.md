---
phase: 76-multiselect-bulk-bar-action-union
plan: 04
type: execute
wave: 4
depends_on: [76-03]
files_modified: []
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Full Angular test suite runs to completion with zero failures."
    - "All 5 FIX-01 describe-block tests pass (2 Wave 1 characterization + 3 D-09 D-09 coverage)."
    - "Pre-existing 489+ Angular specs remain green (no regressions)."
    - "Zero visual, string, SCSS, or DOM changes across Phase 76 — confirmed via git diff scoped to template/style files."
    - "Phase goal (union-of-applicable-actions, DELETED-inclusive, per-row dispatch filter) is provably observed by the union of Wave 2 fix + Wave 3 coverage + Wave 1 regression anchor."
    - "A regression-guard report is recorded in 76-04-SUMMARY.md."
  artifacts:
    - path: ".planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md"
      provides: "Final regression-guard report — proof that Phase 76 is done without collateral damage."
      contains: "## Full-Suite Verification"
  key_links:
    - from: "full Angular suite"
      to: "Wave 2 production fix + Wave 1/3 tests"
      via: "karma test run (--watch=false --browsers=ChromeHeadless)"
      pattern: "0 failures"
---

<objective>
Wave 4 — Full regression verification. No code is written in this wave.
The executor runs the complete Angular test suite, confirms zero
regressions, verifies the visual/DOM freeze mandated by D-06/D-07, and
writes a regression-guard report that becomes the phase's shipping proof.

Purpose: Phase 76 is a bug fix that touched one production file and two
test files. Before declaring done, we need (a) a full-suite green run
(not just the one spec file Waves 1-3 exercised), (b) a diff audit
confirming no template/SCSS/string changes leaked in, (c) a durable
summary for the phase SUMMARY chain.

Output: 76-04-SUMMARY.md with the suite-level pass counts, the diff
audit, the list of FIX-01 regression-guard tests, and any observations
to carry forward into Phase 77 (E2E specs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-multiselect-bulk-bar-action-union/76-CONTEXT.md

# Prior-wave summaries — to reference what landed in each wave
@.planning/phases/76-multiselect-bulk-bar-action-union/76-01-SUMMARY.md
@.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md
@.planning/phases/76-multiselect-bulk-bar-action-union/76-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Angular test suite and record the regression-guard report</name>

  <files>.planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md</files>

  <read_first>
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-01-SUMMARY.md (Wave 1 result)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md (Wave 2 fix location + test-green confirmation)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-03-SUMMARY.md (Wave 3 coverage addition)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-CONTEXT.md (§D-06, §D-07 for the visual-freeze assertions)
  </read_first>

  <action>
    Step 1 — Run the full Angular test suite (NOT just one spec file):
    ```bash
    cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless 2>&1 | tee /tmp/76-04-full-suite.txt
    ```
    Extract the total TESTS line (format: `TOTAL: X SUCCESS` or `TOTAL: N FAILED, X SUCCESS`). Record:
    - Total tests executed
    - Passed count
    - Failed count (MUST be 0)
    - Skipped count (if any)

    Step 2 — Locate and list all Phase 76 regression-guard tests:
    ```bash
    grep -n "FIX-01" src/angular/src/app/tests/unittests/pages/files/bulk-actions-bar.component.spec.ts
    ```
    Confirm: exactly one `describe("FIX-01 DELETED union regression"` line
    plus exactly 5 `it("..." FIX-01 ...)` lines (2 Wave 1 characterization +
    3 Wave 3 D-09 coverage).

    Step 3 — Diff audit (visual/DOM freeze check per D-06/D-07). Find the
    commit SHA immediately before Phase 76 started (the `phase: 75`
    completion commit — look in git log for `docs(75): complete phase
    execution` or similar). Let that SHA = `$BEFORE`. Then run:
    ```bash
    git diff --stat $BEFORE HEAD -- \
      'src/angular/src/app/pages/files/bulk-actions-bar.component.html' \
      'src/angular/src/app/pages/files/bulk-actions-bar.component.scss'
    ```
    Expected: EMPTY output — template and SCSS byte-identical.

    Also run:
    ```bash
    git diff $BEFORE HEAD -- 'src/angular/src/app/pages/files/bulk-actions-bar.component.html' | grep -E '^\+\-' | grep -v '^\+\+\+\|^---'
    ```
    Expected: empty.

    Also confirm the Queue button label is intact:
    ```bash
    grep -c '<span>Queue</span>\|>Queue<' src/angular/src/app/pages/files/bulk-actions-bar.component.html
    ```
    Expected: >= 1.

    Step 4 — File-modification audit. List every file changed by Phase 76:
    ```bash
    git diff --name-only $BEFORE HEAD
    ```
    Expected changes (exactly):
    - Exactly ONE production file under `src/angular/src/app/` (the Wave
      2 fix site — whichever of the four D-02 candidates won).
    - `src/angular/src/app/tests/unittests/pages/files/bulk-actions-bar.component.spec.ts`
      (Waves 1 + 3).
    - `.planning/phases/76-multiselect-bulk-bar-action-union/76-0{1,2,3,4}-SUMMARY.md`
    - `.planning/phases/76-multiselect-bulk-bar-action-union/76-0{1,2,3,4}-PLAN.md`
    - Possibly `.planning/STATE.md` / `.planning/ROADMAP.md` (bookkeeping)

    If ANY unexpected file is in the list — especially anything under
    `src/angular/src/app/pages/files/*.html`, `*.scss`, or `common/
    localization.ts` — STOP and flag it in the summary as a D-06/D-07
    violation. Do not proceed to commit.

    Step 5 — Write
    `.planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md`
    with the following structure:

    ```markdown
    # Phase 76 Wave 4 Summary — Full-Suite Verification

    **Status:** Complete
    **Date:** <today>

    ## Full-Suite Verification

    - Command: `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless`
    - Total tests: <N>
    - Passed: <N>
    - Failed: **0**
    - Skipped: <N>

    ## FIX-01 Regression Guard (5 tests)

    All tests inside `describe("FIX-01 DELETED union regression", ...)`:
    1. `alone-DELETED: Queue enabled + onQueueClick emits deleted name (FIX-01)` — PASS (Wave 1 anchor)
    2. `mixed DELETED + DOWNLOADING: union includes Queue for DELETED row (FIX-01)` — PASS (Wave 1 anchor)
    3. `All-DELETED union: Queue + Delete Remote enabled; Stop/Extract/DeleteLocal disabled (FIX-01 D-09 case 1)` — PASS (Wave 3)
    4. `DELETED + DOWNLOADING union: Queue (1) + Stop (1) + DeleteRemote (2); Extract/DeleteLocal disabled (FIX-01 D-09 case 2)` — PASS (Wave 3)
    5. `DELETED + DOWNLOADED + STOPPED union: Queue (2), Extract (1), DeleteLocal (2), DeleteRemote (3); Stop disabled (FIX-01 D-09 case 3)` — PASS (Wave 3)

    ## Visual/DOM Freeze Verification (D-06 / D-07)

    Diff from pre-Phase-76 baseline (`$BEFORE`) to HEAD, scoped to
    template + SCSS:
    - `bulk-actions-bar.component.html`: <empty | any lines>
    - `bulk-actions-bar.component.scss`: <empty | any lines>
    - `Queue` label present: <YES — N occurrences>

    Conclusion: <D-06/D-07 satisfied | VIOLATION>

    ## Files Touched by Phase 76

    <verbatim `git diff --name-only $BEFORE HEAD` output>

    ## ROADMAP Success Criteria — All Met

    1. Selecting a deleted file (alone or mixed) exposes "Re-Queue from Remote" (= Queue dispatched against DELETED) in the bulk-actions bar → **verified by Wave 1 anchor tests + Wave 2 fix**.
    2. Mixed selections show the union of applicable actions → **verified by Wave 3 cases 1, 2, 3**.
    3. Each action dispatches only to rows where it is valid → **verified by Wave 3 per-handler emit assertions**.
    4. Unit tests cover the union logic for at least three representative mixed selections → **verified by Wave 3 3x D-09 cases**.

    ## Phase 77 Handoff Note

    The FIX-01 behavior is now green at the unit-test level. Phase 77
    (UAT-01 scope) will add the Playwright E2E spec that exercises this
    at the browser level per CONTEXT.md §D-11.
    ```

    Step 6 — Commit with message:
    `docs(76): record Wave 4 full-suite verification — FIX-01 shipped`
  </action>

  <verify>
    <automated>cd src/angular &amp;&amp; npm test -- --watch=false --browsers=ChromeHeadless 2&gt;&amp;1 | tee /tmp/76-04-verify.txt &amp;&amp; ! grep -qE 'FAILED|[1-9][0-9]* FAILED' /tmp/76-04-verify.txt &amp;&amp; grep -q 'SUCCESS' /tmp/76-04-verify.txt &amp;&amp; test -f .planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md &amp;&amp; grep -q '^## Full-Suite Verification' .planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md &amp;&amp; grep -q '^## FIX-01 Regression Guard' .planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md &amp;&amp; grep -q '^## Visual/DOM Freeze Verification' .planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless` exits 0.
    - Karma output contains zero `FAILED` lines in the TOTAL summary.
    - `grep -c 'FIX-01' src/angular/src/app/tests/unittests/pages/files/bulk-actions-bar.component.spec.ts` returns >= 6 (1 describe + 5 it names).
    - `git diff --name-only $BEFORE HEAD` (where `$BEFORE` is the phase-75 completion SHA) contains exactly ONE file under `src/angular/src/app/` that is NOT a `.spec.ts` (= the Wave 2 fix site).
    - `git diff $BEFORE HEAD -- 'src/angular/src/app/pages/files/bulk-actions-bar.component.html'` returns empty (grep `-c .` on the diff output returns 0).
    - `git diff $BEFORE HEAD -- 'src/angular/src/app/pages/files/bulk-actions-bar.component.scss'` returns empty.
    - `grep -c 'Queue' src/angular/src/app/pages/files/bulk-actions-bar.component.html` returns the same value it returned at `$BEFORE` (Queue label count unchanged — look up the baseline count and assert equality).
    - `.planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md` exists and contains the four required `##` sections listed in the action.
    - Summary lists 5 FIX-01 test names with PASS status.
    - Commit message matches pattern `docs\(76\).*Wave 4` (grep-verifiable via `git log -1 --format=%s`).
  </acceptance_criteria>

  <done>
    Full Angular suite is green, FIX-01 regression guard lists 5 passing tests, visual/DOM freeze is verified, and the phase SUMMARY is written + committed. Phase 76 is ready to be marked complete in ROADMAP.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries created or modified. This plan runs existing tests
and writes a markdown report.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-01 | N/A | 76-04-SUMMARY.md | accept | No new threat surfaces — verification-only wave, no code, no network paths, no user inputs, no persisted app data. ASVS L1 N/A. |
</threat_model>

<verification>
- `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless` — 0 failures.
- `grep -c 'FIX-01' bulk-actions-bar.component.spec.ts` returns >= 6.
- `git diff $BEFORE HEAD -- '*.html' '*.scss'` scoped to `src/angular/src/app/pages/files/` returns empty.
- 76-04-SUMMARY.md exists with all four required `##` sections.
</verification>

<success_criteria>
Wave 4 ships when:
1. Full Angular test suite runs with 0 failures.
2. 5 FIX-01 tests are enumerated and confirmed passing.
3. D-06/D-07 visual/DOM freeze is verified via git diff.
4. 76-04-SUMMARY.md is committed.
5. Phase 76 is declared shippable.
</success_criteria>

<output>
Final file: `.planning/phases/76-multiselect-bulk-bar-action-union/76-04-SUMMARY.md`.
This file is the phase's shipping proof — references the Wave 2 fix, the 5 FIX-01 guard tests, and the visual-freeze audit.
</output>
