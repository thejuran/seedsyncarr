---
phase: 76-multiselect-bulk-bar-action-union
plan: 02
type: execute
wave: 2
depends_on: [76-01]
files_modified:
  - src/angular/src/app/pages/files/bulk-actions-bar.component.ts
  - src/angular/src/app/services/files/view-file.service.ts
  - src/angular/src/app/services/files/file-selection.service.ts
  - src/angular/src/app/pages/files/transfer-table.component.ts
  - src/angular/src/app/pages/files/transfer-table.component.html
autonomous: false
requirements: [FIX-01]

must_haves:
  truths:
    - "A root-cause note exists in 76-02-SUMMARY.md (single paragraph, max ~200 words) identifying which of the four D-02 candidate sites contains the actual bug, with evidence from the Wave 1 red output and code inspection."
    - "Exactly ONE of the five listed `files_modified` is actually edited. The other four remain byte-identical to their pre-Wave-2 state (confirmed by `git diff --name-only`)."
    - "The edit is minimal per D-03 — no new services, no API changes, no refactors, no new imports unless unavoidable."
    - "The two Wave 1 characterization tests now PASS (green)."
    - "All other previously-passing Angular tests remain green (489+ specs)."
    - "Zero visual, string, or DOM changes — `Queue` button label stays literal `Queue`, no tooltip / icon / badge changes (D-06/D-07)."
  artifacts:
    - path: ".planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md"
      provides: "Root-cause note (Task 1 output) + fix diff summary (Task 2 output) + test-green confirmation."
      contains: "## Root Cause"
    - path: "src/angular/src/app/pages/files/bulk-actions-bar.component.ts OR src/angular/src/app/services/files/view-file.service.ts OR src/angular/src/app/services/files/file-selection.service.ts OR src/angular/src/app/pages/files/transfer-table.component.ts OR .component.html"
      provides: "Minimal production-code edit that makes the FIX-01 characterization tests pass."
      contains: "(file-dependent — see Task 2 action)"
  key_links:
    - from: "Wave 1 red test output"
      to: "the one edited file"
      via: "root-cause analysis (Task 1)"
      pattern: "actionCounts\\.queueable"
    - from: "the edited file"
      to: "bulk-actions-bar.component.spec.ts FIX-01 tests"
      via: "karma test run green"
      pattern: "FIX-01 DELETED union regression"
---

<objective>
Wave 2 — Root-cause discovery + minimal fix. Read the Wave 1 red test
output, inspect the four D-02 candidate sites using the PATTERNS.md
excerpts as a map, identify the single root cause, and apply the smallest
possible edit that drives the FIX-01 characterization tests green WITHOUT
breaking any of the 489+ existing Angular specs.

Purpose: The characterization test from Wave 1 is the contract. This plan
answers "why did it fail?" and "what is the minimum change to make it
pass?" Per D-02 the planner is explicitly forbidden from pre-committing
to a fix site — the root cause is a discovery output of this plan.

Output: (a) root-cause paragraph in 76-02-SUMMARY.md; (b) a one-file edit
to whichever of the listed `files_modified` turned out to be the real
bug site; (c) karma green on the two FIX-01 tests + the full Angular
suite.

**NOTE on autonomous=false:** The root-cause identification step (Task 1)
is a non-trivial discovery. This plan includes a
`checkpoint:human-verify` gate AFTER the root-cause note is written and
BEFORE the fix is applied, so the human can confirm the chosen candidate
is the right one. If the user approves immediately ("proceed"), Task 3
runs without further interaction. This keeps the bug-fix honest without
adding ceremony.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-multiselect-bulk-bar-action-union/76-CONTEXT.md
@.planning/phases/76-multiselect-bulk-bar-action-union/76-PATTERNS.md

# Wave 1 red output — Task 1 MUST start here
@.planning/phases/76-multiselect-bulk-bar-action-union/76-01-SUMMARY.md

# The failing tests — Task 1 reads these to understand the assertions
@src/angular/src/app/tests/unittests/pages/files/bulk-actions-bar.component.spec.ts

# Four D-02 candidate sites — Task 1 inspects all four; Task 3 edits exactly one
@src/angular/src/app/pages/files/bulk-actions-bar.component.ts
@src/angular/src/app/services/files/view-file.service.ts
@src/angular/src/app/services/files/file-selection.service.ts
@src/angular/src/app/pages/files/transfer-table.component.ts
@src/angular/src/app/pages/files/transfer-table.component.html

# View model definitions for reference
@src/angular/src/app/services/files/view-file.ts

<interfaces>
<!-- The four D-02 candidate fix sites, with the specific code region to
     inspect for each. Executor reads each file, finds the region quoted
     here, and decides which one's behavior matches the Wave 1 failure. -->

**Candidate #1 — eligibility flags** (view-file.service.ts §createViewFile
 lines 348-369):
```typescript
const isQueueable: boolean = [ViewFile.Status.DEFAULT,
                            ViewFile.Status.STOPPED,
                            ViewFile.Status.DELETED].includes(status)
                            && remoteSize > 0;
// ... isRemotelyDeletable has same shape, lines 364-369
```
**Failure signature if this is the cause:** DELETED row with remoteSize=0
after null→0 normalization (lines 298-306) produces isQueueable=false.
Wave 1 fixtures bypass this service by setting flags directly, so this
candidate would only show up as a root cause via INTEGRATION — if the
service is producing ViewFiles with isQueueable=false, the bar never
even sees a queueable DELETED row.

**Candidate #2 — selection pruning** (file-selection.service.ts
 `pruneSelection` lines 213-228):
```typescript
pruneSelection(existingFileNames: Set<string>): void {
    // Skip pruning during bulk operations to prevent race conditions
    if (this._operationInProgress()) { return; }
    const currentSet = this.selectedFiles();
    const toRemove = Array.from(currentSet).filter(f => !existingFileNames.has(f));
    // ...
}
```
**Failure signature if this is the cause:** DELETED filenames silently
dropped from `selectedFiles` before the bar reads them. Per CONTEXT
"Existing Code Insights," this method "is not called outside tests" —
should NOT be the cause but verify by grep.

**Candidate #3 — `ngOnChanges` cache-miss** (bulk-actions-bar.component.ts
 lines 74-79 + 86-132):
```typescript
ngOnChanges(changes: SimpleChanges): void {
    if (changes["visibleFiles"] || changes["selectedFiles"]) {
        this._recomputeCachedValues();
    }
}
private _recomputeCachedValues(): void {
    this._cachedSelectedViewFiles = this.visibleFiles
        .filter(f => f.name != null && this.selectedFiles.has(f.name))
        .toArray();
    // ...
}
```
**Failure signature if this is the cause:** visibleFiles and selectedFiles
arrive in separate change-detection ticks, leaving the cache stale. Would
show up in the mixed test but not the alone-DELETED test if the fixture
sets both inputs in one call.

**Candidate #4 — intersection filter** (bulk-actions-bar.component.ts
 lines 88-90):
```typescript
this._cachedSelectedViewFiles = this.visibleFiles
    .filter(f => f.name != null && this.selectedFiles.has(f.name))
    .toArray();
```
**Failure signature if this is the cause:** a selected filename is NOT
present in `visibleFiles` (e.g., user pages, selection straddles pages),
so the intersection returns [], so `actionCounts.queueable === 0`. In
Wave 1 both fixtures explicitly put the DELETED row INTO visibleFiles,
so pure candidate #4 alone should NOT cause Wave 1 tests to fail. If
Wave 1 still fails, inspect for a subtler variant (e.g., `f.name` lookup
failing due to Immutable.List equality semantics, or a falsy `.name`
property on the fixture ViewFile).

**Shared pattern — the dispatch path** (lines 237-242):
```typescript
onQueueClick(): void {
    const files = this.queueableFiles;
    if (files.length > 0) {
        this.queueAction.emit(files);
    }
}
// where this.queueableFiles is derived from _recomputeCachedValues
```
Per D-08 this dispatch MUST NOT be modified. If the root cause is here,
the fix must be upstream in one of the four candidates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause analysis — identify the single bug site</name>

  <files>.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md</files>

  <read_first>
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-01-SUMMARY.md (Wave 1 red output — THE primary input)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-CONTEXT.md (§D-02 "possible root causes" list — the four candidates to evaluate)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-PATTERNS.md (§pattern assignments — line-range map to each candidate's code)
    - src/angular/src/app/tests/unittests/pages/files/bulk-actions-bar.component.spec.ts (the FIX-01 describe block at the end — you must understand what the assertion is actually asserting)
    - src/angular/src/app/pages/files/bulk-actions-bar.component.ts (ALL 283 lines — you must read the entire component once)
    - src/angular/src/app/services/files/view-file.service.ts (lines 297-394 — the createViewFile transform including the five eligibility flags)
    - src/angular/src/app/services/files/view-file.ts (ViewFile class — check that fixture fields used in Wave 1 tests actually map to what _recomputeCachedValues reads)
    - src/angular/src/app/services/files/file-selection.service.ts (lines 1-230 — to confirm pruneSelection is not called in production)
    - src/angular/src/app/pages/files/transfer-table.component.ts (lines 100-260 — wiring between pagedFilesList$ and the bar)
    - src/angular/src/app/pages/files/transfer-table.component.html (lines 170-190 — the bar wiring site)
  </read_first>

  <action>
    Step 1 — Read the Wave 1 output at
    `.planning/phases/76-multiselect-bulk-bar-action-union/76-01-SUMMARY.md`.
    Extract the exact assertion that failed (which expect() fired, what
    the received value was vs the expected value).

    Step 2 — Form a hypothesis. Use this decision tree:
    - If `component.actionCounts.queueable` is `undefined`, fix is likely
      in `_recomputeCachedValues` or `actionCounts` getter (candidate #4).
    - If `component.actionCounts.queueable === 0` despite the Wave 1
      fixture setting `isQueueable: true` AND the file being in
      `visibleFiles`, inspect the intersection at lines 88-90 character-
      by-character — likely a name-equality or `Immutable.List.filter`
      edge case (candidate #4 variant).
    - If `component.queueAction.emit` was called with `[]` (empty array)
      instead of the DELETED filename, the `onQueueClick` guard
      (`if (files.length > 0)`) fires — upstream cache is empty, trace
      back to `_recomputeCachedValues` (candidate #4).
    - If `component.queueAction.emit` was NOT called at all, also
      candidate #4 (the guard skipped dispatch entirely).
    - If the error trace points elsewhere (e.g., `hasSelection` is
      false), re-examine `hasSelection` getter — probably an auxiliary
      cause worth noting, likely still candidate #4.

    Step 3 — VERIFY the hypothesis by inspecting the code region. Cite
    specific line numbers. If the hypothesis doesn't hold, cycle to the
    next candidate.

    Step 4 — Write
    `.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md`
    with this structure:

    ```markdown
    # Phase 76 Wave 2 Summary

    **Status:** Task 1 complete — root cause identified. Awaiting
    checkpoint before Task 3 applies the fix.

    ## Wave 1 Failure Output (verbatim)

    <copy-paste the failing assertion block from 76-01-SUMMARY.md>

    ## Root Cause

    **Candidate:** #<1|2|3|4>
    **File:** <path>
    **Region:** lines <start>-<end>

    <single paragraph, ~150-200 words, explaining:
     - what the code does today
     - why it produces the observed failure given the Wave 1 fixture
     - why the other three candidates are NOT the cause (one sentence each)
     - the minimal edit that will make the test pass>

    ## Proposed Fix

    <a 3-10 line diff showing the exact edit — BEFORE and AFTER>

    ## Impact Scan

    - Other specs that exercise the affected code region:
      <grep result: specs importing or asserting on the affected symbol>
    - Risk of breaking existing behavior: <low|medium|high> — <one sentence>
    - Other production callers of the affected code:
      <grep result>
    ```

    IMPORTANT: Do NOT apply any code edit in Task 1. Only produce the
    root-cause note + proposed diff. The checkpoint after Task 1 gates
    the actual edit.

    If after diligent inspection you CANNOT identify a single root cause
    (e.g., the failure appears to involve two interacting sites), STOP
    and flag this in 76-02-SUMMARY.md under a `## Unable to Isolate`
    section — the human will decide whether to investigate further or
    accept a multi-file fix.
  </action>

  <verify>
    <automated>test -f .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md &amp;&amp; grep -q '^## Root Cause' .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md &amp;&amp; grep -qE '\*\*Candidate:\*\* #[1-4]' .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md &amp;&amp; grep -q '^## Proposed Fix' .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md &amp;&amp; grep -q '^## Impact Scan' .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md` exists.
    - Contains a `## Root Cause` section with `**Candidate:** #N` where N is 1, 2, 3, or 4 (grep-verifiable).
    - Contains a `## Proposed Fix` section with a BEFORE/AFTER diff (look for `-` and `+` markers inside a code fence).
    - Contains a `## Impact Scan` section identifying at least one other spec or caller.
    - No production code (under `src/angular/src/app/`) is modified in this task — `git diff --name-only src/angular/src/app/` returns empty.
    - The root-cause paragraph is between 100 and 300 words (verifiable with `wc -w`).
  </acceptance_criteria>

  <done>
    A root-cause note identifying exactly one of the four D-02 candidates is written to 76-02-SUMMARY.md, with a concrete proposed diff. No code is edited yet. The checkpoint follows.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Human reviews the root-cause note before fix is applied</name>

  <files>.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md</files>

  <read_first>
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md (output of Task 1)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-CONTEXT.md (§D-02, D-03, D-06, D-07)
  </read_first>

  <what-built>
    Task 1 produced a root-cause paragraph in 76-02-SUMMARY.md identifying
    one of the four D-02 candidate fix sites as the actual bug, plus a
    proposed minimal diff.
  </what-built>

  <how-to-verify>
    1. Read `.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md`.
    2. Confirm the `## Root Cause` section identifies a specific candidate (#1-#4) with concrete line numbers.
    3. Confirm the `## Proposed Fix` section has a BEFORE/AFTER diff that:
       - Edits exactly one file.
       - Is minimal (ideally <= 5 changed lines; >= 10 changed lines requires justification in the paragraph).
       - Does NOT change any button label, tooltip, icon, or DOM structure (D-06/D-07).
       - Does NOT add a new service, new imports (unless unavoidable and noted), or new API.
    4. Confirm the `## Impact Scan` lists which other specs/callers exercise the affected code — these are the specs at risk of breakage in Task 3.
    5. Spot-check the hypothesis against the Wave 1 red output: does the explanation match the exact assertion that failed?
  </how-to-verify>

  <action>
    HUMAN REVIEW GATE. The executor pauses here and presents the root-cause
    note (76-02-SUMMARY.md §Root Cause, §Proposed Fix, §Impact Scan) to the
    user. No code is executed until the user responds.

    Acceptable responses:
    - `approved` — Task 3 proceeds with the proposed diff unchanged.
    - `approved with changes: <describe>` — Task 3 proceeds with the
      user's specified modifications applied to the diff.
    - `reject: <reason>` — Task 1 is rerun with the user's feedback; no
      fix is applied.

    Executor MUST NOT make any assumption about the user's response. Wait
    for explicit signal.
  </action>

  <verify>
    <automated>echo "checkpoint:human-verify — gate satisfied by user response; no automated verify"</automated>
  </verify>

  <resume-signal>
    Type "approved" to let Task 3 apply the proposed fix as-is.
    Type "approved with changes: <describe>" to let Task 3 apply a modified fix.
    Type "reject: <reason>" to send Task 1 back for re-analysis.
  </resume-signal>

  <acceptance_criteria>
    - User has provided an explicit resume signal matching one of: `approved`, `approved with changes: ...`, `reject: ...`.
    - No production code was modified during the checkpoint itself.
  </acceptance_criteria>

  <done>
    User has reviewed the root-cause note and issued a resume signal. Task 3 now knows whether to apply the diff verbatim, apply it with modifications, or halt.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply the minimal fix and drive Wave 1 tests green</name>

  <files>
    (Exactly ONE of the following, per the Task 1 root-cause note:)
    - src/angular/src/app/pages/files/bulk-actions-bar.component.ts
    - src/angular/src/app/services/files/view-file.service.ts
    - src/angular/src/app/services/files/file-selection.service.ts
    - src/angular/src/app/pages/files/transfer-table.component.ts
    - src/angular/src/app/pages/files/transfer-table.component.html
  </files>

  <read_first>
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md (Task 1 output — the diff to apply is in `## Proposed Fix`)
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-CONTEXT.md (§D-03 "minimal edit," §D-06/D-07 "no visual changes")
    - .planning/phases/76-multiselect-bulk-bar-action-union/76-PATTERNS.md (the "Pattern Assignments" section for the chosen fix-site file)
    - The one file being edited (full contents, so you see the change in context)
  </read_first>

  <action>
    Step 1 — Open the single file identified in 76-02-SUMMARY.md
    `## Root Cause` → `**File:**`.

    Step 2 — Apply the diff exactly as shown in `## Proposed Fix`, with any
    modifications the human specified in the checkpoint resume-signal. Use
    the Edit tool. Do NOT touch any other file.

    Step 3 — Run the FIX-01 tests:
    `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless --include='**/bulk-actions-bar.component.spec.ts'`
    Confirm both FIX-01 tests now PASS. If they don't, re-read the
    failure output, confirm the edit was applied correctly, and adjust
    ONLY within the same file. If after 2 adjustment attempts the tests
    still fail, STOP and write the failure output to 76-02-SUMMARY.md
    under a `## Fix Failed — Re-open Investigation` section; do not
    continue.

    Step 4 — Run the full Angular test suite:
    `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless`
    Confirm ZERO test regressions beyond the 2 new FIX-01 tests flipping
    from red to green (i.e., pre-Wave-1 passing count + 2 = post-Wave-2
    passing count; failing count = 0).

    Step 5 — Verify no visual/string changes by running:
    - `git diff -- src/angular/src/app/pages/files/bulk-actions-bar.component.html`
      MUST return empty (template unchanged).
    - `git diff --stat` MUST show exactly ONE non-test file changed.
    - `grep -c '>Queue<' src/angular/src/app/pages/files/bulk-actions-bar.component.html`
      MUST return the same value it returned before Wave 2 (Queue label
      unchanged).

    Step 6 — Append to `76-02-SUMMARY.md`:

    ```markdown
    ## Task 3: Fix Applied

    **File changed:** <path>
    **Lines changed:** <summary: e.g., "1 line modified, 0 added, 0 deleted">
    **FIX-01 tests:** PASS (both green)
    **Full Angular suite:** PASS, 0 failures, N passing (was N-2 passing, 2 failing before fix)
    **DOM/visual diff:** none (template + SCSS unchanged)

    <!-- include `git diff <file>` output inline for the record -->
    ```

    Step 7 — Commit with message:
    `fix(76): <concise 1-line fix description> — drives FIX-01 characterization green`

    EXAMPLES of acceptable commit messages (depending on the root cause):
    - `fix(76): include selection ∖ visibleFiles names in union cache — drives FIX-01 characterization green`
    - `fix(76): null-guard DELETED remoteSize in isQueueable predicate — drives FIX-01 characterization green`
    - `fix(76): trigger _recomputeCachedValues on separate-tick input changes — drives FIX-01 characterization green`

    Whatever the root cause, the commit message MUST mention `fix(76)` and
    `FIX-01` and NOT mention "refactor" or "cleanup."
  </action>

  <verify>
    <automated>cd src/angular &amp;&amp; npm test -- --watch=false --browsers=ChromeHeadless --include='**/bulk-actions-bar.component.spec.ts' 2&gt;&amp;1 | tee /tmp/76-02-wave2-output.txt &amp;&amp; grep -q 'FIX-01 DELETED union regression' /tmp/76-02-wave2-output.txt &amp;&amp; ! grep -qE '(FAILED|1 failure|2 failures)' /tmp/76-02-wave2-output.txt</automated>
  </verify>

  <acceptance_criteria>
    - Exactly one production file (not the spec file) under `src/angular/src/app/` is modified — verify with `git diff --name-only HEAD~1 HEAD` returning one entry.
    - Both FIX-01 tests (`alone-DELETED: ...` and `mixed DELETED + DOWNLOADING: ...`) now PASS when running the karma command above.
    - Karma output for the full suite contains zero `FAILED` lines and the total passing count is (pre-Wave-2 passing) + 2.
    - `src/angular/src/app/pages/files/bulk-actions-bar.component.html` is byte-identical to HEAD (before Wave 2) — `git diff HEAD~1 HEAD -- src/angular/src/app/pages/files/bulk-actions-bar.component.html` returns empty.
    - `grep -c '>Queue<\|>Queue</span>' src/angular/src/app/pages/files/bulk-actions-bar.component.html` returns >= 1 (Queue label preserved).
    - `76-02-SUMMARY.md` now contains a `## Task 3: Fix Applied` section with FIX-01 tests PASS and full-suite 0 failures.
    - Commit message matches pattern `fix\(76\).*FIX-01` (grep-verifiable via `git log -1 --format=%s`).
    - No new dependencies added to `src/angular/package.json`.
  </acceptance_criteria>

  <done>
    A single minimal edit to exactly one production file makes the Wave 1 FIX-01 tests green while keeping all 489+ other Angular specs green and preserving the bar's template byte-for-byte. The commit is made.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries are created or modified by this plan. The fix is a
client-side Angular logic correction. No new network paths, no new user
inputs crossed, no new persisted data, no auth changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02-01 | Tampering | one of {bulk-actions-bar.component.ts, view-file.service.ts, file-selection.service.ts, transfer-table.component.{ts,html}} | accept | No new threat surfaces — client-only UI logic fix. The union/eligibility computation is already a trust-untrusted boundary (browser is always untrusted) and the backend (BulkCommandService) still independently validates every action it receives (D-08 dispatch path unchanged). ASVS L1 satisfied. |
| T-76-02-02 | Elevation of Privilege | BulkActionDispatcher (unchanged) | accept | Dispatcher is unchanged per D-08. Any DELETED-action "Re-Queue" request goes to existing server-side Queue endpoint, which already has current auth + authorization. No escalation path added. |
</threat_model>

<verification>
- `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless` — full suite green; FIX-01 tests now pass.
- `git diff --stat HEAD~1 HEAD` — shows exactly one production file modified (plus SUMMARY.md and possibly test file if Wave 1 committed in same branch).
- `git diff HEAD~1 HEAD -- src/angular/src/app/pages/files/bulk-actions-bar.component.html` — empty (no template changes).
- `git diff HEAD~1 HEAD -- src/angular/src/app/pages/files/bulk-actions-bar.component.scss` — empty (no SCSS changes).
- `grep -r 'Queue' src/angular/src/app/pages/files/bulk-actions-bar.component.html` — returns identical output to pre-Wave-2 state (button label unchanged).
</verification>

<success_criteria>
Wave 2 ships when:
1. A root-cause note is committed to 76-02-SUMMARY.md.
2. The proposed fix is reviewed at the checkpoint.
3. Exactly ONE production file is edited with the minimal diff.
4. Both FIX-01 characterization tests from Wave 1 turn green.
5. All other pre-existing Angular specs remain green.
6. Zero template, SCSS, string, icon, or DOM changes (D-06/D-07 preserved).
7. Commit is made.
</success_criteria>

<output>
Task 1 creates `.planning/phases/76-multiselect-bulk-bar-action-union/76-02-SUMMARY.md`.
Task 3 appends a `## Task 3: Fix Applied` section to the same file.
Final file contains: Wave 1 failure recap + Root Cause + Proposed Fix + Impact Scan + Fix Applied + test-green confirmation.
</output>
