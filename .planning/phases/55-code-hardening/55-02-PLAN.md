---
phase: 55-code-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/pages/logs/logs-page.component.ts
  - src/angular/src/app/pages/files/file.component.ts
  - src/angular/src/app/services/base/stream-service.registry.ts
  - src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts
  - src/angular/src/app/tests/unittests/services/files/model-file.spec.ts
  - src/angular/src/app/tests/unittests/services/logs/log-record.spec.ts
  - src/angular/src/app/tests/unittests/services/settings/config.service.spec.ts
  - src/angular/src/app/common/cached-reuse-strategy.ts
  - src/angular/src/app/app.config.ts
  - src/angular/src/app/pages/settings/option.component.ts
  - src/angular/src/app/pages/settings/settings-page.component.ts
  - src/angular/src/app/pages/files/file-list.component.ts
  - src/angular/src/app/services/settings/config.service.ts
  - src/angular/src/app/services/server/server-command.service.ts
  - src/angular/src/app/services/utils/logger.service.ts
  - src/angular/src/app/services/utils/notification.service.ts
  - src/angular/src/app/services/files/view-file.service.ts
  - src/angular/src/app/services/files/view-file.ts
  - src/angular/src/app/services/autoqueue/autoqueue.service.ts
  - src/angular/src/app/tests/mocks/mock-view-file.service.ts
  - src/angular/src/app/tests/mocks/mock-storage.service.ts
  - src/angular/src/app/tests/unittests/services/utils/notification.service.spec.ts
  - src/angular/src/app/tests/unittests/services/files/model-file.service.spec.ts
  - src/angular/src/app/tests/unittests/services/autoqueue/autoqueue.service.spec.ts
  - src/e2e/tests/bulk-actions.spec.ts
  - src/angular/package.json
autonomous: true
requirements:
  - HARD-02
  - HARD-06
  - HARD-01
  - HARD-05

must_haves:
  truths:
    - "npm run lint in src/angular exits 0 with --max-warnings 0 (zero warnings)"
    - "No @typescript-eslint/no-explicit-any warnings remain"
    - "No @typescript-eslint/explicit-function-return-type warnings remain"
    - "No // noinspection comments remain in Angular source"
    - "No planning doc references exist in tracked source files"
    - "Dependabot alerts API returns zero open alerts (or Dependabot is enabled with zero alerts)"
    - "Angular unit tests still pass after type changes"
  artifacts:
    - path: "src/angular/src/app/pages/files/file.component.ts"
      provides: "All 9 @HostBinding getters have : boolean return type"
    - path: "src/angular/src/app/pages/logs/logs-page.component.ts"
      provides: "@ViewChild decorators typed with TemplateRef/ViewContainerRef/ElementRef"
    - path: "src/angular/src/app/services/base/stream-service.registry.ts"
      provides: "SseFrame interface replacing any in SSE observable"
  key_links:
    - from: "src/angular/src/app/**/*.ts"
      to: "npm run lint"
      via: "ESLint validation"
      pattern: "npm run lint.*--max-warnings 0"
    - from: "src/angular/src/app/tests/**/*.spec.ts"
      to: "npm test"
      via: "Karma/Jasmine test runner"
      pattern: "npm test -- --watch=false"
---

<objective>
Fix all 25 ESLint warnings, remove IDE suppression comments and dead code, remove the planning doc reference, and enable Dependabot so the codebase has zero lint warnings and zero open security alerts.

Purpose: HARD-06 (zero lint warnings + zero Dependabot alerts), HARD-02 (no planning doc references), HARD-01 and HARD-05 (remove noinspection IDE comments and commented-out dead code from Angular source).

Output: ESLint exits 0 with --max-warnings 0, all Angular tests pass, Dependabot enabled with zero alerts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-code-hardening/55-RESEARCH.md

<interfaces>
<!-- Key Angular types needed for fixing @ViewChild any types -->
From @angular/core:
```typescript
import { TemplateRef, ViewContainerRef, ElementRef } from '@angular/core';
```

<!-- SSE frame shape used in stream-service.registry.ts -->
The SSE observable emits objects with shape: { event: string, data: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 25 ESLint warnings and remove IDE/dead code comments</name>
  <files>
    - src/angular/src/app/pages/logs/logs-page.component.ts
    - src/angular/src/app/pages/files/file.component.ts
    - src/angular/src/app/services/base/stream-service.registry.ts
    - src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts
    - src/angular/src/app/tests/unittests/services/files/model-file.spec.ts
    - src/angular/src/app/tests/unittests/services/logs/log-record.spec.ts
    - src/angular/src/app/tests/unittests/services/settings/config.service.spec.ts
    - src/e2e/tests/bulk-actions.spec.ts
    - All files with // noinspection comments (27 occurrences across ~22 files)
  </files>
  <read_first>
    - src/angular/src/app/pages/logs/logs-page.component.ts (4 @ViewChild any types at lines 32, 35, 37, 38 + dead code at line 106)
    - src/angular/src/app/pages/files/file.component.ts (9 @HostBinding getters missing return types at lines 111-119)
    - src/angular/src/app/services/base/stream-service.registry.ts (SSE observable any at line 233 + noinspection comments)
    - src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts (any types in test mocks)
    - src/angular/src/app/tests/unittests/services/files/model-file.spec.ts (any types for test fixtures)
    - src/angular/src/app/tests/unittests/services/logs/log-record.spec.ts (any type for log record)
    - src/angular/src/app/tests/unittests/services/settings/config.service.spec.ts (any type for config param)
    - src/e2e/tests/bulk-actions.spec.ts (planning doc reference at line 320)
  </read_first>
  <action>
Fix all 25 ESLint warnings and clean up IDE artifacts. Group by rule:

**Fix 1 — `@typescript-eslint/no-explicit-any` (16 warnings):**

In `logs-page.component.ts`:
- Line 32: Change `templateRecord: any` to `templateRecord!: TemplateRef<unknown>` (add `TemplateRef` to the `@angular/core` import)
- Line 35: Change `container: any` to `container!: ViewContainerRef` (ViewContainerRef already imported or add to import)
- Line 37: Change `logHead: any` to `logHead!: ElementRef` (add `ElementRef` to import)
- Line 38: Change `logTail: any` to `logTail!: ElementRef`

In `stream-service.registry.ts`:
- Line 233: Change `next: (x: any) =>` to `next: (x: { event: string; data: string }) =>`. Then change `x["event"]` to `x.event` and `x["data"]` to `x.data` on subsequent lines.

In `stream-service.registry.spec.ts`:
- Lines 205, 207-210, 212, 218: Replace `any` types with proper Jasmine typed mocks. Read the file first to determine exact types. Use `jasmine.SpyObj<ServiceType>` for spy objects, or `Partial<ConcreteType>` with `as ConcreteType` cast for partial mocks.

In `model-file.spec.ts`:
- Lines 6, 7: Type the JSON fixture against the actual `ModelFile` interface (import it). Use `Partial<ModelFile>` if not all fields are provided.

In `log-record.spec.ts`:
- Line 7: Type the log record against the actual `LogRecord` interface.

In `config.service.spec.ts`:
- Line 147: Type the config parameter against the actual config type from the service.

**Fix 2 — `@typescript-eslint/explicit-function-return-type` (9 warnings):**

In `file.component.ts` lines 111-119, add `: boolean` return type to all 9 `@HostBinding` getter methods. They all follow the pattern:
```typescript
// Before:
@HostBinding("class.status-default") get isStatusDefault() { return this.file?.status === ViewFile.Status.DEFAULT; }
// After:
@HostBinding("class.status-default") get isStatusDefault(): boolean { return this.file?.status === ViewFile.Status.DEFAULT; }
```

**Fix 3 — Remove all `// noinspection` comments (27 occurrences):**

Delete every line containing `// noinspection` from these files:
- `src/app/common/cached-reuse-strategy.ts` (line 13)
- `src/app/app.config.ts` (line 28)
- `src/app/pages/settings/option.component.ts` (line 32)
- `src/app/pages/settings/settings-page.component.ts` (line 93)
- `src/app/pages/files/file-list.component.ts` (line 253)
- `src/app/services/settings/config.service.ts` (lines 20, 143)
- `src/app/services/server/server-command.service.ts` (line 54)
- `src/app/services/utils/logger.service.ts` (line 30)
- `src/app/services/utils/notification.service.ts` (line 20)
- `src/app/services/files/view-file.service.ts` (line 28)
- `src/app/services/files/view-file.ts` (line 71)
- `src/app/services/autoqueue/autoqueue.service.ts` (line 171)
- `src/app/services/base/stream-service.registry.ts` (lines 209, 210, 323)
- `src/app/tests/mocks/mock-view-file.service.ts` (lines 22, 27)
- `src/app/tests/mocks/mock-storage.service.ts` (lines 4, 7, 12)
- `src/app/tests/unittests/services/settings/config.service.spec.ts` (line 15)
- `src/app/tests/unittests/services/utils/notification.service.spec.ts` (lines 76, 97)
- `src/app/tests/unittests/services/files/model-file.service.spec.ts` (line 12)
- `src/app/tests/unittests/services/autoqueue/autoqueue.service.spec.ts` (line 14)

These are IntelliJ/WebStorm IDE suppression comments. The project uses VS Code — they add no value and look generated.

**Fix 4 — Remove commented-out dead code:**

In `logs-page.component.ts` line 106: Delete `// this.logHead.nativeElement.scrollIntoView(true);`

**Fix 5 — Remove planning doc reference (HARD-02):**

In `src/e2e/tests/bulk-actions.spec.ts` line 320: Replace `// See planning docs/BULK_ACTIONS_FIXES.md for details.` with `// Removed: bulk actions only operated on visible files, not all matching.`

After all changes, run:
1. `cd src/angular && npm run lint` — must exit 0 with zero warnings
2. `cd src/angular && npm test -- --watch=false` — must pass (type changes could break tests)
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npm run lint 2>&1 | tail -5 && npm test -- --watch=false 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/angular && npm run lint 2>&1 | grep -c "warning"` returns 0 (no warning lines in output)
    - `cd src/angular && npm run lint` exits 0
    - `cd src/angular && npm test -- --watch=false` exits 0
    - `grep -rn "noinspection" src/angular/src/app/` returns zero lines
    - `grep -rn "planning docs" src/e2e/` returns zero lines
    - `grep -rn "scrollIntoView" src/angular/src/app/pages/logs/logs-page.component.ts` returns zero lines
    - `file.component.ts` contains `get isStatusDefault(): boolean` (and all 8 other getters with `: boolean`)
    - `logs-page.component.ts` contains `TemplateRef<unknown>` and `ElementRef`
    - `stream-service.registry.ts` contains `event: string; data: string` (not `any`)
  </acceptance_criteria>
  <done>All 25 ESLint warnings fixed with proper types (not suppression comments). All 27 noinspection comments removed. Dead code removed. Planning doc reference replaced. Angular tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Dependabot and enforce zero ESLint warnings in CI</name>
  <files>
    - src/angular/package.json
  </files>
  <read_first>
    - src/angular/package.json (current lint script definition)
    - .github/workflows/ci.yml (if exists — check current lint step)
  </read_first>
  <action>
Two final hardening steps:

**Step 1 — Enable Dependabot on the repository:**

Run: `gh api repos/thejuran/seedsyncarr/vulnerability-alerts -X PUT` to enable Dependabot alerts via the GitHub API. Then verify: `gh api repos/thejuran/seedsyncarr/dependabot/alerts --jq 'length'` should return 0 (empty array = zero open alerts). If any alerts appear, triage them — the old repo had overrides for hono already in place, so zero alerts are expected.

If the PUT command fails with 403 or requires different permissions, try enabling via: `gh api -X PUT repos/thejuran/seedsyncarr/vulnerability-alerts` (the endpoint toggles Dependabot vulnerability alerts). If that also fails, document the failure and note that the user must enable Dependabot manually in GitHub Settings > Code security > Dependabot alerts.

**Step 2 — Add `--max-warnings 0` to the lint script:**

In `src/angular/package.json`, find the `"lint"` script in the `"scripts"` section. Append ` --max-warnings 0` to it so that future lint runs fail on any warning. For example, if the current lint script is:
```json
"lint": "eslint src/"
```
Change to:
```json
"lint": "eslint src/ --max-warnings 0"
```

Or if it uses ng lint:
```json
"lint": "ng lint"
```
Check if ng lint supports --max-warnings. If not, add a separate script or modify the CI step instead. Read the file first to determine the exact current script.

After both steps, run the full lint to confirm it still passes with the stricter flag.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npm run lint 2>&1 | tail -5 && echo "LINT OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/angular && npm run lint` exits 0 (now enforcing zero warnings)
    - `gh api repos/thejuran/seedsyncarr/vulnerability-alerts` returns 204 (enabled) OR is documented as requiring manual enablement
    - `src/angular/package.json` lint script contains `--max-warnings 0` (or equivalent enforcement)
  </acceptance_criteria>
  <done>Dependabot enabled (or documented for manual enablement). ESLint zero-warning enforcement added to lint script. Full lint passes with stricter config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface introduced. This plan fixes type annotations and removes comments — no behavioral code changes. Dependabot enablement improves security posture.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Information Disclosure | Dependabot alerts | mitigate | Enable Dependabot to surface vulnerabilities; resolve any open alerts before marking complete |
| T-55-03 | Tampering | TypeScript type changes | accept | Angular test suite validates no behavioral regression from type annotation changes |
</threat_model>

<verification>
After both tasks complete:
1. `cd src/angular && npm run lint` exits 0 with zero warnings
2. `cd src/angular && npm test -- --watch=false` passes
3. `grep -rn "noinspection" src/angular/src/app/` returns zero matches
4. `grep -rn "planning docs" src/e2e/` returns zero matches
5. Dependabot alerts enabled and zero open alerts (or documented for manual action)
</verification>

<success_criteria>
Angular codebase has zero ESLint warnings with strict enforcement. All IDE artifact comments removed. Planning doc reference cleaned. Dependabot enabled. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/55-code-hardening/55-02-SUMMARY.md`
</output>
