---
phase: 81
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - src/python/common/config.py
  - src/python/tests/unittests/test_common/test_config.py
autonomous: true
requirements:
  - SEC-02
tags:
  - security
  - configuration
  - python
  - fernet
user_setup: []

must_haves:
  truths:
    - "A config string without `[Encryption]` loads with `config.encryption.enabled == False` (backward compat)."
    - "A config with `[Encryption] enabled = True` and plaintext secrets loads with plaintext visible in memory and no data loss (plaintext fallback on from_str)."
    - "A config with `[Encryption] enabled = True` and already-encrypted secrets loads with plaintext values in memory (transparent decrypt)."
    - "A `Config` with `encryption.enabled = True` and a keyfile path set produces ciphertext in `to_str()` output for all 5 secret fields (and leaves empty fields empty)."
    - "Flipping `encryption.enabled = False` and calling `to_str()` produces plaintext for the 5 fields (round-trip preservation)."
    - "Every existing test that instantiates `Config()` directly and calls `to_str()`/`to_file()` still passes after the `[Encryption]` section is appended to the serialized output."
  artifacts:
    - path: "src/python/common/config.py"
      provides: "Config.Encryption inner class + [Encryption] section in from_dict/as_dict + encrypt/decrypt hooks in from_str/to_str + Config.set_keyfile_path classmethod + _SECRET_FIELD_PATHS module tuple + _decrypt_errors collection"
      contains: "class Encryption(IC)"
    - path: "src/python/tests/unittests/test_common/test_config.py"
      provides: "5 new test methods + golden-string update + keyfile-aware setUp/tearDown"
      contains: "test_encryption_disabled_by_default"
  key_links:
    - from: "src/python/common/config.py Config.from_str"
      to: "src/python/common/encryption.py decrypt_field"
      via: "import + call per-field when enabled"
      pattern: "from .encryption import"
    - from: "src/python/common/config.py Config.to_str"
      to: "src/python/common/encryption.py encrypt_field"
      via: "import + call per-field when enabled and value is plaintext"
      pattern: "encrypt_field\\("
    - from: "src/python/common/config.py Config.from_dict"
      to: "Config.Encryption with enabled=False default"
      via: "backward-compat branch (mirrors AutoDelete at config.py:429-438)"
      pattern: "\"Encryption\" in config_dict"
    - from: "src/python/tests/unittests/test_common/test_config.py"
      to: "src/python/common/config.py Config.set_keyfile_path"
      via: "setUp calls Config.set_keyfile_path(tempfile_path); tearDown resets to None"
      pattern: "Config\\.set_keyfile_path"
---

<objective>
Widen `Config.from_str` / `Config.to_str` to encrypt/decrypt the 5 secret fields at the serialization seam, add `Config.Encryption` as a backward-compatible optional section, and cover all 5 SEC-02 success criteria with unit tests. This is the core of SEC-02 — after this plan, the crypto is functionally complete in-process; plan 03 only wires startup hooks.

Purpose: Put the encrypt/decrypt transform exactly on the boundary between on-disk bytes and in-memory Python, so rest-of-app callers keep seeing plaintext (criterion #2, "no code changes outside `config.py`").
Output: `config.py` widened with `Encryption` inner class, `_SECRET_FIELD_PATHS` tuple, `Config.set_keyfile_path` classmethod, `Config._decrypt_errors` collection, encrypt/decrypt hooks in `from_str` / `to_str`; `test_config.py` with 5 new test methods + golden-string update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-optional-fernet-encryption-at-rest/81-RESEARCH.md
@.planning/phases/81-optional-fernet-encryption-at-rest/81-PATTERNS.md
@.planning/phases/81-optional-fernet-encryption-at-rest/81-VALIDATION.md
@.planning/phases/81-optional-fernet-encryption-at-rest/81-01-SUMMARY.md
@src/python/common/config.py
@src/python/common/encryption.py
@src/python/common/persist.py
@src/python/tests/unittests/test_common/test_config.py

<interfaces>
<!-- Existing + new contracts. Do NOT re-explore. -->

Public surface from plan 01 (`common/encryption.py`):
```python
def load_or_create_key(keyfile_path: str) -> bytes
def is_ciphertext(s: str) -> bool
def encrypt_field(key: bytes, plaintext: str) -> str
def decrypt_field(key: bytes, token: str) -> str  # raises DecryptionError on InvalidToken
class DecryptionError(AppError)
```

Existing `Config.AutoDelete` inner-class pattern (config.py:323-332) — the closest template because AutoDelete's first field is also `enabled: bool`:
```python
class AutoDelete(IC):
    enabled = PROP("enabled", Checkers.null, Converters.bool)
    dry_run = PROP("dry_run", Checkers.null, Converters.bool)
    delay_seconds = PROP("delay_seconds", Checkers.int_positive, Converters.int)

    def __init__(self):
        super().__init__()
        self.enabled = None
        self.dry_run = None
        self.delay_seconds = None
```

Existing backward-compat branch (config.py:429-438) — the canonical `enabled = False` default when the INI section is missing:
```python
if "AutoDelete" in config_dict:
    config.autodelete = Config.AutoDelete.from_dict(
        Config._check_section(config_dict, "AutoDelete")
    )
else:
    config.autodelete.enabled = False
    config.autodelete.dry_run = False
    config.autodelete.delay_seconds = 60
```

Existing serialization seam (config.py:358-389):
```python
@classmethod
@overrides(Persist)
def from_str(cls, content: str) -> "Config":
    config_parser = configparser.ConfigParser()
    try:
        config_parser.read_string(content)
    except (configparser.MissingSectionHeaderError, configparser.ParsingError) as e:
        raise PersistError(...)
    config_dict = {}
    for section in config_parser.sections():
        config_dict[section] = {}
        for option in config_parser.options(section):
            config_dict[section][option] = config_parser.get(section, option)
    return Config.from_dict(config_dict)

@overrides(Persist)
def to_str(self) -> str:
    config_parser = configparser.ConfigParser()
    config_dict = self.as_dict()
    for section in config_dict:
        config_parser.add_section(section)
        section_dict = config_dict[section]
        for key in section_dict:
            config_parser.set(section, key, str(section_dict[key]))
    str_io = StringIO()
    config_parser.write(str_io)
    return str_io.getvalue()
```

Existing `_strtobool` helper (config.py:12-26) — DO use this verbatim for reading `[Encryption].enabled`; do NOT import `distutils.util.strtobool` (removed in Python 3.12).

New module-level tuple to add near the top of config.py (after `_strtobool`):
```python
# Format: (attr_name_on_Config, field_name, INI_section_name)
# INI sections are TitleCase; Config attrs are lowercase.
_SECRET_FIELD_PATHS = (
    ("general", "webhook_secret", "General"),
    ("general", "api_token", "General"),
    ("lftp", "remote_password", "Lftp"),
    ("sonarr", "sonarr_api_key", "Sonarr"),
    ("radarr", "radarr_api_key", "Radarr"),
)
```
</interfaces>

<nyquist_test_commands>
Per 81-VALIDATION.md Task IDs 81-02-01 through 81-02-04 — test method names below are the validation contract:
- `test_enable_new_install_encrypts_on_write` (81-02-01, SEC-02 #1b)
- `test_from_file_enabled_decrypts` (81-02-02, SEC-02 #2)
- `test_encryption_disabled_by_default` (81-02-03, SEC-02 #3b)
- `test_disable_restores_plaintext` (81-02-04, SEC-02 #4)

Plus one bonus test for the plaintext-fallback-on-read branch (criterion #3 in-memory half):
- `test_from_str_enabled_with_plaintext_falls_back` (preserves plaintext in memory; re-encrypt on write is plan 03's startup hook)
</nyquist_test_commands>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Config.Encryption inner class, _SECRET_FIELD_PATHS, set_keyfile_path, _decrypt_errors — no seam changes yet</name>
  <files>src/python/common/config.py</files>
  <read_first>
    - src/python/common/config.py (ENTIRE file — you need exact line numbers for AutoDelete class ~323-332, Config.__init__ ~334-342, from_dict backward-compat ~411-438, as_dict ~443-455)
    - 81-PATTERNS.md §`src/python/common/config.py` (quotes exact analog blocks + insertion points)
    - 81-RESEARCH.md §6.3 "Pattern 1: Serialization-Seam Encryption" + §6.5 "Pattern 3: Backward-Compatible Optional Section"
    - 81-RESEARCH.md Pitfall 8.6 (`Config.from_dict` is used in tests directly — default-value drift is the primary test-breaking risk)
  </read_first>
  <behavior>
    - New module-level tuple `_SECRET_FIELD_PATHS` exists with exactly 5 entries in the order specified in the interfaces block above.
    - New `Config.Encryption(IC)` inner class with single field `enabled = PROP("enabled", Checkers.null, Converters.bool)`, `__init__` sets `self.enabled = None` (matches AutoDelete:328-332).
    - `Config.__init__` registers `self.encryption = Config.Encryption()` at the end of the existing inner-class registration block.
    - `Config.__init__` also initializes `self._decrypt_errors: list[str] = []` — list of "Section.field" strings populated during `from_str` when a value looks like ciphertext but fails to decrypt (read by plan 03's startup hook).
    - Class-level attr `_keyfile_path: Optional[str] = None`.
    - Classmethod `Config.set_keyfile_path(cls, path: Optional[str]) -> None` that assigns `cls._keyfile_path = path`. Docstring explains: "Inject the keyfile path before any from_file/to_file call when encryption may be enabled. Pass `None` to reset (test isolation)."
    - `Config.from_dict` gains a backward-compat branch for `[Encryption]` at the tail (after the existing AutoDelete block at ~438): `if "Encryption" in config_dict: config.encryption = Config.Encryption.from_dict(Config._check_section(config_dict, "Encryption"))` else `config.encryption.enabled = False`.
    - `Config.as_dict` appends `config_dict["Encryption"] = self.encryption.as_dict()` at the end (after `config_dict["AutoDelete"]`).
    - No import of `common.encryption` yet — no seam changes in this task. Defer to task 2.
    - `config.encryption` is a full `Config.Encryption` instance on every constructed Config (never None).
  </behavior>
  <action>
    Edit `src/python/common/config.py`:

    1. Near the top of the file (after the `_strtobool` helper at lines ~12-26), add the module-level tuple `_SECRET_FIELD_PATHS` with exactly the 5 entries from the interfaces block above.

    2. Add `Config.Encryption` as the last inner class before the `__init__` method (insert right after `Config.AutoDelete` ends around line 332). Match the AutoDelete pattern verbatim but with one field only:

       ```python
       class Encryption(IC):
           enabled = PROP("enabled", Checkers.null, Converters.bool)

           def __init__(self):
               super().__init__()
               self.enabled = None
       ```

    3. In `Config.__init__` (around lines 334-342), append `self.encryption = Config.Encryption()` after `self.autodelete = Config.AutoDelete()`. Also append `self._decrypt_errors: list[str] = []`.

    4. Add class-level attr `_keyfile_path: Optional[str] = None` above `__init__` (type-annotated at class body). Import `Optional` from `typing` if not already imported (check existing imports first).

    5. Add the `set_keyfile_path` classmethod just after `__init__`:

       ```python
       @classmethod
       def set_keyfile_path(cls, path: Optional[str]) -> None:
           """
           Inject the keyfile path before any from_file/to_file call when encryption
           may be enabled. Pass None to reset (test isolation).
           """
           cls._keyfile_path = path
       ```

    6. In `Config.from_dict` (around lines 411-438), add the `[Encryption]` branch AFTER the existing `[AutoDelete]` branch, following the exact same shape:

       ```python
       # Encryption section is optional for backward compatibility (SEC-02 criterion #3)
       if "Encryption" in config_dict:
           config.encryption = Config.Encryption.from_dict(
               Config._check_section(config_dict, "Encryption")
           )
       else:
           # Default values for existing installs missing [Encryption] section
           config.encryption.enabled = False
       ```

    7. In `Config.as_dict` (around lines 443-455), append `config_dict["Encryption"] = self.encryption.as_dict()` as the final entry.

    8. Do NOT import `common.encryption` in this task. Do NOT modify `from_str` or `to_str`. Those are Task 2.
  </action>
  <verify>
    <automated>cd src/python && python3 -c "from common.config import Config, _SECRET_FIELD_PATHS; c = Config(); assert c.encryption.enabled is None; assert c._decrypt_errors == []; assert Config._keyfile_path is None; Config.set_keyfile_path('/tmp/foo'); assert Config._keyfile_path == '/tmp/foo'; Config.set_keyfile_path(None); assert len(_SECRET_FIELD_PATHS) == 5; assert _SECRET_FIELD_PATHS[0] == ('general', 'webhook_secret', 'General'); print('ok')"</automated>
  </verify>
  <done>
    `Config.Encryption` inner class exists with single `enabled` field defaulting to `None`. `_SECRET_FIELD_PATHS` has 5 entries in the documented order. `Config.set_keyfile_path` classmethod exists and mutates class-level `_keyfile_path`. `Config._decrypt_errors` is an empty list on every fresh Config. `as_dict()` emits `"Encryption"` key. `from_dict` applies `enabled = False` default when the section is missing. No seam changes yet — `from_str` / `to_str` unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen Config.from_str / Config.to_str with encrypt/decrypt hooks; add _decrypt_errors population</name>
  <files>src/python/common/config.py</files>
  <read_first>
    - src/python/common/config.py (your Task 1 edits MUST be in place first)
    - 81-PATTERNS.md §`src/python/common/config.py` "Decrypt hook location" and "Encrypt hook location" (exact line refs + hook placement)
    - 81-RESEARCH.md §6.3 "Pattern 1: Serialization-Seam Encryption" (from_str/to_str integration sketch, lines 208-247)
    - 81-RESEARCH.md §9.3 "Widened Config.from_str" (the `Config.set_keyfile_path` injection pattern)
    - 81-RESEARCH.md Pitfall 8.4 (keyfile-missing + existing-ciphertext refusal — do NOT auto-create a key when existing values are ciphertext)
    - 81-RESEARCH.md §6.3 "only encrypt non-empty" — empty-string fields stay empty, never become ciphertext of the empty string.
  </read_first>
  <behavior>
    - `Config.from_str` (widen existing method at ~358-376):
        1. After `config_dict` is fully built but BEFORE `Config.from_dict(config_dict)` is called, inspect `config_dict.get("Encryption", {}).get("enabled", "False")` via `_strtobool`.
        2. If `encryption_enabled AND Config._keyfile_path is not None`:
            a. Pitfall 8.4 guard: if keyfile does NOT exist on disk AND any of the 5 fields in `config_dict` has a value where `is_ciphertext(value) is True`, DO NOT call `load_or_create_key` (that would generate a new key and orphan the existing ciphertext). Instead, append a `_decrypt_errors` entry for each such field (stored on a module-level temporary list — the `Config` instance doesn't exist yet at this point; use a local list and pass it into `from_dict` or store on the returned Config after construction). Simpler path: do the decrypt walk AFTER `Config.from_dict` so we have the instance; but the field values must be decrypted BEFORE `from_dict` so the PROP/Checker/Converter validation sees plaintext. Resolution: collect errors in a local list, call `from_dict`, then attach `config._decrypt_errors.extend(local_errors)` before returning.
            b. Call `key = load_or_create_key(Config._keyfile_path)` ONLY if the keyfile exists OR none of the 5 fields are ciphertext-shaped (first-enable case, plaintext everywhere — safe to create key).
            c. Walk `_SECRET_FIELD_PATHS`; for each `(attr_name, field_name, ini_section)`:
                - If `ini_section in config_dict AND field_name in config_dict[ini_section]`:
                    - `value = config_dict[ini_section][field_name]`
                    - If `is_ciphertext(value)`:
                        - Try `decrypt_field(key, value)` → replace with plaintext.
                        - On `DecryptionError`: leave value as-is (it will not decrypt; keeping it preserves data that the PROP may or may not validate), append `f"{ini_section}.{field_name}"` to the local errors list.
                    - Else: leave as-is (plaintext fallback — value stays plaintext in memory, re-encryption happens on next `to_str` if flag stays enabled).
        3. Call `Config.from_dict(config_dict)`.
        4. Attach `config._decrypt_errors = local_errors`.
        5. Return `config`.
    - `Config.to_str` (widen existing method at ~378-389):
        1. After `config_dict = self.as_dict()` (existing line ~381), if `self.encryption.enabled AND Config._keyfile_path is not None`:
            a. `key = load_or_create_key(Config._keyfile_path)`.
            b. Walk `_SECRET_FIELD_PATHS`; for each `(_, field_name, ini_section)`:
                - If `ini_section in config_dict AND field_name in config_dict[ini_section]`:
                    - `value = config_dict[ini_section][field_name]`
                    - If `value AND not is_ciphertext(value)`:
                        - `config_dict[ini_section][field_name] = encrypt_field(key, value)`
                    - Empty-string values remain empty. Already-ciphertext values remain untouched (idempotent).
        2. Continue with the existing serialization loop.
    - Raise `ConfigError` if `self.encryption.enabled is True` but `Config._keyfile_path is None` at `to_str` time — message: "Encryption enabled but no keyfile path set. Call Config.set_keyfile_path() before saving." (Prevents silent no-op writes per Pitfall 8.6.)
    - Imports added at top of config.py: `from .encryption import load_or_create_key, is_ciphertext, encrypt_field, decrypt_field, DecryptionError` (top-level is fine because plan 01's module has no side-effect imports).
  </behavior>
  <action>
    Edit `src/python/common/config.py`:

    1. Top-of-file imports: add `from .encryption import load_or_create_key, is_ciphertext, encrypt_field, decrypt_field, DecryptionError`.

    2. Modify `Config.from_str` (around lines 358-376):
       - After the `config_dict` build loop completes and BEFORE `return Config.from_dict(config_dict)`:
         - Insert the encryption-aware decrypt pass exactly as specified in the Behavior section. Use `_strtobool(config_dict.get("Encryption", {}).get("enabled", "False"))` to resolve the flag.
         - Implement the Pitfall 8.4 guard: check `os.path.isfile(Config._keyfile_path)` before key load; if keyfile is missing AND any of the 5 fields has a ciphertext-shaped value, skip the `load_or_create_key` call and treat every ciphertext-shaped field as a decrypt error. Requires `import os` at module level (likely already imported — verify first).
         - Collect decrypt errors in a local `list` named `_decrypt_errors_local`.
         - Replace `return Config.from_dict(config_dict)` with:
           ```python
           config = Config.from_dict(config_dict)
           config._decrypt_errors.extend(_decrypt_errors_local)
           return config
           ```

    3. Modify `Config.to_str` (around lines 378-389):
       - Right after `config_dict = self.as_dict()`, insert the encryption-aware encrypt pass.
       - Before entering the pass, guard: if `self.encryption.enabled is True` AND `Config._keyfile_path is None`, `raise ConfigError("Encryption enabled but no keyfile path set. Call Config.set_keyfile_path() before saving.")`. Match the existing `ConfigError` import at the top of config.py.

    4. Ensure the `[Encryption]` section itself is NEVER encrypted — skip it in the walk (it's not in `_SECRET_FIELD_PATHS`, so the existing logic already skips it, but add a code comment to make this explicit).

    5. Verify the `ConfigError` class is accessible from the `to_str` method — it's defined at config.py:28-32, same module, no import needed.
  </action>
  <verify>
    <automated>cd src/python && python3 -c "
import tempfile, os
from common.config import Config
from common.encryption import is_ciphertext

# Round-trip test: encrypt then decrypt via Config serialization
tmpdir = tempfile.mkdtemp()
keyfile = os.path.join(tmpdir, 'secrets.key')
Config.set_keyfile_path(keyfile)

# Build a minimal config with encryption enabled + plaintext secret
c = Config()
# Set sane defaults for every field that has a non-null checker
c.general.debug = False
c.general.verbose = False
c.general.webhook_secret = 'my_webhook'
c.general.api_token = 'my_token'
c.general.allowed_hostname = ''
c.lftp.remote_address = 'host'
c.lftp.remote_username = 'user'
c.lftp.remote_password = 'my_pass'
c.lftp.remote_port = 22
c.lftp.remote_path = '/'
c.lftp.local_path = '/'
c.lftp.remote_path_to_scan_script = '/'
c.lftp.use_ssh_key = False
c.lftp.num_max_parallel_downloads = 1
c.lftp.num_max_parallel_files_per_download = 1
c.lftp.num_max_connections_per_root_file = 1
c.lftp.num_max_connections_per_dir_file = 1
c.lftp.num_max_total_connections = 1
c.controller.use_local_path_to_check_for_extracts = False
c.controller.extract_path = '/'
c.controller.use_remote_path_to_check_for_extracts = False
c.web.port = 8800
c.autoqueue.enabled = False
c.autoqueue.patterns_file = 'x'
c.autoqueue.patterns = ''
c.sonarr.enabled = False
c.sonarr.sonarr_host = ''
c.sonarr.sonarr_api_key = 'my_sonarr_key'
c.sonarr.sonarr_port = 8989
c.sonarr.sonarr_https = False
c.sonarr.detect_imports = False
c.radarr.enabled = False
c.radarr.radarr_host = ''
c.radarr.radarr_api_key = 'my_radarr_key'
c.radarr.radarr_port = 7878
c.radarr.radarr_https = False
c.radarr.detect_imports = False
c.autodelete.enabled = False
c.autodelete.dry_run = False
c.autodelete.delay_seconds = 60
c.encryption.enabled = True

serialized = c.to_str()
# All 5 secret fields should now start with gAAAAA
assert 'gAAAAA' in serialized, 'No Fernet token produced'

# Parse back: values should be plaintext in memory
c2 = Config.from_str(serialized)
assert c2.general.api_token == 'my_token', f'decrypt failed: {c2.general.api_token}'
assert c2.lftp.remote_password == 'my_pass'
assert c2.general.webhook_secret == 'my_webhook'
assert c2.sonarr.sonarr_api_key == 'my_sonarr_key'
assert c2.radarr.radarr_api_key == 'my_radarr_key'
Config.set_keyfile_path(None)
print('ok')
"</automated>
  </verify>
  <done>
    Round-trip `Config -> to_str -> from_str -> Config` preserves the 5 plaintext secret values across encryption. `to_str()` output contains `gAAAAA` prefixes for the 5 fields. `from_str` raises nothing on a valid ciphertext config. `Config._decrypt_errors` is populated (empty list) on clean loads. Raising `ConfigError` when `encryption.enabled=True` and `_keyfile_path is None` is verified by sight-reading the code path — explicit unit test lands in Task 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 5 SEC-02 test methods to test_config.py + update test_to_file golden-string for [Encryption] section</name>
  <files>src/python/tests/unittests/test_common/test_config.py</files>
  <read_first>
    - src/python/tests/unittests/test_common/test_config.py (ENTIRE file — you need `test_to_file` at ~479, `test_from_file` at ~390-430, and the existing golden-string block at ~526-580 which MUST be updated)
    - 81-PATTERNS.md §`test_config.py` (exact test-method list with criterion mapping)
    - 81-VALIDATION.md Task IDs 81-02-01 through 81-02-04 (authoritative test method names — preserve verbatim)
    - 81-RESEARCH.md §9.5 (test skeletons for the 5 encryption tests)
    - 81-RESEARCH.md Pitfall 8.6 (the `test_to_file` golden-string update is called out explicitly here — every existing test that calls `to_str()`/`to_file()` must accept `[Encryption]\nenabled = False` appended to the output)
    - src/python/common/encryption.py (the primitive helpers from plan 01)
    - src/python/common/config.py (your Task 1 + Task 2 edits must be in place)
  </read_first>
  <behavior>
    - CRITICAL golden-string update: `test_to_file` at test_config.py:~479-580 currently compares the serialized output line-by-line against a golden INI string that ends at `[AutoDelete]`. After Task 1 added `[Encryption]` to `as_dict`, the actual output now has two extra lines (`[Encryption]` and `enabled = False`). Update the golden string to append these 2 lines. Test must pass without any production-code change to this method.

    - If any OTHER existing test builds a `Config()` directly and calls `to_str()` / `to_file()` (grep for `.to_str()` and `.to_file(` in test_config.py), those outputs now include the `[Encryption]` section. Update each affected golden string.

    - 5 new test methods inside `TestConfig` (preserve names — they are the 81-VALIDATION.md contract):
        1. `test_encryption_disabled_by_default` (81-02-03, SEC-02 #3b): Build an INI string WITHOUT any `[Encryption]` section but otherwise full (copy the format from the existing `test_from_file` body). Call `Config.from_str(content)`. Assert `config.encryption.enabled is False`. Assert `config._decrypt_errors == []`. This test MUST NOT call `set_keyfile_path` — it proves backward-compat for installs that never heard of encryption.
        2. `test_enable_new_install_encrypts_on_write` (81-02-01, SEC-02 #1b): In `setUp`-equivalent (see keyfile setUp below), build a full `Config()` with `encryption.enabled = True` and 5 plaintext secrets. Call `c.to_str()`. Parse the result with `configparser.ConfigParser()`. Assert each of the 5 fields (`General.webhook_secret`, `General.api_token`, `Lftp.remote_password`, `Sonarr.sonarr_api_key`, `Radarr.radarr_api_key`) starts with `gAAAAA`. Assert the keyfile was created at the tempfile path.
        3. `test_from_file_enabled_decrypts` (81-02-02, SEC-02 #2): In setUp, create a keyfile via `load_or_create_key(self.keyfile)` and `encrypt_field(key, "known_value")` for each of the 5 secrets. Write an INI to a tempfile with `[Encryption]\nenabled = True` and the 5 encrypted values. Call `Config.from_file(path)`. Assert all 5 fields come back as `"known_value"` (or the specific per-field value). Assert `config._decrypt_errors == []`.
        4. `test_disable_restores_plaintext` (81-02-04, SEC-02 #4): Start from a Config with `encryption.enabled = True` and 5 plaintext secrets; call `to_str()` → get encrypted output; parse back → in-memory plaintext; flip `c2.encryption.enabled = False`; call `c2.to_str()` → assert the 5 fields appear in plaintext in the output (NO `gAAAAA` prefix).
        5. `test_from_str_enabled_with_plaintext_falls_back` (plaintext-fallback half of criterion #3; plan 03's startup hook does the re-encrypt): In setUp, create a keyfile. Build an INI with `[Encryption]\nenabled = True` AND plaintext values in the 5 fields. Call `Config.from_str(content)`. Assert the 5 fields are the plaintext values. Assert `config._decrypt_errors == []` (plaintext is not a decrypt error — it's a "not yet encrypted" state).

    - Plus one defensive test:
        6. `test_to_str_raises_when_enabled_without_keyfile_path`: Build `c` with `encryption.enabled = True`, call `Config.set_keyfile_path(None)` to reset, assert `c.to_str()` raises `ConfigError` with message containing "keyfile path".

    - Keyfile-aware setUp/tearDown: Because tests 2, 3, 4, 5 need `Config._keyfile_path` set, and class-level state is a test-isolation hazard, wrap them in a dedicated nested `TestConfigEncryption(unittest.TestCase)` class in the same file, OR add `setUp` / `tearDown` on the existing `TestConfig` class that set and reset `_keyfile_path`. Chosen approach per PATTERNS §`test_config.py`: add `setUp` that creates a tempdir + keyfile path + calls `Config.set_keyfile_path(self.keyfile)`, and `tearDown` that calls `Config.set_keyfile_path(None)` + `shutil.rmtree(self.temp_dir)`. If the existing `TestConfig` already has `setUp`/`tearDown`, extend them; otherwise add them.
  </behavior>
  <action>
    Edit `src/python/tests/unittests/test_common/test_config.py`:

    1. Imports: ensure `tempfile`, `shutil`, `os`, `sys` are imported. Add `from common.encryption import load_or_create_key, encrypt_field, is_ciphertext`. Add `from common.config import Config, ConfigError` (ConfigError may already be imported — check first).

    2. Locate `test_to_file` (~line 479). Read its golden-string block (~526-580). Append to the golden string:
       ```
       [Encryption]
       enabled = False
       ```
       Keep the existing line-by-line stripped-compare loop; it will work unchanged because it filters blank lines.

    3. Search the rest of the file for other `.to_str()` or `.to_file(` calls whose output is string-compared. Update any affected goldens with the 2 extra lines. If the file only uses `to_str` for assertion of SOME content (not full-line compare), no update needed.

    4. Add/extend `setUp` and `tearDown` on `TestConfig`:
       ```python
       def setUp(self):
           self.temp_dir = tempfile.mkdtemp(prefix="test_config_enc")
           self.keyfile = os.path.join(self.temp_dir, "secrets.key")
           Config.set_keyfile_path(self.keyfile)
       def tearDown(self):
           Config.set_keyfile_path(None)
           shutil.rmtree(self.temp_dir)
       ```
       IMPORTANT: if `TestConfig` already has setUp/tearDown (read carefully before editing), merge rather than replace. Preserve any existing setup logic.

    5. Append the 6 new test methods to `TestConfig` (names exactly as in Behavior section). Each test is self-contained:
       - For the "build a full Config" tests, factor out a `_build_plaintext_config(self) -> Config` helper that returns a `Config()` with all non-nullable fields populated (same pattern as the existing `test_to_file`). Place this helper on the `TestConfig` class.
       - Test 6 (`test_to_str_raises_when_enabled_without_keyfile_path`) explicitly calls `Config.set_keyfile_path(None)` AFTER the default setUp ran — this is intentional isolation.

    6. Do NOT modify any other existing test. If any other test fails after the golden-string update, fix it by adding the `[Encryption]\nenabled = False` lines to its specific golden — do not modify production code to work around it.
  </action>
  <verify>
    <automated>cd src/python && poetry run pytest tests/unittests/test_common/test_config.py -x -v</automated>
  </verify>
  <done>
    `test_config.py` has 6 new test methods (`test_encryption_disabled_by_default`, `test_enable_new_install_encrypts_on_write`, `test_from_file_enabled_decrypts`, `test_disable_restores_plaintext`, `test_from_str_enabled_with_plaintext_falls_back`, `test_to_str_raises_when_enabled_without_keyfile_path`) — all pass. The existing `test_to_file` and any other golden-comparing tests pass with the `[Encryption]\nenabled = False` suffix. The entire `test_config.py` module is green. SEC-02 criteria #1b, #2, #3b, #4 are covered at the config.py layer; criterion #3a (startup re-encrypt) and #5b (decrypt warning) remain for plan 03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk ↔ Config | `settings.cfg` is parsed by `configparser` into `config_dict` (dict[str, dict[str, str]]). The 5 secret fields cross this boundary; all other fields are untrusted-but-validated by existing Checkers. |
| Config ↔ rest-of-app | In-memory `config.general.api_token` etc. are plaintext. Downstream callers (web serializer, webhook HMAC, lftp process) trust the value. No change to this trust level. |
| Config class-level state | `Config._keyfile_path` is class-level; cross-test pollution risk. Mitigated by `Config.set_keyfile_path(None)` in tearDown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-01 | Information Disclosure | Plaintext secret survives encrypted write | mitigate | `to_str` walks `_SECRET_FIELD_PATHS` and encrypts every non-empty, non-ciphertext value when `enabled=True`. Idempotent (already-ciphertext values skipped). |
| T-81-02-02 | Information Disclosure | Downgrade attacker flips flag to False, writes plaintext to disk | accept | Attacker with write access to settings.cfg already has full access; disabling encryption via the flag is the documented "allow manual editing" use case (criterion #4). Threat model excludes local-filesystem write attacker. |
| T-81-02-03 | Data loss | User deletes keyfile, startup auto-creates new key, orphans existing ciphertext (Pitfall 8.4) | mitigate | `from_str` refuses to call `load_or_create_key` when keyfile is absent AND any of the 5 fields is ciphertext-shaped. Instead, appends each to `_decrypt_errors` for plan 03's warning. No silent data loss. |
| T-81-02-04 | Tampering | Ciphertext corruption in settings.cfg | mitigate | Fernet HMAC-SHA256 detects bit-flip → `DecryptionError` → collected in `_decrypt_errors` → plan 03 logs warning. Field left as-is in config_dict (not overwritten). |
| T-81-02-05 | Data integrity | `encryption.enabled=True` but `_keyfile_path is None` at save time (pitfall 8.6) | mitigate | `to_str` raises `ConfigError` with clear message. Prevents silent no-op that would leave plaintext on disk while UI says "encrypted". |
| T-81-02-06 | Information Disclosure | `ConfigError` message leaks secret value | mitigate | Error message is a fixed string; no secret content interpolated. Existing `context.py` redactor covers the field names for log output. |
| T-81-02-07 | Information Disclosure | `[Encryption]` section itself accidentally encrypted | accept | Explicitly excluded from `_SECRET_FIELD_PATHS`. Code comment added. The `enabled` boolean is not a secret. |
| T-81-02-08 | Denial of Service | `is_ciphertext` called on every field during load | accept | O(n) per-field cost; only 5 fields; per-call overhead negligible (<1ms). Length gate short-circuits for short values. |
| T-81-02-09 | Information Disclosure | Test pollution — `_keyfile_path` leaks between tests | mitigate | tearDown resets class-level state to `None`. Cross-test failures would surface immediately in CI. |

ASVS coverage: V6 Cryptography (Fernet at serialization seam), V8 Data Protection (secrets in settings.cfg), V14 Configuration (backward-compat default).
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/unittests/test_common/test_config.py -x -v` — all existing + 6 new tests green.
- `cd src/python && poetry run pytest tests/unittests/test_common/ -x` — entire common/ unit suite green (no collateral damage to test_persist, test_context, etc.).
- `cd src/python && poetry run pytest tests/unittests/ -x` — full unit suite green. Any failure outside `test_common/` indicates a missed golden-string update.
- Integration: `python3 -c "<the to_str/from_str round-trip from Task 2 verify>"` still green.
- `Config._keyfile_path` reset to `None` after every test (no class-level pollution).
</verification>

<success_criteria>
- `Config.Encryption` inner class exists; `config.encryption.enabled` defaults to `False` for configs loaded without the `[Encryption]` section.
- `Config.from_str` transparently decrypts when enabled; returns plaintext Config to callers; populates `_decrypt_errors` on bad tokens; refuses to create a new keyfile when one is missing but existing values are ciphertext.
- `Config.to_str` encrypts the 5 fields when `encryption.enabled=True`, leaves empty fields empty, is idempotent on already-ciphertext values, and raises `ConfigError` if keyfile path is not set.
- `_SECRET_FIELD_PATHS` has 5 entries; every code path that walks it touches exactly the 5 listed fields.
- 6 new tests in `test_config.py` pass; golden-string for `test_to_file` updated to include `[Encryption]\nenabled = False`.
- SEC-02 criteria #1b, #2, #3b, #4 covered by unit tests. Criterion #3a (startup re-encrypt) and #5b (decrypt-failure warning) explicitly deferred to plan 03.
- Full `test_common/` unit suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/81-optional-fernet-encryption-at-rest/81-02-SUMMARY.md`. Summary must list:
- Exact line numbers where `Config.Encryption`, `_SECRET_FIELD_PATHS`, `set_keyfile_path`, `_decrypt_errors`, `from_str` hook, `to_str` hook landed.
- The final golden-string addition for `test_to_file` (the 2 new lines).
- Any OTHER existing tests that required a golden-string update (list them).
- Commit SHAs for the 3 tasks.
- Confirmation that `Config._keyfile_path` is reset to `None` in tearDown (no test pollution).
</output>
