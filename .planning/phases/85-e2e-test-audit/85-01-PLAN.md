---
phase: 85-e2e-test-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03]

must_haves:
  truths:
    - "Every E2E spec file has been independently cross-referenced against live Angular routes and selectors"
    - "The staleness audit produces an explicit per-file verdict (LIVE or STALE) for all 7 spec files"
    - "Zero stale specs exist — therefore zero removals are made"
    - "The CSP canary spec and all v1.1.1 selection/filter/URL-roundtrip dashboard specs remain in place"
    - "The autoqueue.page.spec.ts Pitfall 1 caveat (disabled button in CI harness) is documented"
    - "The Logs page coverage gap is documented as out-of-scope"
  artifacts:
    - path: ".planning/phases/85-e2e-test-audit/85-01-SUMMARY.md"
      provides: "E2E staleness audit result with per-file verdicts, zero-removal inventory, caveats, and verification evidence"
      contains: "Removal Inventory"
  key_links:
    - from: "85-01-SUMMARY.md"
      to: "86-final-validation"
      via: "Phase 86 reads E2E audit result to confirm CI green across all layers"
      pattern: "E2E-01.*COMPLETE"
---

<objective>
Independently verify the E2E staleness audit findings from 85-RESEARCH.md by cross-referencing all 7 Playwright spec files (37 test cases) against live Angular routes and HTML template selectors. Document a zero-removal inventory table, per-file verdicts, caveats (autoqueue disabled-button pitfall, Logs page gap, settings.page.spec.ts minor redundancy), and verification evidence.

Purpose: Satisfy E2E-01 (identify redundant/stale specs), E2E-02 (remove identified specs — zero removals), and E2E-03 (verify remaining specs are live) for the v1.1.2 Test Suite Audit milestone. This follows the same zero-removal audit pattern established by Phase 83 (Python) and Phase 84 (Angular).

Output: `.planning/phases/85-e2e-test-audit/85-01-SUMMARY.md` containing the complete audit result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-e2e-test-audit/85-RESEARCH.md
@.planning/phases/85-e2e-test-audit/85-PATTERNS.md
@.planning/phases/83-python-test-audit/83-01-SUMMARY.md
@.planning/phases/84-angular-test-audit/84-01-SUMMARY.md

<interfaces>
<!-- E2E spec files to audit (all 7): -->
src/e2e/tests/app.spec.ts              — 3 tests (title, nav links, default dashboard)
src/e2e/tests/about.page.spec.ts       — 2 tests (nav active, version badge)
src/e2e/tests/settings.page.spec.ts    — 1 test (nav active)
src/e2e/tests/settings-error.spec.ts   — 1 test (Sonarr connection error)
src/e2e/tests/autoqueue.page.spec.ts   — 3 tests (nav active, add/remove patterns, alphabetical order)
src/e2e/tests/csp-canary.spec.ts       — 1 test (CSP violation detection)
src/e2e/tests/dashboard.page.spec.ts   — 26 tests (nav, file list, action bar, UAT-01 selection, UAT-02 filters)

<!-- Live Angular routes (from src/angular/src/app/routes.ts): -->
dashboard, settings, logs, about, "" (redirect to dashboard)

<!-- Key selectors to cross-reference: -->
app.spec.ts:          #top-nav .nav-link, .nav-link.active
about.page.spec.ts:   .version-badge
settings.page.spec.ts: .nav-link.active
settings-error.spec.ts: fieldset, .test-result, text-danger
autoqueue.page.spec.ts: .pattern-section, .pattern-chip, .pattern-chip-text, .pattern-chip-remove, .pattern-add, .btn-pattern-add
csp-canary.spec.ts:    (no app selectors — uses csp-listener fixture)
dashboard.page.spec.ts: .transfer-table, .segment-filters, app-bulk-actions-bar, app-transfer-row, .cell-name, .cell-status, .status-badge, .bell-btn, .bell-notif
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Independent staleness verification via selector cross-reference</name>
  <read_first>
    - src/e2e/tests/app.spec.ts
    - src/e2e/tests/about.page.spec.ts
    - src/e2e/tests/settings.page.spec.ts
    - src/e2e/tests/settings-error.spec.ts
    - src/e2e/tests/autoqueue.page.spec.ts
    - src/e2e/tests/csp-canary.spec.ts
    - src/e2e/tests/dashboard.page.spec.ts
    - src/angular/src/app/routes.ts
  </read_first>
  <files>.planning/phases/85-e2e-test-audit/85-01-SUMMARY.md</files>
  <action>
Independently verify the research findings by executing the following cross-reference checks:

**Step 1 — Route existence check:**
Run `grep -n "path" src/angular/src/app/routes.ts` and confirm all 4 routes used by specs exist: `dashboard`, `settings`, `logs`, `about`. The root redirect (`""` -> `dashboard`) must also be present.

**Step 2 — Selector existence check for each spec file:**
For each of the 7 spec files, extract the CSS selectors used, then grep for each selector in the Angular HTML templates:

```bash
# app.spec.ts selectors
grep -rn "top-nav\|nav-link" src/angular/src/app/**/*.component.html

# about.page.spec.ts selectors
grep -rn "version-badge" src/angular/src/app/**/*.component.html

# settings-error.spec.ts selectors
grep -rn "test-result\|text-danger" src/angular/src/app/**/*.component.html

# autoqueue.page.spec.ts selectors
grep -rn "pattern-section\|pattern-chip\|btn-pattern-add" src/angular/src/app/**/*.component.html

# dashboard.page.spec.ts selectors
grep -rn "transfer-table\|segment-filters\|bulk-actions-bar\|transfer-row\|cell-name\|cell-status\|status-badge\|bell-btn\|bell-notif" src/angular/src/app/**/*.component.html
```

For `csp-canary.spec.ts`: no app selectors to check — this spec uses the `csp-listener` fixture to inject a script and detect CSP violations. Verify `src/e2e/tests/fixtures/csp-listener.ts` exists.

For `settings.page.spec.ts`: uses `.nav-link.active` which is already covered by the app.spec.ts check above.

**Step 3 — Duplicate detection:**
Compare `settings.page.spec.ts` test name `should have Settings nav link active` against `autoqueue.page.spec.ts` test name `should have Settings nav link active`. Confirm both exist. Document this as a minor redundancy that is kept per conservative audit mandate (D-01 targets deleted production code, not duplicated assertions against live code).

**Step 4 — Fixture file existence check:**
Verify both fixture files exist:
- `src/e2e/tests/fixtures/csp-listener.ts`
- `src/e2e/tests/fixtures/seed-state.ts`

**Step 5 — Page object existence check:**
Verify all 5 page objects exist:
- `src/e2e/tests/app.ts`
- `src/e2e/tests/about.page.ts`
- `src/e2e/tests/autoqueue.page.ts`
- `src/e2e/tests/settings.page.ts`
- `src/e2e/tests/dashboard.page.ts`

**Step 6 — autoqueue.page.spec.ts Pitfall 1 investigation:**
Read `src/docker/test/e2e/configure/setup_seedsyncarr.sh` and confirm it sets `autoqueue/patterns_only/true` but does NOT set `autoqueue/enabled/true`. Read `src/angular/src/app/pages/settings/settings-page.component.ts` and confirm the `btn-pattern-add` button is conditionally disabled by `autoqueueEnabled`. Document this as a pre-existing harness configuration concern (NOT a staleness issue — the selectors are live).

**Step 7 — Zero-modification confirmation:**
Run `git diff --name-only src/e2e/` and `git status --short src/e2e/` to confirm zero E2E files have been modified or deleted by this audit.

Record all cross-reference results for inclusion in the SUMMARY.
  </action>
  <verify>
    <automated>grep -rn "pattern-section\|pattern-chip\|btn-pattern-add" src/angular/src/app/pages/settings/settings-page.component.html | head -5 && grep -rn "transfer-table\|segment-filters" src/angular/src/app/pages/files/*.component.html | head -5 && grep -rn "version-badge" src/angular/src/app/pages/about/*.component.html | head -3 && grep -rn "top-nav\|nav-link" src/angular/src/app/pages/main/header.component.html | head -3 && echo "All selector cross-checks returned matches"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "pattern-section" src/angular/src/app/pages/settings/settings-page.component.html` returns at least 1 match
    - `grep -rn "transfer-table" src/angular/src/app/pages/files/` returns at least 1 match
    - `grep -rn "version-badge" src/angular/src/app/pages/about/` returns at least 1 match
    - `grep -rn "top-nav" src/angular/src/app/pages/main/header.component.html` returns at least 1 match
    - `grep -n "path" src/angular/src/app/routes.ts` shows dashboard, settings, logs, about
    - `ls src/e2e/tests/fixtures/csp-listener.ts` exits 0
    - `ls src/e2e/tests/fixtures/seed-state.ts` exits 0
    - `git diff --name-only src/e2e/` returns empty (zero modifications)
    - `git status --short src/e2e/` returns empty (zero modifications)
  </acceptance_criteria>
  <done>All 7 spec files independently verified LIVE via selector cross-reference against Angular HTML templates; all 4 routes confirmed in routes.ts; both fixtures confirmed on disk; all 5 page objects confirmed on disk; autoqueue Pitfall 1 documented; zero E2E file modifications confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Write audit SUMMARY with zero-removal inventory and caveats</name>
  <read_first>
    - .planning/phases/83-python-test-audit/83-01-SUMMARY.md
    - .planning/phases/84-angular-test-audit/84-01-SUMMARY.md
    - .planning/phases/85-e2e-test-audit/85-RESEARCH.md
  </read_first>
  <files>.planning/phases/85-e2e-test-audit/85-01-SUMMARY.md</files>
  <action>
Create `.planning/phases/85-e2e-test-audit/85-01-SUMMARY.md` following the exact format used in `83-01-SUMMARY.md` and `84-01-SUMMARY.md`. The SUMMARY must contain these sections:

**Frontmatter** (YAML):
```yaml
---
phase: 85-e2e-test-audit
plan: 01
subsystem: testing
tags: [playwright, e2e, audit, staleness]

requires:
  - phase: 84-angular-test-audit
    provides: "Angular audit complete — E2E layer is next in sequence"
provides:
  - "Zero-removal staleness audit result for Playwright E2E suite (7 spec files, 37 tests)"
  - "Verified all 7 spec files test live Angular routes and live UI selectors"
affects: [86-final-validation]

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - ".planning/phases/85-e2e-test-audit/85-01-SUMMARY.md"
  modified: []

key-decisions:
  - "Zero stale E2E specs found -- all 7 spec files test live routes and live selectors"
  - "autoqueue.page.spec.ts has a pre-existing harness concern (Pitfall 1: btn-pattern-add may be disabled in CI) -- not a staleness issue"
  - "settings.page.spec.ts has 1 test duplicated in autoqueue.page.spec.ts -- conservative keep per audit mandate"
  - "Logs page (/logs) has zero E2E coverage -- documented as out-of-scope gap"

patterns-established: []

requirements-completed: [E2E-01, E2E-02, E2E-03]

duration: TBD
completed: TBD
---
```

**Body sections** (in order):
1. **Title line**: `# Phase 85 Plan 01: E2E Staleness Audit and Zero-Removal Inventory Summary`
2. **Bold summary sentence**: Confirm zero stale specs across 7 files / 37 tests
3. **Performance**: Duration, tasks, files modified
4. **Accomplishments**: 4-5 bullets covering the audit, cross-reference, caveats, documentation
5. **Task Commits**: List each task commit atomically
6. **Removal Inventory (D-05 format from Phase 83)**:
   - Table with columns: Spec File Path | Test Count Removed | Reason
   - Single row: `*(none)* | 0 | All 7 spec files verified LIVE against Angular routes and selectors`
   - Totals: 0 tests removed, 0 files deleted
7. **Detailed Per-File Verdicts**: Table with all 7 spec files, test count, route tested, selectors verified live, verdict (all LIVE)
8. **Verification Evidence**:
   - **E2E-01: Selector Cross-Check** — method (grep selectors against HTML templates), result (all selectors found), verdict (zero stale)
   - **E2E-02: Zero Spec Files Deleted** — `git diff --name-only src/e2e/` returns empty
   - **E2E-03: Remaining Specs Status** — note that full E2E harness run (`make run-tests-e2e`) requires Docker CI environment; static analysis confirms all selectors are live; runtime verification deferred to Phase 86 Final Validation (VAL-01)
9. **Caveats and Documented Gaps**:
   - **Caveat 1 (autoqueue Pitfall 1)**: `setup_seedsyncarr.sh` sets `autoqueue/patterns_only/true` but NOT `autoqueue/enabled/true`; the `btn-pattern-add` button is conditionally disabled by `autoqueueEnabled` in `settings-page.component.ts`; if `autoqueue.enabled` defaults to null/falsy, the add/remove pattern tests may time out in the CI harness; this is a pre-existing harness configuration concern, NOT a staleness issue (selectors exist in live templates); runtime behavior to be confirmed in Phase 86
   - **Caveat 2 (settings.page.spec.ts redundancy)**: The single test `should have Settings nav link active` is also present in `autoqueue.page.spec.ts`; both kept per conservative audit mandate (D-01 staleness targets deleted production code, not duplicated assertions against live code); candidate for future cleanup
   - **Gap 1 (Logs page)**: `Paths.LOGS` exists in `src/e2e/urls.ts` but no `logs.page.spec.ts` exists; `app.spec.ts` asserts "Logs" in the nav but no spec navigates to `/logs` or verifies any Logs page behavior; this is a coverage gap, NOT a staleness issue; out of scope per REQUIREMENTS.md ("Writing new tests for uncovered code — Only if coverage drops below fail_under")
10. **Requirements Addressed**: Table mapping E2E-01, E2E-02, E2E-03 to evidence
11. **Files Created/Modified**: Only this SUMMARY file
12. **Decisions Made**: None beyond following plan
13. **Deviations from Plan**: Document any (expect none)
14. **Issues Encountered**: Document any (expect none or minor)
15. **User Setup Required**: None
16. **Next Phase Readiness**: Ready for Phase 86 (Final Validation); E2E audit complete with zero removals; Phase 86 should run full CI harness to confirm runtime pass (including autoqueue Pitfall 1 runtime verification)
17. **Self-Check**: Verify the SUMMARY file exists, contains all required sections, and all claims are backed by evidence

Fill in `duration` and `completed` frontmatter fields with actual values at time of writing. Use ISO date format for `completed`.
  </action>
  <verify>
    <automated>test -f .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && grep -c "Removal Inventory" .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && grep -c "E2E-01" .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && grep -c "E2E-02" .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && grep -c "E2E-03" .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && grep -c "Pitfall 1\|autoqueue.*disabled\|harness.*enabled" .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && grep -c "Logs page\|logs.*gap\|/logs.*coverage" .planning/phases/85-e2e-test-audit/85-01-SUMMARY.md && echo "SUMMARY validated"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/85-e2e-test-audit/85-01-SUMMARY.md` exists
    - File contains "Removal Inventory" section with zero-removal table row `*(none)* | 0`
    - File contains "E2E-01" with selector cross-check evidence
    - File contains "E2E-02" with zero-deletion evidence
    - File contains "E2E-03" with remaining-specs-live evidence
    - File contains autoqueue Pitfall 1 documentation (mentions `autoqueue/enabled`, `btn-pattern-add`, `setup_seedsyncarr.sh`)
    - File contains Logs page gap documentation (mentions `/logs`, `Paths.LOGS`, out of scope)
    - File contains settings.page.spec.ts redundancy note
    - File contains `requirements-completed: [E2E-01, E2E-02, E2E-03]` in frontmatter
    - File contains "csp-canary.spec.ts" with MUST KEEP notation
    - File contains "dashboard.page.spec.ts" with MUST KEEP notation
    - `git diff --name-only src/e2e/` returns empty (no E2E production files modified)
  </acceptance_criteria>
  <done>SUMMARY file written with complete audit results: zero-removal inventory, all 7 per-file verdicts, E2E-01/02/03 verification evidence, autoqueue Pitfall 1 caveat, Logs page gap, settings.page.spec.ts redundancy note, and Phase 86 readiness statement</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a read-only audit phase — no production code is modified; no trust boundaries are crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | I (Information Disclosure) | csp-canary.spec.ts | accept | CSP canary MUST remain — it verifies CSP enforcement; removing it would reduce security posture. Success Criterion 3 explicitly protects it. |
| T-85-02 | T (Tampering) | E2E spec files | accept | No spec files are modified or deleted in this phase; zero-change audit. `git diff --name-only src/e2e/` confirms zero modifications. |
</threat_model>

<verification>
1. All 7 spec files independently verified LIVE via selector cross-reference
2. Zero E2E files modified (`git diff --name-only src/e2e/` is empty)
3. Zero E2E files deleted (`git status --short src/e2e/` is empty)
4. SUMMARY file exists with all required sections
5. CSP canary spec preserved (MUST KEEP per Success Criterion 3)
6. Dashboard specs preserved (MUST KEEP per Success Criterion 3)
</verification>

<success_criteria>
- All 7 spec files have explicit LIVE/STALE verdicts (expect all LIVE)
- Zero-removal inventory is documented in D-05 format
- Autoqueue Pitfall 1, Logs page gap, and settings.page.spec.ts redundancy are all documented
- E2E-01, E2E-02, E2E-03 requirements are all marked COMPLETE with evidence
- No E2E production files have been modified or deleted
- SUMMARY follows the same format as 83-01-SUMMARY.md and 84-01-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/85-e2e-test-audit/85-01-SUMMARY.md`
</output>
