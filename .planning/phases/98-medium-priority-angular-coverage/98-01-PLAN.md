---
phase: 98-medium-priority-angular-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts
autonomous: true
requirements: [COVMED-04]

must_haves:
  truths:
    - "escapeHtml maps every documented metacharacter to its HTML entity: & -> &amp;, < -> &lt;, > -> &gt;, \" -> &quot;, ' -> &#039; (COVMED-04 success criterion #1, D-04)"
    - "escapeHtml replaces & first, so a raw < yields &lt; not &amp;lt; — no double-escaping of the entity ampersands it produces (D-04 ordering)"
    - "An attacker payload in any of the six escaped inputs (title, body, okBtn, cancelBtn, okBtnClass, cancelBtnClass) produces a rendered modal with no <script> element, no on* event-handler attribute, and no javascript: href/src (COVMED-04 success criterion #2, D-03, D-05)"
    - "The rendered modal's raw innerHTML contains the escaped entity string (e.g. &lt;script&gt;) for tag payloads and &quot; for the attribute-breakout payload — proving entity encoding, not silent browser stripping (D-03)"
    - "The class-attribute breakout payload \" onmouseover=\"alert(1) injected into cancelBtnClass/okBtnClass does NOT create an onmouseover attribute on the rendered button (D-05 class-attribute context)"
    - "A test documents that backtick, U+2028, U+2029, and null byte are intentionally NOT escaped — not exploitable in element-content or double-quoted-attribute contexts (D-01 documenting test)"
    - "A test documents that skipCount (the one un-escaped interpolation site) is a TypeScript number, renders the numeric count, and is the only bypass-exempt site — no attacker string reaches it (COVMED-04 success criterion #3, D-02)"
    - "confirm-modal.service.ts is unchanged — no production edit (D-01 no-op trivial-fix window)"
    - "The new tests extend the existing confirm-modal.service.spec.ts in place (no parallel spec file) and supersede the two partial XSS tests at lines 300 and 322, preserving their unique textContent assertions (D-06 test isolation)"
  artifacts:
    - path: "src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts"
      provides: "Nested describe block with D-04 direct escapeHtml unit tests, D-03/D-05 end-to-end DOM XSS tests for all six inputs, D-02 skipCount-exemption test, and the escape()/hasOnAttribute() helpers"
      contains: "describe(\"XSS / escapeHtml coverage\""
  key_links:
    - from: "spec escape() wrapper"
      to: "ConfirmModalService.escapeHtml (private static)"
      via: "(ConfirmModalService as any).escapeHtml(input) type cast"
      pattern: "ConfirmModalService as any\\)\\.escapeHtml"
    - from: "spec end-to-end tests"
      to: "rendered modal innerHTML (confirm-modal.service.ts:100-116 sink)"
      via: "service.confirm(options) + tick() + document.querySelector('.modal')"
      pattern: "document\\.querySelector\\(\"\\.modal\"\\)"
    - from: "hasOnAttribute helper"
      to: "rendered modal subtree"
      via: "querySelectorAll('*') + Element.attributes loop checking name.startsWith('on')"
      pattern: "attributes\\[i\\]\\.name\\.startsWith\\(\"on\"\\)"
---

<objective>
Add full end-to-end XSS coverage for `ConfirmModalService.escapeHtml` — the single Medium-priority Angular test gap (COVMED-04). The `escapeHtml` helper is the sole sanitization layer between caller-supplied strings and the direct `innerHTML` assignment at `confirm-modal.service.ts:100`. This plan proves, by test, that (1) every documented metacharacter is escaped, (2) attacker payloads in all six escaped inputs produce no executable markup, and (3) there is no bypass call site.

This is TEST-ONLY work. Per locked decision D-01, NO production code change is expected — the escape set stays `&<>"'` because the two HTML contexts in use (element content and double-quoted class attribute) are fully protected by it. `confirm-modal.service.ts` MUST NOT be modified.

Purpose: Close the last Medium-priority coverage gap of milestone v1.3.0, hardening the XSS regression guard around the `innerHTML` sink so a future reorder/removal of `escapeHtml` is caught by CI.
Output: An extended `confirm-modal.service.spec.ts` with a new nested `describe("XSS / escapeHtml coverage")` block; the two existing partial XSS tests (spec lines 300-320, 322-338) superseded by the fuller assertions (D-06).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — locked decisions and patterns (BINDING)
@.planning/phases/98-medium-priority-angular-coverage/98-CONTEXT.md
@.planning/phases/98-medium-priority-angular-coverage/98-RESEARCH.md
@.planning/phases/98-medium-priority-angular-coverage/98-PATTERNS.md

# Source under test — READ-ONLY context for this phase (DO NOT MODIFY)
@src/angular/src/app/services/utils/confirm-modal.service.ts

# Spec being extended
@src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the source under test. -->
<!-- The executor MUST NOT modify the source — these are read-only contracts the tests assert against. -->

From src/angular/src/app/services/utils/confirm-modal.service.ts:

ConfirmModalOptions (lines 3-11):
  - title: string                 (escaped, element content — <h5> at line 104)
  - body: string                  (escaped, element content — <p> at line 107)
  - okBtn?: string                (escaped, element content — button text at line 112; defaults "OK")
  - okBtnClass?: string           (escaped, double-quoted class attr at line 112; defaults "btn btn-primary")
  - cancelBtn?: string            (escaped, element content — button text at line 111; defaults "Cancel")
  - cancelBtnClass?: string       (escaped, double-quoted class attr at line 111; defaults "btn btn-secondary")
  - skipCount?: number            (NOT escaped — numeric only, lines 59-64; the D-02 exempt site)

escapeHtml (private static, lines 33-40) — replacement chain, in order:
  & -> &amp;   then   < -> &lt;   then   > -> &gt;   then   " -> &quot;   then   ' -> &#039;
  Access in tests via: (ConfirmModalService as any).escapeHtml(input)

innerHTML sink template (lines 100-116):
  <h5 class="modal-title" id="confirm-modal-title">${safeTitle}</h5>          (element content)
  <p>${safeBody}</p>                                                          (element content)
  ${skipMessage}                                                             (numeric-only, exempt)
  <button type="button" class="${safeCancelBtnClass}" data-action="cancel">${safeCancelBtn}</button>
  <button type="button" class="${safeOkBtnClass}" data-action="ok">${safeOkBtn}</button>

Selectors the rendered modal exposes (always resolve — data-action is injected raw, not via escapeHtml):
  document.querySelector(".modal")
  modal.querySelector(".modal-title")
  modal.querySelector(".modal-body p")
  modal.querySelector("[data-action=\"cancel\"]")
  modal.querySelector("[data-action=\"ok\"]")

skipMessage rendering (lines 59-64): renders a second .modal-body p when skipCount > 0:
  "${skipCount} file${plural} will be skipped (not eligible for this action)"
</interfaces>

<existing_spec_setup>
<!-- Reuse exactly — the new describe block adds NO beforeEach/afterEach of its own. -->
The spec (confirm-modal.service.spec.ts) already provides at outer scope:
  - import {fakeAsync, TestBed, tick} from "@angular/core/testing"  (line 1)
  - import {ConfirmModalService, ConfirmModalOptions} from ".../confirm-modal.service"  (line 3)
  - beforeEach: TestBed.configureTestingModule({ providers: [ConfirmModalService] }); service = TestBed.inject(...)  (lines 8-16)
  - afterEach: removes .modal / .modal-backdrop, removes body.modal-open class  (lines 18-23)
  - Canonical modal-read idiom: service.confirm(options); tick(); const modal = document.querySelector(".modal")  (lines 29-42)
  - data-action button selector idiom (lines 80, 97, 219)
  - Closing }); of outer describe is at line 463 — new block is appended just before it.
No HTTP providers, no provideHttpClient — the service has no HTTP dependency. RendererFactory2 is supplied by BrowserDynamicTestingModule (wired in src/angular/src/test.ts).
</existing_spec_setup>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the XSS describe block scaffold, helpers, and D-04 direct escapeHtml unit tests</name>
  <files>src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts</files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts (whole file — outer describe at line 5, helpers go inside a new nested describe appended before the closing }); at line 463; reuse the line 8-16 setup and line 18-23 teardown from outer scope, do NOT redeclare them)
    - src/angular/src/app/services/utils/confirm-modal.service.ts (escapeHtml lines 33-40 — the exact replacement chain and order under test; READ-ONLY)
    - .planning/phases/98-medium-priority-angular-coverage/98-CONTEXT.md (D-01 escape-set scope, D-04 metacharacter + ordering)
    - .planning/phases/98-medium-priority-angular-coverage/98-PATTERNS.md (section F escape() wrapper, section G hasOnAttribute helper)
    - .planning/phases/98-medium-priority-angular-coverage/98-RESEARCH.md (Pattern 3 private-static access, Pattern 4 hasOnAttribute, Area 5 ordering determinism, Area 8 deferred-chars analysis)
  </read_first>
  <behavior>
    - escape("&") === "&amp;"; escape("<") === "&lt;"; escape(">") === "&gt;"; escape("\"") === "&quot;"; escape("'") === "&#039;"
    - Ordering: escape("<") === "&lt;" (5 chars), NOT "&amp;lt;" (8 chars); escape("<&>") === "&lt;&amp;&gt;"
    - Combined payload: escape("<script>alert(\"xss\")</script>") === "&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;"
    - Documenting (D-01): escape("`") === "`"; escape("\u2028") === "\u2028"; escape("\u2029") === "\u2029"; escape("\0") === "\0" -- backtick/U+2028/U+2029/null pass through unchanged because they are not XSS-exploitable in element-content or double-quoted-attribute contexts (the only two contexts this service renders into)
  </behavior>
  <action>
    Append a single nested `describe("XSS / escapeHtml coverage", ...)` block inside the existing outer `describe("Testing confirm modal service", ...)` — insert it immediately before the outer block's closing `});` at line 463. Do NOT add a `beforeEach`/`afterEach` to the new block; it inherits the outer-scope setup (lines 8-16) and teardown (lines 18-23).

    At the top of the new block define two module-local helper functions (function declarations, not inline closures created in a test body — per CLAUDE.md "no functions created inside render"):
      1. `escape(s: string): string` — returns `(ConfirmModalService as any).escapeHtml(s)`. This casts the class constructor (never null) to `any` to reach the `private static` method; this is the idiomatic TypeScript test pattern per PATTERNS.md section F and is distinct from the CLAUDE.md value-cast rule (no runtime guard needed — calling a class method cannot null-fault).
      2. `hasOnAttribute(root: Element): boolean` — collect `Array.from(root.querySelectorAll("*"))`, push `root`, then for each element loop `el.attributes` and return `true` if any `attributes[i].name.startsWith("on")`; else `false`. Use this walk, NOT a CSS selector — CSS has no attribute-NAME-prefix selector (`[on*=...]` matches attribute VALUES, not names). Use `===`/strict comparisons throughout.

    Then add the D-04 direct-unit `it` blocks (Jasmine, synchronous — escapeHtml is pure, no fakeAsync needed):
      - "should escape each metacharacter to its HTML entity": the five `expect(escape(...)).toBe(...)` mappings from <behavior>.
      - "should replace & first so entity ampersands are not double-escaped": assert `escape("<")` is `"&lt;"` (NOT `"&amp;lt;"`), `escape("&")` is `"&amp;"`, and `escape("<&>")` is `"&lt;&amp;&gt;"`. This guards the line 35 `&`-first ordering against a regression that reorders the replace chain.
      - "should handle a combined attacker payload correctly": the full `<script>alert("xss")</script>` mapping from <behavior>.
      - A documenting test (name it to read as intentional, e.g. "should NOT escape backtick / U+2028 / U+2029 / null byte (not exploitable in this service's two HTML contexts, per D-01)"): assert each of the four characters passes through unchanged, with an inline comment citing D-01 and the two contexts (element content, double-quoted class attribute) so the omission reads as a locked decision, not an oversight.

    Do NOT modify confirm-modal.service.ts. Do NOT route skipCount through escapeHtml (that is Task 3's documented exemption). Keep all four existing imports; add no new imports (TestBed/fakeAsync/tick/ConfirmModalService/ConfirmModalOptions already imported).
  </action>
  <verify>
    <automated>cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec contains `describe("XSS / escapeHtml coverage"` (grep: `grep -c 'describe("XSS / escapeHtml coverage"' src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts` returns 1).
    - Spec contains `function escape(` and `function hasOnAttribute(` (both helper declarations present).
    - Spec contains the literal assertions `expect(escape("<")).toBe("&lt;")` and `expect(escape("&")).toBe("&amp;")` and `expect(escape(">")).toBe("&gt;")` and `expect(escape("\"")).toBe("&quot;")` and `expect(escape("'")).toBe("&#039;")`.
    - Spec contains the combined-payload assertion `escape("<script>alert(\"xss\")</script>")` expected `"&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;"`.
    - Spec contains `(ConfirmModalService as any).escapeHtml` exactly once (inside the escape() wrapper).
    - Spec contains `attributes[i].name.startsWith("on")` (the hasOnAttribute walk).
    - Karma run exits 0 with the new D-04 `it` blocks passing (at least 4 new `it` blocks added in this task).
    - `git diff --quiet src/angular/src/app/services/utils/confirm-modal.service.ts` exits 0 (source UNCHANGED).
    - eslint clean: `cd src/angular && npx eslint "src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts" --max-warnings 0` exits 0 (uses `===`, no functions created inside test bodies).
  </acceptance_criteria>
  <done>The new nested describe block exists with escape() and hasOnAttribute() helpers and four D-04 direct-unit it-blocks (five-metacharacter mapping, &-first ordering, combined payload, deferred-chars documenting test); the spec Karma run passes; eslint on the spec passes; confirm-modal.service.ts shows no git diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-03/D-05 end-to-end DOM XSS coverage for all six escaped inputs and supersede the two existing partial XSS tests</name>
  <files>src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts</files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts (the new describe block from Task 1; the two partial XSS tests at lines 300-320 and 322-338 to supersede; the line 29-42 modal-read idiom; the line 208-224 custom-button-classes idiom for class-attribute tests)
    - src/angular/src/app/services/utils/confirm-modal.service.ts (innerHTML sink lines 100-116 — confirms element-content vs class-attribute placement and the data-action selectors; READ-ONLY)
    - .planning/phases/98-medium-priority-angular-coverage/98-CONTEXT.md (D-03 four-layer assertion set, D-05 all-six-inputs, D-06 supersede default)
    - .planning/phases/98-medium-priority-angular-coverage/98-PATTERNS.md (sections C, D, E, H, I, J; "What D-03 adds on top")
    - .planning/phases/98-medium-priority-angular-coverage/98-RESEARCH.md (Pattern 4 DOM assertions, Pitfall 1 innerHTML vs textContent, Pitfall 3 on-as-name-not-value, Pitfall 4 attribute-breakout parsing, Area 3 canonical payloads)
  </read_first>
  <behavior>
    - For each of the six inputs (title, body, okBtn, cancelBtn — element-content payloads; okBtnClass, cancelBtnClass — class-attribute payloads), after service.confirm(options) + tick():
      - modal.querySelector("script") is null
      - hasOnAttribute(modal) is false
      - no [href]/[src] element value starts with "javascript:" (case-insensitive)
      - raw modal.innerHTML contains the escaped entity proving encoding (e.g. "&lt;script&gt;" for a <script> payload; "&quot;" for the attribute-breakout payload)
    - Element-content payload (title/body/okBtn/cancelBtn): "<script>alert(1)</script>" → innerHTML contains "&lt;script&gt;" and "&lt;/script&gt;"; querySelector("script") null
    - Class-attribute payload (okBtnClass/cancelBtnClass): "btn\" onmouseover=\"alert(1)" → the rendered [data-action="ok"]/[data-action="cancel"] button has getAttribute("onmouseover") === null; hasOnAttribute(modal) false; innerHTML contains "&quot;"
    - Superseded title/body assertions still hold: textContent of .modal-title contains "<script>" (browser-decoded visible text) AND innerHTML contains "&lt;script&gt;" (entity-encoded), proving encoding not stripping
  </behavior>
  <action>
    Inside the same nested describe block, add the D-03/D-05 end-to-end `it(..., fakeAsync(() => {...}))` blocks — one per escaped input, six total (or grouped so every one of the six inputs is individually exercised). Each follows the canonical idiom: build a `ConfirmModalOptions` putting the attacker payload in exactly the input under test (fill the two required fields title/body with benign values when testing okBtn/cancelBtn/okBtnClass/cancelBtnClass), call `service.confirm(options)`, `tick()` once, then `const modal = document.querySelector(".modal")!`.

    For the four element-content inputs (title, body, okBtn, cancelBtn) use payload `"<script>alert(1)</script>"` (and you MAY add `"<img src=x onerror=alert(1)>"` as a second body/title payload to preserve the superseded test's coverage). Assert the four D-03 layers: (a) `expect(modal.querySelector("script")).toBeNull()`, (b) `expect(hasOnAttribute(modal)).toBe(false)`, (c) the javascript:-URL check — `Array.from(modal.querySelectorAll("[href],[src]"))` `.some(...)` of href/src lowercased `.startsWith("javascript:")` expected `false`, (d) `expect(modal.innerHTML).toContain("&lt;script&gt;")` (and `"&lt;/script&gt;"`). For okBtn/cancelBtn, locate the button via `modal.querySelector("[data-action=\"ok\"]")` / `[data-action=\"cancel\"]` to confirm the payload rendered as inert button text.

    For the two class-attribute inputs (okBtnClass, cancelBtnClass) use breakout payload `"btn\" onmouseover=\"alert(1)"` (per PATTERNS.md section J — mirror the line 208-224 custom-button-classes call shape). Assert: locate the button via its `[data-action]` selector, then `expect(button.getAttribute("onmouseover")).toBeNull()`, `expect(hasOnAttribute(modal)).toBe(false)`, and `expect(modal.innerHTML).toContain("&quot;")` (the `"` was encoded, trapping the payload inside the class value). You MAY add a second class-attribute payload `"\"><script>alert(1)</script><b class=\""` to prove combined attribute+tag breakout is neutralized (`querySelector("script")` null, innerHTML contains `&lt;script&gt;` and `&quot;`).

    Supersede the two existing partial XSS tests at spec lines 300-320 ("should sanitize HTML in title and body to prevent XSS") and 322-338 ("should render HTML entities as literal text in body") per D-06: remove them and fold their unique assertions into the new title/body D-03 blocks. PRESERVE their distinguishing assertions — the line 314 `textContent.toContain("<script>")` (visible-text-is-decoded) and the line 334 `textContent.toContain("<b>file.txt</b>")` (entity-as-literal-text) idioms — by carrying those textContent checks into the corresponding new title/body block ALONGSIDE the new `innerHTML.toContain("&lt;...")` entity check. The result is a strict superset: textContent proves the browser decoded the visible text, innerHTML proves the source escaped it (distinguishing escaping from silent stripping per Pitfall 1).

    Use `===`/strict comparisons and module-scope/declared helpers only (no functions created inside test bodies). Call `tick()` exactly once after each `service.confirm` (the modal is in the DOM after one tick; the `setTimeout(0)` focus call also fires but these tests do not assert focus). Do NOT modify confirm-modal.service.ts.
  </action>
  <verify>
    <automated>cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six escaped inputs are individually exercised: spec contains payload assignments for `title`, `body`, `okBtn`, `cancelBtn`, `okBtnClass`, and `cancelBtnClass` inside the XSS describe block (grep each input name appears in a confirm() options object within the new block).
    - Spec contains `expect(modal.innerHTML).toContain("&lt;script&gt;")` and `expect(modal.innerHTML).toContain("&quot;")`.
    - Spec contains `expect(hasOnAttribute(modal)).toBe(false)` in every end-to-end block.
    - Spec contains the class-attribute breakout payload string `onmouseover=` inside a confirm() options value, and an assertion `getAttribute("onmouseover")` expected `toBeNull()`.
    - The two old tests are gone: `grep -c 'should sanitize HTML in title and body to prevent XSS' <spec>` returns 0 AND `grep -c 'should render HTML entities as literal text in body' <spec>` returns 0; their preserved `textContent` assertions (`textContent` + `toContain("<script>")` and `toContain("<b>file.txt</b>")`) now appear inside the new XSS describe block.
    - Karma run exits 0 with all end-to-end blocks passing (at least 6 new fakeAsync it-blocks, one per input).
    - eslint clean: `cd src/angular && npx eslint "src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts" --max-warnings 0` exits 0.
    - `git diff --quiet src/angular/src/app/services/utils/confirm-modal.service.ts` exits 0 (source UNCHANGED).
  </acceptance_criteria>
  <done>Six per-input end-to-end DOM XSS it-blocks exist covering all of title/body/okBtn/cancelBtn (element content) and okBtnClass/cancelBtnClass (class attribute), each asserting the four D-03 layers (no script element, no on* attribute, no javascript: URL, entity-encoded innerHTML); the two old partial XSS tests are superseded with their unique textContent assertions preserved; Karma and eslint pass; source unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the D-02 skipCount-exemption / no-bypass-call-site documenting test</name>
  <files>src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts</files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts (the XSS describe block from Tasks 1-2; the existing skipCount tests around lines 253-298 that render the second .modal-body p — reuse the `document.querySelectorAll(".modal-body p")[1]` idiom)
    - src/angular/src/app/services/utils/confirm-modal.service.ts (skipMessage lines 59-64: guarded by `if (options.skipCount && options.skipCount > 0)`, interpolates only `${options.skipCount}` + literal pluralization; the six escapeHtml interpolations lines 51-56; READ-ONLY)
    - .planning/phases/98-medium-priority-angular-coverage/98-CONTEXT.md (D-02 skipMessage exemption rationale, success-criterion #3 "no bypass call site")
    - .planning/phases/98-medium-priority-angular-coverage/98-RESEARCH.md (Pitfall 5 don't over-test skipCount, Code Examples D-02 block)
  </read_first>
  <behavior>
    - service.confirm({ title: "t", body: "b", skipCount: 3 }) + tick(): the second .modal-body p textContent contains "3 file" and "will be skipped" (numeric count rendered, plural "s" present for count !== 1)
    - The skipCount path injects no markup: modal.querySelector("script") is null; hasOnAttribute(modal) is false
    - skipCount is typed number | undefined in ConfirmModalOptions — a number literal cannot carry HTML metacharacters, so the un-escaped interpolation is safe (documented, not fixed)
  </behavior>
  <action>
    Inside the same nested XSS describe block, add one `it(..., fakeAsync(() => {...}))` whose name documents the exemption (e.g. "should leave skipCount interpolation un-escaped because skipCount is a TypeScript number, not an attacker-controlled string (D-02 — only bypass-exempt site, no string can reach it)"). Build options `{ title: "t", body: "b", skipCount: 3 }`, `service.confirm(options)`, `tick()`, read `const skipP = document.querySelectorAll(".modal-body p")[1]!`. Assert `skipP.textContent` contains `"3 file"` and `"will be skipped"` (the numeric count rendered with pluralization), and assert the path injected nothing: `expect(modal.querySelector("script")).toBeNull()` and `expect(hasOnAttribute(modal)).toBe(false)`.

    Add an inline comment block in this test documenting the success-criterion #3 audit: skipMessage (source lines 59-64) is the ONLY interpolation site that does not pass through escapeHtml; it interpolates only the numeric `options.skipCount` guarded by `if (options.skipCount && options.skipCount > 0)`; all six STRING inputs (title, body, okBtn, okBtnClass, cancelBtn, cancelBtnClass) route through `ConfirmModalService.escapeHtml` at source lines 51-56 before reaching the innerHTML sink — therefore no attacker-controlled string reaches innerHTML un-escaped (no bypass call site). Do NOT route skipCount through escapeHtml — it is a number; escaping would be a misleading no-op implying string risk where none exists (D-02). Do NOT modify confirm-modal.service.ts.

    Use `===`/strict comparisons and the already-declared `hasOnAttribute` helper (no new functions inside the test body).
  </action>
  <verify>
    <automated>cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec contains a skipCount-exemption it-block whose name references D-02 / bypass-exempt, with options `skipCount: 3` and an assertion `skipP.textContent` (or equivalent second `.modal-body p`) `toContain("3 file")` and `toContain("will be skipped")`.
    - That it-block asserts `expect(modal.querySelector("script")).toBeNull()` and `expect(hasOnAttribute(modal)).toBe(false)`.
    - Spec contains an inline comment naming all six escaped inputs (title, body, okBtn, okBtnClass, cancelBtn, cancelBtnClass) and stating skipMessage/skipCount is the only un-escaped site (the no-bypass audit, success criterion #3).
    - The spec does NOT introduce any new escapeHtml call for skipCount and does NOT modify the source: `grep -c 'escapeHtml' src/angular/src/app/services/utils/confirm-modal.service.ts` is unchanged from baseline (the six calls + one definition); `git diff --quiet src/angular/src/app/services/utils/confirm-modal.service.ts` exits 0.
    - Full spec Karma run exits 0: `cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts`.
    - Full Angular suite still green: `cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI` exits 0 (no regression in sibling specs).
    - eslint clean: `cd src/angular && npx eslint "src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts" --max-warnings 0` exits 0.
  </acceptance_criteria>
  <done>The skipCount-exemption documenting it-block exists and passes, asserting the numeric count renders and no markup is injected; an inline comment records the no-bypass-call-site audit (six escaped inputs + the one numeric exempt site); the full Angular suite is green; eslint passes; confirm-modal.service.ts shows no git diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → ConfirmModalService.confirm() | Caller-supplied strings (title, body, okBtn, cancelBtn, okBtnClass, cancelBtnClass) may be attacker-influenced and cross into the service untrusted. |
| escapeHtml → innerHTML sink | The only sanitization boundary. The six `safe*` strings (lines 51-56) cross into the direct `innerHTML` assignment at line 100. If escapeHtml regresses (reorder, removal) or a NEW un-escaped interpolation site is added, attacker markup reaches the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering / Elevation | `confirm-modal.service.ts:100` innerHTML sink fed by attacker strings | mitigate | This plan's tests ARE the regression guard. Task 1 (D-04) locks the escapeHtml metacharacter mapping + &-first ordering; Task 2 (D-03/D-05) proves end-to-end that all six inputs produce no `<script>`, no `on*` attribute, no `javascript:` URL in the rendered DOM, with entity-encoding confirmed via innerHTML string assertion. A future regression that reorders/removes escapeHtml or adds an un-escaped string site fails CI. |
| T-98-02 | Tampering (escape-set completeness) | escapeHtml character set `&<>"'` (no backtick/U+2028/U+2029/null) | accept | Per locked D-01 this is a no-op: the service renders into only two HTML contexts — element content (`<h5>`, `<p>`, button text) and double-quoted class attribute (`class="..."`). Backtick cannot close a tag or a double-quoted attribute; U+2028/U+2029 are text-node characters (only exploitable in a `<script>` sink, none present); null byte is normalized by the HTML parser. Task 1's documenting test records this reasoning so the omission is verifiably intentional, not an oversight. No source change. |
| T-98-03 | Tampering (bypass call site) | `skipMessage` interpolation `confirm-modal.service.ts:59-64` (un-escaped) | accept | Per locked D-02 this site interpolates only `options.skipCount`, a TypeScript `number` guarded by `if (skipCount && skipCount > 0)`. A number literal cannot carry HTML metacharacters, so no attacker string can reach innerHTML through it. Task 3 documents the audit (all six STRING inputs route through escapeHtml; this numeric site is the only exemption) — satisfying success criterion #3. Routing a number through escapeHtml is explicitly rejected as misleading no-op noise. |
| T-98-SC | Tampering | npm installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: zero new packages — extends an existing spec file only). No slopcheck checkpoint required; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |

Note on trivial-fix window (D-01): No production change is expected. The escape-set expansion is a documented no-op because the two HTML contexts are not exploitable via backtick/U+2028/U+2029/null byte. The tests document this reasoning. IF — and only if — a test surfaces a genuine exploitable bypass for a CURRENT caller (none expected per research), that would be a trivial fix per policy (≤10 net lines, no public-API/behavior change); a larger finding defers to v1.4.0 with a STATE.md entry. Otherwise `confirm-modal.service.ts` MUST remain unchanged.
</threat_model>

<verification>
Phase-level checks (executor runs from repo root and `src/angular/`):

1. Spec Karma run (fast, ~5s): `cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts` exits 0.
2. Full Angular suite (no sibling regression): `cd src/angular && npx ng test --watch=false --browsers=ChromeHeadlessCI` exits 0.
3. Lint discipline (specs lint like production): `cd src/angular && npx eslint "src/app/tests/unittests/services/utils/confirm-modal.service.spec.ts" --max-warnings 0` exits 0. (Project lint script is `eslint "src/**/*.ts" --max-warnings 0`.)
4. Source untouched: `git diff --quiet src/angular/src/app/services/utils/confirm-modal.service.ts` exits 0 — confirm-modal.service.ts has NO diff (D-01 no-op).
5. The new nested `describe("XSS / escapeHtml coverage")` block exists; the two old partial XSS tests (former lines 300-320, 322-338) are superseded.
</verification>

<success_criteria>
Maps to ROADMAP Phase 98 success criteria + COVMED-04:

- [ ] (Crit #1, D-04) Every metacharacter `&<>"'` is asserted escaped to its entity in a direct `escapeHtml` unit test, plus a `&`-first ordering test proving no double-escaping.
- [ ] (Crit #2, D-03/D-05) Attacker payloads in all six inputs (title, body, okBtn, cancelBtn, okBtnClass, cancelBtnClass) produce a rendered modal with no `<script>`, no `on*` attribute, no `javascript:` URL — proven via parsed-DOM assertions plus entity-encoded `innerHTML` string assertions.
- [ ] (Crit #3, D-02) A test documents skipCount as the only un-escaped interpolation site (numeric, safe) and records the no-bypass audit: all six string inputs route through escapeHtml.
- [ ] (Crit #4 / D-01) No production change — escape-set expansion is a documented no-op; `confirm-modal.service.ts` has no git diff. (If a real exploitable bypass for a current caller were found — none expected — it would be a trivial fix; otherwise deferred per policy.)
- [ ] The two existing partial XSS tests are superseded with their unique `textContent` assertions preserved (D-06).
- [ ] Spec Karma run, full Angular suite, and eslint on the spec all exit 0.
</success_criteria>

<output>
Create `.planning/phases/98-medium-priority-angular-coverage/98-01-SUMMARY.md` when done.
</output>
