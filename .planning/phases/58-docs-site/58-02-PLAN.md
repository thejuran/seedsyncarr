---
phase: 58-docs-site
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - README.md
autonomous: false
requirements:
  - PRES-04

must_haves:
  truths:
    - "README docs link points to https://thejuran.github.io/seedsyncarr (not wiki)"
    - "thejuran.github.io/seedsyncarr loads the MkDocs Material site after CI deploys"
  artifacts:
    - path: "README.md"
      provides: "Correct docs link"
      contains: "thejuran.github.io/seedsyncarr"
  key_links:
    - from: "README.md"
      to: "https://thejuran.github.io/seedsyncarr"
      via: "markdown link"
      pattern: "thejuran.github.io/seedsyncarr"
---

<objective>
Update the README docs link and verify the live docs site is accessible after CI deploys.

Purpose: Complete PRES-04 by ensuring the docs site is live and discoverable from the README.

Output: Updated README.md, confirmed live site
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-docs-site/58-01-SUMMARY.md

@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README docs link and enable GitHub Pages</name>
  <files>README.md</files>
  <read_first>
    - README.md (find the current wiki link to replace)
  </read_first>
  <action>
1. In README.md, find the line:
   ```
   For detailed setup instructions, see the [documentation](https://github.com/thejuran/seedsyncarr/wiki).
   ```
   Replace it with:
   ```
   For detailed setup instructions, see the [documentation](https://thejuran.github.io/seedsyncarr).
   ```

2. After committing and pushing, verify GitHub Pages is configured. Run:
   ```bash
   gh api repos/thejuran/seedsyncarr/pages 2>/dev/null || \
   gh api repos/thejuran/seedsyncarr/pages -X POST -f source.branch=gh-pages -f source.path=/
   ```
   This checks if Pages is already enabled; if not, enables it on the gh-pages branch.

3. Wait for CI to complete the publish-docs job (triggered by push to main), then verify the site is accessible:
   ```bash
   curl -sL -o /dev/null -w '%{http_code}' https://thejuran.github.io/seedsyncarr
   ```
   Expected: 200.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr && grep -q "thejuran.github.io/seedsyncarr" README.md && ! grep -q "seedsyncarr/wiki" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `https://thejuran.github.io/seedsyncarr` as the docs link
    - README.md does NOT contain `seedsyncarr/wiki`
    - GitHub Pages is enabled on the `gh-pages` branch (verified via `gh api repos/thejuran/seedsyncarr/pages`)
  </acceptance_criteria>
  <done>README docs link updated to point to live MkDocs site</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live docs site</name>
  <files>N/A</files>
  <action>
Human verifies the live docs site after CI deployment completes. No code changes in this task.
  </action>
  <what-built>MkDocs documentation site deployed to GitHub Pages via CI. Includes home page, installation guide, configuration reference, Sonarr/Radarr setup guide, FAQ, and changelog.</what-built>
  <how-to-verify>
    1. Visit https://thejuran.github.io/seedsyncarr — should load the MkDocs Material site with teal theme
    2. Check navigation has 6 entries: Home, Installation, Configuration, Sonarr & Radarr, FAQ, Changelog
    3. Click "Installation" — should show Docker Compose and pip install instructions
    4. Click "Configuration" — should show tables for all 8 config sections
    5. Click "Sonarr & Radarr" — should show step-by-step webhook setup with endpoint URLs
    6. Click "FAQ" — should show at least 3 troubleshooting entries
    7. Verify dark mode toggle works (sun/moon icon in header)
    8. Verify the SeedSyncarr logo/favicon appears
  </how-to-verify>
  <verify>
    <automated>curl -sL -o /dev/null -w '%{http_code}' https://thejuran.github.io/seedsyncarr | grep -q 200 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - https://thejuran.github.io/seedsyncarr returns HTTP 200
    - Human confirms all 6 navigation pages load with correct content
  </acceptance_criteria>
  <done>Live docs site verified by human</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries — README edit and static site verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02 | S (Spoofing) | GitHub Pages | accept | GitHub controls DNS for *.github.io; no custom domain risk |
</threat_model>

<verification>
1. README.md links to `https://thejuran.github.io/seedsyncarr`
2. Site loads at that URL with all 6 pages
3. Human confirms visual quality and content accuracy
</verification>

<success_criteria>
- https://thejuran.github.io/seedsyncarr is publicly accessible
- All 6 navigation pages load correctly
- README links to the live docs site
</success_criteria>

<output>
After completion, create `.planning/phases/58-docs-site/58-02-SUMMARY.md`
</output>
