---
phase: 80
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - SEC-01
tags:
  - dependabot
  - npm-overrides
  - basic-ftp
  - security

must_haves:
  truths:
    - "After `npm install`, `basic-ftp` resolves to a version ≥5.3.0 at the repo root."
    - "`npm audit --audit-level=high` reports `found 0 vulnerabilities`."
    - "Dependabot alert #3 is in a non-`open` terminal state (`fixed` | `dismissed` | `auto_dismissed`)."
  artifacts:
    - path: "package.json"
      provides: "Flat-form npm `overrides` block pinning basic-ftp to ^5.3.0."
      contains: "\"basic-ftp\": \"^5.3.0\""
    - path: "package-lock.json"
      provides: "Regenerated lockfile where the `basic-ftp` subgraph resolves to 5.3.0 instead of 5.2.2."
      contains: "basic-ftp"
  key_links:
    - from: "package.json `overrides` key"
      to: "package-lock.json `basic-ftp` resolved version"
      via: "`npm install --ignore-scripts --no-audit --no-fund` (regenerates lockfile)"
      pattern: "basic-ftp@5\\.3"
    - from: "package-lock.json resolved version"
      to: "GitHub Dependabot alert #3 state"
      via: "async rescan on next push; `gh api repos/thejuran/seedsyncarr/dependabot/alerts/3`"
      pattern: "^(fixed|dismissed|auto_dismissed)$"
---

<objective>
Close Dependabot alert #3 (`basic-ftp <= 5.2.2`, GHSA-rp42-5vxx-qpwr) by adding a flat-form npm `overrides` entry at the repo-root `package.json` that pins `basic-ftp` to `^5.3.0`, then regenerating `package-lock.json` via `npm install`.

Purpose: Close the only outstanding high-severity Dependabot alert without altering any Angular or backend source. The override mirrors the existing `src/angular/package.json` pattern exactly and has been reproduced in an isolated install (RESEARCH §3.4).

Output: Two files changed (`package.json` gets a 3-line `overrides` block; `package-lock.json` is tool-regenerated). No source code touched. No test touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md

<interfaces>
<!-- Extracted directly from src/angular/package.json:62-68 — the only prior `overrides` block in this repo. Reuse shape verbatim (flat form, no nested parent>child paths). -->

From src/angular/package.json (lines 62-68):
```json
  "overrides": {
    "undici": "^7.24.0",
    "lodash": "^4.18.0",
    "vite": "^7.3.2",
    "hono": "^4.12.14",
    "@hono/node-server": "^1.19.13"
  }
```

Current root package.json (all 5 lines):
```json
{
  "devDependencies": {
    "puppeteer": "^24.40.0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `overrides` block to root package.json and regenerate lockfile</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `/Users/julianamacbook/seedsyncarr/package.json` (current 5-line state; confirm devDependencies.puppeteer is the sole entry)
    - `/Users/julianamacbook/seedsyncarr/src/angular/package.json:62-68` (existing flat-form overrides block — exact pattern to replicate)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §3.3, §3.4, §3.5 (option analysis + verified diff + file list)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` (§ `package.json` (root) — SEC-01) — exact JSON shape and trailing-comma convention
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Per-Task Verification Map, rows 1-7 (automated commands for this task, including the confinement + structural rows added during revision)
  </read_first>
  <action>
    **Pre-install snapshot (required for confinement check below):**

    Before editing `package.json`, capture a resolved-dependency snapshot so the post-install diff can be reasoned about at package-name granularity (not diff-hunk granularity — per revision fix for the buggy `grep -v basic-ftp` filter):

    ```bash
    npm ls --all --json > /tmp/before.json 2>/dev/null || true
    ```

    Keep `/tmp/before.json` around until the acceptance check below has run. (The `|| true` is defensive: if the lockfile currently has vulnerabilities, `npm ls` may exit non-zero while still producing the tree.)

    **Edit `/Users/julianamacbook/seedsyncarr/package.json`** from its current 5-line content to add a top-level `"overrides"` key immediately after the closing `}` of `"devDependencies"`. Target content, byte-for-byte (note the trailing comma after the `devDependencies` closing `}`, and no trailing comma on the `"basic-ftp"` entry since it is the only child):

    ```json
    {
      "devDependencies": {
        "puppeteer": "^24.40.0"
      },
      "overrides": {
        "basic-ftp": "^5.3.0"
      }
    }
    ```

    Key insertion rules (from 80-PATTERNS.md § `package.json` (root) — SEC-01):
    - Use flat form `"basic-ftp": "^5.3.0"` — NOT nested path form like `"puppeteer > basic-ftp"` (RESEARCH §8.1 pitfall).
    - Semver spec is exactly `"^5.3.0"` (caret-range allowing 5.3.x and 5.x up to < 6.0.0).
    - `"overrides"` sits as a sibling to `"devDependencies"` — not nested, not in a new object.
    - No `"resolutions"` key — that is yarn-only.

    After writing `package.json`, regenerate the lockfile from the repo root:

    ```bash
    npm install --ignore-scripts --no-audit --no-fund
    ```

    Flags explained:
    - `--ignore-scripts`: do not run lifecycle scripts during install (reduces risk of unrelated side-effects).
    - `--no-audit`: skip audit during install (we run `npm audit` explicitly in the verify step).
    - `--no-fund`: skip funding notices (output-noise reduction).

    **Post-install snapshot (paired with pre-install):**

    ```bash
    npm ls --all --json > /tmp/after.json 2>/dev/null || true
    ```

    Expected lockfile delta: `package-lock.json` updates the `basic-ftp` entry's `"version": "5.2.2"` → `"version": "5.3.0"` and regenerates the `integrity` hash. No unrelated transitive bumps should appear. If the post-install diff (see the confinement assertion in `<acceptance_criteria>` below) reveals version changes outside the `basic-ftp` subgraph, STOP and report — do not commit unrelated bumps. (Threat model mitigation for confinement threat T-80-01.)

    Do NOT hand-edit `package-lock.json`.
  </action>
  <verify>
    <automated>npm ls basic-ftp 2>&amp;1 | grep -qE 'basic-ftp@5\.3' &amp;&amp; npm audit --audit-level=high 2>&amp;1 | grep -q 'found 0 vulnerabilities'</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls basic-ftp 2>&1 | grep -qE 'basic-ftp@5\.3'` — exit 0 (at least one resolved `basic-ftp@5.3.x` entry in the dep tree). (80-VALIDATION.md row 1)
    - `npm audit --audit-level=high 2>&1 | grep -q 'found 0 vulnerabilities'` — exit 0. (80-VALIDATION.md row 2)
    - `grep -E '"overrides"' package.json` — returns ≥1 line (override key present). (80-VALIDATION.md row 4)
    - `grep -E '"basic-ftp": "\^5\.3\.0"' package.json` — returns exactly 1 line. (80-VALIDATION.md row 5)
    - `git diff --stat package.json package-lock.json | tail -1 | grep -qE '2 files changed'` — both files modified (sanity: not only one). (80-VALIDATION.md row 6)
    - **Confinement (revised — JSON-diff approach, Option B):** After `/tmp/before.json` and `/tmp/after.json` have been captured around the `npm install` step, run:
      ```bash
      diff /tmp/before.json /tmp/after.json | grep -E '"(resolved|version)":' | grep -v basic-ftp | wc -l
      ```
      Expected output: `0`. Interpretation: the JSON snapshots record each package's resolved `version`/`resolved` fields at the package-name level, so a diff line that changes any `version`/`resolved` field appears alongside its enclosing package's JSON key nearby — but the `basic-ftp` subgraph lines will literally contain `basic-ftp` in the matched line or its close neighbours, allowing `grep -v basic-ftp` to filter them. If an unrelated package is bumped (e.g. `puppeteer` jumps minor versions), its `"version":` line will not contain `basic-ftp` and the count will be `≥1`, correctly tripping the guard. (Replaces the earlier `git diff package-lock.json | grep -E '^\+\s+"version":' | grep -v basic-ftp` assertion, which was buggy: in a unified `package-lock.json` diff the `+    "version":` line has no package name on the same line, so the filter could not exclude the legitimate `basic-ftp` bump and always returned `≥1`. Revision-fix per checker WARNING 2.) (80-VALIDATION.md row 7 — confinement threat T-80-01 mitigation)
  </acceptance_criteria>
  <done>
    Root `package.json` contains the `overrides` block with `"basic-ftp": "^5.3.0"`. `package-lock.json` has been regenerated by `npm install` and no longer resolves `basic-ftp@5.2.2`. `npm audit --audit-level=high` is clean. The JSON-snapshot confinement diff returns `0` non-basic-ftp version changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Dependabot alert #3 reaches a non-`open` terminal state</name>
  <files></files>
  <read_first>
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §3.1 (current alert state: `auto_dismissed` on 2026-04-16) + §13 open question 1 (async rescan timing)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Per-Task Verification Map row 3 and § Manual-Only Verifications (GitHub scan cycle is async; accept any non-`open` terminal state)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §3.6 (exact `gh api` command)
  </read_first>
  <action>
    After the Task 1 changes have been committed and pushed (or staged for push as part of the phase commit), query the GitHub Dependabot API for alert #3 to confirm it is no longer in `open` state:

    ```bash
    gh api repos/thejuran/seedsyncarr/dependabot/alerts/3 --jq '.state'
    ```

    Acceptable terminal states (any one of these satisfies SEC-01 per ROADMAP criterion 1):
    - `fixed` — GitHub rescanned and detected the vulnerable version is no longer resolved.
    - `dismissed` — manually dismissed (not required here; override closes this automatically).
    - `auto_dismissed` — current state already satisfies the non-`open` requirement (RESEARCH §3.1 confirms this was the state on 2026-04-22).

    Per RESEARCH §13 open question 1, Dependabot rescan is async — it may take minutes to hours after the push for state to transition to `fixed`. **If state is still `auto_dismissed` at verification time, that is also a pass** — the criterion is "not open," and both `fixed` and `auto_dismissed` satisfy that.

    If the state returns `open` (regression from current `auto_dismissed` — unexpected), investigate whether the override failed to resolve `basic-ftp` to 5.3.0 and reinvoke Task 1.
  </action>
  <verify>
    <automated>gh api repos/thejuran/seedsyncarr/dependabot/alerts/3 --jq '.state' | grep -qE '^(fixed|dismissed|auto_dismissed)$'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/thejuran/seedsyncarr/dependabot/alerts/3 --jq '.state' | grep -qE '^(fixed|dismissed|auto_dismissed)$'` — exit 0. (80-VALIDATION.md row 3)
    - Command output (captured to summary): one of `fixed`, `dismissed`, `auto_dismissed`. Record the exact terminal state in the phase SUMMARY.
  </acceptance_criteria>
  <done>
    `gh api` confirms Dependabot alert #3 is in a non-`open` terminal state. The exact state string is captured in the phase SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Untrusted package metadata and resolved tarballs cross this boundary during `npm install`. |
| repo → GitHub Dependabot | Vulnerability-scan rules are ingested from GitHub; the alert state is read-only from our side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | `npm install` regenerating `package-lock.json` with unrelated transitive bumps | mitigate | Task 1 captures `npm ls --all --json` snapshots before and after the install (`/tmp/before.json`, `/tmp/after.json`). Acceptance criterion: `diff /tmp/before.json /tmp/after.json \| grep -E '"(resolved\|version)":' \| grep -v basic-ftp \| wc -l` returns `0`. Reviewer scans the lockfile diff; commits only when the JSON-snapshot diff confirms changes are confined to the basic-ftp subgraph. (Replaces the earlier unified-diff-parsing assertion per revision fix.) |
| T-80-02 | Information Disclosure | `overrides` entry permanently masking future advisories in the `basic-ftp` package tree | mitigate | Task 1 adds a top-level JSON comment is not legal; instead, the phase SUMMARY records GHSA-rp42-5vxx-qpwr + the override condition (drop when `puppeteer` upgrades stop pulling `get-uri → basic-ftp`) so a future reviewer can remove the pin when no longer needed. |
| T-80-03 | Denial of Service | `basic-ftp <=5.2.2` DoS via malicious FTP listing (GHSA-rp42-5vxx-qpwr) | mitigate | The override itself is the mitigation. Verified by `npm audit --audit-level=high` reporting `found 0 vulnerabilities` (acceptance criterion in Task 1). Attack surface is unreachable in practice (RESEARCH §3.2) but the fix closes the advisory deterministically. |
| T-80-04 | Elevation of Privilege | `npm install` lifecycle scripts | accept | Mitigated operationally via `--ignore-scripts` flag during regeneration (Task 1 action). No high-sev concern because devDependencies only. |

**Severity assessment:** All threats are medium/low. The override mechanism is the deterministic fix for the single high-severity advisory. No threat blocks the phase.
</threat_model>

<verification>
- Root `package.json` contains `"overrides": { "basic-ftp": "^5.3.0" }` (exact flat form).
- `package-lock.json` regenerated by `npm install --ignore-scripts --no-audit --no-fund`; `basic-ftp` resolves to a 5.3.x version.
- `npm audit --audit-level=high` reports `found 0 vulnerabilities`.
- `gh api repos/thejuran/seedsyncarr/dependabot/alerts/3 --jq '.state'` returns a non-`open` terminal state.
- Lockfile diff is confined to the `basic-ftp` subgraph (confinement threat mitigated via JSON-snapshot diff).
</verification>

<success_criteria>
- [ ] `npm ls basic-ftp 2>&1 | grep -qE 'basic-ftp@5\.3'` exits 0.
- [ ] `npm audit --audit-level=high 2>&1 | grep -q 'found 0 vulnerabilities'` exits 0.
- [ ] `gh api repos/thejuran/seedsyncarr/dependabot/alerts/3 --jq '.state'` returns one of `fixed`, `dismissed`, `auto_dismissed`.
- [ ] `package.json` and `package-lock.json` are the only files modified by this plan (sanity check via `git diff --stat`).
- [ ] JSON-snapshot diff (`diff /tmp/before.json /tmp/after.json | grep -E '"(resolved|version)":' | grep -v basic-ftp | wc -l`) returns `0` — lockfile diff contains no unrelated transitive version bumps.
</success_criteria>

<output>
After completion, create `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-01-SUMMARY.md` recording:
- The exact `npm ls basic-ftp` output (post-install).
- The `npm audit --audit-level=high` output.
- The `gh api dependabot/alerts/3` terminal state at verification time.
- The JSON-snapshot confinement diff result (expected `0`).
- The GHSA-rp42-5vxx-qpwr advisory + the override-removal condition (can be removed when puppeteer's dep chain no longer pulls `get-uri → basic-ftp`).
</output>
</output>
