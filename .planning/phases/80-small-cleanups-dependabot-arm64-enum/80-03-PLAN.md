---
phase: 80
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/model/file.py
  - src/python/web/serialize/serialize_model.py
  - src/angular/src/app/services/files/model-file.ts
  - src/angular/src/app/services/files/view-file.ts
  - src/angular/src/app/services/files/view-file.service.ts
  - .planning/PROJECT.md
autonomous: true
requirements:
  - TECH-02
tags:
  - dead-code
  - enum-removal
  - python
  - typescript
  - waiting-for-import

must_haves:
  truths:
    - "Zero source references to `WAITING_FOR_IMPORT` / `waiting_for_import` / `waitingForImport` remain in `src/python/` and `src/angular/src/`."
    - "`make run-tests-python` exits 0 after the removal."
    - "`make run-tests-angular` exits 0 after the removal."
    - "`cd src/angular && npm run build` exits 0 (production-build type check passes)."
    - "`.planning/PROJECT.md` Key Decisions table has a new row recording the TECH-02 resolution (Option A: remove)."
  artifacts:
    - path: "src/python/model/file.py"
      provides: "ImportStatus enum with exactly two members: NONE=0, IMPORTED=1."
      contains: "class ImportStatus(Enum):"
    - path: "src/python/web/serialize/serialize_model.py"
      provides: "`__VALUES_FILE_IMPORT_STATUS` dict with exactly two entries (NONE, IMPORTED), no trailing comma on final entry."
      contains: "__VALUES_FILE_IMPORT_STATUS"
    - path: "src/angular/src/app/services/files/model-file.ts"
      provides: "ModelFile.ImportStatus TS enum with exactly NONE and IMPORTED members."
      contains: "export enum ImportStatus"
    - path: "src/angular/src/app/services/files/view-file.ts"
      provides: "ViewFile.ImportStatus TS enum with exactly NONE and IMPORTED members."
      contains: "export enum ImportStatus"
    - path: "src/angular/src/app/services/files/view-file.service.ts"
      provides: "`mapImportStatus` switch with only `IMPORTED` case + `default → NONE` fallback."
      contains: "mapImportStatus"
    - path: ".planning/PROJECT.md"
      provides: "New Key Decisions table row for TECH-02 (Option A: remove enum)."
      contains: "WAITING_FOR_IMPORT"
  key_links:
    - from: "Python ImportStatus enum (source of truth)"
      to: "TypeScript ModelFile.ImportStatus (wire-format consumer)"
      via: "JSON serialization through __VALUES_FILE_IMPORT_STATUS dict"
      pattern: "__VALUES_FILE_IMPORT_STATUS"
    - from: "ModelFile.ImportStatus (received from API)"
      to: "ViewFile.ImportStatus (rendered by UI)"
      via: "mapImportStatus switch with default → NONE fallback"
      pattern: "mapImportStatus"
    - from: "TECH-02 decision"
      to: ".planning/PROJECT.md Key Decisions table"
      via: "append one row in the existing 3-column pipe format"
      pattern: "WAITING_FOR_IMPORT"
---

<objective>
Remove the dead `WAITING_FOR_IMPORT` enum value and every reference to it across the Python and Angular source trees (5 source files), and record the decision in `.planning/PROJECT.md` Key Decisions. Decision locked: **Option A — REMOVE** per RESEARCH §5.4. Rationale: v1.1.1 milestone is explicitly "no user-visible feature additions" (REQUIREMENTS.md:6); Option B (wire up via Sonarr/Radarr `Grab` events) would add a new webhook-event ingestion path + state transition + UI surface, which violates the milestone constraint (RESEARCH §5.3); Phase 73 already evaluated and explicitly deferred this wiring (73-CONTEXT.md D-02 history).

Purpose: Close TECH-02 by eliminating a 3-month-old placeholder (added v2.0, 2026-02-12 per PATTERNS.md; no business logic ever set it) consistent with the cleanup-only scope of this milestone. The `default → NONE` branch of `mapImportStatus` (view-file.service.ts:402-403) makes the removal behavior-preserving.

Scope note (discovered during planning vs RESEARCH §6 listing): RESEARCH.md §6 originally listed `src/python/controller/webhook_manager.py` and `src/python/web/handler/webhook.py` as potentially referenced. Fresh grep at plan time confirmed **zero references** in either file (verified directly). Actual reference surface is exactly 5 files. This plan edits only the 5 files with real references.

Output: 5 source file edits (deletions only), 1 PROJECT.md append.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md

<interfaces>
<!-- Reference inventory verified by grep at plan time (2026-04-22). 5 files, not 6 per RESEARCH §5.1. -->

Python source of truth (`src/python/model/file.py:30-33`):
    class ImportStatus(Enum):
        NONE = 0                    # Not tracked by *arr / no import detected
        IMPORTED = 1                # Imported by Sonarr/Radarr
        WAITING_FOR_IMPORT = 2      # Detected by *arr, awaiting import  ← line 33, to remove

Python JSON serializer (`src/python/web/serialize/serialize_model.py:60-64`):
    __VALUES_FILE_IMPORT_STATUS = {
        ModelFile.ImportStatus.NONE: "none",
        ModelFile.ImportStatus.IMPORTED: "imported",          ← line 62, trailing comma must be removed
        ModelFile.ImportStatus.WAITING_FOR_IMPORT: "waiting_for_import"  ← line 63, to remove
    }

TS ModelFile enum (`src/angular/src/app/services/files/model-file.ts:156-160`):
    export enum ImportStatus {
        NONE            = "none",
        IMPORTED        = "imported",       ← line 158, trailing comma must be removed
        WAITING_FOR_IMPORT = "waiting_for_import"  ← line 159, to remove
    }

TS ViewFile enum (`src/angular/src/app/services/files/view-file.ts:102-106`):
    export enum ImportStatus {
        NONE                = "none",
        IMPORTED            = "imported",    ← line 104, trailing comma must be removed
        WAITING_FOR_IMPORT  = "waiting_for_import"  ← line 105, to remove
    }

TS mapping service (`src/angular/src/app/services/files/view-file.service.ts:396-405`):
    private static mapImportStatus(status: ModelFile.ImportStatus): ViewFile.ImportStatus {
        switch (status) {
            case ModelFile.ImportStatus.IMPORTED:
                return ViewFile.ImportStatus.IMPORTED;
            case ModelFile.ImportStatus.WAITING_FOR_IMPORT:          ← line 400, to remove
                return ViewFile.ImportStatus.WAITING_FOR_IMPORT;     ← line 401, to remove
            default:
                return ViewFile.ImportStatus.NONE;
        }
    }

Existing PROJECT.md Key Decisions table (`.planning/PROJECT.md:307-341`):
- 3-column pipe format: `| Decision | Rationale | Outcome |`
- 31 existing data rows, final row at line 341: `| pexpect close uses explicit except, not finally | Preserves post-close attribute access for error reporting | ✓ Good |`
- Line 342 is blank; line 343 is `## Project Status`. New row appends at line 342 (was blank, becomes the new row; a new blank line precedes `## Project Status`).
- Outcome column convention: `✓ Good` for adopted decisions.
- **Pre-edit pipe-matching line count in the table (header + separator + 31 data rows) = 33.** Post-edit target = `pre-edit + 1 = 34`. (Verified 2026-04-22 via `awk '/^## Key Decisions/,/^## Project Status/' .planning/PROJECT.md | grep -cE '^\|.*\|.*\|.*\|$'` → `33`.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove WAITING_FOR_IMPORT from 2 Python files (enum member + serializer dict entry)</name>
  <files>src/python/model/file.py, src/python/web/serialize/serialize_model.py</files>
  <read_first>
    - `/Users/julianamacbook/seedsyncarr/src/python/model/file.py:30-35` (current ImportStatus enum block)
    - `/Users/julianamacbook/seedsyncarr/src/python/web/serialize/serialize_model.py:58-66` (current __VALUES_FILE_IMPORT_STATUS dict)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` § `src/python/model/file.py — TECH-02` (sibling enum shape, RESEARCH §8.5 "do not renumber" rule)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` § `src/python/web/serialize/serialize_model.py — TECH-02` (trailing-comma cleanup rule)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §5.1 row 1 + row 2 (reference inventory), §8.5 (Python Enum renumbering pitfall — do NOT renumber IMPORTED from 1 to 2)
  </read_first>
  <action>
    **Edit 1: `/Users/julianamacbook/seedsyncarr/src/python/model/file.py`**

    Delete line 33 only. Do NOT renumber `IMPORTED = 1` — Python `Enum` permits non-contiguous integer values, and the serializer (`serialize_model.py`) maps by enum instance not by integer. (RESEARCH §8.5 pitfall; PATTERNS § `file.py` explicit rule.)

    Before (lines 30-33):
        class ImportStatus(Enum):
            NONE = 0                    # Not tracked by *arr / no import detected
            IMPORTED = 1                # Imported by Sonarr/Radarr
            WAITING_FOR_IMPORT = 2      # Detected by *arr, awaiting import

    After (lines 30-32):
        class ImportStatus(Enum):
            NONE = 0                    # Not tracked by *arr / no import detected
            IMPORTED = 1                # Imported by Sonarr/Radarr

    Key rules:
    - Preserve `NONE = 0` and `IMPORTED = 1` — values stay exact, comments stay exact, column alignment stays exact.
    - Do NOT renumber, do NOT add a replacement member, do NOT change the `Enum` base class.
    - The neighbor enum `class State(Enum)` in the same file (lines 21-28) is contiguous 0-6 — do NOT "fix" the gap in `ImportStatus` to make IT contiguous; leaving `0, 1` is exactly right (PATTERNS § `file.py` explicit contrast).

    **Edit 2: `/Users/julianamacbook/seedsyncarr/src/python/web/serialize/serialize_model.py`**

    Delete line 63 AND remove the trailing comma on line 62 (two-line edit). After the edit, the dict has 2 entries and the final entry has no trailing comma (matching the pre-edit "no trailing comma on final entry" style).

    Before (lines 60-64):
        __VALUES_FILE_IMPORT_STATUS = {
            ModelFile.ImportStatus.NONE: "none",
            ModelFile.ImportStatus.IMPORTED: "imported",
            ModelFile.ImportStatus.WAITING_FOR_IMPORT: "waiting_for_import"
        }

    After (lines 60-63):
        __VALUES_FILE_IMPORT_STATUS = {
            ModelFile.ImportStatus.NONE: "none",
            ModelFile.ImportStatus.IMPORTED: "imported"
        }

    Key rules:
    - Line 62 `ModelFile.ImportStatus.IMPORTED: "imported",` becomes `ModelFile.ImportStatus.IMPORTED: "imported"` (comma removed — matches the "no trailing comma on final entry" style documented in PATTERNS § `serialize_model.py`).
    - Preserve 4-space indent inside the dict literal.
    - Do NOT touch the adjacent dict `__VALUES_FILE_STATE` or any other dict in this file.

    **Local sanity check (run before proceeding to Task 2):**

        cd src/python && poetry run pytest tests/unittests/ -q

    Expect: exit 0. If the Python test subset fails, a reference outside the 5 enumerated sites exists — STOP and investigate before continuing.
  </action>
  <verify>
    <automated>test "$(grep -c WAITING_FOR_IMPORT src/python/model/file.py src/python/web/serialize/serialize_model.py | awk -F: '{s+=$2} END{print s}')" = 0 &amp;&amp; cd src/python &amp;&amp; poetry run pytest tests/unittests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c WAITING_FOR_IMPORT src/python/model/file.py` returns `0`.
    - `grep -c WAITING_FOR_IMPORT src/python/web/serialize/serialize_model.py` returns `0`.
    - `grep -c 'IMPORTED = 1' src/python/model/file.py` returns `1` (IMPORTED value unchanged — not renumbered).
    - `grep -c 'ModelFile.ImportStatus.IMPORTED: "imported"$' src/python/web/serialize/serialize_model.py` returns `1` (no trailing comma on now-final entry; `$` anchors end of line).
    - `grep -c 'ModelFile.ImportStatus.IMPORTED: "imported",$' src/python/web/serialize/serialize_model.py` returns `0` (old trailing-comma style gone).
    - `cd src/python && poetry run pytest tests/unittests/ -q` exits `0`.
  </acceptance_criteria>
  <done>
    `WAITING_FOR_IMPORT` gone from both Python files; `IMPORTED = 1` value preserved; trailing comma removed from the now-final dict entry; Python unit subset still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove WAITING_FOR_IMPORT from 3 Angular TypeScript files (2 enums + 1 switch case)</name>
  <files>src/angular/src/app/services/files/model-file.ts, src/angular/src/app/services/files/view-file.ts, src/angular/src/app/services/files/view-file.service.ts</files>
  <read_first>
    - `/Users/julianamacbook/seedsyncarr/src/angular/src/app/services/files/model-file.ts:154-162` (current ModelFile.ImportStatus enum block)
    - `/Users/julianamacbook/seedsyncarr/src/angular/src/app/services/files/view-file.ts:100-108` (current ViewFile.ImportStatus enum block)
    - `/Users/julianamacbook/seedsyncarr/src/angular/src/app/services/files/view-file.service.ts:395-406` (current `mapImportStatus` method)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` § `model-file.ts`, § `view-file.ts`, § `view-file.service.ts` — TECH-02 (target-shape excerpts + trailing-comma rules + default-branch safety)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §5.1 rows 3-5 (reference inventory), §8.4 (string enum — no numeric drift), §5.5 final row (switch `default → NONE` makes removal behavior-preserving)
  </read_first>
  <action>
    Three files, three surgical deletions. Each file is independent — order does not matter.

    **Edit 1: `/Users/julianamacbook/seedsyncarr/src/angular/src/app/services/files/model-file.ts`**

    Delete line 159 AND remove trailing comma on line 158 (two-line edit).

    Before (lines 156-160):
        export enum ImportStatus {
            NONE            = "none",
            IMPORTED        = "imported",
            WAITING_FOR_IMPORT = "waiting_for_import"
        }

    After (lines 156-159):
        export enum ImportStatus {
            NONE            = "none",
            IMPORTED        = "imported"
        }

    Rules:
    - String enum — no numeric drift (RESEARCH §8.4). `NONE` and `IMPORTED` keep their literal string values `"none"` and `"imported"`.
    - Line 158 trailing comma removed to match "no trailing comma on final member" style (PATTERNS rule).
    - 12-space column alignment before `=` on `NONE`/`IMPORTED` preserved exactly. (Original `WAITING_FOR_IMPORT` broke alignment because the name was longer — its removal restores consistent column.)

    **Edit 2: `/Users/julianamacbook/seedsyncarr/src/angular/src/app/services/files/view-file.ts`**

    Delete line 105 AND remove trailing comma on line 104 (two-line edit).

    Before (lines 102-106):
        export enum ImportStatus {
            NONE                = "none",
            IMPORTED            = "imported",
            WAITING_FOR_IMPORT  = "waiting_for_import"
        }

    After (lines 102-105):
        export enum ImportStatus {
            NONE                = "none",
            IMPORTED            = "imported"
        }

    Rules:
    - Same string-enum, same trailing-comma rule as Edit 1.
    - Column alignment in this file uses 16 spaces before `=` (wider than `model-file.ts`). Preserve exactly.

    **Edit 3: `/Users/julianamacbook/seedsyncarr/src/angular/src/app/services/files/view-file.service.ts`**

    Delete the `WAITING_FOR_IMPORT` case + return (two contiguous lines). The `default: return ViewFile.ImportStatus.NONE;` branch below is the safety net — after removal, the switch handles `IMPORTED` explicitly and anything else (which is now only `NONE`) falls through to `default`, which returns `NONE`. Behavior is bit-for-bit preserved (RESEARCH §5.5 final row; PATTERNS § `view-file.service.ts`).

    Before (lines 396-405):
        private static mapImportStatus(status: ModelFile.ImportStatus): ViewFile.ImportStatus {
            switch (status) {
                case ModelFile.ImportStatus.IMPORTED:
                    return ViewFile.ImportStatus.IMPORTED;
                case ModelFile.ImportStatus.WAITING_FOR_IMPORT:
                    return ViewFile.ImportStatus.WAITING_FOR_IMPORT;
                default:
                    return ViewFile.ImportStatus.NONE;
            }
        }

    After (lines 396-403):
        private static mapImportStatus(status: ModelFile.ImportStatus): ViewFile.ImportStatus {
            switch (status) {
                case ModelFile.ImportStatus.IMPORTED:
                    return ViewFile.ImportStatus.IMPORTED;
                default:
                    return ViewFile.ImportStatus.NONE;
            }
        }

    Rules:
    - Delete exactly lines 400-401 (the `case ModelFile.ImportStatus.WAITING_FOR_IMPORT:` line and its `return ViewFile.ImportStatus.WAITING_FOR_IMPORT;` body line).
    - Preserve 12-space `case` indent and 16-space `return` indent on the surviving `IMPORTED` case.
    - Do NOT remove the `default` branch — it is the behavior-preservation safety net.
    - Do NOT add a comment explaining the removal — keep the method clean per the existing style.

    **Local sanity check (run after all 3 edits):**

        cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless

    Expect: exit 0. Then run the production build type check:

        cd src/angular && npm run build

    Expect: exit 0.
  </action>
  <verify>
    <automated>test "$(grep -c 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/angular/src/app/services/files/model-file.ts src/angular/src/app/services/files/view-file.ts src/angular/src/app/services/files/view-file.service.ts | awk -F: '{s+=$2} END{print s}')" = 0 &amp;&amp; cd src/angular &amp;&amp; npm test -- --watch=false --browsers=ChromeHeadless &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/angular/src/app/services/files/model-file.ts` returns `0`.
    - `grep -c 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/angular/src/app/services/files/view-file.ts` returns `0`.
    - `grep -c 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/angular/src/app/services/files/view-file.service.ts` returns `0`.
    - `grep -cE 'IMPORTED\s+= "imported"$' src/angular/src/app/services/files/model-file.ts` returns `1` (no trailing comma).
    - `grep -cE 'IMPORTED\s+= "imported"$' src/angular/src/app/services/files/view-file.ts` returns `1` (no trailing comma).
    - `grep -cE 'case ModelFile\.ImportStatus\.IMPORTED:' src/angular/src/app/services/files/view-file.service.ts` returns `1` (IMPORTED case preserved).
    - `grep -cE 'default:' src/angular/src/app/services/files/view-file.service.ts` returns `1` (default branch preserved).
    - `cd src/angular && npm test -- --watch=false --browsers=ChromeHeadless` exits `0`.
    - `cd src/angular && npm run build` exits `0` (80-VALIDATION.md row 11).
  </acceptance_criteria>
  <done>
    All 3 TypeScript files free of `WAITING_FOR_IMPORT`; trailing commas on the now-final enum members removed; `mapImportStatus` switch reduced to `IMPORTED` case + `default → NONE`; Angular unit tests green; production build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append TECH-02 decision row to PROJECT.md Key Decisions + cross-tree verification + full suites</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `/Users/julianamacbook/seedsyncarr/.planning/PROJECT.md:307-343` (Key Decisions table header + existing 31 data rows + blank line before `## Project Status`)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` § `.planning/PROJECT.md — TECH-02` (exact row format + target row content)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §5.4 (recommended decision text), §5.5 final row (decision rationale), §7.3 (PROJECT.md format rules)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Per-Task Verification Map rows 8-12 (final-state assertions)
  </read_first>
  <action>
    **Part A: Append the TECH-02 decision row to `.planning/PROJECT.md` Key Decisions.**

    Current final data row is at line 341: `| pexpect close uses explicit except, not finally | Preserves post-close attribute access for error reporting | ✓ Good |`. Line 342 is currently blank; line 343 is `## Project Status`.

    Insert a new row as line 342 (the current blank line becomes the row; a new blank line is added before `## Project Status`). The new line 342 must be exactly:

        | Remove WAITING_FOR_IMPORT enum value (TECH-02) | Placeholder since v2.0 (2026-02-12); never set by business logic; Phase 73 explicitly deferred wiring; re-add alongside future Sonarr Grab-event ingestion if prioritized | ✓ Good |

    Rules (from 80-PATTERNS.md § `.planning/PROJECT.md`):
    - Exact 3-column pipe format: `| Decision | Rationale | Outcome |`.
    - Decision column contains `(TECH-02)` identifier (matches existing convention like `(M003)` on line 319).
    - Rationale is a single phrase with semicolon-joined clauses (matches line 322 style: "Preserves first-connect usability while blocking MITM on reconnects").
    - Outcome column: `✓ Good` (standard adopted-decision marker).
    - NO blank line between the new row (line 342) and the preceding row (line 341). Blank line separates the table from `## Project Status` (was line 342, now line 343).

    Target post-edit layout (lines 340-344):
        | Keep bare except in config.py test handlers | CI mock prevents RequestException class resolution | ✓ Good |
        | pexpect close uses explicit except, not finally | Preserves post-close attribute access for error reporting | ✓ Good |
        | Remove WAITING_FOR_IMPORT enum value (TECH-02) | Placeholder since v2.0 (2026-02-12); never set by business logic; Phase 73 explicitly deferred wiring; re-add alongside future Sonarr Grab-event ingestion if prioritized | ✓ Good |

        ## Project Status

    **Part B: Cross-tree grep verification (ROADMAP success criterion 3 + 80-VALIDATION.md row 8).**

    Run the RESEARCH §5.6 + VALIDATION row 8 cross-tree assertion:

        git grep -n 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/python/ src/angular/src/ | wc -l

    Expected: `0`. If >0, identify the residual site(s) and remove them before proceeding. Also sweep `src/e2e/tests/` for seed-fixture hits (RESEARCH §13 open question 2):

        git grep -n 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/e2e/tests/ | wc -l

    Expected: `0` (RESEARCH §13 recommends this sweep; plan-time grep confirmed clean, but re-verify at execution time).

    **Part C: Full test-suite verification (80-VALIDATION.md rows 9-10).**

    Run the phase-gate full suites:

        make run-tests-python
        echo "python_exit=$?"

        make run-tests-angular
        echo "angular_exit=$?"

    Both must exit 0. If either fails, a reference outside the 5 enumerated sites was missed or the removal broke a test expectation — STOP and investigate.

    **Part D: PROJECT.md row sanity check (80-VALIDATION.md row 12 + row-count guard).**

        grep -E 'WAITING_FOR_IMPORT' .planning/PROJECT.md | wc -l

    Expected: ≥1 (the new row contains the token). This is the ONLY acceptable place `WAITING_FOR_IMPORT` appears after the phase — `src/python/` and `src/angular/src/` must be at 0.

    **Row-count arithmetic (revised per checker BLOCKER 1):** Pre-edit pipe-matching line count in the Key Decisions table is `33` (header + separator + 31 data rows), confirmed 2026-04-22. After appending one new data row, the post-edit count must be `pre-edit + 1 = 34`. Run:

        awk '/^## Key Decisions/,/^## Project Status/' .planning/PROJECT.md | grep -cE '^\|.*\|.*\|.*\|$'

    Expected: `34` (header + separator + 31 prior data rows + 1 new row = 34 lines). Prevents accidental duplicate row or malformed table.
  </action>
  <verify>
    <automated>test "$(git grep -n 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/python/ src/angular/src/ | wc -l | tr -d ' ')" = 0 &amp;&amp; grep -qE 'WAITING_FOR_IMPORT.*TECH-02.*✓ Good' .planning/PROJECT.md &amp;&amp; test "$(awk '/^## Key Decisions/,/^## Project Status/' .planning/PROJECT.md | grep -cE '^\|.*\|.*\|.*\|$')" = 34 &amp;&amp; make run-tests-python &amp;&amp; make run-tests-angular</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/python/ src/angular/src/ | wc -l` returns `0`. (80-VALIDATION.md row 8)
    - `git grep -n 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/e2e/tests/ | wc -l` returns `0` (open-question sweep).
    - `grep -E 'WAITING_FOR_IMPORT' .planning/PROJECT.md | wc -l` returns `≥1`. (80-VALIDATION.md row 12)
    - `grep -cE '\| Remove WAITING_FOR_IMPORT enum value \(TECH-02\) \|.*\| ✓ Good \|' .planning/PROJECT.md` returns `1` (new row present with correct format).
    - `make run-tests-python` exits `0`. (80-VALIDATION.md row 9)
    - `make run-tests-angular` exits `0`. (80-VALIDATION.md row 10)
    - Line count of PROJECT.md Key Decisions table: `awk '/^## Key Decisions/,/^## Project Status/' .planning/PROJECT.md | grep -cE '^\|.*\|.*\|.*\|$'` returns `34` (header + separator + 31 prior rows + 1 new row = 34 lines). (Revised per checker BLOCKER 1 — pre-edit count is `33`, so post-edit target is `pre-edit + 1 = 34`. Prevents accidental duplicate row or malformed table.)
  </acceptance_criteria>
  <done>
    PROJECT.md carries the new TECH-02 decision row in the exact 3-column format, bringing the Key Decisions pipe-matching line count from `33` pre-edit to `34` post-edit. Zero `WAITING_FOR_IMPORT` references remain under `src/python/` and `src/angular/src/`. Full Python and Angular suites green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source of truth → TypeScript consumer | The `ImportStatus` enum crosses the JSON wire format. Removal must stay synchronized across both tiers. |
| Source code → test suites | Test assertions may reference the enum value directly; a residual reference would surface as a test failure. |
| Source code → build pipeline | TypeScript compile and Angular production bundle would fail if any dangling reference remained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-09 | Tampering | Python Enum renumbering `IMPORTED` accidentally (e.g. someone "fixes" the gap 0,1,_ to 0,1) | mitigate | Task 1 acceptance criterion: `grep -c 'IMPORTED = 1' src/python/model/file.py` returns `1`. RESEARCH §8.5 explicitly warns against this. PATTERNS § `file.py` has an explicit "Contrast with neighbor enum" section documenting the rule. |
| T-80-10 | Information Disclosure | Residual reference in an e2e seed fixture or other file deserializing `"waiting_for_import"` as JSON and crashing at runtime | mitigate | Task 3 Part B scans `src/e2e/tests/` in addition to `src/python/` + `src/angular/src/`. Plan-time grep confirmed e2e tree is clean (zero hits), but the execute-time re-verify catches any post-plan drift. |
| T-80-11 | Denial of Service | `mapImportStatus` default branch not covering the removed enum value in type-narrowing, causing TS compile error | accept | The switch matches on `ModelFile.ImportStatus` — a type that, after Task 2 Edit 1, contains only `NONE` and `IMPORTED`. TS exhaustiveness check is satisfied by the remaining `case IMPORTED` + `default`. `npm run build` (acceptance criterion in Task 2) is the gate. RESEARCH §5.5 final row confirms behavior preservation. |
| T-80-12 | Repudiation | Decision rationale not recorded in PROJECT.md, losing traceability | mitigate | Task 3 Part A adds the row; Part D verifies it (row text + row-count arithmetic post-edit = `34`). ROADMAP success criterion 3 requires the PROJECT.md entry. |

**Severity assessment:** All threats are medium/low. No external attack surface is involved (TECH-02 is pure dead-code removal). The two hard gates are: (1) full Python + Angular suites green after removal (Task 3 Part C), and (2) Angular production build succeeds (Task 2 verify — catches any TS type errors from dangling references).
</threat_model>

<verification>
- `src/python/model/file.py` `ImportStatus` enum has exactly `NONE = 0` and `IMPORTED = 1`; no `WAITING_FOR_IMPORT` line; `IMPORTED` value unchanged (not renumbered).
- `src/python/web/serialize/serialize_model.py` `__VALUES_FILE_IMPORT_STATUS` dict has exactly 2 entries (`NONE`, `IMPORTED`), no trailing comma on final entry.
- `src/angular/src/app/services/files/model-file.ts` `ImportStatus` enum has exactly `NONE` and `IMPORTED`, no trailing comma on `IMPORTED`.
- `src/angular/src/app/services/files/view-file.ts` `ImportStatus` enum has exactly `NONE` and `IMPORTED`, no trailing comma on `IMPORTED`.
- `src/angular/src/app/services/files/view-file.service.ts` `mapImportStatus` switch has exactly `case IMPORTED → IMPORTED` and `default → NONE`.
- `git grep 'WAITING_FOR_IMPORT\|waiting_for_import\|waitingForImport' src/python/ src/angular/src/` returns zero matches.
- `.planning/PROJECT.md` Key Decisions has the new TECH-02 row in the canonical 3-column format, `✓ Good` outcome; pipe-matching line count is `34` post-edit (pre-edit `33` + 1 new row).
- `make run-tests-python` exits 0; `make run-tests-angular` exits 0; `cd src/angular && npm run build` exits 0.
</verification>

<success_criteria>
- [ ] Cross-tree grep under `src/python/` + `src/angular/src/` for the three token spellings returns `0` hits.
- [ ] PROJECT.md Key Decisions table has a new row for TECH-02 with `✓ Good` outcome; pipe-matching line count is `34` (pre-edit `33` + 1).
- [ ] `make run-tests-python` exits 0.
- [ ] `make run-tests-angular` exits 0.
- [ ] `cd src/angular && npm run build` exits 0.
- [ ] `IMPORTED = 1` preserved (not renumbered) in `src/python/model/file.py`.
- [ ] Trailing commas removed on the now-final enum member in `model-file.ts`, `view-file.ts`, and on the final dict entry in `serialize_model.py`.
- [ ] `mapImportStatus` retains `IMPORTED` case + `default → NONE` branch (unchanged return for all inputs).
</success_criteria>

<output>
After completion, create `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-03-SUMMARY.md` recording:
- The 5 source edits (file-by-file: before/after excerpt, line numbers).
- The PROJECT.md row text exactly as appended.
- The pre- and post-edit Key Decisions table line counts (`33` → `34`).
- The cross-tree grep output (should be the literal string `0`).
- Python + Angular suite exit codes (both `0`).
- Angular production build exit code (`0`).
- The plan-time discovery that `webhook_manager.py` and `webhook.py` had zero hits despite RESEARCH §6 listing (confirms RESEARCH §5.1's "exactly 5 files" inventory as the canonical count).
</output>
</output>
