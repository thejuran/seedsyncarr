---
phase: 80
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/docker/test/python/Dockerfile
  - src/python/tests/integration/test_controller/test_extract/test_extract.py
  - src/python/tests/integration/test_controller/test_controller.py
autonomous: false
requirements:
  - TECH-01
tags:
  - docker
  - multi-arch
  - arm64
  - apple-silicon
  - pytest

must_haves:
  truths:
    - "`make run-tests-python` builds to completion and exits 0 on an Apple Silicon (arm64) host."
    - "CI amd64 pytest collection count is byte-identical to the pre-change baseline."
    - "On arm64, pytest reports SKIPPED (not ERROR) for every test method in `TestExtract` and `TestController`, with the documented skip reason."
    - "`.github/workflows/ci.yml` is unchanged by this plan."
  artifacts:
    - path: "src/docker/test/python/Dockerfile"
      provides: "Arch-conditional `RUN` that installs `rar` on amd64/i386 only, using the `dpkg --print-architecture` idiom."
      contains: "dpkg --print-architecture"
    - path: "src/python/tests/integration/test_controller/test_extract/test_extract.py"
      provides: "Class-level skipIf decorator on TestExtract gated by shutil.which('rar')."
      contains: "shutil.which"
    - path: "src/python/tests/integration/test_controller/test_controller.py"
      provides: "Class-level skipIf decorator on TestController gated by shutil.which('rar')."
      contains: "shutil.which"
    - path: ".planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt"
      provides: "Pre-change pytest collection count for amd64, used as the parity-verification anchor."
      contains: "BASELINE_COLLECT_COUNT="
  key_links:
    - from: "Dockerfile arch-gate"
      to: "`rar` binary absence on arm64"
      via: "`dpkg --print-architecture` shell check inside RUN layer"
      pattern: "dpkg --print-architecture"
    - from: "`rar` binary absence"
      to: "pytest SKIPPED status on TestExtract / TestController"
      via: "class-level skipIf decorators using shutil.which('rar') is None"
      pattern: "shutil.which"
    - from: "pre-edit amd64 baseline"
      to: "post-edit amd64 parity check"
      via: "`pytest --collect-only | grep -c '<Function'` comparison"
      pattern: "<Function"
---

<objective>
Make `make run-tests-python` build and run to completion on Apple Silicon (`arm64`) hosts while preserving the CI amd64 path byte-for-byte. Mechanism: arch-conditional `rar` install in the test Dockerfile (using the repo's established `dpkg --print-architecture` idiom from `src/docker/test/angular/Dockerfile:6-17`), plus class-level skipIf decorators on the two `rar`-dependent test classes that are impossible to satisfy on arm64 (Debian ships `rar` for amd64/i386 only, RARLAB has no aarch64 Linux build — verified in RESEARCH §4.1).

Purpose: Close the arm64 local-development gap (STATE.md tech-debt line 66; scheduled for Phase 80 as TECH-01) without introducing any change to `.github/workflows/ci.yml` or to the amd64 test surface.

Output: 1 Dockerfile edit, 2 Python test-file edits (decorator-only additions — `shutil` is already imported in both), 1 baseline artifact file in the phase directory.

This plan is marked `autonomous: false` because Task 4 requires an Apple Silicon host to run `make run-tests-python` on arm64; CI matrix does not cover `linux/arm64` for Python tests (RESEARCH §7.2, VALIDATION § Manual-Only Verifications).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md
@.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md

<interfaces>
<!-- Extracted directly from src/docker/test/angular/Dockerfile:6-17 — the only prior arch-conditional RUN in this repo. Matches the TECH-01 arch-gating problem shape exactly. -->

Analog from src/docker/test/angular/Dockerfile (lines 6-17):
- Comment block names both architectures and explains why they differ.
- `ARCH=$(dpkg --print-architecture)` captures the arch inside the same RUN layer.
- `if [ "$ARCH" = "arm64" ]; then ... else ... fi` switches install behavior.
- One shared `apt-get update` for both branches.

Current block at `src/docker/test/python/Dockerfile:5-11` (verbatim):
    # Install dependencies
    # Note: The base image (seedsyncarr_run_python_env) already has non-free repos enabled via DEB822 format
    RUN apt-get update && \
        apt-get install -y \
            openssh-server \
            rar \
            unrar

Current test-file imports (confirmed — `shutil` already present in both):
- `src/python/tests/integration/test_controller/test_extract/test_extract.py:2` already contains `import shutil` (no new import needed)
- `src/python/tests/integration/test_controller/test_controller.py:5` already contains `import shutil` (no new import needed)
- Both files import `unittest` at line 1 (class-level decorators available)

Target class declaration lines (verified):
- `test_extract.py:12` — `class TestExtract(unittest.TestCase):`
- `test_controller.py:47` — `class TestController(unittest.TestCase):`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture amd64 pytest collection baseline (pre-edit parity anchor)</name>
  <files>.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt</files>
  <read_first>
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Per-Task Verification Map row 5 (amd64 collection-count parity assertion)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §4.8 (verification commands block — third command captures the baseline)
    - `src/docker/test/python/Dockerfile` (current state — this runs BEFORE Task 2 edits it)
    - `Makefile:79-94` (the `tests-python` + `run-tests-python` targets)
  </read_first>
  <action>
    Before making any Dockerfile or test-file edits, build and collect pytest tests against the CURRENT amd64 image and record the collection count. This is the parity baseline the post-edit amd64 verification (Task 4) compares against.

    From the repo root, first build the test image for amd64 explicitly:

        docker buildx build --platform linux/amd64 \
            -f src/docker/test/python/Dockerfile \
            -t seedsyncarr/test/python:baseline-amd64 \
            --load \
            .

    Then run pytest in collect-only mode against the baseline image and count test functions:

        BASELINE_COLLECT_COUNT=$(docker run --platform linux/amd64 --rm \
            -v "$(pwd)/src/python:/src" \
            seedsyncarr/test/python:baseline-amd64 \
            pytest --collect-only 2>&1 | grep -c '<Function')
        echo "BASELINE_COLLECT_COUNT=${BASELINE_COLLECT_COUNT}" \
            > .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt
        echo "captured_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            >> .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt
        echo "image=seedsyncarr/test/python:baseline-amd64" \
            >> .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt

    Expected output file shape (3 lines):
    - `BASELINE_COLLECT_COUNT=<integer>`
    - `captured_at=<ISO-8601 UTC>`
    - `image=seedsyncarr/test/python:baseline-amd64`

    The integer is a pure count. Do NOT proceed to Task 2 until this file exists and the count is > 0. If the count is 0, the collection command failed — investigate `docker run` output before proceeding.

    If the base image `seedsyncarr/run/python/devenv` is not yet present on the host, run `make tests-python` once first (which builds both the runtime and test images) before the `docker buildx build` above. The `docker buildx build` step then rebuilds the test-image layer only.
  </action>
  <verify>
    <automated>test -f .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt &amp;&amp; grep -qE '^BASELINE_COLLECT_COUNT=[1-9][0-9]*$' .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt` exits 0.
    - `grep -qE '^BASELINE_COLLECT_COUNT=[1-9][0-9]*$' .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt` exits 0 (positive integer captured).
    - `grep -qE '^captured_at=' .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt` exits 0.
  </acceptance_criteria>
  <done>
    Baseline file exists at `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt` with a positive integer collection count captured against the unedited Dockerfile on amd64.
  </done>
</task>

<task type="auto">
  <name>Task 2: Arch-gate `rar` install in src/docker/test/python/Dockerfile</name>
  <files>src/docker/test/python/Dockerfile</files>
  <read_first>
    - `/Users/julianamacbook/seedsyncarr/src/docker/test/python/Dockerfile` (current 47-line content; target edit block is lines 5-11)
    - `/Users/julianamacbook/seedsyncarr/src/docker/test/angular/Dockerfile:6-17` (exact analog; `dpkg --print-architecture` idiom to replicate)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` § `src/docker/test/python/Dockerfile` — TECH-01 (includes the exact target-shape excerpt)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §4.1, §4.6 (why arm64 cannot install `rar`), §8.2 (TARGETARCH vs dpkg tradeoff)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Per-Task Verification Map rows 4, 5, 7 (criteria for this edit)
  </read_first>
  <action>
    Replace lines 5-11 of `/Users/julianamacbook/seedsyncarr/src/docker/test/python/Dockerfile` (the current `# Install dependencies` comment block and the subsequent `RUN apt-get update && apt-get install -y openssh-server rar unrar` block) with the new 12-line block below. Preserve all other lines (1-4 and 12-47) byte-for-byte.

    Lines 5-11 to REMOVE (verbatim — see 80-PATTERNS.md "Current state"):
    - Line 5: `# Install dependencies`
    - Line 6: `# Note: The base image (seedsyncarr_run_python_env) already has non-free repos enabled via DEB822 format`
    - Line 7: `RUN apt-get update && \`
    - Line 8: `    apt-get install -y \`
    - Line 9: `        openssh-server \`
    - Line 10: `        rar \`
    - Line 11: `        unrar`

    NEW block to INSERT at lines 5-16 (matches the target-shape excerpt in 80-PATTERNS.md § `src/docker/test/python/Dockerfile` — TECH-01):

        # Install dependencies
        # Note: The base image (seedsyncarr_run_python_env) already has non-free repos enabled via DEB822 format
        # ARM64: skip `rar` (Debian ships rar for amd64/i386 only; RARLAB has no aarch64 Linux build)
        # AMD64: install `rar` normally for test-fixture archive creation
        RUN apt-get update && \
            apt-get install -y openssh-server unrar && \
            ARCH=$(dpkg --print-architecture) && \
            if [ "$ARCH" = "amd64" ] || [ "$ARCH" = "i386" ]; then \
                apt-get install -y rar; \
            else \
                echo "Skipping rar install on $ARCH (Debian rar is amd64/i386 only)"; \
            fi

    Rules (from 80-PATTERNS.md § `src/docker/test/python/Dockerfile` — TECH-01):
    - Use the `dpkg --print-architecture` idiom (NOT `ARG TARGETARCH`) to match the existing `src/docker/test/angular/Dockerfile:6-17` analog exactly and avoid the RESEARCH §8.2 empty-TARGETARCH pitfall.
    - `unrar` stays in the unconditional install (it IS arm64-available per Debian Bookworm package database — RESEARCH §4.1).
    - `rar` is gated inside `if [ "$ARCH" = "amd64" ] || [ "$ARCH" = "i386" ]` — both amd64 and i386 are acceptable because Debian ships `rar` for both.
    - The `echo` in the else branch is intentional: it produces a build-log line on arm64 so the skip is discoverable and auditable.
    - Do NOT introduce `ARG TARGETARCH` — this idiom does not use it.
    - Do NOT touch lines 1-4 (`FROM`, blank, `RUN ls -l /app/python`, blank) or the remainder (entrypoint, sshd setup, user setup, `WORKDIR`, `ENV`, `ENTRYPOINT`, `CMD`).

    After editing, do NOT yet build on arm64 — Task 4 handles the arm64 verification after the test-file decorators are in place.
  </action>
  <verify>
    <automated>grep -qE 'dpkg --print-architecture' src/docker/test/python/Dockerfile &amp;&amp; grep -qE 'apt-get install -y openssh-server unrar' src/docker/test/python/Dockerfile &amp;&amp; grep -qE 'amd64.*i386' src/docker/test/python/Dockerfile &amp;&amp; test "$(grep -cE '^\s*rar[[:space:]]*\\\\$' src/docker/test/python/Dockerfile)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'dpkg --print-architecture' src/docker/test/python/Dockerfile` exits 0 (dpkg idiom present).
    - `grep -qE 'apt-get install -y openssh-server unrar' src/docker/test/python/Dockerfile` exits 0 (unconditional install preserved).
    - `grep -qE '"amd64".*"i386"' src/docker/test/python/Dockerfile` exits 0 (both acceptable archs gated together).
    - `grep -cE '^\s*rar[[:space:]]*\\$' src/docker/test/python/Dockerfile` returns `0` (the unconditional `        rar \` line from the original block is gone).
    - `git diff --stat .github/workflows/ci.yml | wc -l` returns `0` (CI file untouched — 80-VALIDATION.md row 7).
    - Docker amd64 rebuild succeeds: `docker buildx build --platform linux/amd64 -f src/docker/test/python/Dockerfile -t seedsyncarr/test/python:post-edit-amd64 --load . ; echo $?` returns `0`.
  </acceptance_criteria>
  <done>
    Dockerfile arch-gates `rar` via `dpkg --print-architecture`; amd64 `docker buildx build` still succeeds; `.github/workflows/ci.yml` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add class-level skipIf decorators to TestExtract and TestController</name>
  <files>src/python/tests/integration/test_controller/test_extract/test_extract.py, src/python/tests/integration/test_controller/test_controller.py</files>
  <read_first>
    - `/Users/julianamacbook/seedsyncarr/src/python/tests/integration/test_controller/test_extract/test_extract.py:1-12` (current imports at lines 1-9; target is line 12 `class TestExtract(unittest.TestCase):`; `import shutil` already present at line 2)
    - `/Users/julianamacbook/seedsyncarr/src/python/tests/integration/test_controller/test_controller.py:1-47` (current imports at lines 1-20; target is line 47 `class TestController(unittest.TestCase):`; `import shutil` already present at line 5)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-PATTERNS.md` § `test_extract.py — TECH-01` and § `test_controller.py — TECH-01` (exact decorator target shape)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §4.7 (decorator pattern + stdlib rationale), §8.3 (why class-level fires before setUpClass)
    - `/Users/julianamacbook/seedsyncarr/src/python/tests/integration/test_web/test_handler/test_stream_log.py:9` (secondary analog — existing class-level `@unittest.skip(...)` in this repo)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Per-Task Verification Map row 6 (arm64 skip-count assertion)
  </read_first>
  <action>
    Add an identical class-level decorator immediately above the `class TestExtract` and `class TestController` declarations in both files. The decorator uses the same skip-reason string in both files (so a single regex matches both in verification — 80-PATTERNS.md explicitly requires string parity).

    **File 1: `src/python/tests/integration/test_controller/test_extract/test_extract.py`**

    Current state (verified): `import shutil` is at line 2; `import unittest` is at line 1; `class TestExtract(unittest.TestCase):` is at line 12; lines 10 and 11 are blank.

    Insert the decorator on the blank line immediately before `class TestExtract`. Replace the single blank line at line 11 with the 4-line decorator below (no new import needed — `shutil` already imported at line 2):

        @unittest.skipIf(
            shutil.which("rar") is None,
            "rar binary not available on this architecture (Debian ships rar for amd64/i386 only, RARLAB has no aarch64 Linux build)"
        )

    Post-edit excerpt (lines 8-14 after edit):
    - Line 8: `from common import overrides`
    - Line 9: `from controller.extract import Extract, ExtractError`
    - Line 10: (blank)
    - Line 11: (blank)
    - Line 12: `@unittest.skipIf(`
    - Line 13: `    shutil.which("rar") is None,`
    - Line 14: `    "rar binary not available on this architecture (Debian ships rar for amd64/i386 only, RARLAB has no aarch64 Linux build)"`
    - Line 15: `)`
    - Line 16: `class TestExtract(unittest.TestCase):`

    Do NOT add a blank line between the closing `)` of the decorator and the `class` keyword — the decorator must be adjacent to the class declaration (PEP 8 decorator style + existing `test_stream_log.py:9` analog).

    **File 2: `src/python/tests/integration/test_controller/test_controller.py`**

    Current state (verified): `import shutil` is at line 5; `import unittest` is at line 1; `class TestController(unittest.TestCase):` is at line 47; line 46 is blank; lines 23-44 define helper classes `DummyListener` and `DummyCommandCallback` (do NOT decorate these — only `TestController`).

    Insert the SAME 4-line decorator on the blank line immediately before `class TestController`. Replace the blank line at line 46 with the 4-line decorator, so the `class TestController(unittest.TestCase):` line shifts down by 3 lines.

    Post-edit excerpt (lines 44-51 after edit):
    - Line 44: (end of `DummyCommandCallback` body)
    - Line 45: (blank)
    - Line 46: (blank)
    - Line 47: `@unittest.skipIf(`
    - Line 48: `    shutil.which("rar") is None,`
    - Line 49: `    "rar binary not available on this architecture (Debian ships rar for amd64/i386 only, RARLAB has no aarch64 Linux build)"`
    - Line 50: `)`
    - Line 51: `class TestController(unittest.TestCase):`

    Critical rules (from 80-PATTERNS.md § `test_extract.py` + § `test_controller.py`):
    - The skip-reason string MUST be byte-identical in both files so `grep -cE 'SKIPPED.*rar binary not available'` counts skips across both files with one regex (RESEARCH §4.8 verification command).
    - Decorator must be `@unittest.skipIf` (conditional) — NOT `@unittest.skip` (unconditional). The only existing `@unittest.skip` in the repo (`test_stream_log.py:9`) is a structural analog, not a pattern to copy literally.
    - Do NOT decorate the helper classes `DummyListener` (line 23) or `DummyCommandCallback` (line 37) in `test_controller.py` — only `TestController`.
    - Do NOT add a new `import shutil` — both files already have it.
    - Do NOT wrap the skip-reason string across source lines inside the f-string or use f-string formatting — keep it a plain literal string.

    After both files are edited, run a local pytest unit sanity against the unit-test subset (which does not require `rar` either way) to confirm no syntax errors were introduced:

        cd src/python && poetry run pytest tests/unittests/ -q
  </action>
  <verify>
    <automated>grep -cE 'shutil\.which\("rar"\) is None' src/python/tests/integration/test_controller/test_extract/test_extract.py src/python/tests/integration/test_controller/test_controller.py | awk -F: '$2 != "1" {exit 1}' &amp;&amp; cd src/python &amp;&amp; poetry run pytest tests/unittests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'shutil.which("rar") is None' src/python/tests/integration/test_controller/test_extract/test_extract.py` returns `1`.
    - `grep -c 'shutil.which("rar") is None' src/python/tests/integration/test_controller/test_controller.py` returns `1`.
    - `grep -cE '@unittest\.skipIf' src/python/tests/integration/test_controller/test_extract/test_extract.py` returns `1`.
    - `grep -cE '@unittest\.skipIf' src/python/tests/integration/test_controller/test_controller.py` returns `1`.
    - `grep -c 'rar binary not available on this architecture (Debian ships rar for amd64/i386 only, RARLAB has no aarch64 Linux build)' src/python/tests/integration/test_controller/test_extract/test_extract.py src/python/tests/integration/test_controller/test_controller.py` — each file returns `1` (string parity).
    - `cd src/python && poetry run pytest tests/unittests/ -q` exits `0` (unit subset still green; catches syntax errors from decorator insertion).
    - No new `import shutil` line added (both files already had it): `grep -c '^import shutil$' src/python/tests/integration/test_controller/test_extract/test_extract.py` returns `1`; same for `test_controller.py`.
  </acceptance_criteria>
  <done>
    Both test files carry the class-level `@unittest.skipIf(shutil.which("rar") is None, ...)` decorator with the identical skip-reason string; unit-test subset still green locally; no new imports added.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [MANUAL — arm64 host required] Verify `make run-tests-python` completes on Apple Silicon + amd64 parity</name>
  <files>(verification-only; no files modified by this task)</files>
  <read_first>
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-VALIDATION.md` § Manual-Only Verifications (table row: arm64 full-suite run)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt` (captured in Task 1 — needed for the amd64 parity comparison)
    - `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-RESEARCH.md` §4.8 (the four verification commands)
  </read_first>
  <what-built>
    - Dockerfile arch-conditional `rar` install (Task 2)
    - Class-level skipIf decorators on `TestExtract` + `TestController` (Task 3)
    - Pre-edit amd64 collection-count baseline file (Task 1)
  </what-built>
  <action>
    [CHECKPOINT: this task requires a human executor on an Apple Silicon (arm64) macOS host. The actions below are the verification steps the human runs manually. Each step produces output to be captured in the phase SUMMARY.]

    Verify host architecture first:

        uname -m
        # Expected: arm64

    If not arm64, STOP — this checkpoint cannot be completed on a non-arm64 host. Record the blocker in the phase SUMMARY.

    **Step 1: arm64 full-suite run (ROADMAP success criterion 2).**

        make run-tests-python
        echo "exit_code=$?"

    Expected:
    - `exit_code=0` (previously: non-zero because `apt-get install -y rar` aborted on arm64).
    - Build log contains the line: `Skipping rar install on arm64 (Debian rar is amd64/i386 only)`.
    - pytest summary prints SKIPPED messages for every test method in `TestExtract` and `TestController` with the documented skip reason.
    - No ERROR (as opposed to SKIPPED) statuses attributable to missing `rar`.

    Capture the final pytest summary line (e.g. `= 1117 passed, 17 skipped, ... =`) for the SUMMARY.

    **Step 2: arm64 skip-count assertion (80-VALIDATION.md row 6).**

        docker run --platform linux/arm64 --rm \
            -v "$(pwd)/src/python:/src" \
            seedsyncarr/test/python \
            pytest -v 2>&1 | grep -cE 'SKIPPED.*rar binary not available'

    Record the integer. Expected: equals the count of test methods in `TestExtract` plus `TestController` combined. The executor records this expected number at this step (determined empirically from the first arm64 run) and the phase SUMMARY pins it as the regression anchor for future arm64 runs.

    **Step 3: amd64 parity assertion (80-VALIDATION.md row 5).**

    Read the pre-edit baseline:

        source .planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-AMD64-BASELINE.txt
        echo "baseline=${BASELINE_COLLECT_COUNT}"

    Rebuild the test image on amd64 with the edited Dockerfile, then compare:

        docker buildx build --platform linux/amd64 \
            -f src/docker/test/python/Dockerfile \
            -t seedsyncarr/test/python:post-edit-amd64 \
            --load \
            .
        POST_EDIT_COUNT=$(docker run --platform linux/amd64 --rm \
            -v "$(pwd)/src/python:/src" \
            seedsyncarr/test/python:post-edit-amd64 \
            pytest --collect-only 2>&1 | grep -c '<Function')
        echo "post_edit_count=${POST_EDIT_COUNT}"
        test "${POST_EDIT_COUNT}" = "${BASELINE_COLLECT_COUNT}" && echo "PASS: amd64 parity" || echo "FAIL: amd64 drift"

    Expected: `PASS: amd64 parity`. Any drift → STOP and investigate (the decorators must be inert on amd64 because `shutil.which("rar")` returns a real path there — skipIf predicate is False — so test count must not change).

    **Step 4: CI file byte-parity (80-VALIDATION.md row 7).**

        git diff --stat .github/workflows/ci.yml | wc -l
        # Expected: 0

    **Step 5: Record outcomes.**

    Append the following block to the phase SUMMARY (the execute-phase workflow will create the SUMMARY file; the executor fills in values here):
    - Host `uname -m` output
    - arm64 `make run-tests-python` exit code + final pytest summary line
    - arm64 skip count from Step 2
    - amd64 post-edit collection count vs baseline (both integers)
    - `git diff --stat .github/workflows/ci.yml` output
  </action>
  <how-to-verify>
    Duplicate of `<action>` above — this block names the manual-verify steps for the checkpoint UI presenter. See `<action>` for the full procedure.
  </how-to-verify>
  <verify>
    <automated>MANUAL — checkpoint:human-verify — requires arm64 macOS host; executor runs `make run-tests-python` and the four verification steps described in the action block, then records outcomes in the phase SUMMARY. No CI-runnable automated equivalent exists (CI matrix does not cover linux/arm64 for Python tests per RESEARCH §7.2).</automated>
  </verify>
  <acceptance_criteria>
    - `uname -m` on the executor host returns `arm64`.
    - `make run-tests-python` exits `0` on the arm64 host.
    - `docker run --platform linux/arm64 ... pytest -v 2>&1 | grep -cE 'SKIPPED.*rar binary not available'` returns a positive integer equal to the combined test-method count in `TestExtract` + `TestController` (pinned in the SUMMARY as the regression anchor).
    - `POST_EDIT_COUNT` from the amd64 rebuild equals `BASELINE_COLLECT_COUNT` from `80-02-AMD64-BASELINE.txt` (PASS: amd64 parity).
    - `git diff --stat .github/workflows/ci.yml | wc -l` returns `0`.
    - Phase SUMMARY contains the 5 recorded items from Step 5.
  </acceptance_criteria>
  <done>
    Human executor has run all 4 steps on an arm64 host, all 4 pass, and the phase SUMMARY records the host arch, arm64 exit code + pytest summary line, arm64 skip count, amd64 parity result, and CI-file diff status.
  </done>
  <resume-signal>
    Type "approved" to proceed, or describe any failure (specific command + output) and the executor will fix and re-verify.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host build → Docker image | Dockerfile arch-conditional branch determines package set; amd64 CI hosts and arm64 dev hosts take different paths. |
| pytest collection → test execution | Skip predicates gate whether class-level `setUpClass` runs. |
| Dockerfile edit → CI workflow | Indirect: if the edit changed CI behavior, amd64 jobs would surface it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-05 | Tampering | Dockerfile arch-gate accidentally skipping `rar` on amd64 CI | mitigate | Task 2 acceptance criterion runs `docker buildx build --platform linux/amd64` post-edit — must succeed. Task 4 Step 3 asserts amd64 pytest collection count equals pre-edit baseline. `i386` included in the amd64-positive branch for Debian parity (RESEARCH §4.1). |
| T-80-06 | Denial of Service | `TestExtract.setUpClass` raising `subprocess.CalledProcessError` on arm64 because `rar` is missing, aborting the test run with ERROR instead of SKIPPED | mitigate | Class-level `@unittest.skipIf` fires BEFORE `setUpClass` (RESEARCH §8.3). Task 4 Step 1 explicitly checks for SKIPPED status, not ERROR. |
| T-80-07 | Information Disclosure | Skip predicate drifting such that a test class silently skips on amd64 when `rar` is present | mitigate | Predicate is `shutil.which("rar") is None` — if `which("rar")` returns a path on amd64 CI, the skip does not fire. If PATH drift ever renders `rar` invisible on amd64 CI, that surfaces as a CI regression (many tests skip that previously ran) — which is exactly the signal we want, not a threat to hide. Task 4 Step 3 pins the amd64 collection count via the baseline. |
| T-80-08 | Tampering | `.github/workflows/ci.yml` accidentally modified by the editor | mitigate | Task 2 + Task 4 acceptance criterion: `git diff --stat .github/workflows/ci.yml | wc -l` returns `0`. |

**Severity assessment:** All threats are medium/low. The amd64 parity assertion (Task 4 Step 3) and the CI-file byte-parity assertion (Task 4 Step 4) are the two hard gates that catch any regression. No threat blocks the phase.
</threat_model>

<verification>
- `src/docker/test/python/Dockerfile` has the `dpkg --print-architecture` arch gate; `rar` installs on amd64/i386 only; `unrar` still installs unconditionally.
- `src/python/tests/integration/test_controller/test_extract/test_extract.py` and `src/python/tests/integration/test_controller/test_controller.py` each carry a class-level `@unittest.skipIf(shutil.which("rar") is None, ...)` decorator with a byte-identical skip-reason string.
- On arm64 host: `make run-tests-python` exits `0`; pytest shows SKIPPED (not ERROR) for `TestExtract` + `TestController`; build log mentions "Skipping rar install on arm64".
- On amd64: pytest collection count equals pre-edit baseline (captured in `80-02-AMD64-BASELINE.txt`).
- `.github/workflows/ci.yml` unchanged (`git diff --stat` returns `0` lines).
</verification>

<success_criteria>
- [ ] `docker buildx build --platform linux/amd64 -f src/docker/test/python/Dockerfile ...` exits 0 post-edit.
- [ ] `make run-tests-python` exits 0 on arm64 host (Task 4 manual verification).
- [ ] `grep -cE 'SKIPPED.*rar binary not available'` on arm64 pytest output equals the documented expected skip count (pinned in SUMMARY).
- [ ] amd64 `pytest --collect-only | grep -c '<Function'` equals `BASELINE_COLLECT_COUNT` from Task 1 baseline file.
- [ ] `git diff --stat .github/workflows/ci.yml | wc -l` returns `0`.
- [ ] Both test files have exactly one `@unittest.skipIf` decorator each, with byte-identical skip-reason strings.
</success_criteria>

<output>
After completion, create `.planning/phases/80-small-cleanups-dependabot-arm64-enum/80-02-SUMMARY.md` recording:
- Host `uname -m` for the arm64 verification (should be `arm64`).
- arm64 `make run-tests-python` exit code + final pytest summary line (passed/skipped counts).
- Pinned arm64 skip count (regression anchor for future runs).
- amd64 baseline vs post-edit collection counts (both integers; must be equal).
- Confirmation that `.github/workflows/ci.yml` was untouched.
- The exact Dockerfile diff (before/after, 12-line replacement block).
</output>
