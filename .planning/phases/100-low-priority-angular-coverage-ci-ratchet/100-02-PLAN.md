---
phase: 100-low-priority-angular-coverage-ci-ratchet
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/tests/unittests/services/utils/auth.interceptor.spec.ts
autonomous: true
requirements: [COVLOW-04]

must_haves:
  truths:
    - "A regression test exists in auth.interceptor.spec.ts proving token rotation: request carries `Bearer token-v1`, then after the meta tag changes and `_resetAuthInterceptorCache()` is called, the next request carries `Bearer token-v2`."
    - "The test is the mirror positive of the existing cache test (94-114): same setup, the one added step being a single mid-test `_resetAuthInterceptorCache()` between the two requests."
    - "A code comment names the ACCURATE page-reload coupling (full page reload re-instantiates the module and resets the cache; NOT version-check.service.ts, which performs no reload)."
    - "The full Angular unit suite stays green."
  artifacts:
    - path: "src/angular/src/app/tests/unittests/services/utils/auth.interceptor.spec.ts"
      provides: "Token-rotation regression test via the _resetAuthInterceptorCache seam"
      contains: "_resetAuthInterceptorCache"
  key_links:
    - from: "src/angular/src/app/tests/unittests/services/utils/auth.interceptor.spec.ts"
      to: "auth.interceptor.ts getApiToken cache + _resetAuthInterceptorCache seam"
      via: "HttpTestingController request header assertions before/after the cache reset"
      pattern: "Bearer token-v2"
---

<objective>
Add a regression test (COVLOW-04) that exercises the `auth.interceptor.ts` token-rotation path through the documented `_resetAuthInterceptorCache()` seam: meta tag set to `token-v1` → first request carries `Bearer token-v1`; mutate the meta tag to `token-v2` → call `_resetAuthInterceptorCache()` once → second request carries `Bearer token-v2`.

Purpose: This is the mirror positive of the existing `"should cache the token and not re-read meta tag on each request"` test (lines 94-114), which proves stale-by-design WITHOUT a reset. The pair fully documents the module-level cache lifecycle. Without this test, a refactor bypassing the cache-reset path would leave a stale Bearer token after rotation (a security-adjacent regression).

Output: One new `it()` appended to the existing `describe` in `auth.interceptor.spec.ts`, reusing `setupWithMeta` and `afterEach` verbatim. A code comment naming the real page-reload coupling (D-04). No new files; no production code change (D-09 — expected green on first run).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-CONTEXT.md
@.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-RESEARCH.md
@.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — no codebase exploration required. -->

From src/angular/src/app/services/utils/auth.interceptor.ts:
- `getApiToken` cache (lines 7-17): module-level `cachedToken` / `tokenRead`; reads `<meta name="api-token">` ONCE, then serves the cached value.
- `authInterceptor` (lines 24-33): attaches `Authorization: Bearer <token>` when a non-empty token is present.
- `_resetAuthInterceptorCache()` (lines 39-42): `@internal` seam — resets `cachedToken = null; tokenRead = false`. The ONLY way (besides module re-instantiation) to force a fresh meta-tag read.

From src/angular/src/app/tests/unittests/services/utils/auth.interceptor.spec.ts (the file being EXTENDED — imports already present at lines 1-5; NO new imports needed):
- `setupWithMeta(content: string | null)` (lines 11-38): removes any existing meta tag, appends `<meta name="api-token" content="...">` (when content !== null), calls `_resetAuthInterceptorCache()` (line 27), configures TestBed with `provideHttpClient(withInterceptors([authInterceptor]))` + `provideHttpClientTesting()`, injects `httpClient` + `httpMock`. NOTE: it ALREADY resets the cache — do NOT add an extra reset before req1 (RESEARCH Pitfall 4).
- `afterEach` (lines 40-48): `httpMock.verify()`, removes the meta tag, calls `_resetAuthInterceptorCache()` (line 47). Reuse verbatim — do NOT duplicate.
- Existing cache test to MIRROR (lines 94-114): `setupWithMeta("cached-token")` → req1 `Bearer cached-token` → mutate meta to `"new-token"` WITHOUT reset → req2 still `Bearer cached-token` (stale). The new test is identical except it calls `_resetAuthInterceptorCache()` between the requests, flipping the second assertion to the NEW token.

Production coupling fact (D-04, corrected per RESEARCH §"Version-Check Service — Coupling Clarification"): `version-check.service.ts` does NOT call `window.location.reload()` — it only checks GitHub and shows a banner. Token rotation in production is realized by a full page reload (user navigates to `/` or reloads), which re-fetches index.html, re-instantiates the Angular module, and resets `cachedToken`/`tokenRead`. The comment MUST name THIS mechanism — do NOT attribute the reload to version-check.service.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token-rotation regression test via the _resetAuthInterceptorCache seam</name>
  <files>src/angular/src/app/tests/unittests/services/utils/auth.interceptor.spec.ts</files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/utils/auth.interceptor.spec.ts (the file being extended — read the `setupWithMeta` helper 11-38, the `afterEach` 40-48, and the cache test to mirror at 94-114)
    - src/angular/src/app/services/utils/auth.interceptor.ts (the cache 7-17 and the `_resetAuthInterceptorCache` seam 39-42 — to confirm the reset semantics)
    - .planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-PATTERNS.md §"auth.interceptor.spec.ts — EXTEND" (the exact mirror structure and the HttpTestingController flush discipline)
  </read_first>
  <behavior>
    - Test: `setupWithMeta("token-v1")` → first GET → `Authorization` header is `Bearer token-v1`.
    - Test: meta tag mutated to `"token-v2"` + single `_resetAuthInterceptorCache()` → second GET → `Authorization` header is `Bearer token-v2`.
  </behavior>
  <action>
    Append one new `it()` to the existing `describe` block in `auth.interceptor.spec.ts` (after the existing tests, e.g. after the cache test at line 114 or near the other `it()` blocks). Per D-03 (mirror of the cache test, one added reset step):
    1. Open the test body with a code comment naming the ACCURATE production coupling (D-04): production has NO in-app caller of `_resetAuthInterceptorCache`; token rotation is realized by a full page reload (user navigates to `/` or reloads), which re-fetches index.html, re-instantiates the Angular module, and resets `cachedToken`/`tokenRead`. The test pins the mechanism the reload relies on. Do NOT claim `version-check.service.ts` triggers a reload — per RESEARCH §coupling-clarification it does not (it only checks GitHub and shows a banner).
    2. `setupWithMeta("token-v1")` — do NOT add an extra `_resetAuthInterceptorCache()` here; setupWithMeta already calls it at line 27 (RESEARCH Pitfall 4).
    3. First request: `httpClient.get("/api/first", {responseType: "text"}).subscribe();` → `const req1 = httpMock.expectOne("/api/first");` → `expect(req1.request.headers.get("Authorization")).toBe("Bearer token-v1");` → `req1.flush("ok");` (flush is mandatory — `afterEach`'s `httpMock.verify()` fails on unflushed requests; PATTERNS §"HttpTestingController flush discipline").
    4. Simulate rotation: query `document.querySelector("meta[name=\"api-token\"]")`, guard the nullable result before use (`if (meta) { meta.setAttribute("content", "token-v2"); }` — never dereference a possibly-null query result, per CLAUDE.md null-safety; mirrors the existing cache test's own null guard), then call `_resetAuthInterceptorCache()` EXACTLY ONCE — this is the intentional mid-test reset that proves rotation (the existing cache test omits it, proving the stale case).
    5. Second request: `httpClient.get("/api/second", {responseType: "text"}).subscribe();` → `const req2 = httpMock.expectOne("/api/second");` → `expect(req2.request.headers.get("Authorization")).toBe("Bearer token-v2");` → `req2.flush("ok");`.
    Use the existing imports (line 5 already imports both `authInterceptor` and `_resetAuthInterceptorCache`) — no new imports. Reuse `afterEach` verbatim (do not add a new one). Name the test descriptively, e.g. "should serve new token after _resetAuthInterceptorCache is called (token rotation)". No production change (D-09); if the test goes red, STOP and report (a red here would mean the reset seam does not actually reset — out of this plan's trivial-fix scope).
  </action>
  <verify>
    <automated>cd src/angular && ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/auth.interceptor.spec.ts</automated>
    <fallback>make run-tests-angular  # if `ng` is not on host PATH — run via Docker</fallback>
  </verify>
  <acceptance_criteria>
    - auth.interceptor.spec.ts contains a new `it()` whose body calls `setupWithMeta("token-v1")` then, after the first request, mutates the meta tag to `"token-v2"` and calls `_resetAuthInterceptorCache()` exactly once.
    - The test asserts `toBe("Bearer token-v1")` on the first request and `toBe("Bearer token-v2")` on the second.
    - Both `expectOne` results are flushed (`req1.flush(...)`, `req2.flush(...)`).
    - A code comment in the test names the full-page-reload coupling and does NOT attribute a reload to `version-check.service.ts`.
    - The meta-tag query result is null-guarded before `setAttribute`.
    - The spec exits 0 (new test + all existing tests green).
    - `cd src/angular && npx eslint "src/app/tests/unittests/services/utils/auth.interceptor.spec.ts" --max-warnings 0` passes.
  </acceptance_criteria>
  <done>The rotation test passes: first request carries `Bearer token-v1`, second carries `Bearer token-v2`; no extra reset before req1; afterEach reused; lint clean; full spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-rendered `<meta name="api-token">` → Angular `authInterceptor` | The Bottle backend injects the API token into the SPA shell HTML; the interceptor reads it and attaches `Authorization: Bearer <token>` to every API request. This plan adds a TEST over that boundary; it does not change the interceptor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-02 | Spoofing / stale credential | `auth.interceptor.ts` token cache (`getApiToken` / `_resetAuthInterceptorCache`) | mitigate (by test, no code change) | The new regression test pins that after token rotation (meta change + cache reset) the next request carries the NEW Bearer token. A future refactor that bypassed the cache-reset path — leaving a stale token after rotation — would now fail this test. Paired with the existing cache test, the cache lifecycle is fully documented. No production change (D-09). |
| T-100-02-R | Repudiation / coupling clarity | Documentation of the rotation mechanism | accept | The in-app `_resetAuthInterceptorCache` has no production caller; rotation relies on a full page reload. The test's code comment documents this accurately (D-04). No in-app rotation path is added (that would be a feature → v1.4.0, explicitly out of scope). |

*Posture (D-09): additive regression net over already-correct, security-adjacent (Bearer-token) code. No production behavior change, no new attack surface, no in-app rotation path added. No package installs (no T-100-*-SC needed).*
</threat_model>

<verification>
- `cd src/angular && ng test --watch=false --browsers=ChromeHeadlessCI --include=src/app/tests/unittests/services/utils/auth.interceptor.spec.ts` exits 0 with the new test green (fallback: `make run-tests-angular`).
- The new test's second assertion is `Bearer token-v2` (rotation succeeded) — contrasting the existing cache test's stale `Bearer cached-token`.
- `npx eslint` on the spec passes with `--max-warnings 0`.
- No production source files modified (only the spec extended).
</verification>

<success_criteria>
- ROADMAP success criterion #2 satisfied: a regression test exercises the `auth.interceptor.ts` token rotation path — meta tag changes, `_resetAuthInterceptorCache` is called, and the next request carries the new token (COVLOW-04).
- The code comment accurately documents the page-reload coupling (D-04).
- Full Angular unit suite remains green.
</success_criteria>

<output>
Create `.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-02-SUMMARY.md` when done.
</output>
