---
phase: 100-low-priority-angular-coverage-ci-ratchet
plan: 03
type: execute
wave: 2
depends_on: [100-01, 100-02]
files_modified:
  - src/angular/karma.conf.js
  - src/docker/test/angular/Dockerfile
  - src/python/pyproject.toml
  - .planning/ROADMAP.md
  - .planning/RETROSPECTIVE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [RATCHET-02]

must_haves:
  truths:
    - "Post-Phase-99 Python coverage is RE-MEASURED fresh against current HEAD as a CONTAINER-INCLUSIVE number (real-lftp integration suite INCLUDED, run inside the test container which has the `lftp` binary + `sshd`) — NOT the provisional host-only 85.19% and NOT a stale number. Angular is re-measured headless with `--code-coverage`. The baseline anchor (v1.3.0-COVERAGE-BASELINE.md, lines 41-91) mandates the container-inclusive number as the honest 'before'."
    - "D-05: `karma.conf.js` has a `coverageReporter.check.global` block (statements/branches/functions/lines) plus a headless reporter (text-summary), AND `src/docker/test/angular/Dockerfile` CMD includes `--code-coverage` so the gate actually runs and fails below threshold (a check block without the flag is a silent no-op)."
    - "D-06: `[tool.coverage.report] fail_under` in pyproject.toml is bumped above 84 (NOT a `[tool.pytest.ini_options]` `--cov-fail-under`), and all config edits land in ONE commit (single revert target)."
    - "D-07: The ratchet is monotonic — every COMPUTED floor is strictly greater than the prior floor (Python floor strictly > 84); the floor is `floor(container-inclusive measured) − margin` with margin tightened so the COMPUTED floor (not the raw measurement) stays > 84; the chosen margin/numbers are logged in PROJECT.md Key Decisions."
    - "D-08: Before/after numbers recorded in ROADMAP.md Coverage Ratchet table AND the v1.3.0 RETROSPECTIVE entry, comparing against the v1.3.0 baseline anchor as 'Before'. The Python 'before' is the container-inclusive re-measured number per the baseline mandate."
  artifacts:
    - path: "src/angular/karma.conf.js"
      provides: "Angular coverage gate (check.global) + headless text-summary reporter"
      contains: "check:"
    - path: "src/docker/test/angular/Dockerfile"
      provides: "CI invocation that actually collects coverage"
      contains: "--code-coverage"
    - path: "src/python/pyproject.toml"
      provides: "Raised Python coverage floor"
      contains: "fail_under"
    - path: ".planning/ROADMAP.md"
      provides: "Coverage Ratchet Before/After table filled"
      contains: "After"
    - path: ".planning/RETROSPECTIVE.md"
      provides: "v1.3.0 milestone retro entry with coverage numbers"
      contains: "v1.3.0"
    - path: ".planning/PROJECT.md"
      provides: "Logged floor/margin decision (D-07)"
      contains: "fail_under"
  key_links:
    - from: "src/docker/test/angular/Dockerfile (--code-coverage CMD)"
      to: "src/angular/karma.conf.js (coverageReporter.check.global)"
      via: "ng test --code-coverage triggers Istanbul instrumentation which the check block evaluates"
      pattern: "code-coverage"
---

<objective>
Establish the Angular CI coverage gate and raise the Python coverage floor (RATCHET-02), recording before/after numbers and the floor decision. This closes milestone v1.3.0.

Purpose: Phases 97-100 added Python and Angular tests; this plan ratchets CI thresholds up to lock in that gain so future PRs cannot silently regress coverage. CRITICAL (RESEARCH §"CI Wiring — THE Critical Unknown"): the Angular side is NET-NEW — `karma.conf.js` has no `check.global` block and the Dockerfile CMD has no `--code-coverage` flag, so a `check` block alone would be a silent no-op. Both `karma.conf.js` and the Dockerfile must be patched together (D-05). The Python side is a single `fail_under` bump (D-06 — confirmed at `pyproject.toml:88`; there is NO `--cov-fail-under` in `[tool.pytest.ini_options]`).

CRITICAL (re-measurement methodology — v1.3.0-COVERAGE-BASELINE.md lines 41-91): the 85.19% baseline Python number is PROVISIONAL — host-only, EXCLUDES the real-lftp integration suite (the host has no `lftp` binary / `sshd`). The baseline explicitly mandates that Phase 100 re-measure a CONTAINER-INCLUSIVE Python "before" — `pytest --cov --cov-report=term-missing` run INSIDE the test container (which has `lftp` + `sshd`), with the real-lftp integration suite INCLUDED. The container CMD (`src/docker/test/python/Dockerfile:60`) is currently plain `pytest -v -p no:cacheprovider` (no `--cov`), so the inclusive-coverage path is NOT wired — Task 1 obtains it by overriding the CMD on a one-off container run (exact command below). The "before" recorded in ROADMAP/RETROSPECTIVE and the ratchet floor MUST both be derived from this container-inclusive number, NOT the provisional host-only 85.19%.

The repo-root `make coverage-python` target (`Makefile:138-139` → `cd ${SOURCEDIR}/python && poetry run pytest --cov ...`) is HOST-only / provisional (excludes the real-lftp suite); it is useful only as a sanity reference, not as the ratchet source. NOTE: `make coverage-python` MUST be invoked from the repo root — there is NO `src/python/Makefile`, so `cd src/python && make coverage-python` would fail (make would find no Makefile).

Output: Three config files patched in ONE commit (D-06 — one revert target); before/after numbers in ROADMAP.md + RETROSPECTIVE.md; floor/margin decision in PROJECT.md (D-07/D-08). No production source change.

Note on success criterion #4 ("CI green on ratcheted thresholds across amd64+arm64"): the executor CANNOT run the arm64 matrix locally — this is a CI-gated acceptance observed AFTER push. The config edits in this plan are autonomous; the green-CI confirmation is a post-push observation, not a local verify step. (Per RESEARCH §"arm64 vs amd64", the `unittests-angular` CI job runs only on amd64; arm64 is E2E-only — no matrix change is needed for RATCHET-02.)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3.0-COVERAGE-BASELINE.md
@.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-CONTEXT.md
@.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-RESEARCH.md
@.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-PATTERNS.md

<interfaces>
<!-- Current state of every file to patch. Extracted from live source — no exploration required. -->

src/angular/karma.conf.js (lines 23-26) — CURRENT:
  coverageReporter: { type: 'html', dir: 'coverage/' }
  No `reporters` array, no `check.global`. (Target: multi-reporter array + check.global — see PATTERNS §karma.conf.js.)

src/docker/test/angular/Dockerfile (lines 31-33) — CURRENT CMD:
  CMD ["node", "/app/node_modules/@angular/cli/bin/ng.js", "test", "--browsers", "ChromeHeadless", "--watch=false"]
  No `--code-coverage`. (Target: append "--code-coverage" as the final array element.)

src/python/pyproject.toml (lines 87-88) — CURRENT:
  [tool.coverage.report]
  fail_under = 84
  `[tool.pytest.ini_options]` (lines 69-77) has NO `--cov`/`--cov-fail-under` (confirmed) — only this one number is bumped.

.planning/milestones/v1.3.0-COVERAGE-BASELINE.md — the "Before" anchor (Phase 97, against main HEAD):
  Python 85.19% (HOST/PROVISIONAL — EXCLUDES the real-lftp integration suite; baseline lines 41-51 mandate Phase 100 re-measure a CONTAINER-INCLUSIVE "before" which is expected to be HIGHER).
  Angular Stmts 83.34% / Branches 69.01% / Functions 79.73% / Lines 84.21% (no Karma global threshold configured then)

.planning/ROADMAP.md — Coverage Ratchet table (lines 306-310): two rows ending "TBD (Phase 100)" to fill with After numbers + chosen floors.

karma-coverage 2.2.1 (installed) — `coverageReporter.reporters` array syntax and `check.global` keys (statements/branches/functions/lines) are documented features. Do NOT set `emitWarning: true` (would make the gate non-fatal — RESEARCH Pitfall 5).

Container test harness (for the container-inclusive Python re-measure):
  src/docker/test/python/Dockerfile — CMD `["pytest", "-v", "-p", "no:cacheprovider"]` (line 60; NO `--cov`). WORKDIR `/src`, `PYTHONPATH=/src`, has `lftp`/`sshd` + the entrypoint that starts sshd. Image tag `seedsyncarr/test/python`.
  src/docker/test/python/compose.yml — service `tests`, binds `src/python` → `/src` READ-ONLY. (Read-only mount ⇒ use `--cov-report=term-missing` to stdout; do NOT request `--cov-report=html` which would try to write into `/src`.)

Re-measure commands (RESEARCH §"Re-Measurement Commands" + baseline lines 85-91):
  Python (CONTAINER-INCLUSIVE — this is the ratchet source): first build the image (`make tests-python`), then run pytest with `--cov` INSIDE the container so the real-lftp suite (which needs the `lftp` binary + `sshd`) is included. Use the existing compose service with a CMD override, e.g. (from repo root):
    `make tests-python && docker compose -f src/docker/test/python/compose.yml run --rm tests pytest --cov --cov-report=term-missing -p no:cacheprovider`
    (the entrypoint starts sshd before exec'ing the override; capture the printed `TOTAL` line coverage %). Record this CONTAINER-INCLUSIVE total — this is the "before"/ratchet source. The host `make coverage-python` (from REPO ROOT — there is no `src/python/Makefile`) is provisional-only and may be run as a cross-check, but is NOT the ratchet source.
  Angular: `make run-tests-angular` AFTER patching the Dockerfile CMD (coverage summary printed by text-summary reporter), OR `cd src/angular && ng test --code-coverage --watch=false --browsers=ChromeHeadlessCI` if `ng`/Chrome are available locally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure post-99 coverage fresh against current HEAD (Python container-inclusive)</name>
  <files>(measurement only — no file edits; produces numbers for Tasks 2-3)</files>
  <read_first>
    - .planning/milestones/v1.3.0-COVERAGE-BASELINE.md (READ lines 41-91 carefully — the provisional-vs-container-inclusive distinction and the mandated container re-measure methodology; this is the "Before" anchor)
    - .planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-RESEARCH.md §"Re-Measurement Commands" + §"Python/Angular Ratchet Floor Calculation"
    - src/docker/test/python/Dockerfile + src/docker/test/python/compose.yml (the container that has lftp+sshd; CMD has no --cov so Task 1 overrides it)
    - src/python/pyproject.toml (the `[tool.coverage.report]` section + confirm `[tool.pytest.ini_options]` has no `--cov-fail-under`)
  </read_first>
  <action>
    Re-measure coverage fresh — do NOT reuse the stale baseline number (RESEARCH Pitfall 6; the baseline is the "Before" anchor only). This task must run AFTER 100-01 and 100-02 have landed (this plan is Wave 2 / depends_on those plans), so the Angular numbers reflect the new SSE + auth tests.
    1. Python (CONTAINER-INCLUSIVE — per baseline lines 41-91, this is the honest "before" and the ratchet source; the host 85.19% is PROVISIONAL/host-only and EXCLUDES the real-lftp suite). Build the test image then run pytest with `--cov` INSIDE the container (which has the `lftp` binary + `sshd`) so the `tests/integration/test_lftp/*` suite is INCLUDED. From the repo root:
         `make tests-python`
         `docker compose -f src/docker/test/python/compose.yml run --rm tests pytest --cov --cov-report=term-missing -p no:cacheprovider`
       (the entrypoint starts sshd; the bind mount is read-only so use `term-missing` to stdout, NOT html). Record the printed `TOTAL` line coverage % — this CONTAINER-INCLUSIVE number is the ratchet source and the "before". Expect it to be ≥ the provisional 85.19% (the real-lftp suite adds covered lines). OPTIONAL cross-check only: the host provisional number via `make coverage-python` run FROM THE REPO ROOT (there is NO `src/python/Makefile`, so do NOT `cd src/python` first — that would fail). The host number is provisional and is NOT used for the floor.
    2. Angular: obtain the four metrics (statements / branches / functions / lines). Preferred path is the Docker route AFTER Task 2 patches the Dockerfile CMD with `--code-coverage` (`make run-tests-angular` → text-summary reporter prints the 4-line summary to stdout); alternatively run `cd src/angular && ng test --code-coverage --watch=false --browsers=ChromeHeadlessCI` if `ng` + Chrome are available locally. Either is acceptable so long as the number is fresh (Claude's discretion per CONTEXT). NOTE the ordering: if you measure Angular via the Docker route you may interleave with Task 2 (patch the Dockerfile first, then `make run-tests-angular` reads the summary) — that is fine since both land in the same commit; just capture the four numbers before writing thresholds.
    3. Compute the floors (D-07): for each metric, threshold = floor(container-inclusive measured%) − margin, where margin is within a ~0–1% band — TIGHTENED as needed so the COMPUTED floor is strictly greater than the prior floor. CRITICAL monotonicity guard (operate on the COMPUTED floor, NOT the raw measurement): the chosen Python `fail_under` floor MUST be strictly > 84 to be a real ratchet (D-07). Worked example: if the container-inclusive Python measurement is ~85.x%, then `floor(85.x) − 1 = 84` is NOT a ratchet (equal to the current floor, not greater) — in that case TIGHTEN the margin (e.g. margin 0 → floor 85, or a sub-1% margin that still rounds to ≥ 85) so the computed floor is ≥ 85 (> 84). If the container-inclusive measurement is high enough that `floor(measured) − 1 > 84` (e.g. measured ≥ 86 → floor 85), the ~0.5–1% margin is fine. STOP and report (do NOT lower or pin at 84) ONLY if the container-inclusive measurement is so low that no margin in band yields a computed floor > 84 (i.e. measured < 85 — not expected, since even the provisional host number was 85.19% and the container-inclusive number is expected higher). Apply the same "computed floor strictly above prior floor" rule to each of the four Angular metrics relative to their baseline values. Record the chosen margin per metric and the resulting computed floors; these feed Tasks 2 and 3.
    Do not edit any file in this task — this is pure measurement. Capture the numbers (write them into the eventual SUMMARY and carry them into Tasks 2-3).
  </action>
  <verify>
    <automated>make tests-python && docker compose -f src/docker/test/python/compose.yml run --rm tests pytest --cov --cov-report=term-missing -p no:cacheprovider</automated>
    <human-check>Container-inclusive Python TOTAL line % captured (real-lftp suite included). Angular four-metric coverage summary captured (statements/branches/functions/lines) via `make run-tests-angular` after the Dockerfile patch, or local `ng test --code-coverage`. The per-metric COMPUTED floors are each strictly above their prior floor (Python > 84), and are recorded for Task 2/Task 3.</human-check>
  </verify>
  <acceptance_criteria>
    - The CONTAINER-INCLUSIVE Python run (real-lftp suite included, inside the test container) produces a fresh TOTAL line coverage % (recorded). This — NOT the provisional host 85.19% — is the ratchet source / "before".
    - Four fresh Angular metrics captured (statements/branches/functions/lines).
    - A floor table computed: per-metric threshold = floor(container-inclusive measured) − chosen margin, with the COMPUTED Python floor strictly > 84 (and each Angular floor strictly above its prior baseline value). Margin tightened where needed to keep the computed floor a real ratchet.
    - The chosen margin per metric is recorded for the PROJECT.md decision row.
  </acceptance_criteria>
  <done>Fresh container-inclusive Python line % (real-lftp included) and four Angular metrics are measured against current HEAD (post 100-01/02), and the per-metric COMPUTED floor values (Python strictly > 84) + chosen margins are computed and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Patch the three config files in one commit (Angular gate + Dockerfile flag + Python floor)</name>
  <files>src/angular/karma.conf.js, src/docker/test/angular/Dockerfile, src/python/pyproject.toml</files>
  <read_first>
    - src/angular/karma.conf.js (current coverageReporter block 23-26)
    - src/docker/test/angular/Dockerfile (current CMD 31-33)
    - src/python/pyproject.toml (current fail_under at 88)
    - .planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-PATTERNS.md §"karma.conf.js", §"Dockerfile", §"pyproject.toml" (exact target shapes)
    - .planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-RESEARCH.md §"Required Changes (Both Are Mandatory)" + Pitfall 1 + Pitfall 5
  </read_first>
  <action>
    Apply all three config edits using the COMPUTED floor values from Task 1 (Python floor strictly > 84, derived from the container-inclusive measurement). These MUST land in ONE commit (D-06 — a failed gate has a single obvious revert target):
    1. `src/angular/karma.conf.js` — replace the single-reporter `coverageReporter: { type: 'html', dir: 'coverage/' }` block (23-26) with: `dir: 'coverage/'`, a `reporters` array containing `{ type: 'html', subdir: '.' }` and `{ type: 'text-summary' }`, and a `check: { global: { statements, branches, functions, lines } }` block filled with the Task 1 floor values. The `text-summary` reporter is mandatory — it emits the 4-line summary to stdout so the check runs headlessly and fails the process below threshold. Do NOT set `emitWarning: true` (omit it — defaults to false; setting true makes the gate non-fatal, RESEARCH Pitfall 5). Keep the file as valid JS (this is a `.js` config — match the existing quoting/indentation style in the file).
    2. `src/docker/test/angular/Dockerfile` — append `"--code-coverage"` as the final element of the CMD array (31-33). Without this, Istanbul instrumentation never runs and the karma `check.global` is a silent no-op (RESEARCH Pitfall 1 — this is the single most important companion change). Preserve the existing line-continuation / array formatting.
    3. `src/python/pyproject.toml` — bump `fail_under` (line 88) from `84` to the Task 1 Python floor (the computed floor strictly > 84, derived from the container-inclusive measurement). Do NOT touch `[tool.pytest.ini_options]` (it has no `--cov-fail-under`; D-06 confirmed).
    After editing, run the gates end-to-end to confirm they BITE (not no-ops): `make run-tests-angular` must now collect coverage and pass at the new thresholds. For the Python floor, re-run the CONTAINER-INCLUSIVE coverage (the same command Task 1 used) so the run that the floor is judged against includes the real-lftp suite: `make tests-python && docker compose -f src/docker/test/python/compose.yml run --rm tests pytest --cov --cov-report=term-missing -p no:cacheprovider` must pass at the new `fail_under` (pytest-cov reads `fail_under` from pyproject and exits non-zero below it). If either gate fails because the threshold was set at or above measured (margin too small), re-derive with a slightly larger margin — but never below a computed floor of 85 for Python (must stay > 84) and never below the prior floor for any metric.
  </action>
  <verify>
    <automated>make run-tests-angular</automated>
    <automated>make tests-python && docker compose -f src/docker/test/python/compose.yml run --rm tests pytest --cov --cov-report=term-missing -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `karma.conf.js` contains `check:` and `global:` with all four keys (`statements`, `branches`, `functions`, `lines`) set to numeric floors, plus a `reporters` array including `text-summary`. No `emitWarning: true`.
    - `src/docker/test/angular/Dockerfile` CMD array contains `"--code-coverage"`.
    - `pyproject.toml` `fail_under` is a number strictly > 84.
    - `grep -c -- "--code-coverage" src/docker/test/angular/Dockerfile` returns ≥ 1.
    - `make run-tests-angular` exits 0 (coverage collected AND check passes — proving the gate is exercised, not a no-op: the text-summary block appears in output).
    - The container-inclusive Python coverage run exits 0 at the new `fail_under` (> 84).
    - All three edits are staged for a single commit.
  </acceptance_criteria>
  <done>karma.conf.js has a functioning check.global gate, the Dockerfile collects coverage, pyproject.toml fail_under strictly > 84; both coverage gates pass at the new thresholds (Angular via run-tests-angular, Python via the container-inclusive run); all three edits in one commit.</done>
</task>

<task type="auto">
  <name>Task 3: Record before/after + floor decision in ROADMAP, RETROSPECTIVE, PROJECT</name>
  <files>.planning/ROADMAP.md, .planning/RETROSPECTIVE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Coverage Ratchet — Before / After" table (lines 302-311) — the two "TBD (Phase 100)" cells to fill
    - .planning/RETROSPECTIVE.md (existing v1.1.2 / v1.2.0 milestone entries as the template shape; the Cross-Milestone Trends table for the cumulative-quality row)
    - .planning/PROJECT.md Key Decisions table (the row shape to append)
    - .planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-PATTERNS.md §"ROADMAP.md", §"RETROSPECTIVE.md", §"PROJECT.md" (exact analog shapes)
  </read_first>
  <action>
    Document the ratchet (D-08 + D-07). Use the measured numbers and floors from Tasks 1-2:
    1. `.planning/ROADMAP.md` — in the Coverage Ratchet table (lines 306-310), replace both "TBD (Phase 100)" cells with the re-measured "After" numbers and the chosen floor values. Python row: After = the CONTAINER-INCLUSIVE measured line % (NOT the provisional host 85.19%) and the new `fail_under` floor (> 84). Note in the row that the "before"/after are container-inclusive per the baseline mandate. Angular row: After = the four measured metrics and the four `check.global` floors. "Before" stays the baseline-anchor numbers already present (do not alter them); if the existing Python "Before" cell shows the provisional host number, annotate it as provisional/host-only and record the container-inclusive re-measure as the After + the comparison basis.
    2. `.planning/RETROSPECTIVE.md` — add a `## Milestone: v1.3.0 — Test Coverage Gaps` entry following the existing v1.1.2 / v1.2.0 entry shape (What Was Built / What Worked / What Was Inefficient / Patterns Established / Key Lessons). Include the before/after coverage numbers (both languages; Python = container-inclusive) and the CI-ratchet outcome. If a `Cross-Milestone Trends → Cumulative Quality` table row applies, add the v1.3.0 row matching its existing columns.
    3. `.planning/PROJECT.md` — append the floor/margin decision to the Key Decisions table (D-07 mandate — log the chosen margin and resulting numbers so the monotonic-ratchet rule is auditable): one row for the Angular Karma floor (statements/branches/functions/lines set at floor(measured) − margin, ~0–1% jitter buffer rationale, each strictly above its prior baseline) and one row for the Python `fail_under` bump (84 → new floor > 84, derived from the container-inclusive measurement). Match the existing `| Decision | Rationale | ✓ Good |` column shape.
    These are doc edits in the SAME commit family as Task 2 is acceptable, OR a follow-on commit — but keep the three CONFIG edits (Task 2) in their own single commit per D-06 so the gate has one revert target; the doc edits may be a second commit. (Executor's discretion on commit grouping, honoring the D-06 single-revert-target mandate for the config trio.)
  </action>
  <verify>
    <automated>grep -c "TBD (Phase 100)" .planning/ROADMAP.md</automated>
    <automated>grep -c "v1.3.0" .planning/RETROSPECTIVE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TBD (Phase 100)" .planning/ROADMAP.md` returns 0 (both cells filled).
    - ROADMAP Coverage Ratchet table shows numeric After values for Python (container-inclusive line) and Angular (four metrics) plus the chosen floors.
    - RETROSPECTIVE.md contains a `## Milestone: v1.3.0` entry with before/after coverage numbers (Python = container-inclusive).
    - PROJECT.md Key Decisions table has two new rows (Angular floor + Python fail_under) naming the chosen margin; the Python row records a floor > 84.
    - The recorded floors match exactly the numbers written into karma.conf.js and pyproject.toml in Task 2 (consistency check).
  </acceptance_criteria>
  <done>ROADMAP table filled (no "TBD" remaining), v1.3.0 RETROSPECTIVE entry added with container-inclusive before/after numbers, PROJECT.md Key Decisions logs the floor/margin decision (Python floor > 84) — all consistent with the Task 2 config values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor PR → CI coverage gate | Future code changes cross the CI boundary; the ratcheted thresholds are the gate that rejects coverage regressions. This plan strengthens that gate (raises the bar); it changes no production runtime code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Tampering (silent gate bypass) | `karma.conf.js check.global` + `Dockerfile` CMD | mitigate | The `check.global` block is only evaluated when `ng test --code-coverage` runs. Patching `karma.conf.js` WITHOUT the Dockerfile `--code-coverage` flag would be a silent no-op (coverage never collected, gate never fails). Both files are patched in the SAME commit and Task 2 verifies the gate actually bites (text-summary appears, sub-threshold would fail). RESEARCH Pitfall 1. |
| T-100-03-W | Tampering (non-fatal gate) | `karma.conf.js check.global emitWarning` | mitigate | `emitWarning: true` would make a sub-threshold run print a warning but still pass CI. The block omits `emitWarning` (defaults to false) so the karma process exits non-zero below threshold. RESEARCH Pitfall 5. |
| T-100-03-R | Repudiation (ratchet drift) | ROADMAP / PROJECT decision record | accept | The monotonic-ratchet rule and chosen margin are logged in PROJECT.md Key Decisions and ROADMAP before/after; the recorded floor is strictly > 84 (a real ratchet, not equal to the prior floor) and the "before" is the container-inclusive re-measure per the baseline mandate; any future floor change is auditable against the recorded baseline anchor. No runtime risk. |
</threat_model>

<verification>
- `make run-tests-angular` exits 0 WITH the new `check.global` gate active (text-summary block present in output → coverage was collected → gate bit; proves it is not a no-op).
- The CONTAINER-INCLUSIVE Python coverage run (`make tests-python && docker compose -f src/docker/test/python/compose.yml run --rm tests pytest --cov --cov-report=term-missing -p no:cacheprovider`) exits 0 at the bumped `fail_under` (> 84). (The host `make coverage-python`, run FROM REPO ROOT — there is no `src/python/Makefile` — is a provisional cross-check only, not the ratchet source.)
- `grep -c -- "--code-coverage" src/docker/test/angular/Dockerfile` ≥ 1; `karma.conf.js` contains `check:` + four global keys; `pyproject.toml` `fail_under` strictly > 84.
- `grep -c "TBD (Phase 100)" .planning/ROADMAP.md` == 0; RETROSPECTIVE.md has a v1.3.0 entry; PROJECT.md Key Decisions has the floor/margin rows.
- Config trio committed together (single revert target, D-06).
- POST-PUSH (CI-gated, observed not locally run): the `unittests-angular` CI job (amd64) is green on the ratcheted thresholds; Python coverage CI green (criterion #4). The arm64 runner is E2E-only — no matrix change needed for this gate.
</verification>

<success_criteria>
- ROADMAP success criteria #3 + #5 satisfied: pytest `fail_under` and Karma `coverageReporter.check.global` thresholds ratcheted upward in a single commit, before/after recorded in ROADMAP + RETROSPECTIVE; the ratchet is monotonic (computed Python floor strictly > 84, each Angular floor strictly above its prior baseline) and the floor decision is logged in PROJECT.md (RATCHET-02).
- The Python "before" and ratchet floor are derived from the CONTAINER-INCLUSIVE re-measure (real-lftp suite included), per the v1.3.0 baseline mandate — NOT the provisional host-only 85.19%.
- Criterion #4 (CI green amd64+arm64) is satisfied by the CI run post-push — config edits here are autonomous; CI confirmation is the observed acceptance.
- The Angular gate is confirmed to BITE (not a silent no-op): coverage is collected via `--code-coverage` and the check is evaluated.
</success_criteria>

<output>
Create `.planning/phases/100-low-priority-angular-coverage-ci-ratchet/100-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
