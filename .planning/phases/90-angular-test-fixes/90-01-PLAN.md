---
phase: 90-angular-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts
  - src/angular/src/app/tests/unittests/services/files/view-file-filter.service.spec.ts
  - src/angular/src/app/tests/unittests/services/server/bulk-command.service.spec.ts
autonomous: true
requirements: [ANGFIX-01, ANGFIX-02, ANGFIX-07]
must_haves:
  truths:
    - "fakeAsync zones in stream-service.registry.spec.ts discard periodic tasks before completing"
    - "ViewFileFilterCriteria variable uses union type instead of double-cast"
    - "Every result?.property assertion in bulk-command.service.spec.ts is preceded by expect(result).toBeDefined()"
  artifacts:
    - path: "src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts"
      provides: "fakeAsync zone cleanup for setInterval from onInit()"
      contains: "discardPeriodicTasks"
    - path: "src/angular/src/app/tests/unittests/services/files/view-file-filter.service.spec.ts"
      provides: "Truthful nullable type for filterCriteria"
      contains: "ViewFileFilterCriteria | undefined"
    - path: "src/angular/src/app/tests/unittests/services/server/bulk-command.service.spec.ts"
      provides: "toBeDefined guards before optional chaining"
      contains: "expect(result).toBeDefined()"
  key_links:
    - from: "stream-service.registry.spec.ts"
      to: "@angular/core/testing"
      via: "discardPeriodicTasks import"
      pattern: "discardPeriodicTasks.*from.*@angular/core/testing"
---

<objective>
Fix fakeAsync zone pollution, double-cast type erasure, and optional chaining false-pass gaps in 3 Angular spec files.

Purpose: Eliminate pending timer warnings from fakeAsync zones (ANGFIX-01), make TypeScript catch null-dereference at compile time instead of hiding it behind a double-cast (ANGFIX-02), and prevent silent test passes when result is undefined (ANGFIX-07).
Output: 3 modified spec files, all 599 Angular tests passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-angular-test-fixes/90-CONTEXT.md
@.planning/phases/90-angular-test-fixes/90-RESEARCH.md
@.planning/phases/90-angular-test-fixes/90-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add discardPeriodicTasks() to stream-service.registry.spec.ts (ANGFIX-01)</name>
  <files>src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts</files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/base/stream-service.registry.spec.ts
  </read_first>
  <action>
Fix the fakeAsync zone pollution caused by `dispatchService.onInit()` starting a `setInterval` via `startTimeoutChecker()` that never gets discarded.

1. **Update import on line 1** -- add `discardPeriodicTasks` to the import:
   Change: `import {fakeAsync, TestBed, tick} from "@angular/core/testing";`
   To:     `import {discardPeriodicTasks, fakeAsync, TestBed, tick} from "@angular/core/testing";`

2. **Add `discardPeriodicTasks()` at the end of the `beforeEach(fakeAsync(...))` block** (line 67, before the closing `}));`):
   Insert `discardPeriodicTasks();` after the `tick();` call at line 67 and before the closing `}));` at line 68.

3. **Add `discardPeriodicTasks()` at the end of EVERY `it(..., fakeAsync(...))` test body** in the "Testing stream dispatch service" describe block. There are 9 fakeAsync `it()` blocks:
   - "should construct an event source with correct url" (line 74-76) -- add before `}));`
   - "should register all events with the event source" (line 78-87) -- add before `}));`
   - "should set an error handler on the event source" (line 89-91) -- add before `}));`
   - "should forward name and data correctly" (line 93-134) -- add before `}));`
   - "should log a warning when receiving an unregistered event name" (line 136-163) -- add before `}));`
   - "should call connect on open" (line 165-170) -- add before `}));`
   - "should call disconnect on error" (line 172-178) -- add before `}));`
   - "should send events after reconnect" (line 180-197) -- add before `}));`

   The "should create an instance" test (line 70-72) is NOT fakeAsync -- do not add it there.

4. **Do NOT touch the "Testing stream service registry" describe block** (lines 202-259) -- it does not use fakeAsync and does not call onInit().

Per D-02: Only stream-service.registry.spec.ts needs this fix. The `setInterval` originates from `StreamDispatchService.onInit()`, which is called in no other spec file. The other 13 fakeAsync spec files do not instantiate StreamDispatchService.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false --include="**/stream-service.registry.spec.ts" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Line 1 contains `discardPeriodicTasks` in the import from `@angular/core/testing`
    - grep -c "discardPeriodicTasks" of the file returns exactly 11 (1 import + 1 beforeEach + 9 it blocks = 11 occurrences)
    - `ng test --watch=false --include="**/stream-service.registry.spec.ts"` exits 0
    - No "timer(s) still in the queue" warnings in test output
  </acceptance_criteria>
  <done>All 15 tests in stream-service.registry.spec.ts pass with zero pending timer warnings. discardPeriodicTasks() is called in 1 beforeEach + 9 fakeAsync it() blocks = 10 call sites (plus 1 import = 11 grep hits).</done>
</task>

<task type="auto">
  <name>Task 2: Fix double-cast and add toBeDefined guards (ANGFIX-02, ANGFIX-07)</name>
  <files>
    src/angular/src/app/tests/unittests/services/files/view-file-filter.service.spec.ts
    src/angular/src/app/tests/unittests/services/server/bulk-command.service.spec.ts
  </files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/files/view-file-filter.service.spec.ts
    - src/angular/src/app/tests/unittests/services/server/bulk-command.service.spec.ts
  </read_first>
  <action>
**ANGFIX-02: Fix double-cast in view-file-filter.service.spec.ts**

Per D-04: Replace the double-cast with a truthful union type.

1. **Line 18** -- change the variable declaration:
   From: `let filterCriteria: ViewFileFilterCriteria;`
   To:   `let filterCriteria: ViewFileFilterCriteria | undefined;`

2. **Line 30** -- remove the double-cast:
   From: `filterCriteria = undefined as unknown as ViewFileFilterCriteria;  // Reset before each test`
   To:   `filterCriteria = undefined;`

3. **Add `!` non-null assertion to all `filterCriteria.meetsCriteria(...)` call sites.** These are safe because `tick()` has already fired the `setFilterCriteria` spy which populates `filterCriteria`. The affected lines are every occurrence of `filterCriteria.meetsCriteria(` -- change each to `filterCriteria!.meetsCriteria(`. There are approximately 25 occurrences across tests: "correctly filters by name" (lines 96-116), "ignores cases when filtering by name" (lines 124-135), "treats dots as spaces when filtering by name" (lines 143-152), "correctly filters by status" (lines 202-249), and "correctly filters by name AND status" (lines 260-279).

**ANGFIX-07: Add toBeDefined guards in bulk-command.service.spec.ts**

Per D-03: Add `expect(result).toBeDefined();` before the first `result?.` assertion in each test block that lacks it.

The `testActions.forEach` block (lines 36-62) already has `expect(result).toBeDefined()` on line 57 -- do NOT modify it.

Add `expect(result).toBeDefined();` immediately after the `tick();` call in these 7 test blocks:

1. **"should handle successful response with all files succeeded"** (after `tick();` at line 80, before `expect(result?.success)` at line 82):
   Add: `expect(result).toBeDefined();`

2. **"should handle partial failure response"** (after `tick();` at line 106, before `expect(result?.success)` at line 108):
   Add: `expect(result).toBeDefined();`

3. **"should handle all files failed response"** (after `tick();` at line 137, before `expect(result?.success)` at line 139):
   Add: `expect(result).toBeDefined();`

4. **"should handle HTTP 400 error with JSON error body"** (after `tick();` at line 158, before `expect(result?.success)` at line 160):
   Add: `expect(result).toBeDefined();`

5. **"should handle HTTP 400 error with invalid files array"** (after `tick();` at line 176, before `expect(result?.success)` at line 179):
   Add: `expect(result).toBeDefined();`

6. **"should handle network error"** (after `tick();` at line 192, before `expect(result?.success)` at line 194):
   Add: `expect(result).toBeDefined();`

7. **"should handle string error response"** (after `tick();` at line 210, before `expect(result?.success)` at line 213):
   Add: `expect(result).toBeDefined();`

Do NOT add guards to "should log debug info on successful response", "should log debug info on error response", or "should share replay for multiple subscribers" -- these tests do not use `result?.` optional chaining.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false --include="**/view-file-filter.service.spec.ts" --include="**/bulk-command.service.spec.ts" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - view-file-filter.service.spec.ts line 18 contains `ViewFileFilterCriteria | undefined`
    - view-file-filter.service.spec.ts does NOT contain the string `as unknown as ViewFileFilterCriteria`
    - view-file-filter.service.spec.ts contains `filterCriteria!.meetsCriteria` (non-null assertion at usage sites)
    - bulk-command.service.spec.ts: grep -c "expect(result).toBeDefined()" returns 8 (1 existing + 7 new)
    - `ng test --watch=false` for both spec files exits 0
  </acceptance_criteria>
  <done>filterCriteria uses `ViewFileFilterCriteria | undefined` with no double-cast. All 7 test blocks in bulk-command.service.spec.ts that use `result?.` optional chaining have `expect(result).toBeDefined()` guards. Both spec files pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries -- all changes are test-only edits with no production code or external interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | N/A | Test spec files only | accept | No production code changes, no new attack surface. Test-only edits. |
</threat_model>

<verification>
cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false 2>&1 | tail -30
- All 599 tests pass
- Zero "timer(s) still in the queue" warnings
- Zero TypeScript compilation errors
</verification>

<success_criteria>
- stream-service.registry.spec.ts: `discardPeriodicTasks()` called in all 10 fakeAsync blocks (1 beforeEach + 9 it)
- view-file-filter.service.spec.ts: `filterCriteria` declared as `ViewFileFilterCriteria | undefined`, no double-cast
- bulk-command.service.spec.ts: 8 total `expect(result).toBeDefined()` calls (1 pre-existing + 7 new)
- `ng test --watch=false` exits 0 with all 599 tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/90-angular-test-fixes/90-01-SUMMARY.md`
</output>
