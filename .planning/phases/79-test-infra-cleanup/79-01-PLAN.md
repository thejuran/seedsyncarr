---
phase: 79
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/docker/test/python/Dockerfile
  - src/python/pyproject.toml
autonomous: true
requirements: [TEST-01]
tags: [test-infra, python, pytest, dockerfile, deprecation-warnings]

must_haves:
  truths:
    - "make run-tests-python stderr contains zero pytest-cache write warnings"
    - "make run-tests-python stderr contains zero webob/cgi DeprecationWarning lines"
    - "pyproject.toml [tool.pytest.ini_options] contains only pythonpath and timeout"
    - "Local dev (poetry run pytest) remains unchanged — no flag lives in pyproject"
  artifacts:
    - path: "src/docker/test/python/Dockerfile"
      provides: "pytest invocation with cacheprovider disabled + interpreter-level cgi DeprecationWarning filter"
      contains: 'CMD ["pytest", "-v", "-p", "no:cacheprovider"]'
    - path: "src/python/pyproject.toml"
      provides: "minimal [tool.pytest.ini_options] with no dead config"
      contains: "[tool.pytest.ini_options]"
  key_links:
    - from: "src/docker/test/python/Dockerfile ENV PYTHONWARNINGS"
      to: "stdlib cgi module DeprecationWarning at interpreter start"
      via: "Python warnings filter applied before webob imports cgi"
      pattern: "ignore::DeprecationWarning:cgi"
    - from: "src/docker/test/python/Dockerfile CMD flag -p no:cacheprovider"
      to: "pytest cacheprovider plugin disablement"
      via: "pytest plugin disablement via CLI flag, preventing writes to the read-only /src mount"
      pattern: "no:cacheprovider"
---

<objective>
Eliminate the four recurring stderr noise sources in `make run-tests-python`:
three pytest-cache write warnings (caused by the read-only `/src` mount) and one
webob/cgi `DeprecationWarning` (caused by webob 1.8.9 still importing stdlib
`cgi` under Python 3.11).

**Purpose:** "CI test output is signal-only" (CONTEXT.md `<specifics>`). Once
Plan 01 ships, the Python test harness stderr contains zero warning lines, so
real failures stand out.

**Output:**
- `src/docker/test/python/Dockerfile` with `-p no:cacheprovider` on CMD and a
  new `ENV PYTHONWARNINGS` line that filters the cgi DeprecationWarning at
  interpreter start.
- `src/python/pyproject.toml` with the dead `cache_dir` line and the
  non-matching `filterwarnings` entry removed.

**Implements:** D-01, D-02, D-04, D-05 (per RESEARCH §2–§3, D-03 is unviable as
of 2026-04-21; commit to D-05 directly — no "try upgrade first" branch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-test-infra-cleanup/79-CONTEXT.md
@.planning/phases/79-test-infra-cleanup/79-RESEARCH.md
@.planning/phases/79-test-infra-cleanup/79-PATTERNS.md
@.planning/phases/79-test-infra-cleanup/79-VALIDATION.md
@src/docker/test/python/Dockerfile
@src/python/pyproject.toml

<interfaces>
<!-- Current state of the two files being edited (verified 2026-04-21). -->
<!-- The executor MUST land the deltas below verbatim. -->

From src/docker/test/python/Dockerfile lines 40-45:
```dockerfile
# src needs to be mounted on /src/
WORKDIR /src/
ENV PYTHONPATH=/src

ENTRYPOINT ["/app/entrypoint.sh"]
CMD ["pytest", "-v"]
```

From src/python/pyproject.toml lines 71-77:
```toml
[tool.pytest.ini_options]
pythonpath = ["."]
timeout = 60
cache_dir = "/tmp/.pytest_cache"
filterwarnings = [
    "ignore:'cgi' is deprecated:DeprecationWarning",
]
```

Target end-state Dockerfile lines 40-47 (after edits):
```dockerfile
# src needs to be mounted on /src/
WORKDIR /src/
ENV PYTHONPATH=/src
ENV PYTHONWARNINGS="ignore::DeprecationWarning:cgi"

ENTRYPOINT ["/app/entrypoint.sh"]
CMD ["pytest", "-v", "-p", "no:cacheprovider"]
```

Target end-state pyproject.toml [tool.pytest.ini_options] (after edits):
```toml
[tool.pytest.ini_options]
pythonpath = ["."]
timeout = 60
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit Dockerfile — add cacheprovider flag (D-01) + PYTHONWARNINGS env (D-05)</name>
  <files>src/docker/test/python/Dockerfile</files>
  <read_first>
    - src/docker/test/python/Dockerfile (full file — current state of lines 40–45)
    - .planning/phases/79-test-infra-cleanup/79-RESEARCH.md §2 "TEST-01: webob Upgrade Target — Primary Fix UNAVAILABLE, D-05 Fallback Required"
    - .planning/phases/79-test-infra-cleanup/79-RESEARCH.md §3 "TEST-01: pytest-cache + pyproject.toml Cleanup"
    - .planning/phases/79-test-infra-cleanup/79-PATTERNS.md "Plan 01 — `src/docker/test/python/Dockerfile`" block
    - .planning/phases/79-test-infra-cleanup/79-RESEARCH.md §9 R-5 (fallback plan if `:cgi` qualifier fails to match)
  </read_first>
  <action>
    Perform two surgical edits to `src/docker/test/python/Dockerfile`:

    **Edit 1 (D-05, add new line between line 42 and line 44):**
    Insert a new line immediately after line 42 (`ENV PYTHONPATH=/src`) containing exactly:
    ```
    ENV PYTHONWARNINGS="ignore::DeprecationWarning:cgi"
    ```
    Preserve the blank line that currently sits between `ENV PYTHONPATH=/src` and `ENTRYPOINT`. The new `ENV PYTHONWARNINGS` line goes ABOVE that blank line, so the structure after edit is:

    ```
    ENV PYTHONPATH=/src
    ENV PYTHONWARNINGS="ignore::DeprecationWarning:cgi"

    ENTRYPOINT ["/app/entrypoint.sh"]
    CMD ["pytest", "-v", "-p", "no:cacheprovider"]
    ```

    **Edit 2 (D-01, replace line 45):**
    Replace the existing CMD line (`CMD ["pytest", "-v"]`) with:
    ```
    CMD ["pytest", "-v", "-p", "no:cacheprovider"]
    ```

    **Why `:cgi` not `:webob`:** The `DeprecationWarning` is emitted by the stdlib `cgi` module itself (`warnings.warn("'cgi' is deprecated and slated for removal in Python 3.13", DeprecationWarning, stacklevel=2)` in Python 3.11's `cgi.py`), not by webob — webob merely triggers the import. Python's `PYTHONWARNINGS` filter matches the emitting module, so `cgi` is the correct qualifier. Per RESEARCH §2.

    **Fallback note (do NOT apply speculatively):** Per RESEARCH §9 R-5, if empirical verification (Task 3's grep check) finds the `:cgi`-scoped qualifier failed to match, drop the module qualifier entirely — change to `ENV PYTHONWARNINGS="ignore::DeprecationWarning"`. Do not apply this fallback unless Task 3's grep returns a non-zero count. If the scoped form works (expected per §2 evidence), leave it scoped. Document any fallback application in the plan's SUMMARY.md.

    **Why commit to D-05 directly, not D-03:** RESEARCH §2 verified against live PyPI on 2026-04-21 that webob 1.8.9 is the latest release, still contains `from cgi import parse_header` at `src/webob/compat.py:4`, and PR #466 (the cgi-removal PR for webob 2.0) is unmerged. Project Python pin is `>=3.11,<3.13` so `legacy-cgi` conditional dependency (3.13+ only) does not activate. No webob release can satisfy D-03; taking D-05 directly is the only viable path.
  </action>
  <verify>
    <automated>grep -c 'CMD \["pytest", "-v", "-p", "no:cacheprovider"\]' src/docker/test/python/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CMD \["pytest", "-v", "-p", "no:cacheprovider"\]' src/docker/test/python/Dockerfile` → 1
    - `grep -c 'ENV PYTHONWARNINGS="ignore::DeprecationWarning:cgi"' src/docker/test/python/Dockerfile` → 1
    - `grep -c '^CMD \["pytest", "-v"\]$' src/docker/test/python/Dockerfile` → 0 (old CMD fully replaced)
    - `grep -c 'ENV PYTHONPATH=/src' src/docker/test/python/Dockerfile` → 1 (untouched)
    - `grep -c 'ENTRYPOINT \["/app/entrypoint.sh"\]' src/docker/test/python/Dockerfile` → 1 (untouched)
  </acceptance_criteria>
  <done>
    Dockerfile contains the new `ENV PYTHONWARNINGS` line and the updated `CMD` with `-p no:cacheprovider`. No other lines changed. All five acceptance greps return the expected values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip dead pyproject.toml test config (D-02 + D-04)</name>
  <files>src/python/pyproject.toml</files>
  <read_first>
    - src/python/pyproject.toml (focus on lines 71–78 `[tool.pytest.ini_options]` section; verify lines 79+ are `[tool.coverage.run]`)
    - .planning/phases/79-test-infra-cleanup/79-RESEARCH.md §3 "D-02 Confirmation" and "D-04 Confirmation"
    - .planning/phases/79-test-infra-cleanup/79-PATTERNS.md "Plan 01 — `src/python/pyproject.toml`" block
  </read_first>
  <action>
    Open `src/python/pyproject.toml`. In the `[tool.pytest.ini_options]` section (starting at line 71), delete lines 74, 75, 76, and 77. Those are:

    ```
    cache_dir = "/tmp/.pytest_cache"
    filterwarnings = [
        "ignore:'cgi' is deprecated:DeprecationWarning",
    ]
    ```

    After deletion, the section MUST read exactly:

    ```toml
    [tool.pytest.ini_options]
    pythonpath = ["."]
    timeout = 60

    [tool.coverage.run]
    ```

    (A single blank line separates `timeout = 60` from the next section header, matching TOML convention. Keep the pre-existing blank-line structure identical to before — do NOT collapse the blank between `[tool.pytest.ini_options]` section and `[tool.coverage.run]`.)

    **Why D-02:** With `-p no:cacheprovider` in Plan 01 Task 1, the `cacheprovider` plugin never loads. The `cache_dir` setting is dead config that misleads future maintainers.

    **Why D-04:** RESEARCH §3 explains that the existing `filterwarnings` entry never matched. The `DeprecationWarning` fires at import time inside the `cgi` module BEFORE pytest installs its filter chain. D-05's interpreter-level `PYTHONWARNINGS` (Task 1) replaces its intent.

    **Scope fence:** Do not touch `pythonpath`, `timeout`, or any line in `[tool.coverage.run]` / `[tool.coverage.report]` sections further down. Do not touch lines above 71.
  </action>
  <verify>
    <automated>grep -c 'cache_dir\|filterwarnings' src/python/pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cache_dir' src/python/pyproject.toml` → 0
    - `grep -c 'filterwarnings' src/python/pyproject.toml` → 0
    - `grep -c "cgi.*deprecated" src/python/pyproject.toml` → 0
    - `grep -c '\[tool.pytest.ini_options\]' src/python/pyproject.toml` → 1 (section header intact)
    - `grep -c 'pythonpath = \["\\."\]' src/python/pyproject.toml` → 1 (untouched)
    - `grep -c 'timeout = 60' src/python/pyproject.toml` → 1 (untouched)
    - `grep -c '\[tool.coverage.run\]' src/python/pyproject.toml` → 1 (next section still present)
  </acceptance_criteria>
  <done>
    Dead config removed; `[tool.pytest.ini_options]` has exactly two settings (`pythonpath`, `timeout`); all downstream sections untouched; all acceptance greps return expected values.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebuild Docker harness + verify zero warnings in stderr</name>
  <files>(verification-only — no file modifications)</files>
  <read_first>
    - .planning/phases/79-test-infra-cleanup/79-VALIDATION.md "Per-Task Verification Map" rows 79-01-01 and 79-01-02
    - .planning/phases/79-test-infra-cleanup/79-RESEARCH.md §8 "Phase Requirements → Test Map" SC 1a and 1b
    - .planning/phases/79-test-infra-cleanup/79-RESEARCH.md §9 R-5 (scoped vs broad PYTHONWARNINGS)
    - Makefile §tests-python (lines 76–89) and §run-tests-python (line 91) for build/run flow
  </read_first>
  <action>
    Run the full test-harness rebuild-and-run flow to exercise the edits from Tasks 1 and 2:

    1. `make tests-python` — rebuilds the `seedsyncarr_test_python` Docker image, picking up the new `ENV PYTHONWARNINGS` line and the new CMD.
    2. `make run-tests-python` — executes the test suite inside the rebuilt container. Capture stderr to a temp file for grep inspection.
    3. Run the two SC#1 verification greps against the captured stderr.

    Exact command sequence:

    ```bash
    make tests-python && make run-tests-python 2>&1 | tee /tmp/phase-79-01-pytest.out
    PYTEST_CACHE_WARNINGS=$(grep -Ec "pytest-cache|could not create cache" /tmp/phase-79-01-pytest.out)
    CGI_WARNINGS=$(grep -Ec "cgi.*deprecated" /tmp/phase-79-01-pytest.out)
    echo "pytest-cache warnings: $PYTEST_CACHE_WARNINGS (expect 0)"
    echo "cgi deprecation warnings: $CGI_WARNINGS (expect 0)"
    ```

    **Expected outcome:** both counters are 0. Test suite exits 0 (all pre-existing tests still pass — no test-code changes in this plan).

    **Fallback trigger (R-5):** If `CGI_WARNINGS > 0`, the scoped `:cgi` qualifier failed to match empirically. Go back to Plan 01 Task 1 and change the `ENV PYTHONWARNINGS` line to the broader form:
    ```
    ENV PYTHONWARNINGS="ignore::DeprecationWarning"
    ```
    Then re-run this task's command sequence. The broad form is still scoped to the CI container (Dockerfile ENV) and does NOT leak into local dev (`poetry run pytest` uses shell env, not the container env). Record the fallback application in the plan SUMMARY.md if taken. Do NOT modify local dev env.

    **Fallback trigger (pytest-cache):** If `PYTEST_CACHE_WARNINGS > 0`, Task 1's `-p no:cacheprovider` flag did not land correctly — re-verify Task 1 acceptance greps. No alternative approach is expected to be needed; this flag is the documented pytest mechanism for plugin disablement.

    **Do not modify:** Makefile, compose.yml, test source files, or the entrypoint script. All changes are confined to Tasks 1 and 2.
  </action>
  <verify>
    <automated>make tests-python && make run-tests-python 2>&1 | tee /tmp/phase-79-01-pytest.out; test $(grep -Ec "pytest-cache|could not create cache|cgi.*deprecated" /tmp/phase-79-01-pytest.out) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `make tests-python` exits 0 (build succeeds on CI amd64; arm64 failure is known tech debt, Phase 80 scope — exempt)
    - `make run-tests-python` exits 0 (all pre-existing tests pass)
    - `grep -Ec "pytest-cache|could not create cache" /tmp/phase-79-01-pytest.out` → 0
    - `grep -Ec "cgi.*deprecated" /tmp/phase-79-01-pytest.out` → 0
    - No new test failures introduced by the plan edits (compare test count to last green CI run if ambiguous)
  </acceptance_criteria>
  <done>
    `make tests-python` + `make run-tests-python` both exit 0 with zero pytest-cache stderr lines and zero cgi-deprecation stderr lines. Plan 01 closes TEST-01 and surfaces SC #1a + SC #1b green. SC #1c (CI log inspection) is the orchestrator's post-merge verification and is tracked in 79-VALIDATION.md "Manual-Only Verifications".
  </done>
</task>

</tasks>

<verification>
Phase-level checks after all 3 tasks land (orchestrator or executor runs these before commit):

```bash
# Acceptance (static):
grep -c 'CMD \["pytest", "-v", "-p", "no:cacheprovider"\]' src/docker/test/python/Dockerfile   # 1
grep -c 'ENV PYTHONWARNINGS' src/docker/test/python/Dockerfile                                 # 1
grep -c 'cache_dir\|filterwarnings' src/python/pyproject.toml                                  # 0

# Acceptance (runtime):
make run-tests-python 2>&1 | grep -Ec "pytest-cache|could not create cache"   # 0
make run-tests-python 2>&1 | grep -Ec "cgi.*deprecated"                       # 0
```

No sequencing with Plan 02 — files_modified sets are disjoint.
</verification>

<success_criteria>
- ROADMAP SC #1a: Zero pytest-cache warnings in `make run-tests-python` stderr. ✅ covered by Task 3 grep.
- ROADMAP SC #1b: Zero webob/cgi DeprecationWarning lines in `make run-tests-python` stderr. ✅ covered by Task 3 grep.
- ROADMAP SC #1c (CI log surface): Post-merge inspection at `.github/workflows/ci.yml:144` confirms zero matching lines in CI stderr. Tracked in 79-VALIDATION.md "Manual-Only Verifications" — NOT in this plan's scope; closed by orchestrator after merge.
- Requirement TEST-01 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infra-cleanup/79-01-SUMMARY.md` documenting:
- The four warning lines eliminated (3 pytest-cache + 1 cgi deprecation)
- That D-05 was applied directly (D-03 unviable as of 2026-04-21; PyPI latest is webob 1.8.9, no cgi-removal release exists)
- Whether the R-5 broad-filter fallback was needed (expected: no — scoped `:cgi` qualifier works)
- Runtime stderr grep results (both counters = 0)
- Recommended follow-up todo: `.planning/todos/pending/2026-04-21-webob-cgi-upstream-unblock.md` tracking webob PR #466 merge for future PYTHONWARNINGS removal
</output>
