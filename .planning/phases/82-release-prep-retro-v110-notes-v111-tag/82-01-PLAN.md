---
phase: 82-release-prep-retro-v110-notes-v111-tag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - DOCS-01
user_setup: []

must_haves:
  truths:
    - "CHANGELOG.md has a v1.1.0 entry summarizing the UI redesign milestone"
    - "A GitHub Release exists for tag v1.1.0 with categorized notes"
    - "v1.1.0-dev pre-release is cleaned up"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.1.0 changelog entry with Added/Changed sections"
      contains: "## [1.1.0]"
  key_links:
    - from: "CHANGELOG.md"
      to: "GitHub Release v1.1.0"
      via: "gh release create --tag v1.1.0 --notes-file"
      pattern: "gh release view v1.1.0"
---

<objective>
Write the retroactive v1.1.0 CHANGELOG.md entry and create a GitHub Release on the existing v1.1.0 tag.

Purpose: v1.1.0 shipped 2026-04-19 without release notes (DOCS-01). This plan adds the retroactive changelog entry covering Phases 62-74 (UI redesign) and creates the corresponding GitHub Release.
Output: CHANGELOG.md with v1.1.0 entry; GitHub Release for v1.1.0 tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-CONTEXT.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-RESEARCH.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-PATTERNS.md

<interfaces>
<!-- CHANGELOG.md current structure (entire file): -->
<!-- Line 1-6: Header + preamble -->
<!-- Line 7-19: v1.0.0 entry -->
<!-- Line 21: Comparison link -->
<!-- New v1.1.0 entry goes ABOVE the v1.0.0 entry (newest first) -->
<!-- New comparison link goes at bottom, above existing [1.0.0] link -->

<!-- GitHub Release title format: "v1.1.0 — SeedSyncarr" -->
<!-- GitHub Release body format per D-03: intro line + CHANGELOG categories + Full changelog link -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write v1.1.0 CHANGELOG.md entry and create GitHub Release</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current state — v1.0.0 entry is the only entry, comparison link at bottom)
    - .planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-RESEARCH.md (changelog draft content in Code Examples section)
    - .planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-PATTERNS.md (Keep a Changelog entry format rules)
    - .planning/ROADMAP.md (Phases 62-74 descriptions for changelog content verification)
  </read_first>
  <action>
Edit CHANGELOG.md to add the v1.1.0 entry ABOVE the existing v1.0.0 entry. Per D-01 (Keep a Changelog sections) and D-02 (user-facing language, no phase refs, no jargon):

Insert this content between line 6 (preamble) and line 7 (v1.0.0 header):

```markdown
## [1.1.0] - 2026-04-20

### Added

- Per-file selection and shift-range select with a bulk-actions bar for Queue, Stop, Extract, Delete Local, and Delete Remote operations on multiple files.
- Dashboard filter with URL persistence: filter transfers by status with state preserved in the browser URL for sharing and page reloads.
- Storage capacity tiles on the dashboard showing local disk and seedbox usage with warning and danger color thresholds.
- New navigation bar with live connection status indicator and notification panel.

### Changed

- Transfer table redesigned with search, pagination, status badges, and progress bars.
- Settings page reorganized into card sections with toggle switches and inline AutoQueue pattern management.
- Logs page updated with full-viewport terminal view, log-level filter buttons, and regex search.
- About page updated with version badge and system information table.
- Color theme unified consistently across all pages.
```

Update the comparison links at the BOTTOM of the file. The final lines should be (reference-style link definitions, in reverse chronological order):

```markdown
[1.1.0]: https://github.com/thejuran/seedsyncarr/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/thejuran/seedsyncarr/releases/tag/v1.0.0
```

Then create the v1.1.0 GitHub Release on the existing tag per D-03 and D-04:

1. Create a temporary file `release-v110-body.md` with this content:
```
UI redesign with Triggarr-style visual identity — new navigation bar, redesigned dashboard and settings, per-file selection with bulk actions, and storage capacity tiles.

### Added

- Per-file selection and shift-range select with a bulk-actions bar for Queue, Stop, Extract, Delete Local, and Delete Remote operations on multiple files.
- Dashboard filter with URL persistence: filter transfers by status with state preserved in the browser URL for sharing and page reloads.
- Storage capacity tiles on the dashboard showing local disk and seedbox usage with warning and danger color thresholds.
- New navigation bar with live connection status indicator and notification panel.

### Changed

- Transfer table redesigned with search, pagination, status badges, and progress bars.
- Settings page reorganized into card sections with toggle switches and inline AutoQueue pattern management.
- Logs page updated with full-viewport terminal view, log-level filter buttons, and regex search.
- About page updated with version badge and system information table.
- Color theme unified consistently across all pages.

**Full changelog:** https://github.com/thejuran/seedsyncarr/blob/main/CHANGELOG.md
```

2. Run: `gh release create v1.1.0 --tag v1.1.0 --title "v1.1.0 — SeedSyncarr" --notes-file release-v110-body.md --latest=false`
   (Use `--latest=false` so v1.0.0 remains "Latest" until v1.1.1 is published.)

3. Delete the v1.1.0-dev pre-release (it is superseded by the proper v1.1.0 release):
   `gh release delete v1.1.0-dev --yes`
   `git push --delete origin v1.1.0-dev` (delete the dev tag too)

4. Clean up the temp file: `rm release-v110-body.md`
  </action>
  <verify>
    <automated>grep '## \[1.1.0\] - 2026-04-20' CHANGELOG.md && grep '\[1.1.0\]: https://github.com/thejuran/seedsyncarr/compare/v1.0.0...v1.1.0' CHANGELOG.md && gh release view v1.1.0 --repo thejuran/seedsyncarr --json tagName,name -q '.tagName + " " + .name'</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains the line `## [1.1.0] - 2026-04-20`
    - CHANGELOG.md contains `### Added` section under v1.1.0 with at least 4 bullet items
    - CHANGELOG.md contains `### Changed` section under v1.1.0 with at least 5 bullet items
    - CHANGELOG.md contains `[1.1.0]: https://github.com/thejuran/seedsyncarr/compare/v1.0.0...v1.1.0` at the bottom
    - CHANGELOG.md v1.1.0 entry appears ABOVE v1.0.0 entry (newest first)
    - `gh release view v1.1.0` returns a release with title containing `v1.1.0`
    - `gh release view v1.1.0-dev` returns "release not found" (pre-release deleted)
    - No phase references (e.g., "Phase 62", "Phase 72") appear in CHANGELOG.md
  </acceptance_criteria>
  <done>CHANGELOG.md has v1.1.0 entry with user-facing Added/Changed sections. GitHub Release v1.1.0 exists on the existing tag with matching categorized notes per D-03. v1.1.0-dev pre-release deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local -> GitHub API | gh CLI authenticates to create release; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | T (Tampering) | GitHub Release body | accept | Release body is static markdown authored by developer; no user input. GitHub renders markdown safely. |
| T-82-02 | I (Information Disclosure) | CHANGELOG.md | accept | Public repo; changelog is intentionally public documentation. |
</threat_model>

<verification>
- `grep '## \[1.1.0\]' CHANGELOG.md` returns a match
- `gh release view v1.1.0 --repo thejuran/seedsyncarr` shows the release exists
- CHANGELOG.md has no developer jargon per D-02 (no "SCSS", "BehaviorSubject", "service class")
</verification>

<success_criteria>
v1.1.0 retroactive CHANGELOG entry exists with user-facing categorized notes. GitHub Release v1.1.0 is live on the existing tag. v1.1.0-dev pre-release is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-01-SUMMARY.md`
</output>
