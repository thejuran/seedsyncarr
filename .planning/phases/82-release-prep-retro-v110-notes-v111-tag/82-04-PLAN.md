---
phase: 82-release-prep-retro-v110-notes-v111-tag
plan: 04
type: execute
wave: 3
depends_on:
  - 82-02
  - 82-03
files_modified: []
autonomous: false
requirements:
  - DOCS-01
user_setup: []

must_haves:
  truths:
    - "Git tag v1.1.1 exists and is pushed to origin"
    - "CI pipeline completes green on the v1.1.1 tag"
    - "GitHub Release v1.1.1 exists with .deb asset attached"
    - "Docker image ghcr.io/thejuran/seedsyncarr:1.1.1 is published"
  artifacts: []
  key_links:
    - from: "git tag v1.1.1"
      to: "CI pipeline"
      via: "refs/tags/v trigger"
      pattern: "startsWith(github.ref, 'refs/tags/v')"
    - from: "CI publish-deb-package"
      to: "GitHub Release v1.1.1"
      via: "gh release create + upload"
      pattern: "gh release view v1.1.1"
    - from: "CI publish-docker-image"
      to: "GHCR"
      via: "docker push"
      pattern: "ghcr.io/thejuran/seedsyncarr:1.1.1"
---

<objective>
Push the v1.1.1 git tag and verify CI publishes all release artifacts: Docker images, .deb package, and GitHub Release.

Purpose: This is the final release step. The tag push triggers CI, which runs tests, builds Docker + Deb artifacts, creates the GitHub Release, and uploads the .deb. Human verification confirms all artifacts are live.
Output: v1.1.1 tagged, CI green, GitHub Release live with .deb, Docker images published.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-CONTEXT.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-02-SUMMARY.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and push v1.1.1 git tag</name>
  <files></files>
  <read_first>
    - .planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-03-SUMMARY.md (confirm version bump is committed)
    - .github/workflows/ci.yml (confirm tag trigger pattern: `v[0-9]+.[0-9]+.[0-9]+`)
  </read_first>
  <action>
Create an annotated git tag for v1.1.1 and push it to origin. This triggers the CI pipeline.

1. Verify all changes from Plans 01-03 are committed: `git status` should show a clean working tree.
2. Verify the current HEAD is on the main branch and up to date: `git log --oneline -1`
3. Create the tag: `git tag -a v1.1.1 -m "v1.1.1 — Post-Redesign Cleanup & Outstanding Work"`
4. Push the tag: `git push origin v1.1.1`
5. Verify the tag is pushed: `git ls-remote --tags origin | grep v1.1.1`

IMPORTANT: Do NOT use `git push --tags` (pushes ALL local tags). Use `git push origin v1.1.1` to push only the intended tag.
  </action>
  <verify>
    <automated>git tag --list v1.1.1 && git ls-remote --tags origin | grep 'refs/tags/v1.1.1'</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v1.1.1` returns `v1.1.1`
    - `git ls-remote --tags origin | grep v1.1.1` shows the tag on the remote
    - The tag points to the commit that includes the version bump and changelog entries
  </acceptance_criteria>
  <done>v1.1.1 tag created and pushed to origin. CI pipeline triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI pipeline and release artifacts</name>
  <what-built>
    Pushed v1.1.1 tag which triggers the full CI pipeline: unit tests, E2E tests, Docker image build + publish, Deb package build + GitHub Release creation + .deb upload.
  </what-built>
  <how-to-verify>
    Wait for the CI pipeline to complete (typically 10-15 minutes), then verify all 5 success criteria:

    1. **CI pipeline green:**
       Visit: https://github.com/thejuran/seedsyncarr/actions
       Or run: `gh run list --limit 5`
       Look for the run triggered by tag `v1.1.1` — all jobs should show green checkmarks.

    2. **GitHub Release v1.1.1 exists:**
       Run: `gh release view v1.1.1`
       Verify: Title is "v1.1.1 — SeedSyncarr", body contains Added/Fixed/Security sections.

    3. **.deb asset attached:**
       Run: `gh release view v1.1.1 --json assets -q '.assets[].name'`
       Verify: Shows `seedsyncarr_1.1.1_amd64.deb`.

    4. **Docker image published:**
       Run: `docker pull ghcr.io/thejuran/seedsyncarr:1.1.1`
       Or check: https://github.com/thejuran/seedsyncarr/pkgs/container/seedsyncarr

    5. **GitHub Release v1.1.0 also exists (from Plan 01):**
       Run: `gh release view v1.1.0`
       Verify: Title is "v1.1.0 — SeedSyncarr", not pre-release.

    If any job fails, share the error output so we can diagnose.
  </how-to-verify>
  <read_first>
    - .github/workflows/ci.yml (understand which jobs run on tag push)
  </read_first>
  <acceptance_criteria>
    - `gh run list --limit 5` shows a v1.1.1 CI run with status "completed" and conclusion "success"
    - `gh release view v1.1.1 --json tagName -q .tagName` returns `v1.1.1`
    - `gh release view v1.1.1 --json assets -q '.assets[].name'` includes `seedsyncarr_1.1.1_amd64.deb`
    - `gh release view v1.1.0 --json tagName -q .tagName` returns `v1.1.0`
    - Docker image `ghcr.io/thejuran/seedsyncarr:1.1.1` is pullable
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push -> GitHub | Tag push triggers CI; authenticated via git credentials |
| CI -> GHCR | Docker image publish; authenticated via GITHUB_TOKEN |
| CI -> GitHub Releases | Release creation + asset upload; authenticated via GITHUB_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-08 | T (Tampering) | git tag | accept | Tag is signed by git author. Force-pushing tags is not done. Tag triggers CI which runs full test suite before publishing. |
| T-82-09 | D (Denial of Service) | CI pipeline | accept | CI runs on GitHub-hosted runners with standard timeouts. Tag push is a single event; no amplification. |
</threat_model>

<verification>
- CI run for v1.1.1 tag is green (all jobs pass)
- `gh release view v1.1.1` shows the release with .deb asset
- `gh release view v1.1.0` shows the retroactive release
- Docker image is published at ghcr.io/thejuran/seedsyncarr:1.1.1
</verification>

<success_criteria>
v1.1.1 is a fully published release: git tag pushed, CI green, Docker images on GHCR (1.1.1 + latest), .deb on GitHub Release, both v1.1.0 and v1.1.1 GitHub Releases exist with categorized notes.
</success_criteria>

<output>
After completion, create `.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-04-SUMMARY.md`
</output>
