---
phase: 82-release-prep-retro-v110-notes-v111-tag
plan: 03
type: execute
wave: 2
depends_on:
  - 82-01
  - 82-02
files_modified:
  - CHANGELOG.md
  - src/angular/package.json
  - src/angular/package-lock.json
  - src/python/pyproject.toml
  - package.json
  - release-notes.md
autonomous: true
requirements:
  - DOCS-01
user_setup: []

must_haves:
  truths:
    - "CHANGELOG.md has a v1.1.1 entry covering all 12 milestone requirements"
    - "All version strings read 1.1.1 across package.json, angular package.json, pyproject.toml"
    - "release-notes.md contains the v1.1.1 GitHub Release body"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.1.1 changelog entry with Added/Fixed/Security sections"
      contains: "## [1.1.1]"
    - path: "src/angular/package.json"
      provides: "Angular app version at 1.1.1"
      contains: '"version": "1.1.1"'
    - path: "src/python/pyproject.toml"
      provides: "Python package version at 1.1.1 in both [project] and [tool.poetry] sections"
      contains: 'version = "1.1.1"'
    - path: "package.json"
      provides: "Root package version at 1.1.1"
      contains: '"version": "1.1.1"'
    - path: "release-notes.md"
      provides: "GitHub Release body template for CI"
      contains: "### Fixed"
  key_links:
    - from: "src/angular/package.json"
      to: "src/angular/src/app/common/version.ts"
      via: "require('../../../package.json').version"
      pattern: "version.*1.1.1"
    - from: "release-notes.md"
      to: ".github/workflows/ci.yml"
      via: "gh release create --notes-file release-body.md"
      pattern: "sed.*VERSION.*release-notes.md"
---

<objective>
Write the v1.1.1 CHANGELOG.md entry, bump all version strings from 1.0.0 to 1.1.1, and populate release-notes.md with the v1.1.1 GitHub Release body.

Purpose: Per D-08 (single commit bump 1.0.0 -> 1.1.1), D-09 (four version files), and DOCS-01 success criterion 3 (v1.1.1 entry covering 12 requirements).
Output: CHANGELOG.md with v1.1.1 entry, all version files at 1.1.1, release-notes.md ready for CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-CONTEXT.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-RESEARCH.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-PATTERNS.md
@.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, CHANGELOG.md has: preamble + v1.1.0 entry + v1.0.0 entry + comparison links -->
<!-- New v1.1.1 entry goes ABOVE the v1.1.0 entry (newest first) -->
<!-- New comparison link [1.1.1] added at bottom above [1.1.0] -->

<!-- Version files current state: -->
<!-- src/angular/package.json line 3: "version": "1.0.0" -->
<!-- src/python/pyproject.toml line 7: version = "1.0.0" (under [project]) -->
<!-- src/python/pyproject.toml line 45: version = "1.0.0" (under [tool.poetry]) -->
<!-- package.json (root): NO version field exists — add "version": "1.1.1" as first key -->

<!-- version.ts reads: require("../../../package.json").version — this is src/angular/package.json -->

<!-- release-notes.md was created by Plan 02 as a placeholder; overwrite with actual v1.1.1 body -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write v1.1.1 CHANGELOG entry and populate release-notes.md</name>
  <files>CHANGELOG.md, release-notes.md</files>
  <read_first>
    - CHANGELOG.md (current state after Plan 01 — has v1.1.0 + v1.0.0 entries)
    - .planning/REQUIREMENTS.md (all 12 v1.1.1 requirements for changelog content)
    - .planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-RESEARCH.md (v1.1.1 changelog draft in Code Examples)
    - .planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-PATTERNS.md (Keep a Changelog entry format)
    - release-notes.md (placeholder from Plan 02 — will be overwritten)
  </read_first>
  <action>
Edit CHANGELOG.md to add the v1.1.1 entry ABOVE the v1.1.0 entry. Per D-01 (Keep a Changelog sections) and D-02 (user-facing language only — UAT, TEST, TECH items are internal and do not appear). The 12 requirements map to user-facing entries as follows:

- FIX-01 -> Fixed (bulk bar "Re-Queue from Remote")
- FIX-02 -> Fixed (auto-delete pack safety)
- SEC-01 -> Security (basic-ftp update)
- SEC-02 -> Added (optional Fernet encryption)
- UAT-01, UAT-02, UAT-03 -> NOT in changelog (internal testing, not user-facing per D-02)
- TEST-01, TEST-02 -> NOT in changelog (internal CI infra, not user-facing per D-02)
- TECH-01, TECH-02 -> NOT in changelog (internal tech debt, not user-facing per D-02)
- DOCS-01 -> NOT in changelog (this phase itself)

Insert this content between the preamble and the v1.1.0 header:

```markdown
## [1.1.1] - 2026-04-22

### Added

- Optional encryption at rest for config secrets (API token, webhook secret, Sonarr/Radarr API keys, remote password). Enable via `[Encryption]` section in config; a keyfile is generated on first enable with restrictive permissions.

### Fixed

- Bulk-actions bar now shows "Re-Queue from Remote" when the selection includes deleted files, matching the single-file behavior.
- Auto-delete for multi-file packs waits until every child file is confirmed as imported before deleting, preventing premature deletion when an import is silently rejected.

### Security

- Updated `basic-ftp` transitive dependency to 5.3.0+ to close a denial-of-service advisory (GHSA-rp42-5vxx-qpwr).
```

Add the v1.1.1 comparison link at the BOTTOM of the file, above the existing [1.1.0] link:

```markdown
[1.1.1]: https://github.com/thejuran/seedsyncarr/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/thejuran/seedsyncarr/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/thejuran/seedsyncarr/releases/tag/v1.0.0
```

Then overwrite `release-notes.md` with the v1.1.1 GitHub Release body per D-03 format:

```markdown
Post-redesign cleanup and hardening — encryption at rest for config secrets, two bug fixes, and a security dependency update.

### Added

- Optional encryption at rest for config secrets (API token, webhook secret, Sonarr/Radarr API keys, remote password). Enable via `[Encryption]` section in config; a keyfile is generated on first enable with restrictive permissions.

### Fixed

- Bulk-actions bar now shows "Re-Queue from Remote" when the selection includes deleted files, matching the single-file behavior.
- Auto-delete for multi-file packs waits until every child file is confirmed as imported before deleting, preventing premature deletion when an import is silently rejected.

### Security

- Updated `basic-ftp` transitive dependency to 5.3.0+ to close a denial-of-service advisory (GHSA-rp42-5vxx-qpwr).

**Full changelog:** https://github.com/thejuran/seedsyncarr/blob/main/CHANGELOG.md
```

Note: release-notes.md does NOT need the `{{VERSION}}` placeholder — the CI job in Plan 02 uses `sed` to replace it, but since the content here is static (no version-dependent text in the body), the `sed` is a no-op. The `--notes-file` reads the file as-is.
  </action>
  <verify>
    <automated>grep '## \[1.1.1\] - 2026-04-22' CHANGELOG.md && grep '### Fixed' CHANGELOG.md && grep '### Security' CHANGELOG.md && grep '\[1.1.1\]: https://github.com/thejuran/seedsyncarr/compare/v1.1.0...v1.1.1' CHANGELOG.md && grep 'Re-Queue from Remote' release-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [1.1.1] - 2026-04-22`
    - CHANGELOG.md v1.1.1 entry has `### Added` with encryption at rest item
    - CHANGELOG.md v1.1.1 entry has `### Fixed` with two items (bulk bar + auto-delete)
    - CHANGELOG.md v1.1.1 entry has `### Security` with basic-ftp item
    - CHANGELOG.md v1.1.1 entry appears ABOVE v1.1.0 entry (newest first)
    - CHANGELOG.md contains `[1.1.1]: https://github.com/thejuran/seedsyncarr/compare/v1.1.0...v1.1.1` at bottom
    - CHANGELOG.md has three comparison links at bottom in order: [1.1.1], [1.1.0], [1.0.0]
    - release-notes.md contains the same Added/Fixed/Security sections as the changelog entry
    - release-notes.md contains `**Full changelog:**` link at the bottom
    - No phase references in either file (no "Phase 75", "Phase 81", etc.)
    - No internal items in changelog (no "pytest-cache", "Playwright CSP", "arm64 rar", "WAITING_FOR_IMPORT")
  </acceptance_criteria>
  <done>CHANGELOG.md has v1.1.1 entry with user-facing Added/Fixed/Security sections covering the 4 user-visible requirements. release-notes.md has matching GitHub Release body ready for CI.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version strings from 1.0.0 to 1.1.1 across all version files</name>
  <files>package.json, src/angular/package.json, src/angular/package-lock.json, src/python/pyproject.toml</files>
  <read_first>
    - package.json (root — currently has NO version field, only devDependencies and overrides)
    - src/angular/package.json (line 3: "version": "1.0.0")
    - src/python/pyproject.toml (line 7: version = "1.0.0" under [project], line 45: version = "1.0.0" under [tool.poetry])
    - src/angular/package-lock.json (line 3 area: version field that must match package.json)
  </read_first>
  <action>
Per D-08 (bump 1.0.0 -> 1.1.1, single commit) and D-09 (four files):

**1. Root `package.json`** — Add a `"version"` field as the first key. The root package.json currently has no version field (RESEARCH.md Pitfall 1). Add it for consistency per D-09. The file should become:

```json
{
  "version": "1.1.1",
  "devDependencies": {
    "puppeteer": "^24.40.0"
  },
  "overrides": {
    "basic-ftp": "^5.3.0"
  }
}
```

**2. `src/angular/package.json`** — Change line 3 from `"version": "1.0.0"` to `"version": "1.1.1"`. This is the version that flows to the Angular app via `version.ts` (`require("../../../package.json").version`).

**3. `src/angular/package-lock.json`** — After editing `src/angular/package.json`, run `cd src/angular && npm install` to regenerate the lockfile with the new version. This updates the `"version"` field in the lockfile to match. Do NOT manually edit the lockfile.

**4. `src/python/pyproject.toml`** — Change BOTH version fields (RESEARCH.md Pitfall 2):
   - Line 7 under `[project]`: `version = "1.0.0"` -> `version = "1.1.1"`
   - Line 45 under `[tool.poetry]`: `version = "1.0.0"` -> `version = "1.1.1"`

After all edits, verify zero occurrences of `1.0.0` remain in the version files:
- `grep "1.0.0" package.json src/angular/package.json src/python/pyproject.toml` should return empty
  </action>
  <verify>
    <automated>grep '"version": "1.1.1"' package.json && grep '"version": "1.1.1"' src/angular/package.json && grep 'version = "1.1.1"' src/python/pyproject.toml | wc -l | grep -q 2 && ! grep '1.0.0' package.json src/angular/package.json src/python/pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` (root) contains `"version": "1.1.1"` as first key
    - `src/angular/package.json` contains `"version": "1.1.1"` (was `"version": "1.0.0"`)
    - `src/python/pyproject.toml` [project] section contains `version = "1.1.1"` (line 7 area)
    - `src/python/pyproject.toml` [tool.poetry] section contains `version = "1.1.1"` (line 45 area)
    - `grep "1.0.0" package.json src/angular/package.json src/python/pyproject.toml` returns zero matches
    - `src/angular/package-lock.json` has been regenerated (contains `"version": "1.1.1"` for the seedsyncarr package)
    - `debian/DEBIAN/control` already contains `Version: 1.1.1` from Plan 02 (no change needed here)
  </acceptance_criteria>
  <done>All four version files show 1.1.1. Zero occurrences of 1.0.0 remain. package-lock.json is regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan edits documentation and config files only; no runtime code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-06 | T (Tampering) | version strings | accept | Version strings are in version-controlled files; git history provides audit trail. No runtime security impact. |
| T-82-07 | I (Information Disclosure) | release-notes.md | accept | Public changelog content for a public repo. Intentionally public. |
</threat_model>

<verification>
- `grep '## \[1.1.1\]' CHANGELOG.md` matches
- `grep '"version": "1.1.1"' src/angular/package.json` matches
- `grep 'version = "1.1.1"' src/python/pyproject.toml` returns 2 matches
- `grep '"version": "1.1.1"' package.json` matches
- `grep '1.0.0' package.json src/angular/package.json src/python/pyproject.toml` returns empty
</verification>

<success_criteria>
CHANGELOG.md has v1.1.1 entry with Added/Fixed/Security sections. All version files bumped to 1.1.1. release-notes.md contains the GitHub Release body for CI. No stale 1.0.0 references remain in version files.
</success_criteria>

<output>
After completion, create `.planning/phases/82-release-prep-retro-v110-notes-v111-tag/82-03-SUMMARY.md`
</output>
