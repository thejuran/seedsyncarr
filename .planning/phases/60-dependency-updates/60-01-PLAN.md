---
phase: 60-dependency-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/pyproject.toml
  - src/python/poetry.lock
autonomous: true
requirements:
  - PLSH-01
  - PLSH-03
must_haves:
  truths:
    - "mkdocs-material, pyinstaller, and pytest-cov Dependabot PRs are merged to main"
    - "poetry.lock reflects the updated minor/patch versions"
    - "CI passes after all three merges"
  artifacts:
    - path: "src/python/pyproject.toml"
      provides: "Updated dev dependency version constraints"
    - path: "src/python/poetry.lock"
      provides: "Resolved lockfile with updated versions"
  key_links:
    - from: "Dependabot PRs #2, #3, #5"
      to: "main branch"
      via: "gh pr merge"
      pattern: "merge.*main"
---

<objective>
Merge all minor/patch Dependabot PRs (#2 mkdocs-material 9.7.1->9.7.6, #3 pyinstaller 6.17.0->6.19.0, #5 pytest-cov 7.0.0->7.1.0) to main.

Purpose: Clear safe dependency updates before tackling major version bumps
Output: Three PRs merged, main branch with updated lockfile, CI green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/python/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge minor/patch Dependabot PRs</name>
  <files>src/python/pyproject.toml, src/python/poetry.lock</files>
  <read_first>src/python/pyproject.toml, src/python/poetry.lock</read_first>
  <action>
Merge the three minor/patch Dependabot PRs one at a time. These PRs may have merge conflicts against main (poetry.lock was regenerated recently), so use merge commits and resolve conflicts if needed.

For each PR (#2, #3, #5), follow this sequence:

1. Check the PR status: `gh pr view {N} --repo thejuran/seedsyncarr`
2. If the PR has merge conflicts, update the PR branch from main first:
   - `gh pr checkout {N}`
   - `git merge main` (resolve any poetry.lock conflicts by re-running `cd src/python && poetry lock --no-update` after accepting the pyproject.toml changes, then committing)
   - `git push`
3. Wait for CI to pass: `gh pr checks {N} --repo thejuran/seedsyncarr --watch`
4. Merge: `gh pr merge {N} --repo thejuran/seedsyncarr --merge --delete-branch`

Order of merges:
- PR #2 (mkdocs-material 9.7.1 -> 9.7.6) first
- PR #3 (pyinstaller 6.17.0 -> 6.19.0) second
- PR #5 (pytest-cov 7.0.0 -> 7.1.0) third

After all three are merged, pull main: `git checkout main && git pull`

IMPORTANT: If any PR's CI fails, stop and investigate -- do NOT force merge.
  </action>
  <verify>
    <automated>gh pr list --repo thejuran/seedsyncarr --state merged --json number,title | grep -E '"number":(2|3|5)' | wc -l | grep -q 3 && echo "All 3 minor PRs merged" || echo "FAIL: not all merged"</automated>
  </verify>
  <acceptance_criteria>
    - PR #2 (mkdocs-material) shows state "merged" in `gh pr view 2 --repo thejuran/seedsyncarr --json state`
    - PR #3 (pyinstaller) shows state "merged" in `gh pr view 3 --repo thejuran/seedsyncarr --json state`
    - PR #5 (pytest-cov) shows state "merged" in `gh pr view 5 --repo thejuran/seedsyncarr --json state`
    - Local main is up to date with remote after `git pull`
  </acceptance_criteria>
  <done>All three minor/patch Dependabot PRs merged to main with no CI failures</done>
</task>

<task type="auto">
  <name>Task 2: Verify CI green on main after merges</name>
  <files></files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
After all three PRs are merged, verify the latest CI run on main is green:

1. Check the latest CI run: `gh run list --repo thejuran/seedsyncarr --branch main --limit 3`
2. If the latest run is still in progress, watch it: `gh run watch {run_id} --repo thejuran/seedsyncarr`
3. Verify all jobs passed: `gh run view {run_id} --repo thejuran/seedsyncarr`
4. Specifically confirm these jobs passed:
   - "Python unit tests" (unittests-python)
   - "Angular unit tests" (unittests-angular)
   - "Build Docker Image" (build-docker-image)

If any job failed, investigate the failure logs: `gh run view {run_id} --repo thejuran/seedsyncarr --log-failed`
  </action>
  <verify>
    <automated>gh run list --repo thejuran/seedsyncarr --branch main --limit 1 --json status,conclusion --jq '.[0].conclusion' | grep -q "success" && echo "CI green" || echo "FAIL: CI not green"</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --repo thejuran/seedsyncarr --branch main --limit 1 --json conclusion` returns "success"
    - Python unit tests job passed
    - Angular unit tests job passed
    - Docker image build job passed
  </acceptance_criteria>
  <done>CI pipeline fully green on main after all minor/patch dependency merges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot PR -> main | Third-party dependency code enters production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | poetry.lock | mitigate | Verify PRs come from Dependabot bot (author check), merge only if CI passes |
| T-60-02 | Elevation | dev dependencies | accept | All updated packages are dev-only (mkdocs-material, pyinstaller, pytest-cov) -- not shipped in production image |
</threat_model>

<verification>
- All three minor/patch PRs (#2, #3, #5) show "merged" state
- CI on main is green (all jobs pass)
- No open Dependabot alerts for these three packages
</verification>

<success_criteria>
PRs #2, #3, #5 merged to main. CI fully green. poetry.lock updated.
</success_criteria>

<output>
After completion, create `.planning/phases/60-dependency-updates/60-01-SUMMARY.md`
</output>
