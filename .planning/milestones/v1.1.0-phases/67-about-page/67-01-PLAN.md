---
phase: 67-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/pages/about/about-page.component.ts
  - src/angular/src/app/pages/about/about-page.component.html
  - src/angular/src/app/pages/about/about-page.component.scss
  - src/angular/src/app/pages/about/about-page.component.spec.ts
autonomous: true
requirements:
  - ABUT-01
  - ABUT-02
  - ABUT-03
  - ABUT-04

must_haves:
  truths:
    - "Identity card shows brand favicon, 'SeedSyncarr' with amber arr, version (Stable), and README tagline"
    - "System info table renders 7 key-value rows with uppercase labels and monospace values"
    - "Link cards grid shows 4 cards (GitHub, Docs, Report Issue, Changelog) with hover-to-amber transitions"
    - "License badge shows 'Apache License 2.0' and copyright footer with fork attribution"
  artifacts:
    - path: "src/angular/src/app/pages/about/about-page.component.ts"
      provides: "Component with version and angularVersion properties"
      contains: "VERSION.full"
    - path: "src/angular/src/app/pages/about/about-page.component.html"
      provides: "4-section layout: identity, sysinfo, links, footer"
      contains: "brand-arr"
    - path: "src/angular/src/app/pages/about/about-page.component.scss"
      provides: "All visual styling with literal hex values from mockup"
      contains: "identity-card"
    - path: "src/angular/src/app/pages/about/about-page.component.spec.ts"
      provides: "Unit tests for ABUT-01 through ABUT-04"
      contains: "SeedSyncarr"
  key_links:
    - from: "about-page.component.html"
      to: "about-page.component.ts"
      via: "{{ version }} and {{ angularVersion }} template bindings"
      pattern: "version.*angularVersion"
    - from: "about-page.component.scss"
      to: "about-page.component.html"
      via: "CSS class selectors matching template elements"
      pattern: "identity-card.*sysinfo-table.*link-card"
---

<objective>
Rewrite the About page component (TS, HTML, SCSS) to match the AIDesigner mockup with pixel-exact fidelity. Create unit tests covering all four ABUT requirements.

Purpose: Deliver the final page in the v1.1.0 UI redesign milestone -- a polished identity/info page with the Deep Moss + Amber palette.
Output: Fully styled About page with identity card, system info table, link cards grid, license footer, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-about-page/67-CONTEXT.md
@.planning/phases/67-about-page/67-RESEARCH.md
@.planning/phases/67-about-page/67-PATTERNS.md

@src/angular/src/app/pages/about/about-page.component.ts
@src/angular/src/app/pages/about/about-page.component.html
@src/angular/src/app/pages/about/about-page.component.scss
@src/angular/src/app/pages/main/app.component.scss
@src/angular/src/app/pages/logs/logs-page.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AboutPageComponent TypeScript and create unit test scaffold</name>
  <files>
    src/angular/src/app/pages/about/about-page.component.ts,
    src/angular/src/app/pages/about/about-page.component.spec.ts
  </files>
  <read_first>
    src/angular/src/app/pages/about/about-page.component.ts,
    src/angular/src/app/pages/main/app.component.ts,
    src/angular/src/app/pages/about/about-page.component.html,
    .planning/phases/67-about-page/67-RESEARCH.md,
    .planning/phases/67-about-page/67-PATTERNS.md
  </read_first>
  <action>
Rewrite `about-page.component.ts` to add the `angularVersion` property per D-02. The complete component:

```typescript
import { Component, VERSION } from "@angular/core";

declare function require(moduleName: string): { version: string };
const { version: appVersion } = require("../../../../package.json");

@Component({
    selector: "app-about-page",
    templateUrl: "./about-page.component.html",
    styleUrls: ["./about-page.component.scss"],
    providers: [],
    standalone: true
})
export class AboutPageComponent {

    public version: string;
    public angularVersion: string;

    constructor() {
        this.version = appVersion;
        this.angularVersion = VERSION.full;
    }
}
```

Key changes from existing file:
- Add `VERSION` to the `@angular/core` import
- Add `public angularVersion: string` property
- Set `this.angularVersion = VERSION.full` in constructor
- No `CommonModule` import needed -- template is fully static with only two interpolation bindings

Then create `about-page.component.spec.ts` with unit tests covering ABUT-01 through ABUT-04:

```typescript
import { ComponentFixture, TestBed } from "@angular/core/testing";
import { AboutPageComponent } from "./about-page.component";

describe("AboutPageComponent", () => {
    let component: AboutPageComponent;
    let fixture: ComponentFixture<AboutPageComponent>;
    let el: HTMLElement;

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            imports: [AboutPageComponent]
        }).compileComponents();
        fixture = TestBed.createComponent(AboutPageComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
        el = fixture.nativeElement;
    });

    // ABUT-01: Identity card
    it("should display brand title with amber arr span", () => {
        const title = el.querySelector(".identity-title");
        expect(title).toBeTruthy();
        expect(title!.textContent).toContain("SeedSync");
        const arrSpan = title!.querySelector(".brand-arr");
        expect(arrSpan).toBeTruthy();
        expect(arrSpan!.textContent).toBe("arr");
    });

    it("should display app version with Stable suffix", () => {
        const badge = el.querySelector(".version-badge");
        expect(badge).toBeTruthy();
        expect(badge!.textContent).toContain(component.version);
        expect(badge!.textContent).toContain("(Stable)");
    });

    it("should display brand favicon image", () => {
        const img = el.querySelector(".brand-favicon") as HTMLImageElement;
        expect(img).toBeTruthy();
        expect(img.src).toContain("favicon");
    });

    it("should display tagline text", () => {
        const tagline = el.querySelector(".identity-tagline");
        expect(tagline).toBeTruthy();
        expect(tagline!.textContent).toContain("Sync files from your seedbox");
    });

    // ABUT-02: System info table
    it("should render 7 system info rows", () => {
        const rows = el.querySelectorAll(".sysinfo-row");
        expect(rows.length).toBe(7);
    });

    it("should display App Version row with live value", () => {
        const rows = el.querySelectorAll(".sysinfo-row");
        const firstLabel = rows[0].querySelector(".sysinfo-label");
        const firstValue = rows[0].querySelector(".sysinfo-value");
        expect(firstLabel!.textContent!.trim()).toBe("APP VERSION");
        expect(firstValue!.textContent).toContain(component.version);
    });

    it("should display Angular version in Frontend Core row", () => {
        const rows = el.querySelectorAll(".sysinfo-row");
        const secondValue = rows[1].querySelector(".sysinfo-value");
        expect(secondValue!.textContent).toContain(component.angularVersion);
    });

    it("should show placeholder dashes for unavailable system info", () => {
        const rows = el.querySelectorAll(".sysinfo-row");
        // Python Version (index 2), Host OS (3), Uptime (4), Process ID (5)
        for (const idx of [2, 3, 4, 5]) {
            const value = rows[idx].querySelector(".sysinfo-value");
            expect(value!.textContent!.trim()).toBe("\u2014");
        }
    });

    it("should show config path as ~/.seedsyncarr", () => {
        const rows = el.querySelectorAll(".sysinfo-row");
        const configValue = rows[6].querySelector(".sysinfo-value");
        expect(configValue!.textContent!.trim()).toBe("~/.seedsyncarr");
    });

    // ABUT-03: Link cards
    it("should render 4 link cards", () => {
        const cards = el.querySelectorAll(".link-card");
        expect(cards.length).toBe(4);
    });

    it("should have correct hrefs on link cards", () => {
        const cards = el.querySelectorAll(".link-card") as NodeListOf<HTMLAnchorElement>;
        expect(cards[0].href).toBe("https://github.com/thejuran/seedsyncarr");
        expect(cards[1].href).toBe("https://thejuran.github.io/seedsyncarr/");
        expect(cards[2].href).toBe("https://github.com/thejuran/seedsyncarr/issues");
        expect(cards[3].href).toBe("https://github.com/thejuran/seedsyncarr/releases");
    });

    it("should open all link cards in new tabs", () => {
        const cards = el.querySelectorAll(".link-card") as NodeListOf<HTMLAnchorElement>;
        cards.forEach(card => {
            expect(card.target).toBe("_blank");
        });
    });

    // ABUT-04: License and copyright
    it("should display Apache License 2.0 badge", () => {
        const badge = el.querySelector(".license-badge");
        expect(badge).toBeTruthy();
        expect(badge!.textContent).toContain("Apache License 2.0");
    });

    it("should display copyright text", () => {
        const copyright = el.querySelector(".copyright-text");
        expect(copyright).toBeTruthy();
        expect(copyright!.textContent).toContain("2017");
        expect(copyright!.textContent).toContain("Inderpreet Singh");
        expect(copyright!.textContent).toContain("thejuran");
    });

    it("should display fork attribution note", () => {
        const forkNote = el.querySelector(".fork-note");
        expect(forkNote).toBeTruthy();
        expect(forkNote!.textContent).toContain("SeedSync");
        const link = forkNote!.querySelector("a") as HTMLAnchorElement;
        expect(link.href).toBe("https://github.com/ipsingh06/seedsync");
    });
});
```

Note: The spec tests reference CSS classes that will be created in Task 2. Tests will fail until the HTML template is written (RED state -- expected).
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng build --configuration=production 2>&1 | head -20 && grep -c 'sysinfo-row' /Users/julianamacbook/seedsyncarr/src/angular/src/app/pages/about/about-page.component.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - about-page.component.ts contains `import { Component, VERSION } from "@angular/core"`
    - about-page.component.ts contains `public angularVersion: string`
    - about-page.component.ts contains `this.angularVersion = VERSION.full`
    - about-page.component.spec.ts contains `describe("AboutPageComponent"`
    - about-page.component.spec.ts contains `.identity-title`
    - about-page.component.spec.ts contains `.sysinfo-row`
    - about-page.component.spec.ts contains `.link-card`
    - about-page.component.spec.ts contains `Apache License 2.0`
    - about-page.component.spec.ts contains `expect(rows.length).toBe(7)`
    - `grep -c 'sysinfo-row' about-page.component.spec.ts` returns at least 3 (confirms spec scaffold has sysinfo coverage)
  </acceptance_criteria>
  <done>TS component compiles with VERSION import; spec file exists with tests for all 4 ABUT requirements; grep confirms sysinfo-row presence in spec</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite About page HTML template and SCSS with pixel-exact mockup values</name>
  <files>
    src/angular/src/app/pages/about/about-page.component.html,
    src/angular/src/app/pages/about/about-page.component.scss
  </files>
  <read_first>
    src/angular/src/app/pages/about/about-page.component.html,
    src/angular/src/app/pages/about/about-page.component.scss,
    .planning/phases/67-about-page/67-RESEARCH.md,
    .planning/phases/67-about-page/67-PATTERNS.md,
    .aidesigner/runs/2026-04-14T03-35-42-278Z-seedsyncarr-about-page-triggarr-styl/design.html,
    src/angular/src/app/pages/main/app.component.scss,
    src/angular/src/app/pages/logs/logs-page.component.scss,
    src/angular/src/app/pages/about/about-page.component.ts
  </read_first>
  <action>
**HTML template** -- Complete rewrite of `about-page.component.html`. Replace all existing content with the 4-section layout. The template uses only `{{ version }}` and `{{ angularVersion }}` bindings -- no Angular directives needed.

Structure (in order):
1. `<main class="about-main">` wrapper -- centered, max-width 42rem, fade-in-up animation
2. **Identity card section** (per D-04, D-05, D-06, D-07):
   - `.identity-card` section with `.identity-card-glow` ambient overlay
   - `.identity-icon-container` with `<img src="assets/favicon-192.png" alt="SeedSyncarr" class="brand-favicon">` (use favicon-192 for sharpness at 6rem display per RESEARCH.md Open Question 1)
   - `<h2 class="identity-title">SeedSync<span class="brand-arr">arr</span></h2>`
   - `<p class="identity-tagline">Sync files from your seedbox to your local media server &mdash; fast, automated, and integrated with Sonarr and Radarr.</p>` (per D-06)
   - `<div class="identity-version-block"><span class="version-badge">Version {{ version }} (Stable)</span></div>` (per D-07)

3. **System info section** (per D-02, D-03):
   - `.about-card` wrapper with `.sysinfo-header` ("System Information" with `<i class="ph ph-info"></i>` icon)
   - `.sysinfo-table` container with 7 `.sysinfo-row` divs, each containing `.sysinfo-label` (uppercase) and `.sysinfo-value` (monospace):
     - "APP VERSION" / `{{ version }} (Stable)`
     - "FRONTEND CORE" / `Angular {{ angularVersion }}`
     - "PYTHON VERSION" / `\u2014` (em dash)
     - "HOST OS" / `\u2014`
     - "UPTIME" / `\u2014`
     - "PROCESS ID" / `\u2014`
     - "CONFIG PATH" / `~/.seedsyncarr`

4. **Link cards section** (per D-08, D-09, D-10, D-11):
   - `.link-cards-grid` section with 4 `<a class="link-card">` elements
   - Each has `target="_blank"` and `rel="noopener noreferrer"` (per D-10)
   - Card 1: `<i class="ph ph-github-logo"></i><span>GitHub</span>` href=`https://github.com/thejuran/seedsyncarr`
   - Card 2: `<i class="ph ph-book"></i><span>Docs</span>` href=`https://thejuran.github.io/seedsyncarr/`
   - Card 3: `<i class="ph ph-bug"></i><span>Report Issue</span>` href=`https://github.com/thejuran/seedsyncarr/issues`
   - Card 4: `<i class="ph ph-git-commit"></i><span>Changelog</span>` href=`https://github.com/thejuran/seedsyncarr/releases`

5. **Footer section** (per D-12, D-13):
   - `.about-footer` section
   - `.license-badge` with `<i class="ph-fill ph-scales"></i><span>Apache License 2.0</span>` (per D-12)
   - `<p class="copyright-text">&copy; 2017-2026 Inderpreet Singh, thejuran. Open source software.</p>`
   - `<p class="fork-note">Based on the original <a href="https://github.com/ipsingh06/seedsync" target="_blank" rel="noopener noreferrer">SeedSync</a> by Inderpreet Singh</p>`

**SCSS stylesheet** -- Complete rewrite of `about-page.component.scss`. Use literal hex values from the mockup, NOT Bootstrap utility classes. Follow the logs-page SCSS pattern for file structure.

The SCSS file structure:

```scss
@use '../../common/common' as *;

// Local palette (same values as logs-page.component.scss)
$amber: #c49a4a;
$text: #e0e8d6;
$textsec: #9aaa8a;
$border: #3e4a38;
$card: #222a20;
$base: #151a14;
$muted: #2c3629;
$row: #1a2019;

// ============================================================================
// Page Animation
// ============================================================================

@keyframes fade-in-up {
    0% {
        opacity: 0;
        transform: translateY(10px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

// ============================================================================
// Host + Main Layout
// ============================================================================

:host {
    display: flex;
    flex-direction: column;
    flex-grow: 1;
}

.about-main {
    flex-grow: 1;
    width: 100%;
    max-width: 42rem;          // 672px per mockup max-w-2xl
    margin: 0 auto;
    padding: 3rem 1.5rem;     // py-12 px-6
    display: flex;
    flex-direction: column;
    gap: 2rem;                 // gap-8
    opacity: 0;
    animation: fade-in-up 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

// ============================================================================
// Identity Card (ABUT-01)
// ============================================================================

.identity-card {
    position: relative;
    overflow: hidden;
    background: $card;
    border: 1px solid $border;
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
}

.identity-card-glow {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 16rem;
    height: 8rem;
    background: rgba(196, 154, 74, 0.05);
    filter: blur(80px);
    pointer-events: none;
    border-radius: 50%;
}

.identity-icon-container {
    width: 6rem;
    height: 6rem;
    border-radius: 1rem;
    background: linear-gradient(to bottom, $row, $card);
    border: 1px solid rgba(196, 154, 74, 0.30);
    box-shadow: 0 0 20px rgba(196, 154, 74, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    position: relative;
    z-index: 10;
}

.brand-favicon {
    width: 3rem;
    height: 3rem;
    object-fit: contain;
}

.identity-title {
    font-size: 1.875rem;       // text-3xl
    font-weight: 700;          // font-bold
    letter-spacing: -0.025em;  // tracking-tight
    color: $text;
    margin: 0 0 0.5rem 0;

    .brand-arr {
        color: $amber;
    }
}

.identity-tagline {
    font-size: 0.875rem;       // text-sm
    color: $textsec;
    max-width: 28rem;          // max-w-md
    line-height: 1.625;        // leading-relaxed
    margin: 0 0 1.5rem 0;
}

.identity-version-block {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
}

.version-badge {
    font-size: 0.75rem;
    font-family: 'JetBrains Mono', var(--bs-font-monospace);
    color: $textsec;
    background: $muted;
    border: 1px solid $border;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
}

// ============================================================================
// System Info (ABUT-02)
// ============================================================================

.about-card {
    background: $card;
    border: 1px solid $border;
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
}

.sysinfo-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;

    i {
        font-size: 1.125rem;
        color: $textsec;
    }

    span {
        font-size: 0.875rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: $textsec;
    }
}

.sysinfo-table {
    background: $row;
    border: 1px solid rgba(62, 74, 56, 0.40);
    border-radius: 0.75rem;
    overflow: hidden;
    box-shadow: inset 0 2px 4px 0 rgba(0, 0, 0, 0.05);
    display: flex;
    flex-direction: column;
}

.sysinfo-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.875rem 1.25rem;
    border-top: 1px solid rgba(62, 74, 56, 0.40);
    transition: background-color 150ms;

    &:first-child {
        border-top: none;
    }

    &:hover {
        background: rgba(44, 54, 41, 0.30);
    }
}

.sysinfo-label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: $textsec;
    width: 33.333%;
}

.sysinfo-value {
    font-size: 0.8125rem;
    font-family: 'JetBrains Mono', var(--bs-font-monospace);
    color: $text;
    width: 66.666%;
    text-align: right;
}

// ============================================================================
// Link Cards (ABUT-03)
// ============================================================================

.link-cards-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;

    @media (min-width: 768px) {
        grid-template-columns: repeat(4, 1fr);
    }
}

.link-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 1rem;
    border-radius: 0.75rem;
    background: $card;
    border: 1px solid $border;
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    text-decoration: none;
    transition: all 200ms;

    &:hover {
        border-color: $amber;
        background: rgba(44, 54, 41, 0.40);
    }

    i {
        font-size: 1.25rem;
        color: $textsec;
        transition: color 200ms;
    }

    span {
        font-size: 0.875rem;
        font-weight: 500;
        color: $text;
        transition: color 200ms;
    }

    &:hover i,
    &:hover span {
        color: $amber;
    }
}

// ============================================================================
// License Footer (ABUT-04)
// ============================================================================

.about-footer {
    margin-top: 1rem;
    margin-bottom: 2rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
}

.license-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    background: rgba(34, 42, 32, 0.50);
    border: 1px solid rgba(62, 74, 56, 0.30);

    i {
        color: rgba(154, 170, 138, 0.60);
    }

    span {
        font-size: 0.75rem;
        font-family: 'JetBrains Mono', var(--bs-font-monospace);
        color: rgba(154, 170, 138, 0.80);
        text-transform: uppercase;
        letter-spacing: 0.1em;
    }
}

.copyright-text {
    font-size: 0.75rem;
    color: rgba(154, 170, 138, 0.50);
    margin: 0;
}

.fork-note {
    font-size: 0.6875rem;
    color: rgba(154, 170, 138, 0.40);
    margin: 0;

    a {
        color: rgba(154, 170, 138, 0.60);
        text-decoration: underline;
        transition: color 200ms;

        &:hover {
            color: $amber;
        }
    }
}
```

CRITICAL anti-patterns to avoid:
- Do NOT use Bootstrap utility classes (`bg-secondary`, `text-muted`, `p-4`) as approximations -- use literal hex values and px/rem values from the mockup
- Do NOT use `<table>` elements for the system info section -- use div-based rows per the mockup
- Do NOT use the Phosphor `ph-arrows-merge` icon for the identity card -- use the actual `favicon-192.png` image per D-04
- Do NOT write "MIT License" -- the license is "Apache License 2.0" per D-12
- Do NOT add `CommonModule` to the component imports -- the template needs no Angular directives

After writing both files, run the unit tests to verify all pass.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false --browsers ChromeHeadless --include='**/about-page.component.spec.ts' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - about-page.component.html contains `class="identity-card"`
    - about-page.component.html contains `SeedSync<span class="brand-arr">arr</span>`
    - about-page.component.html contains `assets/favicon-192.png`
    - about-page.component.html contains `{{ version }} (Stable)`
    - about-page.component.html contains `{{ angularVersion }}`
    - about-page.component.html contains `Sync files from your seedbox`
    - about-page.component.html contains 7 occurrences of `sysinfo-row`
    - about-page.component.html contains `https://github.com/thejuran/seedsyncarr` (GitHub link)
    - about-page.component.html contains `https://thejuran.github.io/seedsyncarr/` (Docs link)
    - about-page.component.html contains `https://github.com/thejuran/seedsyncarr/issues` (Report Issue link)
    - about-page.component.html contains `https://github.com/thejuran/seedsyncarr/releases` (Changelog link)
    - about-page.component.html contains `target="_blank"` on all 4 link cards
    - about-page.component.html contains `ph ph-github-logo` and `ph ph-book` and `ph ph-bug` and `ph ph-git-commit`
    - about-page.component.html contains `Apache License 2.0`
    - about-page.component.html contains `Copyright` and `Inderpreet Singh`
    - about-page.component.html does NOT contain `id="about"` or `id="wrapper"` (old structure removed)
    - about-page.component.scss contains `$amber: #c49a4a`
    - about-page.component.scss contains `max-width: 42rem`
    - about-page.component.scss contains `@keyframes fade-in-up`
    - about-page.component.scss contains `.identity-icon-container` with `width: 6rem`
    - about-page.component.scss contains `.sysinfo-row` with `border-top: 1px solid rgba(62, 74, 56, 0.40)`
    - about-page.component.scss contains `.link-card` with `&:hover` and `border-color: $amber`
    - about-page.component.scss contains `.license-badge` with `border-radius: 9999px`
    - about-page.component.scss does NOT contain `bg-secondary` or `text-muted` (no Bootstrap utility approximations)
    - Unit tests pass: `ng test --include='**/about-page.component.spec.ts'` exits 0
  </acceptance_criteria>
  <done>HTML template renders 4 sections (identity, sysinfo, links, footer) with correct content per all 13 decisions; SCSS uses literal mockup values; all unit tests pass green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This page is fully static -- no user input, no API calls, no dynamic data beyond build-time version strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Information Disclosure | External link cards | accept | All links point to public GitHub/docs URLs; no sensitive data exposed. `rel="noopener noreferrer"` prevents tab-napping |
| T-67-02 | Tampering | Version display from package.json | accept | Build-time read-only value; no user-controlled input; no XSS vector (Angular sanitizes interpolation) |
</threat_model>

<verification>
1. `cd src/angular && npx ng test --watch=false --browsers ChromeHeadless --include='**/about-page.component.spec.ts'` -- all tests pass
2. `cd src/angular && npx ng build --configuration=production` -- builds without errors
3. `grep -c 'sysinfo-row' src/angular/src/app/pages/about/about-page.component.html` -- returns 7
4. `grep 'Apache License 2.0' src/angular/src/app/pages/about/about-page.component.html` -- found
5. `grep 'VERSION' src/angular/src/app/pages/about/about-page.component.ts` -- found
</verification>

<success_criteria>
- About page builds and renders with 4 distinct visual sections
- Identity card shows brand favicon, "SeedSyncarr" with amber "arr", version (Stable), README tagline
- System info table shows 7 rows with live App Version + Angular Version, placeholder dashes for server-side data
- 4 link cards with correct URLs, Phosphor icons, and hover-to-amber CSS transitions
- License badge shows "Apache License 2.0", copyright and fork attribution displayed
- All unit tests pass covering ABUT-01 through ABUT-04
</success_criteria>

<output>
After completion, create `.planning/phases/67-about-page/67-01-SUMMARY.md`
</output>
