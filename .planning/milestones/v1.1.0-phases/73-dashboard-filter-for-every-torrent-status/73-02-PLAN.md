---
phase: 73-dashboard-filter-for-every-torrent-status
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/angular/src/app/pages/files/transfer-table.component.html
autonomous: true
requirements: [D-01, D-03, D-04, D-05, D-14, D-15]
tags: [angular, template, segment-filter, transfer-table, design-spec]

must_haves:
  truths:
    - "A 'Done' parent button is rendered between the Active expand block and the Errors parent button in the .segment-filters group"
    - "Clicking the Done parent triggers onSegmentChange('done') and toggles a chevron icon (down when collapsed, up when expanded)"
    - "When activeSegment === 'done', the Done expand block reveals exactly two sub-buttons labelled 'Downloaded' and 'Extracted'"
    - "The Active expand block reveals four sub-buttons in the order Pending, Syncing, Queued, Extracting (Pending is the new left-most sub)"
    - "Each sub-button uses the existing .btn-sub class verbatim — no per-instance overrides, no new SCSS classes"
    - "The new Pending/Downloaded/Extracted sub-buttons display an .accent-dot when their corresponding ViewFileStatus value matches activeSubStatus"
    - "Mobile responsive behavior is unchanged — the new buttons inherit the existing .segment-filters hidden-below-768px rule from D-14"
  artifacts:
    - path: "src/angular/src/app/pages/files/transfer-table.component.html"
      provides: "Done parent button + Done expand block + Pending sub-button"
      contains: "onSegmentChange('done') AND ViewFileStatus.DEFAULT AND ViewFileStatus.DOWNLOADED AND ViewFileStatus.EXTRACTED"
  key_links:
    - from: "Done parent button"
      to: "TransferTableComponent.onSegmentChange"
      via: "(click)='onSegmentChange(\"done\")'"
      pattern: "onSegmentChange\\(['\"]done['\"]\\)"
    - from: "Done sub-buttons"
      to: "TransferTableComponent.onSubStatusChange"
      via: "(click)='onSubStatusChange(ViewFileStatus.DOWNLOADED)' / 'EXTRACTED'"
      pattern: "onSubStatusChange\\(ViewFileStatus\\.(DOWNLOADED|EXTRACTED|DEFAULT)\\)"
    - from: "Pending sub-button"
      to: "TransferTableComponent.onSubStatusChange"
      via: "(click)='onSubStatusChange(ViewFileStatus.DEFAULT)'"
      pattern: "onSubStatusChange\\(ViewFileStatus\\.DEFAULT\\)"
---

<objective>
Add the new "Done" parent segment button + its expand block (Downloaded, Extracted sub-buttons) to `transfer-table.component.html`, and add the new "Pending" sub-button as the FIRST sub under the existing Active expand block. All new markup mirrors the existing Active/Errors blocks byte-for-byte except for the segment value, label text, and `ViewFileStatus` enum value — every existing class name (`.btn-segment`, `.btn-segment--parent-active`, `.btn-segment--parent-expanded`, `.btn-sub`, `.btn-sub.active`, `.accent-dot`, `.segment-divider`) is reused verbatim per the locked drill-down design spec and per the user's "Port AIDesigner HTML identically" rule (memory feedback_design_spec_rigor.md).

Purpose: D-01 (3 new statuses become reachable as filters), D-03 (user-facing labels: Pending, Downloaded, Extracted), D-04 (Done as new top-level parent in the order All / Active / Done / Errors), D-05 (Pending under Active, left-most), D-14 (mobile responsive behavior inherited — no new media queries), D-15 (reuse SCSS classes verbatim).

Output: `transfer-table.component.html` with one new parent button, one new expand block (Done), and one new sub-button (Pending) inserted into the existing Active expand block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/73-dashboard-filter-for-every-torrent-status/73-CONTEXT.md
@.planning/phases/73-dashboard-filter-for-every-torrent-status/73-PATTERNS.md
@docs/superpowers/specs/2026-04-15-drilldown-segment-filters-design.md

<interfaces>
<!-- Verbatim shape of the existing Errors parent + expand block — this is the byte-level template for Done. -->
<!-- Verbatim shape of the existing Syncing sub-button — this is the byte-level template for Pending. -->
<!-- Source: src/angular/src/app/pages/files/transfer-table.component.html lines 38-47 and 67-95. -->

Existing Errors parent + expand block (lines 67-95):
```html
<button type="button" class="btn-segment"
        [class.btn-segment--parent-active]="activeSegment === 'errors' && activeSubStatus === null"
        [class.btn-segment--parent-expanded]="activeSegment === 'errors' && activeSubStatus !== null"
        (click)="onSegmentChange('errors')">
  Errors
  <i class="ph-bold"
     [class.ph-caret-down]="activeSegment !== 'errors'"
     [class.ph-caret-up]="activeSegment === 'errors'"></i>
</button>

@if (activeSegment === 'errors') {
  <div class="segment-divider"></div>
  <button type="button" class="btn-sub"
          [class.active]="activeSubStatus === ViewFileStatus.STOPPED"
          (click)="onSubStatusChange(ViewFileStatus.STOPPED)">
    @if (activeSubStatus === ViewFileStatus.STOPPED) {
      <span class="accent-dot"></span>
    }
    Failed
  </button>
  <button type="button" class="btn-sub"
          [class.active]="activeSubStatus === ViewFileStatus.DELETED"
          (click)="onSubStatusChange(ViewFileStatus.DELETED)">
    @if (activeSubStatus === ViewFileStatus.DELETED) {
      <span class="accent-dot"></span>
    }
    Deleted
  </button>
}
```

Existing Syncing sub-button (lines 40-47):
```html
<button type="button" class="btn-sub"
        [class.active]="activeSubStatus === ViewFileStatus.DOWNLOADING"
        (click)="onSubStatusChange(ViewFileStatus.DOWNLOADING)">
  @if (activeSubStatus === ViewFileStatus.DOWNLOADING) {
    <span class="accent-dot"></span>
  }
  Syncing
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert Done parent button + Done expand block between Active and Errors</name>
  <files>src/angular/src/app/pages/files/transfer-table.component.html</files>
  <read_first>
    - src/angular/src/app/pages/files/transfer-table.component.html (FULL FILE — see lines 38-95 in particular)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-CONTEXT.md (D-03, D-04, D-15)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-PATTERNS.md (Pattern 1 under template — Errors block is the byte-level template)
    - docs/superpowers/specs/2026-04-15-drilldown-segment-filters-design.md (drill-down state machine + class table)
    - /Users/julianamacbook/.claude/projects/-Users-julianamacbook-seedsyncarr/memory/feedback_design_spec_rigor.md (port HTML identically)
  </read_first>
  <behavior>
    - The new Done parent button renders with the same DOM shape, classes, and chevron behavior as the existing Errors parent button — only `'errors'` → `'done'` and the label text `Errors` → `Done` differ
    - The new Done expand block renders only when `activeSegment === 'done'` and contains exactly two sub-buttons: "Downloaded" (bound to `ViewFileStatus.DOWNLOADED`) and "Extracted" (bound to `ViewFileStatus.EXTRACTED`)
    - The new block is inserted between the existing Active expand block (ends at line 64 with the closing `}` of `@if (activeSegment === 'active')`) and the existing Errors parent button (starts at line 67) — making the visual order in the rendered DOM: All, Active(+expand), Done(+expand), Errors(+expand)
    - No new SCSS classes are introduced; no inline styles; no class-name modifications anywhere in the file
  </behavior>
  <action>
    In `src/angular/src/app/pages/files/transfer-table.component.html`, insert the following block IMMEDIATELY AFTER the closing brace `}` of the existing `@if (activeSegment === 'active')` block (currently at line 64) AND BEFORE the comment `<!-- Errors: expandable parent -->` (currently at line 66). Insert verbatim:

    ```html
            <!-- Done: expandable parent -->
            <button type="button" class="btn-segment"
                    [class.btn-segment--parent-active]="activeSegment === 'done' && activeSubStatus === null"
                    [class.btn-segment--parent-expanded]="activeSegment === 'done' && activeSubStatus !== null"
                    (click)="onSegmentChange('done')">
              Done
              <i class="ph-bold"
                 [class.ph-caret-down]="activeSegment !== 'done'"
                 [class.ph-caret-up]="activeSegment === 'done'"></i>
            </button>

            @if (activeSegment === 'done') {
              <div class="segment-divider"></div>
              <button type="button" class="btn-sub"
                      [class.active]="activeSubStatus === ViewFileStatus.DOWNLOADED"
                      (click)="onSubStatusChange(ViewFileStatus.DOWNLOADED)">
                @if (activeSubStatus === ViewFileStatus.DOWNLOADED) {
                  <span class="accent-dot"></span>
                }
                Downloaded
              </button>
              <button type="button" class="btn-sub"
                      [class.active]="activeSubStatus === ViewFileStatus.EXTRACTED"
                      (click)="onSubStatusChange(ViewFileStatus.EXTRACTED)">
                @if (activeSubStatus === ViewFileStatus.EXTRACTED) {
                  <span class="accent-dot"></span>
                }
                Extracted
              </button>
            }

    ```

    **Substitutions vs the Errors template (the only differences):**
    - Segment string `'errors'` → `'done'` (5 occurrences in this block)
    - Parent label `Errors` → `Done`
    - First sub: `ViewFileStatus.STOPPED` → `ViewFileStatus.DOWNLOADED`, label `Failed` → `Downloaded`
    - Second sub: `ViewFileStatus.DELETED` → `ViewFileStatus.EXTRACTED`, label `Deleted` → `Extracted`
    - HTML comment text `<!-- Errors: expandable parent -->` → `<!-- Done: expandable parent -->`

    **Decision references:** D-03 (labels: Downloaded, Extracted), D-04 (Done as new parent, sibling of Active/Errors, between them visually per D-04 layout), D-15 (reuse `.btn-segment`, `.btn-segment--parent-active`, `.btn-segment--parent-expanded`, `.btn-sub`, `.btn-sub.active`, `.accent-dot`, `.segment-divider` verbatim).

    **Indentation:** Match the existing 8-space indent of the surrounding `<button class="btn-segment">` siblings (the Active and Errors parents in this file). Do NOT reformat any unrelated lines.
  </action>
  <verify>
    <automated>cd src/angular && npx ng build --configuration=development 2>&1 | grep -iE "(error|transfer-table.component.html)" | head -20; echo "build exit code below"; cd src/angular && npx ng build --configuration=development > /dev/null 2>&1; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onSegmentChange('done')" src/angular/src/app/pages/files/transfer-table.component.html` exits 0
    - `grep -c "activeSegment === 'done'" src/angular/src/app/pages/files/transfer-table.component.html` returns at least `4` (parent-active, parent-expanded, chevron-down negation, chevron-up positive, expand `@if`)
    - `grep -q "ViewFileStatus.DOWNLOADED" src/angular/src/app/pages/files/transfer-table.component.html` exits 0
    - `grep -q "ViewFileStatus.EXTRACTED" src/angular/src/app/pages/files/transfer-table.component.html` exits 0
    - The literal text `>Done` appears exactly once in the file (the parent label) — verify with `grep -c '>$' /dev/null; grep -c "^[[:space:]]*Done$" src/angular/src/app/pages/files/transfer-table.component.html` returns `1`
    - The literal text `Downloaded` appears exactly once in the file — verify with `grep -c "^[[:space:]]*Downloaded$" src/angular/src/app/pages/files/transfer-table.component.html` returns `1`
    - The literal text `Extracted` appears exactly once in the file — verify with `grep -c "^[[:space:]]*Extracted$" src/angular/src/app/pages/files/transfer-table.component.html` returns `1`
    - DOM order verified by line number: `grep -n "<!-- Active:\|<!-- Done:\|<!-- Errors:" src/angular/src/app/pages/files/transfer-table.component.html` returns three matches in this exact order: Active, Done, Errors. (Note: Active currently has no comment marker; if absent, instead verify via `grep -nE "onSegmentChange\\('(active|done|errors)'\\)" src/angular/src/app/pages/files/transfer-table.component.html` returns three matches in this order: active, done, errors)
    - No new SCSS class names. Verify with: `grep -oE 'class="[^"]+"' src/angular/src/app/pages/files/transfer-table.component.html | sort -u | grep -vE "^class=\"(btn-segment|btn-segment--parent-active|btn-segment--parent-expanded|btn-sub|btn-sub active|segment-divider|segment-filters|accent-dot|ph-bold|ph-caret-down|ph-caret-up|transfer-card|card-header|card-title-group|card-title|card-subtitle|card-controls|search-box|search-icon|search-input|fa fa-search|transfer-table-wrap|transfer-table|col-checkbox|col-name|col-status|col-progress|col-speed|col-eta|col-size|ss-checkbox|empty-row|text-center|text-muted|py-4|pagination-footer|page-info|page-controls|btn-page)\"$"` returns 0 lines (i.e., no unknown classes were introduced)
    - `cd src/angular && npx ng build --configuration=development` exits 0
  </acceptance_criteria>
  <done>
    Done parent button + expand block inserted between Active and Errors; classes reused verbatim; build passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert Pending sub-button as first sub under Active expand block</name>
  <files>src/angular/src/app/pages/files/transfer-table.component.html</files>
  <read_first>
    - src/angular/src/app/pages/files/transfer-table.component.html (lines 38-64, the existing Active expand block)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-CONTEXT.md (D-03, D-05; "Claude's Discretion" — first/left-most)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-PATTERNS.md (Pattern 2 under template — Syncing sub-button is the byte-level template)
    - /Users/julianamacbook/.claude/projects/-Users-julianamacbook-seedsyncarr/memory/feedback_design_spec_rigor.md
  </read_first>
  <behavior>
    - A new Pending sub-button is inserted as the FIRST sub-button inside the existing `@if (activeSegment === 'active')` block, immediately after the `<div class="segment-divider"></div>` and BEFORE the existing Syncing button
    - The new button uses byte-identical attribute set to the existing Syncing sub — only `ViewFileStatus.DOWNLOADING` → `ViewFileStatus.DEFAULT` and label text `Syncing` → `Pending` differ
    - Visual sub-button order under Active becomes: Pending, Syncing, Queued, Extracting (left-to-right workflow order)
  </behavior>
  <action>
    In `src/angular/src/app/pages/files/transfer-table.component.html`, locate the existing Active expand block at lines 38-64. Insert the new Pending sub-button IMMEDIATELY AFTER the line `<div class="segment-divider"></div>` (currently line 39) AND BEFORE the existing Syncing button (currently starts at line 40). Insert verbatim:

    ```html
              <button type="button" class="btn-sub"
                      [class.active]="activeSubStatus === ViewFileStatus.DEFAULT"
                      (click)="onSubStatusChange(ViewFileStatus.DEFAULT)">
                @if (activeSubStatus === ViewFileStatus.DEFAULT) {
                  <span class="accent-dot"></span>
                }
                Pending
              </button>
    ```

    **Substitutions vs the Syncing template (the only differences):**
    - `ViewFileStatus.DOWNLOADING` → `ViewFileStatus.DEFAULT` (3 occurrences in this block: `[class.active]`, `(click)`, inner `@if`)
    - Label text `Syncing` → `Pending`

    **Indentation:** Match the existing 10-space indent of the sibling `<button class="btn-sub">` (Syncing) inside the Active `@if` block. Do NOT reformat any unrelated lines.

    **Decision references:** D-03 (label: Pending), D-05 (Pending lives under Active), CONTEXT "Claude's Discretion" — Pending placed FIRST (left-most) per the recommended workflow order Pending → Syncing → Queued → Extracting.

    **Per `feedback_design_spec_rigor.md`:** Do NOT add new SCSS classes, do NOT change the markup shape, do NOT introduce per-instance style attributes. The new Pending sub MUST be byte-identical in attribute set to the existing sub-buttons (only label + enum value change).
  </action>
  <verify>
    <automated>cd src/angular && npx ng build --configuration=development > /dev/null 2>&1; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onSubStatusChange(ViewFileStatus.DEFAULT)" src/angular/src/app/pages/files/transfer-table.component.html` exits 0
    - `grep -c "ViewFileStatus.DEFAULT" src/angular/src/app/pages/files/transfer-table.component.html` returns `3` (one for `[class.active]`, one for `(click)`, one for the inner `@if`)
    - The literal text `Pending` appears exactly once in the file (the new sub-button label) — verify with `grep -c "^[[:space:]]*Pending$" src/angular/src/app/pages/files/transfer-table.component.html` returns `1`
    - **Pending precedes Syncing in the file** — verify with: `grep -nE "(>Pending<|>Syncing<|^[[:space:]]*Pending$|^[[:space:]]*Syncing$)" src/angular/src/app/pages/files/transfer-table.component.html | head -2` shows Pending FIRST then Syncing (lower line number for Pending)
    - The new button uses class `btn-sub` (no overrides). Verify with: `awk '/onSubStatusChange\(ViewFileStatus.DEFAULT\)/{found=1} found && /<button/{print; exit}' src/angular/src/app/pages/files/transfer-table.component.html | grep -q 'class="btn-sub"'` exits 0 — actually, since we're checking lines BEFORE the click handler, use: `grep -B 2 "onSubStatusChange(ViewFileStatus.DEFAULT)" src/angular/src/app/pages/files/transfer-table.component.html | grep -q 'class="btn-sub"'` exits 0
    - `cd src/angular && npx ng build --configuration=development` exits 0
  </acceptance_criteria>
  <done>
    Pending sub-button inserted as first sub under Active; class reused verbatim; build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Template-only HTML changes that mirror existing trusted patterns. All new attributes are static class bindings, static event handlers, and static text labels. No new attack surface — extends existing template patterns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-02-01 | XSS | new sub-button labels (Pending, Done, Downloaded, Extracted) | accept | Labels are static string literals in the template. No interpolation, no `[innerHTML]`, no user-supplied content rendered into the new buttons. Angular's default contextual escaping is not even exercised here. |
| T-73-02-02 | Tampering | (click) handlers on new buttons | accept | Handlers call `onSegmentChange('done')` and `onSubStatusChange(ViewFileStatus.DEFAULT/DOWNLOADED/EXTRACTED)` with literal arguments — no user-supplied values reach the handler from this template. URL-supplied values (Plan 03) are validated separately in that plan. |

**No new attack surface in this plan** — extends existing template patterns. ASVS L1: not applicable.
</threat_model>

<verification>
- `cd src/angular && npx ng build --configuration=development` exits 0
- `grep -q "onSegmentChange('done')" src/angular/src/app/pages/files/transfer-table.component.html` exits 0
- `grep -q "onSubStatusChange(ViewFileStatus.DEFAULT)" src/angular/src/app/pages/files/transfer-table.component.html` exits 0
- All new buttons use only the existing `.btn-segment` / `.btn-sub` / `.accent-dot` / `.segment-divider` / `.btn-segment--parent-active` / `.btn-segment--parent-expanded` classes (verified by acceptance criteria in Task 1)
- Visual button order in the DOM is All → Active(+Pending,Syncing,Queued,Extracting) → Done(+Downloaded,Extracted) → Errors(+Failed,Deleted)
</verification>

<success_criteria>
- 4 new buttons rendered in the segment-filter group (1 Done parent, 2 Done subs, 1 Pending sub) using only existing CSS classes
- Done parent expands/collapses via the same chevron + class machinery as Errors
- Pending appears as the FIRST sub-button under Active (left-most), consistent with workflow ordering
- Mobile responsive behavior inherited (no new media queries added — `.segment-filters` already hides below 768px per D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/73-dashboard-filter-for-every-torrent-status/73-02-SUMMARY.md` documenting:
- Exact line ranges where new markup was inserted
- Confirmation that no new SCSS classes were introduced
- Visual order of buttons in the rendered DOM
- Visual screenshot reference (manual verification by user can confirm but not required for this plan)
</output>
