---
phase: 73-dashboard-filter-for-every-torrent-status
plan: 05
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/e2e/tests/dashboard.page.ts
  - src/e2e/tests/dashboard.page.spec.ts
autonomous: true
requirements: [D-01, D-03, D-04, D-09, D-12]
tags: [playwright, e2e, page-object, transfer-table, segment-filter]

must_haves:
  truths:
    - "DashboardPage page-object exposes a getSegmentButton(name) locator scoped to .segment-filters that resolves Active, Done, Errors, and All by accessible name"
    - "DashboardPage page-object exposes a getSubButton(name) locator scoped to .segment-filters .btn-sub that resolves Pending, Syncing, Queued, Extracting, Downloaded, Extracted, Failed, Deleted by visible label"
    - "An e2e test asserts: clicking Done parent reveals the Downloaded and Extracted sub-buttons in the DOM"
    - "An e2e test asserts: clicking Active parent reveals the Pending sub-button"
    - "An e2e test asserts: clicking the Done parent updates the URL to contain ?segment=done (D-09 round-trip)"
    - "Existing Phase 72 tests (selection-clearing, action-bar visibility on select) continue to pass under the new filter values — no regression introduced"
  artifacts:
    - path: "src/e2e/tests/dashboard.page.ts"
      provides: "getSegmentButton and getSubButton locator methods"
      contains: "getSegmentButton AND getSubButton AND segment-filters"
    - path: "src/e2e/tests/dashboard.page.spec.ts"
      provides: "3 new e2e tests covering Done expand, Pending reveal, URL persistence"
      contains: "Done AND Downloaded AND Pending AND segment=done"
  key_links:
    - from: "DashboardPage.getSegmentButton"
      to: "rendered segment parent buttons"
      via: "page.locator('.segment-filters').getByRole('button', { name, exact: true })"
      pattern: "\\.segment-filters.*getByRole"
    - from: "URL persistence test"
      to: "Plan 03's _writeFilterToUrl helper"
      via: "expect(page).toHaveURL(/segment=done/)"
      pattern: "toHaveURL.*segment=done"
---

<objective>
Add page-object selectors for the new Done parent + 4 new sub-buttons (Pending, Downloaded, Extracted, plus a generic accessor that also covers existing Syncing/Queued/Extracting/Failed/Deleted) to `dashboard.page.ts`. Then add three minimal Playwright tests in `dashboard.page.spec.ts` that exercise (a) Done expand revealing Downloaded + Extracted, (b) Active expand revealing Pending, and (c) URL round-trip persistence on Done click. Verify (via existing tests) that Phase 72's selection-clear-on-filter-change behavior still works under the new filter values — no regression introduced for D-12 carry-forward.

Purpose: D-01 (3 new statuses are reachable end-to-end via real browser interaction), D-03 (button labels render correctly), D-04 (Done parent + 2 subs render in the right place), D-09 (URL persistence works in a real browser), D-12 (Phase 72 selection-clear assertion still passes when the new Done segment is selected).

Output: Page-object enriched with two locator methods; spec file with 3 new tests; existing 5 dashboard e2e tests continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/73-dashboard-filter-for-every-torrent-status/73-CONTEXT.md
@.planning/phases/73-dashboard-filter-for-every-torrent-status/73-PATTERNS.md
@.planning/phases/73-dashboard-filter-for-every-torrent-status/73-02-PLAN.md
@.planning/phases/72-restore-dashboard-file-selection-and-action-bar/72-CONTEXT.md

<interfaces>
<!-- Verbatim shape of the existing dashboard.page.ts locator methods. -->
<!-- Source: src/e2e/tests/dashboard.page.ts lines 53-86. -->

Existing locator pattern (lines 64-70):
```typescript
getActionBar(): Locator {
    return this.page.locator('app-bulk-actions-bar .bulk-actions-bar');
}

getActionButton(name: 'Queue' | 'Stop' | 'Extract' | 'Delete Local' | 'Delete Remote'): Locator {
    return this.page.locator('app-bulk-actions-bar').getByRole('button', { name, exact: true });
}
```

Production template after Plan 02 — segment buttons are inside `<div class="segment-filters">` (transfer-table.component.html line 20). Each parent uses class `btn-segment`; each sub uses class `btn-sub`. Sub-buttons contain a `<span class="accent-dot">` (when active) plus a text label — `getByText` with `exact: true` will match the visible label cleanly.

Constraint from CONTEXT D-12 (carry-forward from Phase 72 D-04): filter changes clear file selection. The existing dashboard tests at lines 39-75 (`should show and hide action buttons on select`, `should show action buttons for most recent file selected`, `should have all the action buttons`, etc.) implicitly depend on this behaviour and MUST continue to pass.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add getSegmentButton and getSubButton locator methods to DashboardPage</name>
  <files>src/e2e/tests/dashboard.page.ts</files>
  <read_first>
    - src/e2e/tests/dashboard.page.ts (FULL FILE — note existing locator methods at lines 53-86, especially `getActionButton` shape at line 68)
    - src/angular/src/app/pages/files/transfer-table.component.html (after Plan 02 — note `.segment-filters` container at line 20 and the new buttons)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-PATTERNS.md (Pattern under dashboard.page.ts — getSegmentButton/getSubButton shape)
  </read_first>
  <behavior>
    - `dashboardPage.getSegmentButton('Done').click()` resolves to the Done parent button inside `.segment-filters` and clicks it
    - `dashboardPage.getSegmentButton('All' | 'Active' | 'Done' | 'Errors')` returns a Locator that uniquely matches the corresponding parent button by accessible name (the button's text content)
    - `dashboardPage.getSubButton('Pending' | 'Syncing' | 'Queued' | 'Extracting' | 'Downloaded' | 'Extracted' | 'Failed' | 'Deleted')` returns a Locator that uniquely matches the corresponding `.btn-sub` button by visible label
    - The sub-button locator scopes to `.segment-filters button.btn-sub` to avoid matching any other button on the page that happens to contain the same text (e.g., a row label that says "Downloaded")
    - The new methods do not change any existing method signatures
  </behavior>
  <action>
    In `src/e2e/tests/dashboard.page.ts`, insert the following two methods AFTER the existing `getActionButton` method (currently lines 68-70) and BEFORE the `selectFileByName` method (currently line 72). Insert verbatim:

    ```typescript

        getSegmentButton(name: 'All' | 'Active' | 'Done' | 'Errors'): Locator {
            return this.page.locator('.segment-filters').getByRole('button', { name, exact: true });
        }

        getSubButton(name: 'Pending' | 'Syncing' | 'Queued' | 'Extracting' | 'Downloaded' | 'Extracted' | 'Failed' | 'Deleted'): Locator {
            return this.page.locator('.segment-filters button.btn-sub').getByText(name, { exact: true });
        }
    ```

    **Note (per PATTERNS.md):** the sub-button locator uses `getByText` (not `getByRole({name})`) to select on the visible label. Sub-buttons contain a `<span class="accent-dot">` plus the label text node; `getByText` with `exact: true` selects on the visible label and cleanly avoids the accent-dot span.

    **Decision references:** D-01 (selectors needed for all 8 statuses end-to-end), D-03 (label text selectors), D-04 (Done as a new parent — 4 segment names total).

    **DO NOT** modify any other method in this file. The existing methods (`getRowCheckbox`, `getHeaderCheckbox`, `getActionBar`, `getActionButton`, `selectFileByName`, `clearSelectionViaBar`, `_escapeRegex`) are unchanged.
  </action>
  <verify>
    <automated>cd src/e2e && npx tsc --noEmit 2>&1 | tail -10; echo "tsc exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'getSegmentButton(name: ' src/e2e/tests/dashboard.page.ts` exits 0
    - `grep -q 'getSubButton(name: ' src/e2e/tests/dashboard.page.ts` exits 0
    - `grep -q "'All' | 'Active' | 'Done' | 'Errors'" src/e2e/tests/dashboard.page.ts` exits 0
    - `grep -q "'Pending' | 'Syncing' | 'Queued' | 'Extracting' | 'Downloaded' | 'Extracted' | 'Failed' | 'Deleted'" src/e2e/tests/dashboard.page.ts` exits 0
    - `grep -q "this.page.locator('.segment-filters')" src/e2e/tests/dashboard.page.ts` exits 0
    - `grep -q "this.page.locator('.segment-filters button.btn-sub')" src/e2e/tests/dashboard.page.ts` exits 0
    - The existing methods `getActionBar`, `getActionButton`, `selectFileByName` remain present. Verify with: `grep -c 'getActionBar\\|getActionButton\\|selectFileByName' src/e2e/tests/dashboard.page.ts` returns at least `3`
    - `cd src/e2e && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Two new locator methods added; existing methods unchanged; tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 3 e2e tests covering Done expand, Pending reveal, and URL round-trip</name>
  <files>src/e2e/tests/dashboard.page.spec.ts</files>
  <read_first>
    - src/e2e/tests/dashboard.page.spec.ts (FULL FILE — note existing test structure, beforeEach at lines 9-12, and existing assertions at lines 39-75)
    - src/e2e/tests/dashboard.page.ts (after Task 1 — note new getSegmentButton + getSubButton methods)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-CONTEXT.md (D-01, D-03, D-04, D-09, D-12)
    - .planning/phases/73-dashboard-filter-for-every-torrent-status/73-PATTERNS.md (Pattern under dashboard.page.spec.ts — keep tests minimal + parity-focused with Phase 72)
  </read_first>
  <behavior>
    - Test 1 (Done expand): asserts Downloaded + Extracted sub-buttons are NOT visible initially, become visible after clicking Done parent
    - Test 2 (Pending reveal): asserts Pending sub-button is NOT visible initially, becomes visible after clicking Active parent
    - Test 3 (URL round-trip): clicks Done parent and asserts the page URL contains `?segment=done` (or `&segment=done` if other params are present) — proves Plan 03's URL write-back lands in the real browser
    - All 3 new tests run inside the existing `test.describe('Testing dashboard page', ...)` block at line 6, sharing the existing `beforeEach` page-object setup
    - All 5 existing tests at lines 14-75 continue to pass — D-12 carry-forward is implicitly verified because the existing selection-clearing tests do not depend on segment values
  </behavior>
  <action>
    In `src/e2e/tests/dashboard.page.spec.ts`, append the following three tests at the END of the existing `test.describe('Testing dashboard page', () => { ... })` block — i.e., immediately before the closing `});` at line 76. Insert verbatim:

    ```typescript

        test('should expand Done segment to reveal Downloaded and Extracted subs', async () => {
            await expect(dashboardPage.getSubButton('Downloaded')).not.toBeVisible();
            await expect(dashboardPage.getSubButton('Extracted')).not.toBeVisible();

            await dashboardPage.getSegmentButton('Done').click();

            await expect(dashboardPage.getSubButton('Downloaded')).toBeVisible();
            await expect(dashboardPage.getSubButton('Extracted')).toBeVisible();
        });

        test('should reveal Pending sub under Active', async () => {
            await expect(dashboardPage.getSubButton('Pending')).not.toBeVisible();

            await dashboardPage.getSegmentButton('Active').click();

            await expect(dashboardPage.getSubButton('Pending')).toBeVisible();
        });

        test('should persist Done filter via URL query param (D-09)', async ({ page }) => {
            await dashboardPage.getSegmentButton('Done').click();

            await expect(page).toHaveURL(/[?&]segment=done(&|$)/);
        });
    ```

    **Decision references:**
    - Test 1 covers D-01, D-03 (Downloaded + Extracted labels), D-04 (Done parent renders + expands)
    - Test 2 covers D-01, D-03, D-05 (Pending lives under Active)
    - Test 3 covers D-09, D-10 (URL persistence round-trips through the real browser via Angular Router)

    **D-12 carry-forward verification (no new test needed):** the existing test `should show and hide action buttons on select` at line 39 implicitly covers D-12 because (a) it selects a file via `selectFileByName(TEST_FILE)` (no filter change), (b) it does NOT trigger any segment change, (c) the action bar visibility logic is independent of segment value. If a regression in Phase 73 caused selection to stop clearing on segment change, this would surface as a failure in `should show action buttons for most recent file selected` at line 49 only if the test interleaved a segment click — which it does not. Therefore D-12 is verified by the unit-test assertion in Plan 04 Task 2 ("should clear file selection when 'done' segment selected"), and no e2e test is added in this plan to avoid bloating the e2e suite (per PATTERNS.md "keep it minimal and parity-focused").

    **Per CONTEXT (Discretion section):** the Done segment URL test uses the literal enum-string `done` for the segment value, matching Plan 03's `_writeFilterToUrl` helper output.

    **DO NOT** add tests for back/forward browser navigation (CONTEXT line 133 explicitly defers this to a follow-up phase) or for the `replaceUrl: true` behaviour (also a follow-up concern).
  </action>
  <verify>
    <automated>cd src/e2e && npx playwright test dashboard.page.spec --grep "Done|Pending|persist Done filter" 2>&1 | tail -30; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "should expand Done segment to reveal Downloaded and Extracted subs" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `grep -q "should reveal Pending sub under Active" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `grep -q "should persist Done filter via URL query param" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `grep -q "dashboardPage.getSegmentButton('Done')" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `grep -q "dashboardPage.getSubButton('Pending')" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `grep -q "dashboardPage.getSubButton('Downloaded')" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `grep -q "dashboardPage.getSubButton('Extracted')" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - URL persistence test uses a regex match. Verify with: `grep -q "toHaveURL(/" src/e2e/tests/dashboard.page.spec.ts` exits 0
    - `cd src/e2e && npx playwright test dashboard.page.spec --grep "Done|Pending|persist Done filter"` exits 0 (3 tests pass)
    - Existing tests still pass. Verify with: `cd src/e2e && npx playwright test dashboard.page.spec` exits 0 (full suite)
  </acceptance_criteria>
  <done>
    3 new e2e tests added; all pass; full dashboard e2e suite still green; D-12 implicitly preserved (no segment-interleaved selection test required since unit test in Plan 04 covers it).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | E2E tests + page-object additions exercise existing UI surfaces. The URL round-trip test exercises Plan 03's trust boundary (URL→component) end-to-end but does not introduce new boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05-01 | (e2e-only) | Playwright locators | accept | Locators are scoped to `.segment-filters` and `.btn-sub` to prevent over-matching. No production code paths are added or modified. |
| T-73-05-02 | Tampering | URL round-trip test | accept | Test asserts the URL CONTAINS `?segment=done` (or `&segment=done`) via regex — the test itself does not introduce a new attack surface. The validated round-trip surface is mitigated by Plan 03's hydration validation (T-73-03-01 / T-73-03-02). |

**No new attack surface in this plan** — extends e2e scaffolding only. ASVS L1: not applicable.
</threat_model>

<verification>
- `cd src/e2e && npx tsc --noEmit` exits 0
- `cd src/e2e && npx playwright test dashboard.page.spec` exits 0 (full dashboard suite — 8 tests after this plan: 5 existing + 3 new)
- The URL round-trip test confirms Plan 03 + Plan 02 work together end-to-end in a real browser
- Existing Phase 72 tests at lines 39-75 continue to pass — D-12 selection-clearing carry-forward is preserved (further locked by Plan 04 unit test "should clear file selection when 'done' segment selected")
</verification>

<success_criteria>
- 2 new locator methods on DashboardPage (`getSegmentButton`, `getSubButton`) with exhaustive name unions for all parents and subs
- 3 new e2e tests proving Done expand, Pending reveal, and URL round-trip
- Full dashboard e2e suite remains green (5 existing + 3 new = 8 tests)
- No regression to Phase 72 selection-clearing behaviour
</success_criteria>

<output>
After completion, create `.planning/phases/73-dashboard-filter-for-every-torrent-status/73-05-SUMMARY.md` documenting:
- Locator method signatures added
- Test count delta in `dashboard.page.spec.ts`
- Confirmation that the full dashboard e2e suite passes (8 tests total)
- Confirmation that D-12 carry-forward is verified by Plan 04's unit test (no new e2e test added per PATTERNS.md "keep it minimal and parity-focused")
- Note that browser back/forward filter navigation is explicitly NOT tested here (CONTEXT line 133 — deferred follow-up)
</output>
