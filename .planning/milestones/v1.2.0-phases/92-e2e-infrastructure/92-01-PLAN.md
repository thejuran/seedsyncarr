---
phase: 92-e2e-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/docker/test/e2e/run_tests.sh
  - src/docker/test/e2e/compose.yml
autonomous: true
requirements:
  - E2EINFRA-01
  - E2EINFRA-02
  - E2EINFRA-03

must_haves:
  truths:
    - "SERVER_UP and SCAN_DONE variables are always defined before their post-loop checks in run_tests.sh"
    - "Docker Compose configure service waits for myapp to be application-healthy before running setup scripts"
    - "setup_seedsyncarr.sh uses wait-for-down-then-up pattern after restart (already implemented)"
  artifacts:
    - path: "src/docker/test/e2e/run_tests.sh"
      provides: "Initialized polling variables before loops"
      contains: "SERVER_UP=\"\""
    - path: "src/docker/test/e2e/compose.yml"
      provides: "myapp healthcheck and service_healthy dependency"
      contains: "service_healthy"
  key_links:
    - from: "src/docker/test/e2e/compose.yml"
      to: "src/docker/stage/docker-image/compose.yml"
      via: "Docker Compose V2 deep-merge of myapp service definition across -f files"
      pattern: "myapp:"
    - from: "src/docker/test/e2e/compose.yml (configure depends_on)"
      to: "src/docker/test/e2e/compose.yml (myapp healthcheck)"
      via: "service_healthy condition blocks configure until myapp healthcheck passes"
      pattern: "condition: service_healthy"
---

<objective>
Fix E2E Docker infrastructure shell script and compose orchestration bugs that cause unreliable test startup.

Purpose: Eliminate uninitialized variable errors in run_tests.sh and race conditions where the configure container starts before myapp is application-ready.
Output: Fixed run_tests.sh with initialized variables; compose.yml with myapp healthcheck and service_healthy dependency condition; verification that E2EINFRA-03 is already done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-e2e-infrastructure/92-RESEARCH.md
@.planning/phases/92-e2e-infrastructure/92-PATTERNS.md

<interfaces>
<!-- Key files and their current structure that the executor needs to understand -->

From src/docker/stage/docker-image/compose.yml (myapp base definition -- merged with e2e compose):
```yaml
services:
  myapp:
    image: ${STAGING_REGISTRY}:${STAGING_VERSION}
    container_name: seedsyncarr_test_e2e_myapp
```

From src/docker/test/e2e/compose.yml (existing remote healthcheck analog -- lines 25-30):
```yaml
  remote:
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/1234'"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
```

From src/docker/test/e2e/configure/setup_seedsyncarr.sh (init-before-loop analog -- lines 35, 49):
```bash
WENT_DOWN=0
while [[ ${SECONDS} -lt ${END} ]]; do
  ...
done

CAME_UP=0
END_UP=$((SECONDS+60))
while [[ ${SECONDS} -lt ${END_UP} ]]; do
  ...
done
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize SERVER_UP and SCAN_DONE variables before polling loops in run_tests.sh</name>
  <files>src/docker/test/e2e/run_tests.sh</files>
  <read_first>
    - src/docker/test/e2e/run_tests.sh (current file -- see uninitialized vars at lines 11 and 33)
    - src/docker/test/e2e/configure/setup_seedsyncarr.sh (analog -- see WENT_DOWN=0 at line 35, CAME_UP=0 at line 49)
  </read_first>
  <action>
Add `SERVER_UP=""` initialization before line 8 (before the first `END=$((SECONDS+30))` line) and `SCAN_DONE=""` initialization before line 30 (before the second `END=$((SECONDS+60))` line). Per E2EINFRA-01.

The exact insertions:

1. Insert `SERVER_UP=""` on a new line immediately before `END=$((SECONDS+30))` (current line 8).
2. Insert `SCAN_DONE=""` on a new line immediately before `END=$((SECONDS+60))` (current line 30).

Do NOT modify the `sleep 2` at line 41 -- that is a normal poll interval, not the E2EINFRA-03 race condition.
Do NOT add `set -u` or `set -euo pipefail` -- the existing script does not use strict mode and adding it is out of scope.
Do NOT change any other lines.

Result after edit -- the SERVER_UP block should read:
```bash
# Wait for server to be up
SERVER_UP=""
END=$((SECONDS+30))
while [ ${SECONDS} -lt ${END} ];
do
  SERVER_UP=$(
      curl -s myapp:8800/server/status | \
        python3 ./parse_status.py server_up
  )
  ...
```

And the SCAN_DONE block should read:
```bash
# Wait for remote scan to complete (files need to be scanned before dashboard tests can run)
SCAN_DONE=""
END=$((SECONDS+60))
while [ ${SECONDS} -lt ${END} ];
do
  SCAN_DONE=$(
      curl -s myapp:8800/server/status | \
        python3 ./parse_status.py remote_scan_done
  )
  ...
```
  </action>
  <verify>
    <automated>grep -n 'SERVER_UP=""' src/docker/test/e2e/run_tests.sh && grep -n 'SCAN_DONE=""' src/docker/test/e2e/run_tests.sh && echo "PASS: both variables initialized"</automated>
  </verify>
  <acceptance_criteria>
    - `run_tests.sh` contains `SERVER_UP=""` on a line before `END=$((SECONDS+30))`
    - `run_tests.sh` contains `SCAN_DONE=""` on a line before `END=$((SECONDS+60))`
    - The `sleep 2` poll interval at original line 41 is unchanged
    - No other lines are modified
  </acceptance_criteria>
  <done>SERVER_UP and SCAN_DONE are initialized to empty string before their respective polling loops. If either loop body never executes, the post-loop check sees "" (not unset) and correctly reports failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add myapp healthcheck and change configure dependency to service_healthy in compose.yml</name>
  <files>src/docker/test/e2e/compose.yml</files>
  <read_first>
    - src/docker/test/e2e/compose.yml (current file -- see configure depends_on myapp at line 38-40 with service_started)
    - src/docker/stage/docker-image/compose.yml (myapp base definition -- image and container_name only, no healthcheck)
    - src/docker/test/e2e/compose.yml (same file -- remote service healthcheck at lines 25-30 as analog)
  </read_first>
  <action>
Two changes to `src/docker/test/e2e/compose.yml`. Per E2EINFRA-02.

**Change 1:** Add a `myapp:` service section with a healthcheck. Insert it between the `remote` service block (ends at line 30) and the `configure` service block (starts at line 32). The section should be:

```yaml
  myapp:
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8800/server/status"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
```

This `myapp:` key merges with the `myapp` definition from `src/docker/stage/docker-image/compose.yml` via Docker Compose V2 deep-merge. The healthcheck uses `curl -sf` which exits non-zero on HTTP errors or connection refused and exits 0 on HTTP 2xx. The generous `retries: 12` and `start_period: 10s` account for arm64/QEMU slow startup (2-4x slower than amd64).

**Change 2:** Change `condition: service_started` to `condition: service_healthy` in the configure service's `depends_on` block for `myapp` (current line 40).

Before:
```yaml
    depends_on:
      myapp:
        condition: service_started
```

After:
```yaml
    depends_on:
      myapp:
        condition: service_healthy
```

Do NOT modify the `remote` service healthcheck or the `tests` service configuration.

**Verification of E2EINFRA-03 (no code change):** After making the compose changes, verify that `src/docker/test/e2e/configure/setup_seedsyncarr.sh` already contains the wait-for-down-then-up pattern (lines 33-61: `WENT_DOWN=0` loop, then `CAME_UP=0` loop). This is already implemented -- no edit needed. Just confirm via a grep.
  </action>
  <verify>
    <automated>grep -A2 'myapp:' src/docker/test/e2e/compose.yml | grep -q 'healthcheck' && grep 'service_healthy' src/docker/test/e2e/compose.yml | grep -q 'myapp' && grep -c 'service_started' src/docker/test/e2e/compose.yml | grep -q '^0$' && grep 'WENT_DOWN=0' src/docker/test/e2e/configure/setup_seedsyncarr.sh && grep 'CAME_UP=0' src/docker/test/e2e/configure/setup_seedsyncarr.sh && echo "PASS: healthcheck added, condition changed, E2EINFRA-03 verified"</automated>
  </verify>
  <acceptance_criteria>
    - `compose.yml` contains a `myapp:` section with `healthcheck:` block including `test`, `interval`, `timeout`, `retries`, `start_period`
    - `compose.yml` healthcheck test command is `["CMD-SHELL", "curl -sf http://localhost:8800/server/status"]`
    - `compose.yml` healthcheck has `retries: 12` and `start_period: 10s`
    - `compose.yml` configure service depends_on myapp has `condition: service_healthy` (not `service_started`)
    - `compose.yml` does NOT contain `service_started` anywhere
    - `compose.yml` remote service healthcheck is unchanged (still uses `/dev/tcp/localhost/1234`)
    - `setup_seedsyncarr.sh` contains `WENT_DOWN=0` and `CAME_UP=0` (E2EINFRA-03 verification -- no edit)
  </acceptance_criteria>
  <done>Docker Compose configure service will now wait for myapp to report healthy (HTTP 200 from /server/status) before running setup scripts. The old service_started condition is replaced with service_healthy. E2EINFRA-03 confirmed already implemented in setup_seedsyncarr.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose.yml healthcheck | curl runs inside the myapp container against localhost -- no network trust boundary crossed |
| run_tests.sh variable init | Shell variable initialization -- no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | I (Information Disclosure) | compose.yml healthcheck curl | accept | Healthcheck runs inside container against localhost:8800 -- no external exposure. /server/status is a read-only status endpoint returning operational state only, no secrets. |
| T-92-02 | D (Denial of Service) | healthcheck retries | accept | 12 retries with 5s interval = 60s max wait. This is test infrastructure only, not production. Generous timeouts prevent false negatives on arm64/QEMU. |
</threat_model>

<verification>
1. `grep -n 'SERVER_UP=""' src/docker/test/e2e/run_tests.sh` returns a line number before the first `END=` line
2. `grep -n 'SCAN_DONE=""' src/docker/test/e2e/run_tests.sh` returns a line number before the second `END=` line
3. `grep 'service_healthy' src/docker/test/e2e/compose.yml` shows the myapp dependency condition
4. `grep -A5 'myapp:' src/docker/test/e2e/compose.yml` shows the healthcheck block
5. `grep 'service_started' src/docker/test/e2e/compose.yml` returns empty (no remaining service_started)
6. `grep 'WENT_DOWN=0' src/docker/test/e2e/configure/setup_seedsyncarr.sh` confirms E2EINFRA-03 is implemented
</verification>

<success_criteria>
- run_tests.sh has both SERVER_UP="" and SCAN_DONE="" initialized before their respective polling loops
- compose.yml has a myapp healthcheck block using curl -sf against /server/status
- compose.yml configure service uses condition: service_healthy for myapp dependency
- No service_started references remain in compose.yml for myapp
- setup_seedsyncarr.sh wait-for-down-then-up pattern verified present (E2EINFRA-03)
</success_criteria>

<output>
After completion, create `.planning/phases/92-e2e-infrastructure/92-01-SUMMARY.md`
</output>
