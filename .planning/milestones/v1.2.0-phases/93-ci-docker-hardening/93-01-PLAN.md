---
phase: 93-ci-docker-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - Makefile
autonomous: true
requirements:
  - CISEC-01
  - CISEC-02
  - CISEC-03
  - CISEC-04

must_haves:
  truths:
    - "Workflow-level permissions are contents: read only"
    - "Every job that writes has explicit per-job permissions"
    - "All GitHub Actions uses: lines reference full 40-char SHA hashes"
    - "publish-github-release needs chain includes publish-docker-image"
    - "Python test image build uses registry-based cache in CI"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Hardened CI workflow with least-privilege permissions and SHA-pinned actions"
      contains: "contents: read"
    - path: "Makefile"
      provides: "Conditional cache flags for python test image build"
      contains: "PYTHON_TEST_CACHE_REGISTRY"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Makefile"
      via: "make run-tests-python PYTHON_TEST_CACHE_REGISTRY="
      pattern: "PYTHON_TEST_CACHE_REGISTRY"
---

<objective>
Harden the GitHub Actions CI workflow: restrict workflow-level permissions to read-only, pin all unpinned actions to SHA hashes, fix the publish-github-release job ordering, and add registry-based Docker build cache for the Python test image.

Purpose: Prevent supply-chain attacks via mutable action tags (CISEC-02), limit blast radius of compromised steps via least-privilege permissions (CISEC-01), ensure Docker image exists before GitHub Release is created (CISEC-03), and speed up CI builds with layer caching (CISEC-04).

Output: Updated ci.yml with hardened permissions + SHA pins, updated Makefile with conditional cache support.
</objective>

<execution_context>
@.planning/phases/93-ci-docker-hardening/93-RESEARCH.md
@.planning/phases/93-ci-docker-hardening/93-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current CI workflow structure — executor needs to know all jobs and their current permissions -->

From .github/workflows/ci.yml:
- Workflow-level permissions (lines 15-17): contents: write, packages: write
- Jobs: unittests-python, unittests-angular, lint-python, lint-angular, build-docker-image, e2etests-docker-image, publish-docker-image, publish-github-release, publish-pypi, publish-docker-image-dev, publish-docs
- Unpinned actions (9x checkout@v4, 3x setup-python@v5, 1x setup-node@v4)
- Already-pinned: docker/setup-qemu-action, docker/setup-buildx-action, peaceiris/actions-gh-pages, pypa/gh-action-pypi-publish
- publish-github-release needs: [ e2etests-docker-image ] (missing publish-docker-image)

From Makefile:
- tests-python target (lines 79-85): docker build without cache flags
- docker-image target (lines 30-49): already uses --cache-from/--cache-to type=registry pattern
- GHCR login pattern exists in build-docker-image job (lines 99-104)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden CI workflow permissions, pin actions to SHA, fix job ordering</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/93-ci-docker-hardening/93-PATTERNS.md
  </read_first>
  <action>
Edit `.github/workflows/ci.yml` with the following changes:

**CISEC-01 -- Least-privilege permissions:**

1. Replace the workflow-level permissions block (lines 15-17) from:
```yaml
permissions:
  contents: write
  packages: write
```
to:
```yaml
permissions:
  contents: read
```

2. Add per-job `permissions:` blocks to ONLY the jobs that need write access:

- `unittests-python` -- add `permissions:` block with `packages: write` (needed for CISEC-04 cache push)
- `build-docker-image` -- add `permissions:` block with `packages: write` (pushes staging image to GHCR)
- `publish-docker-image` -- add `permissions:` block with `packages: write` (pushes release image to GHCR)
- `publish-docker-image-dev` -- add `permissions:` block with `packages: write` (pushes dev image to GHCR)
- `publish-github-release` -- add `permissions:` block with `contents: write` (creates GitHub Release via gh CLI)
- `publish-docs` -- add `permissions:` block with `contents: write` (peaceiris/actions-gh-pages pushes to gh-pages branch)
- `publish-pypi` -- already has `permissions: id-token: write` -- keep as-is

Jobs that need NO override (inherit `contents: read`): `unittests-angular`, `lint-python`, `lint-angular`, `e2etests-docker-image`.

**CISEC-02 -- Pin all actions to SHA hashes:**

Replace every unpinned `uses:` line:

- ALL 9 occurrences of `actions/checkout@v4` -> `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1`
  (lines 26, 37, 48, 65, 87, 124, 160, 201, 245, 280)
- ALL 3 occurrences of `actions/setup-python@v5` -> `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0`
  (lines 51, 224, 283)
- The 1 occurrence of `actions/setup-node@v4` -> `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0`
  (line 69)

Do NOT change already-pinned actions (docker/setup-qemu-action, docker/setup-buildx-action, peaceiris/actions-gh-pages, pypa/gh-action-pypi-publish).

**CISEC-03 -- Fix publish-github-release job ordering:**

Change publish-github-release `needs` from:
```yaml
needs: [ e2etests-docker-image ]
```
to:
```yaml
needs: [ e2etests-docker-image, publish-docker-image ]
```

**CISEC-04 -- Add GHCR login + cache env to unittests-python job:**

Add two steps to the `unittests-python` job BEFORE the "Run Python unit tests" step:

```yaml
      - name: Log into GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login https://ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Set staging registry env variable
        run: echo "staging_registry=ghcr.io/${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV
```

Then change the "Run Python unit tests" step from:
```yaml
      - name: Run Python unit tests
        run: make run-tests-python
```
to:
```yaml
      - name: Run Python unit tests
        run: make run-tests-python PYTHON_TEST_CACHE_REGISTRY=${{ env.staging_registry }}
```

**Critical sequencing constraint:** The workflow-level `contents: read` and ALL per-job `permissions:` overrides MUST be in the same commit. Otherwise jobs like publish-github-release and publish-docs will fail with 403 errors.
  </action>
  <verify>
    <automated>
# Verify workflow-level permissions is contents: read only (not write)
grep -A1 "^permissions:" .github/workflows/ci.yml | head -2 | grep "contents: read" && \
! grep -A1 "^permissions:" .github/workflows/ci.yml | head -2 | grep "write" && \
# Verify no unpinned actions remain (all uses: lines should have @[40-char-hex])
test -z "$(grep 'uses: actions/' .github/workflows/ci.yml | grep -v '@[a-f0-9]\{40\}')" && \
# Verify publish-github-release needs includes publish-docker-image
grep -A5 "publish-github-release:" .github/workflows/ci.yml | grep "publish-docker-image" && \
# Verify unittests-python has PYTHON_TEST_CACHE_REGISTRY
grep "PYTHON_TEST_CACHE_REGISTRY" .github/workflows/ci.yml && \
# Verify publish-docs has contents: write
grep -A10 "publish-docs:" .github/workflows/ci.yml | grep "contents: write" && \
# Verify YAML is valid
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && \
echo "PASS: All CI hardening checks passed"
    </automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml line 15-16 reads `permissions:` then `  contents: read` with no `packages: write` at workflow level
    - grep 'uses: actions/' .github/workflows/ci.yml | grep -v '@[a-f0-9]\{40\}' returns empty (all pinned)
    - grep -c 'actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5' .github/workflows/ci.yml returns 9 or 10
    - grep -c 'actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065' .github/workflows/ci.yml returns 3
    - grep -c 'actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020' .github/workflows/ci.yml returns 1
    - publish-github-release needs line contains both e2etests-docker-image and publish-docker-image
    - unittests-python job contains `packages: write` under its permissions block
    - unittests-python job contains `PYTHON_TEST_CACHE_REGISTRY=${{ env.staging_registry }}`
    - publish-docker-image job has `permissions:` with `packages: write`
    - publish-docker-image-dev job has `permissions:` with `packages: write`
    - publish-github-release job has `permissions:` with `contents: write`
    - publish-docs job has `permissions:` with `contents: write`
    - python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0
  </acceptance_criteria>
  <done>CI workflow has least-privilege permissions, all actions SHA-pinned, publish ordering fixed, and python test cache env configured</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional cache flags to Makefile tests-python target</name>
  <files>Makefile</files>
  <read_first>
    - Makefile
    - .planning/phases/93-ci-docker-hardening/93-PATTERNS.md
  </read_first>
  <action>
Edit the `tests-python` target in `Makefile` (lines 79-85) to support optional registry-based Docker build cache.

The current target is:
```makefile
tests-python:
	# python run
	$(DOCKER) build \
		-f ${SOURCEDIR}/docker/build/docker-image/Dockerfile \
		--target seedsyncarr_run_python_devenv \
		--tag seedsyncarr/run/python/devenv \
		${ROOTDIR}
```

Replace with a shell-conditional approach (matching the existing Makefile style where shell variables are used within recipes):
```makefile
tests-python:
	# python run
	@CACHE_FLAGS=""; \
	if [ -n "$(PYTHON_TEST_CACHE_REGISTRY)" ]; then \
		CACHE_FLAGS="--cache-from type=registry,ref=$(PYTHON_TEST_CACHE_REGISTRY):cache-python-test --cache-to type=registry,ref=$(PYTHON_TEST_CACHE_REGISTRY):cache-python-test,mode=max"; \
	fi; \
	$(DOCKER) build \
		-f ${SOURCEDIR}/docker/build/docker-image/Dockerfile \
		--target seedsyncarr_run_python_devenv \
		--tag seedsyncarr/run/python/devenv \
		$$CACHE_FLAGS \
		${ROOTDIR}
```

This uses the same pattern as the existing `docker-image` target (lines 30-49) which already passes `--cache-from` and `--cache-to` with `type=registry`. When `PYTHON_TEST_CACHE_REGISTRY` is empty (local dev), no cache flags are added and behavior is unchanged. When set in CI (via `make run-tests-python PYTHON_TEST_CACHE_REGISTRY=ghcr.io/thejuran/seedsyncarr`), the registry cache is used.

Note: The `$(DOCKER)` macro expands to `DOCKER_BUILDKIT=1 docker` which enables BuildKit, required for `--cache-from`/`--cache-to` type=registry support. The existing `docker-image` target already proves this works.
  </action>
  <verify>
    <automated>
# Verify PYTHON_TEST_CACHE_REGISTRY appears in Makefile
grep "PYTHON_TEST_CACHE_REGISTRY" Makefile && \
# Verify cache-from and cache-to appear in tests-python target area
grep -A15 "^tests-python:" Makefile | grep "cache-from" && \
grep -A15 "^tests-python:" Makefile | grep "cache-to" && \
# Verify the target still works locally (no cache flags when var is empty)
make tests-python 2>&1 | tail -5 && \
echo "PASS: Makefile cache flags verified"
    </automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains `PYTHON_TEST_CACHE_REGISTRY` in the tests-python target
    - grep -A15 "^tests-python:" Makefile | grep "cache-from type=registry" returns a match
    - grep -A15 "^tests-python:" Makefile | grep "cache-to type=registry" returns a match
    - grep -A15 "^tests-python:" Makefile | grep "cache-python-test" returns a match
    - make tests-python (without PYTHON_TEST_CACHE_REGISTRY set) succeeds (no cache flags added)
  </acceptance_criteria>
  <done>Makefile tests-python target conditionally passes registry cache flags when PYTHON_TEST_CACHE_REGISTRY is set</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> GHCR | Runner pushes images/cache to GHCR using GITHUB_TOKEN |
| GitHub Actions runner -> PyPI | Runner publishes packages using OIDC id-token |
| GitHub Actions runner -> gh-pages | Runner pushes docs to gh-pages branch |
| Third-party action -> runner env | Third-party actions execute in runner context with GITHUB_TOKEN access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | actions/checkout@v4 (mutable tag) | mitigate | Pin to SHA `34e114876b0b11c390a56381ad16ebd13914f8d5` (CISEC-02) |
| T-93-02 | Tampering | actions/setup-python@v5 (mutable tag) | mitigate | Pin to SHA `a26af69be951a213d495a4c3e4e4022e16d87065` (CISEC-02) |
| T-93-03 | Tampering | actions/setup-node@v4 (mutable tag) | mitigate | Pin to SHA `49933ea5288caeca8642d1e84afbd3f7d6820020` (CISEC-02) |
| T-93-04 | Elevation of Privilege | Over-privileged GITHUB_TOKEN | mitigate | Restrict workflow-level to `contents: read`, per-job write grants only (CISEC-01) |
| T-93-05 | Information Disclosure | Docker image tag race (release published before image exists) | mitigate | Add `publish-docker-image` to `publish-github-release` needs chain (CISEC-03) |
</threat_model>

<verification>
1. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
2. No unpinned actions remain: `grep 'uses: actions/' .github/workflows/ci.yml | grep -v '@[a-f0-9]\{40\}'` returns empty
3. Workflow-level permissions contain only `contents: read`
4. Each write-requiring job has explicit per-job permissions
5. publish-github-release needs chain includes publish-docker-image
6. `make tests-python` succeeds locally (no regression from cache flag changes)
</verification>

<success_criteria>
- CI workflow follows least-privilege principle: read-only by default, write only where needed
- All 13 action references are pinned to immutable SHA hashes
- GitHub Release cannot be created before Docker image is published
- Python test image build supports optional registry cache
</success_criteria>

<output>
After completion, create `.planning/phases/93-ci-docker-hardening/93-01-SUMMARY.md`
</output>
