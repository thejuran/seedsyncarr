---
phase: 90-angular-test-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts
  - src/angular/src/app/tests/unittests/services/utils/notification.service.spec.ts
  - src/angular/src/app/tests/unittests/services/files/file-selection.service.spec.ts
  - src/angular/src/app/tests/unittests/pages/files/transfer-row.component.spec.ts
autonomous: true
requirements: [ANGFIX-03, ANGFIX-04, ANGFIX-05, ANGFIX-06]
must_haves:
  truths:
    - "Every .subscribe() call in view-file.service.spec.ts has a matching sub.unsubscribe() at end of test body"
    - "Every .subscribe() call in notification.service.spec.ts has a matching sub.unsubscribe() at end of test body"
    - "Every .subscribe() call in file-selection.service.spec.ts observable tests has a matching sub.unsubscribe()"
    - "Every .subscribe() call in transfer-row.component.spec.ts has a matching sub.unsubscribe()"
  artifacts:
    - path: "src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts"
      provides: "Subscription teardown for 21 subscribe calls"
      contains: "sub.unsubscribe()"
    - path: "src/angular/src/app/tests/unittests/services/utils/notification.service.spec.ts"
      provides: "Subscription teardown for 7 subscribe calls"
      contains: "sub.unsubscribe()"
    - path: "src/angular/src/app/tests/unittests/services/files/file-selection.service.spec.ts"
      provides: "Subscription teardown for 5 observable subscribe calls"
      contains: "sub.unsubscribe()"
    - path: "src/angular/src/app/tests/unittests/pages/files/transfer-row.component.spec.ts"
      provides: "Subscription teardown for 2 EventEmitter subscribe calls"
      contains: "sub.unsubscribe()"
  key_links:
    - from: "transfer-row.component.spec.ts"
      to: "component.checkboxToggle"
      via: "EventEmitter subscription"
      pattern: "sub\\.unsubscribe"
---

<objective>
Fix subscription leaks in 4 Angular spec files by adding per-test unsubscribe calls matching the existing transfer-table.component.spec.ts pattern.

Purpose: Prevent cross-test emission contamination from leaked subscriptions in view-file.service, notification.service, file-selection.service, and transfer-row.component spec files (ANGFIX-03/04/05/06).
Output: 4 modified spec files with per-test subscription teardown, all 599 Angular tests passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-angular-test-fixes/90-CONTEXT.md
@.planning/phases/90-angular-test-fixes/90-RESEARCH.md
@.planning/phases/90-angular-test-fixes/90-PATTERNS.md

<interfaces>
<!-- Gold-standard pattern from transfer-table.component.spec.ts (existing codebase precedent) -->
<!-- Executor must replicate this exact pattern in all 4 files -->

```typescript
// Per-test subscription teardown pattern:
let pagedFiles: ViewFile[] = [];
const sub = component.pagedFiles$.subscribe(f => pagedFiles = f);
tick();
sub.unsubscribe();
```

Rules:
- Always assign: `const sub = ...subscribe(...)` (or `sub1`, `sub2` for multiples)
- Always place `sub.unsubscribe()` AFTER the last `expect(...)` in the test body
- For EventEmitter subscriptions: only unsubscribe `sub`, NEVER call `.complete()` on the emitter
- No afterEach approach -- per-test is the project standard (per D-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subscription teardown to view-file.service.spec.ts and notification.service.spec.ts (ANGFIX-03, ANGFIX-04)</name>
  <files>
    src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts
    src/angular/src/app/tests/unittests/services/utils/notification.service.spec.ts
  </files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/files/view-file.service.spec.ts
    - src/angular/src/app/tests/unittests/services/utils/notification.service.spec.ts
    - src/angular/src/app/tests/unittests/pages/files/transfer-table.component.spec.ts (gold-standard pattern reference, lines 255-310)
  </read_first>
  <action>
**ANGFIX-03: view-file.service.spec.ts -- 21 subscribe calls across 16 test bodies**

Per D-01: Use per-test `const sub = ...; sub.unsubscribe()` pattern, matching transfer-table.component.spec.ts precedent.

For EVERY `.subscribe({` or `.subscribe(` call in this file, change the pattern from:

```typescript
viewService.files.subscribe({
    next: list => { ... }
});
```

To:

```typescript
const sub = viewService.files.subscribe({
    next: list => { ... }
});
```

And add `sub.unsubscribe();` as the LAST statement in the test body, AFTER all `expect(...)` assertions.

The affected tests and their subscribe calls:

1. **"should forward an empty model by default"** (line 49) -- 1 subscribe on `viewService.files`. Add `const sub =` prefix, add `sub.unsubscribe();` after `expect(count).toBe(1);` at line 57.

2. **"should forward an empty model"** (line 66) -- 1 subscribe on `viewService.files`. Add `const sub =` prefix, add `sub.unsubscribe();` after `expect(count).toBe(1);` at line 73.

3. **"should correctly populate ViewFile props from a ModelFile"** (line 97) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(1);` at line 118.

4. **"should correctly set the ViewFile status"** (line 145) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(8);` at line 205.

5. **"should always set a non-null file sizes in ViewFile"** (line 219) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(1);` at line 229.

6. **"should correctly set ViewFile percent downloaded"** (line 243) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(testVectors.length);` at line 268.

7. **"should should correctly set ViewFile isQueueable"** (line 299) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(testVectors.length);` at line 328.

8. **"should should correctly set ViewFile isStoppable"** (line 357) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(testVectors.length);` at line 386.

9. **"should should correctly set ViewFile isExtractable"** (line 415) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(testVectors.length);` at line 444.

10. **"should correctly set and unset the selected file"** (line 516) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(7);` at line 572.

11. **"should should correctly set ViewFile isLocallyDeletable"** (line 601) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(testVectors.length);` at line 630.

12. **"should should correctly set ViewFile isRemotelyDeletable"** (line 659) -- 1 subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(testVectors.length);` at line 688.

13. **"should not filter any files by default"** (line 706) -- 1 subscribe on `viewService.filteredFiles`. Add `const sub =`, add `sub.unsubscribe();` after `expect(viewFiles.size).toBe(8);` at line 714.

14. **"should apply filter criteria correctly"** (line 743) -- 1 subscribe on `viewService.filteredFiles`. Add `const sub =`, add `sub.unsubscribe();` after `expect(viewFilesMap.has("blueman")).toBe(true);` at line 756.

15. **"should resend filtered files on criteria change"** (line 776) -- 1 subscribe on `viewService.filteredFiles`. Add `const sub =`, add `sub.unsubscribe();` after `expect(viewFilesMap.has("blueman")).toBe(true);` at line 809.

16. **"should not sort files by default"** (line 827) -- 1 subscribe on `viewService.files`. Add `const sub =`, add `sub.unsubscribe();` after the last `expect(viewFiles.get(7)!.name).toBe("spicy");` at line 843.

17. **"should sort new model correctly"** (line 867) -- 1 subscribe on `viewService.files`. Add `const sub =`, add `sub.unsubscribe();` after the last `expect(viewFiles.get(7)!.name).toBe("tofu");` at line 883.

18. **"should sort existing model on setComparator"** (line 901) -- 1 subscribe on `viewService.files`. Add `const sub =`, add `sub.unsubscribe();` after `expect(viewFiles.get(7)!.name).toBe("aaaa");` at line 926.

19. **FIX-01 "DELETED + remote_size=null must be queueable"** (line 1007) -- 1 subscribe on `viewService.files`. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(1);` at line 1037.

20. **FIX-01 "DELETED + remote_size>0 is queueable"** (line 1042) -- 1 subscribe on `viewService.files`. Add `const sub =`, add `sub.unsubscribe();` after `expect(count).toBe(1);` at line 1072.

That covers all 20 test bodies with subscribe calls (21 total subscribe calls, since test 10 "should correctly set and unset the selected file" has 1 subscribe).

**ANGFIX-04: notification.service.spec.ts -- 7 subscribe calls across 7 test bodies**

Apply the same per-test `const sub = ...; sub.unsubscribe()` pattern to all 7 subscribe calls:

1. **"should show notification"** (line 38) -- `notificationService.notifications.subscribe({...})`. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 47.

2. **"should hide notification"** (line 58) -- subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 66.

3. **"should only send one update if show is called twice"** (line 76) -- subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 85.

4. **"should only send one update if hide is called twice"** (line 96) -- subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 106.

5. **"should sort notifications by level"** (line 121) -- subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 133.

6. **"should sort notifications by timestamp"** (line 150) -- subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 161.

7. **"should sort notifications by level first, then timestamp"** (line 178) -- subscribe. Add `const sub =`, add `sub.unsubscribe();` after `expect(actualCount).toBe(1);` at line 189.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false --include="**/view-file.service.spec.ts" --include="**/notification.service.spec.ts" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - view-file.service.spec.ts: grep -c "sub.unsubscribe()" returns 20 (one per test body with subscribe)
    - view-file.service.spec.ts: grep -c "const sub = " returns at least 20
    - view-file.service.spec.ts: no bare `.subscribe({` without preceding `const sub = ` (excluding commented code)
    - notification.service.spec.ts: grep -c "sub.unsubscribe()" returns 7
    - notification.service.spec.ts: grep -c "const sub = " returns at least 7
    - `ng test --watch=false` for both spec files exits 0
  </acceptance_criteria>
  <done>All 20 subscribe calls in view-file.service.spec.ts and 7 subscribe calls in notification.service.spec.ts have per-test `const sub = ...; sub.unsubscribe()` teardown. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add subscription teardown to file-selection.service.spec.ts and transfer-row.component.spec.ts (ANGFIX-05, ANGFIX-06)</name>
  <files>
    src/angular/src/app/tests/unittests/services/files/file-selection.service.spec.ts
    src/angular/src/app/tests/unittests/pages/files/transfer-row.component.spec.ts
  </files>
  <read_first>
    - src/angular/src/app/tests/unittests/services/files/file-selection.service.spec.ts
    - src/angular/src/app/tests/unittests/pages/files/transfer-row.component.spec.ts
  </read_first>
  <action>
**ANGFIX-05: file-selection.service.spec.ts -- 5 subscribe calls on signal-derived observables**

These tests use `TestBed.flushEffects()` instead of `fakeAsync`/`tick()`. Apply per-test `const sub = ...; sub.unsubscribe()` pattern.

1. **"should emit on selectedFiles$ when selection changes"** (line 267) -- `service.selectedFiles$.subscribe(...)`. Add `const sub =` prefix, add `sub.unsubscribe();` after `expect(emissions[2].size).toBe(0);` at line 285.

2. **"should emit on selectedCount$ when selection changes"** (line 291) -- `service.selectedCount$.subscribe(...)`. Add `const sub =` prefix, add `sub.unsubscribe();` after `expect(emissions[2]).toBe(0);` at line 308.

3. **"should emit on hasSelection$ when selection changes"** (line 314) -- `service.hasSelection$.subscribe(...)`. Add `const sub =` prefix, add `sub.unsubscribe();` after `expect(emissions[2]).toBe(false);` at line 331.

4. **"should not emit when no actual change occurs"** (line 365) -- `service.selectedFiles$.subscribe(...)`. Add `const sub =` prefix, add `sub.unsubscribe();` after `expect(emissionCount).toBe(1);` at line 377.

5. There is also a subscribe in `"should not emit when no actual change occurs"` at line 365. That is item 4 above.

Total: 4 test bodies with subscribe calls (lines 264, 288, 311, 362).

**ANGFIX-06: transfer-row.component.spec.ts -- 2 EventEmitter subscribe calls**

Per D-05: Fix EventEmitter subscription leak. CRITICAL: Only call `.unsubscribe()` on the subscription object -- NEVER call `.complete()` on the EventEmitter itself (it extends Subject, completing it would break subsequent tests).

1. **"emits checkboxToggle with shiftKey=false on plain click"** (line 198) -- `component.checkboxToggle.subscribe(e => emission = e)`.
   Change to: `const sub = component.checkboxToggle.subscribe(e => emission = e);`
   Add `sub.unsubscribe();` after `expect(emission!.shiftKey).toBe(false);` at line 204 (as the last statement before the closing `});`).

2. **"emits checkboxToggle with shiftKey=true on shift+click"** (line 210) -- `component.checkboxToggle.subscribe(e => emission = e)`.
   Change to: `const sub = component.checkboxToggle.subscribe(e => emission = e);`
   Add `sub.unsubscribe();` after `expect(emission!.shiftKey).toBe(true);` at line 215 (as the last statement before the closing `});`).
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false --include="**/file-selection.service.spec.ts" --include="**/transfer-row.component.spec.ts" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - file-selection.service.spec.ts: grep -c "sub.unsubscribe()" returns 4 (one per observable test)
    - transfer-row.component.spec.ts: grep -c "sub.unsubscribe()" returns 2 (one per EventEmitter test)
    - transfer-row.component.spec.ts does NOT contain `.complete()` called on `checkboxToggle`
    - `ng test --watch=false` for both spec files exits 0
  </acceptance_criteria>
  <done>All 4 signal-observable subscribe calls in file-selection.service.spec.ts and 2 EventEmitter subscribe calls in transfer-row.component.spec.ts have per-test `const sub = ...; sub.unsubscribe()` teardown. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries -- all changes are test-only edits with no production code or external interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02 | N/A | Test spec files only | accept | No production code changes, no new attack surface. Test-only edits adding teardown calls. |
</threat_model>

<verification>
cd /Users/julianamacbook/seedsyncarr/src/angular && npx ng test --watch=false 2>&1 | tail -30
- All 599 tests pass
- Zero subscription leak warnings
</verification>

<success_criteria>
- view-file.service.spec.ts: 20 `sub.unsubscribe()` calls (covering all 20 subscribe-containing test bodies)
- notification.service.spec.ts: 7 `sub.unsubscribe()` calls
- file-selection.service.spec.ts: 4 `sub.unsubscribe()` calls
- transfer-row.component.spec.ts: 2 `sub.unsubscribe()` calls
- `ng test --watch=false` exits 0 with all 599 tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/90-angular-test-fixes/90-02-SUMMARY.md`
</output>
