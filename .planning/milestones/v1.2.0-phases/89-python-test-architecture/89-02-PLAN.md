---
phase: 89-python-test-architecture
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/docs/coverage-gaps.md
  - src/python/docs/name-mangling-tradeoff.md
autonomous: true
requirements:
  - PYARCH-04
  - PYARCH-05

must_haves:
  truths:
    - "Coverage gap documentation lists all 4 untested modules with paths, line counts, and indirect coverage notes"
    - "Name-mangling trade-off documentation explains why 155 references across 5 test files are accepted"
  artifacts:
    - path: "src/python/docs/coverage-gaps.md"
      provides: "Coverage gap inventory for modules without dedicated tests"
      contains: "ActiveScanner"
    - path: "src/python/docs/name-mangling-tradeoff.md"
      provides: "Documented rationale for private-API coupling in tests"
      contains: "name-mangling"
  key_links: []
---

<objective>
Create documentation for Python test coverage gaps and the name-mangling trade-off.

Purpose: Make coverage gaps and architectural trade-offs visible so future phases (especially Phase 94: Test Coverage -- Backend) have a clear starting point. These are pure documentation tasks with no code changes.

Output: 2 new markdown files in src/python/docs/.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-python-test-architecture/89-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coverage gap documentation</name>
  <files>src/python/docs/coverage-gaps.md</files>
  <read_first>
    src/python/docs/coverage-gaps.md
    src/python/controller/scan/active_scanner.py
    src/python/web/web_app_job.py
    src/python/web/web_app_builder.py
    src/python/scan_fs.py
  </read_first>
  <action>
**PYARCH-04 -- Create `src/python/docs/coverage-gaps.md`:**

Create this file documenting the 4 modules that lack dedicated test files. Read each source module to get accurate line counts and understand what the module does. Structure:

```markdown
# Python Test Coverage Gaps

Documented: {today's date}
Phase: 89 (PYARCH-04)

## Purpose

This file lists production modules that have no dedicated test file. Some are partially covered by integration tests that exercise them indirectly. Tracked here so future test coverage work (Phase 94) has a clear starting point.

## Modules Without Dedicated Test Files

| Module | Path | Lines | Purpose | Covered Indirectly? | Phase 94 Req |
|--------|------|-------|---------|---------------------|--------------|
| ActiveScanner | controller/scan/active_scanner.py | {actual} | Orchestrates scanner processes, scan scheduling | Partially via test_scanner_process.py | COVER-06 |
| WebAppJob | web/web_app_job.py | {actual} | Manages web app lifecycle thread | No | -- |
| WebAppBuilder | web/web_app_builder.py | {actual} | Constructs Bottle app with middleware | Partially via integration test_web_app.py | -- |
| scan_fs | scan_fs.py | {actual} | Remote filesystem scanning script | No | -- |

## Notes

- Line counts are for the production module only (excluding blank lines and comments would be lower).
- "Covered Indirectly" means another test file exercises this module's code paths without being a dedicated test for it.
- ActiveScanner (COVER-06) and DeleteRemoteProcess (COVER-05) are explicitly tracked for Phase 94.
- WebAppJob and scan_fs remain as future test candidates not yet scheduled.
```

Replace `{actual}` with the real line counts from reading each file. Use `wc -l` output.
  </action>
  <verify>
    <automated>test -f src/python/docs/coverage-gaps.md && grep -q "ActiveScanner" src/python/docs/coverage-gaps.md && grep -q "WebAppJob" src/python/docs/coverage-gaps.md && grep -q "WebAppBuilder" src/python/docs/coverage-gaps.md && grep -q "scan_fs" src/python/docs/coverage-gaps.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/python/docs/coverage-gaps.md` exists
    - File contains "ActiveScanner" with path `controller/scan/active_scanner.py`
    - File contains "WebAppJob" with path `web/web_app_job.py`
    - File contains "WebAppBuilder" with path `web/web_app_builder.py`
    - File contains "scan_fs" with path `scan_fs.py`
    - Each module has an accurate line count (not placeholder)
    - File contains "COVER-06" reference for ActiveScanner
  </acceptance_criteria>
  <done>
    Coverage gap documentation exists listing all 4 modules with accurate line counts, paths, indirect coverage notes, and Phase 94 cross-references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create name-mangling trade-off documentation</name>
  <files>src/python/docs/name-mangling-tradeoff.md</files>
  <read_first>
    src/python/docs/name-mangling-tradeoff.md
  </read_first>
  <action>
**PYARCH-05 -- Create `src/python/docs/name-mangling-tradeoff.md`:**

First, run `grep -rn "_Controller__\|_ControllerHandler__\|_Lftp__\|_LftpManager__\|_ScanManager__\|_ActiveScanner__\|_ModelBuilder__" src/python/tests/ | wc -l` to get the exact count of name-mangling references. Then run `grep -rn "_Controller__\|_ControllerHandler__\|_Lftp__\|_LftpManager__\|_ScanManager__\|_ActiveScanner__\|_ModelBuilder__" src/python/tests/ | sed 's/:.*//;s|.*/||' | sort -u` to get the list of affected files.

Create the documentation file:

```markdown
# Name-Mangling in Python Tests: Accepted Trade-Off

Documented: {today's date}
Phase: 89 (PYARCH-05)

## What Is Name-Mangling?

Python name-mangling transforms attributes with double-underscore prefix (e.g., `self.__field`) into `_ClassName__field` at the class level. Tests that access `obj._ClassName__field` are coupling to the private API surface.

## Current Usage

{N} name-mangling references across {M} test files:

| Pattern | Test File(s) | What It Accesses |
|---------|-------------|------------------|
| `_Controller__` | {files} | Controller internal state (model, started flag, pending auto-deletes, etc.) |
| `_ControllerHandler__` | {files} | ControllerHandler internal state |
| {other patterns found} | {files} | {description} |

## Why This Is Accepted

1. **No public API alternative:** The Controller class exposes limited public methods. Testing internal state transitions (e.g., verifying that a timer was scheduled for auto-delete, or that the model was updated correctly) requires access to private attributes.

2. **Test-only coupling:** Name-mangling access is confined to test files. Production code does not use cross-class name-mangling.

3. **Refactoring cost vs. benefit:** Wrapping every private attribute in a test-visible property would add boilerplate to production code purely for testability, violating encapsulation for the wrong reason.

4. **Established pattern:** This pattern has been stable across {count of milestones} milestones of development. The private API surface has not changed in ways that broke tests.

## Risk

If Controller internals are renamed, these tests will fail with `AttributeError`. This is considered an acceptable early-warning signal rather than a defect -- it forces test updates when internal structure changes.

## Decision

**Status:** Accepted trade-off. No action required.
**Decided:** Phase 89 (PYARCH-05)
**Revisit trigger:** If Controller undergoes major internal refactoring, consider adding test-visible properties.
```

Replace all `{...}` placeholders with actual values from the grep analysis.
  </action>
  <verify>
    <automated>test -f src/python/docs/name-mangling-tradeoff.md && grep -q "name-mangling" src/python/docs/name-mangling-tradeoff.md && grep -q "Accepted trade-off" src/python/docs/name-mangling-tradeoff.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/python/docs/name-mangling-tradeoff.md` exists
    - File contains the actual count of name-mangling references (not a placeholder)
    - File contains a table listing the affected test files
    - File contains "Accepted trade-off" as the decision status
    - File contains "PYARCH-05" reference
  </acceptance_criteria>
  <done>
    Name-mangling trade-off is documented with actual reference counts, affected files, rationale for acceptance, and revisit triggers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan creates documentation files only -- no code changes, no production paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02 | I (Information Disclosure) | docs/coverage-gaps.md | accept | Documents module names and line counts already visible in the open-source repository. No secrets or credentials exposed. |
</threat_model>

<verification>
1. `ls src/python/docs/coverage-gaps.md src/python/docs/name-mangling-tradeoff.md` succeeds (both files exist)
2. `grep -c "ActiveScanner\|WebAppJob\|WebAppBuilder\|scan_fs" src/python/docs/coverage-gaps.md` returns 4+
3. `grep -q "Accepted trade-off" src/python/docs/name-mangling-tradeoff.md`
</verification>

<success_criteria>
- coverage-gaps.md lists all 4 modules (ActiveScanner, WebAppJob, WebAppBuilder, scan_fs) with accurate line counts
- name-mangling-tradeoff.md documents the trade-off with actual reference counts and affected files
- Both files provide actionable context for Phase 94 test coverage work
</success_criteria>

<output>
After completion, create `.planning/phases/89-python-test-architecture/89-02-SUMMARY.md`
</output>
