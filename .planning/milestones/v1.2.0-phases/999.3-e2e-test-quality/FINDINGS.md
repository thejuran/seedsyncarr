# E2E Test Quality — Deep Review Findings

**Reviewed:** 2026-04-24
**Scope:** `src/e2e/tests/` — Playwright specs, page objects, fixtures

---

## Warning

### BUG-01: innerHTML() vs innerText()/textContent() (score: 88)
- **File:** `src/e2e/tests/autoqueue.page.ts:28`
- **Issue:** `getPatterns()` uses `innerHTML()` to read chip text. If patterns contain HTML-special characters, assertions silently compare raw HTML.
- **Fix:**
  ```diff
  - return Promise.all(elements.map(elm => elm.innerHTML()));
  + return Promise.all(elements.map(elm => elm.innerText()));
  ```

### BUG-04: waitForTimeout(500) hard-coded delay (score: 80)
- **File:** `src/e2e/tests/dashboard.page.spec.ts:255`
- **Issue:** Playwright anti-pattern; 500ms may be too short on arm64 CI runners.
- **Fix:**
  ```diff
  - await page.waitForTimeout(500);
  + await dashboardPage.getStatusBadge('testing.gif')
  +     .filter({ hasNotText: 'Syncing' })
  +     .waitFor({ timeout: 10_000 })
  +     .catch(() => {});
  ```

---

## Medium

### BUG-02: settings-error.spec.ts beforeEach calls API before navigateTo() (score: 72)
- **File:** `src/e2e/tests/settings-error.spec.ts:7-9`
- **Issue:** In non-serial mode, `page` fixture from `beforeEach` and test body would be different instances.
- **Fix:** Add `await settingsPage.navigateTo()` in `beforeEach` before `disableSonarr()`.

---

## Filtered

### E2E-02: Deprecated :has-text() pseudo-class (score: 50)
- **File:** `src/e2e/tests/autoqueue.page.ts:35`
- **Issue:** Soft-deprecated in Playwright 1.27; use `locator.filter({ hasText })` instead.

### E2E-04: HTTP responses not checked for rate-limit config calls (score: 40)
- **File:** `src/e2e/tests/dashboard.page.spec.ts:272/302`
- **Issue:** `GET /server/config/set/lftp/rate_limit/...` responses silently discarded; if backend rejects, throttle never applied and test races.

### E2E-05: void page suppresses unused variable warning (score: 20)
- **File:** `src/e2e/tests/dashboard.page.spec.ts:368`
- **Issue:** Remove destructure or use `_page` prefix.

### E2E-06: HTTP GET for state-mutating config operations (score: 30)
- **File:** `src/e2e/tests/settings.page.ts:15/24/33/56`
- **Issue:** enableSonarr/disableSonarr use GET; design concern tied to backend API contract.

### E2E-07: beforeEach constructs throwaway App instance (score: 25)
- **File:** `src/e2e/tests/app.spec.ts:5-8` + 3 others
- **Issue:** Page object created in `beforeEach` then recreated in each test. Store in `let` variable.

### E2E-08: Retry + serial tests risk fixture pollution (score: 35)
- **File:** `src/e2e/playwright.config.ts`
- **Issue:** `retries: 2` on CI with stateful serial blocks; retry-safe guards scattered across tests.

### BUG-03: beforeAll seed context bypasses CSP fixture (score: 60)
- **File:** `src/e2e/tests/dashboard.page.spec.ts:121-133,375-387`
- **Issue:** UAT-01/UAT-02 `beforeAll` creates raw context; CSP violations during seed silently swallowed.

### ARCH: _escapeRegex duplicated (score: 30)
- **Issue:** Identical helper in `dashboard.page.ts` and `seed-state.ts`; extract to shared utility.

### ARCH: waitForBadge vs waitForFileStatus diverge on match semantics (score: 25)
- **Issue:** `seed-state.ts` uses anchored regex, `DashboardPage` uses `hasText` substring.

### ARCH: SettingsPage mixes UI page object with backend API calls (score: 25)
- **Issue:** `enableSonarr` etc. call backend directly; should be in a config-state fixture.

### ARCH: UAT-01/UAT-02 seeding pattern undocumented (score: 15)
- **Issue:** Separate browser context in `beforeAll` opened/closed before tests — no comment explaining why.

---

*Generated by deep code review, 2026-04-24.*
