---
phase: 35-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/pages/files/file-options.component.html
  - src/angular/src/app/pages/files/file-options.component.scss
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "User sees a green terminal `>` character to the left of the search input instead of the SVG search icon"
    - "Search input continues to filter files by name when typed into"
  artifacts:
    - path: "src/angular/src/app/pages/files/file-options.component.html"
      provides: "Terminal prompt span replacing SVG search icon"
      contains: "class=\"prompt\""
    - path: "src/angular/src/app/pages/files/file-options.component.scss"
      provides: "Prompt styling with Fira Code monospace, green color, absolute positioning"
      contains: ".prompt"
  key_links:
    - from: "src/angular/src/app/pages/files/file-options.component.html"
      to: "src/angular/src/app/pages/files/file-options.component.scss"
      via: "CSS class .prompt + #filter-search"
      pattern: "class=\"prompt\""
---

<objective>
Replace the SVG search icon with a terminal-style `>` prompt character in the file list search input.

Purpose: Reinforces the Terminal/Hacker aesthetic by converting the generic search icon into a monospace green prompt character, consistent with the sidebar route indicator established in Phase 34.

Output: Updated file-options component with green `>` prompt prefix in search input.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-dashboard/35-RESEARCH.md
@src/angular/src/app/pages/files/file-options.component.html
@src/angular/src/app/pages/files/file-options.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace search SVG with terminal prompt and style it</name>
  <files>
    src/angular/src/app/pages/files/file-options.component.html
    src/angular/src/app/pages/files/file-options.component.scss
  </files>
  <action>
In `file-options.component.html`:
1. Replace `<img src="assets/icons/search.svg" />` with `<span class="prompt">&gt;</span>`
2. Change the search input placeholder from `"Filter by name..."` to `"filter by name..."` (lowercase for terminal aesthetic)

In `file-options.component.scss`, within the `#filter-search` block:
1. Replace the existing `img { ... }` rule with a `.prompt` rule:
   ```scss
   .prompt {
       font-family: var(--bs-font-monospace);  // Fira Code
       color: #3fb950;                          // terminal green ($primary)
       font-size: 1.1rem;
       font-weight: 500;
       position: absolute;
       left: 10px;
       top: 50%;
       transform: translateY(-50%);
       z-index: $zindex-file-search;
       pointer-events: none;
       user-select: none;
   }
   ```
2. Update the `input` padding-left from `30px` to `24px` to accommodate the narrower `>` character (was `30px` for the wider SVG image).
  </action>
  <verify>
Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build` — build succeeds with no errors.
Visually: The search input shows a green `>` character at the left, input text starts after the prompt, placeholder reads "filter by name..." in lowercase.
  </verify>
  <done>
Search input displays green Fira Code `>` prompt instead of SVG icon. Input padding aligns text correctly after the prompt character. Search filtering functionality is unaffected.
  </done>
</task>

</tasks>

<verification>
1. `ng build` completes without errors
2. No SVG search icon `<img>` element remains in `file-options.component.html`
3. The `.prompt` class is defined in `file-options.component.scss`
4. The `>` character appears green and positioned at the left of the search input
5. Typing in the search input still filters the file list by name
</verification>

<success_criteria>
- Green terminal `>` prompt visible before search input text
- No SVG search icon in the file-options template
- Search functionality works as before (ngModel binding intact)
- Prompt uses Fira Code font family via `--bs-font-monospace`
</success_criteria>

<output>
After completion, create `.planning/phases/35-dashboard/35-01-SUMMARY.md`
</output>
