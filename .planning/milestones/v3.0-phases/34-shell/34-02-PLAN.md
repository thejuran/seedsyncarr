---
phase: 34-shell
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/angular/src/app/pages/main/sidebar.component.ts
  - src/angular/src/app/pages/main/sidebar.component.html
  - src/angular/src/app/pages/main/sidebar.component.scss
autonomous: false
requirements: [NAV-02, NAV-03]

must_haves:
  truths:
    - "Active route shows > prompt indicator when sidebar is expanded"
    - "App version displays at bottom of sidebar"
    - "Version is only visible when sidebar is expanded (hidden at 56px)"
    - "> prompt uses monospace font in terminal green color"
  artifacts:
    - path: "src/angular/src/app/pages/main/sidebar.component.ts"
      provides: "Version property from package.json"
      contains: "appVersion"
    - path: "src/angular/src/app/pages/main/sidebar.component.html"
      provides: "Version display element and sidebar-label class on version"
      contains: "sidebar-version"
    - path: "src/angular/src/app/pages/main/sidebar.component.scss"
      provides: "::before prompt indicator on .selected and version footer styling"
      contains: "::before"
  key_links:
    - from: "src/angular/src/app/pages/main/sidebar.component.ts"
      to: "src/angular/package.json"
      via: "require() import for version string"
      pattern: "require.*package\\.json"
    - from: "src/angular/src/app/pages/main/sidebar.component.html"
      to: "src/angular/src/app/pages/main/sidebar.component.ts"
      via: "template binding {{version}}"
      pattern: "\\{\\{version\\}\\}"
---

<objective>
Add terminal-style `>` prompt indicator on active sidebar route and display app version at the bottom of the sidebar, completing the shell navigation requirements.

Purpose: NAV-02 gives visual feedback for active route in terminal aesthetic. NAV-03 shows app version persistently in the sidebar footer.

Output: Active route has `> ` prefix when sidebar expanded, version string visible at sidebar bottom when expanded.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-shell/34-RESEARCH.md
@.planning/phases/34-shell/34-01-SUMMARY.md
@src/angular/src/app/pages/main/sidebar.component.html
@src/angular/src/app/pages/main/sidebar.component.ts
@src/angular/src/app/pages/main/sidebar.component.scss
@src/angular/src/app/pages/about/about-page.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version property and prompt indicator + version footer to sidebar</name>
  <files>
    src/angular/src/app/pages/main/sidebar.component.ts
    src/angular/src/app/pages/main/sidebar.component.html
    src/angular/src/app/pages/main/sidebar.component.scss
  </files>
  <action>
1. **sidebar.component.ts** — Add version property using existing pattern from `about-page.component.ts`:
   - Add `declare function require(moduleName: string): { version: string };` before the imports (or after — match about-page pattern).
   - Add `const { version: appVersion } = require("../../../../package.json");` at module level.
   - Add `public version: string;` property to the class.
   - In constructor, set `this.version = appVersion;`.

2. **sidebar.component.html** — Add version display at the bottom:
   - After the `<div class="sidebar-spacer"></div>` added in Plan 01, add:
     ```html
     <div class="sidebar-version sidebar-label">v{{version}}</div>
     ```
   - The `sidebar-label` class ensures it's hidden at 56px collapsed width and visible at 200px expanded (inherits the opacity/max-width transition from Plan 01).

3. **sidebar.component.scss** — Add `>` prompt indicator and version styling:
   - Add `::before` pseudo-element on `.button.selected` for the prompt indicator:
     ```scss
     &.selected {
       // ... existing styles ...

       .sidebar-label::before {
         content: '> ';
         font-family: var(--bs-font-monospace);
         color: #3fb950;  // terminal green, matches --app-logo-color
       }
     }
     ```
     NOTE: Put `::before` on `.sidebar-label` (not on `.button`), so the prompt is hidden along with the label at 56px collapsed width. This avoids the pitfall of the `>` taking space and misaligning the icon at collapsed state.
   - Add `.sidebar-version` styling:
     ```scss
     .sidebar-version {
       padding: 8px 12px;
       font-family: var(--bs-font-monospace);
       font-size: 0.75rem;
       color: var(--app-text-muted);
       border-top: 1px solid var(--bs-border-color);
     }
     ```
  </action>
  <verify>
  Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build --configuration=production 2>&1 | tail -5` — build succeeds.
  Run `cd /Users/julianamacbook/seedsync/src/angular && npx ng test --no-watch --browsers=ChromeHeadless 2>&1 | tail -10` — all tests pass.
  </verify>
  <done>
  Active sidebar route shows `> ` prefix in terminal green monospace font when sidebar is expanded. Version string (e.g., "v1.0.0") displays at sidebar bottom in monospace, visible only when expanded. Build passes, tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of sidebar icon-rail</name>
  <action>User visually verifies the complete sidebar icon-rail implementation across desktop and mobile breakpoints.</action>
  <what-built>
  Complete sidebar icon-rail with CSS-only expansion, prompt indicator on active route, version footer, and preserved mobile hamburger menu. All NAV-01 through NAV-04 requirements implemented.
  </what-built>
  <how-to-verify>
  1. Start dev server: `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng serve`
  2. Open http://localhost:4200 in a desktop browser (width > 993px):
     - Verify sidebar appears as narrow icon rail (~56px) on the left
     - Hover over sidebar — it should expand to ~200px with smooth animation
     - Labels (Dashboard, Settings, etc.) fade in during expansion
     - Active route shows `> ` prefix in green monospace font (only when expanded)
     - Version string (e.g., "v1.0.0") visible at bottom of sidebar when expanded
     - Logo block is hidden on desktop
     - Content area is offset by 56px from the left edge
  3. Resize browser to mobile width (<= 992px):
     - Sidebar should be hidden
     - Hamburger button should be visible in title bar
     - Click hamburger — sidebar slides in as full overlay with logo and close button
     - Click outside sidebar or navigate — sidebar closes
  4. Navigate between pages — active route indicator updates correctly
  </how-to-verify>
  <verify>All visual checks pass across desktop and mobile breakpoints</verify>
  <done>User confirms sidebar icon-rail looks correct on desktop and mobile hamburger menu works as before</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Production build succeeds: `ng build --configuration=production`
2. All Angular unit tests pass: `ng test --no-watch --browsers=ChromeHeadless`
3. `>` prompt visible on active route when sidebar expanded
4. Version string visible at sidebar bottom when expanded
5. Mobile hamburger menu fully functional
</verification>

<success_criteria>
- Active route has `> ` prompt prefix in terminal green when sidebar expanded
- Version from package.json displayed at sidebar bottom
- Both hidden at 56px collapsed width
- User visually approves the complete sidebar shell
</success_criteria>

<output>
After completion, create `.planning/phases/34-shell/34-02-SUMMARY.md`
</output>
