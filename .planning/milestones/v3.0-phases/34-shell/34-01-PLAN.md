---
phase: 34-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/common/_common.scss
  - src/angular/src/app/pages/main/app.component.html
  - src/angular/src/app/pages/main/app.component.scss
  - src/angular/src/app/pages/main/sidebar.component.html
  - src/angular/src/app/pages/main/sidebar.component.scss
autonomous: true
requirements: [NAV-01, NAV-04]

must_haves:
  truths:
    - "On large screens (993px+), sidebar is always visible as a 56px icon rail"
    - "On large screens, hovering the sidebar expands it to 200px with smooth CSS transition"
    - "Nav link labels are hidden at 56px and visible at 200px (opacity + max-width transition)"
    - "On mobile (<=992px), hamburger menu and overlay sidebar behavior is unchanged"
    - "Content area has margin-left: 56px on large screens, 0 on mobile"
    - "Logo block hidden on large screens; visible on mobile with close button"
  artifacts:
    - path: "src/angular/src/app/common/_common.scss"
      provides: "New sidebar width variables ($sidebar-collapsed-width, $sidebar-expanded-width)"
      contains: "$sidebar-collapsed-width: 56px"
    - path: "src/angular/src/app/pages/main/sidebar.component.html"
      provides: "Sidebar nav links with icon/label structure"
      contains: "sidebar-label"
    - path: "src/angular/src/app/pages/main/sidebar.component.scss"
      provides: "Sidebar label hide/show transitions"
      contains: "sidebar-label"
    - path: "src/angular/src/app/pages/main/app.component.scss"
      provides: "Icon-rail CSS with large-screen media query and mobile preservation"
      contains: "$sidebar-collapsed-width"
  key_links:
    - from: "src/angular/src/app/pages/main/app.component.scss"
      to: "src/angular/src/app/common/_common.scss"
      via: "@use import of sidebar width variables"
      pattern: "\\$sidebar-collapsed-width"
    - from: "src/angular/src/app/pages/main/sidebar.component.scss"
      to: "src/angular/src/app/pages/main/sidebar.component.html"
      via: "CSS class .sidebar-label applied to label spans"
      pattern: "\\.sidebar-label"
---

<objective>
Restructure the sidebar from a mobile-only overlay drawer into a persistent CSS-only icon-rail on large screens (56px collapsed, 200px on hover), while preserving the existing hamburger/overlay behavior on mobile.

Purpose: NAV-01 requires a persistent, collapsible sidebar on desktop. NAV-04 requires mobile hamburger menu to remain functional. Both are layout-structural changes to the app shell.

Output: Sidebar visible as 56px icon rail on desktop, expanding to 200px on CSS :hover. Mobile hamburger unchanged.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-shell/34-RESEARCH.md
@src/angular/src/app/common/_common.scss
@src/angular/src/app/pages/main/app.component.html
@src/angular/src/app/pages/main/app.component.scss
@src/angular/src/app/pages/main/app.component.ts
@src/angular/src/app/pages/main/sidebar.component.html
@src/angular/src/app/pages/main/sidebar.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure sidebar HTML and SCSS for icon-rail layout</name>
  <files>
    src/angular/src/app/common/_common.scss
    src/angular/src/app/pages/main/sidebar.component.html
    src/angular/src/app/pages/main/sidebar.component.scss
  </files>
  <action>
1. **_common.scss** — Add new sidebar width variables, keep old one temporarily for mobile:
   - Add `$sidebar-collapsed-width: 56px;` and `$sidebar-expanded-width: 200px;` alongside the existing `$sidebar-width: 170px`.
   - The old `$sidebar-width` is still used by mobile overlay code in `app.component.scss` — do NOT remove it yet; it will be updated in Task 2.

2. **sidebar.component.html** — Restructure nav links for icon-rail:
   - Change outer `<div id="sidebar">` to `<nav id="sidebar">` for semantic HTML.
   - On each `<img>` tag, add class `sidebar-icon`.
   - On each `<span class="text">`, change class from `text` to `sidebar-label`.
   - After the Restart `<a>` block, add a spacer `<div class="sidebar-spacer"></div>` (this will flex-grow to push the version footer down — version added in Plan 02).

3. **sidebar.component.scss** — Add label hide/show transitions:
   - Change `#sidebar` selector to `nav#sidebar` (or just keep `#sidebar` — the id stays the same).
   - Add `.sidebar-icon` styles: `width: 24px; height: 24px; flex-shrink: 0;` (slightly larger than current 18px for icon-rail visibility).
   - Update the `.button` block:
     - Change `display: block` to `display: flex; align-items: center; gap: 8px;` so icon and label sit side by side.
     - Remove `width: 100%` (flex handles it).
     - Keep existing `padding: 8px 16px` but adjust to `padding: 8px 12px` for tighter icon-rail fit.
   - Add `.sidebar-label` styles (replaces `.text`):
     ```scss
     .sidebar-label {
       opacity: 0;
       max-width: 0;
       overflow: hidden;
       transition: opacity 0.2s ease, max-width 0.25s ease;
       display: inline-block;
       vertical-align: middle;
       white-space: nowrap;
     }
     ```
   - Add `.sidebar-spacer` style: `flex-grow: 1;`.
   - Update the existing `.button img` rule to use `.sidebar-icon` instead, keeping `margin-right: 0` (gap handles spacing now) and removing `margin-bottom: 2px`.
   - Make `#sidebar` itself a flex column: `display: flex; flex-direction: column; height: 100%;` so spacer pushes version to bottom.
  </action>
  <verify>
  Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build --configuration=production 2>&1 | tail -5` — build succeeds with no errors.
  </verify>
  <done>
  Sidebar HTML uses semantic `nav`, icons have `sidebar-icon` class, labels have `sidebar-label` class with opacity/max-width transition styles, sidebar is flex column layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement icon-rail CSS on large screens and preserve mobile behavior</name>
  <files>
    src/angular/src/app/pages/main/app.component.html
    src/angular/src/app/pages/main/app.component.scss
    src/angular/src/app/common/_common.scss
  </files>
  <action>
1. **app.component.scss** — Rewrite large-screen media query for icon-rail:
   - In the `#top-sidebar` base styles (outside media queries):
     - Keep `background-color`, `position: fixed`, `z-index`, `box-shadow`.
     - Keep `top: 8px` and `height: calc(100% - 8px)`.
     - Change `width: $sidebar-width` to `width: $sidebar-collapsed-width` (from _common.scss).
     - Change `overflow: auto` to `overflow: hidden` (labels hidden at 56px).
     - Add `transition: width 0.25s ease;`.
     - Add `white-space: nowrap;` to prevent label wrapping during animation.
     - Keep the `animation: animateleft 0.4s` for mobile slide-in.
   - In the large-screen `@media (min-width: $large-min-width)` block:
     - `#top-sidebar`:
       - `display: block;` (always visible — keep existing).
       - `width: $sidebar-collapsed-width;` (reinforce).
       - `overflow: hidden;`.
       - `animation: none;` (disable slide-in for desktop).
       - On `&:hover`: `width: $sidebar-expanded-width;`.
       - On `&:hover .sidebar-label`: `opacity: 1; max-width: 150px;`.
     - `#top-content`: Change `margin-left: $sidebar-width` to `margin-left: $sidebar-collapsed-width`.
       - Do NOT animate margin-left — content stays fixed at 56px offset.
     - Keep existing `.sidebar-btn { display: none; }`, `#title-bar { display: none; }`, `.outside-sidebar-show { display: none; }`.
     - Add `#top-sidebar #logo { display: none; }` — hide logo block on large screens (it takes too much space in icon-rail).
   - In the `#top-content` base styles: Change `margin-left: $sidebar-width` to `margin-left: $sidebar-collapsed-width`.
   - In the mobile `@media (max-width: $medium-max-width)` block:
     - Keep `#top-content { margin-left: 0; }`.
     - Add `#top-sidebar { width: $sidebar-width; overflow: auto; white-space: normal; }` to restore full-width overlay on mobile.
   - Update `@keyframes animateleft` to use `$sidebar-width` (still 170px for mobile animation).

2. **app.component.html** — No structural changes needed. The `#logo` block stays in HTML (hidden via CSS on large screens, visible on mobile).

3. **_common.scss** — Keep `$sidebar-width: 170px` for mobile overlay usage. The new `$sidebar-collapsed-width` and `$sidebar-expanded-width` are for desktop icon-rail.

4. **Verify mobile behavior is preserved:**
   - `showSidebar` toggle still controls `top-sidebar-open/closed` classes.
   - `#outside-sidebar` click-dismiss overlay still works.
   - Hamburger button still visible on mobile (`.sidebar-btn` only hidden on large screens).
  </action>
  <verify>
  Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build --configuration=production 2>&1 | tail -5` — build succeeds.
  Run `cd /Users/julianamacbook/seedsync/src/angular && npx ng test --no-watch --browsers=ChromeHeadless 2>&1 | tail -10` — all tests pass.
  </verify>
  <done>
  On large screens: sidebar displays as 56px icon rail, expands to 200px on hover with labels fading in, logo hidden. On mobile: sidebar is full overlay drawer with hamburger toggle, exactly as before. Content area has correct margin-left on all breakpoints.
  </done>
</task>

</tasks>

<verification>
1. Production build succeeds: `ng build --configuration=production`
2. All Angular unit tests pass: `ng test --no-watch --browsers=ChromeHeadless`
3. No SCSS compilation errors
4. Sidebar width variables in _common.scss: $sidebar-collapsed-width (56px), $sidebar-expanded-width (200px), $sidebar-width (170px, kept for mobile)
</verification>

<success_criteria>
- Sidebar renders as 56px icon rail on large screens (993px+)
- Sidebar expands to 200px on CSS :hover with smooth transition
- Labels hidden at 56px, visible at 200px
- Mobile hamburger menu and overlay sidebar unchanged
- Build passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-shell/34-01-SUMMARY.md`
</output>
