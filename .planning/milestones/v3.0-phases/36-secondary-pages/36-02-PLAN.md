---
phase: 36-secondary-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/pages/logs/logs-page.component.scss
  - src/angular/src/app/pages/about/about-page.component.html
  - src/angular/src/app/pages/about/about-page.component.scss
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "Log warning lines use amber text color only, no background color or border color"
    - "Log error/critical lines use red text color only, no background color or border color"
    - "Log debug lines use muted gray color"
    - "Log status message has terminal > prefix in green and Fira Code monospace"
    - "About page displays ASCII art banner in green Fira Code instead of image logo"
    - "About page version number is displayed in green Fira Code monospace"
    - "About page feature/platform list items use > terminal marker instead of bullet"
    - "About page section titles display with --- dashes in green Fira Code"
  artifacts:
    - path: "src/angular/src/app/pages/logs/logs-page.component.scss"
      provides: "Terminal-pure log level coloring without background blocks"
    - path: "src/angular/src/app/pages/about/about-page.component.html"
      provides: "ASCII art banner replacing image logo"
      contains: "ascii-logo"
    - path: "src/angular/src/app/pages/about/about-page.component.scss"
      provides: "Terminal styling for version, section titles, list markers"
      contains: "ascii-logo"
  key_links:
    - from: "about-page.component.html"
      to: "about-page.component.scss"
      via: "ascii-logo class in HTML matches SCSS styling rule"
      pattern: "ascii-logo"
    - from: "logs-page.component.scss"
      to: "logs-page.component.html"
      via: "Log level classes (debug, info, warning, error, critical) still match template class bindings"
      pattern: "&\\.(debug|info|warning|error|critical)"
---

<objective>
Apply terminal aesthetic to Logs and About pages: remove background-color/border-color from log level rules and use direct terminal colors (amber warning, red error, muted debug), replace About page image banner with ASCII art, style version in green Fira Code, and change list markers to terminal `>` prompts.

Purpose: Complete the Terminal/Hacker visual identity across all remaining secondary pages, matching the aesthetic established in Phases 33-35.
Output: Updated Logs and About components with terminal styling.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-secondary-pages/36-RESEARCH.md

@src/angular/src/app/pages/logs/logs-page.component.scss
@src/angular/src/app/pages/logs/logs-page.component.html
@src/angular/src/app/pages/about/about-page.component.html
@src/angular/src/app/pages/about/about-page.component.scss
@src/angular/src/app/pages/about/about-page.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terminal-pure log level colors and status message styling</name>
  <files>
    src/angular/src/app/pages/logs/logs-page.component.scss
  </files>
  <action>
**SCSS changes (logs-page.component.scss):**

1. Replace the `p.record` log level color rules. Change the existing block:
   ```scss
   &.debug {
       color: var(--bs-secondary-color);
   }
   &.info {
       color: var(--bs-body-color);
   }
   &.warning {
       color: var(--bs-warning-text-emphasis);
       background-color: var(--bs-warning-bg-subtle);
       border-color: var(--bs-warning-border-subtle);
   }
   &.error, &.critical {
       color: var(--bs-danger-text-emphasis);
       background-color: var(--bs-danger-bg-subtle);
       border-color: var(--bs-danger-border-subtle);
   }
   ```
   to (direct terminal colors, no backgrounds, no borders):
   ```scss
   &.debug {
       color: #8b949e;
   }
   &.info {
       color: #e6edf3;
   }
   &.warning {
       color: #f0883e;
   }
   &.error, &.critical {
       color: #f85149;
   }
   ```
   These are the same color values from the Terminal palette established in Phase 33 (`_bootstrap-variables.scss` and `_common.scss`).

2. Update the `.status-message` rule to add terminal styling with `>` prefix and Fira Code:
   ```scss
   .status-message {
       padding: 20px;
       text-align: center;
       color: #8b949e;
       font-size: 14px;
       font-family: var(--bs-font-monospace);

       &::before {
           content: '> ';
           color: #3fb950;
       }
   }
   ```
   Change `color: $secondary` to `color: #8b949e` (direct value, consistent with dark-only palette). Add `font-family: var(--bs-font-monospace)` for Fira Code. Add `::before` pseudo-element with green `>` terminal prefix.

3. Update the top-level `#logs` font-family from generic `monospace` to Fira Code:
   ```scss
   font-family: var(--bs-font-monospace);
   ```
   This ensures all log records use Fira Code consistently (the `font-family: monospace` is already there; just change it to the CSS custom property).

**Do NOT change:**
- Scroll buttons (`btn-scroll`, `btn-primary`) — keep as-is per research recommendation
- Log record layout, margins, traceback styling — functional, not cosmetic
- Connected indicator — functional, not part of PAGE-03 requirement
  </action>
  <verify>
Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build` to confirm zero compilation errors. Grep for `background-color` in logs-page.component.scss to confirm no background colors remain on log level rules. Grep for `border-color` in logs-page.component.scss to confirm it only appears in the `.connected` rule (not in log level rules). Verify `#f0883e` (amber) and `#f85149` (red) are present for warning and error levels.
  </verify>
  <done>
Log warning lines display in amber (#f0883e) text only — no background or border. Log error/critical lines display in red (#f85149) text only — no background or border. Debug lines in muted gray (#8b949e). Info lines in body text (#e6edf3). Status message has green > terminal prefix in Fira Code. All log records use Fira Code monospace. Build succeeds with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: ASCII art About page with terminal markers and monospace version</name>
  <files>
    src/angular/src/app/pages/about/about-page.component.html
    src/angular/src/app/pages/about/about-page.component.scss
  </files>
  <action>
**HTML changes (about-page.component.html):**

1. Replace the `#banner` div contents. Change from:
   ```html
   <div id="banner">
       <img src="assets/logo.png" alt="SeedSync" />
       <span><b>Seed</b>Sync</span>
   </div>
   ```
   to:
   ```html
   <div id="banner">
       <pre class="ascii-logo"> ____                _ ____
/ ___|  ___  ___  __| / ___| _   _ _ __   ___
\___ \ / _ \/ _ \/ _` \___ \| | | | '_ \ / __|
 ___) |  __/  __/ (_| |___) | |_| | | | | (__
|____/ \___|\___|\__,_|____/ \__, |_| |_|\___|
                              |___/</pre>
   </div>
   ```
   The `<pre>` preserves the ASCII art whitespace. No leading/trailing blank lines inside the `<pre>` — the art starts right after the opening tag.

2. Change both `section-title` divs to use a class for terminal styling. The HTML stays the same (`<div class="section-title">Features</div>` and `<div class="section-title">Supported Platforms</div>`). The styling change happens in SCSS.

**SCSS changes (about-page.component.scss):**

3. Replace the `#banner` styles completely. Remove the `img` and `span` rules and add the ASCII logo styling:
   ```scss
   #banner {
       display: flex;
       align-items: center;
       justify-content: center;
       margin-bottom: 10px;

       .ascii-logo {
           font-family: var(--bs-font-monospace);
           color: #3fb950;
           font-size: 0.5rem;
           line-height: 1.2;
           text-align: left;
           display: inline-block;
           margin: 0;
           user-select: none;
           white-space: pre;
           overflow: hidden;
       }
   }
   ```

4. Update the `#version` rule to add green Fira Code:
   ```scss
   #version {
       font-size: 150%;
       margin-bottom: 15px;
       font-family: var(--bs-font-monospace);
       color: #3fb950;
   }
   ```

5. Update the `.section-title` rule inside `#features, #platforms` to use terminal dashes and green Fira Code:
   ```scss
   .section-title {
       font-weight: bold;
       margin-bottom: 8px;
       text-align: center;
       font-family: var(--bs-font-monospace);
       color: #3fb950;

       &::before { content: '--- '; }
       &::after  { content: ' ---'; }
   }
   ```

6. Update the `li::before` rule inside `#features, #platforms ul` to use `>` terminal marker instead of bullet `\2022`:
   ```scss
   li {
       padding: 4px 0;
       &::before {
           content: '>';
           color: #3fb950;
           font-family: var(--bs-font-monospace);
           font-weight: normal;
           display: inline-block;
           width: 1em;
           margin-left: 1em;
       }
   }
   ```
   Changes: `content: "\2022"` to `content: '>'`, `color: var(--app-logo-color)` to `color: #3fb950`, add `font-family: var(--bs-font-monospace)`, change `font-weight: bold` to `font-weight: normal`.

7. Update the `#description` rule to add Fira Code monospace:
   ```scss
   #description {
       margin-bottom: 20px;
       line-height: 1.5;
       font-family: var(--bs-font-monospace);
       color: #8b949e;
   }
   ```

**Do NOT change:**
- Links section, license, copyright, fork-note, icons — keep as-is
- about-page.component.ts — version injection works unchanged
  </action>
  <verify>
Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build` to confirm zero compilation errors. Grep for `<img` in about-page.component.html to confirm the logo image is removed. Grep for `ascii-logo` in both HTML and SCSS to confirm the ASCII art is wired. Grep for `\2022` in about-page.component.scss to confirm the bullet is replaced. Grep for `var(--bs-font-monospace)` in about-page.component.scss to confirm Fira Code is applied.
  </verify>
  <done>
About page displays ASCII art "SeedSync" banner in green Fira Code instead of image logo. Version number rendered in green Fira Code monospace. Feature and platform list items use `>` terminal marker in green. Section titles display with `---` dashes in green Fira Code. Description text in muted gray Fira Code. Build succeeds with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `ng build` completes with zero errors
2. No `background-color` or `border-color` on log level rules in logs-page.component.scss (only in `.connected` rule)
3. Direct color values `#f0883e` (warning) and `#f85149` (error) present in logs-page.component.scss
4. `<img` tag removed from about-page.component.html
5. `ascii-logo` class present in both about HTML and SCSS
6. `>` terminal marker replaces `\2022` bullet in about-page.component.scss
7. `var(--bs-font-monospace)` used in both logs and about SCSS files
</verification>

<success_criteria>
- Log levels use color-only terminal styling: debug=gray, info=white, warning=amber, error/critical=red
- No background or border colors on any log level rule
- Log status message has green > prefix in Fira Code
- About page shows ASCII art logo in green Fira Code
- About page version in green Fira Code monospace
- Feature/platform lists use > terminal markers
- Section titles wrapped with --- dashes
- Angular build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-secondary-pages/36-02-SUMMARY.md`
</output>
