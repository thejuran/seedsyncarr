---
phase: 38-terminal-polish-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/pages/main/sidebar.component.scss
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - NAV-03

must_haves:
  truths:
    - "Sidebar version text renders in muted gray (#8b949e), not bright white (#e6edf3)"
    - "REQUIREMENTS.md coverage note reflects Phase 38 completion (no 'pending' language)"
  artifacts:
    - path: "src/angular/src/app/pages/main/sidebar.component.scss"
      provides: "Correct CSS custom property reference for sidebar version color"
      contains: "var(--app-muted-text)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated coverage note reflecting Phase 38 completion"
      contains: "Satisfied: 21"
  key_links:
    - from: "src/angular/src/app/pages/main/sidebar.component.scss"
      to: "src/angular/src/styles.scss"
      via: "CSS custom property var(--app-muted-text)"
      pattern: "var\\(--app-muted-text\\)"
---

<objective>
Fix CSS variable typo in sidebar version text and update requirements traceability.

Purpose: Close the final cosmetic gap identified in the v3.0 milestone audit -- sidebar version text displays as bright white instead of muted gray due to a misspelled CSS custom property name.
Output: Corrected sidebar.component.scss and updated REQUIREMENTS.md coverage note.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/angular/src/app/pages/main/sidebar.component.scss
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CSS variable typo and update traceability note</name>
  <files>src/angular/src/app/pages/main/sidebar.component.scss, .planning/REQUIREMENTS.md</files>
  <action>
    **sidebar.component.scss (line 76):**
    Change `color: var(--app-text-muted);` to `color: var(--app-muted-text);` inside the `.sidebar-version` rule block. This corrects a reversed adjective-noun CSS custom property name. The canonical variable `--app-muted-text` is defined in `styles.scss` `:root` and resolves to `#8b949e` (muted gray). Do NOT add a new variable to styles.scss -- fix the call site only.

    **REQUIREMENTS.md (line 95):**
    Change `Satisfied: 21 (1 with cosmetic fix pending in Phase 38)` to `Satisfied: 21`. The cosmetic fix is now complete; no pending qualifier needed.
  </action>
  <verify>
    1. Grep sidebar.component.scss for `--app-muted-text` -- should find exactly 1 match on the color line in `.sidebar-version`
    2. Grep sidebar.component.scss for `--app-text-muted` -- should find 0 matches (typo removed)
    3. Grep REQUIREMENTS.md for "pending" -- should find 0 matches
    4. Run `cd /Users/julianamacbook/seedsync/src/angular && node_modules/@angular/cli/bin/ng build` -- should compile without errors
  </verify>
  <done>sidebar.component.scss uses `var(--app-muted-text)` (correct property name), REQUIREMENTS.md coverage note has no "pending" qualifier, Angular build succeeds</done>
</task>

</tasks>

<verification>
- `var(--app-muted-text)` appears in sidebar.component.scss line 76
- `var(--app-text-muted)` does NOT appear anywhere in the codebase
- REQUIREMENTS.md line 95 reads "Satisfied: 21" with no pending note
- Angular build compiles cleanly
</verification>

<success_criteria>
The CSS custom property reference in sidebar.component.scss matches the canonical definition in styles.scss, causing the sidebar version text to render in muted gray (#8b949e). REQUIREMENTS.md accurately reflects all v3.0 requirements as fully satisfied with no pending items.
</success_criteria>

<output>
After completion, create `.planning/phases/38-terminal-polish-traceability/38-01-SUMMARY.md`
</output>
