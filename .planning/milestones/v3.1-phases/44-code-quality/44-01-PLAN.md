---
phase: 44-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/common/config.py
  - src/python/model/file.py
  - src/python/controller/controller.py
autonomous: true
requirements: [CODE-04, CODE-11, CODE-01]

must_haves:
  truths:
    - "Application imports config module without ImportError on Python 3.12+"
    - "All type comparisons in model/file.py use isinstance() instead of type() =="
    - "ModelFile frozen bypass in controller uses an explicit unfreeze method, not name-mangled __frozen access"
  artifacts:
    - path: "src/python/common/config.py"
      provides: "Inline strtobool replacement compatible with Python 3.12+"
      contains: "def _strtobool"
    - path: "src/python/model/file.py"
      provides: "isinstance() type checks and unfreeze() method"
      contains: "def unfreeze"
    - path: "src/python/controller/controller.py"
      provides: "Calls to unfreeze() instead of _ModelFile__frozen = False"
      contains: ".unfreeze()"
  key_links:
    - from: "src/python/common/config.py"
      to: "Python 3.12+ stdlib"
      via: "No distutils import"
      pattern: "def _strtobool"
    - from: "src/python/controller/controller.py"
      to: "src/python/model/file.py"
      via: "ModelFile.unfreeze() public API"
      pattern: "new_file\\.unfreeze\\(\\)"
---

<objective>
Fix Python type system issues: replace deprecated distutils.strtobool, convert all type(x) == SomeType to isinstance(), and add an explicit unfreeze() method to ModelFile replacing name-mangled __frozen access.

Purpose: Ensure Python 3.12+ compatibility (distutils removed), improve type checking correctness (isinstance handles subclasses), and eliminate fragile name-mangling bypass pattern.
Output: Three source files updated with correct patterns.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/python/common/config.py
@src/python/model/file.py
@src/python/controller/controller.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace distutils.strtobool and fix type comparisons in model/file.py</name>
  <files>src/python/common/config.py, src/python/model/file.py</files>
  <action>
In src/python/common/config.py:
1. Remove `from distutils.util import strtobool` (line 7).
2. Add a local `_strtobool` function that replicates the same behavior: accepts a string, returns 1 for 'y', 'yes', 't', 'true', 'on', '1' (case-insensitive); returns 0 for 'n', 'no', 'f', 'false', 'off', '0'; raises ValueError otherwise. This is the exact behavior of the removed distutils.strtobool.
3. Replace the `strtobool(value)` call (line 57) with `_strtobool(value)`.

In src/python/model/file.py:
1. Add an `unfreeze()` method to ModelFile (next to `freeze()` and `_check_frozen()`):
   ```python
   def unfreeze(self):
       """Unfreeze this file, allowing modifications. Used for copy-then-modify patterns."""
       self.__frozen = False
   ```
2. Replace all `type(x) == int` checks with `isinstance(x, int)` in setters:
   - remote_size setter: `if type(remote_size) == int:` -> `if isinstance(remote_size, int):`
   - local_size setter: same pattern
   - transferred_size setter: same pattern
   - downloading_speed setter: same pattern
   - eta setter: same pattern
3. Replace all `type(x) != SomeType` checks with `not isinstance(x, SomeType)`:
   - state setter: `if type(state) != ModelFile.State:` -> `if not isinstance(state, ModelFile.State):`
   - update_timestamp setter: `if type(update_timestamp) != datetime:` -> `if not isinstance(update_timestamp, datetime):`
   - import_status setter: `if type(import_status) != ModelFile.ImportStatus:` -> `if not isinstance(import_status, ModelFile.ImportStatus):`
   - local_created_timestamp setter (line ~249): same pattern
   - local_modified_timestamp setter: same pattern
   - remote_created_timestamp setter: same pattern
   - remote_modified_timestamp setter: same pattern

Check ALL setters in the file — there are approximately 12 instances of type() comparisons total. Every one must use isinstance().
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsync/src/python && python -c "from common.config import Config; print('config import OK')" && python -c "from model.file import ModelFile; f = ModelFile('test', False); f.remote_size = 100; f.freeze(); f.unfreeze(); f.remote_size = 200; print('unfreeze OK')" && python -c "import ast, sys; tree = ast.parse(open('model/file.py').read()); matches = [n for n in ast.walk(tree) if isinstance(n, ast.Compare) and any(isinstance(op, (ast.Eq, ast.NotEq)) for op in n.ops) and any(isinstance(c, ast.Call) and isinstance(c.func, ast.Name) and c.func.id == 'type' for c in [n.left])]; sys.exit(1) if matches else print('No type()==X patterns remain')"</automated>
    <manual>Verify no distutils import remains in config.py; verify no type(x) == patterns remain in file.py</manual>
  </verify>
  <done>config.py imports no distutils; _strtobool function handles all boolean string variants; all type comparisons in file.py use isinstance(); ModelFile has public unfreeze() method</done>
</task>

<task type="auto">
  <name>Task 2: Replace name-mangled __frozen access in controller.py with unfreeze()</name>
  <files>src/python/controller/controller.py</files>
  <action>
In src/python/controller/controller.py, find all instances of `_ModelFile__frozen = False` and replace with `.unfreeze()`:

1. Around line 592: `new_file._ModelFile__frozen = False` -> `new_file.unfreeze()`
   This is in the import_status application block before diffing.

2. Around line 716: `new_file._ModelFile__frozen = False` -> `new_file.unfreeze()`
   This is in the webhook import check's Window 2 block.

Both locations follow the same pattern: `copy.copy(file)` then unfreeze then modify. The new pattern is:
```python
new_file = copy.copy(file)
new_file.unfreeze()
new_file.import_status = ModelFile.ImportStatus.IMPORTED
```

Verify there are no other `_ModelFile__frozen` references in the file. There should be exactly 2 replacements.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsync/src/python && grep -c "_ModelFile__frozen" controller/controller.py | grep -q "^0$" && echo "No name-mangled access remains" || (echo "FAIL: name-mangled access still present" && exit 1)</automated>
    <manual>Verify both import_status code paths in controller.py use .unfreeze() instead of _ModelFile__frozen = False</manual>
  </verify>
  <done>No _ModelFile__frozen name-mangled access remains in controller.py; both copy-then-modify patterns use the public unfreeze() API</done>
</task>

</tasks>

<verification>
1. `cd /Users/julianamacbook/seedsync/src/python && python -c "from common.config import Config"` succeeds (no distutils ImportError)
2. `grep -rn "from distutils" src/python/` returns no results
3. `grep -rn "type(.*) ==" src/python/model/file.py` returns no results
4. `grep -rn "type(.*) !=" src/python/model/file.py` returns no results
5. `grep -rn "_ModelFile__frozen" src/python/controller/controller.py` returns no results
6. `cd /Users/julianamacbook/seedsync/src/python && poetry run pytest tests/unittests/test_model/ -x -q` passes
7. `cd /Users/julianamacbook/seedsync/src/python && poetry run pytest tests/unittests/test_common/test_config.py -x -q` passes
</verification>

<success_criteria>
- Python 3.12+ can import config module without distutils
- All type comparisons in model/file.py use isinstance()
- ModelFile has a public unfreeze() method
- Controller uses unfreeze() instead of name-mangled access
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/44-code-quality/44-01-SUMMARY.md`
</output>
