---
phase: 113-presentation-launch-readiness
plan: 04
type: execute
wave: 3
depends_on: ["113-01", "113-02", "113-03"]
files_modified:
  - README.md
  - ACKNOWLEDGMENTS.md
  - SECURITY.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - CHANGELOG.md
  - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-REPO-METADATA.md
autonomous: false
requirements: [LAUNCH-02, LAUNCH-03, LAUNCH-04, LAUNCH-05, LAUNCH-06]
user_setup:
  - service: github-repo-metadata
    why: "About description / topics / homepage must be applied via the GitHub web UI — the CLI session cannot edit repo settings"
    dashboard_config:
      - task: "Apply the drafted About description, topics/tags, and homepage URL"
        location: "GitHub repo page -> About (gear icon) -> edit; source text in 113-REPO-METADATA.md"
  - service: screenshot-capture
    why: "LAUNCH-03 screenshots are captured at the milestone-end walkthrough via Playwright against the NAS-deployed branch build, NOT during phase execution"
    dashboard_config:
      - task: "Capture the three canonical shots (Dashboard hero, Settings, Logs) against the real NAS build and drop them into doc/images/ at the wired filenames"
        location: "Milestone-end walkthrough (D-05/D-06/D-07) — see the capture plan recorded in this plan's SUMMARY"
must_haves:
  truths:
    - "The finalized README/SECURITY/community-health docs address the cynical-reader teardown's prioritized fixes and the codex pass's fix-disposition findings"
    - "The finalized docs do not advantage-frame the missing outbound-push, do not name the sibling fork, and do not claim sole/only fork (enforceable negative greps)"
    - "The finalized security wording keeps the IP-resolution-guard framing for the *arr connection URLs and the enumerated rate-limit scope (no blanket 'all mutable endpoints')"
    - "No user-facing tracked markdown links to LICENSE.txt after finalization (README.md AND ACKNOWLEDGMENTS.md clean)"
    - "The README references the three canonical screenshot filenames (Dashboard, Settings, Logs) so the walkthrough capture drops in cleanly"
    - "The screenshot capture plan is recorded as a phase artifact with the deferred-to-walkthrough dependency flagged"
    - "A copy-paste-ready GitHub repo-metadata draft exists for manual maintainer application"
    - "Decisions covered by this plan — D-05: three canonical screenshots (Dashboard/Settings/Logs); D-06: captured at walkthrough vs real NAS build, no USE_MOCK_MODEL; D-07: capture-time judgment, iterate if a shot looks bad; D-12: repo-metadata drafted as text for manual maintainer application"
  artifacts:
    - path: "README.md"
      provides: "Finalized rewrite addressing both critiques + wired screenshot references"
      contains: "HMAC"
    - path: "ACKNOWLEDGMENTS.md"
      provides: "License link held at LICENSE (no regression to LICENSE.txt through finalization)"
      contains: "[LICENSE](LICENSE)"
    - path: ".planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-REPO-METADATA.md"
      provides: "Copy-paste-ready About/topics/homepage draft for manual application"
      contains: "Topics"
  key_links:
    - from: "113-TEARDOWN.md + 113-CODEX-PASS.md"
      to: "README.md / SECURITY.md (finalized)"
      via: "prioritized fixes + fix-disposition findings applied"
      pattern: "HMAC"
    - from: "README.md screenshots section"
      to: "doc/images/ (3 canonical filenames)"
      via: "img src references wired for walkthrough capture"
      pattern: "doc/images/"
---

<objective>
Finalize the public-facing rewrite by addressing BOTH adversarial critiques (the cynical-reader
teardown from Plan 02 and the codex content pass from Plan 03), wire the README/docs to reference
the three canonical screenshot filenames so the milestone-end walkthrough capture drops in
cleanly, record the screenshot capture plan, and draft the copy-paste-ready GitHub repo-metadata
text for manual maintainer application.

Purpose: This is the finalized rewrite — it exists AFTER the hostile-take-first critiques, so the
shipped docs survive both (design-spec Definition of Done item 4). Screenshots and repo-metadata
application are manual/walkthrough follow-ups, not executed here.
Output: Finalized README/SECURITY/CONTRIBUTING/CoC/CHANGELOG; 113-REPO-METADATA.md; recorded
screenshot capture plan.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-01-SUMMARY.md
@reports/comparison-nitrobass24-seedsync-2026-06-02.md

<dependency_note>
Depends on Plan 01 (drafts), Plan 02 (teardown), Plan 03 (codex pass). This plan reads the
prioritized fixes from 113-TEARDOWN.md and the fix-disposition findings from 113-CODEX-PASS.md
and applies them to the Plan-01 drafts. files_modified overlaps Plan 01 (README/SECURITY/etc.) —
this is the intended sequential finalization, which is why it is in a later wave.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply teardown + codex findings to finalize the public docs (LAUNCH-02, LAUNCH-04, LAUNCH-05)</name>
  <read_first>
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md (the "Priority Fixes Before Rewrite Finalization" list)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md (findings with Disposition: fix, incl. any install-path fixes + deferred runtime checks)
    - README.md, ACKNOWLEDGMENTS.md, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, CHANGELOG.md (the Plan-01 drafts being finalized)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-01..D-04 constraints still bind; D-08/D-09 bars; §9 honesty guardrails)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (ACCURACY GUARD + HONESTY GUARD + decorator map — finalization must not regress these)
    - reports/comparison-nitrobass24-seedsync-2026-06-02.md §9 (outbound-push-as-scope-choice honesty rule)
    - src/python/web/handler/webhook.py, src/python/web/handler/config.py, src/python/web/web_app.py, src/python/common/encryption.py, src/python/web/rate_limit.py (re-verify any claim a codex finding flagged before re-asserting; the five rate-limit decorator sites and the config.py IP-resolution guard are the ground truth)
  </read_first>
  <action>
    Work the prioritized fix list from 113-TEARDOWN.md and every `Disposition: fix` finding from
    113-CODEX-PASS.md, applying each correction to the corresponding draft. Typical corrections:
    trim or re-scope any overstated feature/security claim the codex flagged as exceeding shipped
    behavior; correct any broken/incomplete/version-drifted install step the install-path
    reproducibility check flagged (and carry any deferred-runtime-check note into the SUMMARY for
    the walkthrough); tighten the fork-positioning wording if the teardown flagged bravado risk
    (still crediting ipsingh06, still not naming the sibling, still not claiming sole/only fork,
    still NOT advantage-framing the missing outbound-push); strengthen the security selling-point
    placement if the teardown found it buried. Hold the corrected security wording from Plan 01:
    the *arr-URL guard stays framed as an IP-resolution guard (NOT generic "SSRF", NOT "outbound
    webhook validation"); rate-limiting stays scoped to the enumerated endpoints (NOT "all mutable
    endpoints"). For any finding marked `accept` or `defer`, do NOT silently drop it — note the
    rationale in this plan's SUMMARY. Re-verify against the cited source before re-writing any
    claim. Hold all D-01..D-04 / D-08 / D-09 constraints. Do not regress the LICENSE links (README
    AND ACKNOWLEDGMENTS) or the [1.4.0] CHANGELOG entry landed in Plan 01.
  </action>
  <verify>
    <automated>grep -q 'Sonarr' README.md && grep -q 'HMAC' README.md && grep -q 'ipsingh06' README.md && ! grep -qi 'nitrobass24' README.md && ! grep -qi 'only fork\|sole fork\|only maintained' README.md && ! grep -qiE 'without cluttering|cleaner than push|avoids? (extra )?api calls|lighter.{0,12}than push' README.md && grep -q '## Security posture' SECURITY.md && grep -qi 'IP-resolution' SECURITY.md && ! grep -qi 'outbound webhook calls validate' SECURITY.md && ! grep -qi 'all mutable endpoints' SECURITY.md && ! grep -rn 'LICENSE\.txt' README.md ACKNOWLEDGMENTS.md && grep -q '## \[1.4.0\]' CHANGELOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Every "Disposition: fix" finding from 113-CODEX-PASS.md is addressed (or accept/defer rationale recorded in SUMMARY); any deferred-runtime install check is carried into the walkthrough follow-up list in the SUMMARY
    - Every item in the teardown's "Priority Fixes" list is addressed (or rationale recorded in SUMMARY)
    - README still mentions Sonarr + HMAC, credits ipsingh06, contains no `nitrobass24`, no "only/sole fork"
    - README does NOT advantage-frame the missing outbound-push (zero matches for "without cluttering", "cleaner than push", "avoids/avoid (extra) api calls", "lighter than push"); any mention frames it as chosen scope
    - SECURITY.md still contains `## Security posture`, still describes the *arr-URL guard as an `IP-resolution` guard (no "outbound webhook calls validate"), still scopes rate-limiting to enumerated endpoints (no "all mutable endpoints")
    - No user-facing LICENSE.txt link regressed: `grep -rn 'LICENSE\.txt' README.md ACKNOWLEDGMENTS.md` returns zero hits; CHANGELOG still has `## [1.4.0]`
    - No claim re-asserted that is not present in the shipped source (re-verified against the cited file)
  </acceptance_criteria>
  <done>The finalized docs survive both adversarial critiques; all fix-disposition findings addressed; honesty + accuracy + LICENSE-link constraints held.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the canonical screenshot references + record the capture plan (LAUNCH-03, D-05/D-06/D-07)</name>
  <read_first>
    - README.md (current Screenshots section heading + above-the-fold img tag structure)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-05 three canonical shots, D-06 real product no USE_MOCK_MODEL, D-07 capture-time judgment)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (README screenshots-section note: wire references, do not stub)
  </read_first>
  <action>
    Wire the README to reference the three canonical screenshot filenames per D-05 so the
    walkthrough capture drops in cleanly: keep the existing above-the-fold hero img referencing the
    Dashboard shot (e.g. `doc/images/screenshot-dashboard.png`), and in the Screenshots section
    reference the Settings and Logs shots at stable filenames (e.g. `doc/images/screenshot-settings.png`,
    `doc/images/screenshot-logs.png`) with descriptive alt text. The image FILES are NOT created
    here — they are captured at the milestone-end walkthrough (D-06: real NAS build, no
    USE_MOCK_MODEL; D-07: capture-time judgment). The README references the intended filenames so
    the capture is a drop-in. Record the capture plan in this plan's SUMMARY: the three shots,
    the no-mock-data + scrub-real-secrets/tokens/IPs constraint, and the explicit
    deferred-to-walkthrough dependency. Do NOT create placeholder/stub image files; reference the
    intended paths only.
  </action>
  <verify>
    <automated>grep -c 'doc/images/screenshot' README.md | grep -qv '^0$' && grep -qi 'Settings' README.md && grep -qi 'Logs' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README references three canonical screenshot filenames (Dashboard hero + Settings + Logs) under doc/images/
    - Screenshot alt text identifies each view (Dashboard, Settings, Logs)
    - No placeholder/stub image files are created in the repo
    - The SUMMARY records the capture plan and flags the deferred-to-walkthrough dependency (real NAS build, no mock data, scrub secrets/tokens/IPs)
  </acceptance_criteria>
  <done>README screenshot references wired for clean walkthrough drop-in; capture plan recorded; capture itself correctly deferred.</done>
</task>

<task type="auto">
  <name>Task 3: Draft copy-paste-ready GitHub repo-metadata artifact (LAUNCH-06, D-12)</name>
  <read_first>
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (113-REPO-METADATA.md structure pattern + topic domain)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-12 metadata-as-draft, D-02 owned axis for the About description)
    - README.md (finalized one-liner — the About description should align)
  </read_first>
  <action>
    Write `113-REPO-METADATA.md` in the phase directory as copy-paste-ready plain text for manual
    maintainer application (no YAML front-matter). Sections per the PATTERNS structure: an apply-via
    instruction line (GitHub repo -> Settings/About gear -> edit); a `## Description` (<=350 chars,
    one sentence carrying the owned-axis value prop aligned to the finalized README one-liner); a
    `## Topics / Tags` comma-separated lowercase-hyphen list drawn from the self-hosted / *arr /
    seedbox / sync domain (e.g. self-hosted, sonarr, radarr, seedbox, lftp, media-server, docker,
    python, angular, webhook, automation, file-sync, homelab); a `## Homepage URL`
    (https://thejuran.github.io/seedsyncarr); and a closing note that this is a draft for manual
    application after walkthrough sign-off because the GitHub web UI cannot be edited from the
    session. This file DRAFTS the metadata only — applying it to GitHub and any git push/publish
    are manual maintainer follow-ups outside phase execution.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-REPO-METADATA.md && grep -qi 'Description' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-REPO-METADATA.md && grep -qi 'Topics' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-REPO-METADATA.md && grep -qi 'Homepage' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-REPO-METADATA.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 113-REPO-METADATA.md exists in the phase directory
    - It contains a Description (<=350 chars), a Topics/Tags list, and a Homepage URL
    - The Description carries the owned-axis value prop (mentions Sonarr-driven / safe-delete framing)
    - It explicitly states the metadata is a draft for manual maintainer application
    - It has no YAML front-matter
  </acceptance_criteria>
  <done>Copy-paste-ready repo-metadata draft exists as a phase artifact; manual application correctly flagged as a maintainer follow-up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the finalized presentation surface</name>
  <action>Pause for the maintainer to review the finalized docs against the two adversarial critiques and confirm the screenshot references + repo-metadata draft are ready, before the phase closes. Verification only — all automated work in Tasks 1-3 is already complete.</action>
  <what-built>
    The finalized public-facing surface: README/SECURITY/CONTRIBUTING/CODE_OF_CONDUCT/CHANGELOG
    revised to address the cynical-reader teardown and codex content pass; ACKNOWLEDGMENTS license
    link held at LICENSE; README screenshot references wired for walkthrough drop-in; the
    copy-paste-ready GitHub repo-metadata draft.
  </what-built>
  <how-to-verify>
    1. Read README.md top-to-bottom as a skeptical r/selfhosted visitor: does the owned-axis
       one-liner land in 30 seconds, is the fork note honest (no sole-fork claim, no outbound-push
       bravado), is the security posture a clear selling point near the top?
    2. Confirm the prioritized fixes in 113-TEARDOWN.md and the fix-disposition findings in
       113-CODEX-PASS.md are actually reflected (spot-check 2-3), including any install-path
       corrections.
    3. Read SECURITY.md `## Security posture` — every bullet should match shipped behavior; the
       *arr-URL guard should read as an honest IP-resolution guard (not a full SSRF library, not
       "outbound webhook validation"); the rate-limit bullet should enumerate the actual endpoints
       (webhook/config-set/test-connection/bulk/status), not claim "all mutable endpoints."
    4. Review 113-REPO-METADATA.md — is the About description + topic list ready to paste?
    5. Note the manual/walkthrough follow-ups: (a) capture the three real screenshots at the
       milestone-end walkthrough and drop them into doc/images/ at the wired filenames; (b) apply
       the repo-metadata draft via the GitHub web UI; (c) run any install-path steps flagged as
       deferred runtime checks (live `pip install` / `docker pull` / `docker run`) against the NAS
       build. None of these are done in this phase.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the docs issues to correct.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → public reader | Finalized public README/SECURITY.md are read by skeptical operators |
| docs/screenshots → maintainer secrets | Screenshots (captured later) and example configs could leak real secrets/tokens/IPs/internal hostnames |
| repo-metadata draft → GitHub web UI | Manual maintainer application step outside the session |

## STRIDE Threat Register

This plan modifies public-facing docs and produces one internal artifact; it adds no production
code, no runtime surface, and no dependencies.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | finalized SECURITY.md / README security claims | mitigate | Finalization re-verifies any claim a codex finding flagged against shipped source before re-asserting; the *arr-URL guard stays framed as an IP-resolution guard and rate-limiting stays scoped to the five actual decorator sites (no blanket claim); the codex pass (Plan 03) is the dedicated gate against false/oversold claims that damage credibility |
| T-113-02 | Information Disclosure | screenshots (captured at walkthrough) + example configs | mitigate | Capture plan recorded in SUMMARY explicitly requires real NAS state with NO real credentials/tokens/IPs visible; no placeholder images committed here; example configs in docs carry no real secrets |
| T-113-06 | Tampering | repo-metadata application via GitHub web UI | accept | Application is a manual maintainer action outside phase execution; the phase only drafts text — no automated write to repo settings, so no injection surface in this phase |

No high-severity code-level threats exist for this docs-only plan — no code, configs, tests, or
dependencies are added or changed. Stated explicitly per the security gate.
</threat_model>

<verification>
- `! grep -rn 'LICENSE\.txt' README.md ACKNOWLEDGMENTS.md` — license links stayed fixed through finalization (repo-wide user-facing)
- `grep -q '## Security posture' SECURITY.md` — posture section retained
- `grep -qi 'IP-resolution' SECURITY.md && ! grep -qi 'outbound webhook calls validate' SECURITY.md && ! grep -qi 'all mutable endpoints' SECURITY.md` — corrected security wording held
- `! grep -qiE 'without cluttering|cleaner than push|avoids? (extra )?api calls|lighter.{0,12}than push' README.md` — no outbound-push bravado
- `grep -q '## \[1.4.0\]' CHANGELOG.md` — changelog entry retained
- `grep -c 'doc/images/screenshot' README.md` (non-zero) — screenshot references wired
- `test -f .planning/.../113-REPO-METADATA.md` — metadata draft exists
- Teardown + codex fix-disposition items reflected (human-verify checkpoint)
</verification>

<success_criteria>
- Finalized docs address both adversarial critiques (teardown + codex pass)
- Honesty guardrails enforced: no sole-fork claim, no sibling naming, no outbound-push advantage-framing
- Security wording accurate: IP-resolution-guard framing + enumerated rate-limit scope held
- No user-facing LICENSE.txt link regressed (README + ACKNOWLEDGMENTS)
- README references the three canonical screenshot filenames for clean walkthrough drop-in
- Screenshot capture correctly deferred to walkthrough with secrets-scrub constraint recorded
- Copy-paste-ready repo-metadata draft exists; manual apply + git push + deferred install runtime checks flagged as maintainer follow-ups
- Human-verify checkpoint passed
</success_criteria>

<output>
Create `.planning/phases/113-presentation-launch-readiness/113-04-SUMMARY.md` when done.
Record in the SUMMARY: (1) which teardown/codex items were fix vs accept/defer (+ rationale),
including any install-path deferred-runtime checks; (2) the screenshot capture plan (3 shots, no
mock data, scrub secrets/tokens/IPs, deferred to walkthrough); (3) the manual maintainer
follow-ups (apply repo-metadata, capture+commit screenshots, run deferred install runtime checks,
then push/publish at milestone end).
</output>
</content>
</invoke>
