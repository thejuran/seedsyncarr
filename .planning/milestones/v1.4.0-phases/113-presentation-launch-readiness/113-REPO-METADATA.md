# GitHub Repo Metadata — Draft for Manual Application

This is copy-paste-ready text for the repository's GitHub "About" panel. **It is a draft for the
maintainer to apply manually** — the GitHub web UI cannot be edited from a CLI session, so applying
this (and any git push/publish) is a manual maintainer follow-up after walkthrough sign-off.

**How to apply:** GitHub repo page → the **About** section (gear icon, top-right of the repo
landing page) → edit the Description, Website, and Topics fields → Save changes.

## Description

(≤350 characters — paste into the "Description" field)

```
Sonarr-driven seedbox sync: LFTP transfers from your seedbox to local, then an HMAC-verified import webhook drives safe auto-delete — so you never delete a file that didn't make it into your library. Sonarr/Radarr integration, web UI, Docker, security by default.
```

## Topics / Tags

(paste each as a topic in the "Topics" field — GitHub lowercases and hyphenates automatically)

```
self-hosted, sonarr, radarr, seedbox, lftp, media-server, docker, python, angular, webhook, automation, file-sync, homelab
```

## Homepage URL

(paste into the "Website" field)

```
https://thejuran.github.io/seedsyncarr
```

---

**Note:** This file drafts the repo-metadata text only. Applying it to the GitHub web UI, and any
git push/publish/tag, are manual maintainer actions outside phase execution. Confirm the homepage
URL resolves (it is referenced from the README) before applying.
