---
phase: 113-presentation-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE.txt
  - LICENSE
  - README.md
  - SECURITY.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - CHANGELOG.md
autonomous: true
requirements: [LAUNCH-02, LAUNCH-04, LAUNCH-05, LAUNCH-06]
must_haves:
  truths:
    - "GitHub's license detector recognizes the repo license (file named LICENSE, not LICENSE.txt)"
    - "A first-draft README carries the owned-axis one-liner (Sonarr-driven + HMAC-verified safe auto-delete) and a fork-relationship note that credits SeedSync without naming the sibling"
    - "SECURITY.md states a concise, code-accurate security posture and the opt-in knobs"
    - "CONTRIBUTING.md reflects the real dev workflow (poetry, ruff, karma, e2e, PR flow)"
    - "CODE_OF_CONDUCT.md exists at repo root (Contributor Covenant)"
    - "CHANGELOG.md has a [1.4.0] entry in Keep a Changelog format with a compare-link footer"
    - "Every security/feature claim in the drafts is accurate to the code shipped in Phases 111-112"
    - "Decisions covered by this plan — D-01: targeted rewrite; D-02: owned-axis one-liner; D-03: fork-note honesty guardrails; D-04: fork-note reference framing; D-08: SECURITY.md posture section; D-09: LICENSE rename + CoC + CONTRIBUTING freshen; D-11: [1.4.0] CHANGELOG entry"
  artifacts:
    - path: "LICENSE"
      provides: "Apache 2.0 license recognized by GitHub (renamed from LICENSE.txt)"
      contains: "Apache License"
    - path: "README.md"
      provides: "First-draft targeted rewrite: owned-axis one-liner, fork note, security selling point, fixed LICENSE links"
      contains: "HMAC"
    - path: "SECURITY.md"
      provides: "Reporting policy (honesty-passed) + Security posture section"
      contains: "## Security posture"
    - path: "CONTRIBUTING.md"
      provides: "Freshened dev setup, code-quality checks, PR flow"
      contains: "ruff"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 at repo root"
      contains: "Contributor Covenant"
    - path: "CHANGELOG.md"
      provides: "[1.4.0] entry summarizing the launch-hardening milestone"
      contains: "## [1.4.0]"
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "badge href + license prose link"
      pattern: "\\]\\(LICENSE\\)"
    - from: "README.md"
      to: "SECURITY.md"
      via: "security selling-point link"
      pattern: "SECURITY\\.md"
---

<objective>
Produce the FIRST DRAFT of every repo-facing presentation document, claim-accurate to the
hardened code shipped in Phases 111-112, and perform the mechanical LICENSE rename. This draft
is the input to the cynical-reader teardown (Plan 02) and the codex content pass (Plan 03);
it is finalized in Plan 04 after both critiques land.

Purpose: Hostile-take-first sequencing (D-10) requires a concrete draft to exist before the
teardown and codex pass can critique it. This plan also lands the independent, mechanical
LICENSE rename (HR-05 / D-09) that unblocks the GitHub license badge regardless of the rewrite.
Output: Renamed LICENSE, first-draft README/SECURITY/CONTRIBUTING/CHANGELOG, new CODE_OF_CONDUCT.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md
@.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md
@reports/comparison-nitrobass24-seedsync-2026-06-02.md

<accuracy_note>
Every security/feature claim asserted in README/SECURITY.md MUST be verified against the
shipped source before being written. The executor confirmed-source map (already grounded by
the planner; re-verify if editing wording):
- Webhook HMAC-SHA256 verify + fail-closed 503: src/python/web/handler/webhook.py:55-60 (503 guard), 93-102 (hmac.compare_digest)
- config-set is POST-only, legacy GET path removed: src/python/web/handler/config.py:26
- Fernet encryption at rest: src/python/common/encryption.py
- SSRF guard is IP-resolution-based (NOT a full SSRF library — "out of scope for a homelab tool"): src/python/web/handler/config.py:56-78
- Bearer auth opt-in via api_token (hmac.compare_digest): src/python/web/web_app.py:112-136
- CSP header on responses: src/python/web/web_app.py:153
- Rate limiting on mutable endpoints: src/python/web/rate_limit.py
</accuracy_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename LICENSE.txt to LICENSE and fix README license links (HR-05 / D-09)</name>
  <read_first>
    - LICENSE.txt (confirm Apache 2.0 content; no content edit)
    - README.md lines 15-18 (badge block) and line 118 (license prose)
    - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md (HR-05, lines 99-107)
  </read_first>
  <action>
    Run `git mv LICENSE.txt LICENSE` (no content change). Then edit README.md two locations:
    line 18 license badge href changes `(LICENSE.txt)` to `(LICENSE)`; line 118 prose changes
    both the link text and href from `[LICENSE.txt](LICENSE.txt)` to `[LICENSE](LICENSE)`.
    Make no other README edits in this task. The license badge uses GitHub's license API
    (`shields.io/github/license`) which only resolves when the file is named exactly `LICENSE`.
  </action>
  <verify>
    <automated>test -f LICENSE && test ! -f LICENSE.txt && ! grep -q 'LICENSE\.txt' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE exists at repo root AND LICENSE.txt is absent
    - README.md contains no occurrence of the string `LICENSE.txt`
    - README.md badge block and license prose both link to `LICENSE`
    - LICENSE content is unchanged Apache 2.0 (no diff beyond the rename)
  </acceptance_criteria>
  <done>LICENSE recognized at repo root; README links point to LICENSE; no LICENSE.txt references remain.</done>
</task>

<task type="auto">
  <name>Task 2: First-draft README targeted rewrite (LAUNCH-02, D-01..D-04)</name>
  <read_first>
    - README.md (full current 154 lines — this is a targeted rewrite, preserve working structure)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-01 targeted-rewrite scope, D-02 owned axis, D-03/D-04 fork-note constraints + reference wording)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (README pattern assignment: one-liner target, fork note, security selling point, sections to keep verbatim)
    - reports/comparison-nitrobass24-seedsync-2026-06-02.md §9 (owned-axis + honesty guardrails)
    - src/python/web/handler/webhook.py and src/python/web/handler/config.py (verify HMAC + POST-only claims before asserting)
  </read_first>
  <action>
    Targeted rewrite, NOT a restructure. Keep verbatim: the wordmark block, Quick Start,
    Installation (Docker + pip), Configuration, Usage Examples, Related Projects, Screenshots
    section heading/structure (screenshot src wiring is deferred to Plan 04 — leave a plan-action
    note, do not stub new img tags here).
    (1) Replace the line-13 one-liner blockquote with the owned-axis value prop per D-02: a
    Sonarr-driven seedbox sync where an HMAC-verified import webhook drives safe auto-delete,
    framed "so you never delete a file that didn't make it into your library." Do NOT claim a
    blanket "unique *arr integration."
    (2) Insert a new `## About This Fork` section after the one-liner / before Quick Start,
    adapting the D-04 reference wording: credit `[SeedSync](https://github.com/ipsingh06/seedsync)`
    (ipsingh06) as origin; acknowledge other active forks generically ("like other active forks,
    it modernizes the original"); state the owned-axis differentiator; frame absence of
    outbound-push as a scope choice. Do NOT name nitrobass24. Do NOT claim sole/only/active fork.
    (3) Add a "Security by default" selling point near the top (in the Features list or a short
    block before "How It Works") naming: Fernet-encrypted secrets at rest, HMAC-verified import
    webhooks, Bearer auth, SSRF guards, CSP headers, rate-limited mutable endpoints — with a link
    to SECURITY.md for the full posture. Keep it brief; SECURITY.md holds the detail (avoid
    verbatim duplication per the discretion note).
    (4) Trim any feature bullet that overstates relative to shipped behavior (verify each against
    code/docs before asserting). This is a first draft — the teardown (Plan 02) and codex pass
    (Plan 03) will surface remaining overstatements for Plan 04.
  </action>
  <verify>
    <automated>grep -q 'Sonarr' README.md && grep -q 'HMAC' README.md && grep -qi 'About This Fork' README.md && grep -q 'ipsingh06' README.md && ! grep -qi 'nitrobass24' README.md && ! grep -qi 'only fork\|sole fork\|only maintained' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README one-liner mentions both `Sonarr` and `HMAC`
    - README contains an `About This Fork` section that contains `ipsingh06` and does NOT contain `nitrobass24`
    - README does NOT contain "only fork", "sole fork", or "only maintained" (case-insensitive)
    - README contains a security selling-point referencing SECURITY.md
    - Quick Start, Installation, Configuration, Usage Examples sections are preserved (not restructured)
  </acceptance_criteria>
  <done>First-draft README carries the owned-axis one-liner, honest fork note, and security selling point; structure preserved.</done>
</task>

<task type="auto">
  <name>Task 3: First-draft SECURITY.md posture, CONTRIBUTING.md freshen, CODE_OF_CONDUCT.md, CHANGELOG [1.4.0] (LAUNCH-04, LAUNCH-05, LAUNCH-06)</name>
  <read_first>
    - SECURITY.md (full current 45 lines — add a section, honesty-pass the contact)
    - CONTRIBUTING.md (full current ~44 lines — freshen, keep register)
    - CHANGELOG.md lines 1-30 ([1.3.0] entry is the structural/tonal model) and footer lines 168-175 (compare-link pattern)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (SECURITY.md / CONTRIBUTING.md / CODE_OF_CONDUCT.md / CHANGELOG patterns + shared voice)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-08 posture knobs, D-09 community-health bar, D-11 changelog)
    - src/python/common/encryption.py, src/python/web/web_app.py:112-153, src/python/web/handler/webhook.py:55-102, src/python/web/handler/config.py:56-78, src/python/web/rate_limit.py (verify each posture claim before asserting)
    - docs/DEVELOPMENT.md (full build/test command list for the CONTRIBUTING freshen)
  </read_first>
  <action>
    SECURITY.md: keep the Supported Versions table and Reporting a Vulnerability sections. Add a
    new `## Security posture` section inserted between `## Reporting a Vulnerability` and
    `## Security Best Practices for Users`, with concise honest bullets per D-08: Fernet-encrypted
    secrets at rest; HMAC-SHA256-verified import webhooks with opt-in `webhook_require_secret`
    fail-closed; Bearer auth via opt-in `api_token` (important for non-loopback binds); SSRF guard
    described accurately as IP-resolution-based destination validation (NOT a full SSRF library —
    match the honest framing in config.py:56-61); CSP headers on responses; rate-limited mutable
    endpoints. Close with the caveat "not a substitute for network isolation." Honesty-pass the
    reporting contact (line 19): add/replace with the GitHub private security advisory path
    `https://github.com/thejuran/seedsyncarr/security/advisories/new` as the standard maintained
    channel. Avoid verbatim duplication of the README selling-point wording.
    CONTRIBUTING.md: freshen per D-09 — Prerequisites add Poetry; Getting Started shows explicit
    `make run-tests-python` / `make run-tests-angular` invocations; add a Code Quality Checks block
    with `ruff check src/python`, `ruff format --check src/python`, `npm run lint`, and
    `make run-tests-e2e`; PR flow mentions ruff/lint as a pre-submit check matching the PR-template
    checklist. Keep the terse imperative register; no emojis.
    CODE_OF_CONDUCT.md: create at repo root using the canonical Contributor Covenant 2.1 text. Set
    the enforcement contact to the same channel SECURITY.md uses (GitHub security advisory link or
    maintainer email). No project-specific customization beyond name + contact.
    CHANGELOG.md: prepend a `## [1.4.0] - 2026-06-02` entry above [1.3.0] in Keep a Changelog
    format matching the [1.3.0] shape — a one-paragraph release summary (presentation + launch
    hardening: hardened public surface, community-health files, v1.4.0 documentation), a
    `### Security` subsection (documented security posture; config-set hardened to POST-only), and
    a `### Changed` subsection (README rewrite, SECURITY.md posture section, CONTRIBUTING.md
    freshen, CODE_OF_CONDUCT.md added, LICENSE.txt renamed to LICENSE). Add a `[1.4.0]` compare
    link to the footer following the existing pattern (`v1.3.0...v1.4.0`). Do not add a [1.3.0]
    compare link. This is a documentation entry only — it does NOT cut a tag.
  </action>
  <verify>
    <automated>grep -q '## Security posture' SECURITY.md && grep -q 'webhook_require_secret' SECURITY.md && grep -q 'ruff' CONTRIBUTING.md && grep -q 'make run-tests' CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md && grep -q 'Contributor Covenant' CODE_OF_CONDUCT.md && grep -q '## \[1.4.0\]' CHANGELOG.md && grep -q 'v1.3.0\.\.\.v1.4.0' CHANGELOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md contains `## Security posture` and the strings `webhook_require_secret` and `api_token`
    - SECURITY.md contains the caveat phrase "not a substitute for network isolation"
    - SECURITY.md reporting section references the GitHub security advisory URL
    - CONTRIBUTING.md contains `ruff` and `make run-tests` invocations
    - CODE_OF_CONDUCT.md exists at repo root and contains `Contributor Covenant`
    - CHANGELOG.md contains `## [1.4.0]` and a `v1.3.0...v1.4.0` compare link in the footer
    - No security posture bullet asserts a protection not present in the shipped source
  </acceptance_criteria>
  <done>First-draft SECURITY/CONTRIBUTING/CoC/CHANGELOG land, all claims code-accurate, ready for critique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → public reader | Public README/SECURITY.md are read by skeptical operators who may trust or disprove claims |
| docs → maintainer secrets | Example configs / contact info could leak real secrets or private deployment details |

## STRIDE Threat Register

This is a documentation-only plan (no production code, no runtime attack surface added). The
threat surface is claim accuracy and information disclosure in public docs.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | SECURITY.md / README security claims | mitigate | Every protection bullet is verified against shipped source (webhook.py, config.py, web_app.py, encryption.py, rate_limit.py) before assertion; SSRF claim framed honestly as IP-resolution-based, not a full SSRF library — a false security claim a hostile reader disproves is itself a credibility risk |
| T-113-02 | Information Disclosure | example configs / contact in docs | mitigate | No real secrets, tokens, internal hostnames, or maintainer private deployment details in any draft; reporting contact uses the public GitHub security advisory channel |
| T-113-03 | Spoofing | SECURITY.md reporting channel | mitigate | Reporting path points to the maintained GitHub security advisory URL so reports reach the real maintainer |

No high-severity code-level threats exist for this docs-only plan — no code, configs, tests, or
dependencies are added or changed. Stated explicitly per the security gate rather than inventing
code threats that do not apply.
</threat_model>

<verification>
- `test -f LICENSE && test ! -f LICENSE.txt` — rename landed
- `! grep -q 'LICENSE\.txt' README.md` — no stale license references
- `grep -q '## Security posture' SECURITY.md` — posture section present
- `grep -q '## \[1.4.0\]' CHANGELOG.md` — changelog entry present
- `test -f CODE_OF_CONDUCT.md` — community-health file present
- Markdown integrity: drafts contain no broken relative links to repo files (LICENSE, SECURITY.md, CONTRIBUTING.md all resolve)
</verification>

<success_criteria>
- LICENSE renamed; README license links fixed; no `LICENSE.txt` references remain
- First-draft README carries owned-axis one-liner + honest fork note + security selling point
- SECURITY.md posture section present, all claims code-accurate
- CONTRIBUTING.md freshened; CODE_OF_CONDUCT.md created; CHANGELOG [1.4.0] entry added
- All drafts ready as input to the teardown (Plan 02) and codex content pass (Plan 03)
</success_criteria>

<output>
Create `.planning/phases/113-presentation-launch-readiness/113-01-SUMMARY.md` when done.
Note in the SUMMARY: these are FIRST DRAFTS pending teardown + codex critique; finalization is Plan 04.
</output>
