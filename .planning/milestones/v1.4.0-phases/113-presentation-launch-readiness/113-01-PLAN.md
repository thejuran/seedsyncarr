---
phase: 113-presentation-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE.txt
  - LICENSE
  - README.md
  - ACKNOWLEDGMENTS.md
  - SECURITY.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - CHANGELOG.md
autonomous: true
requirements: [LAUNCH-02, LAUNCH-04, LAUNCH-05, LAUNCH-06]
must_haves:
  truths:
    - "GitHub's license detector recognizes the repo license (file named LICENSE, not LICENSE.txt)"
    - "No user-facing tracked markdown (README.md, ACKNOWLEDGMENTS.md) links to LICENSE.txt after the rename"
    - "A first-draft README carries the owned-axis one-liner (Sonarr-driven + HMAC-verified safe auto-delete) and a fork-relationship note that credits SeedSync without naming the sibling and without advantage-framing the missing outbound-push"
    - "SECURITY.md states a concise, code-accurate security posture and the opt-in knobs, with rate-limiting scoped to the actual decorated endpoints and the *arr-URL guard described honestly as an IP-resolution guard"
    - "CONTRIBUTING.md reflects the real dev workflow (poetry, ruff, karma, e2e, PR flow)"
    - "CODE_OF_CONDUCT.md exists at repo root (Contributor Covenant)"
    - "CHANGELOG.md has a [1.4.0] entry in Keep a Changelog format with a compare-link footer"
    - "Every security/feature claim in the drafts is verified against its specific source file before being written — no claim from pattern memory"
    - "Decisions covered by this plan — D-01: targeted rewrite; D-02: owned-axis one-liner; D-03: fork-note honesty guardrails; D-04: fork-note reference framing; D-08: SECURITY.md posture section; D-09: LICENSE rename + CoC + CONTRIBUTING freshen; D-11: [1.4.0] CHANGELOG entry"
  artifacts:
    - path: "LICENSE"
      provides: "Apache 2.0 license recognized by GitHub (renamed from LICENSE.txt)"
      contains: "Apache License"
    - path: "README.md"
      provides: "First-draft targeted rewrite: owned-axis one-liner, fork note, security selling point, fixed LICENSE links"
      contains: "HMAC"
    - path: "ACKNOWLEDGMENTS.md"
      provides: "License link repointed to LICENSE (line 34) after the rename"
      contains: "[LICENSE](LICENSE)"
    - path: "SECURITY.md"
      provides: "Reporting policy (honesty-passed) + Security posture section with accurate rate-limit + IP-resolution-guard wording"
      contains: "## Security posture"
    - path: "CONTRIBUTING.md"
      provides: "Freshened dev setup, code-quality checks, PR flow"
      contains: "ruff"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 at repo root"
      contains: "Contributor Covenant"
    - path: "CHANGELOG.md"
      provides: "[1.4.0] entry summarizing the launch-hardening milestone"
      contains: "## [1.4.0]"
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "badge href + license prose link"
      pattern: "\\]\\(LICENSE\\)"
    - from: "ACKNOWLEDGMENTS.md"
      to: "LICENSE"
      via: "license prose link (line 34)"
      pattern: "\\]\\(LICENSE\\)"
    - from: "README.md"
      to: "SECURITY.md"
      via: "security selling-point link"
      pattern: "SECURITY\\.md"
---

<objective>
Produce the FIRST DRAFT of every repo-facing presentation document, claim-accurate to the
hardened code shipped in Phases 111-112, and perform the mechanical LICENSE rename. This draft
is the input to the cynical-reader teardown (Plan 02) and the codex content pass (Plan 03);
it is finalized in Plan 04 after both critiques land.

Purpose: Hostile-take-first sequencing (D-10) requires a concrete draft to exist before the
teardown and codex pass can critique it. This plan also lands the independent, mechanical
LICENSE rename (HR-05 / D-09) that unblocks the GitHub license badge regardless of the rewrite.
Output: Renamed LICENSE, first-draft README/SECURITY/CONTRIBUTING/CHANGELOG, new CODE_OF_CONDUCT.md,
repointed ACKNOWLEDGMENTS.md license link.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md
@.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md
@reports/comparison-nitrobass24-seedsync-2026-06-02.md

<accuracy_note>
Every security/feature claim asserted in README/SECURITY.md MUST be verified against the
shipped source BEFORE being written — no claim from pattern memory. The per-claim source map
below was re-verified by the planner against current source on 2026-06-02; the executor MUST
re-open the cited file/lines and confirm each claim before asserting it in prose:

- Webhook HMAC-SHA256 verify + fail-closed 503:
  src/python/web/handler/webhook.py:55-60 (503 guard when webhook_require_secret on + no secret),
  src/python/web/handler/webhook.py:93-102 (hmac.compare_digest constant-time compare, 401 on mismatch)
- config-set is POST-only, legacy GET path removed:
  src/python/web/handler/config.py:25-28 (add_post_handler + rate_limit)
- Fernet encryption at rest (AES-128-CBC + HMAC-SHA256, 0600 keyfile):
  src/python/common/encryption.py:73-87 (Fernet.generate_key, O_EXCL at 0600),
  src/python/common/encryption.py:124-140 (encrypt_field)
- Bearer auth opt-in via api_token (constant-time compare, allow-all when unset):
  src/python/web/web_app.py:112-141 (auth middleware; hmac.compare_digest at :136; backward-compat allow-all at :123-125)
- CSP header on all responses:
  src/python/web/web_app.py:143-153+ (after_request hook setting Content-Security-Policy)
- IP-resolution guard on user-supplied *arr connection URLs (NOT a full SSRF library; DNS-rebinding
  TOCTOU documented out-of-scope — "out of scope for a homelab tool"):
  src/python/web/handler/config.py:54-84 (_validate_url: socket.getaddrinfo, rejects
  private/loopback/reserved/link-local), applied at src/python/web/handler/config.py:147
  inside _test_arr_connection. SeedSyncarr RECEIVES webhooks; it does NOT send outbound webhooks —
  do NOT describe this as "outbound webhook validation."
- Rate limiting — EXACTLY these five decorator sites (enumerate before asserting; do NOT claim
  blanket "all mutable endpoints"):
    * src/python/web/handler/webhook.py:40-41 — sonarr/radarr webhook → rate_limit(60, 60)
    * src/python/web/handler/config.py:27 — POST config-set → rate_limit(60, 60)
    * src/python/web/handler/config.py:31,35 — sonarr/radarr test-connection → rate_limit(5, 60)
    * src/python/web/handler/controller.py:73 — bulk command → rate_limit(10, 60)
    * src/python/web/handler/status.py:16 — get-status → rate_limit(60, 60)
  NOT rate-limited (verified, NON-EXHAUSTIVE — examples of un-rate-limited mutable endpoints
  include): single-file command endpoints (controller.py:66-70 queue/stop/extract/delete_local/
  delete_remote), POST /server/command/restart (server.py:16 — no rate_limit import), autoqueue
  add/remove/get (auto_queue.py — no rate_limit import), config-get (config.py:24). This NOT-list
  is internal guidance to the executor, NOT prose to ship: the README/SECURITY prose must NOT
  claim a complete enumeration of un-rate-limited endpoints. The POSITIVE claim stays precise —
  the shipped prose names ONLY the five actually-decorated rate-limited endpoints above (webhook,
  config-set, test-connection, bulk command, status) and must NOT claim "all mutable endpoints."
  The rate-limit decorator itself lives at src/python/web/rate_limit.py.
</accuracy_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename LICENSE.txt to LICENSE and fix ALL LICENSE.txt links repo-wide (HR-05 / D-09)</name>
  <read_first>
    - LICENSE.txt (confirm Apache 2.0 content; no content edit)
    - README.md lines 15-18 (badge block) and line 118 (license prose: `[LICENSE.txt](LICENSE.txt)`)
    - ACKNOWLEDGMENTS.md line 34 (license prose: `[LICENSE](LICENSE.txt)` — href breaks on the rename)
    - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md (HR-05, lines 99-107)
  </read_first>
  <action>
    Run `git mv LICENSE.txt LICENSE` (no content change). Then repoint EVERY user-facing
    LICENSE.txt link — this is a repo-wide audit, not README-only:
    (a) README.md line 18 license badge href: `(LICENSE.txt)` → `(LICENSE)`.
    (b) README.md line 118 prose: `[LICENSE.txt](LICENSE.txt)` → `[LICENSE](LICENSE)` (both link
        text and href).
    (c) ACKNOWLEDGMENTS.md line 34 prose: `[LICENSE](LICENSE.txt)` → `[LICENSE](LICENSE)` (href only;
        link text is already `LICENSE`).
    Make no other content edits to README/ACKNOWLEDGMENTS in this task. The license badge uses
    GitHub's license API (`shields.io/github/license`) which only resolves when the file is named
    exactly `LICENSE`. Do NOT rewrite historical `.planning/` artifacts (FILE_INVENTORY.md,
    ROADMAP.md, prior-phase docs) — those are planning history, not user-facing links, and are out
    of scope.
  </action>
  <verify>
    <automated>test -f LICENSE && test ! -f LICENSE.txt && ! grep -rn 'LICENSE\.txt' README.md ACKNOWLEDGMENTS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE exists at repo root AND LICENSE.txt is absent
    - `grep -rn 'LICENSE\.txt' README.md ACKNOWLEDGMENTS.md` returns ZERO hits (repo-wide user-facing audit)
    - ACKNOWLEDGMENTS.md line 34 links to `LICENSE` (not `LICENSE.txt`)
    - README.md badge block AND license prose both link to `LICENSE`
    - LICENSE content is unchanged Apache 2.0 (no diff beyond the rename)
  </acceptance_criteria>
  <done>LICENSE recognized at repo root; README + ACKNOWLEDGMENTS links point to LICENSE; no user-facing LICENSE.txt references remain.</done>
</task>

<task type="auto">
  <name>Task 2: First-draft README targeted rewrite (LAUNCH-02, D-01..D-04)</name>
  <read_first>
    - README.md (full current 154 lines — this is a targeted rewrite, preserve working structure)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-01 targeted-rewrite scope, D-02 owned axis, D-03/D-04 fork-note constraints + reference wording, §9 honesty guardrails)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (README pattern assignment incl. the HONESTY GUARD on outbound-push and the ACCURACY GUARD on security wording; one-liner target, fork note, security selling point, sections to keep verbatim)
    - reports/comparison-nitrobass24-seedsync-2026-06-02.md §9 (owned-axis + honesty guardrails: outbound-push-as-scope-choice, NOT an advantage; no "cluttering Sonarr"/"cleaner than push"/"avoids extra API calls" framing)
    - PER-CLAIM SOURCE VERIFICATION (read each before asserting the matching security selling-point token — see <accuracy_note>):
      * HMAC import webhooks → src/python/web/handler/webhook.py:55-60, 93-102
      * Fernet-encrypted secrets at rest → src/python/common/encryption.py:73-87, 124-140
      * opt-in Bearer auth → src/python/web/web_app.py:112-141
      * IP-resolution guard on *arr connection URLs → src/python/web/handler/config.py:54-84 (applied at :147)
      * CSP headers → src/python/web/web_app.py:143-153
      * rate limiting → src/python/web/handler/{webhook.py:40-41, config.py:27/31/35, controller.py:73, status.py:16}
  </read_first>
  <action>
    Targeted rewrite, NOT a restructure. Keep verbatim: the wordmark block, Quick Start,
    Installation (Docker + pip), Configuration, Usage Examples, Related Projects, Screenshots
    section heading/structure (screenshot src wiring is deferred to Plan 04 — leave a plan-action
    note, do not stub new img tags here).
    (1) Replace the line-13 one-liner blockquote with the owned-axis value prop per D-02: a
    Sonarr-driven seedbox sync where an HMAC-verified import webhook drives safe auto-delete,
    framed "so you never delete a file that didn't make it into your library." Do NOT claim a
    blanket "unique *arr integration."
    (2) Insert a new `## About This Fork` section after the one-liner / before Quick Start,
    adapting the D-04 reference wording: credit `[SeedSync](https://github.com/ipsingh06/seedsync)`
    (ipsingh06) as origin; acknowledge other active forks generically ("like other active forks,
    it modernizes the original"); state the owned-axis differentiator; frame absence of
    outbound-push as a SCOPE CHOICE, not an advantage. Do NOT name nitrobass24. Do NOT claim
    sole/only/active fork. Do NOT advantage-frame the missing outbound-push — no "without
    cluttering", no "cleaner than push", no "avoids extra API calls", no "lighter than push-based
    forks" (negative grep gate below enforces this).
    (3) Add a "Security by default" selling point near the top (in the Features list or a short
    block before "How It Works"). Assert ONLY claims you re-verified against the per-claim source
    files above: Fernet-encrypted secrets at rest, HMAC-verified import webhooks, opt-in Bearer
    auth, an IP-resolution guard on Sonarr/Radarr connection URLs (NOT "SSRF protection" generically,
    NOT "outbound webhook validation"), CSP headers, and rate-limited webhook/config/test-connection/
    bulk/status endpoints (NOT "all mutable endpoints"). Link to SECURITY.md for the full posture.
    Keep it brief; SECURITY.md holds the detail (avoid verbatim duplication per the discretion note).
    (4) Trim any feature bullet that overstates relative to shipped behavior (verify each against
    code/docs before asserting). This is a first draft — the teardown (Plan 02) and codex pass
    (Plan 03) will surface remaining overstatements for Plan 04.
  </action>
  <verify>
    <automated>grep -q 'Sonarr' README.md && grep -q 'HMAC' README.md && grep -qi 'About This Fork' README.md && grep -q 'ipsingh06' README.md && ! grep -qi 'nitrobass24' README.md && ! grep -qi 'only fork\|sole fork\|only maintained' README.md && ! grep -qiE 'without cluttering|cleaner than push|avoids? (extra )?api calls|lighter.{0,12}than push' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README one-liner mentions both `Sonarr` and `HMAC`
    - README contains an `About This Fork` section that contains `ipsingh06` and does NOT contain `nitrobass24`
    - README does NOT contain "only fork", "sole fork", or "only maintained" (case-insensitive)
    - README does NOT advantage-frame the missing outbound-push: zero matches for "without cluttering", "cleaner than push", "avoids/avoid (extra) api calls", or "lighter than push" (case-insensitive); any mention of outbound-push reads as a chosen scope, not a quality differentiator
    - README contains a security selling-point referencing SECURITY.md, and EVERY security token asserted was re-verified against its source file (per <accuracy_note>): the *arr-URL guard is described as an IP-resolution guard (not generic "SSRF" or "outbound webhook validation"); rate-limiting is scoped to webhook/config/test-connection/bulk/status (not "all mutable endpoints")
    - Quick Start, Installation, Configuration, Usage Examples sections are preserved (not restructured)
  </acceptance_criteria>
  <done>First-draft README carries the owned-axis one-liner, honest fork note (no outbound-push bravado), and source-verified security selling point; structure preserved.</done>
</task>

<task type="auto">
  <name>Task 3: First-draft SECURITY.md posture, CONTRIBUTING.md freshen, CODE_OF_CONDUCT.md, CHANGELOG [1.4.0] (LAUNCH-04, LAUNCH-05, LAUNCH-06)</name>
  <read_first>
    - SECURITY.md (full current 45 lines — add a section, honesty-pass the contact)
    - CONTRIBUTING.md (full current ~44 lines — freshen, keep register)
    - CHANGELOG.md lines 1-30 ([1.3.0] entry is the structural/tonal model) and footer lines 168-175 (compare-link pattern)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (SECURITY.md posture pattern incl. the ACCURACY GUARD; CONTRIBUTING.md / CODE_OF_CONDUCT.md / CHANGELOG patterns + shared voice)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (D-08 posture knobs, D-09 community-health bar, D-11 changelog)
    - PER-CLAIM SOURCE VERIFICATION (re-open each before asserting the matching posture bullet — no claim from pattern memory; see <accuracy_note> for the exact lines):
      * Fernet at rest → src/python/common/encryption.py:73-87, 124-140
      * HMAC + 503 fail-closed → src/python/web/handler/webhook.py:55-60, 93-102
      * Bearer auth (api_token) → src/python/web/web_app.py:112-141
      * IP-resolution guard on *arr URLs → src/python/web/handler/config.py:54-84 (applied at :147)
      * CSP headers → src/python/web/web_app.py:143-153
      * rate limiting (the five decorator sites) → src/python/web/handler/{webhook.py:40-41, config.py:27/31/35, controller.py:73, status.py:16}; rate_limit decorator at src/python/web/rate_limit.py
    - docs/DEVELOPMENT.md (full build/test command list for the CONTRIBUTING freshen)
  </read_first>
  <action>
    SECURITY.md: keep the Supported Versions table and Reporting a Vulnerability sections. Add a
    new `## Security posture` section inserted between `## Reporting a Vulnerability` and
    `## Security Best Practices for Users`, with concise honest bullets per D-08, asserting ONLY
    what each per-claim source file confirms:
      - Fernet-encrypted secrets at rest (AES-128-CBC + HMAC-SHA256; 0600 keyfile on first enable).
      - HMAC-SHA256-verified import webhooks; opt-in `webhook_require_secret` fail-closed (503 when
        the requirement is on and no secret is set).
      - Bearer auth via opt-in `api_token` (constant-time compare; allow-all when unset for
        backward compat — important to set on non-loopback binds).
      - An IP-resolution guard on the user-supplied Sonarr/Radarr CONNECTION URLs, validated at
        test/connect time: the URL is resolved and rejected if it points to a private/loopback/
        reserved/link-local IP. Describe it HONESTLY as an IP-resolution SSRF guard, NOT a full
        SSRF library, and note SeedSyncarr RECEIVES webhooks (it does not send outbound webhooks).
        Match the honest framing in config.py:54-84 (DNS-rebinding/TOCTOU out of scope for a
        homelab tool). Do NOT write "outbound webhook calls validate destination URLs."
      - CSP headers on all responses (layered with Angular's hash-based meta CSP).
      - Rate limiting on the SPECIFIC endpoints that carry the decorator — enumerate them: webhook
        (60/60s), POST config-set (60/60s), Sonarr/Radarr test-connection (5/60s), bulk command
        (10/60s), status (60/60s). Do NOT write a blanket "all mutable endpoints are rate-limited";
        single-file command and autoqueue endpoints are NOT rate-limited.
    Close with the caveat "not a substitute for network isolation." Honesty-pass the reporting
    contact (line 18): add/replace with the GitHub private security advisory path
    `https://github.com/thejuran/seedsyncarr/security/advisories/new` as the standard maintained
    channel. Avoid verbatim duplication of the README selling-point wording.
    CONTRIBUTING.md: freshen per D-09 — Prerequisites add Poetry; Getting Started shows explicit
    `make run-tests-python` / `make run-tests-angular` invocations; add a Code Quality Checks block
    with `ruff check src/python`, `ruff format --check src/python`, `npm run lint`, and
    `make run-tests-e2e`; PR flow mentions ruff/lint as a pre-submit check matching the PR-template
    checklist. Keep the terse imperative register; no emojis.
    CODE_OF_CONDUCT.md: create at repo root using the canonical Contributor Covenant 2.1 text. Set
    the enforcement contact to the same channel SECURITY.md uses (GitHub security advisory link or
    maintainer email). No project-specific customization beyond name + contact.
    CHANGELOG.md: prepend a `## [1.4.0] - 2026-06-02` entry above [1.3.0] in Keep a Changelog
    format matching the [1.3.0] shape — a one-paragraph release summary (presentation + launch
    hardening: hardened public surface, community-health files, v1.4.0 documentation), a
    `### Security` subsection (documented security posture; config-set hardened to POST-only), and
    a `### Changed` subsection (README rewrite, SECURITY.md posture section, CONTRIBUTING.md
    freshen, CODE_OF_CONDUCT.md added, LICENSE.txt renamed to LICENSE). Add a `[1.4.0]` compare
    link to the footer following the existing pattern (`v1.3.0...v1.4.0`). Do not add a [1.3.0]
    compare link. This is a documentation entry only — it does NOT cut a tag.
  </action>
  <verify>
    <automated>grep -q '## Security posture' SECURITY.md && grep -q 'webhook_require_secret' SECURITY.md && grep -qi 'IP-resolution' SECURITY.md && ! grep -qi 'outbound webhook calls validate' SECURITY.md && ! grep -qi 'all mutable endpoints' SECURITY.md && grep -q 'ruff' CONTRIBUTING.md && grep -q 'make run-tests' CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md && grep -q 'Contributor Covenant' CODE_OF_CONDUCT.md && grep -q '## \[1.4.0\]' CHANGELOG.md && grep -q 'v1.3.0\.\.\.v1.4.0' CHANGELOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md contains `## Security posture` and the strings `webhook_require_secret` and `api_token`
    - SECURITY.md describes the *arr-URL guard as an IP-resolution guard (contains `IP-resolution`) and does NOT contain "outbound webhook calls validate" (the SSRF target is the *arr connection URLs, not outbound webhooks)
    - SECURITY.md rate-limiting bullet enumerates the actual endpoints (webhook / config-set / test-connection / bulk / status) and does NOT contain the blanket phrase "all mutable endpoints"
    - SECURITY.md contains the caveat phrase "not a substitute for network isolation"
    - SECURITY.md reporting section references the GitHub security advisory URL
    - Every posture bullet was re-verified against its cited source file before assertion (per <accuracy_note>); no bullet asserts a protection not present in the shipped source
    - CONTRIBUTING.md contains `ruff` and `make run-tests` invocations
    - CODE_OF_CONDUCT.md exists at repo root and contains `Contributor Covenant`
    - CHANGELOG.md contains `## [1.4.0]` and a `v1.3.0...v1.4.0` compare link in the footer
  </acceptance_criteria>
  <done>First-draft SECURITY/CONTRIBUTING/CoC/CHANGELOG land, all claims code-accurate (rate-limit + IP-resolution-guard wording corrected), ready for critique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → public reader | Public README/SECURITY.md are read by skeptical operators who may trust or disprove claims |
| docs → maintainer secrets | Example configs / contact info could leak real secrets or private deployment details |

## STRIDE Threat Register

This is a documentation-only plan (no production code, no runtime attack surface added). The
threat surface is claim accuracy and information disclosure in public docs.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | SECURITY.md / README security claims | mitigate | Every protection bullet is re-verified against its specific shipped-source file (webhook.py, config.py, web_app.py, encryption.py, rate_limit.py + the five rate-limit decorator sites) before assertion; the *arr-URL guard is framed honestly as an IP-resolution guard (not a full SSRF library, not "outbound webhook validation"); rate-limiting is scoped to the actual decorated endpoints (not "all mutable endpoints") — a false or overclaimed security statement a hostile reader disproves is itself a credibility risk |
| T-113-02 | Information Disclosure | example configs / contact in docs | mitigate | No real secrets, tokens, internal hostnames, or maintainer private deployment details in any draft; reporting contact uses the public GitHub security advisory channel |
| T-113-03 | Spoofing | SECURITY.md reporting channel | mitigate | Reporting path points to the maintained GitHub security advisory URL so reports reach the real maintainer |

No high-severity code-level threats exist for this docs-only plan — no code, configs, tests, or
dependencies are added or changed. Stated explicitly per the security gate rather than inventing
code threats that do not apply.
</threat_model>

<verification>
- `test -f LICENSE && test ! -f LICENSE.txt` — rename landed
- `! grep -rn 'LICENSE\.txt' README.md ACKNOWLEDGMENTS.md` — no stale user-facing license references
- `grep -q '## Security posture' SECURITY.md` — posture section present
- `grep -qi 'IP-resolution' SECURITY.md && ! grep -qi 'outbound webhook calls validate' SECURITY.md` — SSRF target named correctly
- `! grep -qi 'all mutable endpoints' SECURITY.md` — rate-limit scope not overclaimed
- `grep -q '## \[1.4.0\]' CHANGELOG.md` — changelog entry present
- `test -f CODE_OF_CONDUCT.md` — community-health file present
- Markdown integrity: drafts contain no broken relative links to repo files (LICENSE, SECURITY.md, CONTRIBUTING.md all resolve)
</verification>

<success_criteria>
- LICENSE renamed; README + ACKNOWLEDGMENTS license links fixed; no user-facing `LICENSE.txt` references remain
- First-draft README carries owned-axis one-liner + honest fork note (no outbound-push bravado) + source-verified security selling point
- SECURITY.md posture section present, rate-limiting scoped to actual endpoints, *arr-URL guard framed as IP-resolution, all claims code-accurate
- CONTRIBUTING.md freshened; CODE_OF_CONDUCT.md created; CHANGELOG [1.4.0] entry added
- All drafts ready as input to the teardown (Plan 02) and codex content pass (Plan 03)
</success_criteria>

<output>
Create `.planning/phases/113-presentation-launch-readiness/113-01-SUMMARY.md` when done.
Note in the SUMMARY: these are FIRST DRAFTS pending teardown + codex critique; finalization is Plan 04.
Record which security claims were re-verified against which source files (the per-claim source map).
</output>
</content>
</invoke>
