---
phase: 113-presentation-launch-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md
autonomous: true
requirements: [LAUNCH-01]
must_haves:
  truths:
    - "A written cynical r/selfhosted reader teardown of the current presentation exists as a phase artifact"
    - "The teardown critiques first impression, positioning credibility, install-path issues, and security oversell"
    - "The teardown ends with a prioritized list of fixes to address before the rewrite is finalized"
  artifacts:
    - path: ".planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md"
      provides: "Framed cynical-reader teardown driving the finalized rewrite"
      contains: "Priority Fixes Before Rewrite Finalization"
  key_links:
    - from: "113-TEARDOWN.md"
      to: "113-04-PLAN.md (finalize)"
      via: "prioritized fix list consumed at finalization"
      pattern: "Priority Fixes"
---

<objective>
Produce the framed cynical-reader teardown artifact (LAUNCH-01, D-10): a written
"r/selfhosted commenter" critique of SeedSyncarr's presentation — positioning, credibility, and
30-second first impression. This is the hostile-take-first input that drives what Plan 04
finalizes. It is captured as a written artifact BEFORE the rewrite is finalized.

Purpose: Pre-empt the harshest *fair* skeptical reviewer. The maintainer is sensitive to
"vibe-coded" criticism; the substance is genuinely strong (zero active functional bugs post-audit,
Fernet encryption, HMAC webhook, SSRF hardening, ~89% Python coverage), so the launch risk is
presentation underselling real quality, not hidden defects.
Output: 113-TEARDOWN.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md
@.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md
@reports/comparison-nitrobass24-seedsync-2026-06-02.md

<note>
This plan can run in parallel with Plan 01. The teardown targets the CURRENT presentation
(README/SECURITY/CONTRIBUTING as they exist today) plus the positioning constraints from CONTEXT.
The drafts from Plan 01 are NOT a prerequisite — the teardown critiques the existing surface and
the positioning decisions; the codex pass (Plan 03) is what reviews the Plan-01 drafts. Plan 04
addresses BOTH the teardown's prioritized fixes and the codex findings at finalization.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the cynical-reader teardown artifact (LAUNCH-01, D-10)</name>
  <read_first>
    - README.md (current 154 lines — the primary teardown target)
    - SECURITY.md (current 45 lines — for the security-claims critique)
    - CONTRIBUTING.md (current ~44 lines)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md (positioning constraints D-02/D-03, "vibe-coded" sensitivity, fork-honesty rule)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (113-TEARDOWN.md structure pattern + tone)
    - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md (tools-run-clean evidence + the HR findings a reader could surface)
    - reports/comparison-nitrobass24-seedsync-2026-06-02.md §9 (positioning + honesty guardrails the teardown should test the README against)
  </read_first>
  <action>
    Write `113-TEARDOWN.md` in the phase directory as a framed first-person r/selfhosted commenter
    critique — adversarial but fair, complete sentences a real commenter would write, not a bullet
    dump. No YAML front-matter; start with `# Cynical-Reader Teardown` and a `**Framing:**` line.
    Cover these sections (per the PATTERNS structure): First Impressions (above-the-fold claims,
    wordmark, screenshot, badges — note the License badge currently shows "No license" because of
    the LICENSE.txt naming, a credibility hit); Credibility Gaps (claims a skeptic would challenge:
    "prove it", fork-positioning bravado risk, sole-fork claims, blanket "*arr integration"
    overstatement); Install Path Issues (walk the Docker + pip steps as written — missing prereqs,
    version drift, anything causing a first-run failure); Security Claims (any unsupported or
    oversold assertion; whether the posture is stated plainly enough up front); What Actually Lands
    (the genuine positives a fair reader notes — substance is strong); and a closing numbered
    "Priority Fixes Before Rewrite Finalization" list. Hold the honesty guardrails: the teardown
    should flag IF the draft were to name the sibling fork or claim sole/only-fork. Lean on the
    Phase 110 tools-run-clean evidence (ruff 0, Semgrep 0, gitleaks 0) as legitimate counter-points
    a fair reader would credit.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md && grep -q 'Priority Fixes Before Rewrite Finalization' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md && grep -qi 'First Impressions' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-TEARDOWN.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 113-TEARDOWN.md exists in the phase directory
    - It contains the sections First Impressions, Credibility Gaps, Install Path Issues, Security Claims, What Actually Lands
    - It ends with a numbered "Priority Fixes Before Rewrite Finalization" list (at least 3 items)
    - It is framed first-person as an r/selfhosted commenter (contains `**Framing:**`)
    - It has no YAML front-matter (starts with `# Cynical-Reader Teardown`)
  </acceptance_criteria>
  <done>The cynical-reader teardown is captured as a written artifact with a prioritized fix list, ready to drive Plan 04 finalization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase artifact → finalization | The teardown's prioritized fixes drive the finalized public docs |

## STRIDE Threat Register

This plan produces a single internal phase artifact (a Markdown critique). It adds no production
code, no runtime surface, no dependencies, and modifies no public-facing file.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-04 | Information Disclosure | 113-TEARDOWN.md (internal artifact) | accept | The teardown is an internal `.planning/` artifact; it must not paste real secrets/tokens/internal hostnames while quoting the docs it critiques — low risk, the source docs already contain none |

No high-severity code-level threats exist for this artifact-only plan — no code, configs, tests,
or dependencies are added or changed. Stated explicitly per the security gate.
</threat_model>

<verification>
- `test -f .planning/.../113-TEARDOWN.md` — artifact exists
- `grep -q 'Priority Fixes Before Rewrite Finalization'` — prioritized fix list present
- Artifact critiques the current presentation surface, not an imagined one
</verification>

<success_criteria>
- Framed cynical-reader teardown captured as a written phase artifact before finalization
- Covers first impression, credibility, install path, and security oversell
- Ends with a prioritized fix list that Plan 04 consumes
</success_criteria>

<output>
Create `.planning/phases/113-presentation-launch-readiness/113-02-SUMMARY.md` when done.
</output>
