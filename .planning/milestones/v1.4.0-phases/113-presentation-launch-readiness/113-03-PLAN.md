---
phase: 113-presentation-launch-readiness
plan: 03
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md
autonomous: true
requirements: [LAUNCH-01]
must_haves:
  truths:
    - "A codex adversarial content pass over the Plan-01 drafted README/docs is captured as a written artifact before the rewrite is finalized"
    - "The pass flags technical-claims accuracy, broken/incomplete install steps, and unsupported assertions"
    - "Each finding has a severity and a fix/accept/defer disposition"
  artifacts:
    - path: ".planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md"
      provides: "Structured codex content-credibility findings over the drafts"
      contains: "## Findings"
  key_links:
    - from: "113-01 drafts (README.md, SECURITY.md)"
      to: "113-CODEX-PASS.md"
      via: "codex reviews the drafted docs"
      pattern: "README\\.md"
    - from: "113-CODEX-PASS.md"
      to: "113-04-PLAN.md (finalize)"
      via: "findings consumed at finalization"
      pattern: "Disposition"
---

<objective>
Run the codex adversarial CONTENT pass (LAUNCH-01, D-10) over the Plan-01 drafted README/docs and
capture the findings as a structured written artifact. This pass targets content credibility —
technical-claims accuracy, broken/incomplete install steps, unsupported assertions, doc/code
drift — and is SEPARATE from the orchestrator's per-phase codex plan review (which targets
engineering approach). It is captured BEFORE the rewrite is finalized (Plan 04 addresses it).

Purpose: A second adversarial lens (codex) on the actual drafted text, complementing the
cynical-reader teardown's positioning critique with a technical-accuracy audit.
Output: 113-CODEX-PASS.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-01-SUMMARY.md

<dependency_note>
Depends on Plan 01 (the drafted README.md, SECURITY.md, CONTRIBUTING.md, CHANGELOG.md,
CODE_OF_CONDUCT.md). This plan reads those drafts as they exist after Plan 01 lands.
</dependency_note>

<codex_invocation>
This task invokes the `/codex` adversarial pass against the drafted docs. If the codex tooling
is unavailable in the execution environment, the executor performs the equivalent adversarial
content audit directly (claim-by-claim verification against shipped source) and records that the
pass was run manually in the artifact's Methodology section. Either way the OUTPUT is the same:
a structured 113-CODEX-PASS.md findings artifact.
</codex_invocation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run codex content pass over the drafts and capture findings (LAUNCH-01, D-10)</name>
  <read_first>
    - README.md (the Plan-01 draft — primary pass target)
    - SECURITY.md (the Plan-01 draft — security-claims accuracy)
    - CONTRIBUTING.md (the Plan-01 draft — install/dev-command accuracy)
    - CHANGELOG.md (the Plan-01 draft — [1.4.0] entry accuracy)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (113-CODEX-PASS.md structure pattern, derived from 110-FINDINGS conventions)
    - src/python/web/handler/webhook.py, src/python/web/handler/config.py, src/python/web/web_app.py, src/python/common/encryption.py, src/python/web/rate_limit.py (source of truth for verifying every security/feature claim in the drafts)
    - docs/GETTING-STARTED.md, docs/CONFIGURATION.md, docs/DEVELOPMENT.md (cross-check install/quickstart/dev steps for drift)
  </read_first>
  <action>
    Invoke `/codex` as an adversarial content reviewer over the drafted README.md + SECURITY.md
    (+ CONTRIBUTING.md / CHANGELOG.md). The pass checks: (1) every install/quickstart step is
    reproducible as written (Docker compose snippet, `docker run`, `pip install seedsyncarr`,
    `apt install` deps, Python version requirement) — flag any broken/incomplete/version-drifted
    step; (2) every security claim is accurate to the shipped source (HMAC verify + 503 fail-closed,
    config-set POST-only, Fernet at rest, SSRF IP-resolution guard NOT over-claimed as a full SSRF
    library, Bearer auth opt-in, CSP, rate limiting); (3) no unsupported assertion (feature claims
    that exceed shipped behavior, fork-positioning that violates the honesty guardrails); (4)
    doc/code drift between README and docs/ pages. Capture results to `113-CODEX-PASS.md` in the
    phase directory using the PATTERNS structure: `# Codex Adversarial Content Pass — Phase 113`,
    `**Target:**` line, `## Methodology`, `## Findings` with `### CONTENT-NN` entries (each with
    Severity, File+line, Issue: exact claim vs reality, Disposition: fix/accept/defer), and a
    `## Summary` severity table. No YAML front-matter. If the pass finds zero high-severity issues,
    say so explicitly in the Summary rather than manufacturing findings.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -q '## Findings' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -qi 'Disposition' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -qi 'Methodology' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 113-CODEX-PASS.md exists in the phase directory
    - It contains a `## Methodology`, a `## Findings` section, and a `## Summary` severity table
    - Each finding has a Severity and a Disposition (fix / accept / defer)
    - Findings target content credibility (claims/install/assertions), not engineering approach
    - It has no YAML front-matter (starts with `# Codex Adversarial Content Pass`)
  </acceptance_criteria>
  <done>The codex content pass is captured as a structured findings artifact, ready to drive Plan 04 finalization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted docs → finalization | The codex findings drive corrections to the finalized public docs |

## STRIDE Threat Register

This plan produces a single internal phase artifact (structured findings). It adds no production
code, no runtime surface, no dependencies, and modifies no public-facing file.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Information Disclosure | 113-CODEX-PASS.md (internal artifact) | mitigate | The pass's core purpose is catching false/oversold security claims before they ship publicly — directly reduces T-113-01; the artifact itself must not quote real secrets while citing draft lines (the drafts contain none) |

No high-severity code-level threats exist for this artifact-only plan — no code, configs, tests,
or dependencies are added or changed. Stated explicitly per the security gate.
</threat_model>

<verification>
- `test -f .planning/.../113-CODEX-PASS.md` — artifact exists
- `grep -q '## Findings'` — structured findings present
- Each finding cites a draft file+line and a source-of-truth comparison
</verification>

<success_criteria>
- Codex content pass over the Plan-01 drafts captured as a structured artifact before finalization
- Flags claims accuracy, install-step issues, and unsupported assertions with dispositions
- Ready as input to Plan 04 finalization alongside the teardown
</success_criteria>

<output>
Create `.planning/phases/113-presentation-launch-readiness/113-03-SUMMARY.md` when done.
</output>
