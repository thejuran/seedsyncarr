---
phase: 113-presentation-launch-readiness
plan: 03
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md
autonomous: true
requirements: [LAUNCH-01]
must_haves:
  truths:
    - "A codex adversarial content pass over the Plan-01 drafted README/docs is captured as a written artifact before the rewrite is finalized"
    - "The pass flags technical-claims accuracy, broken/incomplete install steps, and unsupported assertions"
    - "Every documented install command/flag/image-tag/volume-path/entry-point is validated against the repo's actual Dockerfile, compose convention, and pyproject.toml — not merely read; in-sandbox-unrunnable steps are explicitly flagged as deferred runtime checks for the walkthrough"
    - "Each finding has a severity and a fix/accept/defer disposition"
    - "Decisions covered by this plan — D-10: hostile-take-first — the codex adversarial content pass over the drafts is captured as a written artifact (distinct from the per-phase codex plan review) before the rewrite is finalized"
  artifacts:
    - path: ".planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md"
      provides: "Structured codex content-credibility findings over the drafts, incl. an install-path reproducibility verification"
      contains: "## Findings"
  key_links:
    - from: "113-01 drafts (README.md, SECURITY.md)"
      to: "113-CODEX-PASS.md"
      via: "codex reviews the drafted docs"
      pattern: "README\\.md"
    - from: "113-CODEX-PASS.md"
      to: "113-04-PLAN.md (finalize)"
      via: "findings consumed at finalization"
      pattern: "Disposition"
---

<objective>
Run the codex adversarial CONTENT pass (LAUNCH-01, D-10) over the Plan-01 drafted README/docs and
capture the findings as a structured written artifact. This pass targets content credibility —
technical-claims accuracy, broken/incomplete install steps, unsupported assertions, doc/code
drift — and is SEPARATE from the orchestrator's per-phase codex plan review (which targets
engineering approach). It is captured BEFORE the rewrite is finalized (Plan 04 addresses it).

Purpose: A second adversarial lens (codex) on the actual drafted text, complementing the
cynical-reader teardown's positioning critique with a technical-accuracy audit — including an
install-path reproducibility verification against the repo's real build/packaging source.
Output: 113-CODEX-PASS.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CONTEXT.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md
@.planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-01-SUMMARY.md

<dependency_note>
Depends on Plan 01 (the drafted README.md, SECURITY.md, CONTRIBUTING.md, CHANGELOG.md,
CODE_OF_CONDUCT.md). This plan reads those drafts as they exist after Plan 01 lands.
</dependency_note>

<install_path_ground_truth>
The quickstart/install steps in the README MUST be validated against these actual source files
(the planner located them on 2026-06-02 — the executor verifies each documented value matches):
- Production image Dockerfile: `src/docker/build/docker-image/Dockerfile`
    * `EXPOSE 8800` (line 164) ↔ README `-p 8800:8800` / `"8800:8800"`
    * apt deps installed at lines 84-88: `lftp`, `p7zip` / `p7zip-full`, `unrar` ↔ README
      `sudo apt install lftp openssh-client p7zip-full unrar bzip2` (executor reconciles whether
      `openssh-client` and `bzip2` are actually required vs the Dockerfile list — flag any drift)
    * `CMD` block at line 156 ↔ runtime entry
- Production compose convention: `src/docker/stage/docker-image/compose.yml` (cross-check service/
  port/volume shape against the README Quick Start compose snippet)
- Python packaging: `src/python/pyproject.toml`
    * `name = "seedsyncarr"` ↔ README `pip install seedsyncarr`
    * `requires-python = ">=3.11,<3.13"` ↔ README "Requires Python 3.11 or 3.12"
    * `[project.scripts] seedsyncarr = "seedsyncarr:main"` ↔ README `seedsyncarr` run command
- Image name `ghcr.io/thejuran/seedsyncarr` and volume `~/.seedsyncarr:/root/.seedsyncarr` ↔
  confirm against the published package name / compose conventions above
</install_path_ground_truth>

<codex_invocation>
This task invokes the `/codex` adversarial pass against the drafted docs. If the codex tooling
is unavailable in the execution environment, the executor performs the equivalent adversarial
content audit directly (claim-by-claim verification against shipped source) and records that the
pass was run manually in the artifact's Methodology section. Either way the OUTPUT is the same:
a structured 113-CODEX-PASS.md findings artifact.
</codex_invocation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run codex content pass over the drafts and capture findings, incl. install-path reproducibility (LAUNCH-01, D-10)</name>
  <read_first>
    - README.md (the Plan-01 draft — primary pass target)
    - SECURITY.md (the Plan-01 draft — security-claims accuracy)
    - CONTRIBUTING.md (the Plan-01 draft — install/dev-command accuracy)
    - CHANGELOG.md (the Plan-01 draft — [1.4.0] entry accuracy)
    - .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-PATTERNS.md (113-CODEX-PASS.md structure pattern, derived from 110-FINDINGS conventions; ACCURACY GUARD + decorator map for the security-claims check)
    - src/python/web/handler/webhook.py, src/python/web/handler/config.py, src/python/web/web_app.py, src/python/common/encryption.py, src/python/web/rate_limit.py (source of truth for verifying every security/feature claim in the drafts)
    - INSTALL-PATH SOURCE (verify every documented install command/flag/tag/path against these — see <install_path_ground_truth>):
      * src/docker/build/docker-image/Dockerfile (EXPOSE 8800; apt deps lines 84-88; CMD line 156)
      * src/docker/stage/docker-image/compose.yml (production compose convention)
      * src/python/pyproject.toml (name, requires-python, [project.scripts] entry point)
    - docs/GETTING-STARTED.md, docs/CONFIGURATION.md, docs/DEVELOPMENT.md (cross-check install/quickstart/dev steps for drift)
  </read_first>
  <action>
    Invoke `/codex` as an adversarial content reviewer over the drafted README.md + SECURITY.md
    (+ CONTRIBUTING.md / CHANGELOG.md). The pass checks:
    (1) INSTALL-PATH REPRODUCIBILITY — every install/quickstart step is validated for runnability,
        NOT merely read. For each step, confirm the documented value matches the repo's actual
        build/packaging source (per <install_path_ground_truth>): the Docker image name
        `ghcr.io/thejuran/seedsyncarr`, the `8800` port (Dockerfile EXPOSE 8800 ↔ `-p 8800:8800` ↔
        compose `"8800:8800"`), the `~/.seedsyncarr:/root/.seedsyncarr` volume, the `seedsyncarr`
        console-script entry point (pyproject `[project.scripts]`), the Python version requirement
        (pyproject `requires-python = ">=3.11,<3.13"` ↔ "Python 3.11 or 3.12"), and the apt
        dependency list (`lftp openssh-client p7zip-full unrar bzip2` ↔ Dockerfile lines 84-88 —
        flag any package that is in the README but not the Dockerfile, or vice versa). Where
        literally executing a step is feasible in-sandbox (e.g. `docker compose config` schema
        validation of the snippet, `python -c` import of the entry point, parsing pyproject), DO it
        and record the result. Where running is NOT feasible in-sandbox (`pip install seedsyncarr`
        against live PyPI, `docker pull` of the ghcr image, full `docker run`), DO NOT hand-wave
        "walked the steps" — instead verify each command/flag/tag/path against source as above AND
        explicitly flag the step as a DEFERRED RUNTIME CHECK for the milestone-end walkthrough in
        the finding. Any value that does NOT match source is a fix-disposition finding.
    (2) SECURITY-CLAIM ACCURACY — every security claim is accurate to the shipped source (HMAC
        verify + 503 fail-closed; config-set POST-only; Fernet at rest; the *arr-URL guard
        described as an IP-resolution guard, NOT over-claimed as a full SSRF library and NOT as
        "outbound webhook validation"; Bearer auth opt-in; CSP; rate-limiting scoped to the five
        actual decorator sites, NOT a blanket "all mutable endpoints"). Flag any claim that
        exceeds source.
    (3) UNSUPPORTED ASSERTIONS — feature claims that exceed shipped behavior; fork-positioning that
        violates the honesty guardrails (naming the sibling, sole/only-fork, OR advantage-framing
        the missing outbound-push — "without cluttering"/"cleaner than push"/"avoids extra API
        calls").
    (4) DOC/CODE DRIFT — between README and docs/ pages.
    Capture results to `113-CODEX-PASS.md` in the phase directory using the PATTERNS structure:
    `# Codex Adversarial Content Pass — Phase 113`, `**Target:**` line, `## Methodology`,
    `## Findings` with `### CONTENT-NN` entries (each with Severity, File+line, Issue: exact claim
    vs reality, Disposition: fix/accept/defer; install-path findings note "verified against source"
    vs "deferred runtime check"), and a `## Summary` severity table. No YAML front-matter. If the
    pass finds zero high-severity issues, say so explicitly in the Summary rather than manufacturing
    findings.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -q '## Findings' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -qi 'Disposition' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -qi 'Methodology' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && grep -qiE 'pyproject|Dockerfile|entry.?point|deferred runtime' .planning/milestones/v1.4.0-phases/113-presentation-launch-readiness/113-CODEX-PASS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 113-CODEX-PASS.md exists in the phase directory
    - It contains a `## Methodology`, a `## Findings` section, and a `## Summary` severity table
    - Each finding has a Severity and a Disposition (fix / accept / defer)
    - The pass VALIDATES install-path runnability (not just reads): every documented command/flag/
      image-tag/volume-path/entry-point/python-version/apt-dep is reconciled against the actual
      Dockerfile (`src/docker/build/docker-image/Dockerfile`), compose convention
      (`src/docker/stage/docker-image/compose.yml`), and `src/python/pyproject.toml`; any mismatch
      is a fix finding
    - Any install step that cannot be executed in-sandbox (live `pip install` / `docker pull` /
      full `docker run`) is EXPLICITLY flagged as a deferred runtime check for the walkthrough — no
      hand-wave "walked the steps"
    - The security-claims check confirms the IP-resolution-guard and rate-limit-scope wording match source
    - Findings target content credibility (claims/install/assertions), not engineering approach
    - It has no YAML front-matter (starts with `# Codex Adversarial Content Pass`)
  </acceptance_criteria>
  <done>The codex content pass is captured as a structured findings artifact with install-path reproducibility verified against source (in-sandbox-unrunnable steps flagged as deferred runtime checks), ready to drive Plan 04 finalization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted docs → finalization | The codex findings drive corrections to the finalized public docs |

## STRIDE Threat Register

This plan produces a single internal phase artifact (structured findings). It adds no production
code, no runtime surface, no dependencies, and modifies no public-facing file.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Information Disclosure | 113-CODEX-PASS.md (internal artifact) | mitigate | The pass's core purpose is catching false/oversold security claims AND non-reproducible install steps before they ship publicly — directly reduces T-113-01; the artifact itself must not quote real secrets while citing draft lines (the drafts contain none) |

No high-severity code-level threats exist for this artifact-only plan — no code, configs, tests,
or dependencies are added or changed. Stated explicitly per the security gate.
</threat_model>

<verification>
- `test -f .planning/.../113-CODEX-PASS.md` — artifact exists
- `grep -q '## Findings'` — structured findings present
- `grep -qiE 'pyproject|Dockerfile|deferred runtime'` — install-path reproducibility verification present
- Each finding cites a draft file+line and a source-of-truth comparison
</verification>

<success_criteria>
- Codex content pass over the Plan-01 drafts captured as a structured artifact before finalization
- Flags claims accuracy, install-step reproducibility (validated against Dockerfile/compose/pyproject), and unsupported assertions with dispositions
- In-sandbox-unrunnable install steps flagged as deferred runtime checks for the walkthrough
- Ready as input to Plan 04 finalization alongside the teardown
</success_criteria>

<output>
Create `.planning/phases/113-presentation-launch-readiness/113-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
