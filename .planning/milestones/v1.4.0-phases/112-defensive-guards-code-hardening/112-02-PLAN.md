---
phase: 112-defensive-guards-code-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/seedsyncarr.py
  - src/python/tests/unittests/test_seedsyncarr.py
autonomous: true
requirements: [GUARD-01, GUARD-02, GUARD-06]
tags: [startup-warnings, security, logging, webhook, config-fallback, hardening]

must_haves:
  truths:
    - "When webhook_secret is empty and webhook_require_secret is True (fail-closed/503 state), the operator does NOT see the misleading 'accept requests from any caller' warning — only the accurate 503 warning"
    - "When webhook_secret is empty and webhook_require_secret is False (default open state), the operator still sees the 'accept any caller' warning (behavior unchanged)"
    - "The api_token and webhook startup warnings carry a consistent prominent [SECURITY] prefix so they are unmissable in the log stream"
    - "When startup falls back to the legacy ~/.seedsync config directory, the operator sees a warning in the configured log stream (it no longer fires before the logger exists and vanishes)"
    - "Existing startup-warning tests still pass (the assertEqual(3, ...) count holds for the default require_secret=False case)"
    - "Implements CONTEXT.md decisions D-04: (GUARD-01/02 confirm-the-gap + correctness condition), D-05: (accept-any-caller suppressed in fail-closed state), D-06: (GUARD-01 [SECURITY] prominence prefix, stays logging.warning), D-07: (GUARD-02 matrix tests), D-11: (GUARD-06 surface fallback warning post-logger), D-12: (GUARD-06 loud-warning path, not opt-in gate)"
  artifacts:
    - path: "src/python/seedsyncarr.py"
      provides: "Corrected _emit_startup_warnings condition + [SECURITY] prefixes + legacy-fallback warning emitted post-logger"
      contains: "not config.general.webhook_secret and not config.general.webhook_require_secret"
    - path: "src/python/tests/unittests/test_seedsyncarr.py"
      provides: "GUARD-02 matrix tests + GUARD-06 fallback-emission test"
      contains: "webhook_require_secret=True"
  key_links:
    - from: "_emit_startup_warnings accept-any-caller branch"
      to: "actual webhook runtime behavior (webhook.py:54-60 fail-closed 503)"
      via: "condition guards on `not webhook_secret and not webhook_require_secret`"
      pattern: "not config\\.general\\.webhook_secret and not config\\.general\\.webhook_require_secret"
    - from: "_parse_args legacy-fallback decision"
      to: "configured logger.warning after _create_logger"
      via: "flag/message threaded out of _parse_args, emitted in __init__ after line 81"
      pattern: "legacy_fallback_warning"
---

<objective>
Deliver GUARD-01 (prominence), GUARD-02 (warning correctness — the real defect), and GUARD-06 (legacy-fallback surfacing). All three are confirm-and-fix items on code that already exists in `seedsyncarr.py`, and all three land in the same operator-visible log stream. Every fix is "warn / observe, don't change default runtime behavior."

Purpose: A skeptical reader sees three "insecure-by-silence" tells today — a webhook warning that says "accept any caller" even when the endpoint actually fails closed with 503 (GUARD-02), security warnings that blend into the log noise (GUARD-01), and a legacy-config fallback that warns to an unconfigured root logger and vanishes (GUARD-06). This plan makes each posture accurate and visible without altering what the app actually does.

Output: A corrected `_emit_startup_warnings`, a `_parse_args` that surfaces its legacy-fallback decision through the configured logger, and the new GUARD-02 matrix + GUARD-06 emission tests. Test-first: write the failing GUARD-02/06 tests before the corresponding fix lands.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-CONTEXT.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md

<interfaces>
<!-- Verified from source. Executor uses these directly — no codebase exploration needed. -->

GUARD-01/02 target — _emit_startup_warnings (seedsyncarr.py:371-397):
  - Line 374: `if not config.general.webhook_secret:` → the accept-any-caller warning (375-378). THIS is the GUARD-02 bug.
  - Line 379: `if config.general.webhook_require_secret and not config.general.webhook_secret:` → the 503 warning (380-383). Already correct.
  - Line 384: `if not config.general.api_token:` → two warnings (385-392: no-token + bound-to-0.0.0.0). Text already accurate (0.0.0.0 hardcoded in web_app_job.py:27).
  - Line 393: `else:` → logger.info (394-397).
  All four warning strings currently start with "Security:". GUARD-01 changes them to a "[SECURITY]" prefix (D-06 discretion).

GUARD-06 target — legacy fallback (seedsyncarr.py:265-274), inside _parse_args (@staticmethod, returns `args` at line 274):
  - Lines 265-272: `if not os.path.exists(args.config_dir):` → `logging.warning(...)` (bare root logger, fires BEFORE _create_logger). This is the bug.
  - Call site: __init__:40 `args = self._parse_args(sys.argv[1:])`.
  - _create_logger runs at __init__:74; `logger.info("Debug mode is ...")` at __init__:81. Emit the fallback warning right after line 81 via the configured logger.

Webhook fail-closed truth (read-only confirmation) — web/handler/webhook.py:54-60: returns 503 when
require_secret is True and no secret is set, before body parse. This is why the unconditional
accept-any-caller warning is misleading in the empty+require_secret=True cell.

Test harness — test_seedsyncarr.py:
  - TestSeedsyncarr (line 13) holds the existing _parse_args tests (config_dir assertions at 22-39+).
  - TestSeedsyncarrStartupWarnings (line 198): _make_mock_config (201-208) already accepts
    webhook_require_secret=False default; test_startup_warns_both_when_both_empty asserts
    `assertEqual(3, mock_logger.warning.call_count)` at line 256.
  - Assertion style to mirror: build mock_logger=MagicMock(), call Seedsyncarr._emit_startup_warnings(mock_logger, mock_config),
    then inspect `[str(call) for call in mock_logger.warning.call_args_list]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing GUARD-02 matrix + GUARD-06 fallback tests (RED)</name>
  <read_first>
    - src/python/tests/unittests/test_seedsyncarr.py (TestSeedsyncarr at 13 for _parse_args tests; TestSeedsyncarrStartupWarnings at 198; _make_mock_config at 201-208; assertEqual(3,...) at 256)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (§test_seedsyncarr.py — the new-test shapes)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md (§GUARD-01/02 Test Impact; §GUARD-06 Test Coverage)
    - src/python/seedsyncarr.py (lines 265-274 _parse_args legacy block; 371-397 _emit_startup_warnings)
  </read_first>
  <files>src/python/tests/unittests/test_seedsyncarr.py</files>
  <behavior>
    - GUARD-02: `test_startup_require_secret_without_secret_does_not_warn_accept_any_caller` — with webhook_secret="" and webhook_require_secret=True, the warning call_args_list contains NO entry matching "any caller". (RED today: the unconditional condition fires it.)
    - GUARD-02: `test_startup_require_secret_without_secret_warns_503` — same config, the call_args_list DOES contain an entry matching "503". (Already passes today, but pins the matrix.)
    - GUARD-06: `test_parse_args_emits_legacy_fallback_warning` — when the configured config_dir is absent and ~/.seedsync exists, the operator gets a visible warning after the logger is configured. (RED today: warning goes to unconfigured root logger.)
  </behavior>
  <action>Extend `TestSeedsyncarrStartupWarnings` with the two GUARD-02 tests using the exact shapes in PATTERNS.md §test_seedsyncarr.py (call `_make_mock_config(webhook_secret="", api_token="configured", webhook_require_secret=True)`, assert against `[str(call) for call in mock_logger.warning.call_args_list]` — `assertFalse(any("any caller" in c ...))` for the suppression test, `assertTrue(any("503" in c ...))` for the 503 test). Per D-07, do NOT weaken the existing `test_startup_warns_both_when_both_empty` count assertion. For GUARD-06 (D-12), add a test that exercises the fallback decision: patch `os.path.exists` so the configured `--config_dir` returns False and `~/.seedsync` returns True, invoke the GUARD-06 surfacing path, and assert the operator-visible warning is produced. Match the chosen GUARD-06 mechanism from Task 3 — Option A (preferred): assert `_parse_args` returns a non-None legacy-fallback message in the second tuple element; if Task 3 instead uses stderr (Option B), assert via `capsys`/`assertLogs` accordingly. Because Task 3 implements Option A, write the test against the tuple return. Use a real path for `~/.seedsync` via `os.path.expanduser` mocking consistent with the existing _parse_args tests in TestSeedsyncarr. These tests MUST fail before Task 2/Task 3 land (RED) — except the 503 test which already passes.</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run ruff check tests/unittests/test_seedsyncarr.py && poetry run pytest tests/unittests/test_seedsyncarr.py -v -k "accept_any_caller or legacy_fallback" ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `poetry run ruff check tests/unittests/test_seedsyncarr.py` reports `All checks passed!`.
    - Running ONLY the new RED tests (`-k "accept_any_caller or legacy_fallback"`) FAILS before the implementation tasks — at least one assertion fails (the `test $? -ne 0` guard confirms a non-zero pytest exit). This proves the tests are meaningful, not vacuous.
    - The new tests reference `webhook_require_secret=True` and exercise the legacy-fallback path; they reuse `_make_mock_config` (no new mock infrastructure).
    - The existing `test_startup_warns_both_when_both_empty` (assertEqual 3) is left unchanged.
  </acceptance_criteria>
  <done>Three new tests exist; the GUARD-02 suppression test and the GUARD-06 fallback test fail (RED), ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix GUARD-02 warning correctness + GUARD-01 prominence (GREEN)</name>
  <read_first>
    - src/python/seedsyncarr.py (lines 371-397 — _emit_startup_warnings)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (§seedsyncarr.py — GUARD-02; the single condition change on line 374 + prefix guidance)
    - src/python/web/handler/webhook.py (lines 54-60 — the fail-closed 503 guard that makes the current warning misleading; read-only)
  </read_first>
  <files>src/python/seedsyncarr.py</files>
  <action>Per D-05 (GUARD-02 correctness): change the condition on line 374 from `if not config.general.webhook_secret:` to `if not config.general.webhook_secret and not config.general.webhook_require_secret:`. This makes the accept-any-caller warning fire ONLY when the endpoint actually accepts unauthenticated callers (empty secret AND require_secret off). The fail-closed cell (empty secret + require_secret True) then emits only the existing 503 warning (lines 379-383, unchanged). Do NOT touch the 503 condition or the api_token block logic. Per D-06 (GUARD-01 prominence): change the leading `"Security:"` token to a consistent `"[SECURITY]"` prefix on all four `logger.warning` strings in this function (the webhook accept-any-caller, the 503, the no-api-token, and the bound-to-0.0.0.0 messages) for visual distinctiveness in the log stream; keep them at `logger.warning` level (do NOT escalate to raising/exiting — GUARD-01's contract is "default behavior unchanged, posture no longer silent"). The `logger.info` else-branch (394-397) prefix may be aligned for consistency but stays at info level. Verify the existing `assertEqual(3, ...)` test still passes: for the default `require_secret=False` case the new condition `not webhook_secret and not require_secret` is True when secret is empty, so the count is preserved (RESEARCH Pitfall 3).</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run ruff check seedsyncarr.py && poetry run pytest tests/unittests/test_seedsyncarr.py -v -k "warns or accept_any_caller or 503 or api_token or both_empty or no_warning or info"</automated>
  </verify>
  <acceptance_criteria>
    - `poetry run pytest tests/unittests/test_seedsyncarr.py -v -k "warns or accept_any_caller or 503 or api_token or both_empty"` — ALL pass, including `test_startup_require_secret_without_secret_does_not_warn_accept_any_caller` (now GREEN) and the unchanged `test_startup_warns_both_when_both_empty` (count still 3).
    - `grep -n "not config.general.webhook_secret and not config.general.webhook_require_secret" seedsyncarr.py` matches exactly once (the corrected condition).
    - `grep -cE "\[SECURITY\]" seedsyncarr.py` returns ≥ 4 (the four warning strings carry the prefix).
    - `poetry run ruff check seedsyncarr.py` reports `All checks passed!`.
  </acceptance_criteria>
  <done>The accept-any-caller warning is suppressed in the fail-closed state, all four security warnings carry the [SECURITY] prefix, the existing count test still holds, ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the legacy ~/.seedsync fallback warning through the configured logger (GUARD-06, GREEN)</name>
  <read_first>
    - src/python/seedsyncarr.py (lines 38-82 __init__ ordering; 263-274 _parse_args legacy block; _create_logger 276-301)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (§seedsyncarr.py — GUARD-06; Option A pattern)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md (§GUARD-06 — Option A; Pitfall 4 on the @staticmethod return-type change)
  </read_first>
  <files>src/python/seedsyncarr.py</files>
  <action>Implement Option A (preferred per D-11, D-12 — do NOT implement the opt-in-gate alternative). Keep the auto-fallback behavior unchanged (silent reassignment of `args.config_dir = legacy_dir` still happens). In `_parse_args` (lines 263-274): initialize `legacy_fallback_warning: str | None = None` before the `if not os.path.exists(args.config_dir):` block; inside the inner `if os.path.exists(legacy_dir):`, replace the bare `logging.warning(...)` (lines 268-271) with assigning the formatted message string to `legacy_fallback_warning` (use `%`-formatting to build the final string, since it is emitted later without args), then keep `args.config_dir = legacy_dir`; change the return on line 274 from `return args` to `return args, legacy_fallback_warning`. Update the SINGLE call site at `__init__:40` to unpack: `args, legacy_fallback_warning = self._parse_args(sys.argv[1:])` (Pitfall 4: this is the only caller — but grep to confirm before editing). After `_create_logger` runs and `logger.info("Debug mode is ...")` is emitted (line 81), add: `if legacy_fallback_warning: logger.warning(legacy_fallback_warning)`. Do NOT remove the `import logging` (still used elsewhere). The warning now lands in the same configured log stream as the GUARD-01/02 warnings. Confirm no other code calls `_parse_args` (grep `_parse_args` across src/python excluding tests) — production has exactly one call site; the test file calls it directly and Task 1's tests assert the new tuple shape.</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run ruff check seedsyncarr.py && grep -rn "_parse_args" --include=*.py . | grep -v "def _parse_args" | grep -v tests/ && poetry run pytest tests/unittests/test_seedsyncarr.py -v</automated>
  </verify>
  <acceptance_criteria>
    - The grep for `_parse_args` callers (excluding the def and tests) returns exactly ONE line — the unpack at `__init__:40` — confirming the tuple-return change broke no other caller.
    - `poetry run pytest tests/unittests/test_seedsyncarr.py -v` — ALL pass, including the GUARD-06 `test_parse_args_emits_legacy_fallback_warning` (now GREEN) AND all pre-existing `_parse_args` tests in TestSeedsyncarr (which must now unpack the tuple — if any existing test asserts on the bare `args` return, it is updated to unpack `args, _`).
    - `grep -n "return args, legacy_fallback_warning" seedsyncarr.py` matches once; `grep -n "if legacy_fallback_warning" seedsyncarr.py` matches once (the post-logger emission).
    - `poetry run ruff check seedsyncarr.py` reports `All checks passed!`.
  </acceptance_criteria>
  <done>The legacy-fallback warning reaches the configured logger after _create_logger, auto-fallback behavior is preserved, the single call site unpacks the tuple, all _parse_args tests pass, ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ← startup log stream | The operator's only signal about insecure-by-default posture is the startup warning log lines |
| filesystem → config loader | A typo'd `--config_dir` silently falls back to a legacy on-disk config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02-01 | Repudiation / Information misrepresentation | `_emit_startup_warnings` misleading text | mitigate | GUARD-02 makes the accept-any-caller warning conditional so the operator is never told the endpoint is open when it actually fails closed with 503 — the warning matrix now matches runtime behavior exactly |
| T-112-02-02 | Information Disclosure | `[SECURITY]` prefix / warning text leaking secret values | mitigate | Warnings state posture only (presence/absence of a token/secret) — they interpolate NO secret values; GUARD-01 changes prominence prefix only, not content (CLAUDE.md: never log sensitive data) |
| T-112-02-03 | Spoofing / silent misconfig | legacy ~/.seedsync silent fallback | mitigate | GUARD-06 surfaces the fallback decision to the configured logger so loading a stale pre-fork config is no longer silent; the fallback message interpolates only directory paths (no secrets) |
| T-112-02-04 | Tampering | changing which config loads | accept | GUARD-06 preserves the auto-fallback behavior exactly (D-12) — only adds visibility; no change to WHICH config is loaded, so no new threat is introduced |
| T-112-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (stdlib + in-repo only); RESEARCH.md Package Legitimacy Audit = N/A |

No `high` threats remain open. All three GUARD items make an existing posture VISIBLE or ACCURATE without changing default runtime behavior; the legacy-fallback message and security warnings interpolate paths/posture only, never secret values.
</threat_model>

<verification>
- `poetry run ruff check .` (whole-tree from `src/python/`) — `All checks passed!`
- `poetry run pytest tests/unittests/test_seedsyncarr.py -v` — all green, including the 3 new GUARD-02/06 tests and all pre-existing startup-warning + _parse_args tests.
- GUARD-02 condition present exactly once; four `[SECURITY]`-prefixed warnings; legacy-fallback emitted post-logger; single `_parse_args` call site.
</verification>

<success_criteria>
- GUARD-01: api_token + webhook warnings carry a consistent prominent [SECURITY] prefix; still at logging.warning level; default behavior unchanged.
- GUARD-02: the accept-any-caller warning fires only when `not webhook_secret and not webhook_require_secret`; the fail-closed cell emits only the 503 warning; existing count test preserved.
- GUARD-06: the legacy-fallback warning reaches the configured logger; auto-fallback behavior preserved; opt-in-gate alternative NOT implemented.
- Full test_seedsyncarr.py green; ruff clean; Python fail_under ≥ 88 holds.
</success_criteria>

<output>
Create `.planning/phases/112-defensive-guards-code-hardening/112-02-SUMMARY.md` when done
</output>
