---
phase: 112-defensive-guards-code-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/controller/delete/delete_process.py
  - src/python/tests/unittests/test_controller/test_delete_process.py
autonomous: true
requirements: [GUARD-03]
tags: [delete, logging, error-handling, log-injection, hardening]

must_haves:
  truths:
    - "When a local shutil.rmtree fails (e.g. permission error), the failure is logged with context via logger.exception rather than silently swallowed by ignore_errors=True"
    - "The local delete remains best-effort and non-fatal — a failed rmtree does not raise out of run_once (the sync tool does not crash on a cleanup failure)"
    - "The interpolated filename in the failure log line is passed through sanitize_log_value (CR/LF/control-char safe, consistent with the SEC-01 delete cluster)"
    - "DeleteLocalProcess now has failure-path test coverage where none existed before"
  artifacts:
    - path: "src/python/controller/delete/delete_process.py"
      provides: "try/except OSError + logger.exception replacing ignore_errors=True in DeleteLocalProcess.run_once"
      contains: "except OSError"
    - path: "src/python/tests/unittests/test_controller/test_delete_process.py"
      provides: "TestDeleteLocalProcess failure-path regression test (rmtree raises → log record, no raise)"
      contains: "TestDeleteLocalProcess"
  key_links:
    - from: "DeleteLocalProcess.run_once rmtree failure"
      to: "self.logger.exception observable log record"
      via: "try/except OSError wrapping shutil.rmtree(file_path) with no ignore_errors"
      pattern: "except OSError"
    - from: "failure log line"
      to: "sanitize_log_value(self.__file_name)"
      via: "log-injection sanitizer from common barrel"
      pattern: "sanitize_log_value"
---

<objective>
Deliver GUARD-03: replace the silent `shutil.rmtree(file_path, ignore_errors=True)` in `DeleteLocalProcess.run_once` with a log-and-continue `try/except OSError` that records the failure via `self.logger.exception`, mirroring the in-repo `DeleteRemoteProcess.run_once` precedent. Add the failure-path regression test that the codebase currently lacks.

Purpose: Delete is the most destructive operation in a sync tool. A skeptical reader who checks the delete path immediately flags `ignore_errors=True` — a failed delete leaves the file on disk with zero signal to the user. GUARD-03 ensures a failed local delete leaves an observable log signal while preserving the best-effort, non-fatal contract.

Output: A logged delete-failure path in `delete_process.py` and a new `TestDeleteLocalProcess` failure-path test. Test-first: write the failing failure-path test before the fix lands.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-CONTEXT.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md

<interfaces>
<!-- Verified from source. Executor uses these directly — no codebase exploration needed. -->

GUARD-03 target — DeleteLocalProcess.run_once (controller/delete/delete_process.py:15-24):
    def run_once(self):
        file_path = os.path.join(self.__local_path, self.__file_name)   # __local_path/__file_name name-mangled
        self.logger.debug("Deleting local file {}".format(self.__file_name))
        if not os.path.exists(file_path):
            self.logger.error("Failed to delete non-existing file: {}".format(file_path))
        else:
            if os.path.isfile(file_path):
                os.remove(file_path)
            else:
                shutil.rmtree(file_path, ignore_errors=True)   # line 24 — GUARD-03 target

In-repo precedent — DeleteRemoteProcess.run_once (same file, lines 42-50): try { ssh.shell(...) } except SshcpError { self.logger.exception("Exception while deleting remote file") } — log-and-continue, no re-raise.

Imports — delete_process.py top (lines 1-7): import os / import shlex / import shutil / from typing import Optional / from common import AppOneShotProcess / from ssh import Sshcp, SshcpError.
sanitize_log_value IS exported from the common barrel (common/__init__.py:1 `from .types import ... sanitize_log_value`),
so extend the existing import to `from common import AppOneShotProcess, sanitize_log_value` — no new module-level import line needed.

Runtime Python is 3.11-slim (Dockerfile). `onexc` is 3.12+ ONLY — DO NOT use it (RESEARCH Pitfall 2). Use try/except OSError (3.11-safe).

Test file — test_controller/test_delete_process.py: imports only DeleteRemoteProcess (line 8); has only TestDeleteRemoteProcess (line 12);
assertLogs failure-path precedent at lines 73-81 (test_run_once_catches_sshcp_error_without_raising).
Add DeleteLocalProcess to the import and a new TestDeleteLocalProcess class.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing DeleteLocalProcess rmtree-failure test (RED)</name>
  <read_first>
    - src/python/tests/unittests/test_controller/test_delete_process.py (imports at 1-9; TestDeleteRemoteProcess setUp/tearDown at 23-38; assertLogs precedent at 73-81)
    - src/python/controller/delete/delete_process.py (DeleteLocalProcess at 9-24)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (section test_delete_process.py — the TestDeleteLocalProcess class shape and @patch ordering)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md (section GUARD-03 Test Gap + Test Pattern)
  </read_first>
  <files>src/python/tests/unittests/test_controller/test_delete_process.py</files>
  <behavior>
    - test_local_delete_logs_rmtree_failure: when shutil.rmtree raises OSError, DeleteLocalProcess.run_once() produces an ERROR-level log record on the "DeleteLocalProcess" logger AND does not raise. (RED today: ignore_errors=True swallows the error and emits no log.)
  </behavior>
  <action>Add DeleteLocalProcess to the import on line 8 (from controller.delete.delete_process import DeleteLocalProcess, DeleteRemoteProcess). Add a new TestDeleteLocalProcess(unittest.TestCase) class using the setUp/tearDown StreamHandler pattern from TestDeleteRemoteProcess (lines 23-38) but for the "DeleteLocalProcess" logger name. Add test_local_delete_logs_rmtree_failure using the shape in PATTERNS.md section test_delete_process.py: patch shutil.rmtree with side_effect=OSError("permission denied"), patch os.path.isfile to return False (so the rmtree branch is taken) and os.path.exists to return True (so the not-exists branch is skipped); construct DeleteLocalProcess(local_path="/fake", file_name="somefile"); wrap proc.run_once() in `with self.assertLogs("DeleteLocalProcess", level="ERROR"):` and assert it does not raise. Mind the @patch decorator order (inside-out: last decorator is first param after self). Patch the names as resolved in the module under test — os.path.isfile, os.path.exists, shutil.rmtree are referenced via the os/shutil module imports in delete_process.py, so patch shutil.rmtree, os.path.isfile, os.path.exists (or the controller.delete.delete_process.shutil/os path if module-local patching is required — verify which resolves by running the test). This test MUST fail before Task 2 (RED).</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run ruff check tests/unittests/test_controller/test_delete_process.py && poetry run pytest tests/unittests/test_controller/test_delete_process.py -v -k "rmtree_failure" ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - poetry run ruff check tests/unittests/test_controller/test_delete_process.py reports All checks passed!.
    - Running ONLY the new test (-k "rmtree_failure") FAILS before Task 2 — assertLogs raises AssertionError (no logs of level ERROR triggered) because ignore_errors=True swallows the error and logs nothing (the test $? -ne 0 guard confirms non-zero pytest exit). This proves the test is meaningful.
    - The test imports DeleteLocalProcess and adds a TestDeleteLocalProcess class; existing TestDeleteRemoteProcess tests are untouched.
  </acceptance_criteria>
  <done>TestDeleteLocalProcess exists with a failure-path test that fails (RED) because the current code swallows the error; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ignore_errors=True with logged try/except OSError (GREEN, GUARD-03)</name>
  <read_first>
    - src/python/controller/delete/delete_process.py (full file — DeleteLocalProcess.run_once at 15-24; DeleteRemoteProcess.run_once precedent at 42-50; imports 1-7)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (section delete_process.py — GUARD-03; the try/except target + import guidance)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md (section GUARD-03 — recommended approach; Pitfall 2 on onexc/3.11)
  </read_first>
  <files>src/python/controller/delete/delete_process.py</files>
  <action>Per D-08/D-09: extend the existing common import to `from common import AppOneShotProcess, sanitize_log_value` (sanitize_log_value is exported from the common barrel — confirmed). Replace line 24 (shutil.rmtree(file_path, ignore_errors=True)) with a `try: shutil.rmtree(file_path)` / `except OSError:` block whose body is `self.logger.exception("Failed to delete local directory: %s", sanitize_log_value(self.__file_name))`. Use %-style lazy logging args (project convention, ruff-clean) — NOT f-strings or .format() inside the log call. Use `except OSError` specifically (NOT bare except — CLAUDE.md) and logger.exception (records the traceback; D-08). DO NOT re-raise — best-effort, non-fatal delete contract preserved (D-09). DO NOT use onexc= (3.12+ only; runtime is 3.11-slim — RESEARCH Pitfall 2). DO NOT change the file-branch (os.remove) or the not-exists branch. The __file_name reference uses the name-mangled self.__file_name exactly as DeleteRemoteProcess does.</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run ruff check controller/delete/delete_process.py && ! grep -nE 'ignore_errors' controller/delete/delete_process.py && poetry run pytest tests/unittests/test_controller/test_delete_process.py -v</automated>
  </verify>
  <acceptance_criteria>
    - poetry run pytest tests/unittests/test_controller/test_delete_process.py -v — ALL pass, including test_local_delete_logs_rmtree_failure (now GREEN) and all pre-existing TestDeleteRemoteProcess tests.
    - grep -nE 'ignore_errors' controller/delete/delete_process.py returns nothing (the silent-swallow flag is gone); the leading ! makes the verify command succeed on no-match.
    - grep -n 'except OSError' controller/delete/delete_process.py matches once; grep -n 'sanitize_log_value' matches in both the import and the log call.
    - poetry run ruff check controller/delete/delete_process.py reports All checks passed!.
  </acceptance_criteria>
  <done>The local rmtree failure is logged via logger.exception with a sanitized filename, no longer swallowed; the delete stays best-effort (no re-raise); failure-path test green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → delete worker | A local delete may partially fail (permission/disk-full); the worker must observe and record the failure |
| operator-provided filename → log line | file_name comes from config/persisted state and is interpolated into a log message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03-01 | Repudiation | DeleteLocalProcess silent rmtree failure | mitigate | GUARD-03 replaces ignore_errors=True with logger.exception so a failed delete leaves an observable, traceback-bearing log record — the failure can no longer go unrecorded |
| T-112-03-02 | Tampering (log injection, CWE-117) | filename interpolated into the failure log line | mitigate | The filename is passed through sanitize_log_value() (Phase 101 SEC-01 helper) so a crafted CR/LF/control-char filename cannot forge a second log line — consistent with the existing delete cluster |
| T-112-03-03 | Denial of Service | exception escaping run_once and crashing the sync tool | accept | except OSError catches only the expected filesystem failure and does NOT re-raise (D-09); the best-effort, non-fatal contract is preserved exactly — no behavior change to the success path |
| T-112-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (stdlib shutil/os + in-repo sanitize_log_value only); RESEARCH.md Package Legitimacy Audit = N/A |

No `high` threats remain open. GUARD-03 adds observability to the destructive delete path and applies the established log-injection sanitizer; it introduces no new high-severity threat and does not change the delete outcome contract.
</threat_model>

<verification>
- poetry run ruff check . (whole-tree from src/python/) — All checks passed!
- poetry run pytest tests/unittests/test_controller/test_delete_process.py -v — all green, including the new failure-path test.
- No ignore_errors remaining in delete_process.py; except OSError + sanitize_log_value present.
- Python fail_under >= 88 holds (new DeleteLocalProcess coverage only adds lines covered).
</verification>

<success_criteria>
- GUARD-03: a failed local rmtree is logged via logger.exception (with a sanitized filename) rather than silently swallowed; the delete remains best-effort and non-fatal (no re-raise).
- DeleteLocalProcess has failure-path test coverage; full test_delete_process.py green; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/112-defensive-guards-code-hardening/112-03-SUMMARY.md` when done
</output>
