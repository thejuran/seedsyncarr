---
phase: 112-defensive-guards-code-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/common/app_process.py
  - .gitignore
autonomous: true
requirements: [GUARD-04, GUARD-05]
tags: [multiprocessing, spawn, pickling, gitignore, hardening]

must_haves:
  truths:
    - "The red test test_process_with_long_running_thread_terminates_properly passes under the spawn start method (macOS default) with no test deleted or skipped"
    - "The full test_app_process.py file passes under both spawn and fork start methods"
    - "AppProcess instances pickle successfully when .start() is invoked under spawn — no TypeError: cannot pickle '_thread.lock' object"
    - "Cross-process propagate_exception() and terminate() still function (Queue/Event survive the spawn boundary)"
    - "git status shows neither .orchestrator.json nor .playwright-mcp/ as untracked"
  artifacts:
    - path: "src/python/common/app_process.py"
      provides: "AppProcess.__getstate__ / __setstate__ stripping threading.Thread instances for spawn-safe pickling"
      contains: "def __getstate__"
    - path: ".gitignore"
      provides: ".orchestrator.json and .playwright-mcp/ ignore entries in the AI-tooling block"
      contains: ".orchestrator.json"
  key_links:
    - from: "AppProcess.__getstate__"
      to: "self.__dict__ threading.Thread values"
      via: "isinstance(v, threading.Thread) sweep that pops Thread instances only"
      pattern: "isinstance.*threading\\.Thread"
    - from: "LongRunningThreadProcess.start() under spawn"
      to: "ForkingPickler.dump of the process instance"
      via: "__getstate__ removes the unpicklable self.thread before pickling"
      pattern: "def __getstate__"
---

<objective>
Deliver GUARD-04 (the highest-blast-radius item — AppProcess is the base class for every worker process) and GUARD-05 (mechanical repo hygiene). GUARD-04 turns the pre-existing red test green under the spawn start method by adding `__getstate__`/`__setstate__` to `AppProcess` that strip `threading.Thread` instances from the pickle state. GUARD-05 adds two run-artifact entries to `.gitignore`.

Purpose: A new contributor running `poetry run pytest` on macOS (spawn default) currently hits `TypeError: cannot pickle '_thread.lock' object` immediately — the single most "vibe-coded" signal a skeptical reviewer encounters. Turning that green, plus hiding stray local tooling artifacts, removes two launch-visible tells.

Output: A spawn-safe `AppProcess` (behavior-preserving under both start methods) and two new `.gitignore` lines. No production subclass stores a `threading.Thread` in `__init__` (verified in RESEARCH.md A2), so there is no production behavioral change.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-CONTEXT.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md
@.planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md

<interfaces>
<!-- Verified from source. Executor uses these directly — no codebase exploration needed. -->

GUARD-04 — AppProcess.__init__ (src/python/common/app_process.py:41-48). DO NOT change these
lines; the Queue/Event they create MUST survive the spawn boundary:
    def __init__(self, name: str):
        self.__name = name           # name-mangled to _AppProcess__name
        super().__init__(name=self.__name)
        self.mp_logger = None
        self.logger = logging.getLogger(self.__name)
        self.__exception_queue = Queue()   # _AppProcess__exception_queue — DO NOT strip
        self._terminate = Event()           # DO NOT strip

`threading` is already imported (app_process.py:8). No new import needed.
`propagate_exception` ends at line 121; `run_init` (abstractmethod) starts at line 123.

Red test (the green target, DO NOT modify) — src/python/tests/unittests/test_common/test_app_process.py:174-180:
    @pytest.mark.timeout(5)
    def test_process_with_long_running_thread_terminates_properly(self):
        self.process = LongRunningThreadProcess()
        self.process.start()
        ...
LongRunningThreadProcess.__init__ (test file lines 57-60) sets self.thread = threading.Thread(...)
— that Thread is the sole unpicklable object (RESEARCH.md §GUARD-04 live repro, all 9 scenarios green).

GUARD-05 — .gitignore AI-tooling block (lines 21-26): header "# AI tooling (local only)" at line 21,
.agents/ (22), .aidesigner/* (23), !.aidesigner/.gitkeep (24), .claude/ (25), .mcp.json (26).
.bg-shell/ at line 36, .turingmind/ at line 61 — existing trailing-slash directory precedents.
Both .orchestrator.json and .playwright-mcp/ confirmed untracked in working tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .gitignore entries for run artifacts (GUARD-05)</name>
  <read_first>
    - .gitignore (the AI-tooling block at lines 21-26; .bg-shell/ at 36)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (§`.gitignore` — GUARD-05)
  </read_first>
  <files>.gitignore</files>
  <action>Per D-13: add two entries to the existing "# AI tooling (local only)" block. Insert `.orchestrator.json` (bare-filename pattern, matching `.mcp.json` at line 26) and `.playwright-mcp/` (trailing-slash directory pattern, matching `.bg-shell/` / `.turingmind/`) immediately after the `.mcp.json` line (line 26). Do not reorder or remove any existing entry. Pure repo hygiene — no code, no test impact.</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr && git check-ignore .orchestrator.json .playwright-mcp/ && git status --short | grep -E '^\?\? \.(orchestrator\.json|playwright-mcp)' ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .orchestrator.json .playwright-mcp/` prints both paths (exit 0 = matched by an ignore rule).
    - `git status --short` shows NEITHER `.orchestrator.json` nor `.playwright-mcp/` as untracked (the grep finds nothing → the `test $? -ne 0` succeeds).
    - `git status --short .gitignore` shows `.gitignore` as the only modified file from this task.
  </acceptance_criteria>
  <done>Both artifacts are git-ignored and no longer appear as untracked; .gitignore is the only file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make AppProcess spawn-safe via __getstate__/__setstate__ (GUARD-04)</name>
  <read_first>
    - src/python/common/app_process.py (full file — `__init__` 41-48, `propagate_exception` ends 121, `run_init` starts 123)
    - src/python/common/multiprocessing_logger.py (lines 49-80 — the INFRA-01 `__getstate__`/`__setstate__` analog shape)
    - src/python/tests/unittests/test_common/test_app_process.py (the red test at line 175; LongRunningThreadProcess at 57-66)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-RESEARCH.md (§GUARD-04 — Live Repro Findings, including Pitfall 1)
    - .planning/milestones/v1.4.0-phases/112-defensive-guards-code-hardening/112-PATTERNS.md (§`app_process.py` — GUARD-04; the verified target pattern at lines 78-97)
  </read_first>
  <files>src/python/common/app_process.py</files>
  <behavior>
    - The red test `test_process_with_long_running_thread_terminates_properly` (test_app_process.py:175) passes under spawn — `LongRunningThreadProcess().start()` no longer raises `TypeError: cannot pickle '_thread.lock' object`.
    - All other AppProcess scenarios still pass: `test_exception_propagates` (Queue survives — parent receives the child's exception via the un-stripped `_AppProcess__exception_queue`), `test_exception_propagates_with_traceback`, `test_process_terminates`, `test_init_called_before_loop`, `test_cleanup_called_after_loop`, `test_long_running_process_is_force_terminated`, and the AppOneShotProcess scenarios.
    - Both fork (Linux/CI default) and spawn (macOS default) start methods pass the full file.
  </behavior>
  <action>Per D-01/D-02/D-03 and the RESEARCH.md live repro (D-02 answered: `__getstate__`/`__setstate__` are required AND sufficient; spawn-context Queue/Event is neither needed nor sufficient): add two methods to the `AppProcess` class, placed after `propagate_exception` (ends line 121) and before the `run_init` abstractmethod (line 123). `__getstate__` copies `self.__dict__`, builds a list of keys whose value satisfies `isinstance(v, threading.Thread)`, pops each, and returns the reduced state dict. `__setstate__` does `self.__dict__.update(state)`. Use the exact shape in PATTERNS.md §`app_process.py` target pattern (lines 78-97), including the docstring noting that subclasses storing Thread objects in `__init__` must re-create them in `run_init()`. CRITICAL (RESEARCH.md Pitfall 1): the sweep is a generic `isinstance(v, threading.Thread)` check — do NOT strip `_AppProcess__exception_queue` or `_terminate` (they are not Thread instances and must survive for cross-process `propagate_exception()`/`terminate()`). Do NOT change the `from multiprocessing import Process, Queue, Event` import (line 5). Do NOT call `set_start_method` anywhere (D-03). `threading` is already imported — add no new imports. Return type hint `-> dict` on `__getstate__` and `-> None` on `__setstate__` (project convention: type hints on public signatures).</action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run ruff check common/app_process.py && poetry run python -c "import multiprocessing as mp; assert mp.get_start_method(allow_none=True) in (None,'spawn'), mp.get_start_method()" && poetry run pytest tests/unittests/test_common/test_app_process.py -v</automated>
  </verify>
  <acceptance_criteria>
    - On this macOS dev host (Python 3.12, spawn is the default start method), `poetry run pytest tests/unittests/test_common/test_app_process.py -v` reports ALL tests passing including `test_process_with_long_running_thread_terminates_properly` — 0 failed, 0 skipped, 0 errors.
    - Fork-parity check (Linux/CI default) passes: `cd src/python && poetry run python -c "import multiprocessing as mp; mp.set_start_method('fork', force=True); import pytest, sys; sys.exit(pytest.main(['tests/unittests/test_common/test_app_process.py','-q']))"` exits 0.
    - `poetry run ruff check common/app_process.py` reports `All checks passed!` (0 findings).
    - No test in `test_app_process.py` is deleted, skipped, or marked xfail (verify with `git diff --stat tests/unittests/test_common/test_app_process.py` showing 0 changed lines — the test file is NOT modified by this task).
    - The diff to `app_process.py` adds only `__getstate__`/`__setstate__`; the `__init__` Queue/Event lines (47-48) and the multiprocessing import (line 5) are unchanged.
  </acceptance_criteria>
  <done>The red test goes green under spawn, the full file passes under both fork and spawn, ruff is clean, no test deleted/skipped, and only the two new methods were added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent process → spawn child | The AppProcess instance is pickled across the process boundary when `.start()` is called under spawn |
| repo working tree → git index | Untracked local artifacts could be accidentally staged/committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01-01 | Tampering / DoS | `AppProcess.__getstate__` over-stripping | mitigate | Sweep strips ONLY `threading.Thread` instances; Queue/Event explicitly retained (RESEARCH Pitfall 1) so cross-process exception propagation and terminate signalling remain intact — no silent loss of the kill switch |
| T-112-01-02 | Information Disclosure | `.orchestrator.json` / `.playwright-mcp/` accidental commit | mitigate | GUARD-05 adds `.gitignore` entries so local orchestration state cannot be inadvertently published to the public repo |
| T-112-01-03 | Elevation / Repudiation | spawn fix changing default start method | accept | D-03 forbids global `set_start_method`; the fix is local to AppProcess pickle state only — platform default start method is unchanged, so no new global behavior is introduced |
| T-112-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (stdlib `multiprocessing`/`threading` only); RESEARCH.md Package Legitimacy Audit = N/A |

No `high` threats remain open. Both GUARD items REDUCE attack surface (GUARD-05) or are behavior-preserving correctness fixes (GUARD-04); neither introduces a new high-severity threat.
</threat_model>

<verification>
- `poetry run ruff check .` (whole-tree from `src/python/`) — `All checks passed!`
- `poetry run pytest tests/unittests/test_common/test_app_process.py -v` green under spawn (macOS default).
- Fork-parity: same file green under a forced `fork` start method.
- `git check-ignore .orchestrator.json .playwright-mcp/` matches both; neither appears in `git status --short`.
- No test deleted or skipped (GUARD-04 non-negotiable acceptance bar).
</verification>

<success_criteria>
- GUARD-04: `test_process_with_long_running_thread_terminates_properly` passes under spawn, full `test_app_process.py` passes under both fork and spawn, nothing deleted/skipped.
- GUARD-05: both run artifacts are git-ignored and no longer untracked.
- ruff clean on touched files; no production behavioral change (no production subclass stores a Thread in `__init__`).
</success_criteria>

<output>
Create `.planning/phases/112-defensive-guards-code-hardening/112-01-SUMMARY.md` when done
</output>
