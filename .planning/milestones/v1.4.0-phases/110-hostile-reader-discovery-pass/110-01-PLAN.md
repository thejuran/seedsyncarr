---
phase: 110-hostile-reader-discovery-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md
autonomous: false
requirements: [SCAN-01, SCAN-02]

must_haves:
  truths:
    - "A maintainer can open 110-FINDINGS.md and read, in one glance, what a skeptical r/selfhosted engineer would flag — ranked by severity with a top-of-file summary rollup (SCAN-01)."
    - "Every finding in the artifact carries exactly one disposition: FOLD into a v1.4.0 fix phase (with target 111|112|113) or PARK with a one-line rationale (SCAN-02)."
    - "Each of the five audit tools (ruff, Semgrep/Shield, gitleaks, pip-audit, npm audit) has a recorded result line, and the AppProcess red test has a recorded run result."
    - "The six pre-named fix items (config-set GET->POST, GUARD-01..06 cluster) are each confirmed-and-recorded as already-folded with their target phase — confirm-only, not re-litigated."
    - "The maintainer has reviewed and approved the fold/park dispositions, since they gate the fix scope of Phases 111-113."
  artifacts:
    - path: ".planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md"
      provides: "Triaged, severity-ranked hostile-reader findings artifact with per-finding fold/park dispositions and a tooling-result rollup"
      contains: "## Summary"
      min_lines: 80
  key_links:
    - from: "110-FINDINGS.md summary rollup"
      to: "per-finding disposition lines"
      via: "severity + disposition counts that sum to the total finding count"
      pattern: "FOLD|PARK"
    - from: "110-FINDINGS.md"
      to: "Phases 111/112/113 planning"
      via: "FOLD-to-phase dispositions consumed as fix-scope input"
      pattern: "Phase (111|112|113)"
---

<objective>
Conduct the bounded hostile-reader discovery pass and produce the single triaged findings
artifact `110-FINDINGS.md`. Answer one question with evidence: "what would a skeptical
r/selfhosted engineer flag if they browsed this public repo today?" — then give every finding an
explicit fold-into-fix-phase or parked disposition so the fix scope for Phases 111-113 is explicit
and traceable.

Purpose: This phase GATES the fix scope for Phase 111 (config-set GET->POST) and Phase 112
(defensive guards). Its output is the input to their detailed planning. A near-clean tooling result
is itself a launch-positive outcome to record, not a problem.

Output: ONE Markdown artifact — `110-FINDINGS.md` in the phase directory. No production code, no
config, no test, no `.gitignore`, no docs changes land in this phase (those are Phases 111/112/113).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — read in full before starting; RESEARCH.md already verified every tool run.
@.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-RESEARCH.md
@.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-CONTEXT.md
@.planning/REQUIREMENTS.md

# Cross-reference baseline — CITE entries by description/section; do NOT mutate this file.
@.planning/codebase/CONCERNS.md
</context>

<constraints>
HARD BOUNDARIES for this phase (from CONTEXT.md D-01..D-09 and the planning brief):

- No production code, ever. No source, config, test, `.gitignore`, or docs changes land here.
  The ONLY file this plan writes is `110-FINDINGS.md`. Fixes flow into Phases 111/112/113.
- All tool runs are read-only / report-only (D-02). Never pass `--fix`, `--autofix`, or any
  mutating flag to any tool. Running a tool that would rewrite source is a hard failure of the pass.
- Do NOT edit `.planning/codebase/CONCERNS.md` (D-05). It is owned by `/gsd:map-codebase`. The pass
  cross-references its entries by description/section/line — it never mutates it.
- The six pre-named fix items are locked / confirm-only (D-08). Confirm each is real and correctly
  targeted and record it as already-folded with its phase. Do NOT re-litigate or down-scope
  D-2/D-4/D-7. The pass's additive value is surfacing NEW findings and giving everything an explicit
  disposition.
- No release/tag/version work. Out of scope for every phase in this milestone.
- RESEARCH.md is authoritative for tool output. The researcher already ran every tool in this
  session and captured exact output. Re-run them to record current state, but the expected results
  (ruff 0, Semgrep 0, gitleaks 0, pip-audit 2 pip-binary CVEs, npm audit 4 moderate devDep CVEs,
  AppProcess test FAILED) are documented in RESEARCH.md sections 1 and 5 — use them to validate runs.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run the read-only audit tool suite and capture current output</name>
  <files>(no files modified — capture tool output to working notes for Task 2; nothing written to disk yet except your notes)</files>
  <read_first>
    - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-RESEARCH.md (section 1 exact tool invocations + verified expected output; section 5 the AppProcess red test; section 8 environment availability)
    - src/python/pyproject.toml (ruff dev dep + fail_under posture)
    - src/angular/package.json (npm devDependency posture for the npm audit dep-chain reading)
    - .gitleaks.toml (allowlist config gitleaks must run with)
  </read_first>
  <action>
    Run each tool below EXACTLY as specified in RESEARCH.md section 1, all read-only (D-02 — never
    add `--fix` or any mutating flag). Capture each tool's full current stdout/stderr to your working
    notes so Task 2 can transcribe a one-line result per tool plus relevant detail.

    1. ruff whole-tree: run `ruff check src/python/` from repo root (the exact CI lint-gate command;
       whole-tree, not scoped). Expected per RESEARCH.md 1.1: "All checks passed!" — 0 findings.
    2. Shield SAST + secrets + dep audit (D-01). Per RESEARCH.md 1.2 you may use either the
       `/shield:shield quick` orchestrator OR the individual scripts under `shield-claude-skill/scripts/`
       (run-sast.sh, run-secrets.sh, run-sca.sh) — Claude's discretion per CONTEXT.md. At minimum
       capture the Semgrep SAST result and the gitleaks secret-scan result. For broader coverage also
       run `semgrep scan src/python --config=auto` (320 auto-rules). Expected: Semgrep 0 findings (320
       rules / 92 files); gitleaks "no leaks found" across full history. Run gitleaks with the repo
       allowlist: `gitleaks detect --source . --config .gitleaks.toml --no-banner`.
    3. pip-audit against the Poetry venv (NOT bare — see RESEARCH.md Pitfall 4). One-liner:
       `PIPAPI_PYTHON_LOCATION="$(poetry -C src/python env info --path)/bin/python" pip-audit`.
       Expected: 2 CVEs in the `pip` binary itself (CVE-2026-3219, CVE-2026-6357), NOT in any app
       dependency. Record which package the CVEs are in (it is `pip`, a build tool, not a runtime dep).
    4. npm audit against the Angular tree: `npm audit --prefix src/angular`. Expected: 4 moderate CVEs
       (ws via karma; brace-expansion via eslint) — ALL devDependencies. Record the `via:` dep chain
       for each so Task 2 can justify the PARK disposition (devDeps are not in the production Docker
       image; only the built static SPA ships). Per RESEARCH.md assumption A2, sanity-check these are
       devDep-only and not in `ng build` output before recording the PARK.
    5. The AppProcess red test (RESEARCH.md section 5): run
       `poetry -C src/python run pytest tests/unittests/test_common/test_app_process.py -v` from repo
       root. Expected: `test_process_with_long_running_thread_terminates_properly` FAILS with
       `TypeError: cannot pickle '_thread.lock' object` (macOS spawn start method). Capture the exact
       failing test id and error string — Task 2 records this as the highest-visibility "vibe-coded" tell.

    Do NOT write the artifact yet. This task only captures evidence. If any tool's actual output
    differs materially from RESEARCH.md's verified expectation (e.g. ruff now reports findings, or a
    new app-dependency CVE appears), note the delta prominently — it becomes a NEW finding in Task 2.
  </action>
  <verify>
    <automated>ruff check src/python/ ; echo "ruff exit: $?"</automated>
    Manual: confirm a result line is captured for each of ruff, Semgrep/Shield, gitleaks, pip-audit,
    npm audit, and the AppProcess pytest run in your working notes.
  </verify>
  <acceptance_criteria>
    - A current result is captured for each of: ruff (whole-tree), Semgrep/Shield SAST, gitleaks
      secret-scan, pip-audit (against the Poetry venv), and npm audit (src/angular).
    - The AppProcess test was run and its result captured, including the exact failing test id
      (`test_process_with_long_running_thread_terminates_properly`) and the `TypeError: cannot pickle
      '_thread.lock' object` message.
    - For npm audit, the `via:` dependency chain is captured for each finding (to support the
      devDep-only PARK justification in Task 2).
    - No tool was invoked with `--fix` or any mutating flag; `git status --porcelain` shows no
      modified tracked source/config/test files attributable to this task (D-02).
  </acceptance_criteria>
  <done>
    Every tool in D-01 plus the AppProcess red test has been run read-only and its current output
    captured to working notes, ready for transcription into the artifact. Any material delta from
    RESEARCH.md's verified expectations is flagged for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Read entry points under launch framing, confirm pre-named fix items, write 110-FINDINGS.md</name>
  <files>.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md</files>
  <read_first>
    - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-RESEARCH.md (section 2 CONCERNS.md cross-reference table; section 3 the artifact schema — severity scheme, HR- ID prefix, per-finding fields, summary rollup; section 4 pre-named fix-scope file:line evidence; section 6 LICENSE gap; section 7 finding-list skeleton HR-01..HR-12)
    - .planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-CONTEXT.md (D-03 fold bar, D-04 park rule, D-05..D-09 artifact home/structure/pre-named-scope/LICENSE)
    - .planning/codebase/CONCERNS.md (the prior-art baseline to CITE per finding — do NOT mutate)
    - .planning/REQUIREMENTS.md (Future Requirements DEFER-* + Out of Scope — the pre-parked items each NEW finding's PARK rationale may reference)
    - README.md (first-impression read — what a hostile reader sees before any code)
    - src/python/seedsyncarr.py (entry point; _emit_startup_warnings; legacy ~/.seedsync fallback)
    - src/python/web/handler/config.py (config-set GET endpoint — the SEC-09 credential-in-URL surface)
    - src/python/controller/delete/delete_process.py (ignore_errors=True delete swallow)
    - src/python/common/app_process.py (Queue()/Event() default-context — the spawn red test root cause)
  </read_first>
  <action>
    Synthesize the Task 1 evidence plus a launch-framed read of the entry points / high-traffic files
    / README into `110-FINDINGS.md` at the phase directory. Follow the schema in RESEARCH.md section 3
    (Claude's discretion on label/prefix per CONTEXT.md, but stay consistent). Structure in this order:

    1. A `## Summary` rollup at the TOP (D-07): a severity table (Critical/High/Medium/Low rows with
       Count / Fold / Park columns and a Total row); a "Fold destinations" breakdown (count per Phase
       111 / 112 / 113); a "Parked" count; and a "Tools run clean" block with one result line each for
       ruff, Semgrep, gitleaks, pip-audit, npm audit (the launch-positive evidence). The counts in the
       summary MUST sum to the actual number of finding entries below.
    2. Severity-grouped finding sections (D-06). Each finding uses the per-finding field block from
       RESEARCH.md 3.3: a stable ID (use the `HR-` prefix, two-digit sequence), a one-line title,
       Severity, Location file:line, Source (cite CONCERNS.md section / SEC-09 / this pass / D-09 where
       applicable), a 1-2 sentence hostile-reader framing (what they see and why they hold it against
       the project), and exactly ONE Disposition line in the form `FOLD -> Phase 111|112|113` OR
       `PARK -- <one-line rationale>`.
    3. The six pre-named fix items recorded as ALREADY-FOLDED, confirm-only (D-08), each with the
       file:line evidence from RESEARCH.md section 4 — do NOT re-litigate scope:
       - Config-set GET endpoint credential leak (SEC-09): config.py route + handler, Angular caller,
         E2E page object + setup script -> FOLD Phase 111.
       - GUARD-01 / GUARD-02 startup security warnings: per RESEARCH.md section 4, these are
         SUBSTANTIALLY ALREADY IMPLEMENTED in seedsyncarr.py `_emit_startup_warnings` with tests; record
         as a confirm-the-gap item (warning prominence/discoverability, not existence) -> FOLD Phase
         112. Do NOT characterize them as unimplemented.
       - GUARD-03 delete-path `ignore_errors=True` swallow: delete_process.py -> FOLD Phase 112.
       - GUARD-04 AppProcess spawn red test: cite test_app_process.py and the `TypeError: cannot pickle
         '_thread.lock' object` from Task 1; mark HIGH severity (a red test in a public CI suite is the
         single most launch-visible "vibe-coded" tell) -> FOLD Phase 112.
       - GUARD-05 `.gitignore` missing `.orchestrator.json` + `.playwright-mcp/` -> FOLD Phase 112.
       - GUARD-06 legacy `~/.seedsync` fallback: per RESEARCH.md section 4 the warning EXISTS but fires
         pre-logger-setup so may not surface; record as a narrow-gap confirm -> FOLD Phase 112.
    4. The LICENSE finding (D-09), correctly characterized per RESEARCH.md section 6: `LICENSE.txt`
       EXISTS (Apache 2.0 content present) but GitHub license detection does not recognize the `.txt`
       suffix, so the fix is a RENAME `LICENSE.txt` -> `LICENSE` (plus README link update), NOT a
       create-from-scratch. Disposition FOLD Phase 113 (LAUNCH-05).
    5. NEW dependency-audit findings: pip-audit (2 `pip`-binary CVEs — PARK: pip is a build tool, not a
       runtime/app dependency, not in the deployed container) and npm audit (4 moderate devDep CVEs —
       PARK: devDependencies only via karma/eslint chains, not present in the production Docker image /
       built SPA). Reference REQUIREMENTS.md framing where it helps.
    6. The pre-parked latent items (D-04): the DEFER-* items (DEFER-SHUTDOWN, DEFER-STREAMQUEUE,
       DEFER-TESTHARDEN, DEFER-WEBOB) and Out-of-Scope items (NAS-QEMU, dual-GET config-set) — each
       recorded with PARK plus a rationale referencing the REQUIREMENTS.md DEFER-* / Out-of-Scope entry.
    7. Optionally (Claude's discretion per CONTEXT.md) a brief "What was checked and came up clean"
       appendix consolidating the launch-positive tooling evidence (ruff/Semgrep/gitleaks clean, ~89%
       Python coverage, Fernet-at-rest, shlex.quote shell discipline, Bearer auth + HMAC + CSP + rate
       limiting).

    The HR-01..HR-12 skeleton in RESEARCH.md section 7 is a STARTING POINT, not the final list — refine
    IDs, severity, and phrasing against your actual Task 1 output and launch-framed read, and ADD any
    new finding the read surfaces. Every finding MUST end with exactly one disposition line (SCAN-02);
    nothing may be left undispositioned.

    Do NOT inline full source files into the artifact — cite file:line and short signatures/strings
    (e.g. the failing test id, the `ignore_errors=True` call, the GET route pattern) only.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md; test -f "$F" && grep -q "^## Summary" "$F" && echo "ARTIFACT+SUMMARY OK"</automated>
    <automated>F=.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md; HEADINGS=$(grep -cE '^### (HR-|SCAN-F|F-)[0-9]' "$F"); DISP=$(grep -cE '^(FOLD|PARK|\*\*Disposition).*(FOLD|PARK)' "$F"); echo "findings=$HEADINGS dispositions=$DISP"; [ "$HEADINGS" -gt 0 ] && [ "$DISP" -ge "$HEADINGS" ] && echo "SCAN-02 OK every finding dispositioned"</automated>
    <automated>F=.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md; for t in ruff semgrep gitleaks pip-audit npm; do grep -qi "$t" "$F" || echo "MISSING tool result line: $t"; done; echo "tool-line check done"</automated>
    <automated>git status --porcelain -- ':!.planning' | grep -vE '^\?\?' && echo "WARN tracked non-planning files modified (D-02 violation)" || echo "D-02 OK no production files modified"</automated>
  </verify>
  <acceptance_criteria>
    - `110-FINDINGS.md` exists in the phase directory and opens with a `## Summary` rollup containing
      a by-severity table and by-disposition counts (SCAN-01).
    - The summary rollup's fold + park counts sum to the total finding count, which equals the number
      of finding entries in the body.
    - Every finding entry has exactly one disposition line matching `FOLD -> Phase ` or `PARK -- `
      (SCAN-02); no finding is left undispositioned.
    - `110-FINDINGS.md` records a result line for each of: ruff, Semgrep/Shield, gitleaks, pip-audit,
      npm audit.
    - The AppProcess red-test finding cites `test_app_process.py` and the
      `TypeError: cannot pickle '_thread.lock' object`, marked HIGH, FOLD -> Phase 112.
    - The LICENSE finding notes `LICENSE.txt` exists (Apache 2.0) and the fix is a RENAME to `LICENSE`,
      disposition FOLD -> Phase 113 — NOT characterized as a missing/create-from-scratch file.
    - All six pre-named fix items appear recorded as already-folded with their target phase (config-set
      -> 111; GUARD-01..06 -> 112), with GUARD-01/02 characterized as confirm-the-gap (already
      implemented), not unimplemented.
    - No production source/config/test/.gitignore/docs file was modified (only `110-FINDINGS.md`).
  </acceptance_criteria>
  <done>
    `110-FINDINGS.md` is a complete, severity-ranked, fully-dispositioned hostile-reader findings
    artifact: maintainer-readable summary at top, every finding fold/park, every tool result recorded,
    the six pre-named items confirmed-and-folded, the LICENSE rename and dependency-CVE parks captured.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Findings checkpoint — maintainer reviews the fold/park dispositions (SCAN-02)</name>
  <files>.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md</files>
  <action>
    Present the completed `110-FINDINGS.md` to the maintainer for review of the fold/park dispositions
    before they are treated as locked fix scope for Phases 111-113. This is a blocking human-verify
    checkpoint — do NOT auto-advance. Surface the `## Summary` rollup and the scope-gating dispositions
    (see how-to-verify below), then pause for the maintainer's decision per the resume signal.
  </action>
  <what-built>
    `110-FINDINGS.md` — the triaged, severity-ranked hostile-reader findings artifact. Every finding
    carries an explicit FOLD -> Phase 111|112|113 or PARK disposition. These dispositions are scope
    decisions that GATE the detailed planning of Phases 111 (config-set migration), 112 (defensive
    guards), and 113 (presentation/launch). The maintainer should see them before they are treated as
    locked fix scope.
  </what-built>
  <how-to-verify>
    1. Open `.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-FINDINGS.md`.
    2. Read the top `## Summary` rollup: confirm the by-severity counts and by-disposition counts
       (per fold target + parked) match your expectation of the launch risk and sum correctly.
    3. Confirm the "Tools run clean" block reflects the launch-positive evidence (ruff / Semgrep /
       gitleaks clean; pip + npm CVEs parked as dev-only).
    4. Scan each finding's Disposition line. Specifically sanity-check the scope-gating ones:
       - Config-set credential leak -> FOLD Phase 111 (the one breaking change).
       - AppProcess red test (HIGH) -> FOLD Phase 112.
       - GUARD-01/02 startup warnings recorded as "already implemented, confirm-the-gap" (NOT
         scoped as build-from-scratch) -> FOLD Phase 112.
       - LICENSE = rename `LICENSE.txt` -> `LICENSE`, FOLD Phase 113.
       - pip / npm CVEs PARKed as dev-only; DEFER-* and Out-of-Scope items PARKed.
    5. Decide: do these dispositions correctly bound the fix scope for 111/112/113? Should any PARKed
       finding be promoted to FOLD, or any FOLD finding be retargeted/parked?
  </how-to-verify>
  <verify>
    <human-check>Maintainer has read 110-FINDINGS.md and confirmed (or amended) every fold/park disposition as the locked fix scope for Phases 111-113.</human-check>
  </verify>
  <resume-signal>
    Type "approved" to lock these dispositions as the fix scope for Phases 111-113, or describe which
    finding dispositions to change (promote a park to fold, retarget a phase, or down/upgrade severity)
    and the executor will revise `110-FINDINGS.md` accordingly.
  </resume-signal>
  <done>
    The maintainer has reviewed the dispositions and either approved them as locked fix scope for
    Phases 111-113 or specified the disposition changes to apply to `110-FINDINGS.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — analysis-only phase. This phase lands no production code and has no runtime attack surface.
It runs read-only audit tools and writes a single Markdown analysis artifact (`110-FINDINGS.md`).
There is no new code path, endpoint, input parser, or dependency introduced, so there is no new
trust boundary to model.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-NA | N/A | Analysis-only phase (writes one Markdown artifact; no production code, no runtime surface) | accept | No threat surface introduced. The audit tools run read-only per D-02 (no `--fix`); the findings about the codebase's existing security posture are recorded in the artifact and dispositioned into fix Phases 111/112, where their own threat models apply. |

Note: the security FINDINGS this phase surfaces (config-set credential-in-URL, opt-in auth/webhook
warnings, delete-swallow) are dispositioned into Phases 111/112; the STRIDE work for those mitigations
belongs to those phases' plans, not this discovery phase.
</threat_model>

<verification>
Phase-level checks (all assert properties of the ARTIFACT and the tool runs, not of production code):

1. `110-FINDINGS.md` exists in the phase directory with a top-of-file `## Summary` rollup
   (by-severity + by-disposition counts) — satisfies SCAN-01 "a maintainer can read a triaged
   findings artifact."
2. Every finding entry has exactly one disposition line (`FOLD -> Phase ...` or `PARK -- ...`);
   finding count == dispositioned-finding count — satisfies SCAN-02.
3. A recorded result line exists for each of ruff, Semgrep/Shield, gitleaks, pip-audit, npm audit;
   the AppProcess red test result is recorded with its exact error string.
4. The six pre-named fix items are present as already-folded with correct target phases; GUARD-01/02
   characterized as already-implemented confirm-the-gap; LICENSE characterized as a rename.
5. No production source/config/test/.gitignore/docs file was modified (D-02 / no-code boundary) —
   `git status` shows only `110-FINDINGS.md` (and standard GSD phase artifacts) changed.
6. The maintainer has approved the dispositions at the Task 3 checkpoint.
</verification>

<success_criteria>
- SCAN-01 satisfied: a maintainer can open `110-FINDINGS.md` and read, ranked by severity with a
  one-glance summary rollup, what a skeptical r/selfhosted engineer would flag — produced from
  reading the entry points, running ruff whole-tree + Semgrep/Shield + gitleaks + pip-audit + npm
  audit under launch framing, and skimming the highest-traffic source files.
- SCAN-02 satisfied: every finding is explicitly marked FOLD (with target phase) or PARK (with
  one-line rationale); scope decisions are explicit and traceable.
- The six pre-named fix items are confirmed-and-recorded as already-folded; the additive findings
  (LICENSE rename, dependency CVEs, any new launch-framed observations) are captured with dispositions.
- The maintainer has reviewed and locked the dispositions as the fix scope for Phases 111-113.
- No production code lands; the only artifact created is `110-FINDINGS.md`.
</success_criteria>

<output>
Create `.planning/milestones/v1.4.0-phases/110-hostile-reader-discovery-pass/110-01-SUMMARY.md` when
done, summarizing: the finding counts by severity and disposition, which findings fold into which
phase (the gating input for 111/112/113), the launch-positive clean-tooling result, and the
maintainer's checkpoint decision. The primary deliverable is `110-FINDINGS.md` in the same directory.
</output>
