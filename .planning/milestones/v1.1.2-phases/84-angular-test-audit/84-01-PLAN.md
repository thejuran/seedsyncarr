---
phase: 84-angular-test-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/84-angular-test-audit/84-01-SUMMARY.md
autonomous: true
requirements: [NG-01, NG-02, NG-03]

must_haves:
  truths:
    - "All 40 Angular spec files have been cross-referenced against live production code and confirmed non-stale per D-01"
    - "Zero spec files removed (all reference live production code)"
    - "Zero mock files orphaned (all 7 mocks imported by remaining specs)"
    - "ng test exits green with zero failures"
    - "Angular coverage baseline percentages captured before any changes"
    - "Removal inventory documented in markdown table per D-06"
  artifacts:
    - path: ".planning/phases/84-angular-test-audit/84-01-SUMMARY.md"
      provides: "Staleness audit results, coverage baseline, removal inventory"
      contains: "Removal Inventory"
  key_links:
    - from: "40 spec files"
      to: "production files in src/angular/src/app/"
      via: "import path resolution to on-disk files"
      pattern: "All imports resolve to existing .ts files"
---

<objective>
Verify zero Angular test staleness, confirm suite green, and capture coverage baseline

Purpose: Formally execute the staleness audit (NG-01), verify tests pass (NG-02/NG-03), document zero-removal inventory (D-06), and capture pre-migration coverage baseline (D-09). This must complete before CI noise cleanup (Plan 02) per D-08.

Output: 84-01-SUMMARY.md with staleness cross-reference evidence, coverage baseline numbers, and zero-removal inventory table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-angular-test-audit/84-CONTEXT.md
@.planning/phases/84-angular-test-audit/84-RESEARCH.md

<interfaces>
<!-- No code interfaces consumed or created by this plan. This is a read-only audit + documentation plan. -->
<!-- The executor needs these paths for verification: -->

Test infrastructure:
- `src/angular/karma.conf.js` — Karma config (ChromeHeadless launcher defined)
- `src/angular/src/test.ts` — Test bootstrap entry point

Test command (local macOS):
```bash
cd /Users/julianamacbook/seedsyncarr/src/angular && node node_modules/@angular/cli/bin/ng.js test --browsers ChromeHeadless --watch=false
```

Coverage command:
```bash
cd /Users/julianamacbook/seedsyncarr/src/angular && node node_modules/@angular/cli/bin/ng.js test --browsers ChromeHeadless --watch=false --code-coverage
```

Spec file locations:
- Co-located: `src/angular/src/app/pages/about/about-page.component.spec.ts`, `src/angular/src/app/pages/settings/option.component.spec.ts`, `src/angular/src/app/pages/settings/settings-page.component.spec.ts`
- Centralized: `src/angular/src/app/tests/unittests/` (37 specs)
- Mocks: `src/angular/src/app/tests/unittests/mocks/` (7 mock files)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute staleness audit, run test suite, capture coverage baseline, and document results</name>
  <files>.planning/phases/84-angular-test-audit/84-01-SUMMARY.md</files>
  <read_first>
    - src/angular/src/app/pages/about/about-page.component.spec.ts
    - src/angular/src/app/pages/settings/option.component.spec.ts
    - src/angular/src/app/pages/settings/settings-page.component.spec.ts
    - src/angular/karma.conf.js
    - .planning/phases/84-angular-test-audit/84-RESEARCH.md (staleness audit results table)
    - .planning/phases/84-angular-test-audit/84-CONTEXT.md (D-01 through D-10 decisions)
    - .planning/phases/83-python-test-audit/83-01-SUMMARY.md (prior phase format reference)
  </read_first>
  <action>
Execute the following steps in order:

**Step 1 — Independent staleness cross-reference (NG-01, per D-01/D-02):**

For ALL 40 spec files, extract non-Angular/non-rxjs imports and verify each resolves to an existing production file on disk. Use `find` to list all spec files:

```bash
find /Users/julianamacbook/seedsyncarr/src/angular/src/app -name "*.spec.ts" | sort
```

Then for each spec file, extract its production imports:
```bash
grep -E "^import.*from\s+['\"]\.\./" {spec_file} | grep -v "@angular" | grep -v "rxjs"
```

Resolve each import path relative to the spec file location and verify the target `.ts` file exists on disk using `ls` or `test -f`. Record results.

**Step 2 — Mock orphan check (per D-05):**

For each of the 7 mock files in `src/angular/src/app/tests/unittests/mocks/`, verify at least one remaining spec file imports it:

```bash
for mock in mock-event-source mock-model-file.service mock-rest.service mock-storage.service mock-stream-service.registry mock-view-file-options.service mock-view-file.service; do
  echo "=== $mock ==="
  grep -rl "$mock" /Users/julianamacbook/seedsyncarr/src/angular/src/app/tests/unittests/ /Users/julianamacbook/seedsyncarr/src/angular/src/app/pages/ 2>/dev/null
done
```

**Step 3 — Run full test suite (NG-02, NG-03):**

```bash
cd /Users/julianamacbook/seedsyncarr/src/angular && node node_modules/@angular/cli/bin/ng.js test --browsers ChromeHeadless --watch=false
```

Record pass/fail/skip counts from Karma output.

**Step 4 — Capture coverage baseline (D-09):**

```bash
cd /Users/julianamacbook/seedsyncarr/src/angular && node node_modules/@angular/cli/bin/ng.js test --browsers ChromeHeadless --watch=false --code-coverage
```

Read coverage summary from the terminal output (Karma outputs statement/branch/function/line percentages). Record all four metrics. This is the "before" baseline per D-09.

**Step 5 — Document results in 84-01-SUMMARY.md (D-06):**

Create the summary following the same format as `83-01-SUMMARY.md`. Include:
- Removal Inventory table (D-06): Zero removals, all 40 spec files verified LIVE
- Coverage Baseline table (D-09): statement%, branch%, function%, line% from Step 4
- Verification Evidence section: import cross-check results (NG-01), mock orphan check (D-05), test suite output (NG-02/NG-03)
- Requirements Addressed table mapping NG-01, NG-02, NG-03 to evidence

The removal inventory table format per D-06:
```
| Spec File Path | Test Count Removed | Reason |
|----------------|-------------------|--------|
| *(none)* | 0 | All 40 spec files verified LIVE against production modules |
```
  </action>
  <verify>
    <automated>test -f /Users/julianamacbook/seedsyncarr/.planning/phases/84-angular-test-audit/84-01-SUMMARY.md && grep -q "Removal Inventory" /Users/julianamacbook/seedsyncarr/.planning/phases/84-angular-test-audit/84-01-SUMMARY.md && grep -q "Coverage Baseline" /Users/julianamacbook/seedsyncarr/.planning/phases/84-angular-test-audit/84-01-SUMMARY.md && grep -q "NG-01" /Users/julianamacbook/seedsyncarr/.planning/phases/84-angular-test-audit/84-01-SUMMARY.md && grep -q "NG-02" /Users/julianamacbook/seedsyncarr/.planning/phases/84-angular-test-audit/84-01-SUMMARY.md && grep -q "NG-03" /Users/julianamacbook/seedsyncarr/.planning/phases/84-angular-test-audit/84-01-SUMMARY.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 84-01-SUMMARY.md exists at `.planning/phases/84-angular-test-audit/84-01-SUMMARY.md`
    - File contains "Removal Inventory" section with markdown table showing zero removals
    - File contains "Coverage Baseline" section with statement%, branch%, function%, line% numbers
    - File contains "NG-01" with evidence of import cross-check across all 40 spec files
    - File contains "NG-02" with evidence of zero test files deleted
    - File contains "NG-03" with evidence of `ng test` exit green
    - File contains "D-05" mock orphan check result (zero orphans)
    - File contains "D-06" inventory table
    - File contains "D-09" coverage baseline
    - `ng test --browsers ChromeHeadless --watch=false` exited with 0 failures
  </acceptance_criteria>
  <done>All 40 Angular spec files verified as live (zero stale per D-01), zero orphaned mocks (D-05), test suite green (NG-02/NG-03), coverage baseline captured (D-09), and zero-removal inventory documented (D-06) in 84-01-SUMMARY.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is a read-only audit + documentation task. No production code is modified, no APIs are called, no user input is processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | I (Info Disclosure) | Coverage report in `src/angular/coverage/` | accept | Coverage HTML is gitignored, local-only, contains no secrets — only line/branch percentages |
</threat_model>

<verification>
- `ng test --browsers ChromeHeadless --watch=false` exits green (0 failures)
- `ng test --code-coverage` produces coverage summary output
- 84-01-SUMMARY.md contains Removal Inventory, Coverage Baseline, and all three requirement IDs (NG-01, NG-02, NG-03)
</verification>

<success_criteria>
- All 40 spec files confirmed as live via independent import cross-reference
- Zero spec files removed, zero mocks orphaned
- Angular test suite passes with zero failures
- Coverage baseline captured with all four metrics (statements, branches, functions, lines)
- Audit results documented in SUMMARY with D-06 inventory table
</success_criteria>

<output>
After completion, create `.planning/phases/84-angular-test-audit/84-01-SUMMARY.md`
</output>
