---
phase: 108-config-handler-refactors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/web/handler/controller.py
  - src/python/tests/unittests/test_web/test_handler/test_controller_handler.py
autonomous: true
requirements: [ARCH-03]
tags: [handler, controller, http, refactor, dedup, dispatch]

must_haves:
  truths:
    - "A single _dispatch_command(action, file_name, success_msg, *, guard=False) helper exists on ControllerHandler"
    - "All five __handle_action_* methods (queue, stop, extract, delete_local, delete_remote) delegate to the helper as one-liners"
    - "extract/delete_local/delete_remote pass guard=True; queue/stop pass guard=False"
    - "The duplicated ~15-line per-handler scaffold is gone from every individual handler"
    - "unquote runs before the path-safety guard; 504-on-timeout, 200-with-success-body, and callback.error+error_code-on-failure are preserved exactly"
    - "_check_path_safe still fires for extract/delete_local/delete_remote and never for queue/stop"
    - "__handle_bulk_command and _process_bulk_commands are byte-identical and not routed through the helper"
    - "add_routes registration and the five public handler method names are unchanged"
  artifacts:
    - path: "src/python/web/handler/controller.py"
      provides: "shared _dispatch_command helper; five thinned handler delegates; bulk path untouched"
      contains: "_dispatch_command"
    - path: "src/python/tests/unittests/test_web/test_handler/test_controller_handler.py"
      provides: "single-action + bulk regression suite, passing unmodified"
  key_links:
    - from: "the five __handle_action_* methods"
      to: "self._dispatch_command(...)"
      via: "one-line delegate"
      pattern: "_dispatch_command"
    - from: "_dispatch_command guard branch"
      to: "self._check_path_safe(file_name)"
      via: "guard=True for extract/delete_local/delete_remote only"
      pattern: "_check_path_safe"
---

<objective>
Deduplicate the five per-action HTTP handler scaffolds in `web/handler/controller.py` into a single shared `_dispatch_command(...)` helper (ARCH-03). The five `__handle_action_*` methods collapse from ~15 near-identical lines each into one-line delegates; the bulk path stays byte-identical and untouched.

Purpose: Removes the "duplicate-by-five" maintenance hazard flagged in CONCERNS.md — today a fix to the dispatch scaffold (timeout handling, error shape) must be applied five times. After this, there is one scaffold.

Output: Updated `controller.py` with `_dispatch_command` + five thinned handlers; `__handle_bulk_command`/`_process_bulk_commands` unchanged; existing unit + integration suites pass unmodified as the behavior-preserving proof.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/108-config-handler-refactors/108-CONTEXT.md
@.planning/milestones/v1.3.0-phases/108-config-handler-refactors/108-PATTERNS.md

<interfaces>
<!-- Current contracts the executor must preserve. Extracted from src/python/web/handler/controller.py. -->

The five handlers (controller.py) and their variation points — everything else in each is identical:
| Handler | lines | Action enum | success body | guard? |
| __handle_action_queue | 76-93 | QUEUE | "Queued file '{}'" | no |
| __handle_action_stop | 95-112 | STOP | "Stopped file '{}'" | no |
| __handle_action_extract | 114-135 | EXTRACT | "Requested extraction for file '{}'" | yes |
| __handle_action_delete_local | 137-158 | DELETE_LOCAL | "Requested local delete for file '{}'" | yes |
| __handle_action_delete_remote | 160-181 | DELETE_REMOTE | "Requested remote delete for file '{}'" | yes |

Shared scaffold (canonical guarded copy = __handle_action_extract, lines 119-135) in execution order:
  file_name = unquote(file_name)                 # value is double encoded — BEFORE guard
  (guarded only) guard = self._check_path_safe(file_name); if guard: return guard
  command = Controller.Command(<ACTION>, file_name)
  callback = WebResponseActionCallback()
  command.add_callback(callback)
  self.__controller.queue_command(command)       # name-mangled: self._ControllerHandler__controller
  completed = callback.wait(timeout=self._ACTION_TIMEOUT)
  if not completed: return HTTPResponse(body="Operation timed out", status=504)
  if callback.success: return HTTPResponse(body=<success_body>.format(file_name))
  else: return HTTPResponse(body=callback.error, status=callback.error_code)

Frozen contract constants:
  _ACTION_TIMEOUT = 30.0 (line 184) — referenced as self._ACTION_TIMEOUT so the test monkeypatch (ControllerHandler._ACTION_TIMEOUT = 0.1) still works
  WebResponseActionCallback default error_code = 400 (lines 31/36)
  504 body is exactly "Operation timed out"
  self.__controller is name-mangled; calling it from a method still inside the class is fine — keep self.__controller

OUT OF SCOPE — do NOT touch (D-03):
  __handle_bulk_command (lines 227-332) and _process_bulk_commands (lines 334-448) stay byte-identical. The bulk path has its own parallel-queue + per-file-timeout + partial-failure semantics and its own guard handling via _GUARDED_ACTIONS (lines 196-200). It must NOT be routed through _dispatch_command.
  add_routes (lines 65-74) unchanged — the five public __handle_action_* method names stay; only their bodies delegate.

Test scaffolds (regression proof — should pass UNMODIFIED):
  test_controller_handler.py TestControllerHandlerSingleAction (lines 38-80): calls via name-mangling self.handler._ControllerHandler__handle_action_queue(file_name); monkeypatches ControllerHandler._ACTION_TIMEOUT
  test_controller.py (integration) TestControllerHandler: full single + bulk HTTP paths via /server/command/{queue,stop,extract,delete_local,delete_remote} and /bulk
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _dispatch_command and delegate the five handlers</name>
  <files>src/python/web/handler/controller.py, src/python/tests/unittests/test_web/test_handler/test_controller_handler.py</files>
  <read_first>
    - src/python/web/handler/controller.py (lines 57-74 __init__ + add_routes; 76-181 the five handlers; 183-200 _ACTION_TIMEOUT / _VALID_ACTIONS / _GUARDED_ACTIONS; 209-225 _check_path_safe; 227-448 bulk path — read to confirm boundaries, do NOT edit)
    - .planning/milestones/v1.3.0-phases/108-config-handler-refactors/108-PATTERNS.md (ARCH-03 section: variation table, target signature, behavior-preserving invariants, out-of-scope list)
    - src/python/tests/unittests/test_web/test_handler/test_controller_handler.py (lines 38-80 TestControllerHandlerSingleAction; note name-mangled call + _ACTION_TIMEOUT monkeypatch)
  </read_first>
  <behavior>
    - Test (existing, unmodified): a timed-out single action returns HTTPResponse status 504 with body "Operation timed out".
    - Test (existing, unmodified): a successful single action returns 200 with the action-specific success body.
    - Behavior to preserve via the delegate structure: guard=True actions (extract/delete_local/delete_remote) call _check_path_safe and short-circuit on a non-None guard response BEFORE queuing; guard=False actions (queue/stop) never call _check_path_safe.
  </behavior>
  <action>
    Add a private helper `_dispatch_command(self, action: "Controller.Command.Action", file_name: str, success_msg: str, *, guard: bool = False) -> HTTPResponse` to `ControllerHandler` (exact positional/keyword split per D-03; the leading `*` keyword-only `guard` is required). Its body is the canonical scaffold from __handle_action_extract, generalized over `action` and `success_msg`:
    - `file_name = unquote(file_name)` FIRST (preserve the current order — unquote before guard);
    - if `guard`: `path_guard = self._check_path_safe(file_name)`; `if path_guard: return path_guard`;
    - build `Controller.Command(action, file_name)`, attach a `WebResponseActionCallback`, `self.__controller.queue_command(command)` (keep `self.__controller` — name-mangling resolves inside the class);
    - `if not callback.wait(timeout=self._ACTION_TIMEOUT): return HTTPResponse(body="Operation timed out", status=504)`;
    - `if callback.success: return HTTPResponse(body=success_msg.format(file_name))`; else `return HTTPResponse(body=callback.error, status=callback.error_code)`.
    Reference the timeout as `self._ACTION_TIMEOUT` (NOT a hardcoded 30.0) so the test monkeypatch keeps working.

    Rewrite each of the five `__handle_action_*` methods to a single-line delegate to `_dispatch_command`, preserving the existing method name, signature `(self, file_name: str) -> HTTPResponse`, and docstring. Pass the exact Action enum and the exact success-body string char-for-char from the variation table. Pass `guard=True` for extract, delete_local, delete_remote; pass `guard=False` (or omit, since it defaults False) for queue and stop. Keep the success body strings byte-identical to the originals ("Queued file '{}'", "Stopped file '{}'", "Requested extraction for file '{}'", "Requested local delete for file '{}'", "Requested remote delete for file '{}'").

    Do NOT modify `add_routes`, `_ACTION_TIMEOUT`, `_VALID_ACTIONS`, `_GUARDED_ACTIONS`, `_check_path_safe`, `__handle_bulk_command`, or `_process_bulk_commands`. The bulk path stays byte-identical (D-03).

    Run the existing single-action unit tests and the integration suite as the RED→GREEN proof — they MUST pass without any edit. If a test needs editing to pass, the refactor changed observable behavior and is wrong; fix the helper, not the test. (No new test is required since the existing suites fully cover the five paths' success/timeout/failure contracts; do not delete or weaken any test.)
  </action>
  <verify>
    <automated>cd src/python && poetry run pytest tests/unittests/test_web/test_handler/test_controller_handler.py tests/integration/test_web/test_handler/test_controller.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "_dispatch_command" src/python/web/handler/controller.py` returns ≥ 6 (one definition + five delegates).
    - Source assertion: each of the five `__handle_action_*` method bodies (excluding docstring) is a single `return self._dispatch_command(...)` statement; the duplicated scaffold (`callback.wait(timeout=self._ACTION_TIMEOUT)`) appears in `_dispatch_command` and NOT inside any individual `__handle_action_*` method.
    - Source assertion: `_dispatch_command` calls `self._check_path_safe` only inside the `if guard:` branch; the three guarded delegates pass `guard=True` and queue/stop do not (`grep -n "guard=True" src/python/web/handler/controller.py` shows it on the extract/delete_local/delete_remote delegates).
    - Source assertion: `__handle_bulk_command` and `_process_bulk_commands` are unchanged — `git diff src/python/web/handler/controller.py` shows no edits between their def lines and the next def; `_process_bulk_commands` still contains its own `self._check_path_safe` call.
    - Source assertion: `add_routes` (lines 65-74) is unchanged; the five public `__handle_action_*` method names still exist (`grep -c "def __handle_action_" src/python/web/handler/controller.py` returns 5).
    - Behavior assertion: 504-on-timeout body is exactly "Operation timed out"; success bodies match the table char-for-char; failure returns callback.error with callback.error_code — all proven by the unmodified TestControllerHandlerSingleAction + integration tests.
    - Test command: `cd src/python && poetry run pytest tests/unittests/test_web/test_handler/test_controller_handler.py tests/integration/test_web/test_handler/test_controller.py -q` exits 0 with no test modified.
  </acceptance_criteria>
  <done>`_dispatch_command` exists with the D-03 signature; the five handlers are one-line delegates with correct Action/success_msg/guard; unquote-before-guard, 504/200/error-code contracts and the guard-only-for-extract/delete paths are preserved; bulk path and add_routes untouched; existing unit + integration suites pass unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → ControllerHandler action endpoints | Untrusted `file_name` (double-URL-encoded) crosses into the handler; `unquote` then path-safety guard decide whether a path-destructive action proceeds |
| handler → Controller command queue | Validated action + file_name cross into the controller for execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-02-01 | Tampering / Elevation | path-safety guard skipped after dedup for extract/delete_local/delete_remote (path traversal regression) | mitigate | guard=True is passed by exactly those three delegates and `_dispatch_command` calls `_check_path_safe` in the guard branch; the existing integration tests for extract/delete_local/delete_remote (which exercise traversal-rejection paths) pass unmodified. Source assertion confirms `guard=True` on the three delegates. |
| T-108-02-02 | Spoofing of behavior | guard wrongly applied to queue/stop (over-blocking) or response shape drift | mitigate | queue/stop delegates pass guard=False (default); success/timeout/error response shapes are frozen and asserted char-for-char by the unmodified single-action + integration suites. |
| T-108-02-03 | Denial of Service | timeout handling diverges (e.g. hardcoded 30.0 breaks the test monkeypatch or alters live behavior) | mitigate | Helper references `self._ACTION_TIMEOUT`; the existing 504-timeout test monkeypatches the class attribute and passes unmodified. |
| T-108-02-04 | Tampering | bulk path accidentally routed through the helper, altering partial-failure semantics | mitigate | `__handle_bulk_command`/`_process_bulk_commands` left byte-identical (D-03); `git diff` asserts no edits; bulk integration tests pass unmodified. |
| T-108-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; RESEARCH Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/unittests/test_web/test_handler/test_controller_handler.py tests/integration/test_web/test_handler/test_controller.py -q` exits 0, no test modified.
- Full Python suite green (the milestone regression net): `make run-tests-python` — Python `fail_under` ≥ 88 holds or rises; no test deleted.
- `grep -c "def __handle_action_" src/python/web/handler/controller.py` returns 5; `grep -c "_dispatch_command" src/python/web/handler/controller.py` returns ≥ 6.
- `git diff` shows `__handle_bulk_command` and `_process_bulk_commands` unchanged.
</verification>

<success_criteria>
- ARCH-03 success criterion #4: a shared `_dispatch_command(action, file_name, success_msg, *, guard=False)` helper is extracted and used by all five `__handle_action_*` methods; the duplicated ~15-line scaffold is gone from each individual handler.
- ARCH-03 success criterion #5 (COMPAT): every single-action endpoint returns the same success/failure HTTP shapes and status codes as before; bulk path byte-identical; confirmed by the existing integration suite staying green. CI green amd64 + arm64; Python fail_under ≥ 88; no test deleted.
- D-03 preserved: bulk loop NOT routed through the helper; public method names + route registration unchanged.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/108-config-handler-refactors/108-02-SUMMARY.md` when done.
</output>
