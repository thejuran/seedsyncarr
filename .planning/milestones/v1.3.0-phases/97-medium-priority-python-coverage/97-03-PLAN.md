---
phase: 97-medium-priority-python-coverage
plan: 03
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/python/tests/unittests/test_web/test_handler/test_config_handler.py
  - src/python/web/handler/config.py
autonomous: true
requirements: [COVMED-02]
must_haves:
  truths:
    - "_validate_url rejects IPv4 private, loopback, and link-local addresses with the private/reserved message"
    - "_validate_url rejects IPv6 link-local, loopback, and unique-local addresses with the private/reserved message"
    - "_validate_url rejects IPv6-mapped PRIVATE IPv4 (::ffff:10.0.0.1), loopback-mapped (::ffff:127.0.0.1), and link-local-mapped (::ffff:169.254.0.1)"
    - "_validate_url ALLOWS IPv6-mapped PUBLIC IPv4 (::ffff:8.8.8.8) — returns None. This is the only case proving the guard does unmap-AND-recheck rather than blanket-blocking all IPv4-mapped addresses"
    - "_validate_url rejects when getaddrinfo returns MULTIPLE addresses and ANY one is private (e.g. [(public),(private)]) — proves the loop checks ALL results, not just the first"
    - "_validate_url returns 'Cannot resolve hostname' when getaddrinfo raises gaierror"
    - "_validate_url returns None for a valid public host (8.8.8.8)"
    - "_validate_url rejects non-http(s) schemes and missing hostnames without a socket call"
    - "D-01: the SSRF tests are written FIRST and run RED against the CURRENT implementation; the D-01/D-02 unmap-and-recheck fix is applied ONLY IF a test is actually red (e.g. ::ffff:8.8.8.8 is wrongly blocked, or a mapped-private case slips through). If all tests pass green against current code (Python's ipaddress natively flags mapped private/loopback/link-local), NO code change is made and the SUMMARY records that D-01's fix was unnecessary."
    - "D-02: IF the fix is needed, it detects addr.ipv4_mapped and re-runs the private/reserved/loopback/link-local checks against the unmapped IPv4; it must NOT blanket-block all mapped addresses (::ffff:8.8.8.8 must still return None); no DNS-rebind handling added"
    - "D-05: tests are additive; the IPv6-mapped fix lands ONLY-IF-RED then GREEN, any broader bug deferred to v1.4.0 with a STATE.md entry"
  artifacts:
    - path: "src/python/tests/unittests/test_web/test_handler/test_config_handler.py"
      provides: "TestValidateUrl class covering all SSRF input classes against the staticmethod directly"
      contains: "TestValidateUrl"
  key_links:
    - from: "TestValidateUrl"
      to: "ConfigHandler._validate_url"
      via: "direct staticmethod call with patched web.handler.config.socket"
      pattern: "_validate_url"
---

<objective>
Add full-path coverage for the SSRF guard `ConfigHandler._validate_url`
(`src/python/web/handler/config.py:55-85`), covering every documented input class
(COVMED-02): IPv4 private/loopback/link-local, IPv6 link-local/loopback/unique-local,
IPv6-mapped IPv4 (private, loopback, link-local — blocked; AND public — allowed),
a multi-address result, unresolved hostname, valid public host, and the scheme/no-hostname
early returns.

Purpose: this is the project's only SSRF defense for the Sonarr/Radarr connection-test
endpoints. IPv6 and reserved-range handling is currently untested, and the IPv6-mapped
unmap-and-recheck semantics (block mapped-private, allow mapped-public) are unproven.
Output: a new `TestValidateUrl` class in the existing unit test file, calling the
`@staticmethod` directly with `socket.getaddrinfo` stubbed (no bottle stack, no real DNS).

ONLY-IF-RED fix (D-01/D-02, Codex-corrected): Codex ran Python 3.12 and found that
`::ffff:10.0.0.1`, `::ffff:127.0.0.1`, and `::ffff:169.254.0.1` ALREADY trip
`is_private`/`is_loopback`/`is_link_local` natively WITHOUT an explicit unmap — so the D-01
fix may be UNNECESSARY and a blanket unmap could even be a regression. Therefore: write ALL
SSRF tests FIRST and run them RED against the current code. Add the D-01/D-02 unmap-and-recheck
fix ONLY IF a test is actually red (i.e. `::ffff:8.8.8.8` is wrongly blocked, or some
mapped-private/loopback/link-local case slips through). If every test passes green against the
current implementation, make NO source change and record in the SUMMARY that D-01's fix was
unnecessary (current native `ipaddress` checks suffice). Do NOT add DNS-rebind handling
(out of scope per the inline comment at config.py:59-62).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<interfaces>
From src/python/web/handler/config.py (the method under test — a pure @staticmethod, directly callable):
- `ConfigHandler._validate_url(url: str) -> str | None` — returns an error string or None if valid.
  - scheme not in {http, https} → "Only http and https URLs are allowed" (no socket call)
  - no hostname → "Invalid URL: no hostname" (no socket call)
  - LOOPS over EVERY `socket.getaddrinfo(hostname, None)` 5-tuple (config.py:74): `ip_str = addr_info[4][0]`, `addr = ipaddress.ip_address(ip_str)`; if `addr.is_private or addr.is_loopback or addr.is_reserved or addr.is_link_local` → returns "URL resolves to a private/reserved IP address" on the FIRST matching address. The loop checking ALL results is load-bearing for the multi-address test.
  - `except socket.gaierror` → "Cannot resolve hostname"
  - otherwise → None
- The module imports `socket` at config.py:4 and `import ipaddress` at config.py:2.
- The current guard (config.py:78) has NO explicit `ipv4_mapped` unmap. Codex verified on Python 3.12 that `ipaddress.ip_address("::ffff:10.0.0.1").is_private` is True natively — so the mapped-private/loopback/link-local cases likely already pass without any fix.

Stub pattern (load-bearing) from test_config_handler.py:131-139 (TestConfigHandlerTestSonarrConnection):
- Patch target is `web.handler.config.socket` (NOT global socket).
- `mock_socket.getaddrinfo.return_value = [(None, None, None, None, (<ip_str>, 0))]` — IP at index [4][0].
- For a MULTI-address result, return a LIST of multiple 5-tuples, e.g.
  `[(None, None, None, None, ("8.8.8.8", 0)), (None, None, None, None, ("10.0.0.1", 0))]`.
- ALWAYS re-assign `mock_socket.gaierror = socket.gaierror` after patching, else `except socket.gaierror` references a MagicMock and won't catch.
- For unresolved host: `mock_socket.getaddrinfo.side_effect = socket.gaierror("...")` plus the gaierror reassignment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestValidateUrl covering all SSRF input classes (RED-first)</name>
  <files>src/python/tests/unittests/test_web/test_handler/test_config_handler.py</files>
  <read_first>
    - src/python/tests/unittests/test_web/test_handler/test_config_handler.py (lines 1-160: imports `import socket`, `from unittest.mock import patch`, `from web.handler.config import ConfigHandler`; the `@patch('web.handler.config.socket')` + `mock_socket.gaierror = socket.gaierror` pattern at 131-139)
    - src/python/web/handler/config.py (lines 55-85, the _validate_url staticmethod; note the getaddrinfo LOOP at 74 and the four-check guard at 78 with NO ipv4_mapped unmap)
  </read_first>
  <behavior>
    - test_rejects_ipv4_private (10.0.0.1) → "private/reserved" in result
    - test_rejects_ipv4_loopback (127.0.0.1) → "private/reserved"
    - test_rejects_ipv4_link_local (169.254.0.1) → "private/reserved"
    - test_rejects_ipv6_link_local (fe80::1) → "private/reserved"
    - test_rejects_ipv6_loopback (::1) → "private/reserved"
    - test_rejects_ipv6_unique_local (fc00::1) → "private/reserved"
    - test_rejects_ipv6_mapped_private_ipv4 (::ffff:10.0.0.1) → "private/reserved"
    - test_rejects_ipv6_mapped_loopback_ipv4 (::ffff:127.0.0.1) → "private/reserved"
    - test_rejects_ipv6_mapped_link_local_ipv4 (::ffff:169.254.0.1) → "private/reserved"
    - test_ACCEPTS_ipv6_mapped_PUBLIC_ipv4 (::ffff:8.8.8.8) → None  [PROVES unmap-AND-recheck, not blanket-block; write RED first]
    - test_rejects_multi_address_when_any_private (getaddrinfo returns [(8.8.8.8),(10.0.0.1)]) → "private/reserved"  [PROVES the loop checks ALL results]
    - test_unresolved_hostname (getaddrinfo raises gaierror) → "Cannot resolve hostname"
    - test_accepts_public_ip (8.8.8.8) → None
    - test_rejects_ftp_scheme (ftp://host/) → "Only http and https" (no socket call)
    - test_rejects_no_hostname (http:///path) → "Invalid URL: no hostname"
  </behavior>
  <action>
    Add a new `TestValidateUrl(unittest.TestCase)` class to the existing file. Call `ConfigHandler._validate_url(<url>)` directly (it is a staticmethod — no handler instance or bottle stack needed). For each resolving case, decorate with `@patch('web.handler.config.socket')`, set `mock_socket.getaddrinfo.return_value = [(None, None, None, None, (<ip_str>, 0))]`, and ALWAYS set `mock_socket.gaierror = socket.gaierror` (the easy-to-miss line). Pass a URL like `http://host.example/` (the hostname is irrelevant because getaddrinfo is stubbed) and assert the returned string contains `"private/reserved"` via `self.assertIn(...)`, OR assert it `is None` for the allowed cases. For the MULTI-ADDRESS test, set `getaddrinfo.return_value` to a LIST of two 5-tuples `[(None,None,None,None,("8.8.8.8",0)), (None,None,None,None,("10.0.0.1",0))]` and assert the result contains `"private/reserved"` — this proves the loop inspects ALL results, not just the first. For the unresolved case, set `mock_socket.getaddrinfo.side_effect = socket.gaierror("name resolution failed")`, set `mock_socket.gaierror = socket.gaierror`, and assert the result is `"Cannot resolve hostname"`. For the scheme and no-hostname cases, no socket patch is needed (the method returns before the socket call) — assert the exact early-return strings. CRITICAL (D-01 RED-first): write ALL cases — especially `test_ACCEPTS_ipv6_mapped_PUBLIC_ipv4` (::ffff:8.8.8.8 → None) and the three mapped-blocked cases — and run the FULL `TestValidateUrl` selection RED against the CURRENT, UNMODIFIED config.py to discover the actual behavior. Do NOT touch config.py in this task. Match the existing file's `unittest.TestCase` + `self.assertIn`/`self.assertIsNone` assertion style.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/unittests/test_web/test_handler/test_config_handler.py::TestValidateUrl -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - A `TestValidateUrl` class exists with one test per row in the behavior block (15 cases).
    - Includes the load-bearing `::ffff:8.8.8.8 → None` (mapped-public allowed) test and the multi-address `[(public),(private)] → private/reserved` test.
    - Every resolving test sets `mock_socket.gaierror = socket.gaierror` (grep: each `@patch('web.handler.config.socket')` test that calls getaddrinfo also assigns gaierror).
    - The scheme and no-hostname tests do NOT patch socket and assert the exact early-return strings.
    - Tests were run RED against the UNMODIFIED config.py; the executor records which cases (if any) failed against current code (this drives Task 2). config.py is NOT modified in this task.
  </acceptance_criteria>
  <done>All 15 SSRF input-class tests written, calling the staticmethod directly with stubbed socket, and run RED-first against current code to determine whether the D-01 fix is even needed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply IPv6-mapped unmap fix ONLY IF a test is red; else confirm green and record D-01 was a no-op</name>
  <files>src/python/web/handler/config.py, src/python/tests/unittests/test_web/test_handler/test_config_handler.py</files>
  <read_first>
    - src/python/web/handler/config.py (lines 72-85: the getaddrinfo loop, the four-check guard at 78, the gaierror catch at 82-83 — note there is NO ipv4_mapped unmap currently)
    - .planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md (D-01, D-02, D-05)
  </read_first>
  <action>
    Run the full `TestValidateUrl` selection. Inspect the RED-first results from Task 1. THREE outcomes:
    (1) ALL tests already GREEN against the current code (the expected outcome per Codex's Python 3.12 finding — `::ffff:10.0.0.1`/`::ffff:127.0.0.1`/`::ffff:169.254.0.1` natively trip is_private/is_loopback/is_link_local, and `::ffff:8.8.8.8` correctly returns None): make NO change to config.py. Record in the SUMMARY that the D-01/D-02 unmap-and-recheck fix was UNNECESSARY — the current native `ipaddress` checks already block mapped-private/loopback/link-local while allowing mapped-public — so applying it would have been a needless change (and a blanket unmap-block would have been a regression on `::ffff:8.8.8.8`).
    (2) A mapped-PRIVATE/loopback/link-local case is RED (slips through), OR `::ffff:8.8.8.8` is wrongly BLOCKED: apply the pre-approved D-01/D-02 trivial fix in `config.py` inside the per-addr loop. Detect the IPv6-mapped form via `addr.ipv4_mapped` (non-None for `::ffff:a.b.c.d`) and, when present, re-run the SAME four checks (`is_private or is_loopback or is_reserved or is_link_local`) against the unmapped IPv4 (`addr.ipv4_mapped`), returning the existing "URL resolves to a private/reserved IP address" string only if the UNMAPPED address matches. This is unmap-AND-RECHECK — it must NOT blanket-block all mapped addresses, so `::ffff:8.8.8.8` (unmapped 8.8.8.8 is public) must still return None. Keep it minimal (target <10 net non-blank/non-comment lines, no signature change, no new return string). Do NOT add DNS-rebind handling. After the fix, re-run and confirm both the mapped-blocked AND the mapped-public-allowed tests pass.
    (3) The needed fix exceeds the trivial window (>10 lines or a behavior change beyond strengthening the guard): STOP, revert any source edit, leave the test asserting the current (documented) behavior, and add a one-line STATE.md deferred-items entry per D-05 referencing the test name.
    In all outcomes, the FULL test_config_handler.py file must remain green (no regression in existing Sonarr/Radarr SSRF tests).
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/unittests/test_web/test_handler/test_config_handler.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All 15 `TestValidateUrl` cases pass, INCLUDING `::ffff:8.8.8.8 → None` (mapped-public allowed) and the three mapped-blocked cases and the multi-address case.
    - If config.py was NOT modified (outcome 1), the SUMMARY explicitly records that D-01's unmap fix was unnecessary because current native checks suffice (and a blanket block would regress `::ffff:8.8.8.8`).
    - If config.py WAS modified (outcome 2), the diff is ≤10 net non-blank/non-comment lines, no signature change, reuses the existing error string, adds NO DNS-rebind logic, performs unmap-AND-recheck (NOT a blanket block), and `::ffff:8.8.8.8` still returns None.
    - The full test_config_handler.py file still passes (no regression).
    - If deferred instead of fixed (outcome 3), a STATE.md deferred-items row references the documenting test.
  </acceptance_criteria>
  <done>The mapped-private/loopback/link-local cases are blocked and mapped-public is allowed; either the current code already does this (D-01 recorded as a no-op) or a minimal unmap-and-recheck fix was applied — never a blanket mapped-block; COVMED-02 fully covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-supplied Sonarr/Radarr URL → server-side requests.get | _validate_url is the SSRF guard at this boundary; attacker-controlled hostnames can resolve to internal IPs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Information Disclosure / EoP (SSRF) | ConfigHandler._validate_url | mitigate | Full-path tests assert private/reserved IPv4+IPv6 AND mapped-private/loopback/link-local are rejected while mapped-PUBLIC (::ffff:8.8.8.8) is allowed (proving unmap-and-recheck, not blanket-block) and a multi-address result with any private addr is rejected; unmap fix applied ONLY IF red (D-01/D-02) |
| T-97-03-02 | Spoofing (DNS rebind / TOCTOU) | _validate_url getaddrinfo-then-requests gap | accept | Documented out-of-scope per inline comment config.py:59-62; test documents the limitation, does not fix it |
| T-97-03-SC | Tampering | pip installs | accept | No new packages; uses stdlib socket/ipaddress and existing test deps |
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/unittests/test_web/test_handler/test_config_handler.py -q` exits 0.
- TestValidateUrl covers all 15 input classes; every resolving test reassigns `mock_socket.gaierror`.
- `::ffff:8.8.8.8 → None` and the multi-address `[(public),(private)] → private/reserved` cases both pass (unmap-and-recheck + all-results-loop proven).
- Any config.py change is within the trivial-fix window AND was applied only because a test was red; otherwise the SUMMARY records D-01 as a no-op. No blanket mapped-block. Out-of-window issues deferred with a STATE.md entry.
</verification>

<success_criteria>
COVMED-02 closed: _validate_url is tested across IPv4 private/loopback/link-local, IPv6
link-local/loopback/unique-local, IPv6-mapped IPv4 (private/loopback/link-local blocked,
public allowed), a multi-address result, unresolved hostname, valid public host, and
scheme/no-hostname early returns — with socket.getaddrinfo stubbed and CI-IPv6-independent.
The D-01 unmap fix is applied only-if-red, preventing a needless regression.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-03-SUMMARY.md` when done.
</output>
</content>
