---
phase: 97-medium-priority-python-coverage
plan: 04
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/python/tests/integration/test_lftp/test_lftp_protocol.py
  - src/python/lftp/lftp.py
autonomous: true
requirements: [COVMED-03]
must_haves:
  truths:
    - "A REAL-parser test proves a malformed `jobs -v` output makes the GENUINE LftpJobStatusParser.parse() raise LftpJobStatusParserError — proven by patching `_Lftp__run_command` to return the malformed fixture string (from test_job_status_parser.py:935-942, ending `bad string uh oh`) so the real parser actually runs against malformed input (NOT by stubbing parse() to raise)"
    - "A controller-counter test proves Lftp.status() increments the consecutive-error counter and returns [] while count <= MAX_CONSECUTIVE_STATUS_ERRORS (this test MAY stub the parser to raise, since it scopes ONLY the controller counter logic, not parser correctness)"
    - "The 3rd consecutive error (count > MAX_CONSECUTIVE_STATUS_ERRORS=2) makes Lftp.status() re-raise LftpJobStatusParserError (controller-counter scope)"
    - "A subsequent successful status() resets the consecutive-error counter to 0 (controller-counter scope)"
    - "D-03: parser-error recovery covered at the integration layer using the existing real-lftp test_lftp_protocol.py fixture pattern, spanning the REAL parser (truth #1 via __run_command injection) -> controller counter"
    - "D-04: all four documented points covered — a malformed line makes the REAL parser raise LftpJobStatusParserError, counter increments, MAX_CONSECUTIVE_STATUS_ERRORS=2 triggers recovery, counter resets on success"
    - "D-05: tests are additive; a missing counter-reset line is an in-scope trivial fix, any broader bug deferred to v1.4.0 with a STATE.md entry"
  artifacts:
    - path: "src/python/tests/integration/test_lftp/test_lftp_protocol.py"
      provides: "Integration coverage of the REAL parser raising on malformed input AND the controller consecutive-error counter in Lftp.status()"
      contains: "LftpJobStatusParserError"
  key_links:
    - from: "test_lftp_protocol.py"
      to: "Lftp.status() consecutive-error counter (lftp.py:298-313) + real LftpJobStatusParser.parse()"
      via: "real-parser via __run_command malformed injection (truth #1); parse-stub for controller counter logic"
      pattern: "status\\(\\)"
---

<objective>
Add integration coverage for the LFTP `JobStatusParser` `ValueError` recovery path
(COVMED-03), covering all four documented points of `Lftp.status()` at
`src/python/lftp/lftp.py:298-313`: a malformed `jobs -v` makes the REAL parser raise
`LftpJobStatusParserError`, the consecutive-error counter increments (swallowing while ≤
`MAX_CONSECUTIVE_STATUS_ERRORS = 2`), the 3rd consecutive error re-raises, and a subsequent
success resets the counter.

Purpose: the real lftp binary always emits well-formed `jobs -v`, so the existing
integration suite (which drives only real `LftpError` paths) has NEVER exercised the parser
-error counter — the documented recovery logic that keeps a transient parse hiccup from
crashing the controller is untested.
Output: new tests extending `TestLftpProtocol` (per D-03, integration layer). Two distinct
test concerns (Codex review fix for the false-positive coverage):
  1. A REAL-PARSER test that feeds malformed `jobs -v` text through the genuine
     `LftpJobStatusParser.parse()` (via `__run_command` injection) so the parser actually
     raises — this PROVES truth #1 end-to-end rather than over a stub.
  2. CONTROLLER-COUNTER tests (increment/swallow/re-raise/reset) that MAY stub
     `parse()` to raise, because those tests scope ONLY the counter logic, not parser
     correctness.

CRITICAL FIX (Codex blockers 1 & 5): the prior plan patched
`_Lftp__job_status_parser.parse` with `side_effect=LftpJobStatusParserError` for EVERY test,
so the REAL parser was never exercised against malformed input — truth #1 was unproven by
the stub. This plan splits the concerns: the real parser is proven against the malformed
fixture in Task 1; the counter logic uses the cleaner parse-stub in Task 2.

Trivial-fix watch (D-05): the reset line already exists at lftp.py:305 — verify it is
reached. If a real defect surfaces needing >10 lines / a behavior change, defer to v1.4.0
with a STATE.md deferred-items entry referencing the test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-medium-priority-python-coverage/97-CONTEXT.md
@.planning/phases/97-medium-priority-python-coverage/97-PATTERNS.md

<interfaces>
From src/python/lftp/lftp.py:
- `MAX_CONSECUTIVE_STATUS_ERRORS = 2` (line 12)
- `Lftp.__init__` sets `self.__consecutive_status_errors = 0` (line 52) and `self.__job_status_parser = LftpJobStatusParser()` (line 50)
- `Lftp.status() -> List[LftpJobStatus]` (lines 298-313):
    out = self.__run_command("jobs -v")          # line 302
    try: statuses = self.__job_status_parser.parse(out); self.__consecutive_status_errors = 0   # reset on success (305)
    except LftpJobStatusParserError:
        self.__consecutive_status_errors += 1     # increment (307)
        if count <= MAX_CONSECUTIVE_STATUS_ERRORS: warn; statuses = []   # swallow (308-310)
        else: raise                               # >2 → propagate (312)
    return statuses
- `__run_command(command)` (line 112, decorated `@with_check_process`) returns the raw lftp stdout string; `status()` feeds its return into `parse()`. Patching `_Lftp__run_command` to return a malformed string makes the REAL parser run against malformed input.
- `exit()` (line 379) calls `kill_all()` + `self.__process.sendline("exit")` + `close(force=True)` — teardown's `exit()` path; a `with patch.object(...)` scoped only around the `status()` calls is REMOVED before teardown, so teardown runs against the real binary unpatched.
- name-mangled members for white-box injection: `_Lftp__run_command`, `_Lftp__job_status_parser`, `_Lftp__consecutive_status_errors`

From src/python/lftp/job_status_parser.py:
- `LftpJobStatusParserError(AppError)` (line 10); `parse()` wraps internal `ValueError` → raises `LftpJobStatusParserError("Error parsing lftp job status")` at lines 723-726.

Malformed `jobs -v` fixture (REUSE for the real-parser test) — from
`src/python/tests/unittests/test_lftp/test_job_status_parser.py:935-942`,
`test_raises_error_on_bad_status`: a multi-line `jobs -v` block whose final content line is
`bad string uh oh`, which makes the genuine `LftpJobStatusParser.parse()` raise
`LftpJobStatusParserError`. Copy this block as the malformed input string for the
`__run_command` injection (or import/share the constant).

From src/python/tests/integration/test_lftp/test_lftp_protocol.py:
- `setUp` (line 98) builds `self.lftp = Lftp(address=self.host, port=self.port, user=self.user, password=None)` (line 111).
- `tearDown` (lines ~120-126) calls `self.lftp.raise_pending_error()` then `self.lftp.exit()` against the REAL binary — so any patch MUST be removed (use `with patch.object(...)` scoping) before teardown.
- Existing tests use `@pytest.mark.timeout(5)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REAL-parser test (truth #1) + controller increment/swallow/re-raise</name>
  <files>src/python/tests/integration/test_lftp/test_lftp_protocol.py</files>
  <read_first>
    - src/python/tests/integration/test_lftp/test_lftp_protocol.py (setUp at line 98 building self.lftp; tearDown at ~120-126; the @pytest.mark.timeout(5) error-driving analogs test_queue_dir_wrong_file_type at ~646)
    - src/python/lftp/lftp.py (status() lines 298-313, __run_command at 112, MAX_CONSECUTIVE_STATUS_ERRORS at 12)
    - src/python/tests/unittests/test_lftp/test_job_status_parser.py (the malformed `jobs -v` fixture at 935-942, ending `bad string uh oh`, in test_raises_error_on_bad_status)
  </read_first>
  <behavior>
    - test_status_real_parser_raises_on_malformed_output (TRUTH #1, real parser): with `__run_command` patched to return the malformed `jobs -v` fixture string, the GENUINE `LftpJobStatusParser.parse()` runs and raises `LftpJobStatusParserError`. Proven WITHOUT stubbing parse(): a single `status()` call (counter starts at 0, so the 1st error is swallowed and returns []) confirms the real parser raised by ALSO asserting — via a fresh Lftp or by driving 3 consecutive malformed calls — that a 3rd consecutive REAL-parser failure re-raises `LftpJobStatusParserError`. The malformed input flows through the real parser, not a mock.
    - test_status_parser_error_increments_and_swallows (controller counter): with status() driven against a parser that raises, the 1st and 2nd consecutive calls return [] (counter at 1 then 2, ≤ MAX) and do not raise.
    - test_status_parser_error_exceeds_max_reraises (controller counter): the 3rd consecutive call (counter would become 3 > MAX=2) re-raises LftpJobStatusParserError.
  </behavior>
  <action>
    Extend `TestLftpProtocol` (do NOT create a new file — D-03 requires integration layer using the real `Lftp` fixture from setUp). Add `from lftp import LftpJobStatusParserError` (import the way `LftpError` is imported at the top of the file — verify the symbol is exported) and `from unittest.mock import patch`.

    REAL-PARSER TEST (proves truth #1 end-to-end — Codex blockers 1 & 5 fix): copy the malformed `jobs -v` block from `test_job_status_parser.py:935-942` (the `bad string uh oh` fixture) into a module-level constant or local string. Scope `with patch.object(self.lftp, '_Lftp__run_command', return_value=<malformed_fixture_string>):` so EVERY `status()` call inside the block makes `__run_command("jobs -v")` return the malformed text and the GENUINE `self.lftp._Lftp__job_status_parser.parse(out)` runs against it. Do NOT stub `parse`. Inside the patch block, call `self.lftp.status()` three times: assert the 1st and 2nd return `[]` (the real parser raised, counter ≤ 2, swallowed) and assert the 3rd raises via `with self.assertRaises(LftpJobStatusParserError):` — this proves BOTH that the real parser raises on malformed input (truth #1) AND the >2 re-raise. The patch is REMOVED at block exit, before tearDown's real-binary `exit()`.

    CONTROLLER-COUNTER TESTS (counter logic in isolation — parse-stub acceptable here): for `test_status_parser_error_increments_and_swallows` and `test_status_parser_error_exceeds_max_reraises`, scope `with patch.object(self.lftp._Lftp__job_status_parser, 'parse', side_effect=[LftpJobStatusParserError("Error parsing lftp job status"), ...]):`. For increment/swallow, call `status()` twice and assert each returns `[]`. For max-exceeded, call `status()` twice (counts 1,2) then assert the 3rd raises via `with self.assertRaises(LftpJobStatusParserError):`. These tests scope ONLY the controller counter, so stubbing parse() to raise is correct — truth #1 (real parser) is already covered by the real-parser test above. Keep the patch tightly scoped so tearDown's real-binary `exit()` is unaffected.

    Decorate each test `@pytest.mark.timeout(5)` to match the suite. Note in the SUMMARY which tests use the real parser (via __run_command injection) vs. the parse-stub (controller-counter scope), and that name-mangled access is acceptable white-box coupling for a recovery test.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/integration/test_lftp/test_lftp_protocol.py -k "real_parser or parser_error" -q 2>&1 | tail -10 && poetry run pytest tests/unittests/test_lftp/test_job_status_parser.py -k "bad_status" -q 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - A REAL-parser test exists that patches `_Lftp__run_command` (NOT `parse`) to return the malformed `jobs -v` fixture so the genuine `LftpJobStatusParser.parse()` raises — proving truth #1 end-to-end (grep: the real-parser test does NOT patch `parse`).
    - The real-parser test asserts the 3rd consecutive REAL-parser failure re-raises `LftpJobStatusParserError`.
    - Two controller-counter tests exist (`increment_and_swallows`, `exceeds_max_reraises`); they MAY stub `parse` because they scope only the counter logic.
    - All new tests are `@pytest.mark.timeout(5)`.
    - All patches are scoped via `with patch.object(...)` (not method-level decorators), so they are removed before tearDown's real-binary `exit()`.
    - `LftpJobStatusParserError` is imported at module top.
    - The `-k "real_parser or parser_error"` selection passes, AND the existing `test_raises_error_on_bad_status` parser unit test still passes (verifies the malformed fixture genuinely makes the real parser raise).
  </acceptance_criteria>
  <done>Truth #1 proven by a REAL-parser test (malformed fixture via __run_command injection); controller increment/swallow (≤2) and max-exceeded (>2) re-raise covered and green — no false-positive parse-stub for truth #1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover counter reset on subsequent success</name>
  <files>src/python/tests/integration/test_lftp/test_lftp_protocol.py, src/python/lftp/lftp.py</files>
  <read_first>
    - src/python/tests/integration/test_lftp/test_lftp_protocol.py (the tests added in Task 1)
    - src/python/lftp/lftp.py (the reset line `self.__consecutive_status_errors = 0` at 305, inside the success branch)
  </read_first>
  <behavior>
    - test_status_parser_error_counter_resets_on_success: after one or two parser errors (counter > 0), a subsequent SUCCESSFUL status() resets the counter to 0, proven by then driving two MORE consecutive errors that are swallowed (return []) rather than immediately re-raising — which is only possible if the counter was reset.
  </behavior>
  <action>
    Add one `@pytest.mark.timeout(5)` test to `TestLftpProtocol`. This is controller-counter scope, so the parse-stub is appropriate. Using a scoped `with patch.object(self.lftp._Lftp__job_status_parser, 'parse', side_effect=[...])`, sequence the side_effect as: two `LftpJobStatusParserError(...)` (driving the counter to 2 via two swallowed `status()` calls returning `[]`), then a SUCCESS value (an empty list `[]`, simulating a clean parse) on the next call to exercise the reset at lftp.py:305, then two MORE `LftpJobStatusParserError(...)`. After the success call, assert the two subsequent error calls are AGAIN swallowed (each returns `[]` without raising) — proving the counter reset to 0 (had it not reset, the very next error would be count 3 and would re-raise). This asserts the reset behavior purely through the public `status()` return values, not by reading `_Lftp__consecutive_status_errors`. If the test reveals the reset does NOT happen (line 305 not reached / counter not cleared on success), apply the D-05 trivial-fix posture: the in-scope fix is the single `self.__consecutive_status_errors = 0` reset line already present at lftp.py:305 — verify it is on the success path; if a real >10-line/behavior-change defect is found instead, revert any source edit, leave the test asserting current behavior, and add a one-line STATE.md deferred-items entry referencing this test. Keep the patch scoped so tearDown's real-binary `exit()` runs unpatched.
  </action>
  <verify>
    <automated>cd /Users/julianamacbook/seedsyncarr/src/python && poetry run pytest tests/integration/test_lftp/test_lftp_protocol.py -k "real_parser or parser_error" -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - A new `@pytest.mark.timeout(5)` test asserts that after a successful status() following errors, two further consecutive errors are swallowed (return []) rather than re-raising — proving reset.
    - The assertion is made via `status()` return values only (grep: no `_Lftp__consecutive_status_errors` read in the test).
    - The patch is scoped via `with patch.object(...)`.
    - Any source change to lftp.py is the single-line reset confirmation within the trivial-fix window, or the issue is deferred via a STATE.md entry.
    - All COVMED-03 recovery tests (real-parser + the three controller-counter tests) pass together (pytest exit 0 for `-k "real_parser or parser_error"`).
  </acceptance_criteria>
  <done>Counter-reset-on-success branch covered and green; COVMED-03 fully covered (all four documented points, with truth #1 proven by the real parser).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| lftp subprocess stdout → JobStatusParser | malformed `jobs -v` output (transient or adversarial seedbox state) is parsed; recovery counter is the resilience boundary under test |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Denial of Service | Lftp.status() consecutive-error recovery | mitigate | A real-parser test proves malformed lftp output genuinely raises LftpJobStatusParserError; controller tests prove transient parse errors are swallowed (≤2) so a momentary malformed status does not crash the controller, while persistent errors (>2) still surface |
| T-97-04-SC | Tampering | pip installs | accept | No new packages; uses existing real-lftp integration harness + unittest.mock |
</threat_model>

<verification>
- `cd src/python && poetry run pytest tests/integration/test_lftp/test_lftp_protocol.py -k "real_parser or parser_error" -q` exits 0 (in the containerized real-lftp environment per CI matrix).
- A REAL-parser test (malformed fixture via `_Lftp__run_command` injection, NOT a parse-stub) proves truth #1: the genuine parser raises on malformed input.
- Controller-counter tests cover increment/swallow, max-exceeded re-raise, and reset-on-success.
- The existing `test_raises_error_on_bad_status` parser unit test passes (confirms the reused malformed fixture makes the real parser raise).
- All parser/run-command patches are scoped so tearDown's real-binary exit() is unaffected.
</verification>

<success_criteria>
COVMED-03 closed: LFTP JobStatusParser error recovery is covered — a malformed `jobs -v`
makes the REAL parser raise LftpJobStatusParserError (proven via __run_command injection, not
a stub), the consecutive-error counter increments and swallows while ≤2, the 3rd consecutive
error re-raises, and a subsequent success resets the counter — all via the integration-layer
real-Lftp fixture, with truth #1 proven against the genuine parser.
</success_criteria>

<output>
Create `.planning/phases/97-medium-priority-python-coverage/97-04-SUMMARY.md` when done.
</output>
</content>
