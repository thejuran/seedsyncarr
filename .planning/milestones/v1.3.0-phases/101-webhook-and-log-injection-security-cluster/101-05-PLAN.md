---
phase: 101-webhook-and-log-injection-security-cluster
plan: 05
type: execute
wave: 3
depends_on: ["101-04"]
files_modified:
  - src/python/controller/controller.py
  - src/python/model/model.py
  - src/python/tests/unittests/test_controller/test_controller.py
  - src/python/tests/unittests/test_model/test_model.py
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Every auto-delete timer log site that interpolates the remote-derived scheduled file name is sanitized (no raw newline/control char can reach the log)"
    - "controller.py:229 (exit-time canceled-pending-auto-delete log) is sanitized — it logs a __pending_auto_deletes key, which is the scheduled file name populated from the same remote-derived root_name (re-evaluated, NOT deferred)"
    - "controller.py auto-delete schedule/skip/dry-run/BFS-limit/unsafe-child/partial-import/success sites all route the file name (and unsafe_child.name) through sanitize_log_value"
    - "model/model.py add/remove/update debug logs route the remote-scanner file name through sanitize_log_value"
    - "All sanitized log values are log-output only; the scheduling key, model dict key, matching, and persisted values stay raw"
  artifacts:
    - path: "src/python/controller/controller.py"
      provides: "sanitize_log_value applied at the auto-delete timer + exit-cancel log sites"
      contains: "sanitize_log_value"
    - path: "src/python/model/model.py"
      provides: "sanitize_log_value import + three debug-log call sites"
      contains: "sanitize_log_value"
  key_links:
    - from: "src/python/model/model.py"
      to: "common.sanitize_log_value"
      via: "from common import sanitize_log_value"
      pattern: "sanitize_log_value"
    - from: "src/python/controller/controller.py"
      to: "auto-delete timer + exit-cancel log sites"
      via: "sanitize_log_value(file_name) at each tainted auto-delete logger call"
      pattern: "sanitize_log_value\\(file_name\\)"
  decisions:
    - "D-02: targeted to provably remote-/user-tainted log sites, not a blanket wrap"
    - "D-03 (re-evaluated after adversarial review): controller.py:229 is NO LONGER deferred — it logs a __pending_auto_deletes key, which is the scheduled file name populated at controller.py:817 from the remote-derived root_name (same taint as 820/975). The auto-delete timer cluster (229,820,841,848,866,876,897,926,948) and model-layer sites (model.py:81,97,112) are sanitized here."
---

<objective>
Close the remaining SEC-01 log-injection sinks that Plan 04 handed off: the **auto-delete timer cluster** in controller.py and the **model-layer remote-scanner** debug logs in model/model.py. These all interpolate file names that originate from the seedbox remote directory listing (lftp scan output) — the same remote-influenceable taint class as the sites Plan 04 sanitized — and currently log them raw.

BLOCKER-4 re-evaluation of the previously-deferred controller.py:229: it logs a key from `__pending_auto_deletes` (the exit-time timer-cancel loop). That dict is populated at controller.py:817 inside `__schedule_auto_delete`, whose `file_name` argument is the remote-derived `root_name` passed from `__check_webhook_imports` (controller.py:803-804). So :229 logs the SAME scheduled/remote-derived name as :820 — deferring it is unsound. It is sanitized here, NOT deferred.

Site set for THIS plan:
- controller.py auto-delete timer sinks (all log the scheduled `file_name`, the remote-derived name):
  - 820 (`__schedule_auto_delete`: "Scheduled auto-delete of '{file_name}'")
  - 841 (`__execute_auto_delete`: "Auto-delete skipped ... feature was disabled")
  - 848 (dry-run: "DRY-RUN: Would delete local file '{file_name}'")
  - 866 (file no longer in model)
  - 876 (skipped: file in non-deletable state)
  - 897 (skipped: BFS node limit exceeded)
  - 926 (skipped: unsafe child — also interpolates `unsafe_child.name`, a remote-scanner child name → sanitize BOTH the file_name and unsafe_child.name)
  - 948 (skipped: partial import — interpolates file_name; the `shown` list is a list of basenames, leave the list rendering as-is but sanitize the primary file_name)
- controller.py:229 (`exit`: "Canceled pending auto-delete for '{file_name}'") — re-included.
- model/model.py debug logs of remote-scanner file names:
  - 81 (`add_file`: "LftpModel: Adding file '{file.name}'")
  - 97 (`remove_file`: "LftpModel: Removing file '{filename}'")
  - 112 (`update_file`: "LftpModel: Updating file '{file.name}'")

INCLUDE the model-layer sites (do NOT silently drop): they are remote-scanner-sourced (model_builder feeds names from remote_scanner.py / system/file.py), debug-level but still a CWE-117 surface. CONTEXT note recorded below.

This is strict hardening (log-output only). The scheduling dict key, the model `__files` dict key, the BFS traversal, the delete dispatch, and all persisted/return values stay RAW — only the interpolated log variable is sanitized (D-01/D-02). The matching/coverage logic that uses file_name lower-cased for dict lookups is untouched.

Purpose: Complete the SEC-01 taint coverage so NO remote-/user-derived file name reaches a log line unescaped across the webhook→auto-delete→model lifecycle.
Output: ~10 controller.py auto-delete/exit log sites + 3 model.py sites routed through sanitize_log_value; tests pinning escaped output and raw scheduling/model keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-RESEARCH.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-PATTERNS.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-VALIDATION.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-01-SUMMARY.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-04-SUMMARY.md

<interfaces>
sanitize_log_value comes from Plan 01 (Wave 1): from common import sanitize_log_value
  signature: def sanitize_log_value(value: str) -> str   # CR/LF -> \r/\n, other C0 controls/DEL -> \xHH; plain unchanged

After Plan 04, controller.py already imports sanitize_log_value on line 19 (from common import Context, AppError, MultiprocessingLogger, sanitize_log_value). This plan REUSES that import — do not re-add it; just add the new call sites.

From src/python/controller/controller.py (taint flow proving these are remote-derived):
  - __check_webhook_imports schedules per remote-resolved root: line 803-804 `for root_name in {r for r, _ in newly_imported}: self.__schedule_auto_delete(root_name)`.
  - __schedule_auto_delete(file_name): line 810-820 stores the timer under file_name in __pending_auto_deletes and logs at 820.
  - __execute_auto_delete(file_name): lines 834-975 — the skip/dry-run/BFS/unsafe-child/partial/success log sites all interpolate file_name (and 926 interpolates unsafe_child.name from the model BFS).
  - exit(): lines 226-230 iterate list(self.__pending_auto_deletes.items()) and log each key (file_name) at 229.

From src/python/model/model.py (current import line ~1-10 — add `from common import sanitize_log_value` if not present; model.py does not currently import from common, so add the import):
  - add_file (line 77): line 81 `self.logger.debug("LftpModel: Adding file '{}'".format(file.name))`
  - remove_file (line 93): line 97 `... Removing file '{}'".format(filename)`
  - update_file (line 108): line 112 `... Updating file '{}'".format(file.name)`
  The __files dict key (file.name / filename) is used for storage and lookup — keep it RAW; only the logged value is wrapped.

From tests:
  - tests/unittests/test_controller/test_controller.py — has controller setup; add auto-delete log-sanitization assertions (assertLogs on the controller logger). The auto-delete path is Timer-driven; drive __execute_auto_delete / __schedule_auto_delete directly (name-mangled) or via the existing auto-delete test harness with a CRLF-bearing name.
  - tests/unittests/test_model/test_model.py — has model tests; add cases asserting add/remove/update logs escape a CRLF-bearing file name while the model still stores/looks up the raw name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sanitize the auto-delete timer + exit-cancel log sites in controller.py</name>
  <files>src/python/controller/controller.py, src/python/tests/unittests/test_controller/test_controller.py</files>
  <read_first>
    - src/python/controller/controller.py (file being modified — import line 19 already has sanitize_log_value from Plan 04; exit-cancel line 229; __schedule_auto_delete line 806-821 (log 820); __execute_auto_delete lines 823-975 (logs 841,848,866,876,897,926,948))
    - src/python/tests/unittests/test_controller/test_controller.py (file being modified — existing auto-delete test harness / controller fixture is the analog)
    - 101-PATTERNS.md §"src/python/controller/controller.py — replace + add sanitize_log_value calls"
    - 101-CONTEXT.md D-02/D-03 (re-evaluated: 229 included, not deferred)
  </read_first>
  <behavior>
    - test_schedule_auto_delete_log_sanitized: __schedule_auto_delete with a CRLF-bearing file_name -> the "Scheduled auto-delete" log line has no literal newline; __pending_auto_deletes still keyed by the RAW file_name (the timer can still be canceled/looked up by the exact original name)
    - test_execute_auto_delete_skip_log_sanitized: drive __execute_auto_delete with a CRLF-bearing file_name through a skip branch (e.g. feature disabled or file not in model) -> the skip log line is escaped
    - test_exit_cancel_log_sanitized: schedule a pending auto-delete with a CRLF-bearing name, call exit() -> the "Canceled pending auto-delete" log line (229) is escaped; the dict is cleared
  </behavior>
  <action>In controller.py, REUSE the sanitize_log_value import added in Plan 04 (line 19) — do not re-import. Wrap the interpolated remote-derived file name at each of these logger calls with sanitize_log_value(...): line 820 (Scheduled auto-delete), 841 (feature disabled), 848 (DRY-RUN would delete), 866 (no longer in model), 876 (non-deletable state), 897 (BFS node limit), 926 (unsafe child — wrap BOTH file_name AND unsafe_child.name), 948 (partial import — wrap file_name; leave the numeric counts and the `shown` basename list rendering unchanged), and line 229 (exit: canceled pending auto-delete). For each, sanitize ONLY the value passed into the format/interpolation for logging — do NOT change __pending_auto_deletes keys/lookups (810-817, 836), the model.get_file(file_name) lookup (863), the BFS, imported_children.pop(file_name) (906, 970), or any control flow. Add the three tests from <behavior> to test_controller.py, driving the name-mangled __schedule_auto_delete / __execute_auto_delete / exit with a CRLF-bearing name and asserting escaped log output plus raw dict keys. Write tests first (RED), then implement (GREEN).</action>
  <verify>
    <automated>cd src/python && python -m pytest tests/unittests/test_controller/ -x -q && test "$(grep -v '^[[:space:]]*#' controller/controller.py | grep -c 'sanitize_log_value')" -ge "14" && echo "plan04 (6) + plan05 (>=8 auto-delete/exit) sanitize calls present" && grep -q 'sanitize_log_value(unsafe_child.name)' controller/controller.py && echo "unsafe_child.name wrapped" && grep -n 'Canceled pending auto-delete' controller/controller.py | head -1</automated>
  </verify>
  <done>All listed auto-delete timer log sites (820/841/848/866/876/897/926/948) and the exit-cancel site (229) wrap the file name (and unsafe_child.name at 926) in sanitize_log_value; >=14 total sanitize_log_value occurrences in controller.py (Plan 04's 6 + Plan 05's auto-delete/exit set); scheduling dict keys, model lookups, BFS, and persist values stay raw; the three new tests pass; existing test_controller suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sanitize the model-layer remote-scanner debug log sites in model/model.py</name>
  <files>src/python/model/model.py, src/python/tests/unittests/test_model/test_model.py</files>
  <read_first>
    - src/python/model/model.py (file being modified — imports at top; add_file line 77-81, remove_file line 93-97, update_file line 108-112)
    - src/python/tests/unittests/test_model/test_model.py (file being modified — existing add/remove/update tests are the analog; reuse a ModelFile fixture)
    - 101-PATTERNS.md (sanitize_log_value usage pattern)
    - 101-CONTEXT.md D-02 (remote-scanner-sourced names are in-scope per the re-derived taint set)
  </read_first>
  <behavior>
    - test_add_file_log_sanitized: add_file with a ModelFile whose name contains CRLF -> the "Adding file" debug log has no literal newline; the file is still stored under and retrievable by the RAW name
    - test_remove_file_log_sanitized: remove_file with a CRLF-bearing filename -> the "Removing file" debug log is escaped; removal still works on the raw key
    - test_update_file_log_sanitized: update_file with a CRLF-bearing name -> the "Updating file" debug log is escaped; update still keyed on the raw name
  </behavior>
  <action>In model/model.py, add `from common import sanitize_log_value` to the imports (model.py does not currently import from common — add the import; common/types.py has zero circular-import risk with model, per Plan 01 research). Wrap the interpolated name in each debug log: line 81 sanitize_log_value(file.name), line 97 sanitize_log_value(filename), line 112 sanitize_log_value(file.name). Sanitize ONLY the logged value — the `if file.name in self.__files`, `self.__files[file.name] = file`, `del self.__files[filename]`, and all listener notifications must keep using the RAW name/filename. Add the three tests from <behavior> to test_model.py asserting escaped debug output (assertLogs at DEBUG) and that storage/lookup/removal still operate on the raw key. Write tests first (RED), then implement (GREEN).</action>
  <verify>
    <automated>cd src/python && python -m pytest tests/unittests/test_model/test_model.py -x -q && grep -q 'from common import sanitize_log_value' model/model.py && test "$(grep -v '^[[:space:]]*#' model/model.py | grep -c 'sanitize_log_value')" -ge "4" && echo "model.py import + 3 call sites OK"</automated>
  </verify>
  <done>model/model.py imports sanitize_log_value and wraps the name in the add/remove/update debug logs (>=4 occurrences: import + 3 calls); __files keys, lookups, deletions, and listener notifications stay raw; the three new tests pass; existing test_model suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seedbox remote directory listing → auto-delete timer log sinks | The scheduled file name (remote-derived root_name) is logged across the schedule/execute/exit lifecycle; CR/LF/control bytes can forge or split log entries (CWE-117) |
| remote scanner → model debug log sinks | Model add/remove/update debug logs interpolate remote-scanner file names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-16 | Tampering | controller.py auto-delete timer sinks (820,841,848,866,876,897,926,948) | mitigate | Wrap the scheduled remote-derived file name (and unsafe_child.name at 926) in sanitize_log_value at every auto-delete log line; tests pin escaped output. |
| T-101-17 | Tampering | controller.py:229 (exit-time canceled-pending log) | mitigate | Previously deferred; re-evaluated as tainted (logs a __pending_auto_deletes key = scheduled remote-derived name, controller.py:817 ← 803-804). Sanitized, not deferred. test_exit_cancel_log_sanitized pins it. |
| T-101-18 | Tampering | model/model.py:81,97,112 (remote-scanner debug logs) | mitigate | Wrap the model file name in the add/remove/update debug logs; remote-scanner-sourced names can no longer inject newlines/control chars even at DEBUG level. Recorded as an explicit inclusion (not a silent drop). |
| T-101-19 | Tampering / Integrity (INTRODUCED) | sanitizing a value used as a dict key or lookup instead of only the log value | mitigate | Only the interpolated log variable is wrapped; __pending_auto_deletes keys, model __files keys, get_file lookups, BFS traversal, imported_children.pop, and listener notifications all keep the RAW name. Tests assert scheduling/storage/lookup still operate on the unsanitized key. |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH.md Package Legitimacy Audit). Gate not triggered. |
</threat_model>

<verification>
- cd src/python && python -m pytest tests/unittests/test_controller/ tests/unittests/test_model/ -x -q — all green.
- Grep gates inspect BOTH sanitized files (controller.py and model.py) — no sanitized file is left ungated (BLOCKER-4 false-green fix).
- controller.py:229 is sanitized (re-evaluated, not deferred): grep + test_exit_cancel_log_sanitized.
- unsafe_child.name (926) sanitized: grep confirms sanitize_log_value(unsafe_child.name).
- model-layer sites (81/97/112) sanitized: model.py import + >=4 occurrences; three test_model cases pass.
- Raw-key invariants held: scheduling dict keys, model __files keys, lookups, BFS, persist values unchanged (tests assert).
- Full-suite gate before verify-work: cd src/python && python -m pytest --cov --cov-report=term-missing -x — green with fail_under=88 held or raised.
</verification>

<success_criteria>
- Every remote-/user-derived file name reaching a log line across the webhook→auto-delete→model lifecycle is escaped (Plan 04 webhook/command cluster + Plan 05 auto-delete/exit/model cluster = complete SEC-01 coverage).
- controller.py:229 is sanitized (the prior deferral is closed as unsound).
- model/model.py:81,97,112 are sanitized (explicit inclusion, not a silent drop).
- All sanitization is log-output only; scheduling/model dict keys, lookups, BFS, matching, and persisted values stay raw.
- Python coverage floor (fail_under=88) holds or rises. No release/tag/version work.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-05-SUMMARY.md` when done.

CONTEXT note (taint-set re-derivation, carry into phase wrap-up): the SEC-01 taint set was re-derived after adversarial review. controller.py:229 is NO LONGER deferred (it logs the remote-derived scheduled name). The model-layer remote-scanner debug logs (model/model.py:81,97,112) are now INCLUDED rather than dropped. The lftp/job_status_parser.py:725 parse-error output and lftp/lftp.py job-name logs remain out of scope this slice (parse-error context, lower risk) — record as an explicit deferred-item note for a future audit, not a silent omission.
</output>
