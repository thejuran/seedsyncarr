---
phase: 101-webhook-and-log-injection-security-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/common/types.py
  - src/python/common/__init__.py
  - src/python/tests/unittests/test_common/test_types.py
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "sanitize_log_value() exists as a shared importable helper"
    - "sanitize_log_value escapes CR and LF to their literal escape sequences"
    - "sanitize_log_value leaves plain strings unchanged"
    - "The helper is importable as `from common import sanitize_log_value`"
  artifacts:
    - path: "src/python/common/types.py"
      provides: "sanitize_log_value(value: str) -> str helper"
      contains: "def sanitize_log_value"
    - path: "src/python/common/__init__.py"
      provides: "re-export of sanitize_log_value"
      contains: "sanitize_log_value"
    - path: "src/python/tests/unittests/test_common/test_types.py"
      provides: "TestSanitizeLogValue unit tests"
      contains: "class TestSanitizeLogValue"
  key_links:
    - from: "src/python/common/__init__.py"
      to: "src/python/common/types.py"
      via: "from .types import ... sanitize_log_value"
      pattern: "from .types import .*sanitize_log_value"
  decisions:
    - "D-01: shared sanitize_log_value() helper in common/types.py, re-exported from common/__init__.py, preserving inline CR/LF escape behavior"
---

<objective>
Create the shared `sanitize_log_value()` log-injection helper (SEC-01, CWE-117) in `common/types.py` and re-export it from `common/__init__.py`. This is the leaf dependency: Plan 04 replaces the three inline `.replace("\n","\\n").replace("\r","\\r")` copies and adds two new call sites — all five depend on this helper existing first.

This plan ONLY creates the helper and its unit tests. It does NOT change any call site (that is Plan 04). Production log output is byte-identical until Plan 04 runs, so this plan is behavior-neutral on its own.

Purpose: One definition, no copy-paste drift, single source of truth for CWE-117 escaping (D-01).
Output: `sanitize_log_value()` in `common/types.py`, re-export in `common/__init__.py`, `TestSanitizeLogValue` unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-RESEARCH.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-PATTERNS.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-VALIDATION.md

<interfaces>
<!-- Current full contents of common/types.py (small file) — append after overrides(). -->
From src/python/common/types.py:
```python
import inspect

def overrides(interface_class):
    """Decorator to check that decorated method is a valid override ..."""
    assert(inspect.isclass(interface_class)), "Overrides parameter must be a class type"
    def overrider(method):
        assert(method.__name__ in dir(interface_class)), "Method does not override super class"
        return method
    return overrider
```

From src/python/common/__init__.py (line 1):
```python
from .types import overrides as overrides
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for sanitize_log_value</name>
  <files>src/python/tests/unittests/test_common/test_types.py</files>
  <read_first>
    - src/python/tests/unittests/test_common/test_types.py (the file being modified; replicate the `TestOverrides` class style — unittest.TestCase, plain assertEqual)
    - 101-PATTERNS.md §"tests/unittests/test_common/test_types.py — add TestSanitizeLogValue class" (exact test body template)
  </read_first>
  <behavior>
    - test_plain_string_unchanged: sanitize_log_value("hello world") == "hello world"
    - test_newline_escaped: sanitize_log_value("line1\nline2") == "line1\\nline2"
    - test_carriage_return_escaped: sanitize_log_value("line1\rline2") == "line1\\rline2"
    - test_crlf_both_escaped: sanitize_log_value("line1\r\nline2") == "line1\\r\\nline2"
    - test_empty_string: sanitize_log_value("") == ""
    - test_multiple_newlines: sanitize_log_value("a\nb\nc") == "a\\nb\\nc"
  </behavior>
  <action>Add a `TestSanitizeLogValue(unittest.TestCase)` class to test_types.py with the six test methods named in <behavior>. Import the helper via `from common import sanitize_log_value` (the import will fail until Task 2 lands the re-export — that is the RED state). Use plain `self.assertEqual(expected, sanitize_log_value(input))` assertions matching the existing `TestOverrides` style. Do NOT test control-character stripping beyond CR/LF — the helper intentionally matches the existing inline scope (only `\n` and `\r`) so existing log output is preserved. Run the test to confirm it fails (RED) on the missing import.</action>
  <verify>
    <automated>cd src/python && python -m pytest tests/unittests/test_common/test_types.py::TestSanitizeLogValue -x -q 2>&1 | grep -Eq "ImportError|cannot import name|collected 6 items.*[1-9] (failed|error)" && echo RED-CONFIRMED</automated>
  </verify>
  <done>test_types.py contains class TestSanitizeLogValue with 6 test methods; running them fails because sanitize_log_value cannot yet be imported (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sanitize_log_value and re-export it</name>
  <files>src/python/common/types.py, src/python/common/__init__.py</files>
  <read_first>
    - src/python/common/types.py (file being modified — append after `overrides()`; imports only stdlib `inspect`, zero circular-import risk)
    - src/python/common/__init__.py (file being modified — line 1 is the `overrides` re-export to extend)
    - 101-PATTERNS.md §"src/python/common/types.py — add sanitize_log_value()" (exact signature + Google-style docstring template)
  </read_first>
  <action>In `common/types.py`, append a module-level function `def sanitize_log_value(value: str) -> str:` after `overrides()`. Body returns `value.replace("\r", "\\r").replace("\n", "\\n")` — escape `\r` first then `\n`, matching the research-recommended order. Give it a Google-style docstring (Args/Returns) naming CWE-117 log injection, mirroring the existing function style. Do NOT expand scope to strip other control characters — only CR/LF, to keep existing log output identical. In `common/__init__.py`, extend line 1 to `from .types import overrides as overrides, sanitize_log_value as sanitize_log_value` (explicit re-export alias matching the existing convention). Tests from Task 1 must now pass (GREEN).</action>
  <verify>
    <automated>cd src/python && python -m pytest tests/unittests/test_common/test_types.py::TestSanitizeLogValue -x -q && python -c "from common import sanitize_log_value; assert sanitize_log_value('a\nb')=='a\\nb' and sanitize_log_value('a\rb')=='a\\rb' and sanitize_log_value('plain')=='plain'; print('OK')"</automated>
  </verify>
  <done>sanitize_log_value exists in common/types.py, is re-exported from common/__init__.py, importable as `from common import sanitize_log_value`, and all 6 unit tests pass (GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook/*arr callback → log sink | Untrusted *arr-supplied filenames/titles cross into log lines; CR/LF can forge or split log entries (CWE-117) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Tampering | sanitize_log_value (common/types.py) | mitigate | Escape CR/LF to literal `\r`/`\n` so crafted payloads cannot inject newlines into the log stream; unit tests pin the exact escaping. This plan only builds the primitive — application at call sites is Plan 04. |
| T-101-02 | Tampering | scope creep (over-stripping control chars) | accept | Helper deliberately limited to CR/LF to preserve existing log output byte-for-byte; broader control-char stripping is out of scope (D-02) and would risk changing existing-site output. No introduced threat: the primitive is strictly stronger than the status quo when applied. |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No new packages — RESEARCH.md §"Package Legitimacy Audit" confirms zero new external dependencies for phase 101. No install task, so the legitimacy gate is not triggered. |
</threat_model>

<verification>
- `cd src/python && python -m pytest tests/unittests/test_common/test_types.py -x -q` — all green.
- `cd src/python && python -c "from common import sanitize_log_value"` — import succeeds.
- No production call site changed in this plan (grep confirms inline `.replace("\n", "\\n")` copies still present until Plan 04): `grep -rc 'replace("\\\\n"' src/python/controller/webhook_manager.py` returns a nonzero count (still 2 inline copies — Plan 04 removes them).
</verification>

<success_criteria>
- sanitize_log_value(value: str) -> str defined in common/types.py with CR/LF escaping only.
- Re-exported from common/__init__.py; importable as `from common import sanitize_log_value`.
- TestSanitizeLogValue (6 cases) passes.
- COMPAT: no call site touched; existing log output unchanged this plan. Python coverage floor (fail_under=88) holds or rises (new helper is covered).
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-01-SUMMARY.md` when done.
</output>
