---
phase: 101-webhook-and-log-injection-security-cluster
plan: 04
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - src/python/controller/webhook_manager.py
  - src/python/controller/controller.py
  - src/python/tests/unittests/test_controller/test_webhook_manager.py
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "The three inline .replace newline copies are replaced by sanitize_log_value() calls (no inline copies remain)"
    - "webhook_manager.py logs webhook-supplied file names through sanitize_log_value at both sites"
    - "controller.py logs the webhook matched_name through sanitize_log_value (line 790 site)"
    - "controller.py sanitizes the two remote-scanner-sourced borderline sites (root_name mapping lookup, auto-deleted file name)"
    - "A CR/LF-bearing webhook file name appears escaped (no literal newline) in log output"
    - "The queue/matching value is used raw and unchanged — only log output is sanitized"
  artifacts:
    - path: "src/python/controller/webhook_manager.py"
      provides: "sanitize_log_value import + two call sites (replacing inline escapes)"
      contains: "sanitize_log_value"
    - path: "src/python/controller/controller.py"
      provides: "sanitize_log_value import + three call sites (790 replace, 760+975 new)"
      contains: "sanitize_log_value"
  key_links:
    - from: "src/python/controller/webhook_manager.py"
      to: "common.sanitize_log_value"
      via: "from common import Context, sanitize_log_value"
      pattern: "from common import .*sanitize_log_value"
    - from: "src/python/controller/controller.py"
      to: "common.sanitize_log_value"
      via: "from common import ... sanitize_log_value"
      pattern: "sanitize_log_value"
---

<objective>
Apply the shared sanitize_log_value() helper (built in Plan 01) at the locked SEC-01 call-site set (D-01 + D-03), closing the CWE-117 log-injection gap for webhook-/remote-sourced file names.

Locked site set (5 total):
- REPLACE the three inline newline-escape copies with sanitize_log_value(...): webhook_manager.py:37, webhook_manager.py:76, controller.py:790.
- ADD sanitization at the two borderline remote-scanner-sourced sites confirmed in D-03: controller.py:760 (webhook root-name mapping lookup) and controller.py:975 (auto-deleted local file name).

DEFERRED (do NOT plan): controller.py:229 (canceled pending auto-delete) — logs an internal dict key validated upstream, lower risk; CONTEXT.md D-03 records it as a deferred-item note. Leave it untouched.

This is a behavior-neutral refactor at the three replacement sites (the helper produces the same escaping as the inline copies) and a strict hardening at the two new sites. The queue/matching value stays raw — only the LOG variable is sanitized (D-01/D-02).

Purpose: One sanitizer, no copy-paste drift, complete coverage of provably remote-/webhook-tainted log sinks.
Output: 5 call sites routed through sanitize_log_value; a CR/LF-injection behavioral test pinning escaped log output.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-RESEARCH.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-PATTERNS.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-VALIDATION.md
@.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-01-SUMMARY.md

<interfaces>
sanitize_log_value comes from Plan 01 (Wave 1, already merged).

From src/python/common/__init__.py (after Plan 01):
  from .types import overrides as overrides, sanitize_log_value as sanitize_log_value
  signature: def sanitize_log_value(value: str) -> str   # escapes CR then LF; plain strings unchanged

From src/python/controller/webhook_manager.py — current import (line 4): from common import Context
  Site 1 (line 37, enqueue_import) and Site 2 (line 76, process drain loop), both currently do an
  inline .replace newline-escape on file_name into safe_file_name. The queue value file_name passed
  to name_to_root.get(file_name.lower()) and to the returned (root_name, file_name) tuple stays RAW
  — only safe_file_name is for logging.

From src/python/controller/controller.py — current import (line 19):
    from common import Context, AppError, MultiprocessingLogger
  Site 3 (line 790, __check_webhook_imports): inline .replace newline-escape on matched_name into safe_matched.
  Site 4 (line 760, ModelError branch): logs root_name unsanitized in a debug message.
  Site 5 (line 975, auto-delete): logs file_name unsanitized in an info message.

From tests/unittests/test_controller/test_webhook_manager.py: TestWebhookManager(unittest.TestCase);
  WebhookManager(MagicMock()) construction; existing logging tests (test_enqueue_logs_info,
  test_child_file_match_logs_root_name) assert on the logger. Use self.assertLogs on the module
  logger, or inspect the logger mock call args, to assert escaped output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route webhook_manager log sites through sanitize_log_value + injection test</name>
  <files>src/python/controller/webhook_manager.py, src/python/tests/unittests/test_controller/test_webhook_manager.py</files>
  <read_first>
    - src/python/controller/webhook_manager.py (file being modified — import line 4, site line 37 enqueue_import, site line 76 process drain loop)
    - src/python/tests/unittests/test_controller/test_webhook_manager.py (file being modified — TestWebhookManager; test_enqueue_logs_info + test_child_file_match_logs_root_name are the logging-assertion analogs)
    - 101-PATTERNS.md section "src/python/controller/webhook_manager.py — replace inline escapes"
    - 101-CONTEXT.md D-01/D-02 (queue value stays raw; only log var sanitized)
  </read_first>
  <behavior>
    - test_enqueue_sanitizes_newlines_in_log: enqueue_import with a file_name containing CRLF -> the logged message contains the escaped backslash-r backslash-n form and contains NO literal newline character
    - test_queue_value_unchanged_with_newline: a file_name with an embedded newline that matches a model entry -> the returned matched tuple preserves the RAW value (sanitization does not alter matching/return data)
  </behavior>
  <action>In webhook_manager.py change the import to: from common import Context, sanitize_log_value. Replace the line 37 assignment with safe_file_name = sanitize_log_value(file_name) (enqueue_import), and the line 76 assignment with safe_file_name = sanitize_log_value(file_name) (process drain loop). Do NOT change the queue put/get, the name_to_root.get(file_name.lower()) lookup, or the returned tuple — those keep the raw value (D-01). Add the two tests from the behavior block to TestWebhookManager, asserting on the captured log message (assertLogs on the module logger, or the logger mock call args) that newlines are escaped and that the returned/queued value is raw. Write tests first (RED), then implement (GREEN).</action>
  <verify>
    <automated>cd src/python && python -m pytest tests/unittests/test_controller/test_webhook_manager.py -x -q && test "$(grep -v '^[[:space:]]*#' controller/webhook_manager.py | grep -c 'sanitize_log_value')" = "3" && grep -v '^[[:space:]]*#' controller/webhook_manager.py | grep -q 'replace("\\n"' && echo INLINE-LEFTOVER || echo CLEAN</automated>
  </verify>
  <done>Import updated; both webhook_manager log sites use sanitize_log_value (3 occurrences: import + 2 calls); no inline .replace newline copy remains (CLEAN printed); queue/return value raw; both new tests pass; existing TestWebhookManager tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Route controller log sites through sanitize_log_value (790 replace, 760 + 975 add)</name>
  <files>src/python/controller/controller.py</files>
  <read_first>
    - src/python/controller/controller.py (file being modified — import line 19, site 790 __check_webhook_imports, site 760 ModelError branch, site 975 auto-delete)
    - 101-PATTERNS.md section "src/python/controller/controller.py — replace + add sanitize_log_value calls"
    - 101-CONTEXT.md D-03 (exact 3-site controller set; line 229 is DEFERRED, leave untouched)
  </read_first>
  <action>In controller.py extend the import on line 19 to include sanitize_log_value (append to the existing: from common import Context, AppError, MultiprocessingLogger). Site 3 (line 790): replace the inline safe_matched = matched_name newline-escape with safe_matched = sanitize_log_value(matched_name) (behavior-neutral). Site 4 (line 760, the ModelError debug log): wrap the interpolated root_name argument in sanitize_log_value(root_name). Site 5 (line 975, the auto-deleted-file info log): wrap the interpolated file_name argument in sanitize_log_value(file_name). Do NOT touch line 229 (deferred). Do NOT sanitize the canonical root_name in the matched-import info log near line 792 (model-internal value, out of scope per PATTERNS.md). Change no control-flow or non-log values.</action>
  <verify>
    <automated>cd src/python && test "$(grep -v '^[[:space:]]*#' controller/controller.py | grep -c 'sanitize_log_value')" -ge "4" && grep -v '^[[:space:]]*#' controller/controller.py | grep -q 'matched_name.replace("\\n"' && echo INLINE-LEFTOVER || echo CLEAN && python -m pytest tests/unittests/test_controller/ -x -q</automated>
  </verify>
  <done>Import extended on line 19 (sanitize_log_value present); site 790 inline escape replaced; sites 760 and 975 wrapped (>=4 sanitize_log_value occurrences: import + 3 calls); CLEAN printed (no matched_name inline escape); line 229 untouched; existing test_controller suite green.</done>
</task>

</tasks>

<verification>
- cd src/python && python -m pytest tests/unittests/test_controller/ -x -q — all green.
- No inline newline-escape copies remain at the three replacement sites (grep gates filter comment lines with grep -v before counting, so header prose cannot self-invalidate the gate).
- CR/LF injection escaped in log output: test_enqueue_sanitizes_newlines_in_log passes.
- Queue/return value raw: test_queue_value_unchanged_with_newline passes.
- controller.py:229 untouched (deferred per D-03).
- Full-suite gate before verify-work: cd src/python && python -m pytest --cov --cov-report=term-missing -x — green with fail_under=88 held or raised.
</verification>

<success_criteria>
- All five locked SEC-01 sites route through sanitize_log_value (3 replacements + 2 additions).
- No inline .replace newline-escape copy remains at any of the three replacement sites.
- controller.py:229 is untouched (deferred).
- A CR/LF-bearing webhook file name is escaped in log output; the queue/matching/return value is unchanged (raw).
- Python coverage floor (fail_under=88) holds or rises. No release/tag/version work.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *arr webhook JSON body → log sink | File names/titles from webhook callbacks reach log lines; CR/LF can forge or split log entries (CWE-117) |
| seedbox remote directory listing → log sink | Remote-scanner-sourced model file names (root_name, auto-deleted name) also carry attacker-influenceable bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-11 | Tampering | webhook_manager.py:37,76 + controller.py:790 (webhook-tainted log sinks) | mitigate | Replace inline copies with sanitize_log_value so a single audited helper escapes CR/LF; behavioral test pins escaped output. |
| T-101-12 | Tampering | controller.py:760,975 (remote-scanner-sourced log sinks) | mitigate | Add sanitize_log_value at the two borderline sites confirmed remote-sourced (D-03), extending CWE-117 coverage beyond direct webhook payloads. |
| T-101-13 | Tampering / Integrity (INTRODUCED) | sanitizing the matching/queue value instead of only the log value | mitigate | Only the log variable is wrapped; the raw file_name remains the matching key and tuple value. test_queue_value_unchanged_with_newline pins that matching/return data is untouched, so sanitization cannot break import matching. |
| T-101-14 | Tampering (residual/accepted) | controller.py:229 (deferred site) | accept | Logs an internal dict key validated upstream; lower risk per D-03. Recorded as a deferred-item note, not closed this phase. |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH.md Package Legitimacy Audit). Gate not triggered. |
</threat_model>

<output>
Create `.planning/milestones/v1.3.0-phases/101-webhook-and-log-injection-security-cluster/101-04-SUMMARY.md` when done.

Deferred-item note to carry forward (do NOT implement here): controller.py:229 (canceled pending auto-delete) log-injection sanitization — internal dict key, lower risk, deferred per CONTEXT.md D-03.
</output>
