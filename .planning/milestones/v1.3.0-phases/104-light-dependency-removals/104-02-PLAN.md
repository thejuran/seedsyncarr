---
phase: 104-light-dependency-removals
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md
autonomous: false
requirements: [DEPS-01a, DEPS-01c]
must_haves:
  truths:
    - "ng build --configuration production completes without error after both removals (D-01: clean-production-build half of the removal-rigor proof)"
    - "The production dist contains no jQuery library code (no 'jQuery JavaScript Library' factory header) and no css-element-queries code (no ResizeSensor/ElementQueries)"
    - "Total production bundle size is equal to or smaller than the pre-removal baseline (no regression)"
    - "All Bootstrap-driven interactions (nav, dropdowns/bell, dashboard rows + action bar, settings form controls, confirm modal) render and function with no console errors referencing jQuery or a missing dependency (D-01: manual Bootstrap smoke-test half of the removal-rigor proof)"
    - "Karma check.global floors (stmts/branches/fns/lines 83/68/79/83) hold or rise"
  artifacts:
    - path: ".planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md"
      provides: "before/after bundle delta record (D-02 after section appended)"
      contains: "AFTER"
  key_links:
    - from: "src/angular/package.json"
      to: "src/angular/dist"
      via: "ng build --configuration production produces a dist with neither dep's code"
      pattern: "jQuery JavaScript Library"
---

<objective>
After both phantom deps are removed (plan 104-01), prove the removal is safe and complete: a clean production build, a before/after bundle-size delta showing no regression (D-02), a dist grep confirming no residual jQuery or css-element-queries library code, a manual Bootstrap-interaction smoke test (D-01), and Karma coverage floors holding.

Purpose: D-01 (LOCKED) requires "unused" to be proven by static grep AND a clean `ng build --configuration production` AND a manual smoke-test of Bootstrap dropdowns/modals/collapses — static evidence alone is insufficient. This plan supplies the build + bundle + smoke + CI-floor evidence.
Output: An updated `104-BUNDLE-BASELINE.md` with the AFTER table and computed delta, a clean dist grep result, a Karma floors-held result, and a human-verified smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-01-SUMMARY.md

<notes>
- Depends on 104-01: both deps must already be removed from package.json + package-lock.json before this plan runs.
- D-01 (LOCKED): manual Bootstrap smoke-test is the load-bearing check for jQuery removal and is mandatory (not optional).
- D-02 (LOCKED): record the AFTER `ng build --configuration production` table next to the BEFORE table in 104-BUNDLE-BASELINE.md and compute the delta. Expected delta ≈ zero (neither dep was ever bundled) or a tiny shrink (license metadata). Frame as "no regression," NOT a savings claim. The esbuild builder does NOT support `--stats-json` — use the stdout table.
- Dist grep gotchas (RESEARCH Pitfalls 2 & 3): `window.jQuery` appears in `scripts-[hash].js` as Bootstrap's OPTIONAL no-op detection — that is NOT jQuery the library and is ACCEPTABLE. The pass/fail signal is the ABSENCE of `"jQuery JavaScript Library"` (jQuery's factory header) and the ABSENCE of `ResizeSensor`/`ElementQueries`. Do NOT gate on a bare `grep "jquery"` of dist.
- Smoke-test surface (RESEARCH): nav render, notification bell dropdown, dashboard file rows + selection/action-bar, settings form controls, confirm modal (Renderer2-based from Phase 103). The app uses Bootstrap CSS classes (no `data-bs-toggle` JS-plugin usage); confirm modal does not use Bootstrap's Modal class.
- Karma floors: stmts/branches/fns/lines 83/68/79/83 (karma.conf.js check.global). No source changed, so no new unit tests are needed — floors must simply still hold.
- No release/tag/version work in this phase (ROADMAP SC5).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build, before/after bundle delta, and dist residual-string grep</name>
  <files>.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md (the BEFORE table written by 104-01 Task 1; this file is also the file being modified)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Bundle/Build Verification Approach; Residual String Verification; Anti-Patterns to Avoid)
    - .planning/milestones/v1.3.0-phases/104-01-SUMMARY.md (confirm both deps already removed)
  </read_first>
  <action>
    Run `ng build --configuration production` from `src/angular/`. Append the resulting "Initial Chunk Files" output table to `104-BUNDLE-BASELINE.md` under an `## AFTER (post-removal)` heading, then compute and record the per-chunk and total size delta versus the BEFORE table. The delta is expected to be zero or a tiny shrink; record it as "no regression" — do NOT claim a savings the data does not show. Do NOT pass `--stats-json`. Then grep the production dist for residual library code: confirm `grep -l "jQuery JavaScript Library" src/angular/dist/*.js` returns no match, and `grep -l "ResizeSensor\|ElementQueries" src/angular/dist/*.js` returns no match. Note in the summary that any `window.jQuery` string in `scripts-[hash].js` is Bootstrap's optional no-op detection and is acceptable (RESEARCH Pitfall 2), and that the prior license-metadata `"css-element-queries"` string in `main-[hash].js` should now be gone since the dep left package.json (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>cd src/angular && ng build --configuration production && ! grep -lq "jQuery JavaScript Library" dist/*.js && ! grep -lq "ResizeSensor\|ElementQueries" dist/*.js && echo "DIST-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `ng build --configuration production` exits 0 (no "Cannot resolve 'jquery'" or "Cannot resolve 'css-element-queries'" error — a resolve error would be the hidden-usage FINDING signal).
    - `grep -l "jQuery JavaScript Library" src/angular/dist/*.js` returns no match (jQuery library factory header absent).
    - `grep -l "ResizeSensor\|ElementQueries" src/angular/dist/*.js` returns no match.
    - The combined verify command prints `DIST-CLEAN`.
    - `104-BUNDLE-BASELINE.md` now contains both a BEFORE and an `## AFTER (post-removal)` table plus a recorded total-size delta, and the AFTER total is ≤ the BEFORE total (equal counts as pass per D-02).
  </acceptance_criteria>
  <done>Production build is green, dist is free of jQuery/css-element-queries library code, and the before/after bundle delta (no regression) is recorded in 104-BUNDLE-BASELINE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Karma coverage floors hold</name>
  <files>src/angular/karma.conf.js</files>
  <read_first>
    - src/angular/karma.conf.js (confirm check.global floors stmts/branches/fns/lines = 83/68/79/83)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Validation Architecture; "Wave 0 Gaps: None")
  </read_first>
  <action>
    Run the Karma unit suite with coverage from `src/angular/` (`ng test --browsers ChromeHeadless --watch=false --code-coverage`). No source changed in this phase, so no new tests are expected; the goal is to confirm the suite stays green and the existing `check.global` floors (83/68/79/83) still hold or rise — Karma fails the run if any floor is breached. Record the four measured percentages in the summary. If Karma cannot run locally (no Chrome), note that the authoritative check is the CI `unittests-angular` job on amd64+arm64 and rely on the build-green + dist-clean evidence from Task 1 plus the CI run. Do not edit karma.conf.js (read-only confirmation of the floors); this phase makes no coverage-ratchet change.
  </action>
  <verify>
    <automated>cd src/angular && ng test --browsers ChromeHeadless --watch=false --code-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `ng test --browsers ChromeHeadless --watch=false --code-coverage` exits 0 (Karma does not fail the run on a coverage-floor breach).
    - The measured statements/branches/functions/lines coverage is each ≥ the floor (83/68/79/83) and is recorded in the summary.
    - No new or modified `.ts`/`.spec.ts` source files were introduced by this phase (the removal touched only package.json + package-lock.json).
  </acceptance_criteria>
  <done>Karma suite is green and all four coverage floors (83/68/79/83) hold or rise.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual Bootstrap-interaction smoke test (D-01)</name>
  <files>src/angular/src</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Bootstrap-5-Without-jQuery Smoke Test; smoke-test surface table)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md (D-01: smoke-test is the load-bearing check for jQuery removal)
  </read_first>
  <action>
    AUTOMATION FIRST: confirm the build and dist evidence from Task 1 and the Karma floors from Task 2 are green before pausing. Then start the dev server (`cd src/angular && ng serve`) for the human reviewer. This task does not modify source — it is the D-01 manual verification that Bootstrap-driven UI interactions still function with `window.jQuery` undefined. Present the verification steps below to the user and pause for their signal.
  </action>
  <what-built>
    Both phantom deps (jquery, css-element-queries) are removed, the production build is green, the dist is free of jQuery/css-element-queries library code, and Karma floors hold. This checkpoint is the D-01 load-bearing manual verification that Bootstrap-driven UI interactions still work with `window.jQuery` now undefined.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd src/angular && ng serve` and open the printed local URL (typically http://localhost:4200).
    2. Open the browser DevTools Console and keep it visible throughout.
    3. Nav bar: confirm the top nav renders and navigation links route between pages.
    4. Notification bell: click the bell — confirm the dropdown/notification panel opens and closes.
    5. Dashboard: confirm file rows render and the stats strip shows; select one or more file rows and confirm the bulk action bar appears and its buttons are interactive.
    6. Settings page: confirm form controls and inputs render and accept input.
    7. Confirm modal: trigger a destructive action (e.g. delete/stop/queue) and confirm the confirmation modal opens, displays correctly, and confirm/cancel works (this modal is Renderer2-based from Phase 103).
    8. Console check: confirm there are NO errors referencing `jQuery`, `$ is not a function`, or a missing dependency / unresolved module. (A `TypeError: $ is not a function` or "Cannot resolve" would indicate a hidden jQuery usage — that would be a scoped FINDING, not a version bump.)
  </how-to-verify>
  <verify>
    <human-check>User confirms every smoke-test surface (nav, bell dropdown, dashboard rows + action bar, settings form controls, confirm modal) renders and functions with zero jQuery/missing-dependency console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - Nav renders and links route correctly.
    - Notification bell dropdown opens and closes.
    - Dashboard file rows render; multi-select shows the bulk action bar with interactive buttons.
    - Settings form controls render and accept input.
    - Confirm modal opens, renders, and confirm/cancel both work.
    - The browser console shows zero errors referencing jQuery, `$ is not a function`, or a missing/unresolved dependency.
  </acceptance_criteria>
  <done>User has confirmed all Bootstrap interactions work with no jQuery/missing-dependency console errors.</done>
  <resume-signal>Type "approved" if all Bootstrap interactions work with no jQuery/missing-dep console errors, or describe the specific interaction or console error that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → dist | esbuild composes the production bundle from resolved imports; unused/removed deps must not appear |
| browser runtime → Bootstrap bundle | `window.jQuery` is now undefined; Bootstrap's optional jQuery adapter must remain a no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03 | Information Disclosure | dist residual-string grep false signal | mitigate | Gate on the ABSENCE of `"jQuery JavaScript Library"` and `ResizeSensor`/`ElementQueries`, not on a bare `grep "jquery"` (which matches Bootstrap's optional `window.jQuery` no-op and license metadata) — RESEARCH Pitfalls 2 & 3. |
| T-104-04 | Denial of Service | hidden dynamic import of jquery/css-element-queries | mitigate | `ng build --configuration production` is the catch-all: esbuild emits a "Cannot resolve" error for any residual static/dynamic import. A resolve error is treated as a scoped FINDING per REQUIREMENTS.md (not a version bump), and the manual smoke test (Task 3) catches any runtime `$ is not a function`. |
| T-104-SC | Tampering | npm/pip/cargo installs | accept | No new packages are installed in this phase (removals only); the legitimacy gate is N/A per RESEARCH Package Legitimacy Audit. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages, so no blocking-human legitimacy checkpoint applies. |
</threat_model>

<verification>
- `ng build --configuration production` exits 0 after both removals.
- Dist grep: no `"jQuery JavaScript Library"`, no `ResizeSensor`/`ElementQueries` (the `window.jQuery` no-op in scripts is acceptable).
- `104-BUNDLE-BASELINE.md` has BEFORE + AFTER tables and a recorded delta showing no regression (D-02).
- Karma `ng test --code-coverage` green; floors 83/68/79/83 hold or rise.
- Human-verified Bootstrap smoke test passes with zero jQuery/missing-dep console errors (D-01).
- CI authoritative: `unittests-angular` green on amd64 + arm64; E2E green; Python unaffected and stays green; Python `fail_under` ≥ 88 unchanged. No tag/version work (ROADMAP SC5).
</verification>

<success_criteria>
- DEPS-01a fully verified: jQuery removed, production build green, no jQuery library code in dist, Bootstrap interactions confirmed working, no regression.
- DEPS-01c fully verified: css-element-queries removed, no ResizeSensor/ElementQueries in dist.
- D-01 satisfied: grep + clean build + manual Bootstrap smoke test all pass.
- D-02 satisfied: before/after bundle delta recorded showing no regression.
- Karma floors hold; CI green on both architectures; no tag/version work.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-02-SUMMARY.md` when done.
</output>
