---
phase: 104-light-dependency-removals
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md
autonomous: false
requirements: [DEPS-01a, DEPS-01c]
must_haves:
  truths:
    - "ng build --configuration production completes without error AFTER both removals, matching the pre-removal BEFORE build captured in 104-01 Task 1 (D-01: clean-production-build half of the removal-rigor proof, proven as before/after no-regression)"
    - "The production dist contains no jQuery library code (no 'jQuery JavaScript Library' factory header and no jQuery.fn.jquery) and no css-element-queries library code (no ResizeSensor/ElementQueries) — proven against library-code signatures, NOT against the bare package-name strings, which leak into the bundle as package.json metadata via src/app/common/version.ts's require('../../../package.json')"
    - "Total production bundle size is equal to or smaller than the pre-removal BEFORE baseline (no regression)"
    - "The actual UI surfaces (nav, notification bell, dashboard file rows + selection/action-bar, settings form controls, custom Renderer2 confirm modal) render and function the same as the pre-removal BEFORE state, with no console errors referencing jQuery or a missing dependency. The app uses Bootstrap for CSS only — there are NO Bootstrap JS-plugin invocations — so jQuery-independence is proven by 'no jQuery console errors + these real surfaces function', not by Bootstrap plugins firing (D-01: manual smoke-test half of the removal-rigor proof)"
    - "Karma check.global floors (stmts/branches/fns/lines 83/68/79/83) hold or rise"
  artifacts:
    - path: ".planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md"
      provides: "before/after bundle delta record (D-02 AFTER section appended to the 104-01 BEFORE section)"
      contains: "AFTER"
  key_links:
    - from: "src/angular/package.json"
      to: "src/angular/dist"
      via: "ng build --configuration production produces a dist with neither dep's library code"
      pattern: "jQuery JavaScript Library"
---

<objective>
After both phantom deps are removed (plan 104-01) and the D-01 BEFORE gate (pre-removal build + Bootstrap-surface state) is recorded, prove the removal is safe and complete: a clean AFTER production build matching the BEFORE, a before/after bundle-size delta showing no regression (D-02), a dist grep confirming no residual jQuery or css-element-queries LIBRARY code, a manual smoke test of the actual UI surfaces (D-01 AFTER half), and Karma coverage floors holding.

Purpose: D-01 (LOCKED) requires "unused" to be proven by static grep AND a clean `ng build --configuration production` AND a manual smoke-test of the jQuery-adjacent UI surfaces. The BEFORE half of that gate (build green + interactions verified before drop) is captured in 104-01 Task 1; this plan supplies the AFTER half — re-running the build + smoke test to prove no regression — plus the bundle delta and CI-floor evidence.
Output: An updated `104-BUNDLE-BASELINE.md` with the AFTER table and computed delta, a clean dist library-code grep result, a Karma floors-held result, and a human-verified AFTER smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-01-SUMMARY.md

<notes>
- Depends on 104-01: both deps must already be removed from package.json + package-lock.json, and the D-01 BEFORE gate (pre-removal build table + Bootstrap-surface pre-state) recorded in 104-BUNDLE-BASELINE.md, before this plan runs.
- D-01 (LOCKED) — AFTER half: the BEFORE half (build green + interactions verified before drop) lives in 104-01 Task 1. This plan re-runs the production build and re-smoke-tests the SAME UI surfaces to prove no regression. The manual smoke-test is the load-bearing check for jQuery removal and is mandatory (not optional).
- D-02 (LOCKED): append the AFTER `ng build --configuration production` table beneath the BEFORE table in 104-BUNDLE-BASELINE.md and compute the delta. Expected delta ≈ zero (neither dep was ever bundled) or a tiny shrink (license metadata). Frame as "no regression," NOT a savings claim. The `ng build --configuration production` stdout table is the D-02 record. NOTE: contrary to an earlier RESEARCH claim, this builder DOES support `--stats-json` (verified at `node_modules/@angular/build/src/builders/application/schema.json:536`); `--stats-json` may optionally be added to emit a machine-checkable `dist/.../stats.json`, but the stdout table stays the primary record.
- npm + .npmrc: any npm command (none expected in this plan, but `ng build`/`ng test` resolve the tree) must run FROM `src/angular/` so `.npmrc` (`legacy-peer-deps=true`) is in scope; do NOT modify/remove `.npmrc`.
- Dist grep — CRITICAL metadata-leak gotcha: `src/angular/src/app/common/version.ts` does `require("../../../package.json")`, so the BARE package-name strings `"jquery"` and `"css-element-queries"` already appear in the production bundle as embedded package.json metadata, NOT as bundled library code. A naive `grep "jquery" dist/` or `grep "css-element-queries" dist/` WILL match this metadata and give a FALSE POSITIVE that the lib is still bundled. Gate ONLY on library-code signatures: the ABSENCE of `"jQuery JavaScript Library"` (jQuery's factory banner) and `jQuery.fn.jquery` for jQuery, and the ABSENCE of `ResizeSensor`/`ElementQueries` (css-element-queries' own API symbols) for css-element-queries. Additionally, `window.jQuery` appearing in `scripts-[hash].js` is Bootstrap's OPTIONAL no-op detection — NOT jQuery the library — and is ACCEPTABLE. Do NOT gate on a bare `grep "jquery"`/`grep "css-element-queries"` of dist.
- Bootstrap usage reality (RESEARCH + verified): there are ZERO Bootstrap JS-plugin invocations in source — no `data-bs-toggle`, no `data-bs-target`, no `new bootstrap.Modal/Dropdown/Collapse`, no `bootstrap.*` JS API. The app uses Bootstrap for CSS only. The confirm modal is a custom `Renderer2` DOM construction (`src/angular/src/app/services/utils/confirm-modal.service.ts`, Phase 103), not Bootstrap's Modal class. So the smoke test does NOT prove "Bootstrap dropdown/modal/collapse JS plugins work without jQuery" (those code paths don't exist in the app); it proves "no jQuery console errors + the real UI surfaces function".
- Smoke-test surface (the actual surfaces): nav render + routing, notification bell panel open/close, dashboard file rows + selection/action-bar, settings form controls, custom Renderer2 confirm modal open/confirm/cancel.
- Karma floors: stmts/branches/fns/lines 83/68/79/83 (karma.conf.js check.global). No source changed, so no new unit tests are needed — floors must simply still hold.
- No release/tag/version work in this phase (ROADMAP SC5).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AFTER production build, before/after bundle delta, and dist library-code residual grep</name>
  <files>.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md (the BEFORE table + Bootstrap-surface pre-state written by 104-01 Task 1; this file is also the file being modified)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Bundle/Build Verification Approach; Residual String Verification; Anti-Patterns to Avoid)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-01-SUMMARY.md (confirm both deps already removed)
    - src/angular/src/app/common/version.ts (confirm the require('../../../package.json') metadata leak — why bare package-name grep false-positives)
  </read_first>
  <action>
    Run `ng build --configuration production` FROM `src/angular/` (so `.npmrc` is in scope). Append the resulting "Initial Chunk Files" output table to `104-BUNDLE-BASELINE.md` under an `## AFTER (post-removal)` heading, then compute and record the per-chunk and total size delta versus the BEFORE table from 104-01 Task 1. The delta is expected to be zero or a tiny shrink; record it as "no regression" — do NOT claim a savings the data does not show. Optionally also pass `--stats-json` to emit `dist/.../stats.json` (supported by this builder) as a machine-checkable companion; the stdout table is the primary record.

    Then grep the production dist for residual LIBRARY code (NOT bare package-name strings, which leak as version.ts package.json metadata): confirm `grep -l "jQuery JavaScript Library" src/angular/dist/*.js` returns no match AND `grep -l "jQuery.fn.jquery" src/angular/dist/*.js` returns no match (jQuery library absent), and `grep -l "ResizeSensor\|ElementQueries" src/angular/dist/*.js` returns no match (css-element-queries library absent). Document in the summary: (a) any `window.jQuery` string in `scripts-[hash].js` is Bootstrap's optional no-op detection and is acceptable (RESEARCH Pitfall 2); (b) a bare `grep "jquery"`/`grep "css-element-queries"` of dist is NOT a valid pass/fail signal because `version.ts`'s `require("../../../package.json")` embeds package.json (including those names) into `main-[hash].js` as metadata — only the library-code signatures above are authoritative (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>cd src/angular && ng build --configuration production && ! grep -lq "jQuery JavaScript Library" dist/*.js && ! grep -lq "jQuery.fn.jquery" dist/*.js && ! grep -lq "ResizeSensor\|ElementQueries" dist/*.js && echo "DIST-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `ng build --configuration production` (run from `src/angular/`) exits 0 (no "Cannot resolve 'jquery'" or "Cannot resolve 'css-element-queries'" error — a resolve error would be the hidden-usage FINDING signal).
    - `grep -l "jQuery JavaScript Library" src/angular/dist/*.js` returns no match AND `grep -l "jQuery.fn.jquery" src/angular/dist/*.js` returns no match (jQuery library factory absent; bare `grep "jquery"` is NOT used as the gate because version.ts leaks the name as package.json metadata).
    - `grep -l "ResizeSensor\|ElementQueries" src/angular/dist/*.js` returns no match (css-element-queries library API absent; the bare `"css-element-queries"` metadata string is disregarded).
    - The combined verify command prints `DIST-CLEAN`.
    - `104-BUNDLE-BASELINE.md` now contains both the `## BEFORE (pre-removal)` table (from 104-01) and an `## AFTER (post-removal)` table plus a recorded total-size delta, and the AFTER total is ≤ the BEFORE total (equal counts as pass per D-02).
  </acceptance_criteria>
  <done>AFTER production build is green and matches BEFORE, dist is free of jQuery/css-element-queries LIBRARY code (proven via library-code signatures, not metadata strings), and the before/after bundle delta (no regression) is recorded in 104-BUNDLE-BASELINE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Karma coverage floors hold</name>
  <files>src/angular/karma.conf.js</files>
  <read_first>
    - src/angular/karma.conf.js (confirm check.global floors stmts/branches/fns/lines = 83/68/79/83)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Validation Architecture; "Wave 0 Gaps: None")
  </read_first>
  <action>
    Run the Karma unit suite with coverage FROM `src/angular/` (`ng test --browsers ChromeHeadless --watch=false --code-coverage`). No source changed in this phase, so no new tests are expected; the goal is to confirm the suite stays green and the existing `check.global` floors (83/68/79/83) still hold or rise — Karma fails the run if any floor is breached. Record the four measured percentages in the summary. If Karma cannot run locally (no Chrome), note that the authoritative check is the CI `unittests-angular` job on amd64+arm64 and rely on the build-green + dist-clean evidence from Task 1 plus the CI run. Do not edit karma.conf.js (read-only confirmation of the floors); this phase makes no coverage-ratchet change.
  </action>
  <verify>
    <automated>cd src/angular && ng test --browsers ChromeHeadless --watch=false --code-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `ng test --browsers ChromeHeadless --watch=false --code-coverage` exits 0 (Karma does not fail the run on a coverage-floor breach).
    - The measured statements/branches/functions/lines coverage is each ≥ the floor (83/68/79/83) and is recorded in the summary.
    - No new or modified `.ts`/`.spec.ts` source files were introduced by this phase (the removal touched only package.json + package-lock.json).
  </acceptance_criteria>
  <done>Karma suite is green and all four coverage floors (83/68/79/83) hold or rise.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual AFTER smoke test of the actual UI surfaces (D-01)</name>
  <files>src/angular/src</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Bootstrap-5-Without-jQuery Smoke Test; smoke-test surface table)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md (D-01: smoke-test is the load-bearing check for jQuery removal)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md (the recorded BEFORE Bootstrap-surface state — this AFTER smoke test compares against it)
  </read_first>
  <action>
    AUTOMATION FIRST: confirm the AFTER build and dist evidence from Task 1 and the Karma floors from Task 2 are green before pausing. Then start the dev server (`cd src/angular && ng serve`) for the human reviewer. This task does not modify source — it is the D-01 AFTER manual verification that the actual UI surfaces still function with `window.jQuery` undefined, compared against the BEFORE state recorded in 104-01 Task 1. The app uses Bootstrap for CSS only (no Bootstrap JS-plugin invocations exist in source), so the proof is "no jQuery/missing-dep console errors + these real surfaces function" — NOT "Bootstrap plugins fire". Present the verification steps below to the user and pause for their signal.
  </action>
  <what-built>
    Both phantom deps (jquery, css-element-queries) are removed, the AFTER production build is green and matches the BEFORE, the dist is free of jQuery/css-element-queries library code, and Karma floors hold. This checkpoint is the D-01 AFTER load-bearing manual verification that the real UI surfaces still work with `window.jQuery` now undefined. Note: the app uses Bootstrap for CSS only — there are no Bootstrap JS-plugin (Modal/Dropdown/Collapse) invocations in source, and the confirm modal is a custom Renderer2 DOM construction — so this verifies the actual surfaces function, not that Bootstrap JS plugins fire.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd src/angular && ng serve` and open the printed local URL (typically http://localhost:4200).
    2. Open the browser DevTools Console and keep it visible throughout.
    3. Nav bar: confirm the top nav renders and navigation links route between pages.
    4. Notification bell: click the bell — confirm the notification panel/dropdown opens and closes.
    5. Dashboard: confirm file rows render and the stats strip shows; select one or more file rows and confirm the bulk action bar appears and its buttons are interactive.
    6. Settings page: confirm form controls and inputs render and accept input.
    7. Confirm modal: trigger a destructive action (e.g. delete/stop/queue) and confirm the confirmation modal opens, displays correctly, and confirm/cancel works (this modal is a custom Renderer2 DOM construction from Phase 103 — not Bootstrap's Modal class).
    8. Console check: confirm there are NO errors referencing `jQuery`, `$ is not a function`, or a missing dependency / unresolved module. (A `TypeError: $ is not a function` or "Cannot resolve" would indicate a hidden jQuery usage — that would be a scoped FINDING, not a version bump.)
  </how-to-verify>
  <verify>
    <human-check>User confirms every actual UI surface (nav + routing, notification bell panel, dashboard rows + action bar, settings form controls, custom Renderer2 confirm modal) renders and functions the same as the pre-removal BEFORE state, with zero jQuery/missing-dependency console errors. (Bootstrap is CSS-only here; no JS-plugin behavior is being asserted.)</human-check>
  </verify>
  <acceptance_criteria>
    - Nav renders and links route correctly.
    - Notification bell panel/dropdown opens and closes.
    - Dashboard file rows render; multi-select shows the bulk action bar with interactive buttons.
    - Settings form controls render and accept input.
    - Custom Renderer2 confirm modal opens, renders, and confirm/cancel both work.
    - The browser console shows zero errors referencing jQuery, `$ is not a function`, or a missing/unresolved dependency.
    - (Explicitly NOT asserted: Bootstrap JS-plugin behavior — the app has no Bootstrap JS-plugin invocations; jQuery-independence is proven by no-error + surfaces-function, not by plugins firing.)
  </acceptance_criteria>
  <done>User has confirmed all actual UI surfaces function the same as the BEFORE state with no jQuery/missing-dependency console errors.</done>
  <resume-signal>Type "approved" if all UI surfaces function with no jQuery/missing-dep console errors, or describe the specific interaction or console error that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → dist | esbuild composes the production bundle from resolved imports; unused/removed deps must not appear as library code |
| browser runtime → Bootstrap bundle | `window.jQuery` is now undefined; Bootstrap's optional jQuery adapter must remain a no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03 | Information Disclosure | dist residual-string grep false signal | mitigate | Gate on the ABSENCE of `"jQuery JavaScript Library"` / `jQuery.fn.jquery` and `ResizeSensor`/`ElementQueries` (library-code signatures), NOT on a bare `grep "jquery"`/`grep "css-element-queries"` — those bare names appear in the bundle as package.json metadata embedded by `version.ts`'s `require("../../../package.json")`, plus Bootstrap's optional `window.jQuery` no-op (RESEARCH Pitfalls 2 & 3). |
| T-104-04 | Denial of Service | hidden dynamic import of jquery/css-element-queries | mitigate | `ng build --configuration production` is the catch-all: esbuild emits a "Cannot resolve" error for any residual static/dynamic import. A resolve error is treated as a scoped FINDING per REQUIREMENTS.md (not a version bump), and the manual smoke test (Task 3) catches any runtime `$ is not a function`. |
| T-104-SC | Tampering | npm/pip/cargo installs | accept | No new packages are installed in this phase (removals only); the legitimacy gate is N/A per RESEARCH Package Legitimacy Audit. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages, so no blocking-human legitimacy checkpoint applies. |
</threat_model>

<verification>
- `ng build --configuration production` (from `src/angular/`) exits 0 after both removals and matches the BEFORE build.
- Dist grep: no `"jQuery JavaScript Library"`, no `jQuery.fn.jquery`, no `ResizeSensor`/`ElementQueries` (the `window.jQuery` no-op in scripts and the bare package-name metadata strings are acceptable / disregarded).
- `104-BUNDLE-BASELINE.md` has BEFORE + AFTER tables and a recorded delta showing no regression (D-02).
- Karma `ng test --code-coverage` green; floors 83/68/79/83 hold or rise.
- Human-verified AFTER smoke test passes: the actual UI surfaces function the same as BEFORE with zero jQuery/missing-dep console errors (D-01); Bootstrap is CSS-only, so no JS-plugin behavior is asserted.
- CI authoritative: `unittests-angular` green on amd64 + arm64; E2E green; Python unaffected and stays green; Python `fail_under` ≥ 88 unchanged. No tag/version work (ROADMAP SC5).
</verification>

<success_criteria>
- DEPS-01a fully verified: jQuery removed, production build green, no jQuery library code in dist, actual UI surfaces confirmed functioning, no regression.
- DEPS-01c fully verified: css-element-queries removed, no ResizeSensor/ElementQueries in dist.
- D-01 satisfied: grep + clean build + manual smoke test of the actual UI surfaces all pass (BEFORE in 104-01 Task 1, AFTER here).
- D-02 satisfied: before/after bundle delta recorded showing no regression.
- Karma floors hold; CI green on both architectures; no tag/version work.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-02-SUMMARY.md` when done.
</output>
