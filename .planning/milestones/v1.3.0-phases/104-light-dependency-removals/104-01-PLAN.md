---
phase: 104-light-dependency-removals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/package.json
  - src/angular/package-lock.json
autonomous: true
requirements: [DEPS-01a, DEPS-01c]
must_haves:
  truths:
    - "A grep of src/angular/src/ confirms zero direct import/require of jquery, $, or jQuery (including window.$, window.jQuery, dynamic import('jquery'), and require('jquery')), and zero ResizeSensor/ElementQueries/css-element-queries usage (D-01: static-evidence half of the removal-rigor proof; the clean prod build + Bootstrap smoke-test halves are completed in 104-02, with the BEFORE baseline captured here in Task 1)"
    - "A pre-removal clean production build (ng build --configuration production) and the pre-removal state of the Bootstrap-driven UI surfaces are recorded BEFORE either dep is dropped (D-01: build-green-before-drop gate, satisfied by the BEFORE baseline; 104-02 supplies the AFTER no-regression comparison)"
    - "jquery and css-element-queries are removed from src/angular/package.json dependencies"
    - "package-lock.json is regenerated with both deps and their node_modules entries gone, with any npm-audit delta noted but unrelated advisories left untouched (D-04)"
    - "The two removals land as two atomic commits, one per requirement (D-03)"
  artifacts:
    - path: "src/angular/package.json"
      provides: "dependency manifest with jquery and css-element-queries removed"
      contains: "\"bootstrap\""
    - path: "src/angular/package-lock.json"
      provides: "regenerated lockfile v3 reflecting both removals"
      contains: "\"lockfileVersion\": 3"
    - path: ".planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md"
      provides: "pre-removal ng build production output table + Bootstrap-surface pre-state (D-01 build/interactions-before-drop gate + D-02 before record)"
      min_lines: 8
  key_links:
    - from: "src/angular/package.json"
      to: "src/angular/package-lock.json"
      via: "npm install regenerates lockfile after manifest edit"
      pattern: "lockfileVersion"
---

<objective>
Capture the pre-removal production bundle baseline AND the pre-removal state of the Bootstrap-driven UI surfaces (the D-01 "build green + interactions verified before drop" gate), then delete the two phantom dependencies (`jquery` and `css-element-queries`) from `src/angular/package.json` as two atomic commits per D-03, regenerating `package-lock.json` after each edit.

Purpose: jQuery 4 and css-element-queries are phantom deps (declared in package.json, referenced nowhere in source, not in the `angular.json` scripts/styles arrays, depended-on by root only). Removing them satisfies DEPS-01a and DEPS-01c and shrinks the dependency surface without a refactor.
Output: An edited `package.json` (both deps gone), a regenerated `package-lock.json`, a `104-BUNDLE-BASELINE.md` recording the before-state `ng build` output table plus the pre-removal Bootstrap-surface state, and two atomic commits.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md
@.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md

<notes>
- D-01 (LOCKED) — before/after interpretation (documented here so it is unambiguous): D-01 requires that "before either dep is dropped... the build must be green and the Bootstrap interactions verified working." This phase satisfies D-01 at TWO points. (1) BEFORE-the-drop gate, in this plan's Task 1: capture a clean `ng build --configuration production` AND record the pre-removal state of the Bootstrap-driven UI surfaces (nav, notification bell, dashboard file rows + selection/action-bar, settings form controls, custom Renderer2 confirm modal) — this is the literal "build green + interactions verified before drop" requirement, captured against the unchanged tree before Task 2/Task 3 remove anything. (2) AFTER-the-drop re-verification, in plan 104-02: re-run the production build and re-smoke-test the same surfaces, proving no regression vs. the BEFORE baseline. Net: the BEFORE half (here) honors D-01 literally; the AFTER half (104-02) proves the removal caused no regression.
- D-03 (LOCKED): two atomic commits, one per requirement — DEPS-01a (jquery) and DEPS-01c (css-element-queries). Not one combined commit. Order is Claude's discretion; this plan does jquery first.
- D-04 (LOCKED): regenerate package-lock.json via `npm install` (or `npm uninstall <pkg>`) after editing package.json. Note any npm-audit delta but do NOT expand scope to unrelated advisories (4 pre-existing moderate vulns are out of scope).
- D-02 (LOCKED): capture a before-state production bundle table as evidence. The `ng build --configuration production` stdout "Initial Chunk Files" table is the human-readable record (the D-02 record of choice). NOTE: contrary to an earlier RESEARCH claim, the installed `@angular/build:application` builder DOES expose `statsJson` (verified at `src/angular/node_modules/@angular/build/src/builders/application/schema.json:536`); `--stats-json` is therefore SUPPORTED and may optionally be added to also emit a machine-checkable `dist/.../stats.json` artifact. The stdout table remains the primary D-02 record either way. Expected delta is zero/negligible — frame as "no regression," not a savings claim.
- index.html path: the real file is `src/angular/src/index.html` (there is NO `src/angular/index.html`; `angular.json` `index` points at `src/index.html` relative to the `src/angular` root). All script-tag checks must target `src/angular/src/index.html`.
- npm + .npmrc (CRITICAL): run all `npm install` / `npm uninstall` commands FROM the `src/angular/` directory so `src/angular/.npmrc` is in scope. That `.npmrc` sets `legacy-peer-deps=true`, which is REQUIRED — the project uses typescript `~6.0.3` while `@angular/build` peer-requires typescript <6.0, so without the flag `npm ci`/`npm install --no-legacy-peer-deps` fails on the peer conflict. Do NOT modify, delete, or override `.npmrc`; the Dockerfile relies on it too.
- Pitfall (RESEARCH): `$(` in `.ts` is RxJS observable naming (`stats$`, `toasts$`), NOT jQuery. Grep for the literal strings `jquery`/`jQuery` plus the dynamic/global forms (`window.$`, `window.jQuery`, `import('jquery')`, `require('jquery')`), not `$(`.
- Do NOT touch the Dockerfile `--legacy-peer-deps` flag — it exists for the same pre-existing Angular 21 / typescript peer-dep conflict that `.npmrc` covers, unrelated to these removals (RESEARCH Pitfall 4).
- The `aidesigner-frontend` project skill is UI-design tooling and does NOT apply to this dependency-removal phase.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit zero-usage, capture pre-removal bundle baseline + Bootstrap-surface pre-state (D-01 before gate)</name>
  <read_first>
    - src/angular/package.json (confirm jquery and css-element-queries are in `dependencies`)
    - src/angular/.npmrc (confirm `legacy-peer-deps=true` — must not be changed)
    - src/angular/angular.json (confirm `index` = `src/index.html`, and scripts array loads only bootstrap.bundle.min.js; neither dep in scripts/styles)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (Phantom-Dependency Audit grep patterns; Bundle/Build Verification Approach; Bootstrap-5-Without-jQuery Smoke Test surface table)
  </read_first>
  <action>
    Run the phantom-dependency audit grep over `src/angular/src/` for the literal strings `jquery`, `jQuery`, `css-element-queries`, `ResizeSensor`, and `ElementQueries` (case-insensitive) — expect zero matches. Also grep for the dynamic/global usage forms so they cannot slip past a static scan: `window.$`, `window.jQuery`, `import('jquery')`/`import("jquery")`, and `require('jquery')`/`require("jquery")` — expect zero matches. Do NOT grep for `$(` (RxJS observable naming false positive per RESEARCH Pitfall 1). Then run a transitive-consumer check from `src/angular/` via `npm explain jquery css-element-queries` (or `npm ls jquery css-element-queries`) and confirm both are depended-on by the root project only with no real consumers — NOTE: a hit inside packaged test fixtures such as `node_modules/resolve/test/...` is a known false positive, not a real consumer. Confirm `src/angular/src/index.html` has zero `<script` tags and that `src/angular/angular.json` scripts array contains only `bootstrap.bundle.min.js`. If ANY jQuery or css-element-queries source usage is found, STOP and record it as a scoped FINDING per REQUIREMENTS.md (a hidden hard-usage is a finding, NOT a jQuery version bump) — do not proceed to removal.

    Then capture the D-01 BEFORE gate against the unchanged tree, BEFORE Task 2/Task 3 remove anything: (a) run `ng build --configuration production` FROM `src/angular/` (so `.npmrc` is in scope) and write the full "Initial Chunk Files" output table verbatim to `.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md` under a `## BEFORE (pre-removal)` heading, with a one-line header noting this is the BEFORE record per D-02 and the commit SHA of HEAD; optionally also pass `--stats-json` to emit `dist/.../stats.json` as a machine-checkable companion artifact (supported by this builder). (b) record the pre-removal state of the Bootstrap-driven UI surfaces (nav, notification bell, dashboard file rows + selection/action-bar, settings form controls, custom Renderer2 confirm modal) — a short note in 104-BUNDLE-BASELINE.md that these surfaces build and are expected to function, establishing the D-01 "interactions verified before drop" reference point that 104-02 re-checks. Do NOT run `npm install`/`uninstall` in this task — Task 1 captures the unchanged-tree baseline only.
  </action>
  <verify>
    <automated>cd src/angular && grep -rniE "jquery|css-element-queries|ResizeSensor|ElementQueries|window\.\$|import\(['\"]jquery|require\(['\"]jquery" src/; test $? -eq 1 && echo "ZERO-USAGE-CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "jquery|css-element-queries|ResizeSensor|ElementQueries|window\.\$|import\(['\"]jquery|require\(['\"]jquery" src/angular/src/` exits 1 (zero matches) and prints `ZERO-USAGE-CONFIRMED`.
    - `grep -c "<script" src/angular/src/index.html` returns 0.
    - `npm explain jquery css-element-queries` (run from `src/angular/`) shows both depended-on by the root project only (any `node_modules/resolve/test/...` fixture hit is disregarded as a known false positive).
    - `src/angular/.npmrc` still contains `legacy-peer-deps=true` (unchanged).
    - `ng build --configuration production` (run from `src/angular/`) completes without error and prints an "Initial Chunk Files" table.
    - `.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md` exists, contains the build output table under a `## BEFORE (pre-removal)` heading (at least the `main`, `scripts`, `polyfills`, and `styles` rows with Raw Size and Estimated Transfer Size columns), records the pre-removal Bootstrap-surface state, and is labeled as the BEFORE record per D-01/D-02.
    - No jQuery/css-element-queries source usage was found; if any was found, a FINDING is recorded and the plan halts before removal.
  </acceptance_criteria>
  <done>Audit confirms zero source usage (incl. dynamic/global forms), and the D-01 BEFORE gate — pre-removal production build table + Bootstrap-surface pre-state — is recorded in 104-BUNDLE-BASELINE.md before any removal runs.</done>
</task>

<task type="auto">
  <name>Task 2: Remove jQuery (DEPS-01a) — atomic commit 1</name>
  <read_first>
    - src/angular/package.json (the file being modified; line with `"jquery": "^4.0.0"`)
    - src/angular/.npmrc (confirm `legacy-peer-deps=true` — must not be changed)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md (D-03 commit granularity, D-04 lockfile regen)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (npm/Lockfile Mechanics; "Removal command")
  </read_first>
  <action>
    Remove the exact line `"jquery": "^4.0.0",` from the `dependencies` block of `src/angular/package.json`. Run the removal FROM the `src/angular/` directory (so `src/angular/.npmrc` is in scope): either `npm uninstall jquery` run from `src/angular/`, or a manual edit followed by `npm install` run from `src/angular/`. Do NOT pass `--no-legacy-peer-deps` and do NOT modify or remove `.npmrc` — its `legacy-peer-deps=true` is required for the typescript/@angular/build peer conflict. This regenerates `src/angular/package-lock.json` and prunes `node_modules/jquery`. Do NOT pass or remove `--legacy-peer-deps` from any Dockerfile (out of scope; preserve as-is per RESEARCH Pitfall 4). Run `npm audit` (from `src/angular/`) and note the advisory count in the commit body, but do NOT fix any unrelated advisory (D-04 — the 4 pre-existing moderate vulns stay out of scope). Commit as a single atomic commit for DEPS-01a only (per D-03) with a message referencing DEPS-01a; do NOT bundle the css-element-queries removal into this commit.
  </action>
  <verify>
    <automated>cd src/angular && ! grep -q '"jquery"' package.json && ! grep -q '"jquery"' package-lock.json && grep -q 'legacy-peer-deps=true' .npmrc && echo "JQUERY-REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"jquery"' src/angular/package.json` returns no match (exit 1).
    - `grep '"jquery"' src/angular/package-lock.json` returns no match (the lockfile node and any root dependency entry are gone).
    - `node_modules/jquery` is absent (`test ! -d src/angular/node_modules/jquery`).
    - `src/angular/.npmrc` still contains `legacy-peer-deps=true` (unchanged).
    - `src/angular/package.json` still contains `"bootstrap"`, `"@popperjs/core"`, and `"css-element-queries"` (css-element-queries NOT yet removed — that is commit 2).
    - Exactly one commit was created for this step, its message references DEPS-01a (not DEPS-01c), and the npm-audit advisory count is noted in the commit body with no unrelated advisory fixes included.
  </acceptance_criteria>
  <done>jquery is gone from package.json and package-lock.json; one atomic DEPS-01a commit exists; lockfile regenerated via `.npmrc`-scoped npm; css-element-queries still present.</done>
</task>

<task type="auto">
  <name>Task 3: Remove css-element-queries (DEPS-01c) — atomic commit 2</name>
  <read_first>
    - src/angular/package.json (the file being modified; line with `"css-element-queries": "^1.1.1"`)
    - src/angular/.npmrc (confirm `legacy-peer-deps=true` — must not be changed)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-CONTEXT.md (D-03 commit granularity, D-04 lockfile regen)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-RESEARCH.md (css-element-queries audit section)
  </read_first>
  <action>
    Remove the exact line `"css-element-queries": "^1.1.1",` from the `dependencies` block of `src/angular/package.json`. Run the removal FROM the `src/angular/` directory (so `src/angular/.npmrc` is in scope): either `npm uninstall css-element-queries` run from `src/angular/`, or a manual edit followed by `npm install` run from `src/angular/`. Do NOT modify or remove `.npmrc` — its `legacy-peer-deps=true` must remain. This regenerates `src/angular/package-lock.json` and prunes `node_modules/css-element-queries`. Do NOT touch any Dockerfile flag. Note the `npm audit` advisory count delta (run from `src/angular/`) in the commit body but fix nothing unrelated (D-04). Commit as a single atomic commit for DEPS-01c only (per D-03) with a message referencing DEPS-01c; separate from the DEPS-01a commit.
  </action>
  <verify>
    <automated>cd src/angular && ! grep -q '"css-element-queries"' package.json && ! grep -q '"css-element-queries"' package-lock.json && grep -q 'legacy-peer-deps=true' .npmrc && echo "CSS-EQ-REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"css-element-queries"' src/angular/package.json` returns no match (exit 1).
    - `grep '"css-element-queries"' src/angular/package-lock.json` returns no match.
    - `node_modules/css-element-queries` is absent (`test ! -d src/angular/node_modules/css-element-queries`).
    - `src/angular/.npmrc` still contains `legacy-peer-deps=true` (unchanged).
    - `src/angular/package-lock.json` still has `"lockfileVersion": 3`.
    - `src/angular/package.json` still contains `"bootstrap"`, `"@popperjs/core"`, and `"@phosphor-icons/web"` (only the two target deps were removed; Font Awesome `"font-awesome"` is untouched — that is Phase 105).
    - Exactly one commit was created for this step, separate from Task 2's commit, its message references DEPS-01c, and no unrelated advisory fixes are included.
  </acceptance_criteria>
  <done>css-element-queries is gone from package.json and package-lock.json; a second atomic DEPS-01c commit exists; two atomic commits total per D-03; `.npmrc` and font-awesome untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | `npm install` resolves the regenerated tree from the registry/cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | package-lock.json regeneration | accept | Lockfile v3 is regenerated by npm from the existing resolved tree; only the two removed deps and their (root-only, no transitive) entries change. No new packages are added — net surface shrinks. |
| T-104-05 | Denial of Service | `.npmrc` legacy-peer-deps flag | mitigate | Run all npm commands from `src/angular/` so `.npmrc` (`legacy-peer-deps=true`) is in scope; do not modify/remove it. Without it, npm fails on the typescript `~6.0.3` vs `@angular/build` (<6.0) peer conflict, breaking lockfile regen and the Docker build. |
| T-104-SC | Tampering | npm/css-element-queries/jquery installs | accept | This phase performs ZERO new installs — it only removes two packages. RESEARCH Package Legitimacy Audit marks the legitimacy gate N/A for removals; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages are introduced, so no blocking-human legitimacy checkpoint is required. |
| T-104-02 | Denial of Service | Docker `--legacy-peer-deps` flag | mitigate | Do not remove or alter the pre-existing `--legacy-peer-deps` flag in any Dockerfile (RESEARCH Pitfall 4); removing it would break the Angular 21 install on CI. This plan touches only package.json + package-lock.json. |
</threat_model>

<verification>
- Source audit: `grep -rniE "jquery|css-element-queries|ResizeSensor|ElementQueries|window\.\$|import\(['\"]jquery|require\(['\"]jquery" src/angular/src/` exits 1 (zero matches).
- D-01 BEFORE gate recorded in 104-BUNDLE-BASELINE.md: pre-removal `ng build --configuration production` table + Bootstrap-surface pre-state, captured before any removal.
- Both deps absent from `src/angular/package.json` and `src/angular/package-lock.json`.
- `src/angular/.npmrc` unchanged (`legacy-peer-deps=true`).
- `node_modules/jquery` and `node_modules/css-element-queries` pruned.
- Two atomic commits exist (one referencing DEPS-01a, one referencing DEPS-01c) per D-03.
- Build/bundle/smoke/Karma AFTER verification is deferred to plan 104-02.
</verification>

<success_criteria>
- DEPS-01a satisfied at the manifest level: jquery removed from package.json + lockfile, one atomic commit.
- DEPS-01c satisfied at the manifest level: css-element-queries removed from package.json + lockfile, one atomic commit.
- D-01 BEFORE gate captured (pre-removal build green + Bootstrap-surface state) for the before/after no-regression delta.
- Pre-removal bundle baseline captured for the D-02 before/after delta.
- No source files (`.ts`/`.html`/`.scss`) were edited (clean phantom deletions) unless a hidden usage forced a documented FINDING.
- `.npmrc`, font-awesome, and all retained deps (bootstrap, @popperjs/core, @phosphor-icons/web) untouched.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-01-SUMMARY.md` when done.
</output>
