---
phase: 106-mock-fixture-bundle-hygiene
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/environments/environment.ts
  - src/angular/src/environments/environment.prod.ts
  - src/angular/src/app/services/files/view-file.service.ts
  - src/angular/src/app/tests/fixtures/mock-model-files.ts
  - src/angular/src/app/tests/fixtures/mock-model-files.prod.ts
  - src/angular/src/app/services/files/screenshot-model-files.ts
  - src/angular/angular.json
  - .planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md
autonomous: true
requirements: [DEPS-02]

must_haves:
  truths:
    - "The production bundle contains none of the mock dataset (a unique mock filename literal grepped over dist/ returns zero hits)"
    - "view-file.service.ts has no hardcoded USE_MOCK_MODEL class field; it branches on environment.useMockModel"
    - "environment.ts (dev) sets useMockModel: true; environment.prod.ts sets useMockModel: false; both objects are key-parallel"
    - "mock-model-files.ts lives under src/angular/src/app/tests/fixtures/ (not services/files/) and an empty prod stub mock-model-files.prod.ts sits beside it"
    - "screenshot-model-files.ts is deleted (zero importers)"
    - "angular.json production config has a second fileReplacements entry swapping the relocated fixture for the prod stub; development and test configs have no fileReplacements"
    - "ng build --configuration production succeeds and the AFTER total bundle is <= the Phase 105 AFTER baseline (1.13 MB / 207.70 kB transfer)"
    - "Karma global coverage floors (stmts 83 / branches 68 / functions 79 / lines 83) still hold"
  decisions:
    - "D-01: two-layer guarantee (env flag + second fileReplacements entry to prod stub) — implemented in Task 1 steps 4+5"
    - "D-02: useMockModel added to both env files (dev true / prod false), USE_MOCK_MODEL class field removed, service branches on environment.useMockModel — Task 1 steps 3+4"
    - "D-03: empty-map prod stub mock-model-files.prod.ts with identical MOCK_MODEL_FILES signature + angular.json second fileReplacements entry — Task 1 steps 2+5"
    - "D-04: screenshot-model-files.ts deleted outright (zero importers) — Task 1 step 6"
    - "D-05: mock-model-files.ts relocated to src/app/tests/fixtures/ via git mv — Task 1 step 1"
    - "D-06: bundle delta (part 1) + dist absence grep (part 3) — Task 2 (part 2 dev smoke is plan 106-02)"
    - "D-07: Karma floors hold, CI green amd64+arm64, no tag/version work — Task 2 + success_criteria"
  artifacts:
    - path: "src/angular/src/environments/environment.ts"
      provides: "Dev env flag useMockModel: true"
      contains: "useMockModel: true"
    - path: "src/angular/src/environments/environment.prod.ts"
      provides: "Prod env flag useMockModel: false"
      contains: "useMockModel: false"
    - path: "src/angular/src/app/services/files/view-file.service.ts"
      provides: "View store branching on environment.useMockModel"
      contains: "environment.useMockModel"
    - path: "src/angular/src/app/tests/fixtures/mock-model-files.ts"
      provides: "Relocated dev mock fixture dataset"
      contains: "MOCK_MODEL_FILES"
    - path: "src/angular/src/app/tests/fixtures/mock-model-files.prod.ts"
      provides: "Empty-map prod stub matching MOCK_MODEL_FILES signature"
      contains: "Immutable.Map<string, ModelFile>()"
    - path: "src/angular/angular.json"
      provides: "Production fileReplacements swapping fixture for prod stub"
      contains: "mock-model-files.prod.ts"
    - path: ".planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md"
      provides: "Before/after prod bundle delta record + dist absence-grep result"
      contains: "AFTER"
  key_links:
    - from: "src/angular/src/app/services/files/view-file.service.ts"
      to: "src/angular/src/environments/environment.ts"
      via: "import { environment } and branch on environment.useMockModel"
      pattern: "environment\\.useMockModel"
    - from: "src/angular/src/app/services/files/view-file.service.ts"
      to: "src/angular/src/app/tests/fixtures/mock-model-files.ts"
      via: "import MOCK_MODEL_FILES from relocated path"
      pattern: "tests/fixtures/mock-model-files"
    - from: "src/angular/angular.json"
      to: "src/angular/src/app/tests/fixtures/mock-model-files.prod.ts"
      via: "production fileReplacements entry"
      pattern: "mock-model-files\\.prod\\.ts"
---

<objective>
Make the development-only mock-model fixtures absent from the production Angular bundle (DEPS-02). Convert the hardcoded `USE_MOCK_MODEL` class field into a build-time `useMockModel` environment flag, relocate `mock-model-files.ts` to a dev-only fixtures directory, add an empty prod stub, swap it in production via a second `angular.json` `fileReplacements` entry, and delete the dead `screenshot-model-files.ts`. Then prove the mock data left the prod build via a before/after bundle delta and a dist absence grep, with Karma floors held.

Purpose: The mock dataset (mock-model-files.ts 192 lines + screenshot-model-files.ts 135 lines dead) currently ships in production output (CONCERNS.md Tech Debt). This is the last bundle-hygiene item of v1.3.0 slice 3.
Output: Edited env files + service + relocated fixture + prod stub + angular.json + deleted dead file + a 106-BUNDLE-BASELINE.md record.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# User decisions (load-bearing — D-01 through D-07)
@.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-CONTEXT.md

# Pattern map — exact analog files, line numbers, relative-import depths, code excerpts
@.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-PATTERNS.md

# Sibling-phase bundle-delta rhythm + the AFTER baseline this phase must beat (1.13 MB / 207.70 kB)
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from current source — no codebase exploration required. -->

From src/angular/src/environments/environment.ts (current, dev):
```typescript
import {LoggerService} from "../app/services/utils/logger.service";

export const environment = {
    production: false,
    logger: {
        level: LoggerService.Level.DEBUG
    }
};
```

From src/angular/src/environments/environment.prod.ts (current, prod):
```typescript
import {LoggerService} from "../app/services/utils/logger.service";

export const environment = {
    production: true,
    logger: {
        level: LoggerService.Level.WARN
    }
};
```

From src/angular/src/app/services/files/mock-model-files.ts (current, line 1-5 — the public signature to preserve in the move and to copy into the empty prod stub):
```typescript
import * as Immutable from "immutable";
import {ModelFile} from "./model-file";
export const MOCK_MODEL_FILES: Immutable.Map<string, ModelFile> = Immutable.Map({ /* 192-line dataset */ });
```

From src/angular/src/app/services/files/view-file.service.ts (current flag/branch sites):
- line 11: `import {MOCK_MODEL_FILES} from "./mock-model-files";`
- line 71: `private readonly USE_MOCK_MODEL = false;`
- line 92: `if (!this.USE_MOCK_MODEL) {`
- line 105: `this.buildViewFromModelFiles(MOCK_MODEL_FILES);`

From src/angular/angular.json (current production fileReplacements, project seedsyncarr, architect.build.configurations.production):
```json
"fileReplacements": [
  { "replace": "src/environments/environment.ts", "with": "src/environments/environment.prod.ts" }
]
```
The `development` and `test` configs have NO fileReplacements and must stay that way.

Relative-import depths (no path aliases in this project — confirmed via main.ts / app.config.ts / tests/mocks):
- view-file.service.ts (at src/app/services/files/) -> relocated fixture: `../../tests/fixtures/mock-model-files`
- view-file.service.ts -> environment: `../../../environments/environment`
- tests/fixtures/* -> ModelFile: `../../services/files/model-file`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate fixture, add env flag, repoint service, add prod stub, extend fileReplacements, delete dead file</name>
  <read_first>
    - src/angular/src/app/services/files/view-file.service.ts (the file being edited — read lines 1-115)
    - src/angular/src/environments/environment.ts (object shape to extend)
    - src/angular/src/environments/environment.prod.ts (object shape to keep key-parallel)
    - src/angular/src/app/services/files/mock-model-files.ts (head signature to preserve; line 1-5)
    - src/angular/angular.json (existing fileReplacements array, production config)
    - .planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-PATTERNS.md (exact relative-import depths + analog from tests/mocks/mock-view-file.service.ts:5)
  </read_first>
  <files>
    src/angular/src/app/tests/fixtures/mock-model-files.ts (moved via git mv from src/angular/src/app/services/files/mock-model-files.ts),
    src/angular/src/app/tests/fixtures/mock-model-files.prod.ts (new),
    src/angular/src/environments/environment.ts,
    src/angular/src/environments/environment.prod.ts,
    src/angular/src/app/services/files/view-file.service.ts,
    src/angular/src/app/services/files/screenshot-model-files.ts (deleted via git rm),
    src/angular/angular.json
  </files>
  <action>
    Make the following edits, all per D-01 through D-05 (use git mv / git rm so history follows the files):

    1. (D-05) `git mv src/angular/src/app/services/files/mock-model-files.ts src/angular/src/app/tests/fixtures/mock-model-files.ts` (creates the new `tests/fixtures/` directory). The ONLY content change inside the moved file is its `ModelFile` import: change `import {ModelFile} from "./model-file";` to `import {ModelFile} from "../../services/files/model-file";` (the inverse depth already used by tests/mocks/mock-view-file.service.ts:5). The 192-line dataset body is unchanged.

    2. (D-03) Create `src/angular/src/app/tests/fixtures/mock-model-files.prod.ts` exporting `MOCK_MODEL_FILES` with the IDENTICAL name and type as the real module but an EMPTY map: const type annotation `Immutable.Map<string, ModelFile>` initialized to `Immutable.Map<string, ModelFile>()`. Import `* as Immutable from "immutable"` and `{ModelFile} from "../../services/files/model-file"` (the ModelFile import is required for the generic type parameter — keep it so lint/tsConfig stays clean).

    3. (D-02) In `environment.ts` add key `useMockModel: true` as a sibling field alongside `production: false` and `logger`. In `environment.prod.ts` add the SAME key with value `useMockModel: false` alongside `production: true` and `logger`. Keep both objects key-parallel (identical key set: production, useMockModel, logger); leave the existing LoggerService import and production/level values untouched.

    4. (D-02) In `view-file.service.ts`: DELETE the line `private readonly USE_MOCK_MODEL = false;` (current line 71). Repoint the fixture import (current line 11) from `"./mock-model-files"` to `"../../tests/fixtures/mock-model-files"`. ADD an `environment` import: `import {environment} from "../../../environments/environment";`. Change the condition (current line 92) from `if (!this.USE_MOCK_MODEL) {` to `if (!environment.useMockModel) {`. The bodies of both branches (the rxjs subscribe block and the `this.buildViewFromModelFiles(MOCK_MODEL_FILES)` else-branch at line 105) are UNCHANGED.

    5. (D-01/D-03) In `angular.json`, append a SECOND object to the EXISTING `fileReplacements` array under `projects.seedsyncarr.architect.build.configurations.production` (do not create a new array or build step): `{ "replace": "src/app/tests/fixtures/mock-model-files.ts", "with": "src/app/tests/fixtures/mock-model-files.prod.ts" }`. The `replace` path MUST point at the new relocated location (the move in step 1 happens this same phase). Leave the existing environment.ts->environment.prod.ts entry first. Do NOT add fileReplacements to the `development` or `test` configurations — dev/test must see the real populated fixture.

    6. (D-04) `git rm src/angular/src/app/services/files/screenshot-model-files.ts` — it has zero importers (only its own export line) and is dead code (~135 lines). Do NOT relocate it; git history preserves the dataset.

    Commit granularity per CONTEXT Claude's-Discretion: the relocation + stub + flag + fileReplacements may be one atomic commit; the screenshot deletion may be its own commit or folded in.
  </action>
  <verify>
    <automated>cd src/angular && grep -L "USE_MOCK_MODEL" src/app/services/files/view-file.service.ts >/dev/null 2>&1; test ! -f src/app/services/files/mock-model-files.ts && test ! -f src/app/services/files/screenshot-model-files.ts && test -f src/app/tests/fixtures/mock-model-files.ts && test -f src/app/tests/fixtures/mock-model-files.prod.ts && grep -q "environment.useMockModel" src/app/services/files/view-file.service.ts && grep -q "useMockModel: true" src/environments/environment.ts && grep -q "useMockModel: false" src/environments/environment.prod.ts && grep -q "mock-model-files.prod.ts" angular.json && npx tsc --noEmit -p tsconfig.json && echo MECHANISM-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c USE_MOCK_MODEL src/angular/src/app/services/files/view-file.service.ts` returns 0 (the class field is gone; the condition now reads `environment.useMockModel`).
    - `src/angular/src/app/services/files/mock-model-files.ts` does NOT exist; `src/angular/src/app/services/files/screenshot-model-files.ts` does NOT exist.
    - `src/angular/src/app/tests/fixtures/mock-model-files.ts` exists and its only edit vs. the original is the `ModelFile` import path `../../services/files/model-file`.
    - `src/angular/src/app/tests/fixtures/mock-model-files.prod.ts` exists, exports `MOCK_MODEL_FILES` typed `Immutable.Map<string, ModelFile>` with an empty map, and contains no mock filename literals.
    - `environment.ts` contains `useMockModel: true`; `environment.prod.ts` contains `useMockModel: false`; both objects have the identical key set {production, useMockModel, logger}.
    - `angular.json` production `fileReplacements` has exactly two entries (environment swap + the new fixture->prod-stub swap); `development` and `test` configs still have no `fileReplacements`.
    - `npx tsc --noEmit` (or `ng build` typecheck) is clean — the prod-stub signature makes the dead else-branch type-compatible.
  </acceptance_criteria>
  <done>The mechanism is in place: env-flag-driven runtime branch + physical fileReplacements swap; fixture relocated; prod stub empty; screenshot dead file deleted; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: AFTER prod build, bundle delta, dist absence grep, Karma floors</name>
  <read_first>
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md (the sibling BEFORE/AFTER/delta format to mirror; the Phase 105 AFTER total 1.13 MB / 207.70 kB is the figure this phase must be <=)
    - src/angular/src/app/tests/fixtures/mock-model-files.ts (source of the unique mock string to grep for — the literal `A Really Cool Video About Cats` at the head of the dataset)
    - src/angular/karma.conf.js (the check.global floors: statements 83 / branches 68 / functions 79 / lines 83)
  </read_first>
  <files>
    .planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md (new)
  </files>
  <action>
    (D-06 parts 1 + 3, and D-07.) The hard proof of this phase is the dist absence-grep on distinctive mock FILENAME LITERALS (part 3); the bundle delta (part 1) is corroborating evidence. Capture a TRUE Phase-106 before/after so the delta is not confounded by drift since Phase 105 (codex MAJOR): the executor MUST capture the BEFORE build from the pre-edit tree.

    PRECONDITION (true before/after): If this Task runs after the Task-1 edits are already committed, recover the BEFORE figure by building at the pre-106 parent commit (e.g. `git stash` is not enough — check out the parent of the Task-1 commit in a throwaway worktree OR record that the BEFORE build was captured before Task 1 began). Record the exact BEFORE commit SHA. The Phase 105 AFTER record (Initial total 1.13 MB / 207.70 kB transfer, styles 442.21 kB, main 574.61 kB) is retained ONLY as a secondary sanity anchor, not as the BEFORE measurement.

    1. Run `ng build --configuration production` from `src/angular/` (exit 0 expected). Capture the chunk table (main / styles / scripts / polyfills + Initial total raw + estimated transfer).
    2. Dist absence grep (the core HARD proof per D-06.3): `grep -rl "A Really Cool Video About Cats" dist/` MUST return ZERO matches, AND a second distinctive mock FILENAME literal taken from the relocated fixture MUST also return zero. These two filename-literal greps are the blocking gate. Additionally grep dist/ for the symbol `MOCK_MODEL_FILES` and record the result as INFORMATIONAL only — the prod stub legitimately re-exports that symbol name, so a non-zero symbol hit (sourcemaps / emitted export name) is NOT a failure as long as the filename-literal greps are clean and the bundle delta shows the ~payload-sized drop. Print `MOCK-ABSENT-FROM-PROD-DIST` only when both filename-literal greps return empty.
    3. Write `.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md` mirroring the 105 format: a BEFORE section with the TRUE Phase-106 pre-edit build (chunk table + BEFORE commit SHA), an AFTER section with the post-edit build's chunk table, a delta table, the Phase 105 AFTER figure as a secondary sanity anchor, and the verification gates (AFTER total <= BEFORE total AND <= Phase 105 AFTER 1.13 MB; filename-literal absence-grep result PASS; symbol-grep result informational). Record HEAD/commit SHA for BOTH the BEFORE and AFTER captures.
    4. Run the Karma suite (`ng test` in single-run/headless mode as the project's CI invokes it) and confirm the four `check.global` floors hold (stmts >= 83, branches >= 68, functions >= 79, lines >= 83). Record measured-vs-floor in the baseline file. (No Python files changed — Python fail_under unaffected; note this.)
  </action>
  <verify>
    <automated>cd src/angular && ng build --configuration production && test -z "$(grep -rl 'A Really Cool Video About Cats' dist/ 2>/dev/null)" && echo MOCK-ABSENT-FROM-PROD-DIST</automated>
  </verify>
  <acceptance_criteria>
    - `ng build --configuration production` exits 0 with no errors.
    - HARD GATE: `grep -rl "A Really Cool Video About Cats" dist/` returns ZERO matches AND a second distinctive mock filename literal returns ZERO matches — verify prints `MOCK-ABSENT-FROM-PROD-DIST`. The `grep -rl "MOCK_MODEL_FILES" dist/` symbol check is recorded as INFORMATIONAL only (the prod stub re-exports that symbol name; a non-zero hit is acceptable when the filename-literal greps are clean).
    - A TRUE Phase-106 before/after delta is recorded: AFTER Initial total <= the Phase-106 BEFORE build total (both captured this phase, BEFORE commit SHA recorded) AND <= the Phase 105 AFTER secondary anchor (1.13 MB / 207.70 kB) — recorded as a PASS gate in 106-BUNDLE-BASELINE.md (bundle must not grow; mock data measurably absent).
    - `106-BUNDLE-BASELINE.md` exists with a TRUE Phase-106 BEFORE section (chunk table + BEFORE commit SHA), AFTER chunk table + AFTER commit SHA, delta table, filename-literal absence-grep PASS, informational symbol-grep result, and Karma floor table.
    - Karma single-run passes with all four `check.global` floors held (stmts 83 / branches 68 / functions 79 / lines 83); measured values recorded.
  </acceptance_criteria>
  <done>Production build is green, the unique mock strings are physically absent from dist/, the AFTER bundle is <= the Phase 105 AFTER baseline, Karma floors hold, and the proof is recorded in 106-BUNDLE-BASELINE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces NO new trust boundary. It is a build-config / dead-code-hygiene change: it removes dev-only mock data from the production bundle and adds a build-time env flag (`useMockModel`). There is no user input, no network call, no auth path, no data-flow change, and no schema change. The only runtime code touched (`view-file.service.ts`) swaps a hardcoded `false` constant for a build-time-constant env flag that is `false` in production — behavior in prod is identical to today.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Information Disclosure | dev mock dataset in prod bundle | mitigate | This phase REMOVES the exposure: the mock dataset is physically swapped out via `fileReplacements` and proven absent by the dist absence grep (Task 2). Net attack surface decreases. |
| T-106-02 | Tampering | npm/pip/cargo installs | n/a (accept) | No new package installs in this phase — only a file move, an env-flag add, a build-config edit, and a dead-file deletion. No `[ASSUMED]`/`[SUS]` packages introduced; no Package Legitimacy Gate triggered. |

No high-severity threats exist in this phase (block-on severity = high; nothing meets it). Removing an end-of-life-adjacent dev fixture from shipped output is a net reduction in attack surface.
</threat_model>

<verification>
- `ng build --configuration production` exits 0.
- Production dist contains none of the mock dataset (unique-string greps return zero).
- AFTER bundle total <= Phase 105 AFTER baseline (1.13 MB / 207.70 kB).
- Karma global floors hold (83 / 68 / 79 / 83).
- TypeScript compiles clean (prod stub signature keeps the dead branch type-compatible).
- Dev-mode mock toggle smoke test is verified in Plan 106-02 (checkpoint).
</verification>

<success_criteria>
- USE_MOCK_MODEL class field removed; service branches on environment.useMockModel (ROADMAP #1).
- mock-model-files.ts relocated to tests/fixtures/; screenshot-model-files.ts deleted; angular.json production fileReplacements swaps in the empty prod stub (ROADMAP #2).
- Prod dist contains none of the mock data, proven by distinctive-string grep (ROADMAP #3).
- Prod bundle measurably <= Phase 105 baseline; Karma floors hold; no Python change (ROADMAP #5).
- No release/tag/version work.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-01-SUMMARY.md` when done.
</output>
