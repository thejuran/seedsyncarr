---
phase: 106-mock-fixture-bundle-hygiene
plan: "02"
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - .planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md
  - .planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-02-SUMMARY.md
autonomous: false
requirements: [DEPS-02]

must_haves:
  truths:
    - "With environment.ts useMockModel: true, a dev build/serve renders the mock file rows in the dashboard file view — the dev-mode mock toggle still works through the new env flag (COMPAT)"
    - "The dev path is end-to-end functional: env flag true -> view-file.service.ts else-branch -> buildViewFromModelFiles(MOCK_MODEL_FILES) -> mock rows visible"
  decisions:
    - "D-06: dev-mode smoke test (part 2 of the three-part proof) — with useMockModel: true the relocated fixture still renders mock rows; the prod-absence parts 1+3 live in plan 106-01"
    - "D-07: COMPAT constraint upheld (dev-mode mock toggle keeps working via the new env flag); no tag/version work"
  artifacts:
    - path: ".planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md"
      provides: "Dev-mode smoke test result appended (approval + observed mock rows)"
      contains: "Dev-mode smoke test"
  key_links:
    - from: "src/angular/src/environments/environment.ts"
      to: "src/angular/src/app/services/files/view-file.service.ts"
      via: "useMockModel: true drives the else-branch that loads MOCK_MODEL_FILES"
      pattern: "useMockModel"
---

<objective>
Prove the dev-mode mock toggle still works through the new `useMockModel` env flag (DEPS-02 / ROADMAP success criterion #4, COMPAT constraint). With `environment.ts` set to `useMockModel: true`, a development build renders the mock file rows in the dashboard — confirming the dev path (env flag -> else-branch -> `buildViewFromModelFiles(MOCK_MODEL_FILES)`) is intact after the relocation and env-flag refactor done in Plan 106-01.

Purpose: The relocation + env-flag change must not break the developer's layout/style-testing path. This is the COMPAT half of DEPS-02 (the prod-absence half is proven in 106-01).
Output: A recorded dev-mode smoke-test result appended to 106-BUNDLE-BASELINE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# User decisions — D-04 (dev-mode smoke test) + D-06 part 2
@.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-CONTEXT.md

# Sibling D-04 smoke-test precedent (Phase 105 ran Playwright against ng serve at localhost:4200)
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-04-SUMMARY.md

<interfaces>
<!-- The dev path the smoke test exercises, after Plan 106-01 edits land. -->
- environment.ts: `useMockModel: true` (dev default).
- view-file.service.ts constructor: `if (!environment.useMockModel) { ...subscribe to real model... } else { this.buildViewFromModelFiles(MOCK_MODEL_FILES); }`.
- MOCK_MODEL_FILES is imported from `../../tests/fixtures/mock-model-files` (relocated in 106-01) and contains the populated dataset (first row filename literal: `[AUTHOR] A Really Cool Video About Cats.mkv`).
- Dev server: `ng serve` defaults to the `development` configuration, which has NO fileReplacements, so it sees the REAL populated fixture and `environment.ts` (useMockModel: true).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server with mock flag and capture the rendered mock rows</name>
  <read_first>
    - src/angular/src/app/services/files/view-file.service.ts (confirm the else-branch loads MOCK_MODEL_FILES when environment.useMockModel is truthy)
    - src/angular/src/environments/environment.ts (confirm useMockModel: true is the committed dev default)
    - src/angular/src/app/tests/fixtures/mock-model-files.ts (the mock filenames the smoke test should observe — e.g. `A Really Cool Video About Cats`)
  </read_first>
  <files></files>
  <action>
    (D-06 part 2, automation-first.) BLOCKING PRECONDITION (codex MAJOR — no temporary toggle): the committed `environment.ts` MUST already read `useMockModel: true` from Plan 106-01. If the committed value is `useMockModel: false`, that is a FAILURE of Plan 106-01 — STOP and report it; do NOT set it true here, do NOT use a temporary toggle, and do NOT proceed with the smoke test against an edited working tree. The smoke test must run against the committed state exactly as it will ship to developers.

    With the committed `useMockModel: true` confirmed, start `ng serve` from `src/angular/` in the background (development configuration — no fileReplacements, so the real populated fixture loads). Wait for the dev server to be ready on its port, then drive a real browser-level assertion (codex MAJOR — bundle wiring is NOT sufficient evidence): use a short headless-browser script (Playwright is available under src/e2e) that loads the dashboard route and asserts at least one mock filename literal from the fixture (e.g. `A Really Cool Video About Cats`) is present in the RENDERED file-table DOM (a real DOM node in the file list, not just a string in the served JS bundle). A curl / bundle-inspection check is acceptable ONLY as supplementary compile-evidence, never as the smoke-test proof. Capture the observed rendered mock row(s) (the matched DOM text) for the reviewer. Stop the dev server when done. Do NOT edit or commit any `useMockModel` flip.
  </action>
  <verify>
    <automated>cd src/angular && grep -q "useMockModel: true" src/environments/environment.ts && grep -q "buildViewFromModelFiles(MOCK_MODEL_FILES)" src/app/services/files/view-file.service.ts && echo DEV-MOCK-PATH-WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `environment.ts` COMMITTED value is `useMockModel: true` (dev default, unedited). If it is `false`, this plan FAILS and reports a 106-01 defect — no temporary toggle is permitted.
    - The dev-mode else-branch `this.buildViewFromModelFiles(MOCK_MODEL_FILES)` is present and reachable when the flag is true.
    - The dev server starts cleanly (`ng serve` ready, no compile error) and a browser-level DOM assertion confirms at least one mock file row (a rendered file-table DOM node, e.g. `A Really Cool Video About Cats`) sourced from the relocated fixture; the matched rendered text is captured for the checkpoint. Bundle/curl evidence alone does NOT satisfy this criterion.
  </acceptance_criteria>
  <done>Dev server boots against the committed useMockModel: true state and a browser-level DOM assertion confirms the dashboard renders mock rows from the relocated fixture; rendered evidence captured for the human-verify checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify dev-mode mock toggle renders mock rows</name>
  <files></files>
  <action>Verification-only checkpoint — no implementation. Present the dev-server evidence from Task 1 to the reviewer using the what-built / how-to-verify content below, and pause for approval.</action>
  <what-built>
    Plan 106-01 moved `mock-model-files.ts` to `tests/fixtures/`, replaced the hardcoded `USE_MOCK_MODEL` field with the `environment.useMockModel` build-time flag, added an empty prod stub swapped in via `angular.json` fileReplacements, and deleted the dead `screenshot-model-files.ts`. The prod-absence half is proven automatically (dist grep + bundle delta). This checkpoint confirms the COMPAT half: the dev-mode mock toggle still renders mock rows through the new env flag.
  </what-built>
  <how-to-verify>
    1. From `src/angular/`, run `ng serve` (development configuration — `environment.ts` has `useMockModel: true`).
    2. Open the served app (default http://localhost:4200) and go to the dashboard / file view.
    3. Confirm the mock file rows render — e.g. a row named `[AUTHOR] A Really Cool Video About Cats.mkv` and the other mock entries from the fixture appear in the file table.
    4. Confirm there are no console errors referencing the relocated `tests/fixtures/mock-model-files` import or a missing `environment.useMockModel`.
    Expected: mock rows visible (the dev layout/style-testing path works exactly as before the refactor). If the rows do NOT appear, the env-flag wiring or the relocated import path is broken — report it.
  </how-to-verify>
  <verify>
    <human-check>Mock file rows (e.g. `[AUTHOR] A Really Cool Video About Cats.mkv`) render in the dashboard under `ng serve` with no console errors for the relocated import or env flag.</human-check>
  </verify>
  <done>Reviewer confirms mock rows render in dev mode; result appended to 106-BUNDLE-BASELINE.md.</done>
  <resume-signal>Type "approved" if mock rows render in dev mode, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan is a verification-only checkpoint: it starts a development server, observes that mock rows render, and records the result. It introduces no production code, no network endpoint, no auth path, no data-flow change, and installs no packages. The dev-only mock data it exercises never ships to production (proven absent in Plan 106-01).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03 | Information Disclosure | dev mock dataset visible only under `ng serve` (development config) | accept | The mock dataset is intentionally dev-only and is physically excluded from production output via fileReplacements (verified in 106-01). It is never served in production; rendering it under a local dev server is the intended developer affordance. |

No high-severity threats (block-on severity = high; nothing meets it). This is a hygiene/verification phase with no new attack surface.
</threat_model>

<verification>
- Dev server boots with `useMockModel: true` and renders mock rows from the relocated fixture (human-verified).
- No console errors for the relocated import path or the env flag.
- Result appended to 106-BUNDLE-BASELINE.md.
</verification>

<success_criteria>
- A development build with the env flag enabled renders mock file rows in the dashboard, confirming the dev-mode path still functions end-to-end (ROADMAP #4 / COMPAT).
- No release/tag/version work.
</success_criteria>

<output>
Append the dev-mode smoke-test result to `.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-BUNDLE-BASELINE.md` as a distinct `## Dev-Mode Smoke Test (Plan 106-02)` section that carries its OWN provenance line — its own capture timestamp and HEAD/commit SHA (codex MINOR: do not rely on the earlier prod-build-capture HEAD recorded by 106-01, which identifies a different capture point). Then create `.planning/milestones/v1.3.0-phases/106-mock-fixture-bundle-hygiene/106-02-SUMMARY.md` when done.
</output>
