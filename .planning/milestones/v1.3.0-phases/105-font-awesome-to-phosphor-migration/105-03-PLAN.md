---
phase: 105-font-awesome-to-phosphor-migration
plan: 03
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - src/angular/src/app/pages/settings/settings-page.component.html
  - src/angular/src/app/pages/settings/settings-page.component.scss
  - src/angular/src/app/pages/settings/option.component.html
  - src/angular/src/app/pages/settings/options-list.ts
  - src/angular/src/app/pages/logs/logs-page.component.html
  - src/angular/src/app/pages/logs/logs-page.component.scss
  - src/angular/src/app/pages/main/app.component.html
  - src/angular/src/app/pages/main/app.component.ts
  - src/angular/src/app/pages/main/notification-bell.component.html
  - src/angular/src/app/tests/unittests/pages/main/notification-bell.component.spec.ts
autonomous: true
requirements: [DEPS-01b]
must_haves:
  truths:
    - "Every fa-* icon usage in the settings, logs, and main clusters is migrated to Phosphor across all five D-05 edit layers — static `fa fa-*` classes (PATTERN A), conditional `[class.fa-*]` toggle bindings (PATTERN B), dynamic `<i class=\"fa {{icon}}\">` interpolated bindings (PATTERN C), `[ngClass]` nav bindings (PATTERN D), and the SCSS selectors keyed on `.fa-*` (PATTERN F) — leaving zero fa-* substrings in these source files"
    - "The COORDINATED dynamic-binding edits land together so no broken mid-migration class is ever rendered: the options-list.ts data strings (fa-*→ph-*) move in the SAME plan as the settings-page.component.html:4 and option.component.html:13 `<i class=\"fa {{icon}}\">`→`<i class=\"ph {{icon}}\">` prefix change; and the app.component.ts NAV_ICONS map (incl. the `?? 'fa-circle'`→`'ph-circle'` fallback) moves in the SAME plan as app.component.html:21 `[ngClass]` prefix change (RESEARCH Pitfalls 2 & 6 — prefix + data string must move together or the rendered class is invalid)"
    - "The two corrected non-intuitive mappings are applied: settings-page header fa-server→ph-computer-tower (NOT ph-server) and options-list.ts fa-server→ph-computer-tower; the conditional autoscroll toggle preserves its on/off visual distinction (ph-check-circle active / ph-circle inactive)"
    - "The notification-bell unit spec inline template is updated faithfully (fa-bell→ph-bell, fa-times→ph-x) per D-03; the SCSS color rules keyed on `.fa-*` (settings-page i.fa consolidation + .fa-check + autodelete i.fa danger; logs-page .fa-check-circle/.fa-circle-o) are re-pointed to their .ph-* equivalents so icon colors survive; Karma floors 83/68/79/83 hold or rise"
  artifacts:
    - path: "src/angular/src/app/pages/main/app.component.ts"
      provides: "the migrated NAV_ICONS map (ph-squares-four / ph-gear / ph-terminal / ph-info) + the ph-circle fallback — coordinated with the app.component.html [ngClass] prefix"
      contains: "ph-circle"
    - path: "src/angular/src/app/pages/settings/options-list.ts"
      provides: "the seven migrated icon data strings incl. the corrected ph-computer-tower (NOT ph-server) — coordinated with the settings/option dynamic-binding prefix change"
      contains: "ph-computer-tower"
  key_links:
    - from: "src/angular/src/app/pages/settings/options-list.ts"
      to: "src/angular/src/app/pages/settings/settings-page.component.html"
      via: "the `<i class=\"ph {{icon}}\">` interpolation renders the ph-* data strings as a valid `ph ph-{name}` class — both moved together (Pattern C)"
      pattern: "ph \\{\\{icon\\}\\}"
    - from: "src/angular/src/app/pages/main/app.component.ts"
      to: "src/angular/src/app/pages/main/app.component.html"
      via: "navIcon() returns ph-* strings applied via `<i class=\"ph\" [ngClass]=...>` — both moved together (Pattern D)"
      pattern: "ngClass.*navIcon"
---

<objective>
Migrate every Font Awesome icon in the settings, logs, and main (nav + notification-bell) clusters to Phosphor across all five D-05 edit layers, keeping the coordinated dynamic-binding edits (data strings + their template prefixes) in this single plan so no invalid `fa ph-*` / `ph fa-*` class is ever rendered mid-migration, re-point the SCSS color selectors, and faithfully update the notification-bell spec. Runs in parallel with 105-02 (no shared files).

Purpose: D-05 layers 1–5 all live in this cluster. The load-bearing risk (RESEARCH Pitfalls 2 & 6) is the dynamic-binding layer: `options-list.ts` strings render through `<i class="fa {{icon}}">`, and `app.component.ts` NAV_ICONS render through `<i class="fa" [ngClass]="navIcon(...)">`. The static prefix (`fa`→`ph`) and the data strings (`fa-*`→`ph-*`) MUST change together or production renders a broken class. Owning both the TS data files and their consuming templates in one plan guarantees that. This cluster also carries the two corrected non-intuitive mappings (fa-server→ph-computer-tower, and the fa-ban→ph-prohibit is in the files cluster) and the autoscroll toggle's fill/outline distinction.
Output: 5 migrated templates, 2 migrated TS data files, 2 migrated SCSS files, 1 faithfully-updated spec, Karma green with floors held.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-PATTERNS.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md

<notes>
- Depends on 105-01: the six ambiguous icons this cluster owns must use the FINAL signed-off classes from 105-MAPPING-SIGNOFF.md DECISION lines — Q1 fa-tachometer (settings:141 + options-list.ts:101), Q2 fa-floppy-o (settings:471), Q3 fa-hdd-o (settings:95 [icon]), Q6 fa-th-large (app.component.ts:93 NAV_ICONS dashboard), Q7 fa-sliders (settings:32 + options-list.ts:150), Q8 fa-file-code-o (settings:99 [icon]). Proposed picks: ph-gauge, ph-floppy-disk, ph-hard-drive, ph-squares-four, ph-sliders-horizontal, ph-file-code. All other icons in this cluster are confident 1:1 mappings.
- CRITICAL coordination (RESEARCH Pitfalls 2 & 6 — must move together within this plan):
  - Layer C dynamic binding: options-list.ts icon data strings (lines 19,75,101,150,170,196,222: fa-server→ph-computer-tower, fa-search→ph-magnifying-glass, fa-tachometer→ph-gauge, fa-sliders→ph-sliders-horizontal, fa-list→ph-list, fa-trash→ph-trash, fa-file-archive-o→ph-file-archive) MUST change in lockstep with settings-page.component.html:4 `<i class="fa {{icon}}">`→`<i class="ph {{icon}}">` and option.component.html:13 `<i class="fa {{icon}}">`→`<i class="ph {{icon}}">`. The {{icon}} token is unchanged; only the prefix moves.
  - Layer D nav binding: app.component.ts NAV_ICONS (lines 92-100: dashboard fa-th-large→ph-squares-four, settings fa-cog→ph-gear, logs fa-terminal→ph-terminal, about fa-info-circle→ph-info) AND the fallback `?? "fa-circle"`→`?? "ph-circle"` MUST change in lockstep with app.component.html:21 `<i class="fa" [ngClass]="navIcon(routeInfo.path)">`→`<i class="ph" [ngClass]=...>`. Pitfall 6: forgetting the fallback renders `ph fa-circle` for unknown routes.
- settings-page.component.html exact edits (PATTERNS per-file map + RESEARCH §2a/§2b/§2c):
  - Dynamic binding (Pattern C): L4 `fa {{icon}}`→`ph {{icon}}`.
  - Inline [icon] literals (these feed option.component.html): L91 `[icon]="'fa-folder-open-o'"`→`'ph-folder-open'`; L95 `[icon]="'fa-hdd-o'"`→`'ph-hard-drive'` (Q3); L99 `[icon]="'fa-file-code-o'"`→`'ph-file-code'` (Q8).
  - Static section headers (Pattern A): L32 fa-sliders→ph-sliders-horizontal (Q7); L56 fa-server→ph-computer-tower (CORRECTED, NOT ph-server); L106 fa-search→ph-magnifying-glass; L141 fa-tachometer→ph-gauge (Q1); L175 fa-list→ph-list; L389 fa-trash→ph-trash; L421 fa-shield→ph-shield; L443 fa-refresh→ph-arrows-clockwise; L457 `fa fa-check-circle floating-save-icon--success`→`ph ph-check-circle floating-save-icon--success`; L471 fa-floppy-o→ph-floppy-disk (Q2).
  - Conditional bindings (Pattern B): L308 `fa [class.fa-check]=sonarrWebhookCopied [class.fa-copy]=!sonarrWebhookCopied`→`ph [class.ph-check]=... [class.ph-copy]=...`; L377 same for radarrWebhookCopied; L434 `fa [class.fa-eye]=!tokenRevealed [class.fa-eye-slash]=tokenRevealed`→`ph [class.ph-eye]=... [class.ph-eye-slash]=...`; L438 `fa [class.fa-check]=tokenCopied [class.fa-copy]=!tokenCopied`→`ph [class.ph-check]=... [class.ph-copy]=...`.
- option.component.html: L13 `fa {{icon}}`→`ph {{icon}}` (Pattern C); L68 `fa [class.fa-eye]=!passwordVisible [class.fa-eye-slash]=passwordVisible`→`ph [class.ph-eye]=... [class.ph-eye-slash]=...` (Pattern B).
- app.component.html: L21 `<i class="fa" [ngClass]="navIcon(routeInfo.path)">`→`<i class="ph" [ngClass]=...>` (Pattern D); L37 `fa fa-cog`→`ph ph-gear` (Pattern A, direct settings nav button).
- logs-page.component.html: L21 fa-search→ph-magnifying-glass (keep trailing search-icon); L35 autoscroll toggle `<i class="fa" [class.fa-check-circle]="autoScroll" [class.fa-circle-o]="!autoScroll">`→`<i class="ph" [class.ph-check-circle]="autoScroll" [class.ph-circle]="!autoScroll">` (Pattern B — fa-circle-o maps to ph-circle, NOT ph-circle-o; the on/off check-circle vs plain-circle distinction is preserved); L44 fa-trash→ph-trash; L49 fa-download→ph-download; L99 fa-clock-o→ph-clock.
- SCSS (Pattern F) — re-point color selectors:
  - settings-page.component.scss: F3 — lines 47, 271, 306 `i.fa, i.ph`→`i.ph` (drop the dead i.fa selector); F4 — line 334 `i.fa { color: $danger; }`→`i.ph { color: $danger; }` (autodelete danger color); F5 — line 132 `.webhook-copy-btn .fa-check`→`.ph-check` and line 560 `.fa-check`→`.ph-check` (token-action copy-confirm green). The `.floating-save-icon--success` rule does NOT key on .fa-check-circle so it needs no change — confirm during read.
  - logs-page.component.scss: F2 — line 149 `&--active .fa-check-circle`→`&--active .ph-check-circle`; line 155 `.fa-circle-o`→`.ph-circle`.
- notification-bell.component.html (Pattern A): L4 fa-bell→ph-bell; L22 fa-check-circle→ph-check-circle; L23 fa-exclamation-circle→ph-warning-circle; L24 fa-exclamation-triangle→ph-warning; L25 fa-info-circle→ph-info; L31 fa-times→ph-x.
- notification-bell.component.spec.ts (D-03): inline overrideTemplate L35 `fa fa-bell`→`ph ph-bell`, L55 `fa fa-times`→`ph ph-x`. No fa-* querySelector assertions in this spec (it asserts on .bell-* classes), so only the inline template copy changes.
- D-02 (LOCKED): default regular `ph` weight; do NOT introduce new ph-bold/ph-fill. Leave the existing deliberate ph-bold section-header usages already in settings-page (the Sonarr/Radarr card headers) as-is — those are already Phosphor and out of this swap's scope.
- options-list.ts OPTIONS_CONTEXT_SERVER note (RESEARCH §2b): its `<ng-template>` outlet isn't currently rendered, but the `"fa-server"` data string must still change to ph-computer-tower so no residual fa- string remains in source.
- IOptionsContext.icon stays typed `string` — no interface change.
- Run Karma FROM src/angular/. Floors stmts/branches/fns/lines 83/68/79/83 must hold or rise. Do NOT remove font-awesome from package.json/angular.json here — that is 105-04.
- No release/tag/version work (ROADMAP SC5).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the settings cluster — settings-page (.html + .scss), option.component.html, options-list.ts (coordinated dynamic-binding + conditional + SCSS layers)</name>
  <files>src/angular/src/app/pages/settings/settings-page.component.html, src/angular/src/app/pages/settings/settings-page.component.scss, src/angular/src/app/pages/settings/option.component.html, src/angular/src/app/pages/settings/options-list.ts</files>
  <read_first>
    - src/angular/src/app/pages/settings/settings-page.component.html (the file being modified — static headers, dynamic L4 binding, inline [icon] literals, conditional bindings)
    - src/angular/src/app/pages/settings/settings-page.component.scss (the file being modified — i.fa/i.ph consolidation + .fa-check + autodelete i.fa danger)
    - src/angular/src/app/pages/settings/option.component.html (the file being modified — L13 dynamic binding + L68 conditional)
    - src/angular/src/app/pages/settings/options-list.ts (the file being modified — the 7 icon data strings)
    - src/angular/src/app/pages/files/transfer-table.component.html (PATTERN B analog: the in-repo `[class.ph-*]` conditional-binding idiom, lines 33-35)
    - src/angular/src/app/pages/about/about-page.component.html (PATTERN A analog: static `ph ph-{name}` idiom)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-PATTERNS.md (PATTERN A/B/C/E/F + the settings per-file edit table)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md (signed-off Q1/Q2/Q3/Q7/Q8 classes)
  </read_first>
  <action>
    Migrate all four settings files together so the dynamic-binding prefix and data strings move in lockstep (RESEARCH Pitfall 2). In options-list.ts apply PATTERN E to the seven icon data strings (lines 19,75,101,150,170,196,222): fa-server→ph-computer-tower (CORRECTED — NOT ph-server), fa-search→ph-magnifying-glass, fa-tachometer→ph-gauge, fa-sliders→ph-sliders-horizontal, fa-list→ph-list, fa-trash→ph-trash, fa-file-archive-o→ph-file-archive (use the final signed-off classes from 105-MAPPING-SIGNOFF.md for the ambiguous ones). In settings-page.component.html: change the L4 dynamic binding prefix `fa {{icon}}`→`ph {{icon}}` (Pattern C), the three inline `[icon]` literals (folder-open / hard-drive / file-code per the sign-off), the ten static section-header icons (Pattern A — incl. ph-computer-tower for fa-server and ph-check-circle preserving the floating-save-icon--success utility class), and the four conditional `[class.fa-*]` blocks at L308/L377/L434/L438 (Pattern B — prefix `fa`→`ph` and each binding fa-check/fa-copy/fa-eye/fa-eye-slash→ph-equivalent). In option.component.html: L13 dynamic prefix `fa {{icon}}`→`ph {{icon}}` (Pattern C) and L68 conditional eye/eye-slash (Pattern B). In settings-page.component.scss apply Pattern F3 (collapse `i.fa, i.ph`→`i.ph` at lines 47/271/306), F4 (autodelete `i.fa`→`i.ph` danger color, line 334), and F5 (`.fa-check`→`.ph-check` at lines 132 and 560); confirm `.floating-save-icon--success` does not key on `.fa-check-circle` (if it does, re-point it; per research it targets the utility class directly and needs no change). Keep every non-icon utility class intact; do NOT introduce ph-bold/ph-fill (D-02); leave existing ph-bold card headers untouched.
  </action>
  <verify>
    <automated>cd src/angular && if grep -rIn "fa fa-\|\"fa-\|'fa-\|class=\"fa\b\|class='fa\b\|\[class\.fa-\|i\.fa\b" src/app/pages/settings/settings-page.component.html src/app/pages/settings/settings-page.component.scss src/app/pages/settings/option.component.html src/app/pages/settings/options-list.ts | grep .; then echo "FA-RESIDUAL-FOUND"; exit 1; fi; grep -q "ph-computer-tower" src/app/pages/settings/options-list.ts && grep -q "ph {{icon}}" src/app/pages/settings/settings-page.component.html && grep -q "ph {{icon}}" src/app/pages/settings/option.component.html && echo "SETTINGS-CLUSTER-MIGRATED"</automated>
  </verify>
  <acceptance_criteria>
    - Searching the four settings files for FA-shaped patterns (`fa fa-`, `"fa-`, `'fa-`, `class="fa`, `class='fa`, `[class.fa-`, `i.fa`) returns zero matches.
    - `options-list.ts` contains `ph-computer-tower` (corrected fa-server mapping) and none of the seven `fa-*` data strings remain.
    - `settings-page.component.html` line 4 reads `<i class="ph {{icon}}">` and `option.component.html` line 13 reads `<i class="ph {{icon}}">` (dynamic-binding prefix moved together with the data strings — no `fa {{icon}}` left).
    - The four conditional `[class.fa-*]` blocks in settings-page and the one in option.component.html are now `[class.ph-*]` with a `ph` prefix; the autoscroll/eye/copy toggle bindings preserve their two-state distinction.
    - settings-page.component.scss has `i.ph` (no bare `i.fa`) at the consolidated rules and `.ph-check` (no `.fa-check`); logs/danger colors still apply.
    - The verify command prints `SETTINGS-CLUSTER-MIGRATED` (and not `FA-RESIDUAL-FOUND`).
  </acceptance_criteria>
  <done>The settings cluster is fully migrated with the dynamic-binding data strings and template prefixes moved in lockstep, the corrected ph-computer-tower applied, conditional toggles preserved, and SCSS color selectors re-pointed — zero residual fa-* in the four files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the logs + main clusters (logs-page .html/.scss, app.component .html/.ts coordinated nav binding, notification-bell .html + spec) and confirm Karma floors hold</name>
  <files>src/angular/src/app/pages/logs/logs-page.component.html, src/angular/src/app/pages/logs/logs-page.component.scss, src/angular/src/app/pages/main/app.component.html, src/angular/src/app/pages/main/app.component.ts, src/angular/src/app/pages/main/notification-bell.component.html, src/angular/src/app/tests/unittests/pages/main/notification-bell.component.spec.ts</files>
  <read_first>
    - src/angular/src/app/pages/logs/logs-page.component.html (the file being modified — search, autoscroll toggle, clear/export/clock)
    - src/angular/src/app/pages/logs/logs-page.component.scss (the file being modified — .fa-check-circle / .fa-circle-o color rules, lines 149/155)
    - src/angular/src/app/pages/main/app.component.html (the file being modified — L21 [ngClass] nav, L37 settings cog)
    - src/angular/src/app/pages/main/app.component.ts (the file being modified — NAV_ICONS map + fallback, lines 92-100)
    - src/angular/src/app/pages/main/notification-bell.component.html (the file being modified — bell/notification icons)
    - src/angular/src/app/tests/unittests/pages/main/notification-bell.component.spec.ts (the file being modified — inline overrideTemplate)
    - src/angular/src/app/pages/files/transfer-table.component.html (PATTERN B/D analog: `[class.ph-*]` / `[ngClass]` with `ph` prefix)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-PATTERNS.md (PATTERN A/B/D/E/F2 + logs/main per-file tables; Spec File Pattern)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md (signed-off Q6 fa-th-large class)
    - src/angular/karma.conf.js (confirm check.global floors 83/68/79/83)
  </read_first>
  <action>
    Migrate app.component.ts and app.component.html together (RESEARCH Pitfall 6): in app.component.ts apply PATTERN E to NAV_ICONS (dashboard fa-th-large→ph-squares-four [Q6 sign-off], settings fa-cog→ph-gear, logs fa-terminal→ph-terminal, about fa-info-circle→ph-info) AND change the `?? "fa-circle"` fallback to `?? "ph-circle"`; in app.component.html change L21 `<i class="fa" [ngClass]="navIcon(routeInfo.path)">`→`<i class="ph" [ngClass]=...>` (Pattern D) and L37 `fa fa-cog`→`ph ph-gear` (Pattern A). In logs-page.component.html apply Pattern A to L21 (ph-magnifying-glass, keep search-icon), L44 (ph-trash), L49 (ph-download), L99 (ph-clock), and Pattern B to the L35 autoscroll toggle (`ph` prefix; `[class.ph-check-circle]="autoScroll"` active, `[class.ph-circle]="!autoScroll"` inactive — fa-circle-o maps to ph-circle, preserving the check-circle-vs-plain-circle on/off distinction). In logs-page.component.scss apply Pattern F2: line 149 `&--active .fa-check-circle`→`&--active .ph-check-circle`, line 155 `.fa-circle-o`→`.ph-circle`. In notification-bell.component.html apply Pattern A to all six icons (ph-bell, ph-check-circle, ph-warning-circle, ph-warning, ph-info, ph-x). In notification-bell.component.spec.ts (D-03) update the inline overrideTemplate: `fa fa-bell`→`ph ph-bell`, `fa fa-times`→`ph ph-x` (no querySelector assertions to change). Then run the full Karma suite FROM src/angular/ and confirm check.global floors (83/68/79/83) hold or rise; record the four measured percentages. If Chrome is unavailable locally, note CI unittests-angular (amd64+arm64) is authoritative.
  </action>
  <verify>
    <automated>cd src/angular && if grep -rIn "fa fa-\|\"fa-\|'fa-\|class=\"fa\b\|class='fa\b\|\[class\.fa-\|fa-circle\b\|fa-th-large\|fa-cog\|fa-bell\|fa-times" src/app/pages/logs/logs-page.component.html src/app/pages/logs/logs-page.component.scss src/app/pages/main/app.component.html src/app/pages/main/app.component.ts src/app/pages/main/notification-bell.component.html src/app/tests/unittests/pages/main/notification-bell.component.spec.ts | grep .; then echo "FA-RESIDUAL-FOUND"; exit 1; fi; grep -q "ph-circle" src/app/pages/main/app.component.ts && grep -q 'class="ph" \[ngClass\]\|class=.ph. \[ngClass\]' src/app/pages/main/app.component.html; ng test --browsers ChromeHeadless --watch=false --code-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Searching the six logs/main files for FA-shaped patterns returns zero matches (incl. no `fa-circle` fallback, no `fa-th-large`, `fa-cog`, `fa-bell`, `fa-times`).
    - `app.component.ts` NAV_ICONS holds ph-squares-four / ph-gear / ph-terminal / ph-info and the fallback returns `ph-circle` (Pitfall 6 — fallback migrated); `app.component.html` L21 uses `<i class="ph" [ngClass]="navIcon(...)">` (prefix moved together with the map).
    - logs-page autoscroll toggle renders `[class.ph-check-circle]="autoScroll"` / `[class.ph-circle]="!autoScroll"` on a `ph` host; logs-page.component.scss `&--active .ph-check-circle` and `.ph-circle` color rules are present (no `.fa-*`).
    - notification-bell.component.html uses ph-bell/ph-check-circle/ph-warning-circle/ph-warning/ph-info/ph-x; the spec inline template uses `ph ph-bell` and `ph ph-x` (faithful per D-03, no library-agnostic loosening).
    - `ng test --browsers ChromeHeadless --watch=false --code-coverage` exits 0 with check.global floors each ≥ 83/68/79/83 (four percentages recorded). If Chrome unavailable, summary notes CI as authoritative.
  </acceptance_criteria>
  <done>The logs + main clusters are fully migrated with the NAV_ICONS map + fallback and the [ngClass] prefix moved in lockstep, the autoscroll fill/outline distinction preserved, SCSS color rules re-pointed, the notification-bell spec faithfully updated, and the full Karma suite green with floors held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Icon-class CSS swap across static classes, conditional `[class.*]` bindings, and interpolated `{{icon}}` bindings whose data strings are compile-time constants in TS source. No auth, no user input, no data flow. The `{{icon}}` and `[ngClass]` values originate from hardcoded `options-list.ts` / NAV_ICONS literals — never from user-controlled data — so no injection sink is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03 | (n/a) | settings/logs/main icon bindings | accept | No new threat surface: the dynamic `{{icon}}` / `[ngClass]` bindings resolve hardcoded class-name literals from TS data files, not user input. An icon-library class swap introduces no injection vector; FA CSS/font removal (105-04) reduces shipped attack surface. RESEARCH Security Domain: ASVS V2/V3/V4/V5/V6 do not apply. |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (Phosphor already present; font-awesome removed in 105-04). RESEARCH Package Legitimacy Audit = N/A. No [ASSUMED]/[SUS]/[SLOP] packages, so no blocking-human legitimacy checkpoint applies. |
</threat_model>

<verification>
- The ten settings/logs/main source files contain zero residual fa-* tokens across all five edit layers.
- Coordinated dynamic-binding edits proven moved together: options-list.ts ph-* data strings + `ph {{icon}}` prefix in settings-page/option; NAV_ICONS ph-* map + `?? "ph-circle"` fallback + `ph [ngClass]` prefix in app.component — no invalid `fa ph-*`/`ph fa-*` class possible.
- Corrected ph-computer-tower applied for fa-server; autoscroll/eye/copy toggles preserve their two-state distinction; SCSS color selectors re-pointed to .ph-*.
- notification-bell spec faithfully updated per D-03; Karma green with floors 83/68/79/83 held or risen.
- font-awesome is NOT yet removed from package.json/angular.json (that is 105-04).
- No release/tag/version work (ROADMAP SC5).
</verification>

<success_criteria>
- DEPS-01b (settings/logs/main): every fa-* in these ten files replaced with its mapped Phosphor class across all five D-05 layers; zero residual fa-*; regular ph weight per D-02.
- The coordinated dynamic-binding edits land together — no broken mid-migration class.
- The notification-bell spec updated faithfully per D-03; Karma floors hold.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-03-SUMMARY.md` when done.
</output>
