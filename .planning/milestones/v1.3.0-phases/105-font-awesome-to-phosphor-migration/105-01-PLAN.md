---
phase: 105-font-awesome-to-phosphor-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md
  - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md
autonomous: false
requirements: [DEPS-01b]
must_haves:
  truths:
    - "The complete fa→ph mapping table for all 39 distinct fa-* classes is documented (D-01 success criterion #1: every usage mapped to an explicit Phosphor equivalent BEFORE any code change lands), including the two corrected non-intuitive mappings fa-server→ph-computer-tower (NOT ph-server) and fa-ban→ph-prohibit (NOT ph-ban)"
    - "The 8 ambiguous mappings (fa-tachometer, fa-floppy-o, fa-hdd-o, fa-file-archive-o, fa-circle-o-notch+fa-spin, fa-th-large, fa-sliders, fa-file-code-o) are presented to the user with proposed Phosphor picks + rationale and the user has signed off (or substituted) BEFORE any replacement code touches those specific icons (D-01 LOCKED: pause for user sign-off on the ambiguous ones only)"
    - "A pre-migration clean production build (ng build --configuration production) bundle table is recorded as the BEFORE baseline for the D-07 before/after delta, while font-awesome is still in package.json"
  artifacts:
    - path: ".planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md"
      provides: "the full 39-class fa→ph mapping table + the 8 ambiguous picks with the user's recorded sign-off decision (confirm/substitute per icon)"
      contains: "ph-computer-tower"
    - path: ".planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md"
      provides: "BEFORE (pre-migration) production bundle table for the D-07 delta, captured while font-awesome is still present"
      contains: "BEFORE"
  key_links:
    - from: "105-MAPPING-SIGNOFF.md"
      to: "105-02-PLAN.md / 105-03-PLAN.md ambiguous-icon tasks"
      via: "the signed-off ambiguous picks are the authoritative class names the migration plans apply"
      pattern: "ph-gauge|ph-floppy-disk|ph-hard-drive|ph-file-archive|ph-circle-notch|ph-squares-four|ph-sliders-horizontal|ph-file-code"
---

<objective>
Produce the authoritative, complete fa→ph mapping table for all 39 distinct Font Awesome icon classes in use, surface the 8 ambiguous mappings for explicit user sign-off (D-01 LOCKED — sign-off must land before any replacement code touches those icons), and capture the pre-migration production bundle baseline for the D-07 before/after delta. This is the leaf prerequisite wave: no source `fa-*` is edited here.

Purpose: D-01 requires the complete mapping documented before any code change lands, AND a pause for user sign-off on the 8 ambiguous icons only. The ~31 confident mappings are already verified against the installed Phosphor 2.1.2 CSS (RESEARCH §1a) and need no wait; the 8 ambiguous ones (RESEARCH §1b / §5) gate the migration tasks that touch them. D-07 requires a before/after bundle delta — the BEFORE half must be captured while font-awesome is still in the bundle.
Output: `105-MAPPING-SIGNOFF.md` (full table + recorded sign-off decisions) and `105-BUNDLE-BASELINE.md` (BEFORE bundle table).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md

<notes>
- D-01 (LOCKED): Claude produces the complete fa→ph mapping table for all 39 distinct fa-* classes; the obvious 1:1 set is auto-applied (no wait); the 8 ambiguous icons are proposed with rationale and PAUSE for user sign-off BEFORE any replacement code lands. This plan delivers the table + the sign-off gate.
- The 31 confident mappings are in RESEARCH §1a (all verified against node_modules/@phosphor-icons/web/src/regular/style.css). The 8 ambiguous picks are in RESEARCH §1b / §5. Two corrected NON-intuitive mappings must be recorded prominently: fa-server→ph-computer-tower (ph-server does NOT exist), fa-ban→ph-prohibit (ph-ban does NOT exist).
- The spinner case (Q5): fa-circle-o-notch → ph-circle-notch, and fa-spin has NO Phosphor equivalent — a @keyframes ph-spin + .ph-spin CSS rule must be added in plan 105-02 (this checkpoint also confirms the user approves adding that local spin CSS rule).
- D-07 (LOCKED): before/after production bundle delta. Capture the BEFORE table here, against the unchanged tree, while font-awesome ^4.7.0 is still in package.json and its CSS is still in angular.json. The AFTER table is appended in plan 105-04. Mirror the Phase 104 104-BUNDLE-BASELINE.md format (Initial Chunk Files table + HEAD SHA + green build note).
- Run any ng/npm command FROM src/angular/ so .npmrc (legacy-peer-deps=true) is in scope. Do NOT modify .npmrc. This plan does NOT run npm install/uninstall and does NOT remove font-awesome — it only reads/builds against the unchanged tree.
- This checkpoint is autonomous:false (blocking-human) because the ambiguous picks are a user-judgment design decision per D-01. It is NOT a package-legitimacy gate (no packages are installed in this phase).
- No release/tag/version work (ROADMAP SC5).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the complete 39-class fa→ph mapping table + capture the BEFORE bundle baseline</name>
  <files>.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md, .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md (§1a Confident Mappings — 31 classes; §1b Ambiguous Mappings — 8 icons; §5 Open Questions; §3b Bundle-Size Delta)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md (D-01 mapping fidelity, D-07 bundle proof)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md (the BEFORE-table format to mirror: Initial Chunk Files table + HEAD SHA + green-build note)
    - src/angular/package.json (confirm font-awesome ^4.7.0 is still present — this build is the BEFORE state)
  </read_first>
  <action>
    Write `105-MAPPING-SIGNOFF.md` containing: (a) a CONFIRMED section listing the 31 confident fa→ph mappings from RESEARCH §1a verbatim (fa-arrow-down→ph-arrow-down, fa-bell→ph-bell, fa-check→ph-check, fa-check-circle→ph-check-circle, fa-circle→ph-circle, fa-circle-o→ph-circle, fa-clock-o→ph-clock, fa-cloud→ph-cloud, fa-cog→ph-gear, fa-copy→ph-copy, fa-database→ph-database, fa-download→ph-download, fa-exclamation-circle→ph-warning-circle, fa-exclamation-triangle→ph-warning, fa-expand→ph-arrows-out, fa-eye→ph-eye, fa-eye-slash→ph-eye-slash, fa-folder-open-o→ph-folder-open, fa-info-circle→ph-info, fa-list→ph-list, fa-play→ph-play, fa-plus-circle→ph-plus-circle, fa-refresh→ph-arrows-clockwise, fa-search→ph-magnifying-glass, fa-shield→ph-shield, fa-stop→ph-stop, fa-tasks→ph-list-checks, fa-terminal→ph-terminal, fa-times→ph-x, fa-trash→ph-trash, fa-ban→ph-prohibit, fa-server→ph-computer-tower), with a prominent CALLOUT that fa-server→ph-computer-tower (NOT ph-server) and fa-ban→ph-prohibit (NOT ph-ban) are the two corrected non-intuitive mappings because ph-server and ph-ban do not exist in 2.1.2. (b) an AMBIGUOUS — AWAITING SIGN-OFF section listing the 8 icons (Q1 fa-tachometer→ph-gauge; Q2 fa-floppy-o→ph-floppy-disk; Q3 fa-hdd-o→ph-hard-drive; Q4 fa-file-archive-o→ph-file-archive [vs ph-file-zip]; Q5 fa-circle-o-notch+fa-spin→ph-circle-notch + custom .ph-spin CSS rule; Q6 fa-th-large→ph-squares-four [vs ph-grid-four]; Q7 fa-sliders→ph-sliders-horizontal [vs ph-sliders]; Q8 fa-file-code-o→ph-file-code) with the RESEARCH §5 rationale + usage location for each, and a per-icon DECISION line left blank for the user. (c) note that Q5 also requires approving the addition of a local @keyframes ph-spin + .ph-spin CSS rule (Phosphor ships no spin class).

    Then capture the D-07 BEFORE bundle baseline against the unchanged tree (font-awesome STILL present): run `ng build --configuration production` FROM src/angular/ and write the full "Initial Chunk Files" output table verbatim to `105-BUNDLE-BASELINE.md` under a `## BEFORE (pre-migration, font-awesome still present)` heading, with a one-line header noting this is the D-07 BEFORE record and the HEAD commit SHA. Do NOT edit package.json/angular.json and do NOT run npm install in this task.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md && grep -q "ph-computer-tower" .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md && grep -q "ph-prohibit" .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md && grep -q "BEFORE" .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md && grep -q "font-awesome" src/angular/package.json && echo "MAPPING+BASELINE-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `105-MAPPING-SIGNOFF.md` exists and contains all 31 confident mappings (the verify greps for `ph-computer-tower` and `ph-prohibit`, the two corrected non-intuitive picks, both present).
    - `105-MAPPING-SIGNOFF.md` contains an AMBIGUOUS section listing all 8 icons (Q1–Q8) each with a proposed Phosphor class, rationale, usage location, and a blank per-icon DECISION line for the user.
    - `105-MAPPING-SIGNOFF.md` notes Q5 requires approving a local @keyframes ph-spin + .ph-spin CSS rule.
    - `105-BUNDLE-BASELINE.md` contains the `ng build --configuration production` Initial Chunk Files table under a `## BEFORE (pre-migration, font-awesome still present)` heading, labeled the D-07 BEFORE record with HEAD SHA.
    - `grep "font-awesome" src/angular/package.json` still matches (this plan did NOT remove the dep — the BEFORE build legitimately includes FA).
    - The verify command prints `MAPPING+BASELINE-OK`.
  </acceptance_criteria>
  <done>The complete 39-class mapping table is documented with the two corrected non-intuitive mappings called out, the 8 ambiguous picks are laid out for sign-off, and the D-07 BEFORE bundle baseline (font-awesome still present) is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User sign-off on the 8 ambiguous icon mappings (D-01 LOCKED gate)</name>
  <files>.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md (the AMBIGUOUS — AWAITING SIGN-OFF section written in Task 1)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md (§5 Open Questions / Ambiguous Icons — rationale for each pick)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md (D-01: pause for user sign-off on the ambiguous ones only, before any replacement code lands)
  </read_first>
  <action>
    AUTOMATION FIRST: confirm Task 1 wrote the full mapping table + the 8 ambiguous proposals before pausing. Then present the 8 ambiguous picks to the user (the Q1–Q8 list with proposed class, rationale, and the documented alternates where they exist: Q4 ph-file-archive vs ph-file-zip; Q6 ph-squares-four vs ph-grid-four; Q7 ph-sliders-horizontal vs ph-sliders), plus the Q5 request to approve adding a local .ph-spin CSS animation rule. Pause for the user's per-icon decision. After the user responds, record each confirmed/substituted class on the DECISION line for that icon in `105-MAPPING-SIGNOFF.md`. The recorded decisions are the authoritative class names plans 105-02 and 105-03 apply to the ambiguous icons. This task edits ONLY the documentation file — no source fa-* is touched.
  </action>
  <what-built>
    Task 1 produced the complete 39-class fa→ph mapping table. The 31 confident mappings are auto-applied with no wait (all verified against the installed Phosphor 2.1.2 CSS). The 8 ambiguous mappings (icons with no exact Phosphor twin) require the user's design judgment per D-01 before any code touches them.
  </what-built>
  <how-to-verify>
    Review the 8 proposed ambiguous mappings and reply confirm-or-substitute for each:
    1. Q1 — fa-tachometer (Settings "LFTP Connection Limits" header + options-list.ts) → proposed `ph-gauge`. Confirm or substitute.
    2. Q2 — fa-floppy-o (Settings "Save Settings" button) → proposed `ph-floppy-disk`. Confirm or substitute.
    3. Q3 — fa-hdd-o (Settings "Local Directory" field icon) → proposed `ph-hard-drive`. Confirm or substitute.
    4. Q4 — fa-file-archive-o (bulk-actions "Extract" + "Archive Operations" header + options-list.ts) → proposed `ph-file-archive`; alternate `ph-file-zip`. Pick one.
    5. Q5 — fa-circle-o-notch + fa-spin (dashboard log-pane loading spinner) → proposed `ph-circle-notch` + a NEW local `@keyframes ph-spin` / `.ph-spin` CSS rule (Phosphor ships no spin class). Confirm the icon AND approve adding the local spin CSS rule.
    6. Q6 — fa-th-large (dashboard nav icon) → proposed `ph-squares-four`; alternate `ph-grid-four`. Pick one.
    7. Q7 — fa-sliders (Settings "General Options" header + options-list.ts) → proposed `ph-sliders-horizontal`; alternate `ph-sliders`. Pick one.
    8. Q8 — fa-file-code-o (Settings "Server Script Path" field icon) → proposed `ph-file-code`. Confirm or substitute.
  </how-to-verify>
  <verify>
    <human-check>User has confirmed or substituted a final Phosphor class for each of the 8 ambiguous icons (Q1–Q8) and approved adding the local .ph-spin CSS rule for Q5; each decision is recorded on its DECISION line in 105-MAPPING-SIGNOFF.md.</human-check>
  </verify>
  <acceptance_criteria>
    - Each of the 8 ambiguous icons (Q1–Q8) has a non-blank DECISION line in `105-MAPPING-SIGNOFF.md` recording the user's confirmed or substituted Phosphor class.
    - The Q5 DECISION records both the icon class (`ph-circle-notch` or substitute) AND the user's explicit approval to add the local `.ph-spin` CSS rule.
    - No source `.html`/`.ts`/`.scss` file was modified by this task (only the sign-off doc).
  </acceptance_criteria>
  <done>The user has signed off on (or substituted) all 8 ambiguous icon mappings and approved the local spin CSS rule; the recorded decisions gate the ambiguous-icon edits in plans 105-02 and 105-03.</done>
  <resume-signal>Reply with confirm/substitute for each of Q1–Q8 (and approve the Q5 spin CSS rule), or type "approved" to accept all 8 proposed picks as-is.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan produces documentation + a read-only production build. No code path, no user input, no data flow is introduced or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | (n/a) | icon-library mapping + bundle baseline | accept | No new threat surface: this plan authors a mapping table and runs a read-only `ng build`. No user-controlled data, no auth, no injection sink. Honestly minimal — an icon-class CSS/font swap reduces (does not add) shipped attack surface. |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | No packages are installed in this phase (font-awesome is REMOVED in plan 105-04; Phosphor is already present). RESEARCH Package Legitimacy Audit = N/A. No [ASSUMED]/[SUS]/[SLOP] packages, so no blocking-human legitimacy checkpoint applies. |
</threat_model>

<verification>
- `105-MAPPING-SIGNOFF.md` exists with all 31 confident mappings + the two corrected non-intuitive picks (ph-computer-tower, ph-prohibit) + the 8 ambiguous proposals.
- The user has signed off per-icon on all 8 ambiguous mappings (recorded DECISION lines) and approved the local .ph-spin CSS rule.
- `105-BUNDLE-BASELINE.md` has the BEFORE production bundle table captured while font-awesome is still present (D-07 BEFORE record).
- No source fa-* edited in this plan.
- No release/tag/version work (ROADMAP SC5).
</verification>

<success_criteria>
- D-01 success criterion #1 satisfied: complete fa→ph inventory/mapping documented BEFORE any code change lands; the 8 ambiguous icons signed off by the user before their code lands.
- The two non-intuitive corrected mappings (fa-server→ph-computer-tower, fa-ban→ph-prohibit) are recorded prominently.
- D-07 BEFORE bundle baseline captured for the later before/after delta.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-01-SUMMARY.md` when done.
</output>
