---
phase: 105-font-awesome-to-phosphor-migration
plan: 04
type: execute
wave: 3
depends_on: ["105-02", "105-03"]
files_modified:
  - src/angular/package.json
  - src/angular/angular.json
  - src/angular/package-lock.json
  - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md
autonomous: false
requirements: [DEPS-01b]
must_haves:
  truths:
    - "font-awesome is removed from src/angular/package.json dependencies, both `node_modules/font-awesome/css/font-awesome.css` lines are removed from angular.json (the build `styles` array AND the test/serve `styles` array), and package-lock.json is regenerated via `npm install` — while Phosphor's three weight stylesheets (regular/bold/fill) STAY in angular.json (D-06 closing act, ROADMAP success criterion #3)"
    - "ng build --configuration production completes cleanly AFTER the FA drop, and a broad ripgrep over dist confirms zero Font Awesome CSS/font artifacts (no FontAwesome/fontawesome font files, no font-awesome.css) and zero residual fa- icon tokens (`\\.fa-`, `\\bfa-`, `[class.fa-`, `class=\"fa\"`/`class='fa'`, incl. `fa-spin`) in production-relevant output (ROADMAP success criteria #2 and #3)"
    - "A broad ripgrep over src/angular/src confirms zero remaining fa-* icon tokens across all migrated source files — catching bare SCSS selectors (`.fa-check`/`.fa-terminal`/`.fa-circle-o`), Angular bindings (`[class.fa-`), multi-class leftovers where fa-* is not first (`ph ph-circle-notch fa-spin`), and stray `fa-spin` (the combined result of plans 105-02 + 105-03 — ROADMAP success criterion #2: every former fa-* class replaced)"
    - "An AFTER production bundle table is appended to 105-BUNDLE-BASELINE.md and the delta versus the 105-01 BEFORE table shows the bundle is equal-to-or-smaller than the BEFORE (FA CSS + fonts gone) and ≤ the Phase 104 baseline (D-07, ROADMAP success criterion #5)"
    - "A dev-server manual smoke test confirms every former-fa icon location renders a visible Phosphor glyph — including the toggle/conditional states (password eye, copied check, token reveal, autoscroll) and the still-animating spinner — with no blank squares / no missing-glyph console errors (D-04, ROADMAP success criterion #4); Karma floors 83/68/79/83 hold or rise"
  artifacts:
    - path: "src/angular/angular.json"
      provides: "the styles arrays with font-awesome.css removed from both build and test/serve, Phosphor's three stylesheets retained"
      contains: "@phosphor-icons"
    - path: ".planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md"
      provides: "the AFTER (post-FA-drop) production bundle table + the before/after delta showing no regression vs BEFORE and ≤ Phase 104 baseline"
      contains: "AFTER"
  key_links:
    - from: "src/angular/package.json"
      to: "src/angular/dist"
      via: "ng build --configuration production produces a dist with no font-awesome CSS/font and no residual fa- icon-class strings"
      pattern: "font-awesome"
---

<objective>
The closing act (D-06/D-07): only after all icon usages are migrated (plans 105-02 + 105-03) and the spinner's local .ph-spin rule is in place, remove font-awesome from package.json, remove BOTH font-awesome.css lines from angular.json (build + test/serve styles arrays), regenerate package-lock.json, run a clean AFTER production build, broad-grep dist + source for zero residual fa-* / no FA CSS+font artifacts, append the AFTER bundle table + before/after delta, confirm Karma floors, and run the D-04 dev-server manual smoke test of every former-fa icon location (incl. toggle/conditional states and the spinner).

Purpose: D-06 mandates the FA dep drop happen ONLY after migration + build + smoke test are green, and that both angular.json font-awesome.css entries go (Phosphor's three stylesheets stay). D-07 mandates a before/after bundle delta (BEFORE captured in 105-01) showing FA CSS+fonts left the bundle and total size ≤ Phase 104 baseline. D-04 mandates a dev-server manual smoke test — the load-bearing "no icon renders missing or visually degraded" proof — covering every surface incl. the conditional toggle states and the spinner that depends on the new .ph-spin rule. This plan is last and depends on both migration plans.
Output: FA-free package.json/angular.json/package-lock.json, a clean AFTER build, a dist+source residual grep pass, the AFTER bundle delta in 105-BUNDLE-BASELINE.md, Karma floors held, and a human-verified smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-02-SUMMARY.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-03-SUMMARY.md

<notes>
- Depends on BOTH 105-02 and 105-03: every fa-* source usage (files cluster + settings/logs/main cluster) must already be migrated, the .ph-spin rule must be in dashboard-log-pane.component.scss, and Karma must be green BEFORE this plan drops the dep. If the source residual-grep (Task 1) finds ANY fa- icon token (incl. a bare `.fa-*` SCSS selector or a stray `fa-spin`), STOP — a migration gap exists; do not drop the dep until it is fixed (re-open the relevant migration plan).
- D-06 (LOCKED) exact closing edits (RESEARCH §2f/§2g):
  - package.json: remove the `"font-awesome": "^4.7.0"` line from dependencies.
  - angular.json: remove the TWO `"node_modules/font-awesome/css/font-awesome.css"` entries — one in the build `styles` array (research notes ~line 38) AND one in the test/serve `styles` array (~line 107). Phosphor's three stylesheets (regular/bold/fill, ~lines 39-41 build + 108-110 test/serve) MUST stay. Confirm exact line numbers on read; do not delete a Phosphor line.
  - regenerate package-lock.json: run `npm install` FROM src/angular/ (so .npmrc legacy-peer-deps=true is in scope). Do NOT modify/remove .npmrc. Note (don't fix) any unrelated npm audit delta — scope is the FA removal, not an advisory remediation pass (Phase 104 D-04 rhythm).
  - Commit granularity (Claude's discretion, CONTEXT): the font-awesome drop should be its own clean closing commit per the audit→migrate→drop→verify rhythm.
- D-07 (LOCKED): append the AFTER `ng build --configuration production` Initial Chunk Files table to 105-BUNDLE-BASELINE.md under an `## AFTER (post-FA-drop)` heading and compute the delta vs the BEFORE table from 105-01. Expect the styles bundle to SHRINK (font-awesome.css ~37 kB + its font files leave the build). Confirm the AFTER total is ≤ the BEFORE total AND ≤ the Phase 104 baseline (104-BUNDLE-BASELINE.md AFTER total). The stdout table is the primary record; `--stats-json` may optionally be emitted as a machine-checkable companion (this builder supports it).
- RESIDUAL GREP DESIGN (the BLOCKING gate — both source-pre-drop and dist-post-build use the SAME broad pattern). The earlier narrow pattern (`fa fa-|"fa-|'fa-|class="fa|class='fa|[class.fa-|i.fa`) was too narrow: it MISSED bare SCSS selectors like `.fa-check`/`.fa-terminal`/`.fa-circle-o`/`.fa-check-circle` (which exist today per RESEARCH surface map §2 in settings-page/logs-page/dashboard-log-pane .scss), multi-class leftovers where `fa-*` is NOT the first class (e.g. `class="ph ph-circle-notch fa-spin"`, `class="ph fa-trash"`), and the stray `fa-spin` animation modifier. The broad pattern fixes all three: `\.fa-` (bare SCSS class selectors), `\[class\.fa-` (Angular conditional bindings), `\bfa-` (whitespace/quote/`>`-delimited class token in ANY position, incl. `fa-spin` and non-first-position multi-class tokens), and `class="fa"`/`class='fa'` (the static-prefix idiom). Use `rg` (ripgrep, present in this env) over `.html`/`.ts`/`.scss` for source and over `dist/` for build output. The `\b`/`\.` anchors make `sonarr`/`safari`/`default`/`surface` non-matching (they contain the letters "fa" but never `\bfa-` or `\.fa-`), so no manual exclusion list is needed for those — they cannot match. The ONLY permitted exclusions are the `font-awesome` dependency line in package.json and the two `font-awesome.css` `styles` lines in angular.json being edited in THIS drop task — and those files are NOT under `src/` (so the source gate never sees them) and the bare `font-awesome` name (not an `fa-` icon token) is checked by its own separate `! grep -q "font-awesome"` gate, not the icon-token pattern. The pre-drop source gate has NO exclusions because it runs only against migrated `src/` source while font-awesome is still installed.
- Dist residual grep (RESEARCH §3a) — gate on: (a) NO `font-awesome` CSS reference and NO FontAwesome/fontawesome font files in dist; (b) zero residual `fa-` ICON tokens (broad pattern) in production-relevant dist output. CAUTION (mirror of Phase 104's metadata-leak gotcha): `src/app/common/version.ts` does `require("../../../package.json")`, so once font-awesome is removed from package.json the bare name no longer leaks — but do NOT gate on a naive bare `grep "fa-"` of dist; use the broad anchored pattern (`\bfa-`/`\.fa-`) which excludes the sonarr/safari/default/surface substrings by construction. The authoritative dist gates are: no `font-awesome.css`, no `FontAwesome*`/`fontawesome*` font files, and zero broad-pattern `fa-` icon-token matches.
- D-04 (LOCKED) dev-server manual smoke test — the load-bearing visual proof. AUTOMATION FIRST: Task 1 (drop + build + dist/source grep + bundle delta) and Karma floors must be green before pausing. Surfaces to cover (CONTEXT D-04): dashboard stats strip; transfer rows; bulk-actions bar (incl. the ph-prohibit Delete-Remote overlay + the Q4 signed-off Extract icon); settings page incl. the dynamic `{{icon}}` section-header binding AND the conditional toggle states (password eye reveal, copied-check on webhook/token copy, token reveal eye/eye-slash); logs page (search, autoscroll toggle on/off, clear, export, clock); dashboard log pane (terminal, copy, expand, and the spinner — confirm it STILL ANIMATES, proving the .ph-spin rule works now that font-awesome.css is gone); notification bell (bell + the four notification-type icons + dismiss). No Playwright screenshot harness (deferred).
- Karma floors stmts/branches/fns/lines 83/68/79/83 must hold or rise (already green from 105-02/105-03; re-confirm here after the dep drop since angular.json test/serve styles changed).
- CI authoritative: unittests-angular + E2E green on amd64+arm64; Python unaffected and stays green (fail_under ≥ 88 unchanged). No release/tag/version work (ROADMAP SC5).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source residual-grep gate, drop font-awesome (package.json + both angular.json styles lines + lock regen), AFTER production build, dist residual grep, and before/after bundle delta</name>
  <files>src/angular/package.json, src/angular/angular.json, src/angular/package-lock.json, .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md</files>
  <read_first>
    - src/angular/package.json (the file being modified — the font-awesome ^4.7.0 dependency line)
    - src/angular/angular.json (the file being modified — the TWO font-awesome.css entries in the build + test/serve styles arrays, and the three Phosphor stylesheets that must stay)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-BUNDLE-BASELINE.md (the BEFORE table from 105-01 — this file is also being modified to append AFTER + delta)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md (§2f/§2g closing edits; §2 surface map — confirms the bare `.fa-*` SCSS selectors the broad gate must catch; §3a residual grep + exclusion list; §3b bundle delta; Pitfall 8)
    - .planning/milestones/v1.3.0-phases/104-light-dependency-removals/104-BUNDLE-BASELINE.md (the Phase 104 AFTER total — the bundle must be ≤ this baseline)
    - src/angular/src/app/common/version.ts (confirm the require('../../../package.json') metadata behavior — why a naive bare fa-/package-name dist grep is not the gate)
  </read_first>
  <action>
    FIRST — source residual gate (MUST pass before dropping the dep): run the BROAD ripgrep over `src/angular/src` restricted to `.html`, `.ts`, and `.scss` that catches every leftover form. Exact command (must return ZERO matches):
    `cd src/angular && rg -n --type-add 'scss:*.scss' -t html -t ts -t scss '\.fa-|\[class\.fa-|\bfa-|class="fa"|class='"'"'fa'"'"'' src` — this catches bare SCSS selectors (`.fa-check`/`.fa-terminal`/`.fa-circle-o`/`.fa-check-circle`), Angular conditional bindings (`[class.fa-`), ANY whitespace/quote/`>`-delimited class token via `\bfa-` (incl. multi-class leftovers where `fa-*` is NOT first, e.g. `class="ph ph-circle-notch fa-spin"` or `class="ph fa-trash"`, plus the stray `fa-spin` modifier), and the static-prefix idiom `class="fa"`/`class='fa'`. At this PRE-DROP source stage there are NO permitted exclusions — angular.json/package.json are not under `src/`, font-awesome is still installed, and the gate runs against migrated source only. If ANY line matches, STOP and re-open the relevant migration plan (105-02 or 105-03) — do NOT proceed to the drop with a migration gap.

    Then perform the D-06 closing edits: remove `"font-awesome": "^4.7.0"` from src/angular/package.json dependencies; remove BOTH `"node_modules/font-awesome/css/font-awesome.css"` entries from src/angular/angular.json (one in the build `styles` array, one in the test/serve `styles` array), leaving the three `@phosphor-icons/web` stylesheets in both arrays untouched; run `npm install` FROM src/angular/ to regenerate package-lock.json (do NOT touch .npmrc; note but do not fix any unrelated npm audit delta).

    Then run `ng build --configuration production` FROM src/angular/ and append the resulting Initial Chunk Files table to 105-BUNDLE-BASELINE.md under `## AFTER (post-FA-drop)`, computing the per-chunk + total delta vs the 105-01 BEFORE table; record that the AFTER total is ≤ the BEFORE total AND ≤ the Phase 104 baseline total (expect the styles chunk to shrink as font-awesome.css + fonts leave). Optionally emit `--stats-json` as a companion.

    Then run the BROAD dist residual gate over the built `dist/` output using the SAME pattern: `cd src/angular && rg -n '\.fa-|\[class\.fa-|\bfa-|class="fa"|class='"'"'fa'"'"'' dist` — confirm ZERO matches; also confirm no `font-awesome.css` reference (`! grep -rq "font-awesome" dist`) and no `FontAwesome*`/`fontawesome*` font file (`! find dist -iname 'fontawesome*' | grep .`). The `\b`/`\.` anchors exclude sonarr/safari/default/surface by construction (they contain "fa" but never `\bfa-` or `\.fa-`), so no manual exclusion-list review is needed — a single genuine match is a hard fail. Document the dist-clean result (zero matches) in the summary.
  </action>
  <verify>
    <automated>cd src/angular && if rg -n --type-add 'scss:*.scss' -t html -t ts -t scss '\.fa-|\[class\.fa-|\bfa-|class="fa"|class='"'"'fa'"'"'' src; then echo "SOURCE-FA-RESIDUAL — DO NOT DROP"; exit 1; fi; ! grep -q "font-awesome" package.json && ! grep -q "font-awesome" angular.json && grep -q "@phosphor-icons" angular.json && ng build --configuration production && ! grep -rq "font-awesome" dist && { find dist -iname 'fontawesome*' | grep . && exit 1 || true; } && if rg -n '\.fa-|\[class\.fa-|\bfa-|class="fa"|class='"'"'fa'"'"'' dist; then echo "DIST-FA-RESIDUAL"; exit 1; fi; echo "FA-DROPPED-DIST-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - The BROAD source residual grep over `src/angular/src` returns ZERO matches BEFORE the drop — proving 105-02 + 105-03 left no `fa-` icon token, no bare `.fa-*` SCSS selector, no `[class.fa-` binding, no non-first-position `fa-*` token, and no stray `fa-spin` (ROADMAP success criterion #2). Exact command that must return zero matches: `cd src/angular && rg -n --type-add 'scss:*.scss' -t html -t ts -t scss '\.fa-|\[class\.fa-|\bfa-|class="fa"|class='"'"'fa'"'"'' src`.
    - `grep "font-awesome" src/angular/package.json` returns no match (dependency removed) and `grep "font-awesome" src/angular/angular.json` returns no match (both styles entries removed), while `grep "@phosphor-icons" src/angular/angular.json` still matches (Phosphor stylesheets retained — ROADMAP success criterion #3).
    - `package-lock.json` regenerated via `npm install` (run from src/angular/); .npmrc unchanged; any npm audit delta noted-not-fixed.
    - `ng build --configuration production` exits 0 with no "Cannot resolve 'font-awesome'" error.
    - The dist contains no `font-awesome.css` reference, no `FontAwesome*`/`fontawesome*` font file, and the BROAD dist residual grep returns ZERO matches. Exact command that must return zero matches: `cd src/angular && rg -n '\.fa-|\[class\.fa-|\bfa-|class="fa"|class='"'"'fa'"'"'' dist` (the `\b`/`\.` anchors make sonarr/safari/default/surface non-matching by construction — any single match is a hard fail, not a reviewable false positive).
    - `105-BUNDLE-BASELINE.md` has both the BEFORE table (from 105-01) and an `## AFTER (post-FA-drop)` table + recorded delta; the AFTER total is ≤ the BEFORE total AND ≤ the Phase 104 baseline total.
    - The verify command prints `FA-DROPPED-DIST-CLEAN` (and not `SOURCE-FA-RESIDUAL` or `DIST-FA-RESIDUAL`).
  </acceptance_criteria>
  <done>font-awesome is dropped from package.json + both angular.json styles arrays (Phosphor retained), the lock is regenerated, the AFTER build is clean, dist and source both pass the broad residual gate (zero `fa-`/`.fa-`/`[class.fa-`/`fa-spin` matches), and the before/after bundle delta (no regression, ≤ Phase 104 baseline) is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm Karma coverage floors hold after the dep drop</name>
  <files>src/angular/karma.conf.js</files>
  <read_first>
    - src/angular/karma.conf.js (confirm check.global floors stmts/branches/fns/lines = 83/68/79/83)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-VALIDATION.md (Test Framework + coverage floors)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md (§3c Karma Floor Reminder; Wave 0 Gaps: None)
  </read_first>
  <action>
    Run the Karma unit suite with coverage FROM src/angular/ (`ng test --browsers ChromeHeadless --watch=false --code-coverage`) and confirm it stays green with check.global floors (83/68/79/83) held or risen after the angular.json test/serve styles change (font-awesome.css removed). The 4 specs were already faithfully updated and passing in 105-02/105-03; this re-confirms nothing regressed once FA's CSS is no longer loaded into the test harness. Record the four measured percentages in the summary. Do NOT edit karma.conf.js (read-only floor confirmation — no coverage-ratchet change this phase). If Chrome is unavailable locally, note CI unittests-angular (amd64+arm64) is the authoritative check and rely on the build-green + dist-clean evidence from Task 1 plus CI.
  </action>
  <verify>
    <automated>cd src/angular && ng test --browsers ChromeHeadless --watch=false --code-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `ng test --browsers ChromeHeadless --watch=false --code-coverage` exits 0 (Karma fails the run on a floor breach, so exit 0 means floors held).
    - The measured statements/branches/functions/lines coverage is each ≥ 83/68/79/83 and is recorded in the summary.
    - karma.conf.js is unchanged (no ratchet change this phase).
    - If Chrome is unavailable locally, the summary notes CI unittests-angular as the authoritative check.
  </acceptance_criteria>
  <done>Karma suite is green and all four coverage floors (83/68/79/83) hold or rise after the FA dep drop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dev-server manual smoke test of every former-fa icon location (D-04)</name>
  <files>src/angular/src</files>
  <read_first>
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md (D-04: dev-server manual smoke test; the surfaces-to-cover list incl. toggle/conditional states)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md (the signed-off icon picks — the reviewer confirms each rendered glyph matches the approved intent)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md (§1d spinner special case — confirm it still animates via the local .ph-spin rule)
  </read_first>
  <action>
    AUTOMATION FIRST: confirm Task 1 (FA dropped, AFTER build green, dist clean, bundle delta ≤ BEFORE and ≤ Phase 104 baseline) and Task 2 (Karma floors) are green before pausing. Then start the dev server (`cd src/angular && ng serve`) for the human reviewer. This task does not modify source — it is the D-04 load-bearing visual proof that every former-fa icon location renders a visible Phosphor glyph with no blank squares / no missing-glyph console errors, AND that the spinner still animates now that font-awesome.css is gone (proving the local .ph-spin rule works). Present the verification steps below and pause for the user's signal.
  </action>
  <what-built>
    Every fa-* icon across the app (files cluster + settings/logs/main cluster) was migrated to Phosphor (plans 105-02 + 105-03), the local .ph-spin animation rule was added for the spinner, and font-awesome was dropped from package.json + angular.json with the bundle re-built clean (this plan, Task 1). This checkpoint is the D-04 load-bearing manual verification that no icon renders missing or visually degraded — including the conditional toggle states and the still-animating spinner — with font-awesome.css no longer loaded.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd src/angular && ng serve` and open the printed local URL (typically http://localhost:4200). Open DevTools Console and keep it visible throughout.
    2. Nav bar: confirm the dashboard / settings / logs / about nav icons each render a visible Phosphor glyph (dashboard = the signed-off grid icon, settings = gear, logs = terminal, about = info).
    3. Dashboard stats strip: confirm the cloud, database, arrow-down, and tasks (list-checks) stat icons render.
    4. Transfer rows: confirm the download (arrow-down) status icon renders in a file row.
    5. Bulk-actions bar: select file rows to reveal the bar; confirm Queue (play), Stop (stop), Extract (the Q4 signed-off archive glyph), Delete Local (trash), and Delete Remote (cloud + the prohibit overlay) icons render — the prohibit overlay must be a visible slash/no-entry glyph, not a blank box.
    6. Dashboard log pane: confirm the terminal, copy, and expand icons render; trigger/observe the loading state and confirm the SPINNER STILL ROTATES (this proves the local .ph-spin rule animates now that font-awesome.css is removed — a static/frozen spinner is a FAIL).
    7. Settings page — section headers: confirm each dynamic `{{icon}}` section-header glyph renders (General Options = sliders, Remote Server = computer-tower, File Discovery = magnifying-glass, LFTP Limits = gauge, AutoQueue = list, Pruning = trash, API & Security = shield) plus the Save Settings (floppy-disk) and Regenerate Token (arrows-clockwise) buttons.
    8. Settings page — conditional toggle states: in a password field click the eye toggle and confirm it swaps between eye and eye-slash; copy a webhook/token value and confirm the copy icon swaps to a check; reveal the API token and confirm the reveal eye/eye-slash toggle renders; the inline option-field icons (Local Directory = hard-drive, Server Script Path = file-code) render.
    9. Logs page: confirm search (magnifying-glass), clear (trash), export (download), and the status-bar clock icons render; toggle autoscroll on/off and confirm the icon visibly changes between a check-circle (on) and a plain circle (off).
    10. Notification bell: click the bell (confirm bell glyph), and confirm the success (check-circle), danger (warning-circle), warning (warning-triangle), and info notification-type icons render, plus the dismiss (x) icon.
    11. Console check: confirm there are NO errors and NO missing-glyph warnings; confirm there is no blank square / tofu box anywhere an icon should be.
  </how-to-verify>
  <verify>
    <human-check>User confirms every former-fa icon location renders a visible Phosphor glyph (incl. nav, stats strip, transfer rows, bulk-actions incl. the prohibit overlay, log-pane icons + the still-rotating spinner, settings section headers + the eye/check/reveal conditional toggles + inline option icons, logs incl. the autoscroll on/off swap, and the notification bell + its four type icons), with no blank squares and no missing-glyph console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - Every surface in the how-to-verify list renders a visible Phosphor glyph (no blank squares / tofu boxes).
    - The dashboard log-pane spinner still rotates (the local .ph-spin rule animates with font-awesome.css removed).
    - The conditional toggle states swap correctly: password eye↔eye-slash, copy→check, token reveal eye↔eye-slash, autoscroll check-circle↔plain-circle.
    - The bulk-actions Delete-Remote prohibit overlay renders as a visible slash/no-entry glyph (ph-prohibit, not a blank box).
    - The browser console shows zero errors and zero missing-glyph/blank-square warnings.
  </acceptance_criteria>
  <done>User has confirmed every former-fa icon location renders a visible Phosphor glyph with no missing/degraded icons and the spinner still animates.</done>
  <resume-signal>Type "approved" if every icon location renders a visible Phosphor glyph (incl. the toggle states and the rotating spinner) with no blank squares or console errors, or describe the specific icon/surface that rendered missing or degraded.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json/angular.json → dist | esbuild composes the production bundle from the resolved styles array; the removed font-awesome.css must not appear as a bundled stylesheet or font file. |
| browser runtime → icon CSS | Only Phosphor's three weight stylesheets remain; every icon class must resolve to a Phosphor glyph (no orphaned fa-* class with no backing font). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | (n/a) | font-awesome dep + stylesheet removal | accept | No new threat surface: removing an icon-library dependency + its CSS/font REDUCES shipped attack surface (one fewer EOL 4.7 library). No auth, no user input, no data flow, no injection sink touched. The remaining Phosphor stylesheets are static CSS fonts. |
| T-105-04b | Denial of Service | hidden font-awesome import after drop | mitigate | `ng build --configuration production` is the catch-all: esbuild emits "Cannot resolve 'font-awesome'" for any residual import; the BROAD source residual-grep gate (Task 1) blocks the drop if any `fa-`/`.fa-`/`[class.fa-`/`fa-spin` token remains; the D-04 smoke test catches any runtime missing-glyph. |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | No packages are ADDED in this phase — font-awesome is REMOVED and the lock regenerated; Phosphor is already present. RESEARCH Package Legitimacy Audit = N/A. No [ASSUMED]/[SUS]/[SLOP] packages, so no blocking-human legitimacy checkpoint applies. |
</threat_model>

<verification>
- Broad source residual grep over src/angular/src returns zero `fa-`/`.fa-`/`[class.fa-`/`fa-spin` tokens BEFORE the drop (proves 105-02 + 105-03 complete, incl. SCSS selectors and non-first-position multi-class leftovers — ROADMAP success criterion #2).
- font-awesome removed from package.json and BOTH angular.json styles arrays; Phosphor's three stylesheets retained; package-lock.json regenerated (D-06, ROADMAP success criterion #3).
- AFTER `ng build --configuration production` exits 0; dist has no font-awesome.css, no FontAwesome/fontawesome font files, and zero broad-pattern fa- icon-token matches (ROADMAP success criteria #2/#3).
- 105-BUNDLE-BASELINE.md has BEFORE + AFTER tables and a recorded delta; AFTER total ≤ BEFORE total AND ≤ Phase 104 baseline (D-07, ROADMAP success criterion #5).
- Karma `ng test --code-coverage` green; floors 83/68/79/83 hold or rise.
- Human-verified D-04 dev-server smoke test passes: every former-fa icon location renders a visible Phosphor glyph incl. the conditional toggles and the still-animating spinner, no blank squares, no console errors (ROADMAP success criterion #4).
- CI authoritative: unittests-angular + E2E green on amd64+arm64; Python unaffected and stays green (fail_under ≥ 88 unchanged). No tag/version work (ROADMAP SC5).
</verification>

<success_criteria>
- DEPS-01b fully delivered: font-awesome removed from package.json + angular.json (Phosphor retained), no icon renders missing or degraded, only one icon library ships.
- D-06 satisfied: dep drop is the clean closing act after migration + build + smoke test, both angular.json FA lines removed, lock regenerated.
- D-07 satisfied: before/after bundle delta recorded; bundle ≤ BEFORE and ≤ Phase 104 baseline.
- D-04 satisfied: dev-server manual smoke test of every former-fa surface (incl. toggle states + spinner) passes.
- Karma floors hold; CI green on both architectures; no tag/version work.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
