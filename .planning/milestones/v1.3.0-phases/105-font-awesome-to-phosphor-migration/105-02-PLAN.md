---
phase: 105-font-awesome-to-phosphor-migration
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - src/angular/src/app/pages/files/dashboard-log-pane.component.html
  - src/angular/src/app/pages/files/dashboard-log-pane.component.scss
  - src/angular/src/app/pages/files/stats-strip.component.html
  - src/angular/src/app/pages/files/transfer-row.component.html
  - src/angular/src/app/pages/files/transfer-table.component.html
  - src/angular/src/app/pages/files/bulk-actions-bar.component.html
  - src/angular/src/app/tests/unittests/pages/files/dashboard-log-pane.component.spec.ts
  - src/angular/src/app/tests/unittests/pages/files/stats-strip.component.spec.ts
  - src/angular/src/app/tests/unittests/pages/files/transfer-table.component.spec.ts
autonomous: true
requirements: [DEPS-01b]
must_haves:
  truths:
    - "Every fa-* icon class in the dashboard files cluster (dashboard-log-pane, stats-strip, transfer-row, transfer-table, bulk-actions-bar) is replaced with its mapped Phosphor class using the canonical two-class idiom `ph ph-{name}` (D-05 layer 1, D-02 regular weight); zero fa-* substrings remain in these five templates"
    - "The loading spinner keeps animating after font-awesome's CSS is removed: a local @keyframes ph-spin + .ph-spin rule is added to dashboard-log-pane.component.scss and the spinner uses `ph ph-circle-notch ph-spin` (RESEARCH §1d / §4 Pitfall 1 — Phosphor 2.1.2 ships NO spin class; this prerequisite must land before the FA dep drop in 105-04)"
    - "The dashboard-log-pane .scss .fa-terminal selector is re-pointed to .ph-terminal so the amber terminal-icon color survives the migration (D-05 layer 5)"
    - "The 3 files-cluster unit specs (dashboard-log-pane, stats-strip, transfer-table) are updated faithfully to the exact Phosphor classes the components now render — both the overrideTemplate inline copies AND the querySelector DOM assertions (D-03: no loosening to library-agnostic assertions); the Karma suite passes with check.global floors 83/68/79/83 held or risen"
  artifacts:
    - path: "src/angular/src/app/pages/files/dashboard-log-pane.component.scss"
      provides: "the new @keyframes ph-spin + .ph-spin animation rule (compensating for the removed fa-spin) + the .fa-terminal→.ph-terminal selector rename"
      contains: "ph-spin"
    - path: "src/angular/src/app/pages/files/bulk-actions-bar.component.html"
      provides: "the migrated bulk-actions icons incl. the corrected ph-prohibit (NOT ph-ban) overlay and the signed-off ph-file-archive Extract icon"
      contains: "ph-prohibit"
  key_links:
    - from: "src/angular/src/app/pages/files/dashboard-log-pane.component.html"
      to: "src/angular/src/app/pages/files/dashboard-log-pane.component.scss"
      via: "the spinner element class `ph ph-circle-notch ph-spin` is animated by the locally-added .ph-spin keyframe rule"
      pattern: "ph-circle-notch ph-spin"
    - from: "src/angular/src/app/pages/files/stats-strip.component.html"
      to: "src/angular/src/app/tests/unittests/pages/files/stats-strip.component.spec.ts"
      via: "the spec's querySelector('.stat-icon.ph-*') assertions match the migrated rendered classes"
      pattern: "stat-icon.ph-"
---

<objective>
Migrate every Font Awesome icon in the dashboard `files/` component cluster — dashboard-log-pane, stats-strip, transfer-row, transfer-table, bulk-actions-bar — to its mapped Phosphor class, add the net-new `.ph-spin` CSS animation rule the spinner needs once FA's CSS is gone, re-point the one `.fa-terminal` SCSS selector, and faithfully update the 3 affected unit specs (inline templates + DOM assertions per D-03). Runs in parallel with 105-03 (no shared files).

Purpose: D-05 puts all icon edit layers in scope; this plan owns the `files/` cluster (static `fa fa-*` classes, the one SCSS selector, the spinner special case, and the 3 specs that assert on those rendered classes). The `.ph-spin` rule is the single net-new code wrinkle — it MUST land here, before the FA dep drop in 105-04, or the spinner freezes. The 3 specs use `overrideTemplate` with inline template copies, so both the inline copies and the `querySelector(".fa-*")` assertions must move to Phosphor in the same change (RESEARCH §2e, Pitfall 7).
Output: 5 migrated templates, 1 migrated+augmented SCSS, 3 faithfully-updated specs, Karma green with floors held.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-CONTEXT.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-RESEARCH.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-PATTERNS.md
@.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md

<notes>
- Depends on 105-01: the ambiguous-icon sign-off must be complete before this plan applies the two ambiguous icons it owns — Q4 fa-file-archive-o (bulk-actions-bar:29 Extract button) and Q5 fa-circle-o-notch+fa-spin (dashboard-log-pane:18 spinner). Use the FINAL classes recorded on the DECISION lines in 105-MAPPING-SIGNOFF.md (proposed: ph-file-archive and ph-circle-notch + the approved .ph-spin rule). All other icons in this cluster are confident 1:1 mappings.
- PATTERN A (RESEARCH/PATTERNS): static `<i class="fa fa-{x}">` → `<i class="ph ph-{mapped-x}">`. The two-class idiom (weight-prefix `ph` + icon-name `ph-{name}`) is mandatory; never a single class. Analog: pages/about/about-page.component.html.
- D-02 (LOCKED): default to regular `ph` weight. Do NOT introduce new ph-bold/ph-fill weights this cluster didn't already have. (transfer-table already uses ph-bold for its caret toggle — leave that as-is; only the line-13 fa-search → ph-magnifying-glass swap is in scope there.)
- Exact per-line edits (PATTERNS per-file map + RESEARCH §2a):
  - dashboard-log-pane.component.html: L4 fa-terminal→ph-terminal; L8 fa-copy→ph-copy; L11 fa-expand→ph-arrows-out; L18 `fa fa-circle-o-notch fa-spin`→`ph ph-circle-notch ph-spin`.
  - stats-strip.component.html: L5,7 fa-cloud→ph-cloud; L46,48 fa-database→ph-database; L87,89 fa-arrow-down→ph-arrow-down; L101,103 fa-tasks→ph-list-checks (both watermark + stat-icon instances each).
  - transfer-row.component.html: L18 fa-arrow-down→ph-arrow-down.
  - transfer-table.component.html: L13 `fa fa-search search-icon`→`ph ph-magnifying-glass search-icon` (ONLY line 13; the rest already uses Phosphor).
  - bulk-actions-bar.component.html: L19 fa-play→ph-play; L24 fa-stop→ph-stop; L29 fa-file-archive-o→ph-file-archive (Q4 signed-off); L35 fa-trash→ph-trash; L40 fa-cloud→ph-cloud AND `fa fa-ban action-overlay`→`ph ph-prohibit action-overlay` (CORRECTED: ph-prohibit, NOT ph-ban — ph-ban does not exist).
- dashboard-log-pane.component.scss: PATTERN F1 — rename .fa-terminal (line 38) → .ph-terminal. PATTERN F6 — append the net-new spin rule at end of file: a `@keyframes ph-spin` (0deg→360deg rotate) + `.ph-spin { animation: ph-spin 1s linear infinite; display: inline-block; }`. The display:inline-block is required for transform to apply to inline <i>. This rule is what keeps the spinner rotating once font-awesome.css (which supplied fa-spin) is removed in 105-04.
- Spec updates (D-03 — RESEARCH §2e / PATTERNS Spec File Pattern). All 3 use overrideTemplate with inline template copies; update BOTH the inline copies AND the DOM assertions:
  - dashboard-log-pane.component.spec.ts: inline LOG_PANE_TEMPLATE L16 fa-terminal→ph-terminal, L19 fa-copy→ph-copy, L22 fa-expand→ph-arrows-out, L29 `fa fa-circle-o-notch fa-spin`→`ph ph-circle-notch ph-spin`. (No fa-* querySelector in this spec — the spinner test queries .log-pane__spinner, not the icon class.)
  - stats-strip.component.spec.ts: inline STATS_STRIP_TEMPLATE cloud/database/arrow-down/tasks → ph equivalents; DOM assertions L182 `.stat-icon.fa-cloud`→`.stat-icon.ph-cloud`, L190 `.stat-icon.fa-database`→`.stat-icon.ph-database`, L198 `.stat-icon.fa-arrow-down`→`.stat-icon.ph-arrow-down`, L206 `.stat-icon.fa-tasks`→`.stat-icon.ph-list-checks`, L242 `.stat-icon.fa-arrow-down`→`.stat-icon.ph-arrow-down`.
  - transfer-table.component.spec.ts: inline TEST_TEMPLATE L25 `fa fa-search search-icon`→`ph ph-magnifying-glass search-icon`; DOM assertion L202 `.fa-search`→`.ph-magnifying-glass`.
- The spec file path prefix is `src/angular/src/app/tests/unittests/...` (CONTEXT lists `tests/unittests/...`; confirm the on-disk path when reading — adjust the leading dirs to the actual repo layout).
- Run Karma FROM src/angular/. Floors stmts/branches/fns/lines 83/68/79/83 (karma.conf.js check.global) must hold or rise. Do NOT remove font-awesome from package.json/angular.json here — that is the closing act in 105-04 (the spinner must be validated against the FA-still-present build first; the .ph-spin rule is what makes it survive the later drop).
- No release/tag/version work (ROADMAP SC5).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the files-cluster templates + add the ph-spin CSS prerequisite + re-point the .fa-terminal selector</name>
  <files>src/angular/src/app/pages/files/dashboard-log-pane.component.html, src/angular/src/app/pages/files/dashboard-log-pane.component.scss, src/angular/src/app/pages/files/stats-strip.component.html, src/angular/src/app/pages/files/transfer-row.component.html, src/angular/src/app/pages/files/transfer-table.component.html, src/angular/src/app/pages/files/bulk-actions-bar.component.html</files>
  <read_first>
    - src/angular/src/app/pages/files/dashboard-log-pane.component.html (the file being modified)
    - src/angular/src/app/pages/files/dashboard-log-pane.component.scss (the file being modified — .fa-terminal selector + where to append the spin rule)
    - src/angular/src/app/pages/files/stats-strip.component.html (the file being modified)
    - src/angular/src/app/pages/files/transfer-row.component.html (the file being modified)
    - src/angular/src/app/pages/files/transfer-table.component.html (the file being modified)
    - src/angular/src/app/pages/files/bulk-actions-bar.component.html (the file being modified)
    - src/angular/src/app/pages/about/about-page.component.html (PATTERN A canonical analog: the in-repo `ph ph-{name}` two-class idiom)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-PATTERNS.md (PATTERN A, F1, F6 + the per-file edit tables for this cluster)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-MAPPING-SIGNOFF.md (the signed-off final classes for Q4 ph-file-archive and Q5 ph-circle-notch + approved .ph-spin rule)
  </read_first>
  <action>
    Apply PATTERN A to every static `fa fa-*` in the five templates, using the exact target classes from the per-file map (see notes): dashboard-log-pane (ph-terminal, ph-copy, ph-arrows-out, and the spinner `ph ph-circle-notch ph-spin`), stats-strip (ph-cloud, ph-database, ph-arrow-down, ph-list-checks — both the watermark and stat-icon instances on each line pair), transfer-row (ph-arrow-down), transfer-table line 13 only (ph-magnifying-glass, preserving the trailing `search-icon` utility class), and bulk-actions-bar (ph-play, ph-stop, ph-file-archive for Extract per the Q4 sign-off, ph-trash, ph-cloud, and the corrected `ph-prohibit action-overlay` for the Delete-Remote overlay — ph-prohibit NOT ph-ban). Keep all non-icon utility classes (e.g. search-icon, action-overlay, floating-save-icon--success) intact; only the `fa`/`fa-*` tokens change. Do NOT introduce ph-bold/ph-fill (D-02 regular weight); leave transfer-table's existing ph-bold caret block untouched.

    In dashboard-log-pane.component.scss: rename the `.fa-terminal` selector (PATTERN F1) to `.ph-terminal` so the amber terminal-icon color rule still applies. Then append the PATTERN F6 net-new spin rule at the END of the file: a `@keyframes ph-spin` rotating 0deg→360deg and a `.ph-spin` rule that sets `animation: ph-spin 1s linear infinite` and `display: inline-block`, with a one-line comment that Phosphor ships no spin class so this is provided locally. This rule must exist before the FA dep is dropped in 105-04, or the spinner freezes once font-awesome.css is gone.
  </action>
  <verify>
    <automated>cd src/angular && if grep -rIl "fa fa-\|\"fa-\|'fa-\|class=\"fa\b\|class='fa\b" src/app/pages/files/dashboard-log-pane.component.html src/app/pages/files/dashboard-log-pane.component.scss src/app/pages/files/stats-strip.component.html src/app/pages/files/transfer-row.component.html src/app/pages/files/transfer-table.component.html src/app/pages/files/bulk-actions-bar.component.html | grep .; then echo "FA-RESIDUAL-FOUND"; exit 1; fi; grep -q "ph-circle-notch ph-spin" src/app/pages/files/dashboard-log-pane.component.html && grep -q "@keyframes ph-spin" src/app/pages/files/dashboard-log-pane.component.scss && grep -q "ph-prohibit action-overlay" src/app/pages/files/bulk-actions-bar.component.html && echo "FILES-CLUSTER-MIGRATED"</automated>
  </verify>
  <acceptance_criteria>
    - Searching the five templates for the FA-specific patterns (`fa fa-`, `"fa-`, `'fa-`, `class="fa`, `class='fa`) returns zero matches (no residual fa- token; utility classes that merely contain the letters "fa" do not false-positive because the patterns are FA-class-shaped).
    - `dashboard-log-pane.component.html` contains `ph ph-circle-notch ph-spin` (spinner migrated).
    - `dashboard-log-pane.component.scss` contains `@keyframes ph-spin` and a `.ph-spin` rule with `animation: ph-spin` and `display: inline-block`, and contains `.ph-terminal` (and no `.fa-terminal`).
    - `bulk-actions-bar.component.html` contains `ph-prohibit action-overlay` (corrected mapping; NOT ph-ban) and `ph-file-archive` (Q4 sign-off; or the user's substitute).
    - `transfer-table.component.html` line 13 reads `ph ph-magnifying-glass search-icon` and the file's existing `ph-bold` caret block is unchanged.
    - The verify command prints `FILES-CLUSTER-MIGRATED` (and does NOT print `FA-RESIDUAL-FOUND`).
  </acceptance_criteria>
  <done>All five files-cluster templates render Phosphor icons (zero residual fa-*), the .ph-spin animation rule and the .ph-terminal selector rename are in dashboard-log-pane.component.scss, and the corrected ph-prohibit + signed-off ph-file-archive classes are applied.</done>
</task>

<task type="auto">
  <name>Task 2: Faithfully update the 3 files-cluster unit specs (inline templates + DOM assertions) and confirm Karma floors hold</name>
  <files>src/angular/src/app/tests/unittests/pages/files/dashboard-log-pane.component.spec.ts, src/angular/src/app/tests/unittests/pages/files/stats-strip.component.spec.ts, src/angular/src/app/tests/unittests/pages/files/transfer-table.component.spec.ts</files>
  <read_first>
    - src/angular/src/app/tests/unittests/pages/files/dashboard-log-pane.component.spec.ts (the file being modified — inline LOG_PANE_TEMPLATE)
    - src/angular/src/app/tests/unittests/pages/files/stats-strip.component.spec.ts (the file being modified — inline STATS_STRIP_TEMPLATE + the .stat-icon.fa-* querySelector assertions)
    - src/angular/src/app/tests/unittests/pages/files/transfer-table.component.spec.ts (the file being modified — inline TEST_TEMPLATE + the .fa-search querySelector)
    - src/angular/src/app/pages/files/stats-strip.component.html (the migrated real template — cross-check the inline spec copy matches it post-migration)
    - .planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-PATTERNS.md (Spec File Pattern — exact before/after for these specs)
    - src/angular/karma.conf.js (confirm check.global floors 83/68/79/83)
  </read_first>
  <action>
    Per D-03 (faithful update — no loosening to library-agnostic assertions, no new data-testid hooks): update each spec's `overrideTemplate` inline template copy AND its DOM querySelector assertions to the exact Phosphor classes the real components now render.
    - dashboard-log-pane.component.spec.ts: in the inline LOG_PANE_TEMPLATE, change fa-terminal→ph-terminal, fa-copy→ph-copy, fa-expand→ph-arrows-out, and the spinner `fa fa-circle-o-notch fa-spin`→`ph ph-circle-notch ph-spin`. (No fa-* querySelector to change — the spinner test queries `.log-pane__spinner`.)
    - stats-strip.component.spec.ts: in the inline STATS_STRIP_TEMPLATE, migrate cloud/database/arrow-down/tasks to ph equivalents (ph-cloud, ph-database, ph-arrow-down, ph-list-checks); then update the DOM assertions to `.stat-icon.ph-cloud`, `.stat-icon.ph-database`, `.stat-icon.ph-arrow-down`, `.stat-icon.ph-list-checks` (the two arrow-down assertions both become ph-arrow-down).
    - transfer-table.component.spec.ts: in the inline TEST_TEMPLATE change `fa fa-search search-icon`→`ph ph-magnifying-glass search-icon`; update the DOM assertion `querySelector(".fa-search")`→`querySelector(".ph-magnifying-glass")`.
    Then run the Karma suite FROM src/angular/ and confirm it passes with check.global floors (83/68/79/83) held or risen. Record the four measured percentages in the summary. If Chrome is unavailable locally, note CI `unittests-angular` (amd64+arm64) is the authoritative check.
  </action>
  <verify>
    <automated>cd src/angular && ! grep -rn "fa-search\|fa-cloud\|fa-database\|fa-arrow-down\|fa-tasks\|fa-terminal\|fa-copy\|fa-expand\|fa-circle-o-notch\|fa-spin" src/app/tests/unittests/pages/files/dashboard-log-pane.component.spec.ts src/app/tests/unittests/pages/files/stats-strip.component.spec.ts src/app/tests/unittests/pages/files/transfer-table.component.spec.ts && ng test --browsers ChromeHeadless --watch=false --code-coverage</automated>
  </verify>
  <acceptance_criteria>
    - The 3 spec files contain zero `fa-*` icon tokens (the grep for fa-search/fa-cloud/fa-database/fa-arrow-down/fa-tasks/fa-terminal/fa-copy/fa-expand/fa-circle-o-notch/fa-spin returns no match).
    - Each spec's inline overrideTemplate copy matches its migrated real component template (ph classes), and the stats-strip + transfer-table DOM querySelector assertions target the new `.ph-*` classes (faithful per D-03 — no library-agnostic loosening, no new data-testid).
    - `ng test --browsers ChromeHeadless --watch=false --code-coverage` exits 0 with check.global floors stmts/branches/fns/lines each ≥ 83/68/79/83 (the four measured percentages recorded in the summary). If Chrome is unavailable locally, the summary notes CI unittests-angular as authoritative.
  </acceptance_criteria>
  <done>The 3 files-cluster specs faithfully assert the new Phosphor classes (inline templates + DOM selectors), and the full Karma suite is green with all four coverage floors held or risen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Icon-class CSS/font swap in static templates. No auth, no user input, no data flow, no injection sink. The fa-* / ph-* classes are compile-time constants on static `<i>` elements; no user-controlled data reaches the changed code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-02 | (n/a) | files-cluster icon templates + spin CSS | accept | No new threat surface: icon-library class swap on static elements. The added `.ph-spin` rule is a pure CSS keyframe animation with no scripting. FA CSS/font removal (later, 105-04) reduces shipped attack surface. RESEARCH Security Domain: ASVS V2/V3/V4/V5/V6 do not apply. |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase (Phosphor already present; font-awesome removed in 105-04). RESEARCH Package Legitimacy Audit = N/A. No [ASSUMED]/[SUS]/[SLOP] packages, so no blocking-human legitimacy checkpoint applies. |
</threat_model>

<verification>
- The five files-cluster templates contain zero residual fa-* tokens; every former FA icon renders via the `ph ph-{name}` idiom.
- The spinner uses `ph ph-circle-notch ph-spin`, and dashboard-log-pane.component.scss has the `@keyframes ph-spin` + `.ph-spin` rule (so the spinner survives the later FA-CSS removal) plus the `.fa-terminal`→`.ph-terminal` rename.
- bulk-actions-bar uses the corrected `ph-prohibit action-overlay` (NOT ph-ban) and the signed-off `ph-file-archive` Extract icon.
- The 3 specs faithfully assert the new Phosphor classes (inline templates + DOM selectors per D-03); Karma green with floors 83/68/79/83 held or risen.
- font-awesome is NOT yet removed from package.json/angular.json (that is 105-04); the build still includes FA at this stage.
- No release/tag/version work (ROADMAP SC5).
</verification>

<success_criteria>
- DEPS-01b (files cluster): every fa-* in the five files-cluster templates replaced with its mapped Phosphor class; zero residual fa-*; regular ph weight per D-02.
- The spinner animation is preserved by the local .ph-spin rule (the one net-new code wrinkle) — landed before the FA drop.
- The 3 affected specs updated faithfully per D-03; Karma floors hold.
</success_criteria>

<output>
Create `.planning/milestones/v1.3.0-phases/105-font-awesome-to-phosphor-migration/105-02-SUMMARY.md` when done.
</output>
