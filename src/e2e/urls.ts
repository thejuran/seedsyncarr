// Base URL is configured in playwright.config.ts
// These paths are relative to the baseURL
export const Paths = {
  DASHBOARD: '/dashboard',
  SETTINGS: '/settings',

  ABOUT: '/about',
  LOGS: '/logs',
} as const;